# ⚠️ ไฟล์นี้ไม่ใช่กติกา — กติกาอยู่ที่ทรีหลัก

**กติกาของทีมมีฉบับเดียว และมันอยู่ที่:**

```
/Users/park/plan-korea/TEAM.md
```

`CLAUDE.md` ของทรีนี้ import ไฟล์นั้นด้วยพาธเต็มอยู่แล้ว → **ปกติคุณจะได้เนื้อหาครบโดยไม่ต้องทำอะไร**
ถ้าไม่เห็นเนื้อหานั้นในบริบทของคุณ **ให้เปิดอ่านไฟล์ข้างบนก่อนเริ่มงาน**

---

## ทำไมไฟล์นี้ถึงว่าง (P6 เจอ · P1 ตัดสิน · 28 ส.ค. 2026)

เดิมทรีนี้มี `TEAM.md` ฉบับเต็มของตัวเอง แล้วมันหยุดนิ่งขณะที่ฉบับบน `main` เดินหน้าต่อ

```
25 ส.ค.  ฉบับนี้สั้นกว่า main   64 บรรทัด
28 ส.ค.  ฉบับนี้สั้นกว่า main  274 บรรทัด   ← main 391 · ที่นี่ 117
```

สิ่งที่หายไปจากฉบับเก่าของทรีนี้ ไม่ใช่รายละเอียดปลีกย่อย:

- **`D72`** ทั้งข้อ — กติกาการ push · ท่าปักหมุด SHA · การรายงานสถานะ git
- **ข้อยกเว้น `service_role` ข้อที่ 2–6** — ทั้งที่ migration ที่ใช้ข้อยกเว้นพวกนั้น**อยู่ในทรีนี้เอง**
- **checklist ของ CI ทั้งอัน** — `grep "npm run lint"` ในฉบับเก่าได้ **0**

🎯 **และช่องนี้เปิดกับคนที่ทำตามกติกาเป๊ะ** — `TEAM.md` สั่งให้ทำงานแพลตฟอร์มในทรีนี้
ใครทำตาม จะได้กติกาฉบับที่ไม่ครบ **โดยไม่มีอะไรบอกว่ามันไม่ครบ**

**ทางที่เลือก: ให้เอกสารที่มีเนื้อมีใบเดียวในโลก** — drift เกิดไม่ได้ตามนิยาม
ไม่ใช่ "มีสองใบแล้วเราจะคอย sync" ซึ่งเป็นสิ่งที่ลองแล้วและนี่คือผลของมัน
