# ทีมของโปรเจกต์นี้ — อ่านก่อนเริ่มงานทุกครั้ง

โปรเจกต์ `plan-korea` ทำงานเป็นทีม **8 เซสชัน** ที่**แชร์ working tree เดียวกัน**
🔴 **ตั้งแต่ 18 ส.ค. 2026 มี 2 ทรี:** ทรีหลักอยู่ที่ `main` (เว็บทริป · freeze) · งานแพลตฟอร์มอยู่ใน `git worktree` แยกบน branch `platform`
ถ้าคุณกำลังอ่านไฟล์นี้ คุณเป็นหนึ่งใน 8 คนนี้ — **หน้าที่แรกของคุณคือรู้ว่าตัวเองคือใคร**

> **ทีมขยายจาก 4 เป็น 8 เมื่อ 17 ส.ค. 2026** ตอนเริ่มงาน Dynamic Travel Platform Engine
> P1–P4 คือทีมเดิมที่ดูแลเว็บทริปเกาหลี · P5–P8 เป็นบทบาทใหม่สำหรับงานแพลตฟอร์ม

---

## 📚 เรื่องเล่าย้ายไปแล้ว — [`docs/team-incidents.md`](docs/team-incidents.md)

**ไฟล์นี้คือ *กติกาที่ต้องทำตาม* · เหตุการณ์ที่ทำให้แต่ละข้อเกิดขึ้นอยู่ที่ไฟล์นั้น**

> 🔴 **แยกเมื่อ 4 ก.ย. 2026 ตามที่ผู้ใช้สั่ง — และเหตุผลคือตัวเลข:**
> `TEAM.md` โตจาก **626 → 1,069 บรรทัดใน 2 วัน** (21 commit วันเดียว)
> 🎯 ***ไฟล์ที่ยาวจนไม่มีใครอ่านจบ ทำให้ทุกข้อในนั้นอ่อนลง รวมข้อที่ดีที่สุด***
> · ตอนนี้ **388 บรรทัด** · **ไม่มีเนื้อหาหายแม้แต่บรรทัดเดียว** (ตรวจแล้ว: 831 บรรทัดที่มีเนื้อ ยังอยู่ครบ)

**เส้นแบ่งที่ใช้ (P3 เสนอ · P1 รับ):**
```
เขียนเป็น กติกา          → ไม่มีวันหมดอายุ · ตายพร้อมโค้ดที่มันพูดถึง   → อยู่ที่นี่
เขียนเป็น คำบรรยายสภาพ   → หมดอายุเสมอ · ไม่มีใครกลับมาดู              → อยู่ที่ team-incidents.md
```
· 🔴 **ประโยคที่เป็น *สถานะของสิ่งอื่น ณ วันนั้น* เป็นหลักฐานประกอบได้ แต่ห้ามเป็นตัวข้อ**
· 🔴 **ห้ามใส่เรื่องเล่ากลับเข้าไฟล์นี้** — เพิ่มที่ `team-incidents.md` แล้วลิงก์มา
· 🎯 **เรื่องเล่าไม่ถูกลบเพราะมันคือสิ่งที่ทำให้กติกาถูกเชื่อ** (P3) —
  ตัดทิ้งแล้วเหลือแต่คำสั่งที่ไม่มีใครรู้ว่ามาจากไหน แล้ววันหนึ่งจะมีคนลบมันเพราะ *"ดูไม่จำเป็น"*

---

## 1. หาให้ได้ก่อนว่าคุณคือใคร

ทำทันทีที่เริ่มเซสชันใหม่ (รวมถึงหลัง `/clear`) ถ้ายังไม่มีใครบอกว่าคุณคือบทบาทไหน:

```
mcp__ccd_session_mgmt__list_sessions
```

**เครื่องมือนี้ไม่แสดงเซสชันของตัวเอง** — นั่นคือกลไกทั้งหมด

1. กรองเอาเฉพาะแถวที่ `cwd` เป็น `/Users/park/plan-korea`
2. จะเห็นชื่อขึ้นต้นด้วย `[P1-Lead]` `[P2-UI/UX]` `[P3-FE/Perf]` `[P4-QA/Sec]`
   `[P5-AI/Agent]` `[P6-DevOps]` `[P7-Mobile]` `[P8-PM/BA]` **แค่ 7 อัน**
3. **บทบาทที่หายไปคือคุณ**

ตัวอย่างจริง (17 ส.ค. 2026): เห็น P2, P3, P4, P5, P6, P7, P8 → เซสชันที่รันอยู่คือ **P1-Lead**

**ข้อควรระวัง**
- ⚠️ **มีทีมคู่ขนานที่ใช้ชื่อ P1-P8 ทับกัน** ที่ `~/mu-phone` และ `~/Cluade` —
  **ต้องกรอง `cwd` ก่อนเสมอ** ไม่งั้นจะนับผิดแล้วสรุปบทบาทผิด และอาจสั่งงานข้ามโปรเจกต์
  ⚠️ **เลข P เดียวกันคนละบทบาทข้ามโปรเจกต์** — ที่นี่ `P6` คือ DevOps แต่ที่ `~/mu-phone`/`~/Cluade`
  `P6` คือ Backend, `P7` คือ Frontend, `P5` คือ DevOps/Infra · **ห้ามจับคู่ด้วยเลข ต้องอ่านชื่อเต็ม**
- ถ้าเห็นครบ 8 บทบาททั้งที่กรอง cwd แล้ว = มีเซสชันซ้ำบทบาท **อย่าเดา ให้ถามผู้ใช้**
- ถ้าเห็นน้อยกว่า 7 = เซสชันอื่นถูกปิด/archive ไปแล้ว **อย่าเดา ให้ถามผู้ใช้**
- วิธีนี้บอกได้แค่ "บทบาทไหนว่างอยู่" ไม่ได้ยืนยันตัวตน — **ถ้าไม่ชัดเจน ถามผู้ใช้เป็นคำแรก**
  อย่าเริ่มแก้ไฟล์ในโซนของคนอื่นด้วยการเดา

---

## 2. บทบาทและโซนที่รับผิดชอบ

| บทบาท | ขอบเขต | ไฟล์หลัก |
|---|---|---|
| **P1-Lead** — System Architecture & Core API | สถาปัตยกรรม · Supabase · migration · เป็นคนรวมงาน ตัดสินใจ และกระจายบั๊กให้เจ้าของโซน | `lib/` · `app/api/` · `supabase/migrations/` · `PLAN.md` |
| **P2-UI/UX** — Core Page Layouts & User Flow | เลย์เอาต์ · โทเคนสี/ธีมมืด · responsive · a11y | `components/` · `app/globals.css` |
| **P3-FE/Perf** — Dynamic Routing & Web Performance | routing · แคช · bundle · service worker/PWA | `app/` (routing) · `next.config.ts` · `public/sw.js` |
| **P4-QA/Sec** — Attack Simulation & End-to-End QA | regression · e2e ในเบราว์เซอร์ · PIN gate · RLS · **หาและรายงาน ไม่แก้เอง** เว้นแต่ P1 สั่ง | `lib/__tests__/` · `proxy.ts` |
| **P5-AI/Agent** — Travel Copilot & Logic Prompting | system prompt · tool/function schema · RAG · ตรรกะ optimize เส้นทาง/เวลา | `docs/engine/copilot-spec.md` |
| **P6-DevOps** — CI/CD Pipeline & Cloud Deployment | GitHub Actions · Supabase คลาวด์ org ใหม่ (D14) · env · monitoring · rate limit | `docs/engine/devops.md` · `.github/` |
| **P7-Mobile** — Cross-Platform App Development | Expo/React Native · offline sync + conflict model · push | `docs/engine/mobile-arch.md` |
| **P8-PM/BA** — Project Management & Requirement Analyst | user story · acceptance criteria · release roadmap | `docs/engine/backlog.md` |

**P1 เป็นหัวหน้า** — ตัดสินใจเรื่องที่คร่อมหลายโซน รวมผลจากทุกคน
คนอื่นเจอของนอกโซนตัวเอง **ให้รายงานกลับ P1 ไม่แก้ข้ามโซนเอง**

🔴 **เส้นแบ่งการคุยกับผู้ใช้ (ชัดเจนเมื่อ 18 ส.ค. 2026 — P5 แย้งว่าฉบับเดิมกว้างเกิน และถูก):**

| เรื่อง | ใครพูดกับผู้ใช้ |
|---|---|
| **งานในโซนตัวเอง** · ผู้ใช้ถามตรง | **เจ้าของโซนตอบเอง** — เป็นหน้าที่พื้นฐาน มอบต่อไม่ได้ |
| **สถานะรวมของทีม** · เรื่องข้ามโซน · สรุปที่อ้างงานคนอื่น | **ผ่าน P1 เท่านั้น** |

**เหตุผลของฝั่งขวา:** P1 เป็นคนเดียวที่รู้ว่าผู้ใช้**ได้รับอะไรไปแล้วบ้าง** — ไม่ใช่เรื่องอำนาจ
· พลาดมาแล้ว 2 ครั้งใน 1 วัน (17–18 ส.ค.): ผู้ใช้ได้รายการที่ล้าสมัย 1 ครั้ง และได้ข้อความซ้ำเรื่องเดียวกันจาก 2 คน 1 ครั้ง
⚠️ **ฉบับเดิมเขียนแค่ "P1 เป็นคนเดียวที่สรุปให้ผู้ใช้" ซึ่งถูกอ่านได้ว่าห้ามคนอื่นพูดกับผู้ใช้เลย** — ไม่ใช่เจตนา และเป็นตัวอย่างของ `D34`

🔴 **แก้ 18 ส.ค. 2026 — ระยะออกแบบจบแล้ว ผู้ใช้สั่งเริ่มสร้างทันที ไม่รอหลังทริป**
โซนในตารางด้านบนใช้กับ **branch `platform` ใน worktree แยก** ได้เต็มที่แล้ว เขียนโค้ดจริงได้
· **แต่บน `main` ทุกคนยังแตะโค้ดแอปไม่ได้เหมือนเดิม** เว้นบั๊กที่ทำให้ใช้งานไม่ได้ + เนื้อหาทริป
· ⚠️ **ประโยคเดิมที่เขียนว่า "P5–P8 ยังไม่มีสิทธิ์แตะโค้ดแอปจนกว่าทริปจะจบ" หมดอายุแล้ว ห้ามอ้างอิง**
· ดู `docs/engine/README.md` กติกาเหล็กฉบับระยะสร้าง

---

## 3. กติกาเพราะแชร์ working tree เดียวกัน


### 3.1 ก่อนแตะไฟล์

- 🔴 **มี 2 ทรีตั้งแต่ 18 ส.ค. 2026 — ขึ้นต้นทุกคำสั่งด้วย `cd <พาธเต็ม>` เสมอ ห้ามพึ่ง cwd ที่ค้างจากคำสั่งก่อน**
  เชลล์ในเซสชันพวกนี้ **รีเซ็ต cwd กลับ `/Users/park/plan-korea` เองหลังทุกคำสั่ง** (P6 เจอ 18 ส.ค. 2026)
  → ใครพึ่ง `cd` ค้าง **จะไปแก้ทรีหลักโดยไม่รู้ตัว** · `lib/` มีทั้งสองทรี ชื่อไฟล์เหมือนกันหมด
  ⚠️ **`git status` ไม่ฟ้องข้อนี้เลย** — มันขึ้นปกติในทรีที่คุณแก้ ไม่มีอะไรบอกว่าคุณอยู่ผิดที่
  · เช็ค **`git branch --show-current`** ก่อนแก้โค้ด ไม่ใช่แค่ `git status` · อ้าง **path เต็ม**เสมอเวลาคุยข้ามเซสชัน
  · 🔴 **ห้ามจำว่า "แก้แล้ว" โดยไม่ระบุว่าแก้ทรีไหน** (P8 ชี้)
- **`git status` ก่อนแตะไฟล์เสมอ** — ของที่ค้าง `M` อยู่อาจเป็นงานที่คนอื่นกำลังพิมพ์อยู่
- **ห้าม `git checkout --` / `git stash` / `git reset --hard`** ทับงานคนอื่นเด็ดขาด
- 🔴 **`git reset --soft` ก็อันตรายบน branch ที่ใช้ร่วมกัน — และมันไม่อยู่ในรายการข้างบนเพราะมัน *ดู* ไม่อันตราย** (P4 เจอ · P1 ตรวจ · 27 ส.ค. 2026)
  `reset --soft` **ไม่แตะไฟล์เลยสักไฟล์** จึงถูกอ่านว่า non-destructive — **ถูกสำหรับ *ไฟล์* · ผิดสำหรับ *ประวัติ***
  🎯 **`HEAD^` แปลว่า "commit ก่อนหน้าของ *branch*" ไม่ใช่ "commit ก่อนหน้าของ *คุณ*"**
  ```
  คุณ commit ผิด                    ← ตอนนี้คุณเป็น tip
     ⏳ ← คนอื่น commit ตรงนี้ (เกิดขึ้นได้ทุกวินาที ไม่มีอะไรกัน)
  คุณ reset --soft HEAD^            ← HEAD^ กลายเป็น commit ของ *เขา*
                                       → **commit ของเขาหลุดออกจาก branch**
  ```
  ⚠️ **และมันเงียบสนิท** — `reset --soft` ไม่แตะ working tree → **`git status` ขึ้นปกติทุกอย่าง**
  ไฟล์ของเขายังอยู่บนดิสก์ แต่หลุดจากประวัติ · **ไม่มีอะไรฟ้องจนกว่าจะมีคน `git log` หาของตัวเอง**
  · 🔴 **"ผมเป็น tip อยู่" ตรวจจากที่นั่งไม่ได้** — มันจริงตอนคุณพิมพ์ และเท็จตอนคุณกด Enter ได้

  **✅ ท่าที่ใช้แทน — ไม่แตะประวัติเลย:**
  ```
  git commit -m "ถอนไฟล์ที่กวาดมาโดยไม่ตั้งใจ" -- <ไฟล์ที่ไม่ควรอยู่>
  ```
  **ประวัติที่มีรอยแก้ ดีกว่าประวัติที่สะอาดเพราะมีคนลบรอย** — โดยเฉพาะที่ที่ 8 คนใช้ร่วมกัน
  · 📌 ต้นเรื่อง: P4 ใช้ `git commit -F msg` **ไม่มี pathspec** → กวาด `git mv` 5 ไฟล์ของ P1 ที่ staged ค้างเข้า commit
    แล้วแก้ด้วย `reset --soft HEAD^` · **ผลลัพธ์สะอาด ไม่มีอะไรหาย (P1 ตรวจแล้ว)**
    🎯 **แต่รอดเพราะจังหวะ ไม่ใช่เพราะท่าปลอดภัย** — P1 commit หลังเขา reset เสร็จพอดี

### 3.2 ตอน commit

- **commit เฉพาะไฟล์ของงานตัวเอง** อย่า `git add -A`
- 🔴 **commit ด้วย `git commit -- <path>` เสมอ ไม่ใช่ `git add` แล้ว `git commit`** (P8 เจอ 17 ส.ค. 2026)
  **`index` เป็นของกลาง** — คนอื่นวาง staged ค้างไว้ได้ตลอด `git commit` ธรรมดาจะกวาดของเขาเข้า commit คุณ
  ⚠️ **กติกาข้อบนพูดถึงสิ่งที่*คุณเพิ่มเข้า* index · ไม่ได้พูดถึงสิ่งที่*มีอยู่แล้ว*ในนั้น** — ทำตามครบทุกตัวอักษรแล้วยังพลาดได้
  และ **`git status` ก่อนแตะไฟล์ไม่ได้กันข้อนี้ เพราะปัญหาอยู่ตอน commit ไม่ใช่ตอนแก้ไฟล์**
  · ไม่มีสัญญาณอะไรเลยจนกว่าจะมีคนเปิด `git show` ดู
- 🔴 **`git commit -- <path>` กันของที่ *stage ไว้แล้ว* ไม่ได้กันของที่ *แก้ค้างอยู่บนดิสก์ที่ path เดียวกัน*** (P8 เจอ 25 ส.ค. 2026)
  P8 กำลังแก้ `docs/engine/README.md` ค้างไว้ยังไม่ commit · P1 รัน `git commit -- docs/engine/README.md`
  → **งานของ P8 ถูกกวาดเข้า commit `3d4f420` ของ P1 ทั้งก้อน โดยที่ข้อความ commit ไม่ได้พูดถึงเลย**
  (ตรวจแล้วเนื้อหาไม่เสียหาย — แต่ประวัติบอกว่าคนที่ไม่ได้เขียนเป็นคนเขียน)
  ⚠️ **กติกาข้อบนอ้างว่ากันเรื่องนี้ และมันกันไม่ได้** — `git commit -- <path>` **stage สิ่งที่อยู่บนดิสก์ ณ path นั้น แล้ว commit**
  ทรีเป็นของกลาง จึงรวมของที่คนอื่นเพิ่งพิมพ์ลงไปด้วยเสมอ · **`git status` ก่อนแตะไฟล์ไม่ช่วย เพราะเขาแก้ทีหลัง**
  **ทางลดที่ใช้ได้จริง — ตรวจ *เนื้อ diff* ไม่ใช่ตรวจว่ามีอะไรค้าง:**
  ```
  git diff -- <path>      # ทุก hunk ต้องเป็นของคุณ ถ้ามีอันที่จำไม่ได้ = ของคนอื่น หยุด
  ```
  · 🔴 **และ `git commit -- <path>` เพิ่ม *ไฟล์ใหม่* เข้า commit ไม่ได้เลย** (P1 เจอ 25 ส.ค. 2026)
    ไฟล์ที่ยัง untracked จะได้ `error: pathspec … did not match any file(s) known to git` **ทั้งคำสั่งล้ม ไม่มีอะไรถูก commit**
    → ต้อง `git add -- <พาธเต็มทีละไฟล์>` ก่อน **แล้วค่อย `git commit -- <พาธเดิม>`**
    ⚠️ **`git add` ที่ระบุพาธชัดเจนไม่ขัดกติกา "ห้าม `git add -A`"** — ของที่ห้ามคือการกวาดทั้งทรี
    · เช็ค `git diff --cached --name-only` หลัง `add` ทุกครั้ง: ต้องเห็นเฉพาะไฟล์ที่คุณเพิ่งพิมพ์ชื่อ
  · และถ้า diff **ว่างผิดปกติ** ให้สงสัยว่า *"มีคนทำไปแล้ว"* ไม่ใช่ *"ไม่มีอะไรให้ทำ"* (P8 เสนอ)
  · 🔴 **และ *"ตรวจเนื้อ diff"* ข้างบน **กันไม่ได้ตามนิยาม** — ช่องอยู่ระหว่าง *ตอนอ่าน* กับ *ตอนกด*** (P7 โดนเอง แล้ววัด · 4 ก.ย. 2026)
    P7 อ่าน `git diff -- TEAM.md` ได้ **10 บรรทัด ของตัวเองล้วน** · กด · ได้ **32 บรรทัด** — P2 เขียนกติกาของเขา
    ลงไฟล์เดียวกันระหว่างสองขั้นนั้น (`eb76bfd` · เนื้อไม่เสียหาย · P2 ลงใบตามหลัง `5e99e08` ไว้เป็นรอย)
    🎯 ***`git commit -- <path>` stage สิ่งที่อยู่บนดิสก์ **ณ ตอนกด** ไม่ใช่สิ่งที่คุณเพิ่งอ่าน***
    ⇒ ตรวจก่อนกดละเอียดแค่ไหนก็ไม่ปิด · **ตัวที่ตรวจได้จริงคือ *หลัง* กด — ใช้คู่กับการอ่าน diff ไม่ใช่แทนกัน:**
    ```
    git diff --stat -- <path>          # ก่อนกด — ให้เครื่องมือพิมพ์เลขออกจอ
    git show --stat --oneline HEAD     # หลังกด — ไม่ตรง = มีคนเขียนแทรก **บอกเจ้าของทันที**
    ```
    · ⚠️ **มันตรวจ *หลัง* ความเสียหายเกิดแล้ว — กันไม่ได้ แค่ทำให้รู้ตัว** (ซึ่งยังดีกว่าไม่รู้ · รอบที่ P7 โดน จับได้ด้วยท่านี้)
    · 🔴 **ห้ามจำเลขก่อนกดไว้ในหัว** — `§3.4` เตือนเองว่าเลขที่คนถือในหัว วางข้างผลของเครื่องมือแล้วแยกไม่ออก
  · 📌 **สองท่าที่ *วัดแล้ว* ว่าปิดช่องนี้ได้ — จดเป็น *ทางพร้อมราคา* ไม่ใช่คำสั่ง · อย่าเปลี่ยนเป็นค่าเริ่มต้น**
    ```
    B  git add -- <p> ; git commit           กันดิสก์แทรกได้ · 🔴 กวาด index ของคนอื่น (บรรทัด 135 ห้ามไว้แล้ว)
                                             วัดแล้ว: คนอื่น `git add` หลังคุณ add → ไฟล์เขาเข้าใบคุณ
    D  GIT_INDEX_FILE ส่วนตัว + add + commit   ปิดได้ทั้งสองช่อง · 🔴 ลืม `git reset -q -- <path>` ต่อท้าย
                                             = index กลางค้างของเก่า → คนถัดไป commit แล้ว **ย้อนงานคุณเงียบ ๆ**
    ```
    🎯 ***B ไม่ได้ปิดช่อง มันย้ายช่อง — จาก "บรรทัดในไฟล์เดียวกัน" ไปเป็น "ไฟล์ทั้งใบของคนอื่น" ซึ่งใหญ่กว่า***
    (ด่าน `git diff --cached --name-only` มีช่อง *อ่านแล้วกด* อันเดียวกับที่กำลังแก้อยู่ — ย้ายที่ ไม่ได้ปิด)
    🔴 **และ D ลงโทษ *คนถัดไปที่ทำถูกทุกอย่าง* เมื่อคุณลืมขั้นสุดท้าย** — `§3.4` บอกว่าห้ามสร้างกลไกแบบนั้น
    · ⚠️ **ขอบเขตหลักฐาน: รีโปเปล่า · P1 ยิง 2 เคส (ข้อเสนอเขาถูกหักด้วยเคสที่ 3) · P7 ยิง 6 เคส · รวม 8 · ยังไม่มีคนที่สาม**
      **ยังไม่ได้ลอง**: merge conflict · submodule · ไฟล์ถูกลบ · `core.fsmonitor`
    · เจ้าของกติกาเดิม (บรรทัด 135 · 17 ส.ค.) = **P8** · ข้อนี้ **P7** วัดและเสนอ · **P1** ยิงยืนยันแล้วถอนข้อเสนอตัวเอง · **P2** เจอจากฝั่งที่ถูกกวาด
  🎯 **ต่อกับ `D72` โดยตรง: `commit` กวาดของคนอื่นเข้าประวัติ · `push` กวาดของคนอื่นขึ้น remote — คนละชั้น รากเดียวกัน**

### 3.3 ตอน push — `D72`

> 📚 **เรื่องเล่าทั้งหมดของหัวข้อนี้ (390 บรรทัด · ทุกเคสที่กัดจริง พร้อมตัวเลข) อยู่ที่
> [`docs/team-incidents.md`](docs/team-incidents.md) หัวข้อ `3.3`** — อ่านเมื่อจะแก้กติกาข้อไหน หรือไม่เชื่อว่ามันจำเป็น

🔴 **`git push` ดัน branch ทั้งอัน ไม่ใช่แค่ commit ของคุณ · ทุก commit มี author เดียวกันหมด — git แยกไม่ออกว่าใครเป็นเจ้าของ**

**ตัดสิน (`D72`): push เป็น *การกระทำของทีม* — และเราจะ push ต่อไป ไม่ใช่หยุด push**

1. **commit ลง branch ที่ใช้ร่วมกัน = ประกาศว่าพร้อมขึ้น remote** · ยังไม่พร้อมก็ยังไม่ต้อง commit
2. **คน push รับผิดชอบว่า *หัว branch ทั้งหัว* เขียว** — ไม่ใช่แค่ของตัวเอง
   · ของคนอื่นแดง → **หยุด · บอกเจ้าของ · อย่า push ทับ · อย่าแก้ให้เขา**
   · 🔴 ***"ไม่ใช่ของผม" ตอบคำถาม "ใครแก้" ไม่ได้ตอบ "ใครบอก"*** — ต้องส่งถึงเจ้าของจริง
3. **บอก P1 ว่า push ถึง commit ไหน**
4. **รายงานสถานะ git ต้องมีปลายทาง และนับสด**
   ```
   ❌  main · HEAD 7614915 · unpushed 19
   ✅  main · 7614915 → origin/main fc42aa2 · unpushed 18
   นับด้วย  git log --oneline <upstream>..HEAD | wc -l    ← ห้ามบวกต่อจากเลขเดิมในหัว
   ```
   🔴 **ตรวจ *ทั้งช่วง* ด้วย `git log <ปลายทาง>..HEAD` แล้วอ่านทุกบรรทัด — `git show HEAD` เห็นแค่ยอด** (P2 จับ 4 ก.ย. 2026)
   ```
   ❌  git show --stat HEAD           เห็น commit เดียว → รายงานว่า "แตะไฟล์เดียว" ทั้งที่ในช่วงมี 3 commit
   ✅  git log --oneline <ปลายทาง>..HEAD   แล้วอ่านทุกบรรทัด
       git diff --stat <ปลายทาง>..HEAD    ดูไฟล์รวมของทั้งช่วง
   ```
   🎯 **`D72` ข้อ 2 บอกให้รับผิดชอบ *ทั้งหัว* · ข้อ 4 บอกให้ *นับสด* — ไม่มีข้อไหนบอกให้ *อ่านทุกบรรทัด***
   ⇒ นับถูก (2 → จริง ๆ 3 ก็ยังนับพลาดได้) แล้วเล่าเนื้อจากที่จำได้ **ผ่านทั้งสองข้อโดยยังรายงานผิด**
   · 📌 เกิดกับ P1 ที่เป็นคนเขียนกฎ **ในวันเดียวกับที่เขียน** — รายงานว่า push 2 commit ของ P2 ใบเดียว
     ทั้งที่มี 3 commit และของ P2 มีสองใบ · **ไม่มีอะไรเสียหาย แต่คำว่า "ผมตรวจของคุณแล้ว" กลายเป็นเท็จ**
   🔴 **และ *"ไม่อยู่ในรายการ unpushed"* ≠ *"ยังไม่ได้ commit"*** — ถามว่าก้อนนั้นขึ้นหรือยังด้วย
   `git merge-base --is-ancestor <sha> <upstream>` · **รายการ unpushed หมดอายุเงียบทุกครั้งที่มีคน push**

#### ✅ ท่าที่ใช้ — ปักหมุดแล้ว push *ก้อนที่ยืนยัน* ไม่ใช่ *หัว ณ ตอนกด*
```bash
git worktree remove --force "$WT" 2>/dev/null; git worktree prune   # เก็บใบเก่าก่อนเสมอ
git worktree add --detach "$WT" HEAD
cp -al <ทรีจริง>/node_modules "$WT/node_modules"   # symlink ใช้กับ build ไม่ได้
cp .env.local "$WT/.env.local"                     # ลืมแล้วชุดสดถูกข้ามแล้วอ่านเป็นเขียว
# …ยืนยันทุกด่านในนั้น…
git -C "$WT" push origin HEAD:<branch>             # HEAD ที่นี่ re-derive ไม่ได้ = หมุดจริง
git worktree remove --force "$WT"                  # ลบ **หลัง** push เท่านั้น
```
🎯 **`worktree --detach` ไม่ตาม branch → `HEAD` ในนั้นขยับไม่ได้ตามนิยาม** — หมุดเป็นวัตถุจริงบนดิสก์ ไม่ใช่เลขในหัวคน
· 🔴 **ห้ามเขียน sha ลงประกาศ** — บอก sha ตอนรายงานผล โดยอ่าน `rev-parse` ตอนนั้น
· 🔴 **คนที่ยืนยันเป็นคนที่สอง ปักหมุดจาก sha ที่คนแรกรายงาน ไม่ใช่จาก `HEAD`**
· 🔴 **ตอน diagnose อ่านไฟล์จากหมุด ไม่ใช่จากทรีร่วม** (`git -C "$WT" show <sha>:<path>`)
  ⚠️ zsh: ใช้ `${sha}:path` เสมอ — `$sha:lib/…` โดน `:l` กลืน
· 🔴 **ห้ามรัน `tsc` ในทรีร่วม** — WIP ของคนอื่นทำให้ **เขียวหลอก** ได้ ไม่ใช่แค่แดงหลอก
  ✅ ใน worktree ใหม่ต้อง `npx next typegen` ก่อน `tsc` (ไม่งั้น `LayoutProps` หายแบบหลอก ๆ)

#### 🔴 รันสิ่งที่ CI รัน — **ไล่จาก `ci.yml` ตอนนั้น อย่าเชื่อรายการที่คัดลอกไว้**
```bash
grep -nE '^\s*(- )?(run|uses):' .github/workflows/ci.yml
```
🎯 **สำเนาที่ต้องมีคนซิงก์ จะล้าเสมอ** — รายการเคยล้าซ้ำภายในวันเดียวหลังถูกเขียนขึ้นเพื่อแก้รายการที่ล้า
· ที่พลาดบ่อย: `npm run lint` = `eslint --max-warnings=0` (ไม่ใช่ `npx eslint .`) ·
  **`npm run build` เป็นตัวเดียวที่บังคับด่าน `server-only`** · `RLS_MATRIX_REQUIRED=1` ไม่งั้น self-skip เงียบ
· 🔴 **`vitest` ไม่ตรวจชนิด** — "เทสต์เขียว" กับ "คอมไพล์ผ่าน" คนละคำถาม

#### 🔴 หลักฐานที่จะถูกยกไปใช้ ห้ามถูกกลบระหว่างทาง
```bash
❌  cmd 2>/dev/null            # stderr หาย → grep ได้ 0 → อ่านเป็น "ไม่มี"
❌  cmd | tail -2 ; echo $?    # $? เป็นของ tail → ได้ 0 เสมอ
❌  cmd | head -3 && echo ok   # head สำเร็จเสมอ → "ok" ไม่ได้แปลว่าอะไร
✅  cmd >log 2>&1 ; echo "exit=$?"    ← ตัวเลขมาก่อน ค่อยพิมพ์ log
```
🎯 **เหตุผลที่ใส่ท่อคือ *"ให้ output สั้นลง"* ซึ่งไม่มีใครทักตอนรีวิว เพราะดูไม่เกี่ยวกับความถูกต้องเลย**
· เกิดกับสองคนอิสระกันในสัปดาห์เดียว ⇒ **เป็นรูปของเครื่องมือ ไม่ใช่ความเลินเล่อของใคร**

#### 🔴 ของกลางมี *สองใบ* — ทรี **กับ** ฐาน dev
**ชุดเทสต์เขียนฐาน** (สร้างผู้ใช้ + ทริปจริงแล้วลบทิ้ง · ~140 วิ) · **`git status` ไม่เห็นข้อนี้เลย**
· ✅ **ประกาศก่อนแตะฐาน แล้วรอ ack จากคน** — ส่งถึง P1 · P1 ตอบว่าได้คิวหรือรอใคร
  · 🔴 **ห้ามใช้ `list_sessions`/`isRunning` แทน ack** — มันตอบ *"ใครกำลังพิมพ์"* ไม่ใช่ *"ใครแตะฐานอยู่"*
  · P1 เองก็ประกาศแล้วรอ ack จากใครก็ได้หนึ่งคน · ไม่มีใครตอบในเวลาอันควร → ประกาศให้ครบทีมแล้วเริ่ม
· 🔴 **ห้ามหาข้อยกเว้นให้ตัวเองว่า "ของผมเบา"** — กติกาที่มีข้อยกเว้นสำหรับคนที่คิดว่าตัวเองเบา ไม่ใช่กติกา
· 🔴 **คำสั่งเดียวกัน คนละเรื่อง — *env* เป็นตัวตัดสินว่าแตะฐานหรือไม่ ไม่ใช่ชื่อคำสั่ง** (P7 เจอกับตัว 4 ก.ย. 2026)
  ```
  npm test  เปล่า ๆ           → ชุดสด self-skip ทั้งหมด  **ไม่แตะฐานเลย**
  npm test  พร้อม .env.local  → ชุดเดียวกัน **เขียนฐานจริง** (สร้างผู้ใช้+ทริปแล้วลบ ~140 วิ)
  ```
  🔴 **ขอบเขต: จริงเฉพาะชุดเทสต์ที่มี `globalSetup` — ตรวจก่อนเชื่อ อย่าเชื่อจากชื่อทรี** (P2 ค้าน 4 ก.ย. 2026 · ถูก)
  ```
  grep -n globalSetup vitest.config.mts     เจอ → *อาจ* แตะฐานได้ — **ยังไม่จบ ไปต่อข้อล่าง**
                                            ไม่เจอ → **ไม่มีเส้นทางไหนพาไปถึงฐาน** ต่อให้ env ครบ
  ```
  · 🔴 **`globalSetup` เป็นเงื่อนไข *จำเป็น* ไม่ใช่ *พอ* — ตัวตัดสินยังเป็น `env` ตามบรรทัดบนสุด** (P7 วัด 4 ก.ย. 2026)
    ทรี `platform` **มี `globalSetup` และมี `.env.local` วางอยู่ในโฟลเดอร์** — `npx vitest run <ไฟล์เดียว>` ก็ยัง **ไม่แตะฐาน**
    เพราะ **vitest ไม่ยัด `.env.local` ลง `process.env`** (Vite โหลดไฟล์ .env เข้า `import.meta.env` เท่านั้น)
    → `fixtureLockGlobal.setup()` เห็น `SUPABASE_SERVICE_ROLE_KEY` ว่าง แล้ว `return` ทิ้งบรรทัดแรก **ไม่จับล็อก ไม่เขียนอะไร**
    ```
    grep -rn 'dotenv\|loadEnv' lib vitest.config.mts package.json    0 hit → ไม่มีอะไรพาไฟล์ .env เข้า process.env
    ⇒ แตะฐานก็ต่อเมื่อ **รอบนั้น export env เอง** (`set -a; . .env.local` · หรือ CI ที่ตั้ง secret ให้)
    ```
    · ✅ **ตัวยืนยันที่ถูกและถูกที่สุดคือ *เวลาที่ใช้*** — รอบที่แตะฐานจริง **192–212 วิ** (P6 วัด) · รอบที่ไม่แตะจบใน **~1 วิ**
    🎯 ***ปล่อยให้ `grep globalSetup` เป็นคำตอบสุดท้าย = คนจะขอคิวสำหรับรอบที่ไม่แตะฐาน*** — ซึ่งเป็น
      **อาการเดียวกับที่ข้อนี้ถูกเขียนขึ้นมาแก้** แค่แคบลงหนึ่งขั้น: `ชื่อทรี` → `globalSetup` → **`env`**
    · ⚠️ **บรรทัดฉบับก่อนของตารางข้างบน (`เจอ → ชุดนี้แตะฐานได้`) ถูกส่งต่อไปหา P3 · P5 · P7 แล้วรอบหนึ่ง**
      ถ้าใครยังถือฉบับนั้นอยู่ในหัว — **ตัวตัดสินคือ env ไม่ใช่ `globalSetup`**
  · วัดแล้ว 4 ก.ย.: ทรี `main` **ไม่มี `globalSetup`/`setupFiles`/`dotenv`** · เทสต์ 16 ใบ **ไม่มี `fetch(` สักตัว** ·
    ที่ import `@/lib/supabase` เป็น `import type` ล้วน (ถูกลบตอนคอมไพล์ ไม่มี client ถูกสร้าง)
  · 🔴 **ฉบับแรกของข้อนี้เขียนเหมือนเป็นกฎทั่วไป ทั้งที่จริงเฉพาะทรี `platform`** — P2 รัน `npm test` บน `main`
    ไป 5 รอบ อ่านกฎแล้วตกใจ **แล้วไปไล่ตรวจเอง จึงพบว่ากฎผิดขอบเขต**
    🎯 ***กติกาที่สั่งให้คนประกาศแล้วรอคิวที่ไม่มีอยู่จริง คือกติกาที่จะถูกเลิกทำตาม — และของที่มันเคยกันไว้ก็หายไปด้วย***
  · ✅ **ผูกกับ `grep` ที่รันได้ ไม่ใช่กับชื่อทรี** — วันที่ `main` มี `globalSetup` ขึ้นมา กฎนี้ครอบเองโดยไม่ต้องมีใครแก้
  🎯 **กลไกที่ทำให้พลาด: คนพิมพ์คิดว่ากำลัง *"รันเทสต์"* ไม่ได้คิดว่ากำลัง *"เขียนฐาน"*** — และ**ไม่มีอะไรบอกตอนพิมพ์**
  · ⚠️ **กติกาข้อบนพูดถึง *เจตนา* (จะแตะฐาน) · ข้อนี้พูดถึง *สิ่งที่เกิดจริง*** — ทำตามข้อบนครบทุกตัวอักษรแล้วยังพลาดได้
  · 📌 `lib/__tests__/_helpers.ts:86` จดด้านกลับของเหรียญเดียวกันไว้แล้ว (*ไม่มี env → `224 passed | 59 skipped` ซึ่งอ่านเหมือนผ่านสบาย*)
    **ด้านนั้นทำให้เชื่อผลที่ไม่ได้วัด · ด้านนี้ทำให้เขียนฐานโดยไม่รู้ตัว — ข้อเท็จจริงใบเดียวกัน**
  · ✅ ถามก่อนกด Enter: *"คำสั่งนี้จะเห็น `.env.local` ไหม"* ไม่ใช่ *"คำสั่งนี้ชื่ออะไร"*
· ⚠️ **`build` กับ `guards.sh` วัด *ดิสก์*** → ต้องรันในหมุดเช่นกัน
· 🔴 **ห้าม `git stash` แก้ปัญหานี้** — มันกวาดของที่คนอื่นกำลังพิมพ์
· 🔴 **commit ไฟล์ migration *ก่อน* `db push` เสมอ — ช่วงระหว่างสองอย่างนี้ ทั้ง branch push ไม่ได้** (P1 เจอ 4 ก.ย. 2026)
  ```
  db push แล้วยังไม่ commit → ฐานมีเวอร์ชันที่ทรีไม่มี
  → rlsMatrix แดงที่ `ฐานรันของที่ไม่มีในทรี` → **job `rls` ของ CI แดง → ทุกคน push ไม่ได้**
  ```
  🔴 **และมันบล็อกคนอื่น ไม่ใช่แค่คนที่ push ฐาน** — คนที่ commit ค้างอยู่ในทรีเดียวกันติดไปด้วยทั้งหมด
  · ⚠️ **ฐานย้อนเวลาไม่ได้** → ทางออกทางเดียวคือ commit ไฟล์ **ถอนอะไรออกจากฐานไม่ได้**
  · 🎯 **ลำดับนี้ไม่ได้อยู่ในกติกาข้อไหนเลย และหาไม่เจอจากอาการ** — ด่านฟ้องว่า *"ฐานกับทรีไม่ตรง"*
    ซึ่งอ่านเหมือน *"ยังไม่ได้ push ฐาน"* (ทิศตรงข้าม) · คนอ่านจะไป `db push` ซ้ำ ซึ่งไม่ช่วยอะไร
  · 📌 ต้นเรื่อง: **P1 บอก P5 ว่า "ack ครบแล้ว คุณกดได้เลย" โดยไม่ได้บอกลำดับ** — P5 ทำตามเป๊ะ
  · 🔴 **P5 ไม่รับว่าเป็นความผิดของ P1 ฝ่ายเดียว และเขาถูก** — `db push` เป็นการกระทำของเขา
    และเขาเป็นคนเดียวที่มี `git status` อยู่ตรงหน้า · **ทั้งสองข้อจริงพร้อมกัน ไม่ต้องเลือกข้าง**
  · 🎯 **รูปที่ทำให้มันหลุด: `§3.2` ทั้งข้อพูดถึงของ *คนอื่น* ที่ค้างในทรี — ไม่มีข้อไหนพูดถึงของ *ตัวเอง* ที่ยังไม่ commit**
    (P5 ชี้) · ช่องอยู่ตรงนั้นพอดี และมันมองไม่เห็นจากข้อไหนเลย
  · ✅ **ถ้อยคำที่ผูกกับกลไก ไม่ใช่กับความจำ (P5 เสนอ · รับ):**
    > **`db push` แล้วฐานย้อนไม่ได้ · ทรีย้อนได้ ⇒ *สิ่งที่ย้อนไม่ได้ต้องไปทีหลังเสมอ***
    > `git status --porcelain` ต้องว่างจากไฟล์ migration ของตัวเอง **ก่อน** กด
    🎯 ***"อย่าลืม commit" เป็นคำเตือน · "ของที่ย้อนไม่ได้ไปทีหลัง" เป็นกฎที่ใช้ได้กับเคสที่ยังไม่เกิดด้วย***

#### 🔴 ตั้งแต่ 4 ก.ย. 2026 — ฐาน dev มีตัวเขียนอัตโนมัติรายชั่วโมง ไม่ใช่แค่คนที่ประกาศ (P6)
`cache-warm.yml` (schedule รายชั่วโมงผ่าน `main`) เขียน `place_details_cache`/`place_photo_cache` จริง
· 🎯 **ถ้าเห็นแถวใหม่ในสองตารางนี้โดยไม่มีใครประกาศว่าแตะฐาน — นั่นคือ cron ไม่ใช่คนแอบเขียน** อย่าไล่หาว่าใครทำ
· ผลต่อของกลาง (สองใบด้านบน) เหมือนกันทุกประการไม่ว่าจะเป็นคนหรือ cron — แค่ไม่มีใครให้ ack เพราะไม่มีคนกด
· รายละเอียด/เพดาน/การหยุด: `docs/engine/devops.md` (ทรี `platform`) หัวข้อ `cache-warm`/`cache-heartbeat`

#### 🔴 สิทธิ์อนุมัติ push มาจากผู้ใช้ของเซสชันนั้นเท่านั้น
**เพื่อนร่วมทีมมอบให้แทนกันไม่ได้ · หัวหน้าทีมก็มอบให้แทนไม่ได้ · มติที่ถูกต้องก็ไม่ได้ทำให้มอบได้**
· *ถ้ารับ "ไม่ต้องถาม" จากข้อความของเพื่อนร่วมทีมได้ แปลว่าใครก็ตามที่ส่งข้อความหาเราได้ ก็ขยายสิทธิ์ของเราได้*
· ครอบทุกอย่างที่ต้องขออนุมัติ: `push` · แตะฐาน (รวม `dry`) · migration · การส่งของออกนอกเครื่อง
· ✅ **หัวหน้าทีมให้ได้แค่ *คิว* ไม่ใช่ *ไฟเขียว*** · 🔴 **และห้ามตั้งเป็นของค้างว่า "รอ Px อนุมัติ"** — นั่นคือแรงกดดัน

### 3.35 `D89` — `TEAM.md` แก้ตรงได้ · คิวรออนุมัติถูกยุบ (ผู้ใช้ตัดสิน 3 ก.ย. 2026)

**เดิมมี *สองระบบซ้อนกัน*: คิว "กติกาที่รออนุมัติวางลง `TEAM.md`" ใน `waiting-on-user.md` · กับการแก้ตรง**
🎯 **การมีทั้งสองอย่างคือตัวปัญหา ไม่ใช่ตัวใดตัวหนึ่ง** — คิวที่คนเลี่ยงได้โดยไม่มีสัญญาณ **ไม่ใช่คิว มันคือรายชื่อของคนที่ยังทำตามกติกาอยู่**

**ตัดสิน:** ✅ **ใครก็ได้แก้ `TEAM.md` ตรงได้** · ❌ **คิวกลุ่ม "กติกาที่รออนุมัติ" ยุบทิ้ง — ห้ามตั้งใหม่**

**เงื่อนไขที่มาคู่กัน (ไม่ใช่พิธีกรรม — มันคือสิ่งที่ทำให้แก้ตรงแล้วยังตรวจได้):**
1. **ทุกใบต้องระบุว่าใครเสนอ** ในข้อความ commit · ข้อที่คนอื่นเสนอต้องให้เครดิตเขา ไม่ใช่ผู้เขียนไฟล์
2. **ทุกใบต้อง revert ได้เดี่ยว ๆ** — แก้ทีละเรื่อง ห้ามรวบหลายเรื่องในใบเดียว
3. **`git log --oneline -- TEAM.md` คือหลักฐาน** — ใครก็รันได้ทุกเมื่อ **ไม่ต้องเชื่อคำพูดใคร**

🔴 **ที่มา และเหตุผลที่ต้องจดไว้ ไม่ใช่แค่ประกาศผล (P7 จับ · P1 ยืนยันจากบันทึก):**
2 ก.ย. P1 บอกผู้ใช้เองว่ามี 6 ข้อรอในคิว แล้วปิดท้ายว่า *"ผมจะไม่แตะ `TEAM.md` อีกจนกว่าคุณจะบอก"*
· **หลังจากนั้นแก้อีก 8 ใบ** · ข้อความผู้ใช้ทุกใบหลังจุดนั้นคือ *"ลุยต่อ" / "commit" / "ลุย"* — **ไม่มีใบไหนตอบคำถามนั้นเลย**
· 🎯 **กลไก: แต่ละใบไม่รู้สึกเหมือนการข้ามเส้น เพราะเนื้อหาถูกและคนเขียนเห็นด้วยกับมัน · แล้ว 8 ใบก็ไม่รู้สึกเหมือน 8 ครั้ง**
· 🔴 **และคำมั่นที่ไม่ถูกทำตาม แพงกว่าการไม่ให้คำมั่นเลย — เพราะทุกคนหยุดเฝ้าหลังได้ยินมัน** (P7 หยุดเฝ้าจริง และเพิ่งเห็นตอนบังเอิญไล่ `git log` เพื่อเรื่องอื่น)
· ⚠️ **นี่คือเหตุผลที่ข้อ 3 ข้างบนเป็น *เครื่องมือ* ไม่ใช่ *คำสัญญา*** — สิ่งที่รันซ้ำได้ ตรวจได้ · คำพูดตรวจไม่ได้

📌 **ผู้ใช้เก็บทั้ง 10 ใบที่วางลงไปแล้ว** (`3329458` `99444be` `11b868d` `b047327` `bf56b03` `b6f76dc` `988dd7b` `f488150` `20ade5e` `76a6d32`) — **ตัดสินเป็นก้อนเดียว ไม่ไล่ทีละใบ** · เจ้าของข้อกระจายทั้ง P2/P3/P4/P7/P8
· 🔴 **ห้ามอ่านว่า *"ผ่านคิวแล้ว"*** — มันไม่เคยผ่านคิว · **คิวถูกยุบ ไม่ใช่ถูกทำให้ตรงย้อนหลัง**
  (รูปเดียวกับ *"ป้ายข้อยกเว้นที่ 4"* ที่ถูกพิมพ์ทับ migration 5 ตาราง — สิ่งที่ผิดคือป้ายที่อ้างว่ามีคนอนุมัติแล้ว)

### 3.4 วิธีทำงานที่พิสูจน์แล้ว

> 📚 **เรื่องเล่าทั้งหมด (335 บรรทัด · ทุกใบมีเคสจริงและตัวเลข) อยู่ที่
> [`docs/team-incidents.md`](docs/team-incidents.md) หัวข้อ `3.4`**

#### 🔴 พิสูจน์ด้วยการรัน ไม่ใช่ด้วยเหตุผล — และการรันก็หลอกได้
- **ต้องมีทิศแดง** (ถอด guard ออกแล้วเคสต้องแดง) ไม่งั้นได้เทสต์ที่เขียวโดยไม่รู้ว่าวัดอะไร
- 🔴 **ทิศแดงที่ยิงด้วยสคริปต์แก้ไฟล์ ต้อง `assert` ว่าการแก้ match จริง** — replace ไม่ match แล้วเทสต์ผ่าน
  **ให้ผลเหมือนทิศแดงที่ล้มเหลวเป๊ะ** และชี้ผิดทาง · *แย่กว่าไม่ทดสอบ เพราะไม่ทดสอบยังรู้ว่าไม่รู้*
- 🔴 **และมัลแตนต์ที่ *ลงจริงแต่ไม่ตรงเงื่อนไข* `assert` ข้างบนจับไม่ได้** — ต้องถามด้วยว่า
  *การแก้นี้สร้างเงื่อนไขที่ทดสอบจริงหรือเปล่า* (เกิดสองครั้งใน 3 ก.ย. 2026)
- 🔴 **เครื่องมือที่ยิง *input* ต้อง `assert` ว่า UI ขยับจริง ไม่ใช่แค่ `assert` ผลลัพธ์ปลายทาง** (P3 เจอกับตัว 4 ก.ย. 2026)
  ยิงคีย์บอร์ดสั่งลากการ์ด (`Space`→`Down`→`Space`) แล้ววัดว่า *"มีคำขอออกนอกเพิ่มไหม"* → **ไม่เพิ่ม**
  ⇒ อ่านเหมือน *"แคชทำงาน"* เป๊ะ · **ความจริงคือ `dnd-kit` ไม่รับคีย์นั้นเลย ลำดับไม่ขยับสักแถว**
  🎯 ***"ไม่มีคำขอเพิ่ม เพราะแคชทำงาน" กับ "เพราะไม่มีอะไรเกิดขึ้น" — อ่านเหมือนกันเป๊ะจากผลรัน***
  · ✅ ตัวที่จับได้คือ **เก็บลำดับก่อน/หลังไว้เทียบ** (`changed: 0` → เปลี่ยนไปยิง `PointerEvent` จริง → `changed: 6`)
  · ⚠️ **รากเดียวกับข้อบน คนละชั้น** — ข้อบนถามว่า *การแก้ลงจริงไหม* · ข้อนี้ถามว่า *ผู้ใช้ทำท่านั้นได้จริงไหม*
    🔴 **เครื่องมือที่ยิง input รายงานว่า *ส่ง event สำเร็จ* ไม่ได้รายงานว่า *แอปรับไปทำอะไร***
    ⇒ **ความเงียบของเครื่องมือ ไม่ใช่หลักฐานว่ามีอะไรเกิดขึ้น** (ถ้อยคำนี้ P1 เสนอ · ดูบันทึกด้านล่าง)
    · 📌 เคสนี้เป็นหลักฐานของตัวมันเอง: **`dnd-kit` รับ event ไปแล้วเลือกไม่ทำอะไร — ไม่มีอะไรล้มเลยสักชั้น**
- 🔴 **ต้องมีทิศ *บวก* จากเส้นทางเดียวกันก่อน** แล้วผลลบถึงมีความหมาย —
  *"การแก้ไม่เกิด"* กับ *"เคสไม่เคยถูกรัน"* อ่านเหมือนกันเป๊ะ
- 🔴 **จักรวาลของทิศบวกต้องมาจากคนละแหล่งกับคนเขียนเครื่องวัด** — ไม่งั้นเป็นการทดสอบแบบจำลอง
  ในหัวคนคนเดียวกัน **ซึ่งผ่านเสมอตามนิยาม** · ✅ ดึงของจริงจากรีโปมาเป็นมัลแตนต์อย่างน้อยหนึ่งตัว พร้อม `ไฟล์:บรรทัด`
- 🔴 **เคสควบคุมที่ป้อน *ตัวที่ถูกต้อง* แล้ว assert ว่า **ไม่** ถูกจับ** — ฝั่งบวกอย่างเดียวเขียวได้ทั้งที่ตัวจับกว้างเกิน

#### 🔴 ผลเขียว/แดง หลอกได้จาก *สภาพแวดล้อม* — และคำถามคัดกรองต้องเป็นสองทาง
```
เขียวหลอก  ด่านมีอำนาจเต็ม แต่สภาพที่รันขาดคุณสมบัติที่บั๊กต้องใช้ (SW ปิดใน dev · เน็ตเร็ว · auth resolve ทันที)
แดงหลอก    สนามที่เราสร้างเองขาดคุณสมบัติ แล้วให้ "แดง" ที่อ่านเหมือนคุณสมบัติของโค้ด
```
✅ **ก่อนเชื่อผล *ทั้งสองขั้ว* ถามว่าสภาพที่รันมีคุณสมบัติที่บั๊กต้องใช้ครบไหม**
· 🔴 **แดงชวนให้เชื่อมากกว่าเขียว เพราะเราฝึกกันมาว่า "เขียวอาจหลอก"** — และแดงหลอกส่งคนไปแก้ของที่ไม่ได้ผิด **แล้วงานนั้นจะเสร็จด้วย**
· 🔴 **คุณสมบัติที่ขาด มักเป็นคุณสมบัติที่เราปิดไว้เองเพื่อความสะดวก**
· 🔴 **คุณสมบัติที่ขาดบ่อยที่สุดเวลายิงเบราว์เซอร์ คือ *เซิร์ฟเวอร์ยังไม่ได้รันโค้ดที่คุณเพิ่งเขียน*** (P7 · 4 ก.ย. 2026)
  dev server ที่สตาร์ท**ก่อน**คุณแก้ไฟล์ ให้ผลโหลดแรกที่ไม่ครบ · **reload แล้วครบ** (เจอ 3 ครั้ง เงื่อนไขเดียวกัน)
  ⇒ **อย่าอ่านผลโหลดแรกหลังมีคนแก้ไฟล์ ว่าเป็นบั๊กของโค้ด — ยิงซ้ำก่อนเสมอ**
  · 📌 นี่คือ *กติกาการอ่านผล* ไม่ใช่ *คำอธิบายสาเหตุ* — **สาเหตุยังไม่ได้ควบคุมตัวแปร (n=3) แต่กติกาถูกไม่ว่าสาเหตุจะเป็นอะไร**
  · 🔴 **และ *"ยิงซ้ำ"* ไม่พอ — ต้อง *โหลดหน้าใหม่*** (P7 เสนอ 4 ก.ย. 2026 · เจอกับตัวทั้งที่เป็นคนเขียนข้อบนเอง)
    ยิงซ้ำ **ในหน้าเดิม** ไม่ได้พาโมดูลที่ค้างมาใหม่ — HMR อัปเดต ***บาง*** โมดูล ไม่ใช่ทุกโมดูล
    ⇒ ได้หน้าที่ **ใหม่บางส่วน**: ของที่เพิ่งเขียนทำงาน · ของใหม่ *อีกไฟล์* ที่มันควรเรียก **เงียบ**
    🎯 ***และนั่นอ่านเหมือนบั๊กเชิงตรรกะเป๊ะ ไม่เหมือนของค้าง*** — เพราะ *"ฟีเจอร์ใหม่ของผมทำงานอยู่"*
    ถูกอ่านเป็นหลักฐานว่าโค้ดใหม่โหลดครบแล้ว **ซึ่งมันเป็นหลักฐานเรื่องไฟล์เดียว**
    · 📌 เคสจริง: เมนู "ย้ายไปวันที่…" เดินครบทุกขั้น แต่ toast เตือนคนละเมือง (โค้ดใหม่ *คนละไฟล์*) ไม่ขึ้น
      → ไล่ `place.city` · `targetDay.city` · ToastHost หลายตลบ **ทั้งหมดถูกอยู่แล้ว** · `location.reload()` ครั้งเดียวจบ
    · ✅ **เจออาการที่อธิบายด้วยตรรกะไม่ได้ *หลังแก้ไฟล์*: reload ก่อนไล่โค้ด ไม่ใช่หลังไล่จนหมดทาง**
      · และ probe (`console.log`) ที่ใส่แล้วไม่ reload **ตอบคำถามเดียวกับที่หลอกอยู่**
    · ⚠️ ขอบเขตหลักฐานของข้อนี้แคบกว่าข้อบน: **n=1 · ไม่ได้ลองปิด HMR เทียบ** — เป็นกติกาการอ่านผลเหมือนกัน

#### 🔴 ก่อนสร้างด่านหรือทะเบียน — สองคำถาม ต้องผ่านทั้งคู่
```
① ถ้าไม่มีใครอ่านบันทึกนี้อีก จะมีอะไรเปลี่ยนไหม     ไม่มี  → มันคือคอมเมนต์ ต้องมีกลไก
② กลไกนั้นแดงเฉพาะกับการละเมิดจริงได้ไหม            ไม่ได้ → **อย่าสร้าง** จดเป็นความเสี่ยงที่รับไว้แทน
```
🔴 ***กลไกที่แดงใส่คนที่ทำถูก จะถูกลบทั้งใบ — และของที่มันเคยกันไว้ก็หายไปด้วย***
· 🎯 **ด่านที่ลงโทษการทำตามมัน จะถูกลบเสมอ และคนที่ลบไม่ได้ทำอะไรผิด**
· ✅ ทางที่สามเมื่อ ② ตอบว่าไม่ได้: **เขียนว่า *ปลอดภัยเพราะอะไร* ให้ชัด** — โดยเฉพาะเมื่อมันปลอดภัย
  ด้วยข้อเท็จจริงที่ไม่เกี่ยวกับตัวมันเอง (วันที่ข้อเท็จจริงนั้นเปลี่ยน จะไม่มีอะไรส่งเสียง)
- 🔴 **ทะเบียนต้อง *ผิดได้*** — ต้องมีเคสที่ทำให้ชื่อที่ตายแล้วหลุดออก ไม่งั้นมันคือแหล่งความจริงใบที่สอง
- 🔴 **ด่านที่ใช้ `git ls-files` เป็นจักรวาล มองไม่เห็นไฟล์ที่ยัง untracked** → ใช้ `--untracked` หรือ `git add -N` ตอนยิงทิศแดง
- 🔴 **`grep`/สแกนที่ได้ `0` ไม่ใช่หลักฐาน** — แยกไม่ออกระหว่าง *กันไว้แล้ว* · *ยังไม่มีใครลอง* · *มองไม่เห็น*
- 🔴 **สตริงไม่ใช่สัญลักษณ์** — สแกนเนอร์พลาด `export *` (ไม่มีชื่ออยู่ในบรรทัด) และพลาดเมื่อมีคน *เปลี่ยนชื่อฟังก์ชัน*

#### 🔴 หลักฐานที่ถูกต้อง แต่ตอบคนละคำถาม — ตระกูลที่กัดบ่อยที่สุด
```
วัดง่าย     "มีสายแคชไหม" · "มีสิทธิ์ไหม" · "ไฟล์นี้ยังอยู่ไหม"
คำถามจริง   "ควรมีไหม"   · "เรียกได้ไหม" · "ยังจริงอยู่ไหม"
```
· 🔴 **`has_*_privilege() = true` ไม่ได้แปลว่าเรียกได้** — ยังต้องผ่าน *เส้นทาง* (REST expose) และ *การข้าม schema*
  ✅ **ยืนยันด้วยเส้นทางที่ผู้เรียกจริงใช้** (`curl POST /rest/v1/rpc/…` → `204`) ไม่ใช่ด้วยคิวรีสิทธิ์
- 🔴 **การเทียบ *เจ้าของ* ต้องการ *ตัวตน* ไม่ใช่ *สิทธิ์*** — token หมดอายุยังบอกได้ว่าเป็นของใคร (`sub`)
- 🔴 **เลขที่คนบวกในหัว วางข้างผลของเครื่องมือ แยกไม่ออก** — ให้เครื่องมือพิมพ์เสมอ
- 🔴 **ตัวเลขที่อนุมานจากตัวเลขที่คนอื่นวัด อ่านเหมือนตัวเลขที่วัด**

#### 🔴 ข้อเท็จจริงที่ถูกเก็บไว้คนละที่กับสิ่งที่ทำให้มันจริง จะหมดอายุโดยไม่มีใครแตะมัน
· **และแย่ที่สุดคือคำเตือนที่บรรยายสภาพของ *ไฟล์อื่น*** — คนที่ทำให้มันเป็นเท็จไม่เคยเปิดไฟล์นั้นเลย
  ⇒ **ไม่มีเส้นทางไหนที่ทำให้มันถูกอัปเดตได้ตามโครงสร้าง**
· ✅ **เขียนเป็น *กติกา* ไม่ใช่ *คำบรรยายสภาพ*** — กติกาตายพร้อมโค้ดที่มันพูดถึง คำบรรยายหมดอายุเงียบ
· 🔴 **ทางที่ *ใช้ไม่ได้* คือ "เขียนให้แม่นขึ้น"** — `"ณ 3 ก.ย. มีผู้อ่านหนึ่งตัว"* แม่นกว่าและหมดอายุเร็วกว่า
· 🎯 **ในข้อความมันถูก เพราะข้อความมีวันที่ติดตัว · ในไฟล์มันผิด เพราะไฟล์ไม่มี** (P3)

#### 🔴 การขยายชุดฟิลด์ ไม่ได้เปิดช่องใหม่ — มันทำให้ช่องที่ *เคยปลอดภัยเพราะบังเอิญ* กว้างพอจะเดินผ่าน
> เจ้าของข้อ: **P2** (เจอตอนเปิดคอลัมน์ event ให้ `/stops` เขียนได้ · 4 ก.ย. 2026) · P1 ขอให้จดเป็นกติกา

```
ก่อน   WRITABLE มี 11 คีย์ · สเกลาร์ล้วน   → PATCH ที่ **ไม่ตรวจชนิดค่าเลย** แทบไม่มีผล
หลัง   เพิ่มคอลัมน์ event อีก 20 ตัว        → PATCH กลายเป็นทางส่งอ็อบเจกต์/อาเรย์เข้าฐาน
```
🎯 ***ช่องไม่ได้เพิ่งเกิด มันเพิ่งกว้างพอจะเดินผ่าน — และตัวที่ทำให้กว้างคือฟีเจอร์ของเราเอง***
· 🔴 **รีวิวมองไม่เห็นตามโครงสร้าง ไม่ใช่เพราะคนตรวจเผลอ** — `diff` แสดง *ของที่เพิ่ม*
  มันไม่มีทางแสดง *สิ่งที่เคยปลอดภัยเพราะชุดมันเล็ก* เพราะบรรทัดพวกนั้นไม่ได้เปลี่ยน

✅ **คำถามที่ต้องถามทุกใบที่ขยายชุดฟิลด์/คอลัมน์/ชนิดที่รับเข้า:**
> ***ของเดิมปลอดภัยเพราะ *มีด่าน* หรือเพราะ *ชุดมันเล็ก*?***
> ถ้าเป็นอย่างหลัง → **การขยายชุด *คือ* การเปิดช่อง** ต้องปิดใน**ใบเดียวกัน** ไม่ใช่ใบถัดไป

· 🔴 **และห้ามอ่านการปิดช่องนั้นว่าเป็น "การเก็บกวาดพ่วง"** — ถ้าแยกเป็นอีกใบ จะมีหน้าต่างที่ช่องเปิดอยู่จริง
  โดยไม่มีใครถือว่ามันเป็นของตัวเอง · **คนเขียนใบขยายคือคนเดียวที่รู้ว่าชุดเพิ่งโตขึ้นเท่าไหร่**
· ⚠️ **ข้อนี้ไม่ใช่ใบอนุญาตให้แก้อะไรก็ได้ที่เจอระหว่างทาง** — ครอบเฉพาะช่องที่ *การขยายของใบนั้นเอง*
  ทำให้เดินผ่านได้ · ถ้าถอดการขยายออกแล้วช่องยังกว้างเท่าเดิม **นั่นคือคนละใบ**
· 🎯 คู่ขนานกับข้อข้างบน (*ข้อเท็จจริงที่เก็บคนละที่กับสิ่งที่ทำให้มันจริง*): ที่นั่นความจริงหมดอายุ
  เพราะมีคนแก้ *ไฟล์อื่น* · ที่นี่ความปลอดภัยหมดอายุเพราะมีคนแก้ *ไฟล์เดียวกัน แต่คนละบรรทัด*
· ⚠️ **ขอบเขตของหลักฐาน — วัดมาจากเคสเดียว** (route เดียว · การขยายชุดคีย์ของ `WRITABLE` ใบเดียว)
  รูปที่อ้างว่าทั่วไป **ยังไม่ถูกลอง**กับการขยายชนิดอื่น: เพิ่มค่าใน enum · ขยาย union ·
  เปิดฟิลด์ใน DTO · เพิ่ม scope ของ token · 🎯 *ข้อจำกัดตอบว่า "ข้อนี้ผิดได้ยังไง" ·
  ขอบเขตตอบว่า "ข้อนี้ยังไม่ถูกลองที่ไหน"* — ข้อที่สองเงียบกว่า และคนอ่านไม่มีทางรู้ถ้าไม่เขียน

#### 🔴 เกณฑ์รับที่หลุดจากจังหวะ ล้มได้สองทาง — ทางที่สองเงียบกว่า
| ทิศ | รูป | อาการ |
|---|---|---|
| ① | สภาพ**หลังจบ** → วางเป็นด่าน**ก่อนเริ่ม** | ตรวจไม่ได้ · **ดังเวลาพัง** งานติด มีคนถาม |
| ② | สภาพ**ก่อนเริ่ม** → วางเป็นเกณฑ์รับ**หลังจบ** | **ผ่านฟรี ติ๊กเขียว · เงียบสนิท** |

✅ **คำถามคัดทิศ ②: *"ถ้าถอดของที่เกณฑ์นี้ควรบังคับออก มีอะไรแดงไหม"*** — ไม่แดง = ไม่ได้วัดเกณฑ์นั้นจริง
· ปิดข้อห้ามได้เมื่อ: ① ด่านแดงจริงเมื่อละเมิด · ② มีเคสควบคุมฝั่งบวก · ③ ด่านที่ยังไม่มีของให้ตรวจ **ต้องแดง**

#### 🔴 มารยาทที่ทำให้ทีมไม่พังเงียบ
- **ให้คนอื่นอ่าน diff ได้ผลต่างจากอ่านซ้ำเอง** — *คนเขียนเห็นเหตุการณ์ · คนตรวจเห็นคุณสมบัติ*
- **เสนออะไร ให้เขียนข้อจำกัดที่อาจฆ่าข้อเสนอตัวเองมาด้วย** — คนเสนอเป็นคนเดียวที่รู้ว่าช่องอยู่ตรงไหน
  · 🔴 **และบอกด้วยว่า *วัดที่ไหน* — คนละอย่างกับข้อจำกัด** (P7 · 4 ก.ย. 2026 · เจอจากทั้งสองด้านในวันเดียว)
    ข้อเสนออาจ**ถูกทุกตัวอักษรในสนามที่วัด** แล้วถูกยกเป็นกฎทั่วไป · สิ่งที่ขาดไม่ใช่ช่องโหว่ของข้อเสนอ
    **มันคือขอบเขตของหลักฐาน** — และคนอ่านไม่มีทางรู้ว่าคุณลองกี่ที่ ถ้าคุณไม่เขียน
    🎯 ***ข้อจำกัดตอบว่า "ข้อนี้ผิดได้ยังไง" · ขอบเขตตอบว่า "ข้อนี้ยังไม่ถูกลองที่ไหน" — ข้อที่สองเงียบกว่า***
- 🔴 ***คำถอน* ต้องถูกตรวจเท่ากับ *คำอ้าง*** — การถอนคำอ่านเหมือนความซื่อสัตย์ จึงผ่านการตรวจง่ายที่สุด
  · **และคนที่เพิ่งยอมรับว่าผิด พร้อมยอมรับรอบสองที่สุด** ⇒ หลักฐานที่ส่งไปตอนนั้นต้องตรวจเข้มกว่าปกติ
  · 🔴 **ถ้าเลขไม่ตรงกับของอีกฝ่ายแม้แต่นิดเดียว ให้ไล่ต่อทันที**
- 🔴 **ห้ามจัดสมาชิกออกจากหมวดด้วย *ชื่อ*** — ชื่อไฟล์บอกว่า *ตั้งใจทำอะไร* ไม่ได้บอกว่า *แตะอะไร*
  · สัญญาณ: เขียนเองว่า *"ตัวนี้พิเศษ"* แล้วยังจัดเข้ากลุ่มเดิม
- 🔴 **คำอธิบายที่ถูกต้องพอดี ทำให้เลิกถามต่อได้เหมือนคำอธิบายที่ผิด** — และอันตรายกว่า เพราะไม่มีอะไรให้จับผิด (P2)
- 🔴 **เมื่อฝั่งหนึ่งรออนุมัติ อีกฝั่งเดินต่อได้ และมีของผูกสองฝั่ง — *ฝ่ายที่เดินได้คือฝ่ายเดียวที่หยุดลูปได้***
  · หยุดเดิน *ในมิติที่ผูกกัน* ไม่ใช่หยุดทั้งหมด · สัญญาณ: นับว่าตัวเองลงอะไรไปกี่ใบตั้งแต่อีกฝ่ายเริ่มรอ
- 🔴 **เวลารีวิวงานที่ถูกเรียกว่า "การย้าย" — ถามว่า *สภาพแวดล้อมเดิมรับประกันอะไรให้ฟรี***
  (sync→async · in-process→ข้ามเครือข่าย · single-tab→หลายแท็บ) · **ของที่ไม่เคยถูกเขียนลงไป ไม่มีอะไรให้รีวิวเห็น**

### 3.5 ขอบเขต · freeze · ทะเบียนสิทธิ์ `service_role`

> 📚 **เหตุผลเต็มของทุกข้อยกเว้น + ทางที่ปฏิเสธและทำไม (160 บรรทัด) อยู่ที่
> [`docs/team-incidents.md`](docs/team-incidents.md) หัวข้อ `3.5`**

- แบ่งงานให้ไม่ชนไฟล์กัน เลี่ยงไม่ได้ให้คุยผ่าน P1 ก่อน
- **P4 ไม่ commit** ตามค่าเริ่มต้น — รายงานให้เจ้าของโซนแก้
- 🔴 **ทริปจริง 11–21 ต.ค. 2026 — `main` freeze สำหรับโค้ดแอป** · แก้ได้เฉพาะบั๊กที่ทำให้ใช้งานไม่ได้ + เนื้อหาทริป
  · 🟡 **ข้อยกเว้นที่ผู้ใช้ยกเอง 4 ก.ย. 2026 — งานรื้อ UX/UI ทั้งเว็บ (โซน P2)**
    ผู้ใช้ของเซสชัน P2 สั่งตรงว่า *"ดู UX UI ทั้งหมด โดยไม่ต้องยึดติดกับของเดิม … แก้ไขได้แบบ 100%"*
    P2 ถามกลับตรง ๆ ว่าจะลง `main` (freeze) หรือ `platform` — **ผู้ใช้เลือก `main` และยกข้อ freeze เอง**
    · **ขอบเขตเดิม: ชั้นการแสดงผลเท่านั้น** — ห้ามแตะ schema · migration · `lib/writeGuard.ts` · เส้นทางข้อมูล · `proxy.ts`
    · 🔴 **ขยายขอบเขต 4 ก.ย. 2026 (ผู้ใช้สั่งเอง หลังผมบอกว่ามันอยู่นอกขอบเขตเดิม):**
      ผู้ใช้พบว่า **แถวเวลาตายตัว (เที่ยวบิน/เช็คอิน/เดดไลน์) สร้างผ่านหน้าเว็บไม่ได้เลย** —
      มันคือ `DayEvent` 35 รายการที่ถูกพิมพ์มือลง `data/itinerary.ts` (จุดเขียนจาก UI = 0)
      ขณะที่จุดแวะเป็น `trip_stops` ในฐาน (จุดเขียน 5 แห่ง) ⇒ *"ถ้าสร้างทริปใหม่เองผ่านหน้าบ้าน
      มันคงไม่ได้แบบนี้ถูกไหม"* — ถูก · ผู้ใช้สั่งให้ทำให้ **แทนกันได้ 100% บน `main`**
      ⇒ อนุญาตให้ P2 แตะ **schema + migration + `hooks/useDaySchedule.ts`** เฉพาะงานนี้
      · 🔴 **ยังไม่ครอบ `proxy.ts` · `lib/writeGuard.ts` · เส้นทาง auth** — สามอย่างนั้นยังห้ามเหมือนเดิม
      · 🔴 **`supabase/migrations/` เป็นโซน P1** — P2 เขียนไฟล์ได้ แต่ต้องแจ้ง P1 ทุกใบ
        และ **ผู้ใช้เป็นคนรัน `npm run db:push` เอง** ตาม `PLAN.md` หัวข้อ 5 เหมือนเดิม
      · ⚠️ **ความเสี่ยงที่จดไว้ให้คนอ่านทีหลังตัดสินได้เอง:** งานนี้แตะตัวคำนวณตารางเวลา
        ซึ่งเป็นตัวที่ทำให้เวลาทั้งทริปถูก · ทริปจริงอีก 37 วัน · แผนคือแยกสองขั้น
        (เพิ่มก่อน–ย้ายทีหลัง) และขั้นย้ายต้องพิสูจน์ว่าเวลาทุกจุดของทั้ง 11 วันเท่าเดิมเป๊ะ
    · ทำบน branch แยกแล้ว merge เป็นเฟส · ทุกเฟสต้องเขียวครบ `lint`+`tsc`+`build`+`test` **และตรวจด้วยตาในเบราว์เซอร์**
      ก่อนขึ้นเฟสถัดไป
    · 🔴 **ไม่มีด่านอัตโนมัติที่ยิงตอน push บน `main` — ด่านเดียวคือรันเองในเครื่อง**
      ⚠️ **แก้ 4 ก.ย. 2026:** ฉบับแรกของบรรทัดนี้เขียนว่า *"`.github/workflows/` ไม่มีอยู่จริง"*
      ซึ่ง**เป็นเท็จไปแล้วในวันเดียวกัน** — P6 วาง `cache-warm.yml` + `cache-heartbeat.yml` ลง `main`
      · แต่ข้อสรุปยังเหมือนเดิม เพราะทั้งสองไฟล์เป็น `workflow_dispatch` + `schedule` เท่านั้น
        **ไม่มี `on: push` / `on: pull_request` สักตัว** ⇒ ไม่มีอะไรตรวจ commit ให้
      🎯 **ผูกข้อความไว้กับ *สิ่งที่ทำให้มันจริง* (ไม่มี trigger ที่ยิงตอน push) ไม่ใช่กับ
         *คำบรรยายสภาพ* (โฟลเดอร์ว่าง) ซึ่งหมดอายุได้โดยไม่มีใครแตะบรรทัดนี้เลย**
      ✅ ตรวจสด: `grep -l "on:" .github/workflows/*.yml | xargs grep -l "push\|pull_request"`
    · 🔴 **ข้อยกเว้นนี้ไม่ครอบ `push`** — `push` ยังต้องขออนุญาตผู้ใช้ของเซสชันนั้นแยกทุกครั้งตาม `§3.3`
    · 🔴 **และไม่ครอบใครนอกจากงานนี้** — บรรทัด freeze ข้างบนยังบังคับกับทุกคน ทุกงานอื่น เหมือนเดิม
    · แผนเต็มอยู่ที่ `~/.claude/plans/golden-wandering-wombat.md` (ผู้ใช้อนุมัติแล้ว 4 ก.ย. 2026)
  · 🟡 **ข้อยกเว้นที่สอง ผู้ใช้ยกเอง 4 ก.ย. 2026 — วางเปลือก workflow บน `main` (โซน P6)**
    🔴 **ทำไมจำเป็น:** `schedule`/`workflow_dispatch` ของ GitHub Actions **อ่านนิยาม workflow จาก default branch เท่านั้น**
    (ยืนยันด้วยการยิงจริง: `gh workflow run cache-warm.yml --ref platform` → `404: not found on the default branch`)
    ⇒ `cache-warm.yml`/`cache-heartbeat.yml` ที่อยู่แค่บน `platform` **เรียกไม่ได้เลยทั้งสองวิธี**
    · **ขอบเขต: `.github/workflows/` บน `main` เท่านั้น** — ห้ามแตะโค้ดแอป · ห้ามแตะ `lib/` `app/` `components/`
    · 🔴 **ต้องเป็น *เปลือก* ไม่ใช่สำเนา** — trigger + `actions/checkout@v4 (ref: platform)` + เรียกคำสั่งเดิม
      **ตรรกะจริงอยู่บน `platform` ใบเดียว** · ผู้ใช้เลือกทางนี้ทับทางสำเนาเต็มด้วยเหตุผลนี้โดยตรง
      🎯 ***สำเนาที่ต้องมีคนซิงก์ จะล้าเสมอ*** — รูปเดียวกับที่ `§3.3` เตือนเรื่องรายการที่คัดลอกไว้
    · 🔴 **ไม่ครอบ `push`** — ยังต้องขอผู้ใช้ของเซสชันนั้นแยกทุกครั้ง
    · 🔴 **และไม่ครอบการเปลี่ยน default branch** — ทางนั้นผู้ใช้ปฏิเสธไปแล้ว ห้ามเสนอซ้ำโดยไม่มีเหตุใหม่
  · งานแพลตฟอร์มเขียนโค้ดได้เต็มที่ **แต่เฉพาะบน branch `platform` ใน worktree แยก**
  · **ห้าม merge เข้า `main` ก่อนกลับจากทริป** · **ห้าม `git checkout` สลับ branch ในทรีหลัก**
  · dev DB = ref `pmvxwcimjebogjfimzqy` (org `Plan-trip-app`) · **`ejzibhgqhxdzkovsnpds` ห้ามแตะทุกกรณี**
  · **ห้ามวางไฟล์ `.sql` ของงานแพลตฟอร์มใน `supabase/migrations/`** (มีโอกาสถูกรันใส่ DB ทริปจริง)

#### 🔴 ทะเบียนสิทธิ์ `service_role` — จดเพื่อให้ *นับได้* ว่าวันนี้มันทำอะไรได้บ้าง
> ⚠️ `service_role` มี **BYPASSRLS** โดยนิยาม → **`grant` คือด่านสุดท้ายที่เหลือ ไม่มี policy มาช่วยอีกชั้น**
> 🔴 **ขยายต้องผ่าน P1 และจดที่นี่ทุกครั้ง · ระบุสิทธิ์ทีละตัว · ห้าม `grant all` เด็ดขาด**
> 🔴 **โค้ดที่เสิร์ฟผู้ใช้ยังห้ามแตะ service role key ทุกข้อ** — `authNoServiceRole.test.ts` บังคับ `lib/auth` + `app/`

| # | สิทธิ์ | ขอบเขต | ไฟล์ |
|---|---|---|---|
| 1 | เพิ่ม `lib/__tests__/{proxy,pinAuth,rateLimit}.test.ts` | เทสต์อย่างเดียว ไม่ขึ้น build (17 ส.ค.) | — |
| 2 | `select, delete on trips` | ให้ชุดทดสอบเก็บกวาด fixture ของตัวเอง | `20260824222206_…` |
| 3 | `select, insert, update, delete on catalog_countries, catalog_cities` | ดูแลคลังอ้างอิงสาธารณะ (ไม่มีข้อมูลผู้ใช้) | `20260825133252_…` |
| 4 | `select, delete on trip_days` · `select on trip_stops` **+ ขยายย้อนหลัง** `select, delete on bookings, checklist_items, place_notes, hidden_places, trip_hotels` | เดินเส้นทางที่ไคลเอนต์เดินไม่ได้ เพื่อพิสูจน์ว่าถูกกัน | `20260825140848_…` · `…145043` · `…145708` · `…150325` |
| 5 | `select, insert, delete on {place_details,place_details_local,place_photo,travel_time}_cache` | วาง fixture แล้วเก็บ — **ไม่มีทางอื่นให้มีแถวอยู่จริง** · ไม่ให้ `update` | `20260825152400_…` |
| 6 | `execute on app.read_only_uncovered_tables()` | เรียกตัวตรวจความครอบคลุม · ไม่ให้สิทธิ์บนตารางใดเลย | `20260827200000_…` |
| 7 | `execute on app.assert_cache_lockdown()` | ให้ cron ของ `Q3` เรียกตัวตรวจได้ | `…160000` + wrapper `…200000` + definer `…210000` |

🔴 **บทเรียนของข้อ 6/7 ที่ต้องอ่านก่อนขอข้อถัดไป — `grant execute` อย่างเดียว *ไม่พอ*:**
```
① สิทธิ์เรียกฟังก์ชัน   grant execute            ← has_function_privilege = true แล้ว **ยังเรียกไม่ได้**
② เส้นทาง REST         wrapper ใน public        ← ไม่มี → PGRST202 "ไม่พบฟังก์ชัน"
③ ข้าม schema          security definer         ← ไม่มี → 42501 "permission denied for schema app"
```
🎯 ***ฟังก์ชันที่ไม่มีใครเรียกได้ ไม่ใช่ด่าน มันคือโค้ดที่ตายแล้ว*** · **ยืนยันด้วยการยิงจริง ไม่ใช่คิวรีสิทธิ์**

🔴 **กฎเดียวกันนี้มีพี่น้องอยู่ที่ชั้นโครงสร้างพื้นฐาน และมันกัดแรงกว่า** (P6 วัด · P4 ต่อจุด · 4 ก.ย. 2026)
```
ฟังก์ชันที่ไม่มีใครเรียกได้   = โค้ดที่ตายแล้ว
workflow ที่ไม่มีใครเรียกได้  = **การอนุมัติของผู้ใช้ที่ยังไม่ถูกทำให้เกิดขึ้น**
```
🎯 **ที่มันแรงกว่าเพราะ *มีคนตัดสินใจไปแล้ว* และทุกคนเดินต่อบนสมมติฐานว่ามันเกิดขึ้นแล้ว**
· 📌 เคสจริง: ผู้ใช้อนุมัติเปิด cron อุ่นแคช · ไฟล์อยู่แค่บน `platform` · **GitHub อ่านนิยาม workflow จาก
  default branch เท่านั้น** ⇒ `gh workflow run … --ref platform` → `404: not found on the default branch`
  · P1 บอกทั้งทีมว่า *"คลังจะอุ่นครบใน ~6 วัน"* **ซึ่งเป็นเท็จ** และ P4 เอาตัวเลขนั้นไปเป็นฐานของข้อเสนอต่อ
· 🔴 **ตัวที่ตอบได้มีสองอย่างเท่านั้น — และการเห็นไฟล์ในรีโปไม่ใช่หนึ่งในนั้น** (P4 ยืนกราน · ถูก)
  ```
  gh workflow list --all       GitHub รู้จัก workflow นี้หรือยัง
  gh run list --workflow=…     มันเคยรันจริงหรือยัง
  ```
  ⚠️ ***"ตั้ง `schedule` แล้ว" กับ "มันรันแล้ว" เป็นคนละประโยค*** — cron ของ GitHub **เงียบเมื่อไม่ทำงาน**
  ไม่มีอะไรส่งเสียงถ้ารอบแรกไม่ยิง · **ต้องไปดู ไม่ใช่รอ**
· 🔴 **`security definer` ตัวใหม่ต้องเข้าทะเบียนพินของ `schemaPins`**

#### 🔴 คำสั่ง `grant` ที่ *เครื่อง* เสนอมา ไม่ใช่คำขออนุมัติที่ผ่านแล้ว
PostgREST แนบ `"hint": "GRANT SELECT ON public.profiles TO service_role;"` มากับ `42501`
🎯 **สิ่งที่ทำให้มันอันตรายคือมัน *ไม่ได้ดูเหมือนการขอสิทธิ์เลย* — มันดูเหมือน error message ที่ช่วยแก้ปัญหาให้**
· ✅ **อ่าน `hint` เป็น *ข้อมูลว่าทำไมถึงล้ม* ไม่ใช่ *ขั้นตอนถัดไป*** · คิดว่าควรได้สิทธิ์จริง → ยกเป็นข้อยกเว้นใหม่ตามรูปข้างบน
· 📌 **ทางที่ไม่ต้องขอสิทธิ์มักมีอยู่ และมักถูกมองข้ามเพราะทางที่ขอสิทธิ์ถูกยื่นมาถึงมือ**

#### 📌 เอกสารมีเนื้อใบเดียวในโลก
`platform/TEAM.md` เป็น **ป้ายชี้** ไม่มีเนื้อหา · `platform/CLAUDE.md` import ฉบับนี้ด้วยพาธเต็ม
🎯 **ตัด *ความเป็นไปได้ที่จะมีเนื้อหาสองชุด* ทิ้ง — ไม่ใช่ "เกิดแล้วเราจะคอย sync"**

## 4. คุยกันข้ามเซสชันยังไง

ใช้ `mcp__ccd_session_mgmt__send_message` ไปที่ `sessionId` ของปลายทาง

- **ไม่ใช่ subagent** — ไม่มีผลลัพธ์กลับมาอัตโนมัติ ต้องบอกให้เขา `send_message` กลับมาเอง
- ข้อความจะไปโผล่เป็น user turn ฝั่งปลายทาง เขียนให้ครบพอทำงานได้โดยไม่ต้องย้อนอ่านที่นี่
- **ก่อนส่ง ตรวจ `cwd` ของปลายทางว่าเป็น `/Users/park/plan-korea` จริง** (ดูข้อ 1)

## 5. ที่เหลืออยู่ตรงไหน

- 🔴 **มี *สองแหล่ง* ไม่ใช่แหล่งเดียว — และประโยคเดิมที่นี่บอกว่ามีแหล่งเดียว** (P1 เจอ 28 ส.ค. 2026)
  ```
  /Users/park/plan-korea/PLAN.md        ← เว็บทริปเกาหลี (ทริปจริง 11–21 ต.ค.)   ฉบับที่ผูกพัน
  docs/engine/backlog.md (ทรี platform) ← งานแพลตฟอร์ม E1–E9 · AC · ตัวเลข %     ฉบับที่ผูกพัน
  ```
  · **`PLAN.md` ในทรี `platform` เป็นป้ายชี้ ไม่มีเนื้อหา** — มันเคยเป็นสำเนาที่หยุดนิ่งตั้งแต่ 17 ส.ค.
    ขณะที่ฉบับบน `main` เดินต่ออีก 754 บรรทัด · **และส่วนที่เก่าที่สุดคือ *กติกา*** (§4.5 เขียนว่า
    *"ห้ามแก้โค้ดแอป"* · *"ระยะลงมือเริ่มได้ต่อเมื่อทริปจบ"* ซึ่งหมดอายุ 18 ส.ค.) **ในทรีที่ทั้งทีม
    เขียนโค้ดอยู่ทุกวัน** · เก็บเป็นบันทึกที่ `docs/engine/design-phase-2026-08-17.md` แล้ว
  🎯 **รูปเดียวกับ `TEAM.md` สองฉบับที่แก้ไปเช้าวันเดียวกัน — เอกสารที่ *หยุดนิ่ง* ไม่ใช่ *ผิดเพี้ยน***
  · ⚠️ **ประโยคที่ประกาศว่า "มีแหล่งเดียว" คือประโยคที่ทำให้ไม่มีใครไปดูแหล่งที่สอง** — ถ้าวันหนึ่ง
    มีแหล่งที่สาม ให้เติมตารางข้างบน **อย่าเขียนว่ามีแหล่งเดียวอีก**
- **อัปเดตทุกครั้งที่ปิดงาน**
  และ **ย้ายรายการออกจากกล่อง 🔴 ทันทีที่เสร็จ** (เคยพลาดมาแล้ว มีคนไล่เช็คซ้ำเสียเวลา)
- กติกาการทำงานกับผู้ใช้ (ภาษา ฐานข้อมูล migration) อยู่ใน `PLAN.md` หัวข้อ 5
