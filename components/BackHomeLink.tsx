/**
 * 🔴 **ไฟล์นี้เหลือไว้เป็น *ทางเข้าเดิม* เท่านั้น — เนื้อจริงย้ายไป `components/BackLink.tsx` แล้ว**
 * (5 ก.ย. 2026 · ตอนผู้ใช้สั่ง *"ทำปุ่มให้สวย ๆ"* แล้วพบว่ามีปุ่มญาติที่ไม่ได้กลับหน้าแรก)
 *
 * 🎯 ***ทางเข้าเดิมยังทำงาน ⇒ ไม่มีใครต้องรีบตามแก้ · แต่มีนิยามเดียวในโลก ⇒ ไม่มีสองใบให้ซิงก์***
 * · ไฟล์นี้ถูก import อยู่ 5 ที่ตอนย้าย — แก้ทั้งห้าในใบเดียวกันก็ได้ **แต่สองในนั้นเป็นไฟล์ของคนอื่น**
 *   ⇒ `re-export` คือทางที่ไม่ต้องแตะไฟล์ใคร (`TEAM.md §3.2`: ทรีเป็นของกลาง แตะไฟล์คนอื่นแล้วกวาดกันเอง)
 */
export { BackHomeLink } from "@/components/BackLink";
