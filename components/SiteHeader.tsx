"use client";

import Link from "next/link";
import type { ReactNode } from "react";
import { E5_COPY } from "@/lib/i18n";

const COPY = E5_COPY.home;

/**
 * **แถบหัวเว็บ — ใบเดียวของทั้งเว็บ** · เจ้าของ: P2-UI/UX · 5 ก.ย. 2026
 *
 * ## 🔴 ทำไมถึงมี — ผู้ใช้เป็นคนสังเกต ไม่ใช่ด่านไหน
 * > *"ปรับ ux ui ในมุมมองมือถือที่ **ฉันสงสัยว่าทำไม header มันหายไป**"* (ภาพหน้าจอ `/explore/kr` ที่ 375px)
 *
 * แถบหัวเคยอยู่ใน `HomeScreen` **ใบเดียว** ⇒ หน้าอื่นทั้งเว็บไม่มีเลย
 * ⇒ กดจากหน้าแรกเข้ามาหน้าเลือกประเทศ/เลือกเมือง แล้ว **โลโก้ ชื่อเว็บ และทางกลับหายไปพร้อมกันทั้งแถบ**
 * 🎯 ***มันไม่ใช่ของที่ "หาย" — มันคือของที่ไม่เคยถูกวางไว้ตั้งแต่แรก และไม่มีอะไรถามว่าหน้าใหม่มีแถบหัวหรือยัง***
 * · 🔴 ไม่มีด่านไหนจับได้ (`tsc` · `lint` · `build` · เทสต์ เขียวมาตลอด) และ **ทำด่านให้จับก็ไม่คุ้ม**:
 *   *"หน้าไหนต้องมีแถบหัว"* ตอบจากโครงสร้างไม่ได้ — หน้ารับคำชวน (`/invite`) **ไม่ควรมี** เพราะคนเปิดยังไม่ใช่สมาชิก
 *   ⇒ ด่านต้องอาศัยทะเบียนรายชื่อหน้า ซึ่ง **แดงใส่ทุกคนที่วางหน้าใหม่ถูกต้องแต่ยังไม่ได้เติมทะเบียน**
 *   (`TEAM.md §3.4`: *ด่านที่ลงโทษการทำตามมัน จะถูกลบทั้งใบ*) ⇒ เลือกทางที่สาม: **จดว่าปลอดภัยเพราะอะไร**
 *   🔴 **วันนี้ปลอดภัยเพราะมีคนเปิดหน้าใหม่ดูด้วยตา — นั่นคือ *นิสัยการทำงาน* ไม่ใช่ *โค้ด* · วันที่นิสัยเปลี่ยน จะไม่มีอะไรส่งเสียง**
 *
 * ## 🔴 **แยกออกมา ไม่ใช่ก๊อป** — และนี่คือเงื่อนไขที่ทำให้ใบนี้ถูก
 * `HomeScreen` ใช้ตัวนี้ด้วย ⇒ มีนิยามเดียวในโลก · 🎯 ***สำเนาที่ต้องมีคนซิงก์ จะล้าเสมอ***
 * · ของฝั่งขวา (ช่องค้นหา · ปุ่มบัญชี) ส่งเข้ามาทาง `children` เพราะ **มันเป็นของหน้านั้น ๆ ไม่ใช่ของแถบ**
 *   ⇒ หน้าที่ไม่มีอะไรจะใส่ ก็ไม่ต้องใส่ · แถบยังเต็มและสมดุลอยู่ (ชื่อเว็บชิดซ้ายตามเดิม)
 */
/**
 * 🔴 **แถบหัวเป็น *แถบของแอป* ไม่ใช่ *แถบทักทาย*** — รื้อ 4 ก.ย. 2026 (ผู้ใช้สั่งเอง: *"Header มันดูกากไปหน่อย"*)
 *
 * ของเดิม: แถบเขียวสูง มี avatar + `สวัสดี คุณ<ชื่อ>` ตัวหนา กับ ⚙️ **ไม่มีชื่อเว็บ ไม่มีโลโก้**
 * 🎯 ***สิ่งที่เด่นที่สุดในหน้าแรกจึงเป็นชื่อผู้ใช้เอง ซึ่งเป็นข้อมูลชิ้นเดียวที่เขารู้อยู่แล้วโดยไม่ต้องเปิดเว็บ***
 * และหัวข้อจริงของหน้า (`แพลนทริปที่จะมาถึง`) ตัวเล็กกว่าคำทักทาย ⇒ ลำดับความสำคัญกลับหัว
 * · ตอนนี้เว็บกำลังจะให้คนแปลกหน้าใช้ **หน้าแรกคือที่แรกที่เขาตัดสินว่านี่ของจริงหรือของเล่น**
 *
 * ✅ แถบหัวถือ **ตัวตนของสินค้า (ซ้าย) + ทางไปบัญชี (ขวา)** · คำทักทายย้ายลงไปเป็นบรรทัดรองใต้หัวข้อ
 * · `sticky` เพราะตอนนี้มันมีของที่ต้องใช้ (กลับหน้าแรก · บัญชี) — แถบที่เลื่อนหายไปคือแถบที่ไม่มีใครใช้
 */
export function SiteHeader({ children }: { children?: ReactNode }) {
  return (
    <header className="focus-ring-on-dark sticky top-0 z-20 bg-pine text-cream shadow-sm shadow-ink/10">
      <div className="mx-auto flex max-w-[110rem] items-center justify-between gap-3 px-4 py-3">
        <Link href="/" className="flex min-w-0 items-center gap-2.5 rounded-lg">
          {/**
           * 🔴 **มาร์กจริงต้องอยู่บน *พื้นครีมทึบ*** — โลโก้เป็นเขียวสน แถบหัวก็เขียวเข้ม ⇒ เขียวบนเขียว มองแทบไม่เห็น
           * ⚠️ ต้นฉบับเป็น jpeg 2048px มี artifact รอบเส้น · **ใช้ที่ 36px มองไม่เห็น แต่ห้ามขยายใหญ่กว่านี้**
           */}
          <span className="flex h-9 w-9 shrink-0 items-center justify-center overflow-hidden rounded-xl bg-cream">
            {/* eslint-disable-next-line @next/next/no-img-element -- ไฟล์ static ใน public/ ที่ทีมวางเอง */}
            <img src="/logo-mark.png" alt="" className="h-7 w-7 object-contain" />
          </span>
          <span className="min-w-0">
            {/**
             * 🔴 **ชื่อสองภาษา บรรทัดเดียวกัน · ตัวอักษรเหมือนกันทุกประการ** (ผู้ใช้สั่งสองรอบ 4 ก.ย. 2026)
             * ⚠️ ฉบับก่อนทำ `ลุยทริป` ให้เล็กและจางกว่า — **ผู้ใช้เห็นแล้วและสั่งทับ · ข้อนั้นตายแล้ว**
             * 🎯 ***เขาไม่ได้อยากได้ "ชื่อกับคำอ่าน" — เขาอยากได้ชื่อเดียวที่เขียนได้สองภาษา***
             * · ไม่มี `<span>` ซ้อน — **ตัวอักษรเดียวกันต้องมาจากที่เดียวกัน ไม่ใช่สองที่ที่ตั้งค่าให้ตรงกัน**
             * · `truncate` อยู่ที่ตัวเดียวกันนี้ ⇒ จอแคบตัดท้ายสุด ไม่ใช่ตัดชื่อหลักทิ้ง
             */}
            <span className="block truncate text-base font-extrabold leading-tight">
              {COPY.brand} {COPY.brandTh}
            </span>
            {/* คำโปรยซ่อนบนมือถือ — ชื่อไทยขึ้นไปอยู่บรรทัดชื่อแล้ว มันจึงเหลือแค่คำอธิบาย */}
            <span className="hidden text-2xs leading-tight text-cream/70 sm:block">
              {COPY.brandTagline}
            </span>
          </span>
        </Link>

        {children}
      </div>
    </header>
  );
}
