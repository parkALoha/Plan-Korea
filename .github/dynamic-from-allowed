# ไฟล์ที่ได้รับอนุญาตให้ส่งชื่อตารางแบบไม่ใช่สตริงตรงเข้า `.from()`
#
# 🔴 รายชื่ออยู่ในไฟล์เพื่อให้ **การเพิ่มไฟล์ = การประกาศว่ามีจุดใหม่ที่ด่านอื่นมองไม่เห็นชื่อตาราง**
#    ต้องมีคน review เห็นใน diff · รูปเดียวกับ `no-policy-tables` · `migration-guard-exempt`
#
# ⚠️ ฉบับแรกของด่านนี้ (P6 · 25 ส.ค. 2026) **ไม่มีไฟล์นี้ โดยผมให้เหตุผลว่า
#    "ชื่อตารางแบบไดนามิกไม่ควรถูกต้องที่ไหนเลย แม้แต่ใน helper เพราะ helper ก็เขียนชื่อตรง"**
#    🔴 **เหตุผลนั้นผิด** — helper ของ `D81` รับพารามิเตอร์เป็น union `EngineTable`
#    แล้วส่งต่อ ซึ่งคือ *ทั้งหมด* ของสิ่งที่ทำให้มันเป็น helper
#    ด่านตัวเองเป็นคนสอนผมข้อนี้ ตอนมันไปเจอไฟล์ที่ P1 เพิ่งเขียน

lib/engine/db.ts
