# ตารางที่ต้อง "ไม่มี policy และไม่มี grant ให้ anon/authenticated" ตลอดไป (P-33)
#
# 🔴 รายชื่ออยู่ในไฟล์เพื่อให้ **การถอดตารางออกจากรายการ = การปิดการป้องกัน ที่มีคน review เห็น**
#    ถ้าฝังไว้ในสคริปต์ การลบบรรทัดเดียวจะกลายเป็นการแก้โค้ดที่ไม่มีใครสังเกต
#
# 🔴 แก้ 3 ก.ย. 2026 (P6 · `Q3` ทับ `D87` ภายในวันเดียว) — `D87` (select+insert 3 ใบ) ถูกแทนที่แล้ว
#    ผู้ใช้ตัดสิน `Q3` ก้าวที่ 1: **route เลิกเขียนแคชทั้งหมด** — เหลือ `grant select` แค่ 2 ตาราง
#    (`place_details_cache` · `place_photo_cache` — อยู่ใน `.github/cache-client-privileges`)
#    `travel_time_cache` **ไม่ได้อยู่ใน `Q3` ด้วย** จึงกลับมาอยู่ในไฟล์นี้เต็มตัวอีกครั้ง
#    ดูเหตุผลเต็มที่ migration `20260902160000_q3_step1_cache_read_only.sql`
#    🎯 บทเรียน: **ไฟล์นี้ล้าได้เร็วกว่าที่คิด** — D87→Q3 ใช้เวลาไม่ถึงวัน เช็คมติปัจจุบันจาก
#    migration ล่าสุดเสมอ อย่าเชื่อว่ารอบแก้ก่อนหน้ายังใช้ได้
#
# ⚠️ `place_details_local_cache` — ถือชื่อเฉพาะบุคคล (`name_local`/`address_local`) ที่แยกออกมา
#    เพราะ ToS ของ Google คนละแบบจากข้อมูลอีก 3 ใบ (`D77`) ยังไม่มีมติให้ client แตะเลยแม้แต่ select
#
# ⚠️ **ถ้าวันหนึ่งมีมติให้ client แตะ (ตารางไหนก็ตามในไฟล์นี้) — เปิด `grant` อย่างเดียวไม่พอ** (P1 · 2 ก.ย. 2026)
#    `route` ที่เขียนตารางนั้นต้องผ่านประตู `catalogPublicMapsQueries()` (`E3-AC6`)
#    เพราะ `maps_query` เป็น **คีย์** และสำหรับสถานที่ที่ผู้ใช้เพิ่มเอง มันคือ **ข้อความที่เขาพิมพ์**
#    — `grant` ที่ถูกต้องกับคีย์ที่รั่ว เป็นคนละชั้นกัน ด่านนี้เห็นแค่ชั้น `grant`/`policy` เท่านั้น

place_details_local_cache
travel_time_cache
