# ตารางที่ต้อง "ไม่มี policy และไม่มี grant ให้ anon/authenticated" ตลอดไป (P-33)
#
# 🔴 รายชื่ออยู่ในไฟล์เพื่อให้ **การถอดตารางออกจากรายการ = การปิดการป้องกัน ที่มีคน review เห็น**
#    ถ้าฝังไว้ในสคริปต์ การลบบรรทัดเดียวจะกลายเป็นการแก้โค้ดที่ไม่มีใครสังเกต
#
# 🔴 แก้ 2 ก.ย. 2026 (P6 · `D87` ③ ผู้ใช้ตัดสิน) — เดิมไฟล์นี้มี 4 ตาราง ตอนนี้เหลือ 1
#    สามใบ (`place_details_cache` · `place_photo_cache` · `travel_time_cache`) ย้ายไป
#    `.github/insert-only-cache-tables` แล้ว เพราะ `D87` อนุญาตให้ `authenticated` select+insert ได้
#    (ทับไม่ได้ ลบไม่ได้) — **ไม่ใช่การปิดการป้องกัน เป็นคำตอบของ `Q3` ที่ค้างมา 8 วัน**
#    ดูเหตุผลเต็มที่ `docs/engine/handoff-2026-09-02-p1.md` หัวข้อ `D87`
#
# ⚠️ `place_details_local_cache` **ไม่ได้อยู่ใน `D87`** — ถือชื่อเฉพาะบุคคล (`name_local`/
#    `address_local`) ที่แยกออกมาเพราะ ToS ของ Google คนละแบบจากข้อมูลอีก 3 ใบ (`D77`)
#    ยังไม่มีมติให้ client แตะ ต้องคง **ไม่มี policy สักตัว** ไว้จนกว่าจะมีมติแยกต่างหาก
#
# ⚠️ **ถ้าวันหนึ่งมีมติให้ client แตะ — เปิด `grant` อย่างเดียวไม่พอ** (P1 · 2 ก.ย. 2026)
#    `route` ที่เขียนตารางนี้ต้องผ่านประตู `catalogPublicMapsQueries()` เหมือนอีกสามใบ (`E3-AC6`)
#    เพราะ `maps_query` เป็น **คีย์** และสำหรับสถานที่ที่ผู้ใช้เพิ่มเอง มันคือ **ข้อความที่เขาพิมพ์**
#    — `grant` ที่ถูกต้องกับคีย์ที่รั่ว เป็นคนละชั้นกัน ด่านนี้เห็นแค่ชั้น `grant`/`policy` เท่านั้น

place_details_local_cache
