# ไฟล์ที่ได้รับยกเว้นจากด่าน "ทุก migration ต้อง assert app.project_identity + ref"
#
# 🔴 รายชื่ออยู่ในไฟล์ ไม่ใช่ใน regex **โดยตั้งใจ** (มติ P1 · 24 ส.ค. 2026)
#    วันที่มีคนเพิ่มไฟล์ที่ 3 เข้ามา **ต้องมีคน review เห็นใน diff**
#    ถ้าซ่อนไว้ใน pattern การขอยกเว้นจะกลายเป็นการแก้โค้ดที่ไม่มีใครสังเกต
#
# รูปแบบ: ชื่อไฟล์ล้วน บรรทัดละ 1 ชื่อ · `#` = คอมเมนต์ · บรรทัดว่างข้าม
# ⚠️ ชื่อที่อยู่ในนี้แต่ไม่มีไฟล์จริงแล้ว = ด่านจะแดง (ยกเว้นที่อายุยืนกว่าไฟล์ คือของค้างเงียบ)

# ── ① สร้าง schema `app` เอง — assert marker ที่ตัวเองยังไม่ได้สร้างไม่ได้ (ไก่กับไข่)
#    ด่านของมันคือ "`public` ต้องยังไม่มีตารางเลย" ซึ่งเป็นสิ่งเดียวที่มันตรวจได้แล้วสร้างเองไม่ได้
20260824043822_identity.sql

# ── ② เป็นตัว **สร้าง** `app.project_identity` เอง (D48)
# 🔴 หมายเหตุสำคัญ: ไฟล์นี้ **ผ่านด่านแบบสตริงอยู่แล้ว** เพราะมันมีทั้งคำว่า
#    `app.project_identity` และ ref อยู่ในตัว — **แต่มีเพราะมัน `create`/`insert` ไม่ใช่เพราะมัน assert**
#    ยกเว้นไว้เพื่อให้ข้อเท็จจริงนั้น **มองเห็นได้** ไม่ใช่ผ่านไปโดยบังเอิญ
#    ⚠️ อย่าลบบรรทัดนี้ทิ้งเพราะเห็นว่า "ไม่จำเป็น" — มันจำเป็นในเชิงความหมาย ไม่ใช่เชิงผลลัพธ์
20260824220618_project_identity_guard.sql
