# ไฟล์ที่อนุญาตให้มีรูป `//` + ไวลด์การ์ดถึงท้ายบรรทัด (หนึ่งพาธต่อบรรทัด)
#
# 🔴 ใส่ที่นี่ได้ต่อเมื่อ **พิสูจน์แล้วว่าอินพุตไม่มีทางมี URL อยู่เลย**
#    ถ้าไม่แน่ใจ = มีทาง · ใช้ของกลางแทน
# ว่างอยู่โดยตั้งใจ — วันที่มีรายการแรก ต้องมีเหตุผลกำกับบรรทัดนั้น
