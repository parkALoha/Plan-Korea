# ตารางที่ **เข้าถึงได้จากชั้น data-access เท่านั้น** (ไฟล์ในรายการ dynamic-from-allowed)
#
# 🔴 ทำไมต้องบังคับ: `D81` ผูก predicate ไว้กับตารางพวกนี้
#    `catalog_places` ต้องพก `not picker_hidden` เสมอ (`D81` ③.๖)
#    → ถ้ามีใครเรียก `supabase.from("catalog_places")` ตรง ๆ **predicate จะหายไปเงียบ ๆ**
#      และไม่มีอะไรฟ้อง เพราะคิวรีที่ลืม predicate ก็ยังคืนแถวได้ปกติ — แค่คืนแถวที่ไม่ควรเห็น
#
# 📌 เกณฑ์การใส่ชื่อลงที่นี่ (นิยามเป้าหมายที่ P1 แก้เอง 25 ส.ค. 2026):
#    **"ทุกอย่างที่ต้องพก predicate ของคลัง ลอด helper"** — ไม่ใช่ "ทุกอย่างลอด helper"
#    ตารางที่ไม่มี predicate ผูกอยู่ การบังคับให้ลอด helper ไม่ได้ให้อะไรนอกจากราคา
#
# ⚠️ `trip_stops` **ยังไม่ใส่** ทั้งที่ `D81` ③/③.๕ ผูก `deleted_at is null` + `order by rank, id` ไว้กับมัน
#    เหตุผล: มี 7 จุดในโค้ดเว็บทริปที่ freeze อยู่ (`hooks/usePlans.ts` 2 · `hooks/useStops.ts` 5)
#    ใส่วันนี้ = แดง 7 จุดในโค้ดที่ห้ามแตะก่อนทริป 11 ต.ค. · **`E3-AC1` จะไล่รายการนี้อยู่แล้ว**
#    🔴 วันที่ย้าย 7 จุดนั้นเข้า helper เสร็จ **ให้เพิ่ม `trip_stops` ที่นี่ทันทีในคอมมิตเดียวกัน**

catalog_places
