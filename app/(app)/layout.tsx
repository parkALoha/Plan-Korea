import type { ReactNode } from "react";
import { BottomNav } from "@/components/BottomNav";

/**
 * โครงร่วมของ 3 หน้าหลัก — `/` · `/today` · `/summary` (เฟส B1 · 4 ก.ย. 2026)
 *
 * `<BottomNav />` เคยถูกเรียกซ้ำเองในทั้งสามหน้า (`page.tsx:539` · `today:950` · `summary:831`)
 * ⇒ เพิ่มหน้าใหม่แล้วลืมใส่ ก็ไม่มีอะไรฟ้อง · แก้แถบเมนูแล้วต้องไปตรวจว่าครบสามที่ไหม
 *
 * 🔴 **เหตุผลที่ต้องเป็น route group `(app)` ไม่ใช่ใส่ลง `app/layout.tsx` ตรง ๆ:**
 *    `/unlock` (ด่าน PIN) **ต้องไม่มีแถบเมนู** — คนที่ยังไม่ผ่านด่านกดลิงก์พวกนั้นก็โดนเด้งกลับ
 *    มาที่ `/unlock` อยู่ดี (`proxy.ts:32`) ได้แถบที่ทุกปุ่มพาไปที่เดิม = สัญญาว่าไปได้ทั้งที่ไปไม่ได้
 *    วงเล็บทำให้โฟลเดอร์นี้ไม่ปรากฏใน URL — `/` `/today` `/summary` เหมือนเดิมทุกตัวอักษร
 *
 * ⚠️ **`MapsApiProvider` ยังต้องอยู่ที่ `(app)/page.tsx` เหมือนเดิม ห้ามยกขึ้นมาที่นี่**
 *    SDK ของ Google Maps ~840KB และมีหน้าเดียวที่ใช้จริง — `app/layout.tsx:35-41` เขียนเหตุผล
 *    ไว้ยาวแล้วตอนย้ายมันลงไป ถ้ายกขึ้นมาตรงนี้ `/today` กับ `/summary` จะโหลดทิ้งทั้งคู่
 *
 * 📌 หัวเว็บ **ยังไม่ยุบ** เพราะทั้งสามหน้าต่างกันจริง (หน้าแผนมีชื่อทริป/แผน/ตั้งค่า · `/today`
 *    มีนาฬิกาและตัวเปลี่ยนวัน · `/summary` มีสลับภาษา/โหมด ตม./พิมพ์) — ที่เหมือนกันมีแค่
 *    ลิงก์ข้ามหน้ากับปุ่มธีม ซึ่งจะรวมเป็นคอมโพเนนต์ร่วมแยกต่างหาก ไม่ใช่ยัดลง layout
 */
export default function AppLayout({ children }: { children: ReactNode }) {
  return (
    <>
      {children}
      <BottomNav />
    </>
  );
}
