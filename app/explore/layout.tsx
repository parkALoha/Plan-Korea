import type { ReactNode } from "react";
import { SiteNav } from "@/components/SiteNav";

/**
 * Layout ของ `/explore/...` — **แถบเมนูล่างอยู่ที่นี่ ไม่ใช่ในหน้า** · 5 ก.ย. 2026
 * เจ้าของ: P2-UI/UX · **ข้อเสนอเป็นของ P5** (เขาเสนอสำหรับ `SiteNav` · ผมขยายให้ครอบ `SiteHeader` ด้วย)
 *
 * ## 🔴 **แถบหัวเว็บเคยอยู่ที่นี่ 1 ชั่วโมง แล้วผู้ใช้สั่งถอด — เก็บรอยไว้ ไม่ใช่ลบทิ้ง**
 * ```
 * เขาทักว่า  "ทำไม header มันหายไป"       → ผมใส่ให้ทุกหน้า
 * เขาตอบกลับ "เอาออก มันรก แต่ให้ปรับปรุงหน้านั้นให้ดี"
 * ```
 * 🎯 ***คำถาม "ทำไมมันหายไป" ไม่ได้แปลว่า "เอามันมา" — มันแปลว่า "หน้านี้ดูไม่เหมือนส่วนหนึ่งของเว็บ"***
 *    แถบหัวเป็น *คำตอบหนึ่ง* ที่ผมเดาเอง และเป็นคำตอบที่ผิดสำหรับหน้าที่เนื้อหาคือ **ตะแกรงรูปเต็มจอ**
 * 🔴 **ห้ามเอา `SiteHeader` กลับมาที่นี่โดยไม่มีคำสั่งใหม่จากผู้ใช้** — เขาเห็นทั้งสองแบบแล้วและเลือกแบบนี้
 * · ตัวตนของเว็บบนหน้านี้มาจาก **ปุ่มกลับหน้าหลัก + หัวข้อ + การ์ดชุดเดียวกับหน้าแรก** ไม่ใช่จากแถบสีเขียว
 *
 * 🎯 **P5 ชี้ทางที่สามจาก `TEAM.md §3.4` เอง: *"ชั้นตายเมื่อเอาคำถามออก"***
 * ```
 * ก่อน   ทุกหน้าใต้ /explore ต้อง "จำ" ว่าต้องเรนเดอร์ SiteNav เอง
 *        ⇒ คำถาม "หน้าใหม่ใส่ครบหรือยัง" **มีอยู่จริงทุกครั้งที่มีคนวางหน้า**
 * หลัง   layout ให้เอง ⇒ **คำถามหายไปทั้งข้อ ไม่ต้องมีอะไรมาตรวจ**
 * ```
 * · และมันตอบเคสยกเว้นได้โดยไม่ต้องมีทะเบียน: `/invite` **ไม่อยู่ใต้ layout นี้** ⇒ ไม่มีแถบ โดยไม่มีใครต้องเขียนข้อยกเว้น
 *   หน้าในทริปมี `layout` ของตัวเองอยู่แล้ว ⇒ ได้ `BottomNav` ตามเดิม
 *
 * ## ⚠️ ขอบเขต — **P5 เขียนข้อจำกัดนี้มาเองพร้อมข้อเสนอ และมันถูก**
 * `/` (`HomeScreen`) กับ `/account` **ไม่ได้อยู่ใต้ `/explore`** ⇒ ยังเรนเดอร์แถบเอง
 * ⇒ ***เอาคำถามออกได้เฉพาะกลุ่มนี้ ไม่ใช่ทั้งเว็บ***
 * 🔴 **และที่ยังคุ้มคือเพราะ `/explore` เป็น *กลุ่มที่จะโตต่อ* ไม่ใช่เพราะวันนี้มันลดโค้ดได้ 2 บรรทัด**
 *    (สองหน้าเดี่ยวนั้นไม่มีใครต้องจำอะไรซ้ำ — ที่ที่คำถามถูกถามซ้ำคือกลุ่มที่มีหน้าเพิ่มเรื่อย ๆ)
 *
 * ## 📌 ไม่ใช่ `"use client"` โดยตั้งใจ
 * ตัวมันเองไม่มี state/effect — `SiteHeader`/`SiteNav` เป็น client component ของมันเองอยู่แล้ว
 * ⇒ layout เป็น server component ได้ **และควรเป็น** เพราะไม่มีเหตุให้ลาก `children` ทั้งหมดเข้าฝั่ง client
 */
export default function ExploreLayout({ children }: { children: ReactNode }) {
  return (
    <>
      {children}
      <SiteNav />
    </>
  );
}
