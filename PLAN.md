# แผนพัฒนา — เว็บแพลนเที่ยวเกาหลี

> ไฟล์นี้คือแหล่งความจริงเรื่อง "ตอนนี้ทำถึงไหน / ต่อไปทำอะไร / ทำไมตัดสินใจแบบนั้น"
> อ่านไฟล์นี้ก่อนเริ่มงานใหม่ทุกครั้ง และอัปเดตทุกครั้งที่ปิดเฟส
>
> ทริปจริง: **11–21 ต.ค. 2026** (บินไป 11 · เที่ยวเกาหลี 12–20 · บินกลับ 21) · แผนนี้เขียนเมื่อ **6 ส.ค. 2026** (เหลือ ~2 เดือน)

---

## 1. เว็บนี้ต้องทำอะไรได้ (เป้าหมาย)

เว็บตัวเดียวที่ทำงาน **2 โหมด** สำหรับคน 2 คน:

| โหมด | ใช้เมื่อไหร่ | ต้องทำอะไรได้ |
|---|---|---|
| **วางแผน** | ตอนนี้จนถึงก่อนบิน | เลือกที่เที่ยว/ร้านอาหาร จัดลำดับแต่ละวัน ดูว่าเวลาพอไหม เก็บตั๋ว/booking เตรียมของ |
| **ใช้จริง** | ระหว่างอยู่เกาหลี | เปิดมือถือแล้วรู้ทันทีว่าตอนนี้ต้องไปไหน นำทางได้เลย ดูตั๋วของวันนี้ได้ |

**เกณฑ์คุณภาพที่ตกลงกัน:** สวย น่าใช้งาน และใช้ได้ดีจริงทั้งบนมือถือและคอมพิวเตอร์

### ต้องมี (ตกลงแล้ว)
- เวลาเดินทางของจริง ไม่ใช่ตัวเลขเดา
- แผนที่รวมเส้นทางทั้งวัน
- ร้านอาหาร / การวางมื้อ
- เวลาเปิด-ปิดของสถานที่
- การเดินทางระหว่างเมือง (กรอกเองจากที่จองจริง)
- Checklist ของที่ต้องเตรียม
- เก็บ booking/ตั๋วไว้ที่เดียว **อัปโหลดรูป/PDF ได้**
- หน้าแยก "วันนี้" เน้นมือถือ สำหรับใช้ตอนเที่ยวจริง

### ไม่ทำ (ตัดสินใจแล้ว อย่าย้อนกลับมาทำโดยไม่คุยก่อน)
- **งบประมาณ/ค่าใช้จ่าย** — ไม่ต้องการ ตัดออกเพื่อคุม scope
- **ระบบออฟไลน์ / PWA** — มี eSIM/pocket wifi ตลอดทริปแน่นอน ไม่คุ้มกับความซับซ้อน
- **ระบบล็อกอิน** — ตั้งใจให้ใครมีลิงก์ก็แก้ได้ ใช้กัน 2 คนที่ไว้ใจกัน

---

## 2. สถานะปัจจุบัน (ตรวจจากโค้ดจริง 6 ส.ค. 2026)

**Stack:** Next.js 16 (App Router) + TypeScript + Tailwind v4 + Supabase (anon key ตรงจาก browser, RLS สาธารณะ) + Google Places API (New) ผ่าน API route ฝั่งเซิร์ฟเวอร์ + dnd-kit · deploy บน Vercel (`korea-trip-plan`)

### ที่ทำงานได้จริงแล้ว
- ระบบ **"แผน" หลายเวอร์ชัน** — สร้าง/ก๊อป/เปลี่ยนชื่อ/ลบ ทำแผน A/B เทียบกันได้ (`trip_plans` + `trip_meta`)
- **ที่พักต่อ leg** — จับวันที่นอนเมืองเดียวกันติดกันมัดเป็น leg อัตโนมัติจาก `data/itinerary.ts` ค้นหาโรงแรมจาก Google ได้
- **จุดแวะต่อวัน** — ลากจัดลำดับ / ลากข้ามวัน / ลากกลับคืนคลัง (`trip_stops`)
- **Timeline เวลาถึง-ออก** — คำนวณจากเวลาเริ่มวัน + เวลาเดินทาง + เวลาที่อยู่แต่ละจุด
- **คลังสถานที่** — 39 ที่คัดสรรใน `data/places.ts` + เพิ่มเองผ่าน Google Places + ซ่อนที่ไม่เอา + รูปจาก Google + แผนที่ฝัง + คลิป YouTube
- ตั๋ว/booking + checklist ของเตรียม + เดินทางระหว่างเมืองกรอกเอง (เฟส 5)
- **หน้า "วันนี้" (`/today`)** สำหรับใช้จริงตอนเที่ยว — จุดถัดไปเด่น + นำทาง 3 แอป + ติ๊กมาถึงแล้ว + เลื่อน timeline อัตโนมัติเมื่อช้ากว่าแผน (เฟส 6)
- **หน้าสรุปแผน (`/summary`)** — ดูอย่างเดียวทั้งทริป แก้อะไรไม่ได้ · **ล็อกวันที่ลงตัวแล้ว** กันเผลอลากตอนเลื่อนดู · แถบเมนูล่างบนมือถือสลับ 3 หน้า
- ทุกอย่าง **sync สดระหว่าง 2 คน** ผ่าน Supabase Realtime · `npm run lint` สะอาด

**ทั้ง 6 เฟสตามแผนเดิมเสร็จหมดแล้ว (7 ส.ค. 2026)** — เว็บใช้งานได้ครบตามเป้าหมายข้อ 1 ทุกข้อ เหลือแค่ปรับแต่งจากการใช้งานจริงเป็นเคสๆ ไป

### หนี้/ปัญหาที่ต้องเคลียร์
1. ~~เวลาเดินทางทุกตัวเป็นของเดา~~ — แก้แล้ว (เฟส 1) ขนส่งสาธารณะเป็นของจริงจาก Routes API, เดิน/แท็กซี่ยังเป็นประมาณการ (ดูข้อจำกัดด้านล่าง)
2. ~~งานทั้งหมดยังไม่ commit~~ — แก้แล้ว (เฟส 0)
3. ~~โค้ดตาย 5 ไฟล์~~ — ตรวจแล้ว (7 ส.ค. 2026) ไม่มีอยู่จริงในโค้ดแล้ว
4. ~~README ล้าสมัย~~ — ตรวจแล้ว (7 ส.ค. 2026) ไม่มีข้อความล้าสมัยแบบที่บันทึกไว้
5. ~~migration `0009_trip_stops_replica_identity.sql`~~ — รันแล้ว (7 ส.ค. 2026)
6. ~~migration `0010_travel_time_cache.sql`~~ — รันแล้ว ยืนยันด้วย curl (6 ส.ค. 2026)

**หนี้รอบใหม่ (จากการตรวจโค้ดทั้งโปรเจกต์ 7 ส.ค. 2026):** พบบั๊ก 14 ตัว + ปัญหา performance ที่ `lint`/`tsc` จับไม่ได้
เฟส 7-9 (แก้บั๊กเวลา/ลำดับจุดแวะ/แคช+performance ทั้งหมด) **แก้เสร็จแล้ว (7 ส.ค. 2026)** เหลือลบ test data
1 แถวใน DB ที่รอผู้ใช้อนุมัติ + เฟส 10-12 (backup/print/rate-limit และงานเสริม) ที่ยังไม่เริ่ม
→ รายละเอียดทั้งหมดอยู่ที่ **[หัวข้อ 3.5 — เฟส 7-12](#35-รอบตรวจโค้ดทั้งโปรเจกต์-7-สค-2026--เฟส-7-12)** ด้านล่าง

### ⚠️ ข้อจำกัดสำคัญ (ยืนยันแล้วด้วยการทดสอบจริง 6 ส.ค. 2026): Google นำทางในเกาหลีใต้ได้ไม่ครบ
กฎหมายเกาหลีห้ามส่งออกข้อมูลแผนที่ละเอียดไปเซิร์ฟเวอร์ต่างประเทศ Google จึงให้บริการได้แค่ **เส้นทางขนส่งสาธารณะ (TRANSIT)** ส่วน **DRIVE / WALK คืนค่าว่างจริงตามที่คาด** ในเกาหลี

**ผลกระทบ:** ปุ่มเลือกโหมด "เดิน 🚶 / แท็กซี่ 🚕" จะเห็นเวลาเป็นค่าประมาณการ (ติดป้าย "(ประมาณการ)" ใน UI) — **ตัดสินใจแล้ว (6 ส.ค. 2026): ไม่เพิ่ม ODsay/Kakao/Naver** เพราะสมัครยาก (ต้องยืนยันตัวตนแบบเกาหลี) ยอมรับสภาพนี้ไปก่อน ถ้าจำเป็นจริงๆ ค่อยกลับมาคุยใหม่ อย่าเพิ่มเองโดยไม่คุยก่อน

---

## 3. แผนงานเป็นเฟส

### เฟส 0 — กันของหาย + เคลียร์พื้น ✅ เสร็จ (7 ส.ค. 2026)
เล็กแต่สำคัญที่สุด เพราะตอนนี้งาน 2 วันเต็มยังไม่มี backup

- [x] `git add` ทั้งหมด + commit + push ขึ้น remote (`github.com/parkALoha/Plan-Korea`)
- [x] ลบโค้ดตาย 5 ไฟล์ (ดูรายการข้อ 3 ด้านบน)
- [x] เขียน `README.md` ใหม่ให้ตรงกับความจริง แล้วชี้มาที่ไฟล์นี้เป็นแหล่งแผนงาน
- [x] รัน migration `0009` แล้ว (แก้บั๊ก DELETE realtime ไม่ sync)

---

### เฟส 1 — เวลาเดินทางของจริง ✅ เสร็จ (6 ส.ค. 2026)
Timeline ทั้งทริปตอนนี้เชื่อไม่ได้ ทุกฟีเจอร์ที่พึ่งเวลา (เตือนสถานที่ปิด, หน้า "วันนี้") จึงต้องรอเฟสนี้

**ต้องให้ผู้ใช้ทำก่อน:** เปิด **Routes API** ใน Google Cloud Console (โปรเจกต์เดียวกับคีย์ปัจจุบัน)

- [x] ยิงทดสอบจริง 3 โหมด × คู่จุดจริงในทริป (ในปูซาน) 6 ส.ค. 2026: **ขนส่งสาธารณะ = ของจริง** (มิเป่→หาดแฮอึนแด 41 นาที ยืนยันด้วย curl ตรงไปที่ `travel_time_cache`) · **เดิน/แท็กซี่ = ว่าง** (`isReal:false`) ตรงกับที่คาดไว้เรื่องข้อจำกัดกฎหมายแผนที่เกาหลี
- [x] `lib/travelProvider.ts` — interface เดียวสำหรับคำนวณเวลาเดินทาง สลับ backend ได้
- [x] เขียน `/api/travel-time` ใหม่ด้วย `routes.googleapis.com/directions/v2:computeRoutes` — ลบ route เดิมที่เรียก legacy API
- [x] แคชผลถาวรใน Supabase (ตาราง `travel_time_cache`, migration `0010`) — ยืนยันแล้วว่าเขียน/อ่านได้จริง
- [x] **UI แยกให้ชัดว่าตัวเลขไหนของจริง ตัวเลขไหนประมาณการ** — `DayStopsSection` ติดป้าย "(จริง)" / "(ประมาณการ)" แล้ว
- [x] **ตัดสินใจแล้ว (6 ส.ค. 2026): ไม่เพิ่ม ODsay/Kakao/Naver ตอนนี้** — สมัครยาก (ต้องยืนยันตัวตนแบบเกาหลี) ยอมรับว่าเดิน/แท็กซี่เป็นประมาณการไปก่อน ถ้าจำเป็นจริงๆ ค่อยกลับมาคุยใหม่ อย่าเพิ่มเองโดยไม่คุยก่อน

**เฟส 1 เสร็จแล้ว** ✅

---

### เฟส 2 — ร้านอาหาร + เวลาเปิด-ปิด ✅ เสร็จ (7 ส.ค. 2026)
ใช้ Places API (New) ที่เปิดอยู่แล้ว ไม่ต้องเปิด API เพิ่ม

- [x] ตารางใหม่ `place_details_cache` (migration `0011`) — resolve แต่ละสถานที่เป็น Google place ID ครั้งเดียว เก็บพร้อม `regularOpeningHours` ผ่าน `/api/place-details`
- [x] แสดงเวลาเปิด-ปิดบนการ์ดสถานที่ (`PlaceCard` — เวลาของวันที่โฟกัสอยู่จริงๆ) และในหน้ารายละเอียด (`PlaceDetailModal` — ลิสต์ครบ 7 วัน)
- [x] **เตือนใน timeline ถ้าเวลาที่คำนวณได้ตกนอกเวลาเปิด** — `lib/openingHours.ts` (`isOpenDuring`) + `DayStopsSection` แสดงป้ายเตือนใต้แถว ทดสอบจริงแล้วเห็นป้ายเตือนทำงานถูกต้อง
- [x] เพิ่มหมวด "ร้านอาหาร" ในคลัง — ปุ่ม "🍽️ ร้านใกล้ๆ" เปิด `NearbyRestaurantsModal` ยิง Nearby Search (New) รอบจุดแวะล่าสุดของวันนั้น (หรือที่พัก/จุดกึ่งกลางเมืองถ้ายังไม่มีจุดแวะ) ทดสอบจริงแล้วเพิ่มร้านลงวันได้
- [x] **วางมื้ออาหาร** — ใช้ `trip_stops` เดิม เพิ่มหมวด `restaurant` แล้ว หัวการ์ดแต่ละวันโชว์ "🍽️ วางมื้อไว้ N มื้อ" หรือเตือนถ้ายังไม่มี

migration `0011_place_details_cache.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `place_details_cache` (7 ส.ค. 2026)

**ส่วนเสริมหลังปิดเฟส (7 ส.ค. 2026) — ตามฟีดแบ็กจากการใช้งานจริง:**
- [x] ร้านอาหารโชว์เรทติ้ง/จำนวนรีวิว/ประเภทอาหาร + กดเข้าไปดูรีวิว 3 อันล่าสุด/รูปภาพ/เวลาเปิด-ปิดครบก่อนตัดสินใจเพิ่มได้ (migration `0012` เพิ่มคอลัมน์ `rating`, `user_rating_count`, `primary_type`, `reviews` ใน `place_details_cache`)
- [x] "+ เพิ่มสถานที่เอง" มี autocomplete แนะนำระหว่างพิมพ์แล้ว (เหมือนช่องค้นหาที่พัก) ไม่ต้องกดค้นหาแล้วเลือกจากลิสต์เท่านั้น
- [x] มีช่องค้นหาร้านด้วยชื่อใน `NearbyRestaurantsModal` เผื่อร้านอยู่นอกรัศมี Nearby Search หรือรู้ชื่อเป๊ะๆ อยู่แล้ว
- [x] โน้ตสั้นๆ ต่อจุดแวะ (migration `0013` เพิ่มคอลัมน์ `trip_stops.note`) — กด "+ โน้ต" ใต้แต่ละจุดเพื่อจดข้อความอิสระ เช่น "ร้านนี้อร่อย รีบไป"

migration `0012_place_details_extra.sql` และ `0013_trip_stop_notes.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `place_details_cache`/`trip_stops` (7 ส.ค. 2026)

---

### ส่วนเสริม — ตารางบินจริง + วันเดินทาง + คืนที่ยังเลือกเมืองได้ ✅ เสร็จ (7 ส.ค. 2026)

เดิมแพลนเริ่มที่วันแรกในปูซานเลย ไม่มีวันบิน ทำให้ไม่รู้ว่าต้องออกจากที่ไหนกี่โมง และช่วงพักเครื่องฮานอย 11 ชม. หายไปทั้งก้อน

- [x] `Day.events` (`DayEvent[]`) ใน `data/itinerary.ts` — เที่ยวบิน/เดดไลน์ที่จองมาแล้ว แก้ในเว็บไม่ได้ แสดงเป็นแถบแยกเหนือจุดแวะ (`DayEventsPanel` ใน `DayStopsSection`) ไม่ปนกับจุดแวะที่ลากจัดลำดับได้
- [x] **วัน `d0` (11 ต.ค.)** — VN610 สุวรรณภูมิ 11:55 → ฮานอย 13:55 · พักเครื่อง 11 ชม. 20 น. · VN428 ฮานอย 01:15 → กิมแฮ 07:05 · พร้อมเดดไลน์ "ออกจากเมืองเก่า 22:00"
- [x] **วัน `d10` (21 ต.ค.)** — เช็คเอาต์ 05:45 · AREX → ICN · VN409 อินชอน 10:35 → โฮจิมินห์ 13:45 · ต่อเครื่อง 3 ชม. 5 น. · VN607 → สุวรรณภูมิ 18:30
- [x] เมืองใหม่ **ฮานอย** ในคลังสถานที่ (7 ที่ในย่านเมืองเก่า เดินถึงกันหมด) — ใช้ระบบเดิมทั้งหมด (เวลาเปิด-ปิด/ร้านใกล้ๆ/เพิ่มเอง ใช้ได้ปกติ)
- [x] `Day.noHotel` — วันบิน/นอนบนเครื่องไม่ถูกนับเป็น leg ที่พัก (`deriveHotelLegs` ข้ามให้) leg เดิมทั้ง 4 ก้อนและโรงแรมที่ตั้งไว้แล้วไม่กระทบ
- [x] `Day.overnightOptions` + `trip_meta.overnight_overrides` (migration `0014`) — **คืน 16 ต.ค. สลับระหว่างคังนึง (ที่จองไว้จริง) กับซกโชได้จากหัวการ์ดวันนั้น** สลับแล้ว leg ที่พักคำนวณใหม่ให้เอง sync สดทั้ง 2 คน · ของเดิมไม่ถูกลบ สลับกลับได้ตลอด · migration รันแล้ว (7 ส.ค. 2026)
- [x] **`DayEvent.anchor` ("before"/"after")** — วันที่มีช่วงว่างจริงระหว่างเที่ยวบิน (เช่นพักเครื่องฮานอย) แทรกจุดแวะเข้าไปในช่วงนั้นได้เหมือนวันปกติทุกอย่าง เวลาบินเองแก้ไม่ได้ แต่ตารางจุดแวะจะ**เริ่มนับอัตโนมัติจากเวลาที่เหตุการณ์ anchor "before" จบ** (เช่น ถึงย่านเมืองเก่า 15:30) แทนช่อง "ออกเดินทาง" ที่ตั้งเองได้ปกติ และมีป้ายเตือนถ้าจุดแวะที่วางไว้จบช้ากว่าเดดไลน์ anchor "after" (เช่น ต้องออกไปขึ้นเครื่อง 22:00) — วัน `d0` ใช้ฟีเจอร์นี้กับช่วงพักเครื่องฮานอย ทดสอบเพิ่ม/ลบจุดแวะจริงในเบราว์เซอร์แล้ว

**ผู้ใช้ต้องทำ:** ไม่มี — migration `0014` รันแล้ว

---

### เฟส 3 — แผนที่รวมเส้นทางทั้งวัน ✅ เสร็จ (7 ส.ค. 2026)
ผู้ใช้เปิด **Maps JavaScript API** บนคีย์เดิม `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` ใน Google Cloud Console แล้ว ยืนยันด้วยการทดสอบจริงในเบราว์เซอร์ — เห็นแผนที่ Busan จริงพร้อมหมุดเลข 1-2-3 เรียงตามลำดับจุดแวะถูกต้อง

- [x] แผนที่ต่อวัน: หมุดเลข 1-2-3 ตามลำดับจุดแวะ + เส้นเชื่อม + หมุดโรงแรมคนละสี — `components/DayMapPanel.tsx` ใช้ `@vis.gl/react-google-maps` (`Marker` ทรงกลมสี + label ตัวเลข/🏨 + `Polyline`) มี fallback ข้อความเมื่อไม่มีคีย์ **หมายเหตุ:** ตอนแรกใช้ `AdvancedMarker`+`Pin`+`mapId="DEMO_MAP_ID"` แต่เจอ `ApiProjectMapError` เมื่อมีหลายแผนที่พร้อมกันบนหน้าเดียว (DEMO_MAP_ID ไม่รองรับการใช้งานที่ scale หลาย instance) เปลี่ยนมาใช้ `Marker` แบบคลาสสิกกับ `google.maps.SymbolPath.CIRCLE` แทน ไม่ต้องพึ่ง Map ID เลย แก้ปัญหาได้
- [x] กดหมุด → เลื่อนไปแถวนั้นในลิสต์ / กดแถว → หมุดเด้ง (สองทาง) — `activeStopId` ใน `DayStopsSection` ผูกทั้งสองทาง ทดสอบฝั่งกดชื่อในลิสต์แล้วไฮไลต์ ring ติดแถวถูกต้อง (ฝั่งกดหมุดใช้ handler เดียวกัน `onSelectStop`)
- [x] มือถือ: แท็บสลับ "ลิสต์ / แผนที่" · คอม: วางคู่กันซ้าย-ขวา — ทดสอบแล้วทั้งจอมือถือ (375px) และจอใหญ่ (1400px)
- [x] จำกัด key ด้วย HTTP referrer ให้เฉพาะโดเมน Vercel + localhost — ผู้ใช้ตั้งค่าใน Google Cloud Console แล้ว

---

### เฟส 4 — รื้อ UI ให้สวยและ mobile-first ✅ เสร็จเกือบหมด (7 ส.ค. 2026)
เดิมทั้งเว็บอยู่ใน `app/page.tsx` ไฟล์เดียว 561 บรรทัด และเป็น desktop-first (มือถือใช้ได้แต่ไม่ได้ถูกออกแบบมา) — ตอนนี้แตกโครงแล้ว, มือถือมี bottom sheet, คอมใช้พื้นที่จอกว้างขึ้น, มี skeleton ตอนโหลด, ปุ่มมี feedback เหลือแค่ "การเปลี่ยนหน้าลื่น" ที่ตั้งใจข้ามไปก่อนเพราะยังไม่มี route ให้เปลี่ยนจริง (รอเฟส 6)

**รอบแก้ของพัง (7 ส.ค. 2026) — ตรวจของจริงในเบราว์เซอร์ที่ 360 / 375 / 768 / 1350px:**
- [x] **ทั้งเว็บล้นออกนอกจอมือถือ (กว้าง 559px บนจอ 375px เลื่อนซ้ายขวาได้)** — ต้นเหตุคือการ์ดที่พังใน `HotelLegsPanel` เป็น `flex flex-wrap` ซึ่ง flex item กว้างตามเนื้อหา `truncate` ข้างในเลยไม่มีผล ชื่อโรงแรมจาก Google ที่พ่วงที่อยู่เต็มมาด้วยจึงดันหน้าเว็บ · แก้เป็น `grid` (1 คอลัมน์มือถือ / 2 คอลัมน์ ≥`sm`) + `min-w-0` → ล้นแนวนอนเหลือ 0 ทุกความกว้าง
- [x] **ชื่อสถานที่ในแถวจุดแวะเหลือ 74px จาก 341px จนอ่านไม่ออก** — แถวเดียวยัด 5 อย่าง (จับลาก/เวลา/ชื่อ/ปรับเวลา/ลบ) · มือถือยกปุ่มปรับเวลา+ลบลงไปอยู่แถวโน้ตที่มีอยู่แล้ว (ไม่เพิ่มความสูง) → **ชื่อได้ 225px** · จอ `sm` ขึ้นไปยังเป็นแถวเดียวเหมือนเดิม
- [x] **ปุ่ม +/− เวลาที่อยู่เล็กเกินนิ้ว** — 20×18 → **36×36** บนมือถือ (คอม 28×28) พร้อมกวาดปุ่มเล็กอื่นทั้งเว็บ: เลือกโหมดเดินทาง 23→31px · ชื่อสถานที่ 24→36px · "+ แทรกร้านอาหาร" 17→33px · เลือกเมืองนอน 24→30px → **ปุ่มที่เล็กกว่า 28px เหลือ 0**
- [x] ช่องพิมพ์โน้ตแคบจนพิมพ์ไม่ได้จริง — มือถือให้ช่องพิมพ์กินเต็มบรรทัด ปุ่มบันทึก/ยกเลิก/ลบ ตกลงบรรทัดล่าง
- [x] ปุ่มลอย "📍 สถานที่" ทับปุ่ม "+ เพิ่มสถานที่" ของวันสุดท้าย — เพิ่ม `pb-28` ให้เนื้อหา + ปุ่มลอยเผื่อ `env(safe-area-inset-bottom)` ของ iPhone
- [x] ที่อยู่โรงแรมกิน 2-3 บรรทัดในหัวการ์ดวัน — ตัดเหลือบรรทัดเดียว

**เหลือทำ (งานรื้อโครง):**
- [x] **แตก `app/page.tsx` เป็นโครงที่อ่านออก** (7 ส.ค. 2026) — ลดจาก 561 → 356 บรรทัด แยกออกเป็น: `components/TripHeader.tsx` (ส่วนหัว + ตัวสลับแผน), `hooks/useLegacyBootstrap.ts` (bootstrap จากระบบ slot เดิม), `hooks/useHotelSchedule.ts` (hotelLegs/hotelForDay/hotelBeforeDay), `hooks/useTripDnd.ts` (sensors + drag handlers ทั้งหมด) — ย้ายโค้ดตรงๆ ไม่เปลี่ยนพฤติกรรม ผ่าน `npm run lint` + `tsc --noEmit` สะอาด ทดสอบจริงในเบราว์เซอร์แล้ว (header/plan switcher/sidebar/drag ทำงานปกติ)
- [x] **มือถือ: คลังสถานที่เป็น bottom sheet แทน overlay เต็มจอ** (7 ส.ค. 2026) — `components/PlaceSidebar.tsx` เปลี่ยน `MobileOverlay` เป็น `BottomSheet`: เลื่อนขึ้นจากขอบล่าง (max 85vh) มีฉากหลังจางๆ ให้เห็นบริบทหน้าเดิม + หัวชีตโชว์เมือง/วันที่ที่กำลังเพิ่มให้ตลอด (แก้ปัญหาเดิมที่เสียบริบท) แตะฉากหลังหรือปุ่ม ✕ เพื่อปิด แอนิเมชัน slide-up ใน `app/globals.css` ผ่าน lint + typecheck สะอาด ทดสอบจริงที่ 375px แล้ว (เปิดชีตเห็น context ถูกต้อง)
- [x] **คอม: ใช้พื้นที่จอกว้างให้คุ้ม** (7 ส.ค. 2026) — ต้นเหตุคือ container กว้าง `max-w-5xl` (1024px) แต่คอลัมน์จุดแวะข้างในยังโดน `max-w-2xl` (672px) ตรึงซ้อนอยู่อีกชั้น ทำให้แผนที่ต่อวัน+ลิสต์บีบเหลือ ~372px ทั้งที่จอเหลือพื้นที่เยอะ — ขยาย container เป็น `lg:max-w-7xl` (1280px) และปลด `max-w-2xl` ออกจากคอลัมน์เนื้อหาที่ `lg:` (`lg:max-w-none`) ให้ `flex-1` ทำงานจริง วัดจริงในเบราว์เซอร์ที่จอ 1600px: คอลัมน์จุดแวะกว้างขึ้นจาก ~344px → 904px หัวเว็บ (`TripHeader`) ขยายกว้างตามด้วยเพื่อให้ขอบซ้ายชิดกับเนื้อหาด้านล่าง ผ่าน lint + typecheck สะอาด
- [x] **ระบบสี/ระยะห่าง/ขนาดตัวอักษร — ตรวจแล้ว ไม่ต้องแก้** (7 ส.ค. 2026) — ไล่ดู `text-*` ทั้งเว็บ (67× `text-xs`, 45× `text-sm`, 19× `text-[11px]`, 10× `text-[10px]`) ตัวเลข px ที่ดูเหมือนสุ่มจริงๆ คือ tier ขนาดที่ตั้งใจ (11px = meta text รอง, 10px = ป้ายจิ๋วบนแผนที่/badge) ใช้สม่ำเสมอทั้งเว็บอยู่แล้ว ไม่ใช่ความไม่สอดคล้องจริง — สีก็ยึด `@theme` เดิมทั้งหมดไม่มีสีหลุด ไม่พบจุดที่ต้องแก้จริง (ตรงกับที่ PLAN.md บันทึกไว้แต่แรกว่าธีมดีอยู่แล้ว)
- [x] **สถานะกำลังโหลดให้ดูดี** (7 ส.ค. 2026) — เพิ่ม `components/DayCardSkeleton.tsx` (โครงการ์ดวัน ทรงเดียวกับ `DayStopsSection` จริง + `animate-pulse`) แทนข้อความเปล่า "กำลังโหลด..." ใน `app/page.tsx` ตอนโหลดครั้งแรก และแก้ `PhotoGallery.tsx` ให้โชว์โครง grid รูปแทนข้อความ "กำลังโหลดรูป..." ผ่าน lint + typecheck สะอาด (สถานะว่างเปล่าอื่นๆ อย่าง "ยังไม่มีจุดแวะ" / "เลือกครบทุกที่ในโซนนี้แล้ว 🎉" มีข้อความ+อีโมจิที่ช่วยนำทางอยู่แล้ว ไม่ต้องแก้)
- [x] **ปุ่มกดแล้วมีการตอบสนอง** (7 ส.ค. 2026) — เพิ่มกฎ CSS ทั่วเว็บใน `app/globals.css` (`button:active { opacity: 0.7 }`) ครอบคลุมทุกปุ่มโดยไม่ต้องไล่แก้ทีละไฟล์ ใช้ opacity ไม่ใช่ scale เพราะที่จับลาก (`⠿`) ก็เป็น `<button>` เหมือนกัน scale จะไปกวน bounding box ตอน dnd-kit เริ่มวัดระยะลาก ผ่าน lint + typecheck สะอาด
- [x] **การเปลี่ยนหน้าลื่น** (7 ส.ค. 2026) — ตอนนี้มี route ที่สองแล้ว (`/today` จากเฟส 6) ใช้ `next/link` มาตรฐานทั้งสองทิศทาง ("📍 วันนี้" ใน `TripHeader` ↔ "← หน้าแผน" ใน `/today`) Next.js prefetch + client-side transition ให้อัตโนมัติ ไม่ต้องเขียนอะไรเพิ่ม ทดสอบจริงแล้วเปลี่ยนหน้าลื่น ไม่มี full page reload
- [x] **ไม่มีจอกระพริบตอนโหลด — ตรวจแล้ว ไม่มีปัญหา** (7 ส.ค. 2026) — ใช้ `next/font/google` (`Noto_Sans_Thai` ใน `app/layout.tsx`) ซึ่ง self-host ฟอนต์ + จัดการ `font-display` ให้อัตโนมัติอยู่แล้ว ไม่พบ FOUC จริง

---

### เฟส 5 — ตั๋ว/booking + checklist + เดินทางระหว่างเมือง ✅

- [x] **ตาราง `bookings`** (7 ส.ค. 2026) — migration `0016_bookings.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `bookings` · ประเภท (เที่ยวบิน/ที่พัก/KTX/บัส/ตั๋วเข้าชม/อื่นๆ), ผูกกับวันได้ (ไม่บังคับ), วันที่/เวลา, เลขที่จอง, ลิงก์, โน้ต · UI ใหม่ `components/BookingsPanel.tsx` + `components/BookingEditModal.tsx` (เพิ่ม/แก้/ลบ) แสดงอยู่ใต้แผงที่พักในหน้าแรก · hook `hooks/useBookings.ts` sync สดผ่าน Supabase Realtime เหมือนที่พัก · ทดสอบเพิ่ม/แก้/ลบจริงในเบราว์เซอร์แล้ว ผ่าน lint + typecheck สะอาด
- [x] **ไฟล์แนบตั๋ว (รูป/PDF)** (7 ส.ค. 2026) — bucket `booking-files` สร้างใน Supabase Dashboard (Public) + migration `0019_bookings_file.sql` เพิ่มคอลัมน์ `file_url`/`file_name` ใน `bookings` และ policy อ่าน/เขียน/ลบสาธารณะบน `storage.objects` (ต้องตั้งแยกจากตารางปกติ เพราะ Public bucket แค่เปิดอ่าน ไม่ได้เปิดเขียนให้อัตโนมัติ) · `BookingEditModal.tsx` เพิ่มช่องอัปโหลดไฟล์ (รูป/PDF) อัปโหลดตรงไป Supabase Storage แล้วเก็บ public URL, แสดงเป็นลิงก์เปิดไฟล์ + ลบไฟล์ได้ · `BookingsPanel.tsx` โชว์ไอคอน 📎 เมื่อมีไฟล์แนบ · ยืนยัน bucket/policy ด้วย curl จริง (upload/read/delete ผ่านหมด HTTP 200) — **หมายเหตุ:** การอัปโหลดไฟล์จริงผ่าน UI ยังไม่ได้ทดสอบในเบราว์เซอร์ (เครื่องมือ automation ตอนนี้ set ค่า `<input type=file>` ไม่ได้) แต่ path อัปโหลด/ดึง public URL เดียวกับที่ curl ยืนยันแล้วว่าใช้ได้จริง
- [x] **ตาราง `checklist_items`** (7 ส.ค. 2026) — migration `0018_checklist_items.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `checklist_items` · ข้อความ, ติ๊กแล้วหรือยัง, ใครติ๊ก (`checked_by`) · UI ใหม่ `components/ChecklistPanel.tsx` แสดงอยู่ใต้แผง booking ในหน้าแรก เพิ่ม/ติ๊ก/ลบได้ ติ๊กแล้วเลื่อนไปท้ายลิสต์ + ขีดฆ่า · hook `hooks/useChecklist.ts` sync สดผ่าน Supabase Realtime แบบเดียวกับ bookings · ผ่าน lint + typecheck สะอาด ทดสอบเพิ่ม/ติ๊ก/ลบจริงในเบราว์เซอร์แล้ว
- [x] **เดินทางระหว่างเมืองกรอกเอง** (7 ส.ค. 2026) — migration `0017_trip_stops_intercity.sql` รันแล้ว ยืนยันด้วย curl · เพิ่มคอลัมน์ `kind`/`intercity_from`/`intercity_to`/`intercity_mode` ใน `trip_stops` เดิม แถวเดินทางข้ามเมือง (kind="intercity") แทรกกลางลิสต์จุดแวะได้เลยผ่านปุ่ม "+ แทรกเดินทางข้ามเมืองตรงนี้" (คู่กับปุ่มแทรกร้านอาหารเดิมทุกตำแหน่ง) เปิด `IntercityEditModal` เลือกพาหนะ (บัส/KTX/อื่นๆ) + จาก/ไป + ระยะเวลา (มี preset ชม.) · **กินเวลาใน timeline จริง** โดยไม่ต้องแก้ `lib/schedule.ts` เลย — ใช้ฟิลด์ `dwell_minutes` เดิมเก็บระยะเวลาเดินทาง, `place_id` ว่างเปล่าทำให้จุดแวะก่อน/หลังไม่ถูกคำนวณระยะทางข้ามเมืองมั่วๆ (จุดหลังเริ่มนับเวลาใหม่จากตอนถึงปลายทางอัตโนมัติ) · ลาก/ลบ/ปรับเวลาใช้ระบบเดิมของ `trip_stops` ได้ฟรีทั้งหมด · แก้ `RouteSuggestionModal.tsx` ให้ตำแหน่งแถวเดินทางข้ามเมือง **ปักหมุดอยู่ที่เดิมเสมอ** ตอนกด "ลองจัดเส้นทางใหม่" (ของเดิมจะดันไปท้ายวันผิดๆ) · ทดสอบจริงในเบราว์เซอร์แล้ว (แทรกกลางวัน, timeline คำนวณใหม่ถูกต้อง, route suggestion ไม่ย้ายตำแหน่ง, ลบแล้วกลับสภาพเดิม) ผ่าน lint + typecheck สะอาด

---

### เฟส 6 — หน้า "วันนี้" (โหมดใช้จริงตอนเที่ยว) ✅ เสร็จ (7 ส.ค. 2026)
เฟสสุดท้ายเพราะพึ่งทุกเฟสก่อนหน้า แต่นี่คือเฟสที่ทำให้เว็บนี้มีค่าตอนอยู่เกาหลีจริง

**เตรียมพื้นฐานก่อนแตะ UI ใหม่:**
- [x] แยก `hooks/useDaySchedule.ts` ออกจาก `DayStopsSection.tsx` (เดิมฝังในนั้นล้วนๆ) — ย้ายโค้ดตรงๆ ไม่เปลี่ยนพฤติกรรม เพื่อให้หน้าแผนกับหน้า "วันนี้" ใช้ตรรกะคำนวณ timeline เดียวกันเป๊ะ (เวลาถึง-ออก, เวลาจริงจาก Google, เตือนสถานที่ปิด, ที่พักหัว-ท้ายวัน) ไม่เสี่ยงเวลาสองหน้าไม่ตรงกัน ทดสอบหน้าแผนเดิมในเบราว์เซอร์แล้วทำงานเหมือนเดิมทุกอย่าง

- [x] route ใหม่ `app/today/page.tsx` — ออกแบบสำหรับ **มือถือถือเดินอย่างเดียว** ตัวหนังสือใหญ่ ปุ่มใหญ่ กดง่ายด้วยมือเดียว (ปุ่มทุกปุ่ม ≥44px) · ยึด **วันจริงตามนาฬิกาเครื่อง** เป็นค่าเริ่มต้น (ก่อนทริปคืนวันแรก/หลังทริปคืนวันสุดท้าย) + สลับดูวันอื่นได้อิสระด้วยปุ่ม ‹ › และปุ่ม "กลับไปวันนี้จริง" — ตัดสินใจไว้ตอนวางแผน: ใช้เทสต์ได้ตอนนี้ (ส.ค.) และตอนเที่ยวจริงก็ใช้ดูแผนวันถัดไปได้
- [x] **จุดถัดไปเด่นที่สุดบนหน้าจอ** (การ์ดขอบส้มเด่น) **+ ปุ่มนำทางครบ 3 แอปเสมอ** (`lib/mapLinks.ts`) — Google (ลิงก์เว็บมาตรฐาน `maps.google.com/maps/dir` ใช้ได้ทุกที่รวมฮานอย) + Kakao (`map.kakao.com/link/to` เอกสารทางการ ไม่ต้องมี API key) + Naver (`nmap://` app scheme พร้อม fallback ไปหน้าค้นหาเว็บถ้าไม่มีแอป) — ตัดสินใจให้ครบ 3 เพราะเฟส 1 พิสูจน์แล้วว่า Google เดิน/ขับในเกาหลีคืนค่าว่าง คนเกาหลีใช้ Naver/Kakao กัน แต่ฮานอยกลับ Google ดีสุด ให้ผู้ใช้เลือกเองหน้างาน
- [x] แสดงเวลาเปิด-ปิดของจุดถัดไป (เตือนถ้าอาจปิดแล้ว ใช้ตรรกะเดียวกับหน้าแผน) + **ตั๋ว/booking ของวันนี้** (กรองจาก `bookings` ตาม `day_id`/วันที่)
- [x] ติ๊ก "มาถึงแล้ว" ได้ — migration `0020_trip_stops_visited.sql` เพิ่มคอลัมน์ `visited_at` ใน `trip_stops` รันแล้ว ยืนยันด้วย curl · `markVisited`/`unmarkVisited` ใน `useStops` · แตะแถวใน "ผ่านมาแล้ว" เพื่อยกเลิกติ๊กได้ (เผื่อกดพลาด)
- [x] **ถ้าออกช้ากว่าแผน → เลื่อน timeline ที่เหลือของวันให้ดูอัตโนมัติ** (`lib/liveDelay.ts`) — เทียบเวลาจริงที่ติ๊ก "มาถึงแล้ว" ล่าสุดกับแผน (แม่นสุด) หรือถ้ายังไม่ติ๊กจุดไหนเลยเทียบเวลาปัจจุบันกับจุดแรก · **แสดงผลอย่างเดียว ไม่เขียนกลับ DB เด็ดขาด** (ตัดสินใจไว้ตอนวางแผน) · เช็กเวลาเปิด-ปิดซ้ำด้วยเวลาที่เลื่อนแล้ว ขึ้นป้าย ⚠️ ถ้าจุดไหนอาจไปไม่ทันเวลาปิดเพราะความล่าช้า
- [x] ทั้ง 2 คนเห็นสถานะเดียวกันสด — ใช้ระบบ realtime เดิมของ `trip_stops` (ไม่ต้องเพิ่ม channel ใหม่)
- [x] ลิงก์สลับหน้าแผน ↔ วันนี้ — "📍 วันนี้" ใน `TripHeader` และ "← หน้าแผน" ใน `/today`

ทดสอบจริงในเบราว์เซอร์ทั้งหมด: สลับวัน, ป้ายเตือนสถานที่ปิดจริง (Gamcheon Village), ลิงก์นำทาง 3 แอปครบถูกต้อง, ติ๊ก/ยกเลิกติ๊ก "มาถึงแล้ว" round-trip, แถบเลื่อนเวลาเมื่อช้ากว่าแผน (เทียบกับตรวจซ้ำผ่าน curl ว่า state จริงใน DB สะอาด ไม่เหลือข้อมูลทดสอบค้าง) ผ่าน `npm run lint` + `tsc --noEmit` สะอาด

---

### รอบแก้จากการใช้งานจริงบนมือถือ ✅ เสร็จ (7 ส.ค. 2026)
ฟีดแบ็ก 6 ข้อหลังลองใช้จริงบนมือถือ:

- [x] **คลังสถานที่บนมือถือเลื่อนดูไม่ได้** — เจอ 2 สาเหตุซ้อนกัน (1) `dnd-kit` ใส่ `touch-action:none` บนการ์ดทุกใบ นิ้วที่ปัดเลยถูกตีความเป็น "เริ่มลาก" → ตัด `useDraggable` ออกทั้งหมดในชีตมือถือ (`draggable` prop ใน `PlaceSidebar`, จอใหญ่ยังลากได้ตามเดิม) เพราะมือถือมีปุ่ม "+ เพิ่มลงวันนี้" บนการ์ดอยู่แล้ว (2) กล่องคลังใน bottom sheet สูงตามเนื้อหา 3044px ทะลุกรอบชีต 615px แล้วโดน `overflow-hidden` ตัดทิ้ง (`h-full` คำนวณไม่ได้เพราะความสูงพ่อมาจาก flex) → เปลี่ยนเป็น `min-h-0 flex-1` ตลอดสาย · วัดยืนยันในเบราว์เซอร์แล้ว: ช่องเลื่อน 487px เลื่อนถึงท้ายสุดได้จริง
- [x] **วันบิน (11 และ 21 ต.ค.) ใช้ได้เหมือนวันอื่นแล้ว** — เดิมวันที่มี `anchor:"before"` ถูกบังคับเวลาเริ่มตายตัว ตั้งเองไม่ได้ ตอนนี้ตั้งเวลา "ออกเดินทาง" ได้ทุกวัน โดยเวลาจากตารางบินเป็นแค่**ค่าเริ่มต้นที่แนะนำ** (มีปุ่ม "กลับไปใช้เวลาแนะนำ" ถ้าปรับแล้วอยากคืนค่า) — เผื่อกรณีผ่าน ตม./รับกระเป๋าเร็วหรือช้ากว่าที่เผื่อไว้ · ตัว `DayEvent` (ตารางบิน/เดดไลน์) ยังล็อกอ่านอย่างเดียวเหมือนเดิม ติดป้าย "🔒 ตั๋วจองแล้ว แก้ไม่ได้" ให้ชัด
- [x] **ล็อกวันที่ลงตัวแล้ว** — migration `0021_day_lock.sql` (คอลัมน์ `is_locked` ใน `trip_day_settings`) · ปุ่มล็อกรายวันบนหัวการ์ด + ปุ่ม "🔒 ล็อกทุกวัน" รวมทั้งทริปใน `TripHeader` · วันที่ล็อกจะปิดทั้ง drag handle, ปุ่มปรับเวลาที่อยู่, ปุ่มลบ, โน้ต, ปุ่มแทรก, ตัวเลือกโหมดเดินทาง และปิด droppable/sortable ของ dnd-kit (มีด่านกันซ้ำใน `useTripDnd` เผื่อลากด้วยคีย์บอร์ด) — แก้ปัญหา "เผลอลากตอนเลื่อนดู"
- [x] **หน้าสรุปแผน `/summary`** — ดูอย่างเดียวทั้งทริป ไม่มีปุ่มแก้สักปุ่ม: ที่พักทุก leg, ตั๋ว/booking, ของที่ต้องเตรียม, แล้วตามด้วยการ์ดรายวันครบ 11 วัน (เวลาตายตัว, เวลาถึง-ออกทุกจุด, โน้ต, สรุปเวลาเดินทาง/ระยะทาง) · ใช้ `useDaySchedule` ตัวเดียวกับอีก 2 หน้า เวลาจึงตรงกันเสมอ
- [x] **เข้าถึงหน้า "วันนี้" ง่ายขึ้น** — `components/BottomNav.tsx` แถบเมนูล่างของมือถือ 3 ปุ่ม (แผนทริป · วันนี้ · สรุปแผน) อยู่ทุกหน้า ไม่ต้องเลื่อนขึ้นไปหาลิงก์บนหัวเว็บ · จอใหญ่ซ่อนไว้ ใช้ลิงก์บนหัวเว็บแทน (เพิ่ม "📋 สรุปแผน" เข้าไปด้วย)
- [x] `/today` อ่าน `trip_day_settings` แล้ว (เดิม hard-code เวลาเริ่ม 07:00 + โหมดขากลับ null) เวลาที่โชว์จึงตรงกับหน้าแผนจริงๆ

---

## 3.5 รอบตรวจโค้ดทั้งโปรเจกต์ (7 ส.ค. 2026) — เฟส 7-12

ตรวจโค้ดทั้งหมด (~9,200 บรรทัด) + ทดสอบจริงในเบราว์เซอร์ทั้ง 3 หน้า พบบั๊ก 14 ตัว + ปัญหา performance
`npm run lint` และ `tsc --noEmit` สะอาดทั้งคู่ — บั๊กทั้งหมดเป็นชนิดที่ tooling จับไม่ได้ ต้องอ่าน/ทดสอบเอง

**สถานะ (7 ส.ค. 2026 กลางคืน): เฟส 7-9 เสร็จหมดแล้วภายในวันเดียว** (เร็วกว่าเป้า "จบภายใน ส.ค." มาก)
เหลือ เฟส 6.9 ข้อเดียว (ลบ test data ใน DB — รอผู้ใช้อนุมัติ/ทำเอง) + เฟส 10-12 ที่ยังไม่เริ่ม
ไม่มี migration ใหม่เลยทั้ง 3 เฟสที่ทำไปตามที่ตั้งใจไว้ · โค้ดที่แก้ทั้งหมดผ่าน `npm test` + `lint` + `tsc --noEmit`
สะอาด และยืนยันซ้ำในเบราว์เซอร์จริงแล้ว (ไม่ใช่แค่เชื่อจากทฤษฎี)

**ธีมของสิ่งที่เจอ:** เกือบทุกบั๊กคือ **edge case ของเวลา** (ค่าว่าง / ข้ามเที่ยงคืน / "วันนี้จริง" vs "วันที่กำลังดู")
ซึ่งเป็นจุดที่ unit test จับได้หมดในไม่กี่เคส — เฟส 7 จึงรวมการวางเทสต์ชุดแรกไว้ด้วย ไม่ใช่แค่แก้บั๊ก

**ลำดับเฟสอิงจาก "เหลืออีก ~9 สัปดาห์ก่อนบิน":** ความถูกต้องของเวลา → ความถูกต้องของข้อมูล →
ความเร็ว → กันข้อมูลหาย → ฟีเจอร์เสริม → ความสะอาดโค้ด
เป้าหมายเวลา: **เฟส 7-9 ให้จบภายใน ส.ค.** · **เฟส 10 ภายใน ก.ย.** · เฟส 11-12 เท่าที่มีเวลา

---

### เฟส 6.9 — งานจิ๋วทำก่อนเลย (< 15 นาที) ✅ เสร็จ (8 ส.ค. 2026)

- [x] **ล้าง test data ค้างใน DB จริง** — `Mun Dining` (วัน `d0`, stop id `stop-v2twml5x4msimwzds`) มี `visited_at`
      ติดอยู่จากการทดสอบเฟส 6 ทำให้ `/today` คิดว่า "มาถึงแล้ว" ผู้ใช้รัน SQL เองใน Supabase Dashboard
      (`UPDATE trip_stops SET visited_at = NULL WHERE id = 'stop-v2twml5x4msimwzds'`) ยืนยันด้วย SELECT แล้วว่าเป็น NULL
- [x] **ลบคอมเมนต์ล้าสมัย** `lib/schedule.ts:21` (7 ส.ค. 2026) — "(ดู `app/api/travel-time/route.ts` ที่ยัง broken อยู่)"
      route นั้นเขียนใหม่ตั้งแต่เฟส 1 แล้ว แก้เป็นข้อความที่ตรงกับความจริงปัจจุบัน

---

### เฟส 7 — เวลาที่เชื่อถือได้ + เทสต์ชุดแรก ✅ เสร็จ (7 ส.ค. 2026)
ทุกอย่างในเว็บนี้ห้อยอยู่กับ timeline ถ้าเวลาผิด หน้า "วันนี้" ก็พาไปผิดที่ผิดเวลาตอนเที่ยวจริง

**7.1 วางระบบเทสต์**
- [x] ติดตั้ง `vitest@2.1.9` + `vite-tsconfig-paths@5.1.4` — **หมายเหตุ:** ตัวล่าสุด (`vitest@4` ที่พึ่ง `rolldown`)
      พังบนเครื่องนี้เพราะ Node `v20.12.2` เก่ากว่าที่ต้องการ (`^20.19.0`) หา native binding ไม่เจอ ลง `vitest@2.1.9`
      แทน (ยัง esbuild-based ไม่พึ่ง rolldown) ใช้งานได้ปกติ · `vitest.config.mts` ต้องเป็นนามสกุล `.mts` ไม่ใช่ `.ts`
      เพราะ `vite-tsconfig-paths` เป็น ESM-only แล้ว config ปกติถูกโหลดแบบ CJS ในโปรเจกต์นี้
- [x] เพิ่ม script `npm test` (`vitest run`) ใน `package.json`
- [x] เขียนเทสต์ครอบ pure function 6 ไฟล์ (เพิ่ม `lib/stopOrdering.ts` จากเฟส 8 เข้ามาด้วย) — **50 เทสต์ ผ่านหมด**
      ไฟล์: `lib/__tests__/schedule.test.ts`, `openingHours.test.ts`, `liveDelay.test.ts`, `hotelLegs.test.ts`,
      `optimizeRoute.test.ts`, `stopOrdering.test.ts`, `hotelAnchorId.test.ts`

**7.2 `/today` บอกว่า "ช้ากว่าแผน" ทั้งที่ยังไม่ถึงวันทริป** ✅ แก้แล้ว
- [x] `isRealToday` เทียบ `day.date === todayIso` ตรงๆ แทน `dayIndex === todayIndex` — ยืนยันในเบราว์เซอร์แล้ว
      เปิด `/today` วันนี้ (7 ส.ค.) ไม่มีแถบ "ช้ากว่าแผน" อีกต่อไป ขึ้น "(ไม่ใช่วันนี้จริง)" ถูกต้อง

**7.3 ล้างช่อง "🕐 ออกเดินทาง" แล้วตารางทั้งวันกลายเป็น `NaN:NaN`** ✅ แก้แล้ว
- [x] กัน `""` ที่ `onChange` ของ `<input type="time">` ใน `DayStopsSection` ไม่ให้เรียก `onStartTimeChange` เลย
- [x] `timeToMinutes` คืน `null` เมื่อ parse ไม่ได้ (`lib/schedule.ts`) — ทุกจุดที่เรียกใส่ fallback ครบ
      (`computeSchedule` → 0, `useDaySchedule.effectiveStartTime` → เช็ค `.trim() !== ""` ก่อนใช้ค่า)
- [x] `useDaySettings.setStartTime` อัปเดต state ในเครื่องก่อน (optimistic) + เพิ่มด่านกัน `""` ซ้ำอีกชั้น
      ก่อนยิงขึ้น DB (defense-in-depth ตามที่ตกลงไว้ เผื่อมีทางอื่นเรียกเข้ามาในอนาคต)

**7.4 ป้ายเตือน "จบช้ากว่าเดดไลน์" เงียบสนิทเมื่อเลยเที่ยงคืน** ✅ แก้แล้ว
- [x] `computeSchedule` คืน `arrivalMinutes`/`departureMinutes` ต่อจุด (นาทีสะสมไม่ wrap) และ `endOfDayMinutes`
      ระดับวัน — `deadlineOverrunMinutes` ใน `useDaySchedule` เทียบด้วยค่าดิบนี้แทน parse สตริง `HH:MM` ที่ห่อรอบมาแล้ว
- [x] `isOpenDuring` เปลี่ยนมารับนาทีดิบ (ไม่ใช่ `HH:MM` string) + generalize การสร้างช่วงเปิดให้ครอบ
      เมื่อวาน/วันนี้/พรุ่งนี้ทั้ง 3 วัน (เดิมมีแค่เมื่อวาน/วันนี้) แก้เคสจุดแวะคร่อมเที่ยงคืนไปวันถัดไปได้ถูกทั้งสองทาง
      อัปเดตทุกจุดที่เรียก (`useDaySchedule.isClosedAt`, `DayStopsSection` ใช้ `closedStopIds` จาก hook แทนคำนวณเอง
      ซ้ำ — เก็บงานเฟส 12 ที่ตั้งใจไว้ไปด้วยเลย, `RouteSuggestionModal`, `app/today/page.tsx` 2 จุด)

**7.5 `/today` ไม่ข้ามวันเอง** ✅ แก้แล้ว
- [x] `todayIndex` คำนวณใหม่ทุกครั้งที่ `now` เดิน (ทุก 30 วิ) ผ่าน `useMemo` แทน freeze ด้วย `useState` ครั้งเดียว
      + `useRef`/`useEffect` ตามวันจริงอัตโนมัติเฉพาะตอนผู้ใช้ยังไม่กด ‹ › เลือกดูวันอื่นเอง (ไม่กระโดดทับ)

**7.6 `useDayOpeningHours` cancelled bug** ✅ แก้แล้ว (รวมเข้ากับ 9.2 — ดูด้านล่าง เขียนใหม่ทั้งไฟล์ไปพร้อมกัน)

**ยืนยันปิดเฟส:** `npm test` (50/50 ผ่าน) · `npm run lint` + `tsc --noEmit` สะอาด · เปิด `/today` จริงในเบราว์เซอร์
ไม่มีแถบ "ช้ากว่าแผน" ผิดๆ อีกแล้ว · ไม่มี console error

---

### เฟส 8 — ลำดับจุดแวะไม่เพี้ยน ✅ เสร็จ (7 ส.ค. 2026)
ทั้ง 3 ข้อรากเดียวกัน: `order_index` มีช่องว่างได้ แต่โค้ดบางที่คิดว่ามันคือ "ตำแหน่งใน array"

**8.1 `order_index` ชนกันหลังลากจุดแวะข้ามวัน** ✅ แก้แล้ว
- [x] ย้าย `nextOrderIndex`/`orderIndexAtPosition` ไปเป็น pure function ใน `lib/stopOrdering.ts` ใหม่ (เทสต์ตรงๆ
      ได้ ไม่ต้องพึ่ง React/Supabase) — `addStop` เปลี่ยนมาใช้ `max(order_index) + 1` แทน `dayStops.length`
- [x] `moveStopToDay` จัดเลขวันต้นทางที่เหลือใหม่ให้ต่อเนื่อง 0..n-1 ทันทีในคำสั่งเดียวกัน (ทั้ง state ในเครื่อง
      และ DB — เขียนเฉพาะแถวที่เลขจริงๆ เปลี่ยน ไม่ยิง update เปล่าๆ ทุกแถว)

**8.2 "+ แทรกร้านตรงนี้" แทรกผิดช่องเมื่อมีช่องว่าง** ✅ แก้แล้ว
- [x] `insertStopAt`/`insertIntercityAt` แปลง `atIndex` (ตำแหน่งใน array) → `order_index` เป้าหมายจริงก่อน
      ด้วย `orderIndexAtPosition` แล้วค่อยส่งให้ `shiftForInsert`
- [x] เทสต์เคส "วันมีช่องว่าง 0,2,3 แล้วแทรกตรงกลาง (atIndex=1)" ต้องได้ order_index=2 ไม่ใช่ 1 — ผ่าน
      (`lib/__tests__/stopOrdering.test.ts`, 7 เทสต์)

**8.3 จุดที่ติ๊กแล้วโผล่ 2 ที่ใน `/today`** ✅ แก้แล้ว
- [x] `upcoming`/`upcomingSched` กรอง `!stop.visited_at` เพิ่มอีกชั้น ไม่พึ่ง `slice(nextIndex+1)` อย่างเดียว

⚠️ **เฟสนี้แตะลำดับข้อมูลจริงที่ใช้อยู่** — ไม่ได้ทดสอบด้วยการลาก/แทรกจริงในเบราว์เซอร์บนแผนหลัก (กติกาข้อ 5
ห้ามกดปุ่มแก้ข้อมูลจริงตอนทดสอบ) ยืนยันด้วย `tsc`/`lint` สะอาด + เทสต์ unit ครอบ edge case ที่ระบุไว้ครบ
**แนะนำ:** ทดสอบลาก/แทรกจริงกับ "แผน B" ที่ก๊อปมา ก่อนใช้แผนหลักจริงจัง

---

### เฟส 9 — แคชถูกต้อง + โหลดเร็ว ✅ เสร็จ (7 ส.ค. 2026)
วัดจริงตอนโหลดหน้าแรก (ก่อนแก้): **165 requests · 50 URL ซ้ำกันเป๊ะๆ** (`/api/place-photos` ×68, `/api/place-details` ×49)

**9.1 เวลาเดินทางจาก/ถึงโรงแรมค้างค่าเก่าถาวรเมื่อเปลี่ยนโรงแรม** ✅ แก้แล้ว
- [x] เพิ่ม `hotelAnchorId()` ใน `lib/hotelLegs.ts` — สร้าง id จากพิกัดที่พัก (`hotel@{lat.toFixed(5)},{lng...}`)
      แทน `leg_id` ตรงๆ ใช้แทนที่ทั้งใน `useDaySchedule` (anchor เริ่ม/จบวัน) และ `useHotelDistance`
      (label "🏨 X นาที จากที่พัก" บนการ์ดคลัง) → เปลี่ยนโรงแรมได้ key ใหม่เองทันที **ไม่ต้องเขียน migration ใหม่เลย**

**9.2 ยิง API ซ้ำเพราะไม่มี in-flight dedupe** ✅ แก้แล้ว
- [x] `usePlacePhotos`/`usePlaceDetails` เพิ่ม `inFlight` Map แบบเดียวกับ `useDayTravelTimes`
- [x] รวมแคช `place-details` เป็นก้อนเดียว — `usePlaceDetails.ts` export `placeDetailsCache`/`fetchPlaceDetails()`
      ให้ `useDayOpeningHours` เรียกใช้ร่วมแทนการมี `Map` แยกของตัวเอง (แก้บั๊ก 7.6 ไปพร้อมกันในตัว)

**9.3 คลังสถานที่ถูก mount 2 ชุดเสมอ** ✅ แก้แล้ว
- [x] เพิ่ม `hooks/useMediaQuery.ts` (ใช้ `useSyncExternalStore` — ปลอดภัยสำหรับ SSR ไม่ชน eslint
      `react-hooks/set-state-in-effect` ที่เจอตอนลองใช้ `useEffect` ตั้ง state ตรงๆ ก่อน) `PlaceSidebar`
      เรนเดอร์ `<aside>` (คอม) หรือปุ่มลอย+ชีต (มือถือ) แค่ชุดเดียวจริงๆ ตามขนาดจอ แทนเรนเดอร์ทั้งคู่แล้วซ่อนด้วย CSS

**ผลวัดหลังแก้ (เบราว์เซอร์จริง โหลดหน้าแรกซ้ำ):** **113 requests (ลด 32%) · URL ซ้ำเหลือ 0** (จาก 50)
`/api/place-photos` 68→34 · `/api/place-details` 49→34 · ยืนยันแล้วว่าคลังสถานที่เรนเดอร์แค่ 1 ชุดจริง
(`document.querySelectorAll('aside').length` = 0 บนมือถือ / 1 บนจอใหญ่ ไม่มีทั้งคู่พร้อมกัน)

**ยืนยันปิดเฟส:** `npm test` (50/50) · `lint` + `tsc --noEmit` สะอาด · วัด performance ซ้ำแล้วเห็นตัวเลขดีขึ้นจริง

---

### เฟส 10 — กันข้อมูลหาย + พกไปเที่ยวได้ ✅ เสร็จ (8 ส.ค. 2026)
RLS เปิดสาธารณะทุกตาราง + anon key อยู่ใน bundle = ใครเจอ URL Vercel ก็ลบข้อมูลทริปได้หมด
**เรื่องนี้ตั้งใจไว้แต่แรกและไม่แก้** (กติกาข้อ "ไม่ทำระบบล็อกอิน") — แต่ตอนนี้ **ไม่มี backup เลยสักชั้น** ซึ่งคนละเรื่องกัน

- [x] **ปุ่ม Export แผนเป็น JSON** — ปุ่ม "⬇️ Export JSON" บนหัว `/summary` ดาวน์โหลด plans/stops/hotels/bookings/
      checklist/day settings/overnight overrides/custom places รวมไฟล์เดียว (client ล้วน ไม่มี API ใหม่)
- [x] **`/summary` พิมพ์ได้จริง** (`@media print`) — ซ่อนลิงก์นำทาง/BottomNav ด้วย `print:hidden`, บังคับสีพื้นหลัง
      หัวการ์ดวันด้วย `print-color-adjust: exact`, `print:break-inside-avoid` ต่อวัน, ตั้ง `@page { margin: 12mm }`
      เพิ่มปุ่ม "🖨️ พิมพ์" เรียก `window.print()` ให้ด้วย
- [x] **`/api/place-autocomplete` เพิ่ม rate limit ต่อ IP** — `lib/rateLimit.ts` in-memory sliding window (60 req/นาที/IP)
      คืน 429 เมื่อเกิน พอสำหรับ 2 คนใช้งานจริง ไม่ต้องพึ่ง Redis/Upstash
- [x] **ไฟล์แนบ booking ไม่รั่วอีกแล้ว** — `BookingEditModal.tsx`: กด ✕ ลบไฟล์จริงใน bucket (`storagePathFromPublicUrl`
      แกะ path จาก public URL), ปิด modal โดยไม่กดบันทึกก็ลบไฟล์ที่เพิ่งอัปโหลดทิ้งอัตโนมัติ (`pendingUploadPathRef`),
      จำกัดขนาดไฟล์ 10MB
- [x] **ช่อง "ลิงก์" ของ booking กรอง protocol แล้ว** — `lib/url.ts` (`safeHttpUrl`) อนุญาตเฉพาะ `http:`/`https:`
      บังคับใช้ทั้งตอนบันทึก (โชว์ error ในฟอร์ม) และตอนเรนเดอร์เป็น `href` ใน `/today`

**ยืนยันปิดเฟส:** `npm test` (50/50) · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์ (`/summary`
โหลดได้ กด Export JSON ดาวน์โหลดไฟล์สำเร็จ ไม่มี console/server error)

---

### เฟส 11 — ฟีเจอร์ระหว่างเที่ยว (ทำเท่าที่มีเวลาก่อนบิน)
ทั้งหมดใช้ข้อมูลที่มีอยู่แล้ว ไม่ต้องเปิด API ใหม่ ไม่ต้องมี migration

- [ ] **"จุดถัดไปจะปิดในอีก X นาที"** ใน `/today` — มี `regularOpeningHours` ครบอยู่แล้ว เหลือแค่เทียบกับ `now`
      (ต่างจากป้ายปัจจุบันที่เตือนเฉพาะตอน "อาจปิดแล้ว" = สายไปแล้ว)
- [ ] **ปุ่ม "นำทางกลับที่พัก"** ใน `/today` — ตอนนี้มีแค่ปุ่มไปจุดถัดไป พอเที่ยวครบทุกจุดก็ไม่มีปุ่มนำทางเหลือเลย
- [ ] **Checklist ผูกกับวัน/แยกหมวด** (ของใส่กระเป๋า / ก่อนออกจากโรงแรม / ก่อนขึ้นเครื่อง)
- [ ] **โน้ต + รูปหน้างานต่อจุดแวะ** — ใช้ bucket `booking-files` เดิมได้ ต่อยอดจากช่องโน้ตที่มีแล้ว
- [ ] **เวลาเปิด-ปิดวันหยุดพิเศษ** — ตอนนี้ใช้แค่ `regularOpeningHours` ยังไม่ได้ดึง `currentSecondaryOpeningHours`

---

### เฟส 12 — ความสะอาดโค้ด (ไม่มีอะไรเปลี่ยนในสายตาผู้ใช้ ทำหลังสุดได้)

- [ ] **แตก `components/DayStopsSection.tsx`** (973 บรรทัด) — `SortableStopRow` กินไป 330 บรรทัด แยกเป็นไฟล์ของตัวเอง
      (แบบเดียวกับที่แตก `app/page.tsx` ในเฟส 4 — ย้ายโค้ดตรงๆ ไม่เปลี่ยนพฤติกรรม)
- [ ] **ยก hook ที่เป็น global state ขึ้นเป็น Context** — `useHotels`/`useBookings`/`useCustomPlaces` ถูกเรียกซ้ำหลายจุด
      แต่ละครั้ง = fetch เต็มตาราง + เปิด realtime channel ใหม่ (`NearbyPlacesModal` เรียก `useCustomPlaces` เองอีกชุด)
- [ ] `DayStopsSection` คำนวณ `isOpenDuring` ซ้ำในแถว ทั้งที่ `useDaySchedule` คืน `closedStopIds` มาให้แล้ว

---

## 4. สิ่งที่ต้องให้ผู้ใช้ทำเอง (ผมทำแทนไม่ได้)

| เมื่อไหร่ | ต้องทำอะไร |
|---|---|
| เฟส 0 | บอกว่ามี GitHub remote ไหม (ถ้ายังไม่มี ต้องสร้างเพื่อ push กันของหาย) |
| เฟส 1 | เปิด **Routes API** ใน Google Cloud Console |
| เฟส 3 | เปิด **Maps JavaScript API** ใน Google Cloud Console |
| ทุกเฟส | รันไฟล์ SQL migration ใหม่ใน Supabase Dashboard → SQL Editor (ผมรัน SQL ตรงๆ ไม่ได้) |
| เฟส 5 | สร้าง Storage bucket ใน Supabase (หรือรัน SQL ที่ผมเตรียมให้) |
| เฟส 6.9 | ✅ ลบ `visited_at` ของ `Mun Dining` (วัน `d0`) แล้ว (8 ส.ค. 2026 — รันเอง) |
| เฟส 7 | อนุมัติเพิ่ม devDependency `vitest` + `vite-tsconfig-paths` (เฉพาะตอน dev ไม่ขึ้น production bundle) |
| เฟส 7-12 | **ไม่มี migration ใหม่เลยทั้ง 6 เฟส** — ตั้งใจออกแบบทางแก้ให้เลี่ยง SQL ทั้งหมด (ดูเหตุผลที่เฟส 9.1) |

---

## 5. กติกาการทำงานในโปรเจกต์นี้

- **คุยเป็นภาษาไทย** — โค้ด/ชื่อตัวแปร/ศัพท์เทคนิคเป็นอังกฤษได้ตามปกติ
- **Next.js เวอร์ชันนี้ต่างจากที่โมเดลเคยรู้จัก** — อ่าน `node_modules/next/dist/docs/` ก่อนใช้ API ที่ไม่แน่ใจ
- **ห้ามเรียก Google Maps API ตระกูล legacy** (`maps.googleapis.com/maps/api/*`) คีย์โปรเจกต์นี้ไม่ได้เปิดไว้ ใช้ตระกูลใหม่เท่านั้น (`places.googleapis.com`, `routes.googleapis.com`) และควรผ่าน helper ใน `lib/googlePlaces.ts`
- **migration ต้องให้ผู้ใช้ copy-paste รันเอง** แล้วยืนยันผลด้วย curl ก่อนเชื่อว่าฟีเจอร์ทำงาน อย่าเดาว่าเป็นบั๊กโค้ดก่อนเช็ก migration
- **ฐานข้อมูลนี้เป็นข้อมูลทริปจริงที่ใช้อยู่จริง 2 คน** — ตอนทดสอบใน browser ให้เลี่ยงการกดปุ่มที่แก้ข้อมูล ถ้าจำเป็นต้องแก้ ให้แก้กลับเป็นค่าเดิมเสมอ
- **ก่อนบอกว่าเสร็จ ต้อง `npm run lint` ผ่านเสมอ**
- **ปิดเฟสแล้วอัปเดตไฟล์นี้** ทุกครั้ง
