# แผนพัฒนา — เว็บแพลนเที่ยวเกาหลี

> ไฟล์นี้คือแหล่งความจริงเรื่อง "ตอนนี้ทำถึงไหน / ต่อไปทำอะไร / ทำไมตัดสินใจแบบนั้น"
> อ่านไฟล์นี้ก่อนเริ่มงานใหม่ทุกครั้ง และอัปเดตทุกครั้งที่ปิดเฟส
>
> ทริปจริง: **11–21 ต.ค. 2026** (บินไป 11 · เที่ยวเกาหลี 12–20 · บินกลับ 21) · แผนนี้เขียนเมื่อ **6 ส.ค. 2026** (เหลือ ~2 เดือน)

---

## 1. เว็บนี้ต้องทำอะไรได้ (เป้าหมาย)

เว็บตัวเดียวที่ทำงาน **2 โหมด** สำหรับคน 2 คน:

| โหมด | ใช้เมื่อไหร่ | ต้องทำอะไรได้ |
|---|---|---|
| **วางแผน** | ตอนนี้จนถึงก่อนบิน | เลือกที่เที่ยว/ร้านอาหาร จัดลำดับแต่ละวัน ดูว่าเวลาพอไหม เก็บตั๋ว/booking เตรียมของ |
| **ใช้จริง** | ระหว่างอยู่เกาหลี | เปิดมือถือแล้วรู้ทันทีว่าตอนนี้ต้องไปไหน นำทางได้เลย ดูตั๋วของวันนี้ได้ |

**เกณฑ์คุณภาพที่ตกลงกัน:** สวย น่าใช้งาน และใช้ได้ดีจริงทั้งบนมือถือและคอมพิวเตอร์

### ต้องมี (ตกลงแล้ว)
- เวลาเดินทางของจริง ไม่ใช่ตัวเลขเดา
- แผนที่รวมเส้นทางทั้งวัน
- ร้านอาหาร / การวางมื้อ
- เวลาเปิด-ปิดของสถานที่
- การเดินทางระหว่างเมือง (กรอกเองจากที่จองจริง)
- Checklist ของที่ต้องเตรียม
- เก็บ booking/ตั๋วไว้ที่เดียว **อัปโหลดรูป/PDF ได้**
- หน้าแยก "วันนี้" เน้นมือถือ สำหรับใช้ตอนเที่ยวจริง

### ไม่ทำ (ตัดสินใจแล้ว อย่าย้อนกลับมาทำโดยไม่คุยก่อน)
- **งบประมาณ/ค่าใช้จ่าย** — ไม่ต้องการ ตัดออกเพื่อคุม scope
- ~~**ระบบออฟไลน์ / PWA**~~ — **กลับคำแล้ว (11 ส.ค. 2026)** ผู้ใช้ขอให้ทำ · **เฟส 18 เสร็จแล้ว (12 ส.ค. 2026)**
  เหตุผลเดิมที่ตัดออก (มี eSIM ตลอดทริป) ยังจริงอยู่ แต่จุดที่เน็ตหลุดจริงคืออุโมงค์รถไฟใต้ดินโซล/ปูซาน
  และเขาอุทยานซอรัคซาน (`d5`) ซึ่งเป็นวันที่พึ่งหน้า `/today` มากที่สุด · **ขอบเขต: offline แบบอ่านอย่างเดียว**
  (ดูแผนที่วางไว้แล้วได้ตอนเน็ตหลุด) **ไม่ทำ offline editing** เพราะเว็บนี้ sync สด 2 คนและใครมีลิงก์ก็แก้ได้
  การแก้แบบ offline แล้ว sync กลับจะเปิดปัญหา conflict resolution ทั้งกอง ไม่คุ้มกับทริป 11 วัน
- **ระบบล็อกอินรายคน** — ยังไม่ทำ ใช้กัน 2 คนที่ไว้ใจกัน
  **แต่ตอนนี้มีด่าน PIN แล้ว (เฟส 13.5, 11 ส.ค. 2026)** — ยังเป็น "แชร์ลิงก์ + รหัสร่วมกัน" ไม่ใช่บัญชีรายคน
  · PIN เก็บใน env `TRIP_PIN` (ดู `.env.local` — ไม่ได้ commit) · ⚠️ ด่านนี้กันหน้าเว็บ+API เท่านั้น **ไม่ได้กัน Supabase**

---

## 2. สถานะปัจจุบัน (ตรวจจากโค้ดจริง 6 ส.ค. 2026)

**Stack:** Next.js 16 (App Router) + TypeScript + Tailwind v4 + Supabase (anon key ตรงจาก browser, RLS สาธารณะ) + Google Places API (New) ผ่าน API route ฝั่งเซิร์ฟเวอร์ + dnd-kit · deploy บน Vercel (`korea-trip-plan`)

### ที่ทำงานได้จริงแล้ว
- ระบบ **"แผน" หลายเวอร์ชัน** — สร้าง/ก๊อป/เปลี่ยนชื่อ/ลบ ทำแผน A/B เทียบกันได้ (`trip_plans` + `trip_meta`)
- **ที่พักต่อ leg** — จับวันที่นอนเมืองเดียวกันติดกันมัดเป็น leg อัตโนมัติจาก `data/itinerary.ts` ค้นหาโรงแรมจาก Google ได้
- **จุดแวะต่อวัน** — ลากจัดลำดับ / ลากข้ามวัน / ลากกลับคืนคลัง (`trip_stops`)
- **Timeline เวลาถึง-ออก** — คำนวณจากเวลาเริ่มวัน + เวลาเดินทาง + เวลาที่อยู่แต่ละจุด
- **คลังสถานที่** — 39 ที่คัดสรรใน `data/places.ts` + เพิ่มเองผ่าน Google Places + ซ่อนที่ไม่เอา + รูปจาก Google + แผนที่ฝัง + คลิป YouTube
- ตั๋ว/booking + checklist ของเตรียม + เดินทางระหว่างเมืองกรอกเอง (เฟส 5)
- **หน้า "วันนี้" (`/today`)** สำหรับใช้จริงตอนเที่ยว — จุดถัดไปเด่น + นำทาง 3 แอป + ติ๊กมาถึงแล้ว + เลื่อน timeline อัตโนมัติเมื่อช้ากว่าแผน (เฟส 6)
- **หน้าสรุปแผน (`/summary`)** — ดูอย่างเดียวทั้งทริป แก้อะไรไม่ได้ · **ล็อกวันที่ลงตัวแล้ว** กันเผลอลากตอนเลื่อนดู · แถบเมนูล่างบนมือถือสลับ 3 หน้า
- ทุกอย่าง **sync สดระหว่าง 2 คน** ผ่าน Supabase Realtime · `npm run lint` สะอาด

**ทั้ง 6 เฟสตามแผนเดิมเสร็จหมดแล้ว (7 ส.ค. 2026)** — เว็บใช้งานได้ครบตามเป้าหมายข้อ 1 ทุกข้อ เหลือแค่ปรับแต่งจากการใช้งานจริงเป็นเคสๆ ไป

### หนี้/ปัญหาที่ต้องเคลียร์
1. ~~เวลาเดินทางทุกตัวเป็นของเดา~~ — แก้แล้ว (เฟส 1) ขนส่งสาธารณะเป็นของจริงจาก Routes API, เดิน/แท็กซี่ยังเป็นประมาณการ (ดูข้อจำกัดด้านล่าง)
2. ~~งานทั้งหมดยังไม่ commit~~ — แก้แล้ว (เฟส 0)
3. ~~โค้ดตาย 5 ไฟล์~~ — ตรวจแล้ว (7 ส.ค. 2026) ไม่มีอยู่จริงในโค้ดแล้ว
4. ~~README ล้าสมัย~~ — ตรวจแล้ว (7 ส.ค. 2026) ไม่มีข้อความล้าสมัยแบบที่บันทึกไว้
5. ~~migration `0009_trip_stops_replica_identity.sql`~~ — รันแล้ว (7 ส.ค. 2026)
6. ~~migration `0010_travel_time_cache.sql`~~ — รันแล้ว ยืนยันด้วย curl (6 ส.ค. 2026)

**หนี้รอบใหม่ (จากการตรวจโค้ดทั้งโปรเจกต์ 7 ส.ค. 2026):** พบบั๊ก 14 ตัว + ปัญหา performance ที่ `lint`/`tsc` จับไม่ได้
เฟส 7-9 (แก้บั๊กเวลา/ลำดับจุดแวะ/แคช+performance ทั้งหมด) **แก้เสร็จแล้ว (7 ส.ค. 2026)** เหลือลบ test data
1 แถวใน DB ที่รอผู้ใช้อนุมัติ + เฟส 10-12 (backup/print/rate-limit และงานเสริม) ที่ยังไม่เริ่ม
→ รายละเอียดทั้งหมดอยู่ที่ **[หัวข้อ 3.5 — เฟส 7-12](#35-รอบตรวจโค้ดทั้งโปรเจกต์-7-สค-2026--เฟส-7-12)** ด้านล่าง

### ⚠️ ข้อจำกัดสำคัญ (ยืนยันแล้วด้วยการทดสอบจริง 6 ส.ค. 2026): Google นำทางในเกาหลีใต้ได้ไม่ครบ
กฎหมายเกาหลีห้ามส่งออกข้อมูลแผนที่ละเอียดไปเซิร์ฟเวอร์ต่างประเทศ Google จึงให้บริการได้แค่ **เส้นทางขนส่งสาธารณะ (TRANSIT)** ส่วน **DRIVE / WALK คืนค่าว่างจริงตามที่คาด** ในเกาหลี

**ผลกระทบ:** ปุ่มเลือกโหมด "เดิน 🚶 / แท็กซี่ 🚕" จะเห็นเวลาเป็นค่าประมาณการ (ติดป้าย "(ประมาณการ)" ใน UI) — **ตัดสินใจแล้ว (6 ส.ค. 2026): ไม่เพิ่ม ODsay/Kakao/Naver** เพราะสมัครยาก (ต้องยืนยันตัวตนแบบเกาหลี) ยอมรับสภาพนี้ไปก่อน ถ้าจำเป็นจริงๆ ค่อยกลับมาคุยใหม่ อย่าเพิ่มเองโดยไม่คุยก่อน

---

## 3. แผนงานเป็นเฟส

### เฟส 0 — กันของหาย + เคลียร์พื้น ✅ เสร็จ (7 ส.ค. 2026)
เล็กแต่สำคัญที่สุด เพราะตอนนี้งาน 2 วันเต็มยังไม่มี backup

- [x] `git add` ทั้งหมด + commit + push ขึ้น remote (`github.com/parkALoha/Plan-Korea`)
- [x] ลบโค้ดตาย 5 ไฟล์ (ดูรายการข้อ 3 ด้านบน)
- [x] เขียน `README.md` ใหม่ให้ตรงกับความจริง แล้วชี้มาที่ไฟล์นี้เป็นแหล่งแผนงาน
- [x] รัน migration `0009` แล้ว (แก้บั๊ก DELETE realtime ไม่ sync)

---

### เฟส 1 — เวลาเดินทางของจริง ✅ เสร็จ (6 ส.ค. 2026)
Timeline ทั้งทริปตอนนี้เชื่อไม่ได้ ทุกฟีเจอร์ที่พึ่งเวลา (เตือนสถานที่ปิด, หน้า "วันนี้") จึงต้องรอเฟสนี้

**ต้องให้ผู้ใช้ทำก่อน:** เปิด **Routes API** ใน Google Cloud Console (โปรเจกต์เดียวกับคีย์ปัจจุบัน)

- [x] ยิงทดสอบจริง 3 โหมด × คู่จุดจริงในทริป (ในปูซาน) 6 ส.ค. 2026: **ขนส่งสาธารณะ = ของจริง** (มิเป่→หาดแฮอึนแด 41 นาที ยืนยันด้วย curl ตรงไปที่ `travel_time_cache`) · **เดิน/แท็กซี่ = ว่าง** (`isReal:false`) ตรงกับที่คาดไว้เรื่องข้อจำกัดกฎหมายแผนที่เกาหลี
- [x] `lib/travelProvider.ts` — interface เดียวสำหรับคำนวณเวลาเดินทาง สลับ backend ได้
- [x] เขียน `/api/travel-time` ใหม่ด้วย `routes.googleapis.com/directions/v2:computeRoutes` — ลบ route เดิมที่เรียก legacy API
- [x] แคชผลถาวรใน Supabase (ตาราง `travel_time_cache`, migration `0010`) — ยืนยันแล้วว่าเขียน/อ่านได้จริง
- [x] **UI แยกให้ชัดว่าตัวเลขไหนของจริง ตัวเลขไหนประมาณการ** — `DayStopsSection` ติดป้าย "(จริง)" / "(ประมาณการ)" แล้ว
- [x] **ตัดสินใจแล้ว (6 ส.ค. 2026): ไม่เพิ่ม ODsay/Kakao/Naver ตอนนี้** — สมัครยาก (ต้องยืนยันตัวตนแบบเกาหลี) ยอมรับว่าเดิน/แท็กซี่เป็นประมาณการไปก่อน ถ้าจำเป็นจริงๆ ค่อยกลับมาคุยใหม่ อย่าเพิ่มเองโดยไม่คุยก่อน

**เฟส 1 เสร็จแล้ว** ✅

---

### เฟส 2 — ร้านอาหาร + เวลาเปิด-ปิด ✅ เสร็จ (7 ส.ค. 2026)
ใช้ Places API (New) ที่เปิดอยู่แล้ว ไม่ต้องเปิด API เพิ่ม

- [x] ตารางใหม่ `place_details_cache` (migration `0011`) — resolve แต่ละสถานที่เป็น Google place ID ครั้งเดียว เก็บพร้อม `regularOpeningHours` ผ่าน `/api/place-details`
- [x] แสดงเวลาเปิด-ปิดบนการ์ดสถานที่ (`PlaceCard` — เวลาของวันที่โฟกัสอยู่จริงๆ) และในหน้ารายละเอียด (`PlaceDetailModal` — ลิสต์ครบ 7 วัน)
- [x] **เตือนใน timeline ถ้าเวลาที่คำนวณได้ตกนอกเวลาเปิด** — `lib/openingHours.ts` (`isOpenDuring`) + `DayStopsSection` แสดงป้ายเตือนใต้แถว ทดสอบจริงแล้วเห็นป้ายเตือนทำงานถูกต้อง
- [x] เพิ่มหมวด "ร้านอาหาร" ในคลัง — ปุ่ม "🍽️ ร้านใกล้ๆ" เปิด `NearbyRestaurantsModal` ยิง Nearby Search (New) รอบจุดแวะล่าสุดของวันนั้น (หรือที่พัก/จุดกึ่งกลางเมืองถ้ายังไม่มีจุดแวะ) ทดสอบจริงแล้วเพิ่มร้านลงวันได้
- [x] **วางมื้ออาหาร** — ใช้ `trip_stops` เดิม เพิ่มหมวด `restaurant` แล้ว หัวการ์ดแต่ละวันโชว์ "🍽️ วางมื้อไว้ N มื้อ" หรือเตือนถ้ายังไม่มี

migration `0011_place_details_cache.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `place_details_cache` (7 ส.ค. 2026)

**ส่วนเสริมหลังปิดเฟส (7 ส.ค. 2026) — ตามฟีดแบ็กจากการใช้งานจริง:**
- [x] ร้านอาหารโชว์เรทติ้ง/จำนวนรีวิว/ประเภทอาหาร + กดเข้าไปดูรีวิว 3 อันล่าสุด/รูปภาพ/เวลาเปิด-ปิดครบก่อนตัดสินใจเพิ่มได้ (migration `0012` เพิ่มคอลัมน์ `rating`, `user_rating_count`, `primary_type`, `reviews` ใน `place_details_cache`)
- [x] "+ เพิ่มสถานที่เอง" มี autocomplete แนะนำระหว่างพิมพ์แล้ว (เหมือนช่องค้นหาที่พัก) ไม่ต้องกดค้นหาแล้วเลือกจากลิสต์เท่านั้น
- [x] มีช่องค้นหาร้านด้วยชื่อใน `NearbyRestaurantsModal` เผื่อร้านอยู่นอกรัศมี Nearby Search หรือรู้ชื่อเป๊ะๆ อยู่แล้ว
- [x] โน้ตสั้นๆ ต่อจุดแวะ (migration `0013` เพิ่มคอลัมน์ `trip_stops.note`) — กด "+ โน้ต" ใต้แต่ละจุดเพื่อจดข้อความอิสระ เช่น "ร้านนี้อร่อย รีบไป"

migration `0012_place_details_extra.sql` และ `0013_trip_stop_notes.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `place_details_cache`/`trip_stops` (7 ส.ค. 2026)

---

### ส่วนเสริม — ตารางบินจริง + วันเดินทาง + คืนที่ยังเลือกเมืองได้ ✅ เสร็จ (7 ส.ค. 2026)

เดิมแพลนเริ่มที่วันแรกในปูซานเลย ไม่มีวันบิน ทำให้ไม่รู้ว่าต้องออกจากที่ไหนกี่โมง และช่วงพักเครื่องฮานอย 11 ชม. หายไปทั้งก้อน

- [x] `Day.events` (`DayEvent[]`) ใน `data/itinerary.ts` — เที่ยวบิน/เดดไลน์ที่จองมาแล้ว แก้ในเว็บไม่ได้ แสดงเป็นแถบแยกเหนือจุดแวะ (`DayEventsPanel` ใน `DayStopsSection`) ไม่ปนกับจุดแวะที่ลากจัดลำดับได้
- [x] **วัน `d0` (11 ต.ค.)** — VN610 สุวรรณภูมิ 11:55 → ฮานอย 13:55 · พักเครื่อง 11 ชม. 20 น. · VN428 ฮานอย 01:15 → กิมแฮ 07:05 · พร้อมเดดไลน์ "ออกจากเมืองเก่า 22:00"
- [x] **วัน `d10` (21 ต.ค.)** — เช็คเอาต์ 05:45 · AREX → ICN · VN409 อินชอน 10:35 → โฮจิมินห์ 13:45 · ต่อเครื่อง 3 ชม. 5 น. · VN607 → สุวรรณภูมิ 18:30
- [x] เมืองใหม่ **ฮานอย** ในคลังสถานที่ (7 ที่ในย่านเมืองเก่า เดินถึงกันหมด) — ใช้ระบบเดิมทั้งหมด (เวลาเปิด-ปิด/ร้านใกล้ๆ/เพิ่มเอง ใช้ได้ปกติ)
- [x] `Day.noHotel` — วันบิน/นอนบนเครื่องไม่ถูกนับเป็น leg ที่พัก (`deriveHotelLegs` ข้ามให้) leg เดิมทั้ง 4 ก้อนและโรงแรมที่ตั้งไว้แล้วไม่กระทบ
- [x] `Day.overnightOptions` + `trip_meta.overnight_overrides` (migration `0014`) — **คืน 16 ต.ค. สลับระหว่างคังนึง (ที่จองไว้จริง) กับซกโชได้จากหัวการ์ดวันนั้น** สลับแล้ว leg ที่พักคำนวณใหม่ให้เอง sync สดทั้ง 2 คน · ของเดิมไม่ถูกลบ สลับกลับได้ตลอด · migration รันแล้ว (7 ส.ค. 2026)
- [x] **`DayEvent.anchor` ("before"/"after")** — วันที่มีช่วงว่างจริงระหว่างเที่ยวบิน (เช่นพักเครื่องฮานอย) แทรกจุดแวะเข้าไปในช่วงนั้นได้เหมือนวันปกติทุกอย่าง เวลาบินเองแก้ไม่ได้ แต่ตารางจุดแวะจะ**เริ่มนับอัตโนมัติจากเวลาที่เหตุการณ์ anchor "before" จบ** (เช่น ถึงย่านเมืองเก่า 15:30) แทนช่อง "ออกเดินทาง" ที่ตั้งเองได้ปกติ และมีป้ายเตือนถ้าจุดแวะที่วางไว้จบช้ากว่าเดดไลน์ anchor "after" (เช่น ต้องออกไปขึ้นเครื่อง 22:00) — วัน `d0` ใช้ฟีเจอร์นี้กับช่วงพักเครื่องฮานอย ทดสอบเพิ่ม/ลบจุดแวะจริงในเบราว์เซอร์แล้ว

**ผู้ใช้ต้องทำ:** ไม่มี — migration `0014` รันแล้ว

---

### เฟส 3 — แผนที่รวมเส้นทางทั้งวัน ✅ เสร็จ (7 ส.ค. 2026)
ผู้ใช้เปิด **Maps JavaScript API** บนคีย์เดิม `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` ใน Google Cloud Console แล้ว ยืนยันด้วยการทดสอบจริงในเบราว์เซอร์ — เห็นแผนที่ Busan จริงพร้อมหมุดเลข 1-2-3 เรียงตามลำดับจุดแวะถูกต้อง

- [x] แผนที่ต่อวัน: หมุดเลข 1-2-3 ตามลำดับจุดแวะ + เส้นเชื่อม + หมุดโรงแรมคนละสี — `components/DayMapPanel.tsx` ใช้ `@vis.gl/react-google-maps` (`Marker` ทรงกลมสี + label ตัวเลข/🏨 + `Polyline`) มี fallback ข้อความเมื่อไม่มีคีย์ **หมายเหตุ:** ตอนแรกใช้ `AdvancedMarker`+`Pin`+`mapId="DEMO_MAP_ID"` แต่เจอ `ApiProjectMapError` เมื่อมีหลายแผนที่พร้อมกันบนหน้าเดียว (DEMO_MAP_ID ไม่รองรับการใช้งานที่ scale หลาย instance) เปลี่ยนมาใช้ `Marker` แบบคลาสสิกกับ `google.maps.SymbolPath.CIRCLE` แทน ไม่ต้องพึ่ง Map ID เลย แก้ปัญหาได้
- [x] กดหมุด → เลื่อนไปแถวนั้นในลิสต์ / กดแถว → หมุดเด้ง (สองทาง) — `activeStopId` ใน `DayStopsSection` ผูกทั้งสองทาง ทดสอบฝั่งกดชื่อในลิสต์แล้วไฮไลต์ ring ติดแถวถูกต้อง (ฝั่งกดหมุดใช้ handler เดียวกัน `onSelectStop`)
- [x] มือถือ: แท็บสลับ "ลิสต์ / แผนที่" · คอม: วางคู่กันซ้าย-ขวา — ทดสอบแล้วทั้งจอมือถือ (375px) และจอใหญ่ (1400px)
- [x] จำกัด key ด้วย HTTP referrer ให้เฉพาะโดเมน Vercel + localhost — ผู้ใช้ตั้งค่าใน Google Cloud Console แล้ว

---

### เฟส 4 — รื้อ UI ให้สวยและ mobile-first ✅ เสร็จเกือบหมด (7 ส.ค. 2026)
เดิมทั้งเว็บอยู่ใน `app/page.tsx` ไฟล์เดียว 561 บรรทัด และเป็น desktop-first (มือถือใช้ได้แต่ไม่ได้ถูกออกแบบมา) — ตอนนี้แตกโครงแล้ว, มือถือมี bottom sheet, คอมใช้พื้นที่จอกว้างขึ้น, มี skeleton ตอนโหลด, ปุ่มมี feedback เหลือแค่ "การเปลี่ยนหน้าลื่น" ที่ตั้งใจข้ามไปก่อนเพราะยังไม่มี route ให้เปลี่ยนจริง (รอเฟส 6)

**รอบแก้ของพัง (7 ส.ค. 2026) — ตรวจของจริงในเบราว์เซอร์ที่ 360 / 375 / 768 / 1350px:**
- [x] **ทั้งเว็บล้นออกนอกจอมือถือ (กว้าง 559px บนจอ 375px เลื่อนซ้ายขวาได้)** — ต้นเหตุคือการ์ดที่พังใน `HotelLegsPanel` เป็น `flex flex-wrap` ซึ่ง flex item กว้างตามเนื้อหา `truncate` ข้างในเลยไม่มีผล ชื่อโรงแรมจาก Google ที่พ่วงที่อยู่เต็มมาด้วยจึงดันหน้าเว็บ · แก้เป็น `grid` (1 คอลัมน์มือถือ / 2 คอลัมน์ ≥`sm`) + `min-w-0` → ล้นแนวนอนเหลือ 0 ทุกความกว้าง
- [x] **ชื่อสถานที่ในแถวจุดแวะเหลือ 74px จาก 341px จนอ่านไม่ออก** — แถวเดียวยัด 5 อย่าง (จับลาก/เวลา/ชื่อ/ปรับเวลา/ลบ) · มือถือยกปุ่มปรับเวลา+ลบลงไปอยู่แถวโน้ตที่มีอยู่แล้ว (ไม่เพิ่มความสูง) → **ชื่อได้ 225px** · จอ `sm` ขึ้นไปยังเป็นแถวเดียวเหมือนเดิม
- [x] **ปุ่ม +/− เวลาที่อยู่เล็กเกินนิ้ว** — 20×18 → **36×36** บนมือถือ (คอม 28×28) พร้อมกวาดปุ่มเล็กอื่นทั้งเว็บ: เลือกโหมดเดินทาง 23→31px · ชื่อสถานที่ 24→36px · "+ แทรกร้านอาหาร" 17→33px · เลือกเมืองนอน 24→30px → **ปุ่มที่เล็กกว่า 28px เหลือ 0**
- [x] ช่องพิมพ์โน้ตแคบจนพิมพ์ไม่ได้จริง — มือถือให้ช่องพิมพ์กินเต็มบรรทัด ปุ่มบันทึก/ยกเลิก/ลบ ตกลงบรรทัดล่าง
- [x] ปุ่มลอย "📍 สถานที่" ทับปุ่ม "+ เพิ่มสถานที่" ของวันสุดท้าย — เพิ่ม `pb-28` ให้เนื้อหา + ปุ่มลอยเผื่อ `env(safe-area-inset-bottom)` ของ iPhone
- [x] ที่อยู่โรงแรมกิน 2-3 บรรทัดในหัวการ์ดวัน — ตัดเหลือบรรทัดเดียว

**เหลือทำ (งานรื้อโครง):**
- [x] **แตก `app/page.tsx` เป็นโครงที่อ่านออก** (7 ส.ค. 2026) — ลดจาก 561 → 356 บรรทัด แยกออกเป็น: `components/TripHeader.tsx` (ส่วนหัว + ตัวสลับแผน), `hooks/useLegacyBootstrap.ts` (bootstrap จากระบบ slot เดิม), `hooks/useHotelSchedule.ts` (hotelLegs/hotelForDay/hotelBeforeDay), `hooks/useTripDnd.ts` (sensors + drag handlers ทั้งหมด) — ย้ายโค้ดตรงๆ ไม่เปลี่ยนพฤติกรรม ผ่าน `npm run lint` + `tsc --noEmit` สะอาด ทดสอบจริงในเบราว์เซอร์แล้ว (header/plan switcher/sidebar/drag ทำงานปกติ)
- [x] **มือถือ: คลังสถานที่เป็น bottom sheet แทน overlay เต็มจอ** (7 ส.ค. 2026) — `components/PlaceSidebar.tsx` เปลี่ยน `MobileOverlay` เป็น `BottomSheet`: เลื่อนขึ้นจากขอบล่าง (max 85vh) มีฉากหลังจางๆ ให้เห็นบริบทหน้าเดิม + หัวชีตโชว์เมือง/วันที่ที่กำลังเพิ่มให้ตลอด (แก้ปัญหาเดิมที่เสียบริบท) แตะฉากหลังหรือปุ่ม ✕ เพื่อปิด แอนิเมชัน slide-up ใน `app/globals.css` ผ่าน lint + typecheck สะอาด ทดสอบจริงที่ 375px แล้ว (เปิดชีตเห็น context ถูกต้อง)
- [x] **คอม: ใช้พื้นที่จอกว้างให้คุ้ม** (7 ส.ค. 2026) — ต้นเหตุคือ container กว้าง `max-w-5xl` (1024px) แต่คอลัมน์จุดแวะข้างในยังโดน `max-w-2xl` (672px) ตรึงซ้อนอยู่อีกชั้น ทำให้แผนที่ต่อวัน+ลิสต์บีบเหลือ ~372px ทั้งที่จอเหลือพื้นที่เยอะ — ขยาย container เป็น `lg:max-w-7xl` (1280px) และปลด `max-w-2xl` ออกจากคอลัมน์เนื้อหาที่ `lg:` (`lg:max-w-none`) ให้ `flex-1` ทำงานจริง วัดจริงในเบราว์เซอร์ที่จอ 1600px: คอลัมน์จุดแวะกว้างขึ้นจาก ~344px → 904px หัวเว็บ (`TripHeader`) ขยายกว้างตามด้วยเพื่อให้ขอบซ้ายชิดกับเนื้อหาด้านล่าง ผ่าน lint + typecheck สะอาด
- [x] **ระบบสี/ระยะห่าง/ขนาดตัวอักษร — ตรวจแล้ว ไม่ต้องแก้** (7 ส.ค. 2026) — ไล่ดู `text-*` ทั้งเว็บ (67× `text-xs`, 45× `text-sm`, 19× `text-[11px]`, 10× `text-[10px]`) ตัวเลข px ที่ดูเหมือนสุ่มจริงๆ คือ tier ขนาดที่ตั้งใจ (11px = meta text รอง, 10px = ป้ายจิ๋วบนแผนที่/badge) ใช้สม่ำเสมอทั้งเว็บอยู่แล้ว ไม่ใช่ความไม่สอดคล้องจริง — สีก็ยึด `@theme` เดิมทั้งหมดไม่มีสีหลุด ไม่พบจุดที่ต้องแก้จริง (ตรงกับที่ PLAN.md บันทึกไว้แต่แรกว่าธีมดีอยู่แล้ว)
- [x] **สถานะกำลังโหลดให้ดูดี** (7 ส.ค. 2026) — เพิ่ม `components/DayCardSkeleton.tsx` (โครงการ์ดวัน ทรงเดียวกับ `DayStopsSection` จริง + `animate-pulse`) แทนข้อความเปล่า "กำลังโหลด..." ใน `app/page.tsx` ตอนโหลดครั้งแรก และแก้ `PhotoGallery.tsx` ให้โชว์โครง grid รูปแทนข้อความ "กำลังโหลดรูป..." ผ่าน lint + typecheck สะอาด (สถานะว่างเปล่าอื่นๆ อย่าง "ยังไม่มีจุดแวะ" / "เลือกครบทุกที่ในโซนนี้แล้ว 🎉" มีข้อความ+อีโมจิที่ช่วยนำทางอยู่แล้ว ไม่ต้องแก้)
- [x] **ปุ่มกดแล้วมีการตอบสนอง** (7 ส.ค. 2026) — เพิ่มกฎ CSS ทั่วเว็บใน `app/globals.css` (`button:active { opacity: 0.7 }`) ครอบคลุมทุกปุ่มโดยไม่ต้องไล่แก้ทีละไฟล์ ใช้ opacity ไม่ใช่ scale เพราะที่จับลาก (`⠿`) ก็เป็น `<button>` เหมือนกัน scale จะไปกวน bounding box ตอน dnd-kit เริ่มวัดระยะลาก ผ่าน lint + typecheck สะอาด
- [x] **การเปลี่ยนหน้าลื่น** (7 ส.ค. 2026) — ตอนนี้มี route ที่สองแล้ว (`/today` จากเฟส 6) ใช้ `next/link` มาตรฐานทั้งสองทิศทาง ("📍 วันนี้" ใน `TripHeader` ↔ "← หน้าแผน" ใน `/today`) Next.js prefetch + client-side transition ให้อัตโนมัติ ไม่ต้องเขียนอะไรเพิ่ม ทดสอบจริงแล้วเปลี่ยนหน้าลื่น ไม่มี full page reload
- [x] **ไม่มีจอกระพริบตอนโหลด — ตรวจแล้ว ไม่มีปัญหา** (7 ส.ค. 2026) — ใช้ `next/font/google` (`Noto_Sans_Thai` ใน `app/layout.tsx`) ซึ่ง self-host ฟอนต์ + จัดการ `font-display` ให้อัตโนมัติอยู่แล้ว ไม่พบ FOUC จริง

---

### เฟส 5 — ตั๋ว/booking + checklist + เดินทางระหว่างเมือง ✅

- [x] **ตาราง `bookings`** (7 ส.ค. 2026) — migration `0016_bookings.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `bookings` · ประเภท (เที่ยวบิน/ที่พัก/KTX/บัส/ตั๋วเข้าชม/อื่นๆ), ผูกกับวันได้ (ไม่บังคับ), วันที่/เวลา, เลขที่จอง, ลิงก์, โน้ต · UI ใหม่ `components/BookingsPanel.tsx` + `components/BookingEditModal.tsx` (เพิ่ม/แก้/ลบ) แสดงอยู่ใต้แผงที่พักในหน้าแรก · hook `hooks/useBookings.ts` sync สดผ่าน Supabase Realtime เหมือนที่พัก · ทดสอบเพิ่ม/แก้/ลบจริงในเบราว์เซอร์แล้ว ผ่าน lint + typecheck สะอาด
- [x] **ไฟล์แนบตั๋ว (รูป/PDF)** (7 ส.ค. 2026) — bucket `booking-files` สร้างใน Supabase Dashboard (Public) + migration `0019_bookings_file.sql` เพิ่มคอลัมน์ `file_url`/`file_name` ใน `bookings` และ policy อ่าน/เขียน/ลบสาธารณะบน `storage.objects` (ต้องตั้งแยกจากตารางปกติ เพราะ Public bucket แค่เปิดอ่าน ไม่ได้เปิดเขียนให้อัตโนมัติ) · `BookingEditModal.tsx` เพิ่มช่องอัปโหลดไฟล์ (รูป/PDF) อัปโหลดตรงไป Supabase Storage แล้วเก็บ public URL, แสดงเป็นลิงก์เปิดไฟล์ + ลบไฟล์ได้ · `BookingsPanel.tsx` โชว์ไอคอน 📎 เมื่อมีไฟล์แนบ · ยืนยัน bucket/policy ด้วย curl จริง (upload/read/delete ผ่านหมด HTTP 200) — **หมายเหตุ:** การอัปโหลดไฟล์จริงผ่าน UI ยังไม่ได้ทดสอบในเบราว์เซอร์ (เครื่องมือ automation ตอนนี้ set ค่า `<input type=file>` ไม่ได้) แต่ path อัปโหลด/ดึง public URL เดียวกับที่ curl ยืนยันแล้วว่าใช้ได้จริง
- [x] **ตาราง `checklist_items`** (7 ส.ค. 2026) — migration `0018_checklist_items.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `checklist_items` · ข้อความ, ติ๊กแล้วหรือยัง, ใครติ๊ก (`checked_by`) · UI ใหม่ `components/ChecklistPanel.tsx` แสดงอยู่ใต้แผง booking ในหน้าแรก เพิ่ม/ติ๊ก/ลบได้ ติ๊กแล้วเลื่อนไปท้ายลิสต์ + ขีดฆ่า · hook `hooks/useChecklist.ts` sync สดผ่าน Supabase Realtime แบบเดียวกับ bookings · ผ่าน lint + typecheck สะอาด ทดสอบเพิ่ม/ติ๊ก/ลบจริงในเบราว์เซอร์แล้ว
- [x] **เดินทางระหว่างเมืองกรอกเอง** (7 ส.ค. 2026) — migration `0017_trip_stops_intercity.sql` รันแล้ว ยืนยันด้วย curl · เพิ่มคอลัมน์ `kind`/`intercity_from`/`intercity_to`/`intercity_mode` ใน `trip_stops` เดิม แถวเดินทางข้ามเมือง (kind="intercity") แทรกกลางลิสต์จุดแวะได้เลยผ่านปุ่ม "+ แทรกเดินทางข้ามเมืองตรงนี้" (คู่กับปุ่มแทรกร้านอาหารเดิมทุกตำแหน่ง) เปิด `IntercityEditModal` เลือกพาหนะ (บัส/KTX/อื่นๆ) + จาก/ไป + ระยะเวลา (มี preset ชม.) · **กินเวลาใน timeline จริง** โดยไม่ต้องแก้ `lib/schedule.ts` เลย — ใช้ฟิลด์ `dwell_minutes` เดิมเก็บระยะเวลาเดินทาง, `place_id` ว่างเปล่าทำให้จุดแวะก่อน/หลังไม่ถูกคำนวณระยะทางข้ามเมืองมั่วๆ (จุดหลังเริ่มนับเวลาใหม่จากตอนถึงปลายทางอัตโนมัติ) · ลาก/ลบ/ปรับเวลาใช้ระบบเดิมของ `trip_stops` ได้ฟรีทั้งหมด · แก้ `RouteSuggestionModal.tsx` ให้ตำแหน่งแถวเดินทางข้ามเมือง **ปักหมุดอยู่ที่เดิมเสมอ** ตอนกด "ลองจัดเส้นทางใหม่" (ของเดิมจะดันไปท้ายวันผิดๆ) · ทดสอบจริงในเบราว์เซอร์แล้ว (แทรกกลางวัน, timeline คำนวณใหม่ถูกต้อง, route suggestion ไม่ย้ายตำแหน่ง, ลบแล้วกลับสภาพเดิม) ผ่าน lint + typecheck สะอาด

---

### เฟส 6 — หน้า "วันนี้" (โหมดใช้จริงตอนเที่ยว) ✅ เสร็จ (7 ส.ค. 2026)
เฟสสุดท้ายเพราะพึ่งทุกเฟสก่อนหน้า แต่นี่คือเฟสที่ทำให้เว็บนี้มีค่าตอนอยู่เกาหลีจริง

**เตรียมพื้นฐานก่อนแตะ UI ใหม่:**
- [x] แยก `hooks/useDaySchedule.ts` ออกจาก `DayStopsSection.tsx` (เดิมฝังในนั้นล้วนๆ) — ย้ายโค้ดตรงๆ ไม่เปลี่ยนพฤติกรรม เพื่อให้หน้าแผนกับหน้า "วันนี้" ใช้ตรรกะคำนวณ timeline เดียวกันเป๊ะ (เวลาถึง-ออก, เวลาจริงจาก Google, เตือนสถานที่ปิด, ที่พักหัว-ท้ายวัน) ไม่เสี่ยงเวลาสองหน้าไม่ตรงกัน ทดสอบหน้าแผนเดิมในเบราว์เซอร์แล้วทำงานเหมือนเดิมทุกอย่าง

- [x] route ใหม่ `app/today/page.tsx` — ออกแบบสำหรับ **มือถือถือเดินอย่างเดียว** ตัวหนังสือใหญ่ ปุ่มใหญ่ กดง่ายด้วยมือเดียว (ปุ่มทุกปุ่ม ≥44px) · ยึด **วันจริงตามนาฬิกาเครื่อง** เป็นค่าเริ่มต้น (ก่อนทริปคืนวันแรก/หลังทริปคืนวันสุดท้าย) + สลับดูวันอื่นได้อิสระด้วยปุ่ม ‹ › และปุ่ม "กลับไปวันนี้จริง" — ตัดสินใจไว้ตอนวางแผน: ใช้เทสต์ได้ตอนนี้ (ส.ค.) และตอนเที่ยวจริงก็ใช้ดูแผนวันถัดไปได้
- [x] **จุดถัดไปเด่นที่สุดบนหน้าจอ** (การ์ดขอบส้มเด่น) **+ ปุ่มนำทางครบ 3 แอปเสมอ** (`lib/mapLinks.ts`) — Google (ลิงก์เว็บมาตรฐาน `maps.google.com/maps/dir` ใช้ได้ทุกที่รวมฮานอย) + Kakao (`map.kakao.com/link/to` เอกสารทางการ ไม่ต้องมี API key) + Naver (`nmap://` app scheme พร้อม fallback ไปหน้าค้นหาเว็บถ้าไม่มีแอป) — ตัดสินใจให้ครบ 3 เพราะเฟส 1 พิสูจน์แล้วว่า Google เดิน/ขับในเกาหลีคืนค่าว่าง คนเกาหลีใช้ Naver/Kakao กัน แต่ฮานอยกลับ Google ดีสุด ให้ผู้ใช้เลือกเองหน้างาน
- [x] แสดงเวลาเปิด-ปิดของจุดถัดไป (เตือนถ้าอาจปิดแล้ว ใช้ตรรกะเดียวกับหน้าแผน) + **ตั๋ว/booking ของวันนี้** (กรองจาก `bookings` ตาม `day_id`/วันที่)
- [x] ติ๊ก "มาถึงแล้ว" ได้ — migration `0020_trip_stops_visited.sql` เพิ่มคอลัมน์ `visited_at` ใน `trip_stops` รันแล้ว ยืนยันด้วย curl · `markVisited`/`unmarkVisited` ใน `useStops` · แตะแถวใน "ผ่านมาแล้ว" เพื่อยกเลิกติ๊กได้ (เผื่อกดพลาด)
- [x] **ถ้าออกช้ากว่าแผน → เลื่อน timeline ที่เหลือของวันให้ดูอัตโนมัติ** (`lib/liveDelay.ts`) — เทียบเวลาจริงที่ติ๊ก "มาถึงแล้ว" ล่าสุดกับแผน (แม่นสุด) หรือถ้ายังไม่ติ๊กจุดไหนเลยเทียบเวลาปัจจุบันกับจุดแรก · **แสดงผลอย่างเดียว ไม่เขียนกลับ DB เด็ดขาด** (ตัดสินใจไว้ตอนวางแผน) · เช็กเวลาเปิด-ปิดซ้ำด้วยเวลาที่เลื่อนแล้ว ขึ้นป้าย ⚠️ ถ้าจุดไหนอาจไปไม่ทันเวลาปิดเพราะความล่าช้า
- [x] ทั้ง 2 คนเห็นสถานะเดียวกันสด — ใช้ระบบ realtime เดิมของ `trip_stops` (ไม่ต้องเพิ่ม channel ใหม่)
- [x] ลิงก์สลับหน้าแผน ↔ วันนี้ — "📍 วันนี้" ใน `TripHeader` และ "← หน้าแผน" ใน `/today`

ทดสอบจริงในเบราว์เซอร์ทั้งหมด: สลับวัน, ป้ายเตือนสถานที่ปิดจริง (Gamcheon Village), ลิงก์นำทาง 3 แอปครบถูกต้อง, ติ๊ก/ยกเลิกติ๊ก "มาถึงแล้ว" round-trip, แถบเลื่อนเวลาเมื่อช้ากว่าแผน (เทียบกับตรวจซ้ำผ่าน curl ว่า state จริงใน DB สะอาด ไม่เหลือข้อมูลทดสอบค้าง) ผ่าน `npm run lint` + `tsc --noEmit` สะอาด

---

### รอบแก้จากการใช้งานจริงบนมือถือ ✅ เสร็จ (7 ส.ค. 2026)
ฟีดแบ็ก 6 ข้อหลังลองใช้จริงบนมือถือ:

- [x] **คลังสถานที่บนมือถือเลื่อนดูไม่ได้** — เจอ 2 สาเหตุซ้อนกัน (1) `dnd-kit` ใส่ `touch-action:none` บนการ์ดทุกใบ นิ้วที่ปัดเลยถูกตีความเป็น "เริ่มลาก" → ตัด `useDraggable` ออกทั้งหมดในชีตมือถือ (`draggable` prop ใน `PlaceSidebar`, จอใหญ่ยังลากได้ตามเดิม) เพราะมือถือมีปุ่ม "+ เพิ่มลงวันนี้" บนการ์ดอยู่แล้ว (2) กล่องคลังใน bottom sheet สูงตามเนื้อหา 3044px ทะลุกรอบชีต 615px แล้วโดน `overflow-hidden` ตัดทิ้ง (`h-full` คำนวณไม่ได้เพราะความสูงพ่อมาจาก flex) → เปลี่ยนเป็น `min-h-0 flex-1` ตลอดสาย · วัดยืนยันในเบราว์เซอร์แล้ว: ช่องเลื่อน 487px เลื่อนถึงท้ายสุดได้จริง
- [x] **วันบิน (11 และ 21 ต.ค.) ใช้ได้เหมือนวันอื่นแล้ว** — เดิมวันที่มี `anchor:"before"` ถูกบังคับเวลาเริ่มตายตัว ตั้งเองไม่ได้ ตอนนี้ตั้งเวลา "ออกเดินทาง" ได้ทุกวัน โดยเวลาจากตารางบินเป็นแค่**ค่าเริ่มต้นที่แนะนำ** (มีปุ่ม "กลับไปใช้เวลาแนะนำ" ถ้าปรับแล้วอยากคืนค่า) — เผื่อกรณีผ่าน ตม./รับกระเป๋าเร็วหรือช้ากว่าที่เผื่อไว้ · ตัว `DayEvent` (ตารางบิน/เดดไลน์) ยังล็อกอ่านอย่างเดียวเหมือนเดิม ติดป้าย "🔒 ตั๋วจองแล้ว แก้ไม่ได้" ให้ชัด
- [x] **ล็อกวันที่ลงตัวแล้ว** — migration `0021_day_lock.sql` (คอลัมน์ `is_locked` ใน `trip_day_settings`) · ปุ่มล็อกรายวันบนหัวการ์ด + ปุ่ม "🔒 ล็อกทุกวัน" รวมทั้งทริปใน `TripHeader` · วันที่ล็อกจะปิดทั้ง drag handle, ปุ่มปรับเวลาที่อยู่, ปุ่มลบ, โน้ต, ปุ่มแทรก, ตัวเลือกโหมดเดินทาง และปิด droppable/sortable ของ dnd-kit (มีด่านกันซ้ำใน `useTripDnd` เผื่อลากด้วยคีย์บอร์ด) — แก้ปัญหา "เผลอลากตอนเลื่อนดู"
- [x] **หน้าสรุปแผน `/summary`** — ดูอย่างเดียวทั้งทริป ไม่มีปุ่มแก้สักปุ่ม: ที่พักทุก leg, ตั๋ว/booking, ของที่ต้องเตรียม, แล้วตามด้วยการ์ดรายวันครบ 11 วัน (เวลาตายตัว, เวลาถึง-ออกทุกจุด, โน้ต, สรุปเวลาเดินทาง/ระยะทาง) · ใช้ `useDaySchedule` ตัวเดียวกับอีก 2 หน้า เวลาจึงตรงกันเสมอ
- [x] **เข้าถึงหน้า "วันนี้" ง่ายขึ้น** — `components/BottomNav.tsx` แถบเมนูล่างของมือถือ 3 ปุ่ม (แผนทริป · วันนี้ · สรุปแผน) อยู่ทุกหน้า ไม่ต้องเลื่อนขึ้นไปหาลิงก์บนหัวเว็บ · จอใหญ่ซ่อนไว้ ใช้ลิงก์บนหัวเว็บแทน (เพิ่ม "📋 สรุปแผน" เข้าไปด้วย)
- [x] `/today` อ่าน `trip_day_settings` แล้ว (เดิม hard-code เวลาเริ่ม 07:00 + โหมดขากลับ null) เวลาที่โชว์จึงตรงกับหน้าแผนจริงๆ

---

## 3.5 รอบตรวจโค้ดทั้งโปรเจกต์ (7 ส.ค. 2026) — เฟส 7-12

ตรวจโค้ดทั้งหมด (~9,200 บรรทัด) + ทดสอบจริงในเบราว์เซอร์ทั้ง 3 หน้า พบบั๊ก 14 ตัว + ปัญหา performance
`npm run lint` และ `tsc --noEmit` สะอาดทั้งคู่ — บั๊กทั้งหมดเป็นชนิดที่ tooling จับไม่ได้ ต้องอ่าน/ทดสอบเอง

**สถานะล่าสุด (11 ส.ค. 2026): เฟส 7-12 ปิดครบทุกเฟสแล้ว** — แผนทั้งไฟล์นี้ไม่เหลือข้อค้าง
(ยังเหลือ ~8 สัปดาห์ก่อนบิน · งานหลังจากนี้เป็นของใหม่ที่ยังไม่ได้วางแผน ค่อยคุยกันเพิ่มเป็นเรื่องๆ)

**สถานะ (7 ส.ค. 2026 กลางคืน): เฟส 7-9 เสร็จหมดแล้วภายในวันเดียว** (เร็วกว่าเป้า "จบภายใน ส.ค." มาก)
เหลือ เฟส 6.9 ข้อเดียว (ลบ test data ใน DB — รอผู้ใช้อนุมัติ/ทำเอง) + เฟส 10-12 ที่ยังไม่เริ่ม
ไม่มี migration ใหม่เลยทั้ง 3 เฟสที่ทำไปตามที่ตั้งใจไว้ · โค้ดที่แก้ทั้งหมดผ่าน `npm test` + `lint` + `tsc --noEmit`
สะอาด และยืนยันซ้ำในเบราว์เซอร์จริงแล้ว (ไม่ใช่แค่เชื่อจากทฤษฎี)

**ธีมของสิ่งที่เจอ:** เกือบทุกบั๊กคือ **edge case ของเวลา** (ค่าว่าง / ข้ามเที่ยงคืน / "วันนี้จริง" vs "วันที่กำลังดู")
ซึ่งเป็นจุดที่ unit test จับได้หมดในไม่กี่เคส — เฟส 7 จึงรวมการวางเทสต์ชุดแรกไว้ด้วย ไม่ใช่แค่แก้บั๊ก

**ลำดับเฟสอิงจาก "เหลืออีก ~9 สัปดาห์ก่อนบิน":** ความถูกต้องของเวลา → ความถูกต้องของข้อมูล →
ความเร็ว → กันข้อมูลหาย → ฟีเจอร์เสริม → ความสะอาดโค้ด
เป้าหมายเวลา: **เฟส 7-9 ให้จบภายใน ส.ค.** · **เฟส 10 ภายใน ก.ย.** · เฟส 11-12 เท่าที่มีเวลา

---

### เฟส 6.9 — งานจิ๋วทำก่อนเลย (< 15 นาที) ✅ เสร็จ (8 ส.ค. 2026)

- [x] **ล้าง test data ค้างใน DB จริง** — `Mun Dining` (วัน `d0`, stop id `stop-v2twml5x4msimwzds`) มี `visited_at`
      ติดอยู่จากการทดสอบเฟส 6 ทำให้ `/today` คิดว่า "มาถึงแล้ว" ผู้ใช้รัน SQL เองใน Supabase Dashboard
      (`UPDATE trip_stops SET visited_at = NULL WHERE id = 'stop-v2twml5x4msimwzds'`) ยืนยันด้วย SELECT แล้วว่าเป็น NULL
- [x] **ลบคอมเมนต์ล้าสมัย** `lib/schedule.ts:21` (7 ส.ค. 2026) — "(ดู `app/api/travel-time/route.ts` ที่ยัง broken อยู่)"
      route นั้นเขียนใหม่ตั้งแต่เฟส 1 แล้ว แก้เป็นข้อความที่ตรงกับความจริงปัจจุบัน

---

### เฟส 7 — เวลาที่เชื่อถือได้ + เทสต์ชุดแรก ✅ เสร็จ (7 ส.ค. 2026)
ทุกอย่างในเว็บนี้ห้อยอยู่กับ timeline ถ้าเวลาผิด หน้า "วันนี้" ก็พาไปผิดที่ผิดเวลาตอนเที่ยวจริง

**7.1 วางระบบเทสต์**
- [x] ติดตั้ง `vitest@2.1.9` + `vite-tsconfig-paths@5.1.4` — **หมายเหตุ:** ตัวล่าสุด (`vitest@4` ที่พึ่ง `rolldown`)
      พังบนเครื่องนี้เพราะ Node `v20.12.2` เก่ากว่าที่ต้องการ (`^20.19.0`) หา native binding ไม่เจอ ลง `vitest@2.1.9`
      แทน (ยัง esbuild-based ไม่พึ่ง rolldown) ใช้งานได้ปกติ · `vitest.config.mts` ต้องเป็นนามสกุล `.mts` ไม่ใช่ `.ts`
      เพราะ `vite-tsconfig-paths` เป็น ESM-only แล้ว config ปกติถูกโหลดแบบ CJS ในโปรเจกต์นี้
- [x] เพิ่ม script `npm test` (`vitest run`) ใน `package.json`
- [x] เขียนเทสต์ครอบ pure function 6 ไฟล์ (เพิ่ม `lib/stopOrdering.ts` จากเฟส 8 เข้ามาด้วย) — **50 เทสต์ ผ่านหมด**
      ไฟล์: `lib/__tests__/schedule.test.ts`, `openingHours.test.ts`, `liveDelay.test.ts`, `hotelLegs.test.ts`,
      `optimizeRoute.test.ts`, `stopOrdering.test.ts`, `hotelAnchorId.test.ts`

**7.2 `/today` บอกว่า "ช้ากว่าแผน" ทั้งที่ยังไม่ถึงวันทริป** ✅ แก้แล้ว
- [x] `isRealToday` เทียบ `day.date === todayIso` ตรงๆ แทน `dayIndex === todayIndex` — ยืนยันในเบราว์เซอร์แล้ว
      เปิด `/today` วันนี้ (7 ส.ค.) ไม่มีแถบ "ช้ากว่าแผน" อีกต่อไป ขึ้น "(ไม่ใช่วันนี้จริง)" ถูกต้อง

**7.3 ล้างช่อง "🕐 ออกเดินทาง" แล้วตารางทั้งวันกลายเป็น `NaN:NaN`** ✅ แก้แล้ว
- [x] กัน `""` ที่ `onChange` ของ `<input type="time">` ใน `DayStopsSection` ไม่ให้เรียก `onStartTimeChange` เลย
- [x] `timeToMinutes` คืน `null` เมื่อ parse ไม่ได้ (`lib/schedule.ts`) — ทุกจุดที่เรียกใส่ fallback ครบ
      (`computeSchedule` → 0, `useDaySchedule.effectiveStartTime` → เช็ค `.trim() !== ""` ก่อนใช้ค่า)
- [x] `useDaySettings.setStartTime` อัปเดต state ในเครื่องก่อน (optimistic) + เพิ่มด่านกัน `""` ซ้ำอีกชั้น
      ก่อนยิงขึ้น DB (defense-in-depth ตามที่ตกลงไว้ เผื่อมีทางอื่นเรียกเข้ามาในอนาคต)

**7.4 ป้ายเตือน "จบช้ากว่าเดดไลน์" เงียบสนิทเมื่อเลยเที่ยงคืน** ✅ แก้แล้ว
- [x] `computeSchedule` คืน `arrivalMinutes`/`departureMinutes` ต่อจุด (นาทีสะสมไม่ wrap) และ `endOfDayMinutes`
      ระดับวัน — `deadlineOverrunMinutes` ใน `useDaySchedule` เทียบด้วยค่าดิบนี้แทน parse สตริง `HH:MM` ที่ห่อรอบมาแล้ว
- [x] `isOpenDuring` เปลี่ยนมารับนาทีดิบ (ไม่ใช่ `HH:MM` string) + generalize การสร้างช่วงเปิดให้ครอบ
      เมื่อวาน/วันนี้/พรุ่งนี้ทั้ง 3 วัน (เดิมมีแค่เมื่อวาน/วันนี้) แก้เคสจุดแวะคร่อมเที่ยงคืนไปวันถัดไปได้ถูกทั้งสองทาง
      อัปเดตทุกจุดที่เรียก (`useDaySchedule.isClosedAt`, `DayStopsSection` ใช้ `closedStopIds` จาก hook แทนคำนวณเอง
      ซ้ำ — เก็บงานเฟส 12 ที่ตั้งใจไว้ไปด้วยเลย, `RouteSuggestionModal`, `app/today/page.tsx` 2 จุด)

**7.5 `/today` ไม่ข้ามวันเอง** ✅ แก้แล้ว
- [x] `todayIndex` คำนวณใหม่ทุกครั้งที่ `now` เดิน (ทุก 30 วิ) ผ่าน `useMemo` แทน freeze ด้วย `useState` ครั้งเดียว
      + `useRef`/`useEffect` ตามวันจริงอัตโนมัติเฉพาะตอนผู้ใช้ยังไม่กด ‹ › เลือกดูวันอื่นเอง (ไม่กระโดดทับ)

**7.6 `useDayOpeningHours` cancelled bug** ✅ แก้แล้ว (รวมเข้ากับ 9.2 — ดูด้านล่าง เขียนใหม่ทั้งไฟล์ไปพร้อมกัน)

**ยืนยันปิดเฟส:** `npm test` (50/50 ผ่าน) · `npm run lint` + `tsc --noEmit` สะอาด · เปิด `/today` จริงในเบราว์เซอร์
ไม่มีแถบ "ช้ากว่าแผน" ผิดๆ อีกแล้ว · ไม่มี console error

---

### เฟส 8 — ลำดับจุดแวะไม่เพี้ยน ✅ เสร็จ (7 ส.ค. 2026)
ทั้ง 3 ข้อรากเดียวกัน: `order_index` มีช่องว่างได้ แต่โค้ดบางที่คิดว่ามันคือ "ตำแหน่งใน array"

**8.1 `order_index` ชนกันหลังลากจุดแวะข้ามวัน** ✅ แก้แล้ว
- [x] ย้าย `nextOrderIndex`/`orderIndexAtPosition` ไปเป็น pure function ใน `lib/stopOrdering.ts` ใหม่ (เทสต์ตรงๆ
      ได้ ไม่ต้องพึ่ง React/Supabase) — `addStop` เปลี่ยนมาใช้ `max(order_index) + 1` แทน `dayStops.length`
- [x] `moveStopToDay` จัดเลขวันต้นทางที่เหลือใหม่ให้ต่อเนื่อง 0..n-1 ทันทีในคำสั่งเดียวกัน (ทั้ง state ในเครื่อง
      และ DB — เขียนเฉพาะแถวที่เลขจริงๆ เปลี่ยน ไม่ยิง update เปล่าๆ ทุกแถว)

**8.2 "+ แทรกร้านตรงนี้" แทรกผิดช่องเมื่อมีช่องว่าง** ✅ แก้แล้ว
- [x] `insertStopAt`/`insertIntercityAt` แปลง `atIndex` (ตำแหน่งใน array) → `order_index` เป้าหมายจริงก่อน
      ด้วย `orderIndexAtPosition` แล้วค่อยส่งให้ `shiftForInsert`
- [x] เทสต์เคส "วันมีช่องว่าง 0,2,3 แล้วแทรกตรงกลาง (atIndex=1)" ต้องได้ order_index=2 ไม่ใช่ 1 — ผ่าน
      (`lib/__tests__/stopOrdering.test.ts`, 7 เทสต์)

**8.3 จุดที่ติ๊กแล้วโผล่ 2 ที่ใน `/today`** ✅ แก้แล้ว
- [x] `upcoming`/`upcomingSched` กรอง `!stop.visited_at` เพิ่มอีกชั้น ไม่พึ่ง `slice(nextIndex+1)` อย่างเดียว

⚠️ **เฟสนี้แตะลำดับข้อมูลจริงที่ใช้อยู่** — ไม่ได้ทดสอบด้วยการลาก/แทรกจริงในเบราว์เซอร์บนแผนหลัก (กติกาข้อ 5
ห้ามกดปุ่มแก้ข้อมูลจริงตอนทดสอบ) ยืนยันด้วย `tsc`/`lint` สะอาด + เทสต์ unit ครอบ edge case ที่ระบุไว้ครบ
**แนะนำ:** ทดสอบลาก/แทรกจริงกับ "แผน B" ที่ก๊อปมา ก่อนใช้แผนหลักจริงจัง

---

### เฟส 9 — แคชถูกต้อง + โหลดเร็ว ✅ เสร็จ (7 ส.ค. 2026)
วัดจริงตอนโหลดหน้าแรก (ก่อนแก้): **165 requests · 50 URL ซ้ำกันเป๊ะๆ** (`/api/place-photos` ×68, `/api/place-details` ×49)

**9.1 เวลาเดินทางจาก/ถึงโรงแรมค้างค่าเก่าถาวรเมื่อเปลี่ยนโรงแรม** ✅ แก้แล้ว
- [x] เพิ่ม `hotelAnchorId()` ใน `lib/hotelLegs.ts` — สร้าง id จากพิกัดที่พัก (`hotel@{lat.toFixed(5)},{lng...}`)
      แทน `leg_id` ตรงๆ ใช้แทนที่ทั้งใน `useDaySchedule` (anchor เริ่ม/จบวัน) และ `useHotelDistance`
      (label "🏨 X นาที จากที่พัก" บนการ์ดคลัง) → เปลี่ยนโรงแรมได้ key ใหม่เองทันที **ไม่ต้องเขียน migration ใหม่เลย**

**9.2 ยิง API ซ้ำเพราะไม่มี in-flight dedupe** ✅ แก้แล้ว
- [x] `usePlacePhotos`/`usePlaceDetails` เพิ่ม `inFlight` Map แบบเดียวกับ `useDayTravelTimes`
- [x] รวมแคช `place-details` เป็นก้อนเดียว — `usePlaceDetails.ts` export `placeDetailsCache`/`fetchPlaceDetails()`
      ให้ `useDayOpeningHours` เรียกใช้ร่วมแทนการมี `Map` แยกของตัวเอง (แก้บั๊ก 7.6 ไปพร้อมกันในตัว)

**9.3 คลังสถานที่ถูก mount 2 ชุดเสมอ** ✅ แก้แล้ว
- [x] เพิ่ม `hooks/useMediaQuery.ts` (ใช้ `useSyncExternalStore` — ปลอดภัยสำหรับ SSR ไม่ชน eslint
      `react-hooks/set-state-in-effect` ที่เจอตอนลองใช้ `useEffect` ตั้ง state ตรงๆ ก่อน) `PlaceSidebar`
      เรนเดอร์ `<aside>` (คอม) หรือปุ่มลอย+ชีต (มือถือ) แค่ชุดเดียวจริงๆ ตามขนาดจอ แทนเรนเดอร์ทั้งคู่แล้วซ่อนด้วย CSS

**ผลวัดหลังแก้ (เบราว์เซอร์จริง โหลดหน้าแรกซ้ำ):** **113 requests (ลด 32%) · URL ซ้ำเหลือ 0** (จาก 50)
`/api/place-photos` 68→34 · `/api/place-details` 49→34 · ยืนยันแล้วว่าคลังสถานที่เรนเดอร์แค่ 1 ชุดจริง
(`document.querySelectorAll('aside').length` = 0 บนมือถือ / 1 บนจอใหญ่ ไม่มีทั้งคู่พร้อมกัน)

**ยืนยันปิดเฟส:** `npm test` (50/50) · `lint` + `tsc --noEmit` สะอาด · วัด performance ซ้ำแล้วเห็นตัวเลขดีขึ้นจริง

---

### เฟส 10 — กันข้อมูลหาย + พกไปเที่ยวได้ ✅ เสร็จ (8 ส.ค. 2026)
RLS เปิดสาธารณะทุกตาราง + anon key อยู่ใน bundle = ใครเจอ URL Vercel ก็ลบข้อมูลทริปได้หมด
**เรื่องนี้ตั้งใจไว้แต่แรกและไม่แก้** (กติกาข้อ "ไม่ทำระบบล็อกอิน") — แต่ตอนนี้ **ไม่มี backup เลยสักชั้น** ซึ่งคนละเรื่องกัน

- [x] **ปุ่ม Export แผนเป็น JSON** — ปุ่ม "⬇️ Export JSON" บนหัว `/summary` ดาวน์โหลด plans/stops/hotels/bookings/
      checklist/day settings/overnight overrides/custom places รวมไฟล์เดียว (client ล้วน ไม่มี API ใหม่)
- [x] **`/summary` พิมพ์ได้จริง** (`@media print`) — ซ่อนลิงก์นำทาง/BottomNav ด้วย `print:hidden`, บังคับสีพื้นหลัง
      หัวการ์ดวันด้วย `print-color-adjust: exact`, `print:break-inside-avoid` ต่อวัน, ตั้ง `@page { margin: 12mm }`
      เพิ่มปุ่ม "🖨️ พิมพ์" เรียก `window.print()` ให้ด้วย
- [x] **`/api/place-autocomplete` เพิ่ม rate limit ต่อ IP** — `lib/rateLimit.ts` in-memory sliding window (60 req/นาที/IP)
      คืน 429 เมื่อเกิน พอสำหรับ 2 คนใช้งานจริง ไม่ต้องพึ่ง Redis/Upstash
- [x] **ไฟล์แนบ booking ไม่รั่วอีกแล้ว** — `BookingEditModal.tsx`: กด ✕ ลบไฟล์จริงใน bucket (`storagePathFromPublicUrl`
      แกะ path จาก public URL), ปิด modal โดยไม่กดบันทึกก็ลบไฟล์ที่เพิ่งอัปโหลดทิ้งอัตโนมัติ (`pendingUploadPathRef`),
      จำกัดขนาดไฟล์ 10MB
- [x] **ช่อง "ลิงก์" ของ booking กรอง protocol แล้ว** — `lib/url.ts` (`safeHttpUrl`) อนุญาตเฉพาะ `http:`/`https:`
      บังคับใช้ทั้งตอนบันทึก (โชว์ error ในฟอร์ม) และตอนเรนเดอร์เป็น `href` ใน `/today`

**ยืนยันปิดเฟส:** `npm test` (50/50) · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์ (`/summary`
โหลดได้ กด Export JSON ดาวน์โหลดไฟล์สำเร็จ ไม่มี console/server error)

---

### เฟส 11 — ฟีเจอร์ระหว่างเที่ยว ✅ เสร็จ (8 ส.ค. 2026)
ตอนวางแผนคิดว่าไม่ต้อง migration เลย — จริงๆ ต้องเพิ่ม 2 ตัว (checklist หมวด + รูปต่อจุดแวะ) เพราะข้อมูลใหม่ไม่มีที่เก็บ

- [x] **"จุดถัดไปจะปิดในอีก X นาที"** ใน `/today` — `lib/openingHours.ts` เพิ่ม `minutesUntilClose()` (แยก
      `buildOpenWindows()` ใช้ร่วมกับ `isOpenDuring` เดิม) เทียบเวลานาฬิกาจริงกับช่วงเปิดปัจจุบัน โชว์ป้าย
      "⏳ ปิดในอีก X นาที" เมื่อเหลือ ≤60 นาทีและยังไม่ปิด (ต่างจากป้าย "อาจปิดแล้ว" เดิมที่เตือนเฉพาะสายไปแล้ว)
      เฉพาะวันนี้จริงเท่านั้น (`isRealToday`) — เทสต์ 5 เคสใหม่ใน `openingHours.test.ts`
- [x] **ปุ่ม "นำทางกลับที่พัก"** ใน `/today` — โชว์ `NavButtons` (Google/Kakao/Naver เดิม) ชี้ไป `endAnchor`
      ในการ์ด "เที่ยวครบทุกจุดของวันนี้แล้ว" — ไม่ได้ทดสอบ state นี้จริงในเบราว์เซอร์เพราะต้องติ๊ก "มาถึงแล้ว"
      ครบทุกจุดในแผนหลักจริง (ผิดกติกาข้อ 5) พึ่ง code review + `NavButtons` ที่ผ่านการทดสอบอยู่แล้วจากการ์ดจุดถัดไป
- [x] **Checklist แยกหมวด** (🎒 ของใส่กระเป๋า / 🏨 ก่อนออกจากโรงแรม / ✈️ ก่อนขึ้นเครื่อง) — migration `0022`
      เพิ่มคอลัมน์ `checklist_items.category` (default `'packing'` ของเดิมทั้งหมดเข้ากลุ่มนี้อัตโนมัติ) UI มีแท็บเลือก
      หมวดตอนเพิ่ม + จัดกลุ่มแสดงผลเป็นหมวดๆ ใน `ChecklistPanel.tsx` **ไม่ได้ทำ "ผูกกับวัน"** ตามที่ระบุไว้ตอนแรก
      เพราะไอเดียเดิมไม่ชัดว่าจะผูกยังไงให้มีประโยชน์กว่าหมวดคงที่ 3 หมวดนี้ ถ้าต้องการเพิ่มทีหลังค่อยคุยกันใหม่
- [x] **โน้ต + รูปหน้างานต่อจุดแวะ** — migration `0023` เพิ่มคอลัมน์ `trip_stops.photo_url` ใช้ bucket
      `booking-files` เดิม (คนละ path prefix `stop-photo-*` กันชนกับไฟล์แนบ booking) อัปโหลดจาก `DayStopsSection`
      ทันทีที่เลือกไฟล์ (ไม่มีขั้นตอนร่าง/ยกเลิกเหมือน booking เพราะโน้ตเดิมก็เซฟทันทีอยู่แล้ว) ลบรูปเก่าออกจาก bucket
      ก่อนแทนที่ด้วยรูปใหม่เสมอ กันไฟล์ค้าง จำกัด 10MB เหมือนไฟล์แนบ booking · โชว์รูปที่จุดถัดไปใน `/today` ด้วย
- [x] **เวลาเปิด-ปิดวันหยุดพิเศษ** — **แก้ชื่อ field ที่ระบุผิดตอนวางแผน:** `currentSecondaryOpeningHours` เป็น
      เวลาช่องทางรอง (drive-through/delivery) ไม่ใช่วันหยุด ตัวที่ถูกต้องคือ **`currentOpeningHours`** (ยืนยันจาก
      Google docs) ซึ่งคืนเวลาจริง 7 วันข้างหน้ารวมวันหยุดพิเศษ ต่างจาก `regularOpeningHours` ที่เป็นตารางประจำ
      **จงใจไม่แคชถาวรใน `place_details_cache`** เพราะข้อมูลมีความหมายแค่ 7 วันข้างหน้านับจากตอนเรียก แคชไว้นาน
      จะกลายเป็นข้อมูลผิดเงียบๆ — เพิ่ม `?live=1` ใน `/api/place-details` ยิงสดทุกครั้ง (ข้าม cache ทั้ง DB และ
      Next.js fetch 30 วัน) เรียกเฉพาะจุดถัดไปใน `/today` ตอน `isRealToday` เท่านั้น โชว์ป้าย "🎌 วันนี้เวลาเปิด-ปิด
      อาจต่างจากปกติ" เมื่อต่างจาก `regularOpeningHours` ยืนยันด้วย curl จริงว่า field คืนค่าถูกต้อง

**ผู้ใช้ต้องทำ:** รัน migration `0022_checklist_categories.sql` และ `0023_trip_stop_photo.sql` ใน Supabase
Dashboard → SQL Editor (สองไฟล์นี้เท่านั้น ปลอดภัย แค่เพิ่มคอลัมน์ใหม่ ไม่กระทบข้อมูลเดิม)

**ยืนยันปิดเฟส:** `npm test` (55/55 ผ่าน) · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์
(หน้าแรก/`/today` โหลดได้ไม่มี console error, checklist หมวดใหม่แสดงถูกต้อง, ปุ่ม `+ รูป` เรนเดอร์ครบ 15 จุด,
`/api/place-details?live=1` ยืนยันด้วย curl คืน `currentOpeningHours` จริงจาก Google)

---

### เฟส 12 — ความสะอาดโค้ด (ไม่มีอะไรเปลี่ยนในสายตาผู้ใช้ ทำหลังสุดได้) ✅ เสร็จ (11 ส.ค. 2026)

- [x] **แตก `components/DayStopsSection.tsx`** (1,049 บรรทัด ตอนลงมือจริง — โตขึ้นจาก 973 หลังเฟส 11)
      แยกออกเป็น 3 ไฟล์ ย้ายโค้ดตรงๆ ไม่แตะพฤติกรรมเลย: `SortableStopRow.tsx` (422 บรรทัด),
      `TravelModeRow.tsx` (90) — ต้องแยกด้วยเพราะทั้ง `SortableStopRow` และ `DayStopsSection` ใช้ร่วมกัน,
      `DayEventsPanel.tsx` (45) · เหลือ `DayStopsSection.tsx` **511 บรรทัด** (ลด 51%)
- [x] **ยก hook ที่เป็น global state ขึ้นเป็น Context** — `useHotels`/`useBookings`/`useCustomPlaces` เปลี่ยนเป็น
      `.tsx` ไฟล์ละ 3 ส่วน: `useXStore()` (ตัวจริงที่ fetch + เปิด realtime channel, private), `XProvider`,
      และ `useX()` ที่อ่านจาก context (throw ถ้าถูกเรียกนอก provider) · รวมไว้ที่ `components/TripDataProvider.tsx`
      mount ครั้งเดียวใน `app/layout.tsx` — **ผู้เรียกทุกจุด import เหมือนเดิมทุกบรรทัด** ไม่ต้องแก้
      ผลที่ได้: `custom_places` เลิกโหลดซ้ำ/เปิด channel ที่สองตอนเปิด `NearbyPlacesModal` และทั้ง 3 ตาราง
      ไม่ต้องโหลดใหม่ตอนสลับหน้าแรก ↔ `/today` ↔ `/summary` (client-side nav ใช้ state เดิมต่อ)
- [x] `DayStopsSection` คำนวณ `isOpenDuring` ซ้ำในแถว — **เก็บไปแล้วตั้งแต่เฟส 7.4** ตอนเปลี่ยน `isOpenDuring`
      ให้รับนาทีดิบ (ยืนยันซ้ำรอบนี้: ไฟล์ไม่ import `isOpenDuring` เลย ใช้ `closedStopIds` จาก hook อย่างเดียว)

**ยืนยันปิดเฟส:** `npm test` (55/55 ผ่าน) · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์
ครบทั้ง 3 หน้า ไม่มี console error — หน้าแผนขึ้นครบ 19 จุด (12 จุดวันที่ปลดล็อกมีที่จับลาก/ปุ่มปรับเวลา/+โน้ต/+รูป
ครบ · 7 จุดวันที่ล็อกขึ้นไอคอน 🔒 ถูกต้อง), เปิด `NearbyPlacesModal` แล้วโหลดร้านใกล้ๆ ได้ปกติ (จุดที่เคยเรียก
`useCustomPlaces` ซ้ำ), กด "เปลี่ยน" ในแถวเดินทางแล้ว `TravelModeRow` ที่แยกไฟล์ออกมาสลับเป็นโหมดเลือกได้
ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

## 3.6 รอบตรวจสถาปัตยกรรม + UX (11 ส.ค. 2026) — เฟส 13-19

ตรวจโค้ดทั้งโปรเจกต์ (11,328 บรรทัด / 60 ไฟล์) + **วัดของจริงจากเบราว์เซอร์** ไม่ใช่เดาจากการอ่านโค้ด
จุดตั้งต้นคือฟีดแบ็ก 4 ข้อจากผู้ใช้หลังลองใช้จริง แล้วขยายเป็นการตรวจทั้งระบบ

**คำตัดสินเรื่อง "เขียนใหม่":** ผู้ใช้ให้สิทธิ์รื้อทั้งโปรเจกต์ได้ — **ตรวจแล้วไม่ทำ** เพราะเทสต์ผ่าน 55/55,
build สะอาด, 0 console error, การแบ่งชั้น (data → lib → hooks → components) ถูกต้อง และเหลือ **61 วันก่อนบิน**
บนฐานข้อมูลที่ใช้งานจริงอยู่ 2 คน · ปัญหาทั้ง 4 ข้อไม่ได้เกิดจากสถาปัตยกรรมผิด แต่เกิดจาก **3 ช่องว่างที่ยังไม่มีใครสร้าง**

**สถานะล่าสุด (12 ส.ค. 2026): เฟส 13-19 ปิดหมดแล้ว** เหลือค้างข้อเดียวคือ "ยกส่วน static ขึ้น
Server Component" (เฟส 19 ข้อสุดท้าย) ที่ตั้งใจเลื่อนไปหลังทริป · เหลืออีก **60 วันก่อนบิน**
→ **ทั้งไฟล์นี้ไม่เหลืองานที่ต้องทำก่อนบินแล้ว** งานหลังจากนี้เป็นของใหม่ที่ยังไม่ได้วางแผน

### ตัวเลขที่วัดได้จริง (เปิดหน้าแผน 1 ครั้ง, dev server)

| วัดอะไร | ค่า | หมายเหตุ |
|---|---|---|
| เรียก `/api/*` | **129 ครั้ง** | place-details 34 · place-photos 34 · place-photo 33 · travel-time 6 |
| แผนที่ Google ที่ mount | **10 ตัว** (จอคอม) | มือถือ mount 4 ตัว **มองเห็น 0 ตัว** |
| request ไป maps.googleapis.com | **43 ครั้ง** (มือถือ) | ทั้งที่ผู้ใช้ยังไม่ได้กดแท็บ 🗺️ เลย |
| ข้อมูล decode | **13.57 MB** | `<img>` ดิบ 249 ใบ ไม่ผ่าน next/image |
| Supabase realtime channel | **10 เส้น** | เฟส 12 ยกขึ้น Context ไป 3, เหลืออีก 7 |
| API route ที่ไม่มี rate limit | **7 จาก 8** | `lib/rateLimit.ts` มีอยู่แล้วแต่ใช้เส้นเดียว |
| สตริงภาษาเกาหลีในโปรเจกต์ | **0 ตัว** | ต้นเหตุตรงๆ ของฟีดแบ็กข้อ 1 |

### 3 ช่องว่างที่เป็นต้นเหตุของฟีดแบ็กทั้ง 4 ข้อ

1. **ไม่มีชื่อภาษาท้องถิ่นในระบบเลย** — `mapsQuery` ทั้ง 39 ที่เป็นอังกฤษถอดเสียง และ `app/today/page.tsx`
   ส่ง **ชื่อภาษาไทย** เข้า Naver/Kakao ตรงๆ (`NavButtons name={place.nameTh}`) ซึ่งค้นไม่เจอแน่นอน → เฟส 14
2. **ไม่มีชั้นภาษา (i18n)** — ไทยฝังตายใน ~25 ไฟล์ ผูกภาษาไว้ในชื่อฟิลด์เอง (`nameTh`/`cityTh`/`weekdayTh`)
   ส่วน `nameEn` มีครบทั้ง 39 ที่แล้วแต่ถูกใช้แค่ 2 จุดในทั้งเว็บ → เฟส 16
3. **`DayEvent` เป็นลิสต์ข้อความ ไม่ใช่โมเดลเที่ยวบิน** — ระบบไม่รู้ว่าอันไหนคือตั๋ว อันไหนแก้ได้
   ไม่รู้ว่ากระเป๋าเช็คทะลุหรือเปล่า ต้องผ่าน ตม. ไหม จึงคำนวณต่อไม่ได้และแก้ในเว็บไม่ได้ → เฟส 15

### คำตอบของผู้ใช้ต่อ 5 คำถามที่ค้างไว้ (11 ส.ค. 2026)

| เรื่อง | ตัดสินใจแล้ว |
|---|---|
| ขอบเขตภาษาอังกฤษ | **เฉพาะ `/summary`** ไม่แปลทั้งเว็บ |
| ชื่อท้องถิ่น | ผู้ใช้ให้ผมเลือก → **`nameLocal` + locale ผูกกับเมือง** (`hanoi → vi`, ที่เหลือ `→ ko`) ครอบทั้งทริปด้วยโค้ดชุดเดียว |
| offline / PWA | **ทำ** — กลับคำจากข้อ "ไม่ทำ" เดิม (ดูหัวข้อ 1) ขอบเขต = อ่านอย่างเดียว → เฟส 18 |
| กระเป๋า VN610→VN428 | **through-check ถึงกิมแฮจริง** — ยืนยันแล้ว แก้เวลาเผื่อ 45 → 30 นาทีได้ |
| ลำดับเฟส | เอาตามที่เสนอ (13 → 19) |

---

### เฟส 13 — ปิดรูรั่วก่อน (~30 นาที) ✅ เสร็จ (11 ส.ค. 2026)

งานเล็กสุด เสี่ยงศูนย์ ไม่มี migration แต่ปิดรูค่าใช้จ่ายที่เปิดอยู่จริงตอนนี้

- [x] เติม `isRateLimited()` ให้ครบ **7 route ที่ขาด** — ทุก endpoint ต่อตรงถึง Google และคิดเงินต่อ request
      แต่เปิดสาธารณะไม่มี auth · ใครได้ URL ไปก็ยิงจนบิลบานได้ (`geocode`, `place-details`, `place-nearby`,
      `place-photo`, `place-photos`, `place-search`, `travel-time`, `youtube-video`)
- [x] **แผนที่บนมือถือ: unmount จริงแทนซ่อนด้วย CSS** — `DayStopsSection` เดิมใช้ `className="hidden"`
      คอมโพเนนต์เลยยัง mount และโหลด tile ครบทุกตัวทั้งที่มองไม่เห็น (แก้ P-1 ครึ่งแรก)
      ใช้ `useMediaQuery("(min-width: 1024px)")` ที่มีอยู่แล้ว (SSR-safe ผ่าน `useSyncExternalStore`
      ตัวเดียวกับที่ `PlaceSidebar` ใช้) · **จงใจไม่ unmount ฝั่งลิสต์** เพราะลิสต์เป็น droppable target
      ของ dnd-kit ถ้า unmount ตอนสลับไปแท็บแผนที่จะลากข้ามวันไม่ได้

**ผลที่วัดได้จริง (มือถือ 375px, ก่อน → หลัง):**

| วัดอะไร | ก่อน | หลัง |
|---|---|---|
| แผนที่ที่ mount | 4 ตัว (เห็น 0) | **0 ตัว** |
| request ไป maps.googleapis.com | 43 | **6** (เหลือแค่ตัวโหลด API) |
| เรียก `/api/*` | 129 | **67** |
| ข้อมูล decode | 13.57 MB | **9.60 MB** |

(`/api/*` ลดตามไปด้วยเพราะ `DayMapPanel` มี `PlaceThumb` ในป๊อปอัพหมุด ซึ่งยิง `place-photos`/`place-photo`
ของมันเอง — รูปพวกนั้นอยู่ในป๊อปอัพที่ผู้ใช้เปิดไม่ได้อยู่แล้วตอนแท็บแผนที่ยังไม่ถูกกด)

**ยืนยันปิดเฟส (11 ส.ค. 2026):** `npm run lint` + `tsc --noEmit` สะอาด · `npm test` 55/55 ผ่าน ·
ทดสอบจริงในเบราว์เซอร์: กดแท็บ "🗺️ แผนที่" บนมือถือแล้ว mount แผนที่ 1 ตัวขึ้นมาถูกต้อง (หมุดเลข 1-2-3 ครบ)
· จอ 1400px ยังขึ้นแผนที่คู่ลิสต์เหมือนเดิมทุกวัน (4 ตัว mount / 4 ตัวมองเห็น) · console เหลือแค่ warning
`google.maps.Marker` deprecated ที่มีมาก่อนแล้ว (ตั้งใจใช้ Marker แบบคลาสสิก ดูเหตุผลที่เฟส 3)
· ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

### เฟส 13.5 — ล็อกเว็บด้วย PIN ✅ เสร็จ (11 ส.ค. 2026)

**ที่มา:** ตอนยืนยัน migration 0024 ด้วย curl (ตามกติกาข้อ 4) ก็เท่ากับสาธิตปัญหาไปในตัว — อ่านข้อมูลทริปจริง
ออกมาได้จาก terminal ด้วย anon key ที่ติดอยู่ในบันเดิลฝั่ง browser โดยไม่ต้องเข้าเว็บเลย
ผู้ใช้เลือก **"ใส่ PIN ก็พอ"** (ไม่เอา Supabase Auth เต็มรูปแบบตอนนี้) — เป็นการกลับคำข้อ "ไม่ทำระบบล็อกอิน" บางส่วน

- [x] `proxy.ts` ที่ root — **ไม่ใช่ `middleware.ts`** เพราะ Next 16 deprecate ไปแล้วและเปลี่ยนชื่อเป็น `proxy`
      (ยืนยันจาก `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` ตามกติกาข้อ 2)
      · รันบน Node.js runtime เป็นค่าเริ่มต้น จึงใช้ `node:crypto` ได้ตรงๆ
- [x] `lib/pinAuth.ts` — cookie เก็บ **HMAC(secret, pin)** ไม่ใช่ธง `authed=1` (ใครก็ตั้งเองได้)
      และไม่ใช่ `sha256(pin)` เฉยๆ (PIN 4 หลัก = 10,000 ค่า ทำตารางแฮชได้ในไม่กี่วินาที) · เทียบแบบ timing-safe
- [x] `app/unlock/page.tsx` + `app/api/unlock/route.ts` — **rate limit 10 ครั้ง/นาที** ที่ endpoint นี้
      คือสิ่งเดียวที่ทำให้ PIN สั้นๆ พอใช้ได้จริง (ไล่เดาครบ 10,000 ค่าต้องใช้ ~16 ชม.แทนที่จะไม่ถึงนาที) **ห้ามถอด**
- [x] cookie `httpOnly` + `sameSite=lax` + `secure` เฉพาะ production + อายุ 90 วัน (กรอกครั้งเดียวใช้ยาวจนจบทริป)
- [x] จำหน้าที่ตั้งใจเข้าไว้ใน `?next=` แล้วพากลับหลังปลดล็อก (เปิดลิงก์ `/today` ตรงๆ ได้)

**บั๊กที่เจอระหว่างทำ (บันทึกไว้กันพลาดซ้ำ):** matcher เขียนเป็น `(?!_next/static|_next/image|favicon.ico)`
ตามตัวอย่างในเอกสาร → **`/_next/hmr` โดนด่านดักไปด้วย** → HMR ตอน dev พัง → React ไม่ hydrate →
ฟอร์มกรอก PIN ตกไปเป็น native submit กดแล้วรีโหลดเฉยๆ · แก้เป็นตัด **`_next` ทั้งก้อน** ซึ่งปลอดภัยเพราะ
ข้อมูลของหน้าที่หวงเดินทางผ่าน URL ของหน้านั้นเอง (RSC ยิงไป path เดิมพ่วง `?_rsc=` ซึ่งยังเข้าด่านอยู่)

**⚠️ ขอบเขตที่กันได้จริง — อย่าเข้าใจผิด:** ด่านนี้กัน **หน้าเว็บ + API route ของเรา** (= กันบิล Google)
**ไม่ได้กัน Supabase** เพราะ browser คุยกับ Supabase ตรงด้วย anon key ที่ซ่อนไม่ได้ตามการออกแบบ
13 ตาราง 49 policy ยังเป็น `using (true)` ทั้งหมด และ bucket `booking-files` ยังลบได้จาก anon key
→ ถ้าจะปิดจริงต้องทำ **Supabase Auth + แก้ RLS** (ยังไม่ได้ทำ ผู้ใช้เลือกไว้ทีหลัง)

**ยืนยันปิดเฟส (11 ส.ค. 2026):** `lint` + `tsc --noEmit` + `test` 55/55 + `build` สะอาด · ทดสอบด้วย curl 7 เคส:
เข้าหน้าเว็บตอนล็อก → 307 ไป `/unlock?next=%2Ftoday` · ยิง API ตอนล็อก → 401 · PIN ผิด → 401 · PIN ถูก →
200 + `Set-Cookie` httpOnly 90 วัน · cookie ปลอมทั้งแบบสั้นและแบบยาวเท่าของจริง → ถูกปฏิเสธทั้งคู่ ·
ยิง PIN ผิดรัวๆ → 429 หลังครั้งที่ 7 · และทดสอบ UI จริงในเบราว์เซอร์: PIN ผิดขึ้นแถบ "PIN ไม่ถูกต้อง"
ไม่รีโหลดหน้า · PIN ถูกพาไป `/today` ตาม `next` เนื้อหาขึ้นครบรวมรูปจาก `/api/place-photo`
· ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

### เฟส 14 — ชื่อท้องถิ่น + นำทางที่ใช้ได้จริง (ฟีดแบ็กข้อ 1)

**กลไกหลัก:** Places API (New) ที่เปิดใช้อยู่แล้วคืนชื่อในภาษาที่ขอได้ ตอนนี้โค้ดทุกจุดขอ `languageCode: "th"`
เท่านั้น (`lib/googlePlaces.ts:78, 125, 196`) — ขอ `"ko"` เพิ่มก็ได้ `감천문화마을` มาเลย **ไม่ต้องแปลเอง
ไม่ต้องเปิด API เจ้าใหม่** (สอดคล้องกับกติกาข้อ 3 และการตัดสินใจ 6 ส.ค. ที่ไม่เพิ่ม ODsay/Kakao/Naver API)

- [x] migration `0024` — เพิ่ม `name_local`, `address_local`, `locale` ใน `place_details_cache` (รันแล้ว 11 ส.ค.)
- [x] **ฝัง `nameLocal` + `addressLocal` ลง `data/places.ts` ครบ 46 ที่** (39 ตามแผน + ที่เพิ่มมาทีหลัง)
      ดึงจาก Places API ด้วยสคริปต์ครั้งเดียว ไม่ได้พิมพ์เอง — ขึ้นทันทีแม้เน็ตหลุด ซึ่งสำคัญมากตอนอยู่บนแท็กซี่
      · เพิ่ม `CITY_LOCALE` (ฮานอย = `vi`, ที่เหลือ = `ko`) + `isKoreanCity()`
- [x] รื้อ `lib/mapLinks.ts` — เพิ่ม `navigationName()` ลำดับ fallback `local → en → th`
      **ยืนยันแล้วว่าลิงก์ Kakao ส่ง `감천문화마을` จริง** (เดิมส่งชื่อไทยซึ่งค้นไม่เจอ)
- [x] **การ์ด "ยื่นให้คนขับดู"** — `components/LocalNameCard.tsx` ชื่อตัว 2xl + ที่อยู่ กดคัดลอกได้
- [x] **สถานที่ที่ผู้ใช้เพิ่มเอง** — `/api/place-details?locale=ko|vi` resolve + แคชลง migration 0024
      ทดสอบแล้วเขียนจริงครบ 3 ร้านฮานอยใน `place_details_cache` · ขอมาพร้อมเวลาเปิด-ปิดในคำขอเดียว
      (ระวังบั๊ก 9.2 ที่เพิ่งแก้ไป — ห้ามยิงซ้ำสองรอบต่อสถานที่)
- [ ] **ยืนยัน `nmap://` บนมือถือจริงที่ลงแอป Naver ไว้** — ห้ามยืนยันจากความจำ เพราะ `nmap://`
      ไม่คืน response ให้เช็ก ต้องเห็นแอปเปิดขึ้นมาจริงเท่านั้นถึงนับว่าผ่าน
      (เบราว์เซอร์ของ agent เปิด `map.naver.com` ไม่ได้ ถูกบล็อกโดย policy — ผมทดสอบแทนไม่ได้จริงๆ)
- [x] **ที่พักยังไม่มีชื่อภาษาท้องถิ่น** — ✅ ปิดแล้วในเฟส 16 (12 ส.ค. 2026) ด้วย migration `0026`
      + `/api/geocode?locale=` + `hotelNavigationName()` ดูรายละเอียดที่หัวข้อเฟส 16

**เจอระหว่างทำ — คุณภาพข้อมูล `mapsQuery` (ไม่ใช่บั๊กของเฟสนี้ แต่ควรรู้):**
`mapsQuery` บางอันชี้ไปคนละที่กับที่ตั้งใจ ซึ่งกระทบ**เวลาเปิด-ปิดและรูปด้วย** ไม่ใช่แค่ชื่อ —
`seoul-seongsudong` ("Seongsu-dong Seoul cafe") ได้ร้าน 어니언 성수점 แทนย่าน ·
`Quán Café Maison De Hanoi` ได้ "Maison de Éte" ที่ Kim Liên คนละย่านกับที่วางไว้ ·
`hanoi-train-street` ได้ร้านอาหารแทนถนนรถไฟ · `gangneung-ojukheon` Google คืน `오죽헌_<상>` มี artifact ติดมา
→ 4 ตัวนี้ผมยิงใหม่ด้วยคำค้นภาษาท้องถิ่นแล้วใช้ผลนั้นแทนเฉพาะ `nameLocal`/`addressLocal`
**จงใจไม่แตะ `mapsQuery`** เพราะเป็น cache key ของทั้ง `place_details_cache`/`place_photo_cache` แก้แล้วแคชหลุดยกชุด

---

### เฟส 15 — วันบินแก้ได้ + ทรานซิตถูกต้อง (ฟีดแบ็กข้อ 3 + 4) ✅ เสร็จ (12 ส.ค. 2026)

หลักการใหม่: **ล็อกแค่สิ่งที่ซื้อตั๋วมาแล้ว** ที่เหลือของวันบินแก้ได้เหมือนวันเที่ยวปกติ

- [x] รื้อ `DayEvent` เป็นโมเดลจริง — `kind: "flight" | "layover" | "transfer" | "checkin" | "deadline"`
      + `editable?: boolean` (ไม่ใส่ = ล็อก เพื่อให้เหตุการณ์เก่าที่ยังไม่ระบุ kind พฤติกรรมไม่เปลี่ยน)
      · เพิ่ม type `FlightInfo` (`no`/`fromCode`/`toCode`/`fromEn`/`toEn`) ด้วย — **เฟส 16 ต้องใช้**
      เลขเที่ยวบิน + ต้นทาง/ปลายทางอังกฤษบนหน้า ตม. ดึงจากที่เดียวกับตารางบิน ไม่พิมพ์ซ้ำ
      · `DayEventsPanel` โชว์ 🔒/✏️ รายแถว + ป้าย `BKK → HAN` ของเที่ยวบิน แทนป้ายรวม "ล็อกทั้งแผง" เดิม
- [x] เพิ่ม type `Layover` — `baggage` · `immigration` · `leavesAirport` · `terminalChange`
      render เป็น `components/LayoverBadges.tsx` (ป้ายละ 1 คำถามที่คนถามจริงตอนต่อเครื่อง) สีส้ม = ต้องลงมือทำ
      / เขียว = ไม่ต้องทำอะไร · เพิ่ม event ช่วงพักเครื่องฮานอยเข้า `d0` ที่เดิมไม่มีเป็นแถวของตัวเองเลย
- [x] **แก้ข้อมูล `d0` ให้ตรงตั๋วจริง** — VN610→VN428 เช็คกระเป๋าทะลุถึงกิมแฮ (ผู้ใช้ยืนยันแล้ว)
      "ผ่าน ตม. + รับกระเป๋า ~45 น." → **"ผ่าน ตม. ~30 น. (ไม่ต้องรับกระเป๋า)"** และเวลาแนะนำ
      ถึงย่านเมืองเก่า **15:30 → 15:15** = ได้เวลาเที่ยวฮานอยคืนมา 15 นาที (6 ชม. 45 น.)
- [x] `d10` โฮจิมินห์ — `immigration: "none"`, `leavesAirport: false`, `terminalChange: false`
- [x] เพิ่ม `kind: "transfer"` ใน `trip_stops` (migration `0025`) — **ต่างจาก `intercity` ตรงที่แถวนี้มี
      `place_id` จริง** ชี้ไปสนามบินใน `data/transferPoints.ts` (แยกจาก `PLACES` เพราะสนามบินไม่ใช่ที่เที่ยว
      ไม่ควรโผล่ในคลัง — `resolvePlace` หาให้เจอผ่าน fallback ชั้นที่สอง) เวลาเดินทางจึงเป็นของจริงจาก
      Routes API เหมือนจุดแวะปกติ · `dwell_minutes` ใช้เก็บ "เวลาเผื่อที่สนามบิน" ใช้ปุ่ม +/− เดิมได้ฟรี
      · เพิ่มหมวด `transport` ใน `Category` (ไม่ได้ใส่ใน `CATEGORY_ORDER` ของ `PlaceSidebar` = ไม่โผล่ในคลัง)
      · **กัน `RouteSuggestionModal` ย้ายแถวนี้** ด้วย `isReorderable()` — เดิมกันแค่แถวที่ resolve place ไม่ได้
      ซึ่งไม่ครอบแถว transfer ที่มี place จริง (ถ้าไม่กัน "ลองจัดเส้นทางใหม่" จะดันสนามบินไปกลางวัน)
- [x] **ตัวคำนวณ "ควรออกกี่โมง"** — `lib/departureAdvice.ts` + `components/TransferAdvicePanel.tsx`
      `เวลาที่ควรออก = เวลาบิน − เผื่อเช็คอิน − เวลาเดินทางจริง − เผื่อความเสี่ยง 30 น.` (เช็คอินเริ่มต้น 3 ชม.
      ปรับได้ตอนแทรก) · เป็น**คำแนะนำ ไม่เขียนทับเวลาในแผน** · เตือน ⚠️ พร้อมบอกว่าช้าไปกี่นาทีเมื่อไปถึงช้ากว่าเส้นตาย
      · ใช้ **นาทีสะสมไม่ wrap** เทียบข้ามเที่ยงคืนได้ถูก (เคส d0 บินตี 1:15) และมีกฎ 12 ชม. กันเคสกลับด้าน
      "ถึง 11:00 แต่บิน 10:35" ไม่ให้ถูกตีเป็นเที่ยวบินพรุ่งนี้แล้วบอกว่า "มาเร็วไป 23 ชม." — **6 เทสต์ใหม่**
- [x] `d10` เทียบตัวเลือกจริง — `data/airportAccess.ts` (ICN: AREX ด่วน 43 น. / AREX ธรรมดา 59 น. /
      ลิมูซีนบัส ~80 น. · HAN: แท็กซี่ 40 น. / บัส 86 · PUS: BGL+สาย 2 / ลิมูซีน) กางดูได้ใต้แถว transfer
      **พร้อมเวลา "ออกไม่เกินกี่โมง" ของแต่ละตัวเลือก** · ติดป้ายชัดว่าตัวเลขพวกนี้เป็นตารางเดินรถของผู้ให้บริการ
      (คงที่) ส่วนเวลาในแถวเดินทางเป็นของ Google ตามพิกัดโรงแรมที่เลือกไว้จริง — กติกาเดียวกับป้าย "จริง/ประมาณการ"

**ผู้ใช้ต้องทำ:** รัน `0025_trip_stops_transfer.sql` (เพิ่ม 2 คอลัมน์ ปลอดภัย ไม่กระทบข้อมูลเดิม)
จนกว่าจะรัน ปุ่ม "✈️ + ไปสนามบิน" จะบันทึกไม่ผ่าน (Supabase ปฏิเสธคอลัมน์ที่ไม่มีจริง)

**ยืนยันปิดเฟส:** `npm test` **61/61 ผ่าน** · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์
ที่ 375px: การ์ด `d0` ขึ้นแถบพักเครื่องฮานอยพร้อมป้าย 4 ใบถูกต้อง (กระเป๋าทะลุ/ต้องผ่าน ตม./ออกไปเที่ยว/อาคารเดิม),
เวลาแนะนำเป็น 15:15, ป้าย 🔒 กับ ✏️ แยกถูกตามชนิดเหตุการณ์, เปิด modal "✈️ + ไปสนามบิน" ที่ `d10` แล้ว
**เลือก ICN ให้อัตโนมัติตามเมือง + ดึง VN409 10:35 มาเป็นเส้นตายให้เอง** + โชว์ตัวเลือก AREX/ลิมูซีนครบ
· console สะอาดหลังรีโหลด · **ปิด modal โดยไม่กดบันทึก — ไม่มีการแก้ข้อมูลจริงใน DB (กติกาข้อ 5)**

⚠️ **ยังไม่ได้ทดสอบ:** แถว transfer ที่ถูกบันทึกลงวันจริงแล้ว (ต้องเขียนลง DB ที่ใช้งานจริง + ต้องรัน migration
0025 ก่อน) — ตรรกะเวลาครอบด้วย unit test 6 เคสแทน ตามแนวเดียวกับเฟส 8/11 · **แนะนำให้ผู้ใช้ลองแทรกจริง
หลังรัน migration แล้วดูว่าแถบ "ควรออกไม่เกิน HH:MM" ขึ้นถูก**

**อัปเดต (12 ส.ค. 2026 — ผู้ใช้ทักมาว่าการ์ด `d10` แสดงผลต่างจากวันอื่น):**
- [x] **แก้บั๊ก `d10`** — events ของ `d10` ไม่มีแท็ก `anchor` เลย (ต่างจาก `d0` ที่มี) ทำให้เหตุการณ์ทั้ง 6 อัน
      ถูกเทลง panel เดียวแทนที่จะแยก "ก่อนจุดแวะ"/"✈️ ต่อจากนั้น" แบบวันอื่น และ `defaultStartTime` ตกไปใช้
      fallback `07:00` ทั้งที่ต้องออก `05:45` จริง — เพิ่ม `anchor: "before"` ให้ event เช็คเอาต์ `05:45`
      ตามแพทเทิร์นเดียวกับ `d0` (ไม่ใส่ `anchor: "after"` เพราะวันนี้ไม่มีช่วงว่างให้แทรกจุดแวะจริงๆ ต่างจาก
      `d0` ที่มีช่วงว่าง 6 ชม. 45 น. ในฮานอย) ยืนยันในเบราว์เซอร์แล้ว: panel แยกถูก + เวลาแนะนำขึ้น `05:45`
- [x] **แทรกแถว "✈️ ไปสนามบิน" ให้ `d10` จริงแล้ว** (เขียนสคริปต์ยิง Supabase REST ตรง เหมือนแพทเทิร์น backfill
      ที่พักในเฟส 16 — dry-run ก่อนเขียนจริง) ค่าที่ใช้คือค่าเดียวกับที่ `TransferEditModal` จะตั้งให้เองถ้ากด
      "เพิ่ม" ทันที: `airport-icn`, เผื่อเวลา 180 นาที, เส้นตาย VN409 10:35 — ยืนยันด้วย curl + เห็นในเบราว์เซอร์
      แล้วว่าขึ้นแถวจุดแวะ + แผนที่ + `TransferAdvicePanel` ("ควรออกไม่เกิน 07:05") ครบเหมือนวันอื่น
      · ⚠️ **ที่พักโซล (คืน 17-20 ต.ค.) ยังไม่มีใน `trip_hotels`** ทำให้เวลาเดินทางที่พัก→สนามบินคำนวณเป็น
      **0 นาที** เลข "ควรออกไม่เกิน" ที่ขึ้นตอนนี้จึง**มองโลกในแง่ดีเกินจริง** — ต้องตั้งที่พักโซลก่อน เลขถึงจะแม่น
      · **`trip_day_settings.start_time` ของ `d10` ยังเป็น `07:00` เดิมที่ผู้ใช้เคยตั้งไว้เอง** — ไม่ได้แก้ให้
      (เป็นการแก้ข้อมูลจริง ไม่ใช่โค้ด) กดปุ่ม "กลับไปใช้เวลาแนะนำ (05:45)" เองได้ถ้าต้องการ

---

### เฟส 16 — TH/ENG + หน้าสำหรับ ตม. (ฟีดแบ็กข้อ 2) ✅ เสร็จ (12 ส.ค. 2026)

ผู้ใช้เลือก "เฉพาะ `/summary`" — แปลทั้งเว็บจะแตะ ~25 ไฟล์แต่ได้ประโยชน์จริงแค่ตอนยืนหน้าเคาน์เตอร์
โฟกัสหน้าเดียว = ได้ผลเต็มด้วยงาน ~20% และหน้าแผน/หน้าวันนี้ยังเป็นไทยเหมือนเดิมตามที่ใช้กันอยู่

- [x] `lib/i18n.ts` — dictionary 33 คีย์ + `useLang()` (ลำดับ: `?lang=` → localStorage → ไทย)
      กดสลับแล้ว**เขียนภาษาลง URL ด้วยเสมอ** ลิงก์ที่แชร์/บุ๊กมาร์กจึงพกภาษาติดไปเอง
      · **ต้องมี `<Suspense>` ครอบ** เพราะ `useSearchParams` บังคับให้ subtree ถึงขอบ Suspense
      เรนเดอร์ฝั่ง client ล้วน (ยืนยันจาก `node_modules/next/dist/docs/.../use-search-params.md`
      ตามกติกาข้อ 2) — ผลพลอยได้คืออ่าน localStorage ตอน initial state ได้เลยไม่เกิด hydration mismatch
      · ภาษาที่จำไว้เก็บเป็น `useSyncExternalStore` ไม่ใช่ `useState` เพื่อเขียนจาก effect ได้โดยไม่ชน
      eslint `react-hooks/set-state-in-effect` (ปัญหาและทางแก้เดียวกับ `useMediaQuery` ตอนเฟส 9.3)
- [x] เติมคู่ภาษาอังกฤษที่ยังขาด: `Day.cityEn` + `Day.weekdayEn` (ฝังค่าไว้ ไม่ derive จาก
      `toLocaleDateString` เพื่อไม่ให้ผลเปลี่ยนตาม locale ของเครื่องที่เปิด) · `DayEvent.titleEn` 13 ตัว
      · `CITY_NAME_EN` · `TRAVEL_MODE_LABEL_EN` · `BOOKING_CATEGORY_LABEL_EN` · `INTERCITY_MODE_LABEL_EN`
      · `LayoverBadges` รับ `lang` แล้ว · **จงใจไม่ทำ `detailEn`** — detail เป็นโน้ตวางแผนของเราเอง
      โหมด en จึงซ่อนทิ้ง (รวมถึง `day.note` และโน้ตต่อจุด) ดีกว่าโชว์ไทยปนกลางหน้าอังกฤษ
- [x] **`/summary?lang=en&for=immigration`** — `components/ImmigrationSheet.tsx` ตัดทุกอย่างที่เจ้าหน้าที่
      ไม่ได้ถามออกหมด เหลือ 4 ตาราง: ชื่อตามพาสปอร์ต · เที่ยวบิน 4 เที่ยว (วันที่/เลขบิน/เส้นทาง/ออก/ถึง) ·
      ที่พักทุก leg (ชื่ออังกฤษ + ที่อยู่ท้องถิ่น + เบอร์โทร) · ตารางรายวันย่อ (วันที่/วัน/เมือง/สถานที่ EN)
      · หัวเว็บสีเขียวถูก `print:hidden` ในโหมดนี้ กระดาษแผ่นแรกจึงเริ่มที่ "Travel Itinerary" ตรงๆ
      · **`DayEvent.dayOffset`** ใหม่ — VN428 ออกตี 1:15 ซึ่งเป็น *วันที่ 12* แต่แสดงอยู่ในการ์ดวันที่ 11
      ถ้าไม่มีฟิลด์นี้ วันบินเข้าประเทศบนเอกสารจะคลาดไป 1 วัน (ยืนยันในเบราว์เซอร์แล้วว่าขึ้น 12 Oct 2026 ถูก)
      · **ชื่อตามพาสปอร์ตเก็บใน localStorage ไม่ใช่ Supabase โดยตั้งใจ** — RLS โปรเจกต์นี้เปิดสาธารณะ
      ทุกตารางและ anon key อยู่ในบันเดิล (ดูขอบเขตเฟส 13.5) ชื่อ-นามสกุลตามพาสปอร์ตไม่ควรอยู่ในนั้น
- [x] ปุ่มสลับ 🇹🇭/🇬🇧 ที่หัว `/summary` ตำแหน่งเดียวกับปุ่มพิมพ์ + ปุ่มสลับไป/กลับหน้า ตม.

**แถม — ปิดงานค้างของเฟส 14 (ที่พักไม่มีชื่อภาษาท้องถิ่น) ไปพร้อมกัน:**
- [x] migration `0026_trip_hotels_local.sql` — `name_local` / `address_local` / `name_en` / `address_en`
      / `phone` ใน `trip_hotels` · `/api/geocode?locale=ko|vi` ยิง Place Details ซ้ำอีก 2 ภาษาแบบขนาน
      (เพิ่ม `languageCode` ให้ `getPlaceDetails`) เกิดเฉพาะตอนกดบันทึกที่พักซึ่งนานๆ ครั้ง
      · `HotelEditModal` โชว์ชื่อ/ที่อยู่ภาษาท้องถิ่น + เบอร์โทรให้เห็นก่อนกดบันทึก
      · `/today` ปุ่ม "นำทางกลับที่พัก" ใช้ `hotelNavigationName()` แล้ว — เดิมส่งชื่อไทยเข้า Naver/Kakao
      ซึ่งค้นไม่เจอ (บั๊กเดียวกับที่เฟส 14 แก้ให้จุดแวะไปแล้ว)
      · `setHotel()` เปลี่ยนจาก 6 อาร์กิวเมนต์เรียงกันเป็นอ็อบเจกต์เดียว (`HotelInput`) — พอเพิ่มอีก 5 ช่อง
      แล้วสลับตำแหน่งกันง่ายเกินไป

**ผู้ใช้ต้องทำ:** รัน `0026_trip_hotels_local.sql` แล้ว **เปิดที่พักแต่ละที่ในหน้าแผน เลือกจาก dropdown ซ้ำ
แล้วกดบันทึกอีกครั้ง** — แถวที่บันทึกไว้ก่อนหน้านี้ยังไม่มีชื่อ/ที่อยู่/เบอร์ (ตารางที่พักบนหน้า ตม. จะขึ้น "—")

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์:
`/summary?lang=en` ขึ้นอังกฤษทั้งหน้ารวมชื่อสถานที่/เมือง/ป้ายทรานซิต · กดปุ่ม 🇹🇭 แล้วกลับไทยทันที
URL เปลี่ยนเป็น `?lang=th` และ localStorage จำค่าไว้ · `?for=immigration` ขึ้นเอกสาร 4 ตารางครบ
**เที่ยวบิน VN428 ขึ้นวันที่ 12 Oct 2026 ถูกต้อง** (ไม่ใช่ 11) · console สะอาด · ไม่มีการแก้ข้อมูลจริงใน DB

---

### เฟส 17 — ของแถมสำหรับใช้จริงหน้างาน ✅ เสร็จ (12 ส.ค. 2026)

- [x] **พยากรณ์อากาศรายวัน** บนหัวการ์ดวัน + หัว `/today` — `/api/weather` → **Open-Meteo**
      (`lib/weather.ts` + `hooks/useTripWeather.ts` + `components/WeatherBadge.tsx`)
      **ทำไมไม่ใช้ Google Weather API:** กติกาข้อ 3 บอกว่าคีย์นี้เปิดไว้เฉพาะ `places`/`routes`
      การใช้ Weather ต้องไปเปิด API ตระกูลใหม่เพิ่ม · Open-Meteo ฟรี ไม่ต้องสมัคร ไม่ต้องมีคีย์เลย
      · ยิง **1 คำขอต่อเมือง** ครอบช่วงวันของเมืองนั้น (ไม่ใช่ 1 ต่อวัน) + แคชระดับโมดูล + in-flight
      dedupe แบบเดียวกับที่แก้บั๊ก 9.2 · แคชฝั่งเซิร์ฟเวอร์ 3 ชม.
      ⚠️ **พยากรณ์ล่วงหน้าได้ ~16 วันเท่านั้น** — ตอนนี้ (เหลือ 60 วัน) จึงยังไม่มีข้อมูลจริงสักวัน
      ซึ่งดูเหมือนฟีเจอร์พัง จึงขึ้นข้อความบอกตรงๆ เหนือลิสต์วันว่าจะขึ้นเมื่อไหร่
- [x] **การ์ดฉุกเฉิน** (`components/EmergencyCard.tsx` + `data/emergency.ts`) ท้ายหน้า `/today` ยุบไว้เป็นค่าเริ่มต้น
      · **เบอร์เปลี่ยนตามประเทศของวันนั้น** (ฮานอย = เวียดนาม 113/115 + สถานทูตฮานอย · ที่เหลือ =
      เกาหลี 119/112 + 1330 + สถานทูตโซล) กดโทรได้ตรงๆ ผ่าน `tel:` · เลขที่จองทั้งหมดรวมไว้ที่นี่ด้วย
      · โรงพยาบาลดึงสดรอบ**ที่พักคืนนั้น** (ไม่ต้องขอสิทธิ์ location) ยิงเฉพาะตอนกางการ์ด ไม่เสียโควตาฟรี
      · **เจอระหว่างทำ:** Google ติดป้าย `hospital` ให้คลินิกศัลยกรรม/ผิวหนังในเกาหลีเยอะมาก
      เรียงตาม `DISTANCE` แล้วได้คลินิกเสริมความงาม 3 อันดับแรกรอบซอมยอน → เปลี่ยนเป็น `POPULARITY`
      รัศมี 8 กม. ได้ Pusan National University Hospital / Busan Paik / 동아대학교병원 ขึ้นมาแทน (ยืนยันด้วยการยิงจริง)
      · เบอร์สถานทูตติดหมายเหตุให้ตรวจซ้ำจากเว็บทางการก่อนเดินทาง (ข้อมูลชนิดที่เปลี่ยนได้)
- [x] **ธีมมืดของ `/today`** — `hooks/useDarkTheme.ts` ตั้ง `data-theme` ที่ `<html>` (ไม่ใช่กล่องของหน้า
      เพื่อให้พื้น `body` เปลี่ยนตามด้วย) และ **ล้างทิ้งตอน unmount เสมอ** ธีมจึงไม่รั่วไปหน้าอื่น
      · ตามธีมของเครื่องเป็นค่าเริ่มต้น กดปุ่ม 🌙/☀️ ทับได้ จำใน localStorage
      · **บทเรียนสำคัญ:** `@theme inline` ของ Tailwind v4 **ฝังค่าสีลงคลาสยูทิลิตี้** การเขียนทับ
      `--color-pine-soft` ตอนรันไทม์จึงไม่มีผลเลย ต้องประกาศโทเคนใหม่ที่ชี้ไป `var(--surface)` ฯลฯ
      แล้วสลับค่าตัวแปรชั้นนอกแทน · และ**ห้ามพลิก `cream`/`ink` ตรงๆ** เพราะ `text-cream` ถูกใช้เป็น
      "ตัวอักษรสว่างบนพื้นสีเน้น" ด้วย (9 จุดใน `/today`) จะหายไปกับพื้นทันที
      → เพิ่มโทเคนเชิงความหมาย `surface`/`surface-raised`/`surface-soft`/`content`/`content-soft`/`line`
      + คู่แผงสี `panel-pine`/`panel-maple`/`panel-gold` (พื้นจาง+ตัวอักษรเข้ม สลับขั้วกันในธีมมืด)
      · ปุ่ม Kakao (`bg-[#FEE500]`) คงตัวอักษร `text-ink` ไว้ตายตัว — เป็นสีแบรนด์ ไม่ใช่พื้นของธีม
- [x] **แถบวัน sticky + ยุบวันที่ล็อกแล้วอัตโนมัติ** — `components/DayJumpBar.tsx` ชิป 11 วันเลื่อนแนวนอน
      กดแล้วกระโดดไปการ์ดวันนั้น ไฮไลต์วันที่กำลังดูอยู่เองด้วย `IntersectionObserver`
      · วันที่ล็อกแล้วยุบเหลือหัวการ์ด + สรุปบรรทัดเดียว ("📍 4 จุด · 07:00 – 14:15") แตะเพื่อกาง
      · ยุบแล้ว **unmount ทั้งลิสต์และแผนที่จริงๆ** ไม่ใช่ซ่อนด้วย CSS (บทเรียนจากเฟส 13) = ได้ performance
      มาฟรี · ปลอดภัยเพราะ droppable ของวันที่ล็อกถูก `disabled` อยู่แล้ว ลากมาวางไม่ได้ตั้งแต่แรก
      · ใช้สูตร `collapsed = locked && !manuallyExpanded` ไม่ใช่ effect sync — ปลดล็อกแล้วกางเองทันที

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · ไม่ต้องเปิด API อะไรเพิ่ม (Open-Meteo ไม่ต้องใช้คีย์)

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์ที่ 375px:
`/today` ขึ้นธีมมืดตามเครื่องถูกต้อง ปุ่มสลับทำงาน · การ์ดฉุกเฉินกางแล้วได้เบอร์เวียดนามในวัน `d0` และ
เบอร์เกาหลี + โรงพยาบาลใหญ่ 3 แห่งในวัน `d1` · หน้าแผนขึ้นชิปครบ 11 วัน กดแล้วกระโดดถูกวัน ไฮไลต์ตามสกรอลล์
· วันที่ล็อกไว้ 2 วันยุบอยู่ กด "กางดู" แล้วกางครบ · console สะอาด · ไม่มีการแก้ข้อมูลจริงใน DB (กติกาข้อ 5)

---

### เฟส 18 — PWA + offline อ่านอย่างเดียว ✅ เสร็จ (12 ส.ค. 2026)

**กลับคำจากการตัดสินใจเดิม** (ดูหัวข้อ 1) — ผู้ใช้ขอเอง 11 ส.ค. 2026

- [x] `app/manifest.ts` (รูปแบบของ Next เวอร์ชันนี้ ไม่ใช่ `public/manifest.json`) + ไอคอน 4 ขนาด
      สร้างด้วยสคริปต์ PIL (ใบเมเปิลสี maple บนพื้น pine ตามธีมเว็บ) รวมตัว `maskable` สำหรับ Android
      และ `apple-touch-icon` สำหรับ iOS ที่ไม่อ่านไอคอนจาก manifest · `start_url: "/today"` ไม่ใช่ `/`
      เพราะเหตุผลที่ติดลงโฮมสกรีนคือใช้ตอนเที่ยวจริง เปิดแอปต้องเห็น "ตอนนี้ไปไหนต่อ" ทันที
- [x] service worker (`public/sw.js`) — เขียนเอง ไม่ใช้ Workbox (กฎที่ต้องการมีแค่ 3 ข้อ ไม่คุ้ม dependency
      และคู่มือ PWA ของ Next พูดถึงแค่ push notification ส่วนคู่มือ offline-support ระบุเองว่า full offline
      load ต้องเขียน SW เอง) · navigation = network-first แล้วตกไปแคช แล้วตกไปหน้า "ยังไม่เคยเปิดหน้านี้"
      · รูป/รายละเอียดสถานที่/เวลาเดินทาง/อากาศ = stale-while-revalidate · `_next/static` = cache-first
      · **ข้ามทุกอย่างที่ไม่ใช่โดเมนตัวเอง** (Supabase ต้องสดเสมอเพราะเป็นทางเขียนข้อมูล)
      · **ไม่ precache หน้า HTML ตอน install** — ด่าน PIN จะตอบ 307 ไป `/unlock` แล้วเราจะเก็บหน้า unlock
      ไว้เป็นเนื้อหาของ `/today` ถาวร · กันซ้ำอีกชั้นด้วย `!response.redirected` ก่อนเก็บทุกครั้ง
      · **ลงทะเบียนเฉพาะ production** — ตอน dev SW จะเสิร์ฟบันเดิลเก่าทับของที่เพิ่งแก้ HMR พังแบบหาสาเหตุยาก
- [x] snapshot ข้อมูลใน localStorage (`lib/localCache.ts`) — hook ข้อมูล **7 ตัว** (plans/stops/hotels/
      bookings/daySettings/customPlaces/overnightOverrides) ขึ้นจากแคชก่อนแล้วค่อยทับด้วยของสด
      หน้าจึงขึ้นทันทีไม่ต้องรอ Supabase และยังอ่านได้ตอนเน็ตหลุด · เขียน/อ่านใน `init()` ที่เป็น async
      จึงไม่ชน eslint `set-state-in-effect` และไม่เกิด hydration mismatch (effect รันหลัง hydrate)
- [x] **ไม่ทำ offline editing** — ตอนออฟไลน์ขึ้นแถบ "📴 เน็ตหลุด — โหมดอ่านอย่างเดียว" บนสุดของทุกหน้า
      และหน้าแผน**บังคับล็อกทุกวันอัตโนมัติ** โดยใช้กลไก "ล็อกวัน" เดิมที่ปิดปุ่มแก้ครบทุกอันอยู่แล้ว
      (ที่จับลาก/ปรับเวลา/ลบ/โน้ต/รูป/ปุ่มแทรก/โหมดเดินทาง/droppable ของ dnd-kit) แทนการไล่ใส่
      `disabled` ทีละปุ่มใหม่ทั้งหน้า · ตัวนับ "ล็อกแล้ว n/11" ยังอ่านค่าจริงจาก DB ไม่ใช่ค่าที่ถูกบังคับ
- [x] อ่าน `node_modules/next/dist/docs/` ก่อนเขียนแล้ว (กติกาข้อ 2) — เจอ `experimental.useOffline` +
      `useOffline()` จาก `next/offline` ที่เวอร์ชันนี้มีให้ · **ตัดสินใจไม่ใช้** เพราะต้องเปิดธง experimental
      ที่เปลี่ยนพฤติกรรม navigation ทั้งเว็บ (คำขอที่ล้มจะค้างรอ retry แทน error) บนเว็บที่ต้องใช้จริง
      ตอนอยู่เกาหลีอีก 2 เดือน — และเมื่อมี SW เสิร์ฟ shell จากแคชแล้ว ประโยชน์ส่วนเพิ่มก็เหลือน้อย
- [x] เปิด `/sw.js` + `/manifest.webmanifest` ให้ผ่านด่าน PIN — เบราว์เซอร์ขอ 2 ไฟล์นี้แบบ**ไม่แนบคุกกี้**
      ถ้าโดนดักจะได้ HTML แทนไฟล์จริง → ลงทะเบียน SW ไม่ผ่านและปุ่ม "เพิ่มลงหน้าจอโฮม" ไม่ขึ้น

**บั๊กที่เจอระหว่างทำ (บันทึกไว้กันพลาดซ้ำ):** ตอนแรกอ่านสถานะเน็ตจาก `navigator.onLine` ตรงๆ
แล้วเจอของจริงว่า **เบราว์เซอร์ทดสอบคืน `false` ทั้งที่โหลดหน้าเว็บและคุย Supabase ได้ปกติ** — ผลคือเว็บ
ล็อกตัวเองเป็นอ่านอย่างเดียวถาวรโดยไม่มีทางกลับ ซึ่งแย่กว่าไม่มีฟีเจอร์นี้เลย · แก้เป็นใช้ `navigator.onLine`
เป็นแค่ **สัญญาณให้ไปตรวจ** แล้วยืนยันด้วยคำขอจริงไปที่ `/manifest.webmanifest` เสมอ (เริ่มต้นถือว่าออนไลน์
= ฝั่งที่แก้ข้อมูลได้) · ตรวจตอนเหตุการณ์ `online`/`offline`, ตอนกลับมาเปิดหน้าจอ (`visibilitychange` —
ท่าที่เกิดจริงที่สุดคือควักมือถือออกจากกระเป๋ากลางอุโมงค์) และยิงซ้ำทุก 15 วิ**เฉพาะตอนที่เชื่อว่าออฟไลน์**
· ผลพลอยได้: จับเคส "ต่อ WiFi อยู่แต่ไม่มีเน็ตออกนอก" ที่ `navigator.onLine` ไม่เคยจับได้ด้วย

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · หลัง deploy แล้วเปิดเว็บบนมือถือ 1 ครั้งเพื่อให้ SW ติดตั้ง
แล้วกด "เพิ่มลงหน้าจอโฮม" (iOS: ปุ่มแชร์ → Add to Home Screen) · **ต้องเปิด `/today` และ `/summary`
อย่างน้อยคนละครั้งตอนมีเน็ต** ถึงจะเปิดได้ตอนเน็ตหลุด (SW เก็บหน้าตอนเปิดจริง ไม่ได้ precache ไว้ล่วงหน้า)

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` + `npm run build` สะอาด ·
ทดสอบด้วย `next build && next start` ตามที่คู่มือ Next แนะนำ (ไม่ใช่ dev server):
SW ลงทะเบียนสำเร็จ scope `/` · `/manifest.webmanifest` เสิร์ฟด้วย MIME `application/manifest+json` ·
เปิด `/`, `/today`, `/summary` ครบแล้วมี shell 3 หน้า + ข้อมูล 33 รายการในแคช + localStorage 7 คีย์ ·
**แล้ว "ปิดเซิร์ฟเวอร์ทิ้ง" แล้วโหลด `/today` ใหม่ทั้งหน้า → ขึ้นครบทั้งหน้าจากแคช** (จุดถัดไป, รูป,
ปุ่มนำทาง, ธีมมืด) พร้อมแถบ "เน็ตหลุด" · เปิดหน้าที่ไม่เคยเข้า → ได้หน้า fallback ภาษาไทยถูกต้อง ·
จำลองเน็ตหลุด/กลับมาในเบราว์เซอร์: หลุด → แถบขึ้น + การ์ดวันล็อกทั้ง 11 วัน, กลับมา → แถบหาย +
เหลือ 2 วันที่ล็อกจริง · ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

### เฟส 19 — งานรื้อ performance ✅ เสร็จ 5 จาก 6 ข้อ (12 ส.ค. 2026)

ไม่มีข้อไหนกระทบการใช้งานจริงระหว่างทริป — ตั้งใจไว้ว่าตัดทั้งเฟสได้ แต่พอเฟส 15-18 จบเร็วกว่าที่คิด
จึงเก็บข้อที่เสี่ยงต่ำมาทำต่อเลย เหลือข้อสุดท้าย (Server Component) ที่ยังใหญ่เกินกว่าจะทำก่อนบิน

**ผลวัดจริง (เปิดหน้าแผน 1 ครั้ง, dev server, จอ 1400px — ก่อน → หลัง):**

| วัดอะไร | ก่อน | หลัง |
|---|---|---|
| request รวมทั้งหน้า | 147 | **90** |
| `/api/place-details` | 34 | **3** |
| `/api/place-photos` | 27 | **1** |
| ข้อมูลรูปที่โหลด | ~11.5 MB | **1.1 MB** |
| แผนที่ที่ mount ตอนเปิดหน้า (จอคอม) | 8-10 | **0** (mount เมื่อเลื่อนมาใกล้) |
| โหลด `trip_selections` | ทุกครั้ง | **ไม่โหลดแล้ว** |

- [x] **batch `/api/place-details`** — เพิ่ม `?queries=a|b|c` คืน `{ results: { [query]: {...} } }`
      ฝั่งเซิร์ฟเวอร์อ่าน `place_details_cache` ทีเดียวด้วย `.in()` แล้วยิง Google เฉพาะที่ยังไม่เคยแคช
      · ฝั่ง client เพิ่มชั้นรวมคำขอที่เกิดใน ~20ms เดียวกัน (แยกคิวตาม locale) — dedupe เดิมจากบั๊ก 9.2
      กัน "ยิงซ้ำที่เดิม" ได้แล้ว แต่กัน "หลายที่ในเวลาไล่เลี่ยกัน" ไม่ได้ · `?query=` เดี่ยวยังใช้ได้เหมือนเดิม
      (หน้า `/today` ที่ขอ `?live=1` จุดเดียวยังพึ่งอยู่) — **ไม่ได้ทำเวอร์ชัน Server Component** ตามที่เสนอไว้
      เป็นทางเลือก เพราะหน้าแผนเป็น client component ทั้งก้อนและ realtime อยู่แล้ว
- [x] **batch `/api/place-photos`** ด้วยกลไกเดียวกัน (ไม่ได้อยู่ในลิสต์เดิม แต่เป็นตัวที่ยิงถี่พอๆ กัน)
- [x] **lazy mount แผนที่ด้วย IntersectionObserver** — `hooks/useInViewOnce.ts` (`rootMargin: 300px`)
      กล่องเปล่าขนาดเท่าของจริงกันหน้ากระตุกตอนแผนที่โผล่ · **จงใจไม่ unmount ตอนเลื่อนพ้นสายตา**
      เพราะจะโหลด tile ซ้ำและเสียตำแหน่ง/ซูมที่ผู้ใช้ปรับไว้
- [x] **ลบระบบ slot เดิมทิ้ง** — ลบ `useSelections` + `useLegacyBootstrap` + `bulkInsert` + type
      `TripSelection` · ยืนยันก่อนลบแล้วว่า `trip_plans` มีข้อมูลจริง (`plan-default` / "แผนหลัก" สร้าง 6 ส.ค.)
      bootstrap จึงไม่มีทางทำงานอีก · `overallLoaded` ไม่ต้องรอตารางที่ไม่ได้ใช้อีกต่อไป
      · `Day.slots` ใน `data/itinerary.ts` **ยังอยู่แต่ไม่มีใครอ่านแล้ว** — เก็บไว้เป็นบันทึกการวางแผนรอบแรก
      ติดคอมเมนต์เตือนไว้ที่ type แล้ว ถ้าอยากลบจริงต้องเอา `Day.slots` ออกทั้งหมด
- [x] **รูปเล็กลงจริง แทนการใช้ `next/image`** — ต้นเหตุที่แท้จริงคือ `/api/place-photo` คืนรูปกว้าง
      800px เสมอ ทั้งที่รูปย่อในแถวจุดแวะกว้างจริง 40px · เพิ่มพารามิเตอร์ `?w=` (allowlist 160/400/800
      ไม่ใช่ตัวเลขอิสระ เพราะทุกค่าที่ต่างกันคือแคชอีกก้อนทั้งฝั่ง Next และ Vercel edge)
      `PlaceThumb` ขอ 160 · `PlaceCard`/`PhotoGallery` ขอ 400 · **ไม่ต้องใช้ `next/image`/`remotePatterns`
      เลย** — รูปมาจาก API route ของเราเองซึ่งเปลี่ยนขนาดที่ต้นทางได้ตรงๆ และไม่ต้องเพิ่ม proxy อีกชั้น
- [ ] **รวม realtime เหลือ channel เดียวต่อ plan** — **ตรวจแล้วไม่ทำ** `supabase-js` multiplex ทุก channel
      ผ่าน websocket เส้นเดียวอยู่แล้ว "10 เส้น" ที่วัดได้คือ 10 topic ไม่ใช่ 10 connection ของจริง
      ประโยชน์จึงน้อยกว่าที่ประเมินไว้ตอนวางแผนมาก ขณะที่ต้องรื้อ handler ของ 8 hook พร้อมกัน — ไม่คุ้มความเสี่ยง
      บนเว็บที่ต้องใช้จริงอีก 60 วัน
- [ ] **ยกส่วน static ขึ้น Server Component** — งานใหญ่สุดในลิสต์ **ยังไม่ทำ ทำหลังทริป**

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` + `build` สะอาด · ทดสอบจริงในเบราว์เซอร์
ครบทั้ง 3 หน้าที่จอ 1400px: หน้าแผนขึ้นครบ 11 วัน รูปย่อโหลดครบ 27 ใบ (`&w=160`), เลื่อนไปวัน `d1`
แล้วแผนที่ mount ขึ้นมา 1 ตัวถูกต้อง · `/today` ขึ้นการ์ดจุดถัดไป + เวลาเปิด-ปิดครบ ยิง place-details
ครั้งเดียว · `/summary` ขึ้นครบ 11 วัน · console สะอาด · ไม่มีการแก้ข้อมูลจริงใน DB (กติกาข้อ 5)

---

### เฟส 20 — ฟีดแบ็กจากการใช้จริงบนมือถือรอบ 2 ✅ เสร็จ (12 ส.ค. 2026)

ฟีดแบ็ก 4 ข้อหลังผู้ใช้ลองใช้จริงบน iPhone 16 Pro · **ไม่มี migration ใหม่** (คอลัมน์ `kind` ใน
`trip_stops` เป็น text อิสระ ไม่มี CHECK constraint จึงรับค่าใหม่ได้เลย — ดูคอมเมนต์ใน migration 0025)

- [x] **iOS ซูมหน้าจอเองตอนแตะช่องค้นหา** (ฟีดแบ็กข้อ 1) — iOS Safari บังคับซูมเมื่อ font ของช่องกรอก
      เล็กกว่า 16px ซึ่งช่องกรอกทั้งเว็บเป็น `text-sm` (14px) / `text-xs` (12px) หมดทั้ง 27 จุด
      · แก้ด้วยกฎเดียวใน `app/globals.css` บังคับ 16px เฉพาะ `max-width: 767px` แทนไล่แก้ทีละไฟล์
      (กฎอยู่นอก `@layer` จึงชนะคลาสยูทิลิตี้ของ Tailwind v4 เองโดยไม่ต้องใช้ `!important` —
      หลักการเดียวกับกฎ `button:active` ของเฟส 4)
      · **จงใจไม่ใช้ `maximum-scale=1` / `user-scalable=no`** เพราะปิดการซูมด้วยนิ้วทั้งเว็บ = ปัญหา
      accessibility (และ iOS สมัยใหม่ก็ไม่สนใจค่านั้นแล้ว)
      · **บั๊กที่เจอระหว่างทำ:** กฎรุ่นแรกเขียนเป็น "ขนาดตายตัว" แล้วไป **ย่อ** ช่อง PIN ในหน้า `/unlock`
      ที่เป็น `text-2xl` (24px) ลงเหลือ 16px — แก้เป็น "ขนาดขั้นต่ำ" โดยยกเว้น `.text-base` ขึ้นไปด้วย `:not()`
      · วัดจริงในเบราว์เซอร์: 375px → `text-sm` เป็น 16px, `text-2xl` ยังเป็น 24px, checkbox ไม่โดน ·
      1280px → `text-sm` ยังเป็น 14px ตามดีไซน์เดิม
- [x] **แวะที่พักกลางวันได้** (ฟีดแบ็กข้อ 2 — เช็คอิน / ฝากกระเป๋า / พักก่อนเที่ยวต่อ) — เดิมที่พักขึ้นเป็น
      anchor หัววัน/ท้ายวันเท่านั้น แทรกกลางวันไม่ได้ · เพิ่ม `kind: "hotel"` เป็นแถวจุดแวะจริง แทรกได้ทุก
      ตำแหน่งเหมือนร้านอาหาร/ข้ามเมือง (ปุ่ม `🏨 + แวะที่พัก` ท้ายการ์ด + `🏨 + แวะที่พักตรงนี้` ระหว่างจุด)
      · **`place_id` ฝังพิกัดไว้ในตัวเป็น `hotel@{lat},{lng}`** (รูปแบบเดียวกับ `hotelAnchorId` ของเฟส 9.1)
      `resolvePlace` จึง parse กลับเป็น `Place` ได้โดยไม่ต้องส่งตาราง `trip_hotels` เข้าไปทุกจุดที่เรียก
      (12 จุดใน 9 ไฟล์) — และเพราะใช้ id เดียวกับ anchor หัว-ท้ายวัน **แคชเวลาเดินทางจึงใช้ร่วมกันได้
      ไม่ยิง Routes API ซ้ำ** · ชื่อที่โชว์ดึงสดจาก `trip_hotels` ทับอีกชั้น เปลี่ยนโรงแรมแล้วแถวเปลี่ยนตาม
      (ต่างจากถ้าฝังชื่อไว้ใน id ซึ่งจะค้างเป็นของเก่า) · เพิ่ม 2 เทสต์ล็อกสัญญารูปแบบ id ระหว่าง 2 ไฟล์นี้
      เพราะถ้าหลุดจากกัน `resolvePlace` จะคืน null แล้วเวลาเดินทางหายเงียบๆ โดยไม่มี error
      · กัน `RouteSuggestionModal` ย้ายแถวนี้ (เหมือนแถว transfer) เพราะตำแหน่งมีความหมายเฉพาะที่ผู้ใช้วางเอง
      · ปุ่มขึ้นเฉพาะวันที่ตั้งที่พักไว้แล้ว — ตอนนี้คือปูซาน (`d1`-`d3`) กับซกโช (`d4`) เท่านั้น
- [x] **สถานี KTX/บัสจริงในตัวเลือก** (ฟีดแบ็กข้อ 3) — เพิ่ม 8 สถานีเข้า `data/transferPoints.ts`
      (ไฟล์เดิมที่คอมเมนต์เขียนไว้แต่แรกว่า "สนามบิน/สถานี" อยู่แล้ว) ครอบช่วงข้ามเมืองทั้ง 3 ช่วงของทริป:
      บัสปูซาน→ซกโช (15 ต.ค.) · ซกโช→คังนึง (16 ต.ค.) · KTX คังนึง→โซล (17 ต.ค.)
      · โผล่เป็นปุ่มเลือกด่วนใต้ช่อง "จาก"/"ไป" ใน `IntercityEditModal` (กดแล้วเติมชื่อให้ ไม่ต้องพิมพ์เอง)
      และเป็นปลายทางของแถว `kind="transfer"` ได้ด้วย — `TransferEditModal` จัดกลุ่ม ✈️ สนามบิน /
      🚉 สถานี แล้วเรียงเมืองของวันนั้นขึ้นก่อน (ลิสต์รวด 11 อันหาไม่เจอ)
      · ⚠️ **พิกัด/ชื่อทุกตัวดึงจาก Places API ด้วย query ภาษาเกาหลี ไม่ได้พิมพ์จากความจำ** — เพราะเมืองเดียว
      มีสถานีหลายแห่ง: **ซกโชมี 2 แห่งห่างกัน ~2.3 กม.** (고속버스터미널 บัสด่วนจากปูซาน อยู่ติดหาด ·
      시외버스터미널 บัสไปคังนึง อยู่เหนือเมืองใกล้ตลาด) ใส่ทั้งคู่พร้อมคำอธิบายกำกับว่าอันไหนใช้ตอนไหน
      · หมายเหตุ: Google มีแค่พิกัดทางเข้ารถไฟใต้ดินของ 서울역 (คลาดจากตัวสถานี KTX ~200 ม. รับได้)
- [x] **modal จัดการแผน แทน `window.prompt`/`confirm`** (ฟีดแบ็กข้อ 4) — `components/PlanEditModal.tsx`
      ใช้กล่องเดียวครอบทั้งสร้าง/เปลี่ยนชื่อ/ลบ · ตอนลบบอกชัดว่าอะไรหายอะไรไม่หาย (**ตรวจ cascade จริงใน
      โค้ดก่อนเขียนข้อความ**: ลบ `trip_plans` แล้ว `trip_stops` + `trip_day_settings` หายตาม ส่วน
      `trip_hotels`/`bookings`/`checklist_items` ไม่มีคอลัมน์ `plan_id` = ใช้ร่วมกันทุกแผน ไม่หาย)
      · **ฟีเจอร์นี้มีอยู่ก่อนแล้ว** ผู้ใช้ไม่เห็นเพราะปุ่ม "ลบแผนนี้" ขึ้นเฉพาะตอนมีแผนมากกว่า 1 แผน
      (ตอนนี้มีแผนเดียว) — งานรอบนี้คือเปลี่ยนหน้าตาให้เข้ากับเว็บ ไม่ใช่สร้างระบบใหม่

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · ไม่ต้องเปิด API เพิ่ม

**ยืนยันปิดเฟส:** `npm test` **63/63 ผ่าน** (เพิ่ม 2 เทสต์ id ที่พัก) · `lint` + `tsc --noEmit` + `build`
สะอาด · ทดสอบข้อ 1 จริงในเบราว์เซอร์ที่ 375px และ 1280px ครบ (ตัวเลขด้านบน)
⚠️ **ข้อ 2-4 ยังไม่ได้ตรวจด้วยตาในเบราว์เซอร์** — dev server ติดด่าน PIN และผมไม่กรอกรหัสผ่านเอง
ครอบด้วย typecheck/lint/build/unit test แทน · **แนะนำให้ลองจริงบนมือถือหลัง deploy**: แทรก "แวะที่พัก"
ที่วันปูซาน, เปิด modal ข้ามเมืองวันที่ 15 ต.ค. ดูปุ่มสถานี, กด "+ แผนใหม่" ดู modal ใหม่

---

## 3.7 รอบตรวจ UX/UI ทั้งเว็บ (12 ส.ค. 2026) — เฟส 21

เฟส 1-20 โฟกัส "ฟีเจอร์ครบไหม / เลขถูกไหม" มาตลอด รอบนี้เป็นรอบแรกที่ตรวจ **ชั้นปฏิสัมพันธ์**
(feedback ตอนกด · กันพลาด · คีย์บอร์ด/โฟกัส · ลำดับสิ่งที่เห็นก่อน) ผู้ใช้สั่งแก้ทั้ง 4 กลุ่มที่เจอ
และอนุญาตให้จัดโครงหน้าแผนใหม่ได้

### เฟส 21.1 — ของกลางที่ใช้ซ้ำทั้งเว็บ ✅

- **`components/Modal.tsx` + `hooks/useDismissable.ts` (ใหม่)** — โมดัล 8 ตัวเคยก๊อป shell เดียวกัน
  ไปแปะทีละไฟล์ และ**ทุกตัว**ขาดเหมือนกันหมด: `role="dialog"` / `aria-modal` / Esc / ล็อกโฟกัส /
  คืนโฟกัสตอนปิด / `aria-label` ที่ปุ่ม ✕ · รวมมาที่เดียวแล้ว refactor ครบทั้ง 8 ไฟล์
  (`TransferEdit` `NearbyPlaces` `PlanEdit` `RouteSuggestion` `PlaceDetail` `BookingEdit`
  `Intercity` `HotelEdit`) · `BottomSheet` ของคลังสถานที่ใช้ `useDismissable` ร่วมด้วย
  ⚠️ **ห้ามย้ายการโฟกัสเข้ากล่องไปไว้ใน `requestAnimationFrame`** — rAF ไม่ทำงานตอนแท็บถูกซ่อน
  โฟกัสจะค้างอยู่นอกกล่องแบบเงียบๆ (เจอของจริงตอนทดสอบ) ต้องทำ synchronous ใน effect
  ซึ่งปลอดภัยเพราะ React ใส่ `autoFocus` ให้ตั้งแต่ commit phase ที่จบก่อน `useEffect` เสมอ
- **`lib/toast.ts` + `components/ToastHost.tsx` (ใหม่)** — module singleton + `useSyncExternalStore`
  แบบเดียวกับ `hooks/useDarkTheme.ts` เรียกจาก hook ไหนก็ได้ไม่ต้อง prop drill ·
  `showToast(kind, msg)` / `showUndoToast(msg, onUndo)` (ค้าง 8 วิ) · mount ที่ `app/layout.tsx`
  ได้ครบ 3 หน้า · ข้อความซ้ำจะต่อเวลาอันเดิมแทนซ้อนกันเป็นตั้ง (reorder เขียนหลายแถวรวด)
- **กรอบโฟกัสทั้งเว็บ** — เดิมมี `focus:outline-none` 25 จุดแต่ไม่มี focus ring เลยสักอัน
  แก้ด้วยกฎ `:focus-visible` **นอก `@layer`** ใน `globals.css` (ชนะคลาสยูทิลิตี้ Tailwind v4
  โดยไม่ต้อง `!important` — หลักการเดียวกับกฎ `button:active` เดิม) ไม่ต้องไล่แก้ทั้ง 25 จุด
  เพราะ `:focus` กับ `:focus-visible` เป็นคนละสถานะ · พื้นเข้มใช้คลาส `.focus-ring-on-dark`
  สลับเป็นสีทอง (หัวเว็บ 3 หน้า + หัวการ์ดวัน)

### เฟส 21.2 — ความน่าเชื่อถือของการเซฟ + กันลบพลาด ✅

- **`lib/writeGuard.ts` (ใหม่)** — เดิม **0/21 hook** อ่านค่า `error` จาก Supabase เลย เซฟไม่ติดแล้ว
  เงียบสนิท · ตอนนี้ทุกจุดเขียนใน hook ทั้ง 9 ตัวห่อด้วย `writeGuard` แล้ว พังแล้ว toast บอก
  พร้อม `reload()` ดึงของจริงมาทับ state ที่เดาไว้ (ตรงกว่าการเขียน rollback รายฟิลด์ทุก action)
  · จงใจ **ไม่เงียบตอนออฟไลน์** เพราะ `navigator.onLine` เชื่อไม่ได้ เอามาใช้แค่เลือกคำพูด
- **`addStop` เดิมไม่ตั้ง local state เลย** รอ realtime echo อย่างเดียว → insert พัง = กดปุ่ม +
  แล้วไม่มีอะไรเกิดขึ้น ไม่มีข้อความบอก (แถม `flashNewStop` ไฮไลต์ id ที่ไม่มีจริง) แก้เป็น optimistic แล้ว
- **undo แทน confirm** — `removeStop`/`removeItem` คืน snapshot ทั้งแถว (โน้ต/รูป/โหมดเดินทาง/
  `order_index`) ให้ทำปุ่ม "เลิกทำ" ได้ · ครอบคลุม: ลากทิ้งคลัง · ✕ ที่แถวจุดแวะ · ✕ ของที่ต้องเตรียม ·
  ลากข้ามเมือง (เดิมเป็น `window.confirm` ขวางทาง ทั้งที่วันทางผ่านเที่ยว 2 เมืองได้จริง)
- **ลบตั๋ว/booking ยังยืนยันก่อน** (`ConfirmModal`) — เดิมกด "ลบ" ทีเดียวหายพร้อมไฟล์แนบ ไม่ถามสักคำ
  · ที่นี่ใช้กล่องยืนยันไม่ใช่ undo เพราะไฟล์ใน Storage เอากลับจากในเว็บไม่ได้

### เฟส 21.3 — จัดโครงหน้าแผนใหม่ ✅

- **`components/TripPrepPanel.tsx` (ใหม่)** — ยุบ ที่พัก + ตั๋ว + ของที่ต้องเตรียม เป็นบล็อกเดียว
  ปิดไว้ก่อนบนมือถือ (จอ `lg:` กางไว้ตามเดิม) จำสถานะที่ `localStorage: trip-prep-open`
  หัวบล็อกโชว์ "ที่พัก 4/4 · ตั๋ว N · ของที่ต้องเตรียม X/Y" และขึ้นสีเตือนถ้าที่พักยังไม่ครบ
- **`DayJumpBar` ย้ายขึ้นบนสุดใต้หัวเว็บ** — เดิมอยู่ใต้แผงทั้งสาม ต้องเลื่อนผ่านไปก่อนมันถึงจะติดบนจอ
  = ใช้ไม่ได้ตอนที่ต้องใช้ที่สุด · **ชิปที่ไฮไลต์เลื่อนเข้ามาในจอเอง** (เดิมดูวันที่ 9 แล้วชิปหลุดออกนอกจอ)
  มี flag กันชิงเลื่อนตอนผู้ใช้กำลังปัดแถบเอง
- **`TripHeader` เตี้ยลง + `TripSettingsModal` (ใหม่)** — ย้าย ชื่อคุณ / เลือกแผน / สร้าง-เปลี่ยนชื่อ-ลบแผน /
  ล็อกทุกวัน เข้าปุ่ม ⚙️ (ของที่ตั้งครั้งเดียวแล้วแทบไม่แตะอีก) · เหลือบนหัวแค่ชื่อทริป ชื่อแผน จำนวนจุด
  · **จุดแดงบน ⚙️ เมื่อยังไม่ได้ใส่ชื่อ** เพราะช่องกรอกหลบเข้าโมดัลแล้วไม่ได้เห็นเอง
- **`components/InsertBetweenRow.tsx` (ใหม่)** — แถวคั่นระหว่างจุดเดิมมีปุ่มตัวหนังสือ 11px สามอัน
  **ทุกช่องว่าง** (วันละ 6-7 แถว = กำแพงตัวหนังสือที่สูงกว่าตัวจุดแวะเอง) เปลี่ยนเป็นปุ่ม `+` เล็กๆ
  กดแล้วค่อยกาง · ปุ่มท้ายการ์ดเหลือ "+ เพิ่มสถานที่ให้วันนี้" เต็มความกว้าง ที่เหลือยุบใต้ "อื่นๆ"

### เฟส 21.4 — `/today` + เก็บรายละเอียด ✅

- skeleton แทนข้อความ "กำลังโหลด..." เปล่าๆ (ให้เหมือนหน้าแผนที่มี `DayCardSkeleton` อยู่แล้ว)
- **"✅ มาถึงแล้ว" มี undo** · แถบ "ช้ากว่าแผน" ได้ `role="status"` (มันโผล่เองตามเวลา ไม่ได้เกิดจากการกด)
- **ปัดซ้าย/ขวาบนหัวการ์ดเปลี่ยนวันได้** (ปุ่ม ‹ › ยังอยู่ครบ) — เช็คว่าแนวนอนชนะแนวตั้งชัดเจนก่อน
  ไม่งั้นจะไปแย่งกับการสกรอลล์
- **`/summary` รองรับธีมมืดแล้ว** ใช้โทเคนชุดเดียวกับ `/today` + มีปุ่มสลับธีมของตัวเอง
  (เดิมกลางดึกที่เกาหลีกดจาก `/today` มาหน้านี้แล้วโดนพื้นครีมสว่างจ้า ทั้งที่เป็นสองหน้าที่ใช้ต่อกันที่สุด)
  · `@media print` พลิกโทเคนกลับเป็นชุดสว่างเสมอ ไม่งั้นสั่งพิมพ์ตอนธีมมืดได้กระดาษหมึกดำเต็มแผ่น
  · **หน้าแผน `/` จงใจไม่ทำธีมมืด** — ใช้ `bg-white`/`text-ink` ตายตัวหลายร้อยจุด และเป็นหน้าที่ใช้
  ตอนวางแผนที่บ้าน ไม่ใช่ตอนอยู่เกาหลีกลางดึก (ถ้าอยากได้ค่อยแยกเป็นงานของมันเอง)
- `BottomNav` ได้ `aria-label` + `aria-current="page"` และเปลี่ยนมาใช้โทเคนธีม

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · ไม่ต้องเปิด API เพิ่ม · ไม่แตะ schema

**ยืนยันปิดเฟส:** `lint` + `tsc --noEmit` + `build` + `npm test` (63/63) สะอาด ·
ตรวจในเบราว์เซอร์จริงที่ 375px และ 1280px แล้ว (dev server ปลดล็อกโดยผู้ใช้):
ลำดับ DOM = แถบวัน → เตรียมทริป → การ์ดวัน · แผงเตรียมทริปปิดอยู่บนมือถือ (`aria-expanded=false`) ·
ชิปวันที่ active เลื่อนตามเอง · **กรอบโฟกัสขึ้นจริงบน `<input>` ที่มีคลาส `focus:outline-none`**
(computed `outline: solid 2px` — พิสูจน์ว่ากฎนอก `@layer` ชนะ Tailwind) · โมดัลครบวงจร
(`role=dialog` + `aria-modal` + `aria-labelledby` + โฟกัสเข้ากล่อง + Esc ปิด + คืนโฟกัสให้ปุ่มเดิม +
คืน `body.overflow`) · toast + undo ของจริงบน `/today` (ติ๊กมาถึง → กดเลิกทำ → `visited_at` กลับเป็น null
และข้อมูลทริปกลับสภาพเดิมครบ) · ปัดเปลี่ยนวันได้ทั้งซ้าย/ขวา และปัดแนวตั้งไม่เปลี่ยนวัน · `/summary` ธีมมืด

**⚠️ ที่ยังไม่ได้ตรวจด้วยตา:** เส้นทาง toast ของ **error** (`writeGuard` ตอนเขียนพังจริง) — ต้องทำให้
Supabase ตอบ error จริงถึงจะเห็น เลี่ยงไว้เพราะเป็นฐานข้อมูลทริปจริง · ครอบด้วย typecheck/lint/build แทน

### บั๊ก 21.5 — สถานที่ที่เพิ่มเองไป resolve เป็นร้านชื่อเดียวกันคนละประเทศ ✅ แก้แล้ว (12 ส.ค. 2026)

**อาการที่ผู้ใช้เจอ:** ร้าน "Cup & Cup" (คาเฟ่ที่กวางอัลลี ปูซาน) แผนที่ในหน้ารายละเอียดชี้ไปร้านในไทย

**ต้นเหตุ:** `custom_places` เก็บ `maps_query` เป็น**ชื่อดิบล้วนๆ** ที่ Google คืนมา ([NearbyPlacesModal.tsx](components/NearbyPlacesModal.tsx))
ต่างจาก `data/places.ts` ที่ต่อท้ายชื่อเมืองเสมอ ("Jagalchi Market Busan") แล้วทุกเส้นทางที่คุยกับ Google
เอาข้อความนี้ไปยิง `searchText` **โดยไม่ส่งพิกัดกำกับเลย** ผลคือชื่อที่ซ้ำกันทั้งโลกไป resolve ผิดร้าน:
- `place_details_cache` แถว `"Cup & Cup"` เก็บ ⭐5 (611 รีวิว) + ที่อยู่ `8000 Coit Rd #800, Plano, TX` = **ร้านชานมที่ Texas**
  (ของจริงคือ 컵앤컵커피 ⭐4.5 · 125 รีวิว · 11:00–21:30) — เวลาเปิด-ปิด/รีวิวที่โชว์ทั้งทริปจึงเป็นของร้านอื่น
- `GoogleMapEmbed` + ลิงก์ Google Maps ในแท็บ 🗺️ ส่ง `q=Cup & Cup` เฉยๆ Google เลยเดาจากตำแหน่งคนเปิดเว็บ = ได้ร้านในไทย
- ปุ่มนำทางใน `/today` **ไม่โดน** เพราะใช้ `lat`/`lng` ตรงๆ — เลยเป็นอาการ "บางปุ่มไปถูก บางปุ่มไปไทย"

**ทางแก้:** เก็บ Google place id ไว้ตั้งแต่ตอนกดเพิ่ม แล้วใช้ id ระบุตัวร้านแทนการค้นด้วยชื่อ
- [x] migration `0027_custom_places_google_id.sql` — คอลัมน์ `google_place_id` + ลบแถวแคชที่ resolve ผิด
- [x] [lib/placeQuery.ts](lib/placeQuery.ts) — `placeQueryKey()` คืน `place_id:ChIJ...` เมื่อมี id · ตกไปใช้ข้อความเมื่อไม่มี
      (ที่คัดไว้เองใน `PLACES` ไม่ต้องมี id เพราะ `mapsQuery` มีชื่อเมืองต่อท้ายอยู่แล้ว) · คีย์นี้เป็นคีย์ของ
      `place_details_cache`/`place_photo_cache` ด้วย แถวเก่าที่ผิดจึงถูกทิ้งไปเองโดยไม่ต้องแก้ค่าในแถว
- [x] `lookupPlace()` ใน [lib/googlePlaces.ts](lib/googlePlaces.ts) — คีย์เป็น `place_id:` → ยิง **Place Details** ตรงๆ
      (ตัดคำนำหน้า `places.` ใน fieldMask ให้เอง) · เป็นข้อความ → ตกไป `searchText` เหมือนเดิม ·
      `/api/place-details` กับ `/api/place-photos` เรียกผ่านตัวนี้ตัวเดียว
- [x] `googleMapsPlaceUrl()` ใน [lib/mapLinks.ts](lib/mapLinks.ts) — รู้ id ใช้ `place/?q=place_id:...` ที่ชี้ร้านเป๊ะ
- [x] แถวเก่า 33 แถวเติมย้อนหลังด้วย [scripts/backfill-place-ids.mjs](scripts/backfill-place-ids.mjs) — ค้นด้วยชื่อเดิม
      **ในกรอบ 500 ม. รอบพิกัดที่บันทึกไว้** แล้วรับเฉพาะผลที่ห่างจากพิกัดเดิมไม่เกิน 300 ม. (วิธีเดียวกับที่เติม
      ข้อมูลที่พักในเฟส 16) · รันแบบดูก่อนได้ ไม่ใส่ `--write` = ไม่เขียนอะไรลง DB

**ยืนยันแล้วกับ Google จริง:** id `ChIJg9DiTibtaDURDaC-pP90JyU` → `컵앤컵커피` · 부산광역시 수영구 광안해변로 177 ·
⭐4.5 (125) · 카페 · 11:00–21:30 — ตรงกับร้านที่ผู้ใช้เลือกไว้ ไม่ใช่ร้านที่ Texas อีกแล้ว

**หมายเหตุ:** เจอตอนไล่บั๊กนี้ว่า `custom_places` มีแถวซ้ำอยู่หลายชุด (Cup & Cup 6 แถว · Kungkungtta,
Mun Dining, Gamcheon, ตลาดจากัลชิ อย่างละ 2) — น่าจะกด "ลงคลัง" รัวก่อนปุ่มจะ disable
มีแค่แถวเดียวต่อร้านที่ถูกใช้ในแผนจริง ที่เหลือเป็นการ์ดค้างในคลัง · SQL ลบให้เลือกรันเองอยู่ท้าย migration 0027

---

## 3.8 ฟีดแบ็กจากการใช้จริงรอบ 3 (12 ส.ค. 2026) — เฟส 22

ผู้ใช้แจ้ง 4 ข้อ: โน้ตหายตอนลากกลับคลัง · ไม่รู้ว่ารูปที่เพิ่มไปโผล่ที่ไหน · หน้าสรุปข้อมูลน้อยไป ·
หน้า ตม. ยังมีภาษาไทย

### เฟส 22.1 — โน้ตติดไปกับสถานที่ตอนเก็บกลับคลัง ✅

**อาการ:** ลากจุดแวะจากวันไปวางที่คลัง = `removeStop` ลบทั้งแถว โน้ต (0013) กับรูป (0023)
หายไปด้วย · ปุ่ม "เลิกทำ" บน toast กู้ได้แค่ 8 วิ พ้นจากนั้นหายถาวร ทั้งที่ในหัวผู้ใช้
"เก็บกลับคลัง" คือ **พักไว้ก่อน** ไม่ใช่ทิ้ง

- [x] migration `0028_place_notes.sql` — ตาราง `place_notes (plan_id, place_id, note, photo_url)`
      แยกตามแผนเหมือน `trip_stops` (โน้ตเป็นของแผนนี้ ไม่ใช่ของทริปทั้งก้อนแบบ `hidden_places`)
      · PK มี `plan_id` อยู่ในตัวแล้ว จึงไม่ต้อง `replica identity full` แบบ 0009
- [x] [hooks/usePlaceNotes.ts](hooks/usePlaceNotes.ts) — โหลด/subscribe/`stashNote`/`clearNote`
      **ยังไม่รัน migration = `available` เป็น false แล้วเขียนกลายเป็น no-op เงียบๆ** ไม่เด้ง toast
      "บันทึกไม่สำเร็จ" ทุกครั้งที่ลากการ์ด (พฤติกรรมกลับไปเหมือนก่อนเฟส 22 เป๊ะ)
- [x] ลากกลับคลัง → เขียนโน้ต+รูปลง `place_notes` ก่อนลบแถว · toast เปลี่ยนเป็น
      "เก็บ X กลับคลังแล้ว — โน้ตติดไปด้วย" · กด "เลิกทำ" = คืนแถว (ซึ่งมีโน้ตในตัว) แล้วล้างที่ฝากไว้
      ไม่งั้นโน้ตเดียวกันค้างอยู่ 2 ที่
- [x] ลากลงวันอีกครั้ง → `addStopWithStashedNote` ใน [app/page.tsx](app/page.tsx) ใส่โน้ต+รูปกลับ
      ให้จุดแวะใหม่แล้วล้างที่ฝากไว้ · เป็นทางเข้าเดียวของทั้งหน้า (ลากการ์ด · ปุ่ม "+ เพิ่มลงวันนี้" ·
      ปุ่มยืนยันในโมดัลรายละเอียด) ทุกทางจึงได้โน้ตคืนเหมือนกันหมด
- [x] การ์ดในคลังโชว์ "📝 โน้ต" + ใช้**รูปที่เราถ่ายเองแทนรูป Google** พร้อมป้าย "📷 รูปของเรา"
      — ไม่งั้นไม่มีทางรู้เลยว่าที่ฝากไว้มีอะไร
- [x] **ปุ่ม ✕ ก็ฝากโน้ตเหมือนกัน** — บนมือถือการ์ดในคลัง `draggable={false}` (กันนิ้วเลื่อนดูคลังไม่ได้
      ดูคอมเมนต์ใน PlaceSidebar) ปุ่ม ✕ จึงเป็น**ทางเดียว**ที่จะเอาจุดแวะออกบนมือถือ ถ้าไม่ทำด้วย
      ฟีเจอร์นี้จะใช้ได้แต่บนจอใหญ่ ซึ่งไม่ใช่เครื่องที่ใช้วางแผนจริงระหว่างทาง
- [x] `addStop` คืน `undefined` เมื่อเขียนไม่ผ่าน (เดิมคืน id เสมอ) — กันเคสเขียนพังแล้วโน้ตที่ฝากไว้
      ถูกล้างทิ้งทั้งที่จุดแวะไม่ได้ถูกสร้างจริง

**ทดสอบกับฐานข้อมูลจริงแล้วครบทุกเส้นทาง (12 ส.ค. 2026)** — ใช้จุดแวะ `Haeundae Blueline Park`
วัน 14 ต.ค. (โน้ต "จองล่วงหน้า 2 สัปดาห์") **เก็บ snapshot ทั้งแถวก่อน แล้วคืนค่าเดิมครบทุกฟิลด์
รวม `updated_at` เมื่อจบ** (ยืนยันด้วย curl ว่า d3 กลับมาเป็น order 0/1/2 ชุดเดิม · `place_notes` ว่าง ·
ทั้งแผนยัง 9 จุดเท่าเดิม):
1. กด ✕ → `place_notes` มีแถวโน้ต · แถว `trip_stops` หายไป
2. การ์ดในคลังขึ้น "📝 จองล่วงหน้า 2 สัปดาห์"
3. กด "+ เพิ่มลงวันนี้" → จุดแวะใหม่**ได้โน้ตกลับมา** + เดาโหมดเดินทางให้เป็น `transit` + `place_notes` ว่าง
4. ลากแถวไปวางที่คลัง → toast ขึ้น "เก็บ 📸 Haeundae Blueline Park - Mipo Station กลับคลังแล้ว — โน้ตติดไปด้วย"
5. ลากการ์ดจากคลังกลับลงวัน → ได้โน้ตคืนอีกครั้ง + `place_notes` ว่าง
6. ลากไปคลังแล้วกด "เลิกทำ" → แถวกลับมาที่ `order_index` เดิมพร้อมโน้ตในตัว **และ `place_notes` ถูกล้าง**
   (ถ้าไม่ล้าง โน้ตเดียวกันจะค้างอยู่ 2 ที่)

### เฟส 22.2 — รูปที่เพิ่มเองโผล่ที่ไหนบ้าง ✅

เดิมโชว์แค่ 2 ที่: แถวจุดแวะในหน้าแผน กับการ์ด "จุดต่อไป" ใน `/today` — หน้าสรุปกับหน้ารายละเอียด
ไม่เคยเห็นเลย ตอนนี้เพิ่ม:

- [x] **หน้ารายละเอียดสถานที่** — บล็อก "โน้ตของเรา" (โน้ต + รูป) อยู่**บนสุดก่อนข้อมูล Google**
      ไม่ต้องเลื่อนผ่านเวลาเปิด-ปิด/รีวิวคนอื่นไปหา · หัวข้อรูปจาก Google เปลี่ยนชื่อให้ไม่สับสน
- [x] **หน้าสรุป** — รูปเราขึ้นเป็นรูปย่อของแถวนั้น (ไม่มีค่อยตกไปใช้รูปแรกจาก Google)
- [x] **การ์ดในคลัง** — รูปที่ฝากไว้ตอนเก็บกลับคลัง (เฟส 22.1)

⚠️ URL สองชนิดนี้ต่างกัน: ของ Google ผ่าน `/api/place-photo` ที่รับ `?w=` ต่อท้ายได้ (ดู
`photoUrlAtWidth`) · ของเราเป็น public URL ของ Supabase Storage ตรงๆ ที่ **ไม่มี query string**
ต่อ `&w=400` เข้าไปแล้วพังเป็น 404 ทันที — ต้องแยกทางกันเสมอ

### เฟส 22.3 — หน้าสรุปกดดูสถานที่ได้ + มีรูปตัวอย่าง ✅

- [x] แถวจุดแวะกลายเป็นปุ่ม กดแล้วเปิด `PlaceDetailModal` ตัวเดียวกับหน้าแผน (ยังอ่านอย่างเดียว —
      ไม่ส่ง `onConfirm` เข้าไป จึงไม่มีปุ่มแก้อะไรเลย) · แถวข้ามเมือง/แวะที่พักไม่มีอะไรให้ดูต่อ กดไม่ได้
- [x] รูปย่อ 48px หน้าแถว + เรทติ้ง/ประเภทร้าน + เวลาเปิด-ปิดของวันนั้น — ข้อมูลชุดเดียวกับที่การ์ด
      ในคลังใช้ (`usePlaceDetails` แคชร่วมกันทั้งแท็บ) จึงไม่มีคำขอเพิ่ม
- [x] เรทติ้งกับเวลาเปิด-ปิดแยกคนละบรรทัด — บรรทัดเดียวบนจอ 375px โดนตัดเหลือ "🕐 Monday: Op..."

### เฟส 22.4 — ไล่ภาษาไทยออกจากหน้า ตม. ✅

**ต้นเหตุ:** สถานที่ที่เพิ่มเองระหว่างทางเก็บชื่อที่ Google คืนมาตอน `languageCode: "th"` ลง
`custom_places.name_th` แล้ว `name_en` เป็น **null ทั้ง 35 แถว** (ยืนยันด้วย curl) — `resolvePlace`
fallback `name_en ?? name_th` เอกสารที่ยื่นเจ้าหน้าที่จึงมี "ตลาดปลาจากัลชิ" ปนอยู่
(ตัวที่คัดไว้เองใน `data/places.ts` มี `nameEn` ครบ ไม่โดน)

- [x] `/api/place-name?queries=a|b|c&lang=en` (ใหม่) — คืน**แค่ชื่อ** ไม่พ่วงเรทติ้ง/เวลาเปิด-ปิด
      ⚠️ **จงใจไม่เก็บลง `place_details_cache`** เพราะคอลัมน์ `name_local`/`locale` ที่นั่นเก็บได้
      ภาษาเดียวต่อแถวและถูกจองไว้ให้ ko/vi (เฟส 14) — เขียนทับด้วย en เมื่อไหร่ ชื่อเกาหลีที่ `/today`
      ใช้ส่งเข้า Naver/Kakao หายทันที · อาศัย fetch cache 30 วันของ Next ใน `lookupPlace` แทน
- [x] [lib/latinScript.ts](lib/latinScript.ts) — `looksLatin()` เช็คด้วย `\p{Script=Latin}` รายตัวอักษร
      **เวียดนามผ่าน** ("Phở Gia Truyền Bát Đàn") ไทย/เกาหลีไม่ผ่าน · ยิงถาม Google เฉพาะตัวที่ไม่ผ่าน
      — ของจริงทั้งทริปเหลือ **1 คำขอ 1 สถานที่** (ยืนยันจาก network log)
- [x] [ImmigrationSheet.tsx](components/ImmigrationSheet.tsx) ใช้ชื่ออังกฤษที่ได้มาแทน · ขอไม่ได้
      (ออฟไลน์/Google ไม่มีให้) ก็ใช้ชื่อเดิม ดีกว่าเว้นว่างจนเจ้าหน้าที่อ่านไม่ออกว่าไปไหน
- [x] **หน้าสรุปโหมด EN ก็โดนบั๊กเดียวกัน** (ไม่ใช่แค่หน้า ตม.) — แก้ด้วย `placeNameEn()` ตรรกะเดียวกัน
      · โหมดไทยส่งลิสต์ว่างเข้า hook = ไม่ยิงอะไรเลย ไม่มีค่าใช้จ่ายเพิ่มสำหรับคนที่ใช้ไทยตลอด
- [x] แก้ที่ต้นทางด้วย — `NearbyPlacesModal` ขอชื่ออังกฤษมาใส่ `name_en` ตั้งแต่ตอนกดเพิ่ม
      แถวใหม่จึงไม่ต้องพึ่งเส้นนี้อีก (แถวเก่า 35 แถวยังเป็น null อยู่ ปล่อยให้หน้า ตม. ถามเอาตอนเปิด)

**ยืนยันในเบราว์เซอร์จริง (dev server ที่ผู้ใช้เปิดค้างไว้):** `/summary?lang=en&for=immigration`
วัน 12 ต.ค. เดิมเป็น "Arban Hotel, Jeonpo Cafe Street, Gamcheon Culture Village, **ตลาดปลาจากัลชิ**"
→ ตอนนี้ "... **Jagalchi Market**" · ยิง `/api/place-name` แค่ครั้งเดียวต่อการเปิดหน้า (dedupe ด้วย
`inFlight` แบบเก็บ Promise ไม่ใช่แค่ธง — ไม่งั้น StrictMode รอบสองจะข้ามไปแล้วไม่เรนเดอร์ใหม่เลย)

**ยืนยันปิดเฟส:** `lint` + `tsc --noEmit` + `build` + `npm test` (68/68 — เพิ่มชุด `latinScript` 5 เทสต์)
· ผู้ใช้รัน `0027` + `0028` + `backfill-place-ids` แล้ว (ยืนยันด้วย curl: `custom_places` มี
`google_place_id` ครบ 36/36 แถว · ตาราง `place_notes` สร้างแล้ว) · ทดสอบ flow โน้ตกับข้อมูลจริง
ครบ 6 ขั้นตามด้านบนแล้วคืนค่าเดิมเรียบร้อย
· ก่อนรัน migration ก็เปิดหน้าได้ปกติ (ตาราง 404 → ฟีเจอร์ปิดตัวเองเงียบๆ ตามที่ออกแบบ — ตรวจแล้ว)

---

## 4. สิ่งที่ต้องให้ผู้ใช้ทำเอง (ผมทำแทนไม่ได้)

> ### ✅ ปิดไปแล้ว (12 ส.ค. 2026)
>
> **1. รัน migration `0025` + `0026`** — ผู้ใช้รันเองแล้ว ยืนยันด้วย curl ว่าคอลัมน์ใหม่มีจริง
>
> **2. เติมชื่อ/ที่อยู่ท้องถิ่น + เบอร์โทรให้ที่พักแถวเก่า** — **ทำให้แล้วโดยไม่ต้องกดผ่าน UI**
> ตาราง `trip_hotels` ไม่ได้เก็บ `place_id` ไว้ จึงหา place ใหม่ด้วย `places:searchText` จากชื่อโรงแรม
> โดยจำกัดกรอบ 2 กม. รอบพิกัดที่บันทึกไว้เดิม แล้ว**ยืนยันว่าผลที่ได้ห่างจากพิกัดเดิมไม่เกิน 150 ม.
> ก่อนเชื่อว่าเป็นที่เดียวกัน** (ของจริงได้ 0 ม. ทั้ง 2 แถว = place เดียวกันแน่นอน) จากนั้นยิง Place Details
> ซ้ำภาษา `ko` + `en` เอา `displayName`/`formattedAddress`/`internationalPhoneNumber` มา PATCH
> เฉพาะ 5 คอลัมน์ใหม่ · `hotel_name`/`formatted_address`/`lat`/`lng` เดิม**ไม่ถูกแตะ** ยืนยันด้วย curl แล้ว
> - `d1` ปูซาน — 아르반호텔 · 대한민국 부산광역시 부산진구 중앙대로691번길 32 · +82 51-805-9901
> - `d4` ซกโช — Lecollective SokchoBeach · 대한민국 강원특별자치도 속초시 해오름로 201 · +82 70-8655-2966
>
> ⚠️ **มีที่พักแค่ 2 leg จาก 4 ที่ตั้งไว้ในระบบ** — leg โซล/คังนึงยังไม่มีแถวใน `trip_hotels` เลย
> พอตั้งที่พักใหม่ผ่าน UI ช่องพวกนี้จะถูกเติมให้เองตั้งแต่ครั้งแรก (เฟส 16 ทำ path นี้ไว้แล้ว)
> ไม่ต้องมาเติมย้อนหลังอีก
>
> **3. ตั้ง env `TRIP_PIN` + `TRIP_PIN_SECRET` บน Vercel** — ผู้ใช้ตั้งแล้ว
>
> ### 🔴 ค้างอยู่ตอนนี้ (12 ส.ค. 2026)
>
> **0. รัน migration `0027_custom_places_google_id.sql`** (บั๊ก 21.5 — ร้านที่เพิ่มเองชี้ผิดร้าน)
> ⚠️ **ต้องรันก่อน deploy โค้ดรอบนี้** เพราะตอนกดเพิ่มสถานที่ใหม่ระบบจะเขียนคอลัมน์ `google_place_id`
> ถ้าคอลัมน์ยังไม่มี insert จะพังทั้งแถว · รันเสร็จแล้วบอกผม ผมจะรัน `node scripts/backfill-place-ids.mjs --write`
> เติม place id ให้ 33 แถวเก่าให้เอง (รันโดยไม่ใส่ `--write` = ดูผลอย่างเดียว ไม่แตะ DB)
>
> **4. ทดสอบบนมือถือจริง (ผมทดสอบแทนไม่ได้)**
> - ปุ่ม Naver/Kakao บนเครื่องที่ลงแอปไว้ — `nmap://` ไม่คืน response ต้องเห็นแอปเปิดจริงถึงนับว่าผ่าน (ค้างจากเฟส 14)
> - หลัง deploy: เปิดเว็บ 1 ครั้งให้ service worker ติดตั้ง → กด "เพิ่มลงหน้าจอโฮม" → เปิด `/today`
>   กับ `/summary` คนละครั้งตอนมีเน็ต → แล้วลอง airplane mode ดูว่ายังเปิดได้
> - แทรกแถว "✈️ ไปสนามบิน" ที่วัน 21 ต.ค. ดูว่าแถบ "ควรออกไม่เกิน HH:MM" ขึ้นถูก (หลังรัน `0025`)
>
> **5. ตรวจเนื้อหาที่ผมกรอกไว้แทน**
> - คำแปลอังกฤษ + สั่งพิมพ์ `/summary?lang=en&for=immigration` จริงว่าลง A4 พอดีไหม
> - กรอกชื่อตามพาสปอร์ตในหน้า ตม. (เก็บในเครื่องนั้นเท่านั้น อีกเครื่องต้องกรอกเอง)
> - เบอร์สถานทูตในการ์ดฉุกเฉิน — ตรวจซ้ำจากเว็บทางการก่อนเดินทาง (เบอร์ชนิดที่เปลี่ยนได้)
> - ที่นั่ง/อาคารของ VN409 ที่อินชอน (T1 หรือ T2) — `data/transferPoints.ts` ใช้พิกัด T1 ไว้ก่อน
>
> **6. ✅ รัน migration `0027` + `0028` แล้ว (12 ส.ค. 2026)**
> ยืนยันด้วย curl: `custom_places.google_place_id` มีครบ **36/36 แถว** (รัน
> `scripts/backfill-place-ids.mjs` ด้วย) · ตาราง `place_notes` สร้างแล้วและทดสอบ flow เต็มวงจรผ่าน


| เมื่อไหร่ | ต้องทำอะไร |
|---|---|
| เฟส 0 | บอกว่ามี GitHub remote ไหม (ถ้ายังไม่มี ต้องสร้างเพื่อ push กันของหาย) |
| เฟส 1 | เปิด **Routes API** ใน Google Cloud Console |
| เฟส 3 | เปิด **Maps JavaScript API** ใน Google Cloud Console |
| ทุกเฟส | รันไฟล์ SQL migration ใหม่ใน Supabase Dashboard → SQL Editor (ผมรัน SQL ตรงๆ ไม่ได้) |
| เฟส 5 | สร้าง Storage bucket ใน Supabase (หรือรัน SQL ที่ผมเตรียมให้) |
| เฟส 6.9 | ✅ ลบ `visited_at` ของ `Mun Dining` (วัน `d0`) แล้ว (8 ส.ค. 2026 — รันเอง) |
| เฟส 7 | อนุมัติเพิ่ม devDependency `vitest` + `vite-tsconfig-paths` (เฉพาะตอน dev ไม่ขึ้น production bundle) |
| เฟส 7-12 | **ไม่มี migration ใหม่เลยทั้ง 6 เฟส** — ตั้งใจออกแบบทางแก้ให้เลี่ยง SQL ทั้งหมด (ดูเหตุผลที่เฟส 9.1) |
| เฟส 13 | ไม่มี — ไม่มี migration ไม่ต้องตั้งค่าอะไรเพิ่ม |
| เฟส 13.5 | **ตั้ง env `TRIP_PIN` และ `TRIP_PIN_SECRET` บน Vercel** (Settings → Environment Variables) แล้ว redeploy · ⚠️ ถ้าไม่ตั้ง production จะ**ไม่มี PIN ป้องกันเลย** เพราะ proxy ตั้งใจให้ปล่อยผ่านเมื่อไม่มี env (กันเว็บตายตอนอยู่เกาหลี) · ค่าทั้งสองอยู่ใน `.env.local` เครื่อง dev |
| เฟส 14 | รัน `0024_place_local_names.sql` + **ทดสอบปุ่ม Naver/Kakao บนมือถือที่ลงแอปไว้จริง** (ผมเช็กแทนไม่ได้ `nmap://` ไม่คืน response) |
| เฟส 15 | ✅ รัน `0025_trip_stops_transfer.sql` แล้ว (12 ส.ค. 2026) · เหลือลองแทรกแถว "✈️ ไปสนามบิน" จริงที่วัน 21 ต.ค. ดูว่าแถบ "ควรออกไม่เกิน HH:MM" ขึ้นถูก |
| เฟส 16 | ✅ รัน `0026_trip_hotels_local.sql` แล้ว + **เติมข้อมูลที่พักแถวเก่าให้แล้วผ่านสคริปต์** (ไม่ต้องกดบันทึกซ้ำใน UI — ดูวิธีที่หัวข้อ 4) · เหลือตรวจคำแปลอังกฤษ + ลองสั่งพิมพ์ `/summary?lang=en&for=immigration` จริงว่าลง A4 พอดีไหม |
| เฟส 19 | ไม่มี — ไม่มี migration ไม่ต้องตั้งค่าอะไรเพิ่ม |
| เฟส 17 | ไม่มี — ไม่มี migration และ Open-Meteo ไม่ต้องใช้ API key · (ถ้าอยากได้พยากรณ์อากาศตอนนี้เลย รอไม่ได้ — มันจะขึ้นเองเมื่อเหลือ ~16 วันก่อนวันนั้น) |
| บั๊ก 21.5 | ✅ รัน `0027_custom_places_google_id.sql` + `backfill-place-ids` แล้ว (12 ส.ค. 2026 — `google_place_id` ครบ 36/36 แถว) · ในไฟล์ยังมี SQL ลบการ์ดซ้ำ comment ไว้ให้เลือกรันเอง (ยังไม่ได้รัน) |
| เฟส 18 | **เปิดเว็บบนมือถือ 1 ครั้งหลัง deploy** ให้ service worker ติดตั้ง แล้วกด "เพิ่มลงหน้าจอโฮม" · **เปิด `/today` และ `/summary` อย่างน้อยคนละครั้งตอนมีเน็ต** ถึงจะเปิดได้ตอนเน็ตหลุด · แล้วทดสอบ offline จริง (airplane mode) — ไม่มี migration ใหม่ |
| เฟส 22 | ✅ รัน `0028_place_notes.sql` แล้ว (12 ส.ค. 2026) · ทดสอบ flow โน้ตกับข้อมูลจริงครบทุกเส้นทางแล้วคืนค่าเดิม |

---

## 5. กติกาการทำงานในโปรเจกต์นี้

- **คุยเป็นภาษาไทย** — โค้ด/ชื่อตัวแปร/ศัพท์เทคนิคเป็นอังกฤษได้ตามปกติ
- **Next.js เวอร์ชันนี้ต่างจากที่โมเดลเคยรู้จัก** — อ่าน `node_modules/next/dist/docs/` ก่อนใช้ API ที่ไม่แน่ใจ
- **ห้ามเรียก Google Maps API ตระกูล legacy** (`maps.googleapis.com/maps/api/*`) คีย์โปรเจกต์นี้ไม่ได้เปิดไว้ ใช้ตระกูลใหม่เท่านั้น (`places.googleapis.com`, `routes.googleapis.com`) และควรผ่าน helper ใน `lib/googlePlaces.ts`
- **migration ต้องให้ผู้ใช้ copy-paste รันเอง** แล้วยืนยันผลด้วย curl ก่อนเชื่อว่าฟีเจอร์ทำงาน อย่าเดาว่าเป็นบั๊กโค้ดก่อนเช็ก migration
- **ฐานข้อมูลนี้เป็นข้อมูลทริปจริงที่ใช้อยู่จริง 2 คน** — ตอนทดสอบใน browser ให้เลี่ยงการกดปุ่มที่แก้ข้อมูล ถ้าจำเป็นต้องแก้ ให้แก้กลับเป็นค่าเดิมเสมอ
- **ก่อนบอกว่าเสร็จ ต้อง `npm run lint` ผ่านเสมอ**
- **ปิดเฟสแล้วอัปเดตไฟล์นี้** ทุกครั้ง
