# แผนพัฒนา — เว็บแพลนเที่ยวเกาหลี

> ไฟล์นี้คือแหล่งความจริงเรื่อง "ตอนนี้ทำถึงไหน / ต่อไปทำอะไร / ทำไมตัดสินใจแบบนั้น"
> อ่านไฟล์นี้ก่อนเริ่มงานใหม่ทุกครั้ง และอัปเดตทุกครั้งที่ปิดเฟส
>
> ทริปจริง: **11–21 ต.ค. 2026** (บินไป 11 · เที่ยวเกาหลี 12–20 · บินกลับ 21) · แผนนี้เขียนเมื่อ **6 ส.ค. 2026** (เหลือ ~2 เดือน)

---

## 1. เว็บนี้ต้องทำอะไรได้ (เป้าหมาย)

เว็บตัวเดียวที่ทำงาน **2 โหมด** สำหรับคน 2 คน:

| โหมด | ใช้เมื่อไหร่ | ต้องทำอะไรได้ |
|---|---|---|
| **วางแผน** | ตอนนี้จนถึงก่อนบิน | เลือกที่เที่ยว/ร้านอาหาร จัดลำดับแต่ละวัน ดูว่าเวลาพอไหม เก็บตั๋ว/booking เตรียมของ |
| **ใช้จริง** | ระหว่างอยู่เกาหลี | เปิดมือถือแล้วรู้ทันทีว่าตอนนี้ต้องไปไหน นำทางได้เลย ดูตั๋วของวันนี้ได้ |

**เกณฑ์คุณภาพที่ตกลงกัน:** สวย น่าใช้งาน และใช้ได้ดีจริงทั้งบนมือถือและคอมพิวเตอร์

### ต้องมี (ตกลงแล้ว)
- เวลาเดินทางของจริง ไม่ใช่ตัวเลขเดา
- แผนที่รวมเส้นทางทั้งวัน
- ร้านอาหาร / การวางมื้อ
- เวลาเปิด-ปิดของสถานที่
- การเดินทางระหว่างเมือง (กรอกเองจากที่จองจริง)
- Checklist ของที่ต้องเตรียม
- เก็บ booking/ตั๋วไว้ที่เดียว **อัปโหลดรูป/PDF ได้**
- หน้าแยก "วันนี้" เน้นมือถือ สำหรับใช้ตอนเที่ยวจริง

### ไม่ทำ (ตัดสินใจแล้ว อย่าย้อนกลับมาทำโดยไม่คุยก่อน)
- **งบประมาณ/ค่าใช้จ่าย** — ไม่ต้องการ ตัดออกเพื่อคุม scope
- ~~**ระบบออฟไลน์ / PWA**~~ — **กลับคำแล้ว (11 ส.ค. 2026)** ผู้ใช้ขอให้ทำ · **เฟส 18 เสร็จแล้ว (12 ส.ค. 2026)**
  เหตุผลเดิมที่ตัดออก (มี eSIM ตลอดทริป) ยังจริงอยู่ แต่จุดที่เน็ตหลุดจริงคืออุโมงค์รถไฟใต้ดินโซล/ปูซาน
  และเขาอุทยานซอรัคซาน (`d5`) ซึ่งเป็นวันที่พึ่งหน้า `/today` มากที่สุด · **ขอบเขต: offline แบบอ่านอย่างเดียว**
  (ดูแผนที่วางไว้แล้วได้ตอนเน็ตหลุด) **ไม่ทำ offline editing** เพราะเว็บนี้ sync สด 2 คนและใครมีลิงก์ก็แก้ได้
  การแก้แบบ offline แล้ว sync กลับจะเปิดปัญหา conflict resolution ทั้งกอง ไม่คุ้มกับทริป 11 วัน
- **ระบบล็อกอินรายคน** — ยังไม่ทำ ใช้กัน 2 คนที่ไว้ใจกัน
  **แต่ตอนนี้มีด่าน PIN แล้ว (เฟส 13.5, 11 ส.ค. 2026)** — ยังเป็น "แชร์ลิงก์ + รหัสร่วมกัน" ไม่ใช่บัญชีรายคน
  · PIN เก็บใน env `TRIP_PIN` (ดู `.env.local` — ไม่ได้ commit) · ⚠️ ด่านนี้กันหน้าเว็บ+API เท่านั้น **ไม่ได้กัน Supabase**

---

## 2. สถานะปัจจุบัน (ตรวจจากโค้ดจริง 6 ส.ค. 2026)

**Stack:** Next.js 16 (App Router) + TypeScript + Tailwind v4 + Supabase (anon key ตรงจาก browser, RLS สาธารณะ) + Google Places API (New) ผ่าน API route ฝั่งเซิร์ฟเวอร์ + dnd-kit · deploy บน Vercel (`korea-trip-plan`)

### ที่ทำงานได้จริงแล้ว
- ระบบ **"แผน" หลายเวอร์ชัน** — สร้าง/ก๊อป/เปลี่ยนชื่อ/ลบ ทำแผน A/B เทียบกันได้ (`trip_plans` + `trip_meta`)
- **ที่พักต่อ leg** — จับวันที่นอนเมืองเดียวกันติดกันมัดเป็น leg อัตโนมัติจาก `data/itinerary.ts` ค้นหาโรงแรมจาก Google ได้
- **จุดแวะต่อวัน** — ลากจัดลำดับ / ลากข้ามวัน / ลากกลับคืนคลัง (`trip_stops`)
- **Timeline เวลาถึง-ออก** — คำนวณจากเวลาเริ่มวัน + เวลาเดินทาง + เวลาที่อยู่แต่ละจุด
- **คลังสถานที่** — 39 ที่คัดสรรใน `data/places.ts` + เพิ่มเองผ่าน Google Places + ซ่อนที่ไม่เอา + รูปจาก Google + แผนที่ฝัง + คลิป YouTube
- ตั๋ว/booking + checklist ของเตรียม + เดินทางระหว่างเมืองกรอกเอง (เฟส 5)
- **หน้า "วันนี้" (`/today`)** สำหรับใช้จริงตอนเที่ยว — จุดถัดไปเด่น + นำทาง 3 แอป + ติ๊กมาถึงแล้ว + เลื่อน timeline อัตโนมัติเมื่อช้ากว่าแผน (เฟส 6)
- **หน้าสรุปแผน (`/summary`)** — ดูอย่างเดียวทั้งทริป แก้อะไรไม่ได้ · **ล็อกวันที่ลงตัวแล้ว** กันเผลอลากตอนเลื่อนดู · แถบเมนูล่างบนมือถือสลับ 3 หน้า
- ทุกอย่าง **sync สดระหว่าง 2 คน** ผ่าน Supabase Realtime · `npm run lint` สะอาด

**ทั้ง 6 เฟสตามแผนเดิมเสร็จหมดแล้ว (7 ส.ค. 2026)** — เว็บใช้งานได้ครบตามเป้าหมายข้อ 1 ทุกข้อ เหลือแค่ปรับแต่งจากการใช้งานจริงเป็นเคสๆ ไป

### หนี้/ปัญหาที่ต้องเคลียร์
1. ~~เวลาเดินทางทุกตัวเป็นของเดา~~ — แก้แล้ว (เฟส 1) ขนส่งสาธารณะเป็นของจริงจาก Routes API, เดิน/แท็กซี่ยังเป็นประมาณการ (ดูข้อจำกัดด้านล่าง)
2. ~~งานทั้งหมดยังไม่ commit~~ — แก้แล้ว (เฟส 0)
3. ~~โค้ดตาย 5 ไฟล์~~ — ตรวจแล้ว (7 ส.ค. 2026) ไม่มีอยู่จริงในโค้ดแล้ว
4. ~~README ล้าสมัย~~ — ตรวจแล้ว (7 ส.ค. 2026) ไม่มีข้อความล้าสมัยแบบที่บันทึกไว้
5. ~~migration `0009_trip_stops_replica_identity.sql`~~ — รันแล้ว (7 ส.ค. 2026)
6. ~~migration `0010_travel_time_cache.sql`~~ — รันแล้ว ยืนยันด้วย curl (6 ส.ค. 2026)

**หนี้รอบใหม่ (จากการตรวจโค้ดทั้งโปรเจกต์ 7 ส.ค. 2026):** พบบั๊ก 14 ตัว + ปัญหา performance ที่ `lint`/`tsc` จับไม่ได้
เฟส 7-9 (แก้บั๊กเวลา/ลำดับจุดแวะ/แคช+performance ทั้งหมด) **แก้เสร็จแล้ว (7 ส.ค. 2026)** เหลือลบ test data
1 แถวใน DB ที่รอผู้ใช้อนุมัติ + เฟส 10-12 (backup/print/rate-limit และงานเสริม) ที่ยังไม่เริ่ม
→ รายละเอียดทั้งหมดอยู่ที่ **[หัวข้อ 3.5 — เฟส 7-12](#35-รอบตรวจโค้ดทั้งโปรเจกต์-7-สค-2026--เฟส-7-12)** ด้านล่าง

### ⚠️ ข้อจำกัดสำคัญ (ยืนยันแล้วด้วยการทดสอบจริง 6 ส.ค. 2026): Google นำทางในเกาหลีใต้ได้ไม่ครบ
กฎหมายเกาหลีห้ามส่งออกข้อมูลแผนที่ละเอียดไปเซิร์ฟเวอร์ต่างประเทศ Google จึงให้บริการได้แค่ **เส้นทางขนส่งสาธารณะ (TRANSIT)** ส่วน **DRIVE / WALK คืนค่าว่างจริงตามที่คาด** ในเกาหลี

**ผลกระทบ:** ปุ่มเลือกโหมด "เดิน 🚶 / แท็กซี่ 🚕" จะเห็นเวลาเป็นค่าประมาณการ (ติดป้าย "(ประมาณการ)" ใน UI) — **ตัดสินใจแล้ว (6 ส.ค. 2026): ไม่เพิ่ม ODsay/Kakao/Naver** เพราะสมัครยาก (ต้องยืนยันตัวตนแบบเกาหลี) ยอมรับสภาพนี้ไปก่อน ถ้าจำเป็นจริงๆ ค่อยกลับมาคุยใหม่ อย่าเพิ่มเองโดยไม่คุยก่อน

---

## 3. แผนงานเป็นเฟส

### เฟส 0 — กันของหาย + เคลียร์พื้น ✅ เสร็จ (7 ส.ค. 2026)
เล็กแต่สำคัญที่สุด เพราะตอนนี้งาน 2 วันเต็มยังไม่มี backup

- [x] `git add` ทั้งหมด + commit + push ขึ้น remote (`github.com/parkALoha/Plan-Korea`)
- [x] ลบโค้ดตาย 5 ไฟล์ (ดูรายการข้อ 3 ด้านบน)
- [x] เขียน `README.md` ใหม่ให้ตรงกับความจริง แล้วชี้มาที่ไฟล์นี้เป็นแหล่งแผนงาน
- [x] รัน migration `0009` แล้ว (แก้บั๊ก DELETE realtime ไม่ sync)

---

### เฟส 1 — เวลาเดินทางของจริง ✅ เสร็จ (6 ส.ค. 2026)
Timeline ทั้งทริปตอนนี้เชื่อไม่ได้ ทุกฟีเจอร์ที่พึ่งเวลา (เตือนสถานที่ปิด, หน้า "วันนี้") จึงต้องรอเฟสนี้

**ต้องให้ผู้ใช้ทำก่อน:** เปิด **Routes API** ใน Google Cloud Console (โปรเจกต์เดียวกับคีย์ปัจจุบัน)

- [x] ยิงทดสอบจริง 3 โหมด × คู่จุดจริงในทริป (ในปูซาน) 6 ส.ค. 2026: **ขนส่งสาธารณะ = ของจริง** (มิเป่→หาดแฮอึนแด 41 นาที ยืนยันด้วย curl ตรงไปที่ `travel_time_cache`) · **เดิน/แท็กซี่ = ว่าง** (`isReal:false`) ตรงกับที่คาดไว้เรื่องข้อจำกัดกฎหมายแผนที่เกาหลี
- [x] `lib/travelProvider.ts` — interface เดียวสำหรับคำนวณเวลาเดินทาง สลับ backend ได้
- [x] เขียน `/api/travel-time` ใหม่ด้วย `routes.googleapis.com/directions/v2:computeRoutes` — ลบ route เดิมที่เรียก legacy API
- [x] แคชผลถาวรใน Supabase (ตาราง `travel_time_cache`, migration `0010`) — ยืนยันแล้วว่าเขียน/อ่านได้จริง
- [x] **UI แยกให้ชัดว่าตัวเลขไหนของจริง ตัวเลขไหนประมาณการ** — `DayStopsSection` ติดป้าย "(จริง)" / "(ประมาณการ)" แล้ว
- [x] **ตัดสินใจแล้ว (6 ส.ค. 2026): ไม่เพิ่ม ODsay/Kakao/Naver ตอนนี้** — สมัครยาก (ต้องยืนยันตัวตนแบบเกาหลี) ยอมรับว่าเดิน/แท็กซี่เป็นประมาณการไปก่อน ถ้าจำเป็นจริงๆ ค่อยกลับมาคุยใหม่ อย่าเพิ่มเองโดยไม่คุยก่อน

**เฟส 1 เสร็จแล้ว** ✅

---

### เฟส 2 — ร้านอาหาร + เวลาเปิด-ปิด ✅ เสร็จ (7 ส.ค. 2026)
ใช้ Places API (New) ที่เปิดอยู่แล้ว ไม่ต้องเปิด API เพิ่ม

- [x] ตารางใหม่ `place_details_cache` (migration `0011`) — resolve แต่ละสถานที่เป็น Google place ID ครั้งเดียว เก็บพร้อม `regularOpeningHours` ผ่าน `/api/place-details`
- [x] แสดงเวลาเปิด-ปิดบนการ์ดสถานที่ (`PlaceCard` — เวลาของวันที่โฟกัสอยู่จริงๆ) และในหน้ารายละเอียด (`PlaceDetailModal` — ลิสต์ครบ 7 วัน)
- [x] **เตือนใน timeline ถ้าเวลาที่คำนวณได้ตกนอกเวลาเปิด** — `lib/openingHours.ts` (`isOpenDuring`) + `DayStopsSection` แสดงป้ายเตือนใต้แถว ทดสอบจริงแล้วเห็นป้ายเตือนทำงานถูกต้อง
- [x] เพิ่มหมวด "ร้านอาหาร" ในคลัง — ปุ่ม "🍽️ ร้านใกล้ๆ" เปิด `NearbyRestaurantsModal` ยิง Nearby Search (New) รอบจุดแวะล่าสุดของวันนั้น (หรือที่พัก/จุดกึ่งกลางเมืองถ้ายังไม่มีจุดแวะ) ทดสอบจริงแล้วเพิ่มร้านลงวันได้
- [x] **วางมื้ออาหาร** — ใช้ `trip_stops` เดิม เพิ่มหมวด `restaurant` แล้ว หัวการ์ดแต่ละวันโชว์ "🍽️ วางมื้อไว้ N มื้อ" หรือเตือนถ้ายังไม่มี

migration `0011_place_details_cache.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `place_details_cache` (7 ส.ค. 2026)

**ส่วนเสริมหลังปิดเฟส (7 ส.ค. 2026) — ตามฟีดแบ็กจากการใช้งานจริง:**
- [x] ร้านอาหารโชว์เรทติ้ง/จำนวนรีวิว/ประเภทอาหาร + กดเข้าไปดูรีวิว 3 อันล่าสุด/รูปภาพ/เวลาเปิด-ปิดครบก่อนตัดสินใจเพิ่มได้ (migration `0012` เพิ่มคอลัมน์ `rating`, `user_rating_count`, `primary_type`, `reviews` ใน `place_details_cache`)
- [x] "+ เพิ่มสถานที่เอง" มี autocomplete แนะนำระหว่างพิมพ์แล้ว (เหมือนช่องค้นหาที่พัก) ไม่ต้องกดค้นหาแล้วเลือกจากลิสต์เท่านั้น
- [x] มีช่องค้นหาร้านด้วยชื่อใน `NearbyRestaurantsModal` เผื่อร้านอยู่นอกรัศมี Nearby Search หรือรู้ชื่อเป๊ะๆ อยู่แล้ว
- [x] โน้ตสั้นๆ ต่อจุดแวะ (migration `0013` เพิ่มคอลัมน์ `trip_stops.note`) — กด "+ โน้ต" ใต้แต่ละจุดเพื่อจดข้อความอิสระ เช่น "ร้านนี้อร่อย รีบไป"

migration `0012_place_details_extra.sql` และ `0013_trip_stop_notes.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `place_details_cache`/`trip_stops` (7 ส.ค. 2026)

---

### ส่วนเสริม — ตารางบินจริง + วันเดินทาง + คืนที่ยังเลือกเมืองได้ ✅ เสร็จ (7 ส.ค. 2026)

เดิมแพลนเริ่มที่วันแรกในปูซานเลย ไม่มีวันบิน ทำให้ไม่รู้ว่าต้องออกจากที่ไหนกี่โมง และช่วงพักเครื่องฮานอย 11 ชม. หายไปทั้งก้อน

- [x] `Day.events` (`DayEvent[]`) ใน `data/itinerary.ts` — เที่ยวบิน/เดดไลน์ที่จองมาแล้ว แก้ในเว็บไม่ได้ แสดงเป็นแถบแยกเหนือจุดแวะ (`DayEventsPanel` ใน `DayStopsSection`) ไม่ปนกับจุดแวะที่ลากจัดลำดับได้
- [x] **วัน `d0` (11 ต.ค.)** — VN610 สุวรรณภูมิ 11:55 → ฮานอย 13:55 · พักเครื่อง 11 ชม. 20 น. · VN428 ฮานอย 01:15 → กิมแฮ 07:05 · พร้อมเดดไลน์ "ออกจากเมืองเก่า 22:00"
- [x] **วัน `d10` (21 ต.ค.)** — เช็คเอาต์ 05:45 · AREX → ICN · VN409 อินชอน 10:35 → โฮจิมินห์ 13:45 · ต่อเครื่อง 3 ชม. 5 น. · VN607 → สุวรรณภูมิ 18:30
- [x] เมืองใหม่ **ฮานอย** ในคลังสถานที่ (7 ที่ในย่านเมืองเก่า เดินถึงกันหมด) — ใช้ระบบเดิมทั้งหมด (เวลาเปิด-ปิด/ร้านใกล้ๆ/เพิ่มเอง ใช้ได้ปกติ)
- [x] `Day.noHotel` — วันบิน/นอนบนเครื่องไม่ถูกนับเป็น leg ที่พัก (`deriveHotelLegs` ข้ามให้) leg เดิมทั้ง 4 ก้อนและโรงแรมที่ตั้งไว้แล้วไม่กระทบ
- [x] `Day.overnightOptions` + `trip_meta.overnight_overrides` (migration `0014`) — **คืน 16 ต.ค. สลับระหว่างคังนึง (ที่จองไว้จริง) กับซกโชได้จากหัวการ์ดวันนั้น** สลับแล้ว leg ที่พักคำนวณใหม่ให้เอง sync สดทั้ง 2 คน · ของเดิมไม่ถูกลบ สลับกลับได้ตลอด · migration รันแล้ว (7 ส.ค. 2026)
- [x] **`DayEvent.anchor` ("before"/"after")** — วันที่มีช่วงว่างจริงระหว่างเที่ยวบิน (เช่นพักเครื่องฮานอย) แทรกจุดแวะเข้าไปในช่วงนั้นได้เหมือนวันปกติทุกอย่าง เวลาบินเองแก้ไม่ได้ แต่ตารางจุดแวะจะ**เริ่มนับอัตโนมัติจากเวลาที่เหตุการณ์ anchor "before" จบ** (เช่น ถึงย่านเมืองเก่า 15:30) แทนช่อง "ออกเดินทาง" ที่ตั้งเองได้ปกติ และมีป้ายเตือนถ้าจุดแวะที่วางไว้จบช้ากว่าเดดไลน์ anchor "after" (เช่น ต้องออกไปขึ้นเครื่อง 22:00) — วัน `d0` ใช้ฟีเจอร์นี้กับช่วงพักเครื่องฮานอย ทดสอบเพิ่ม/ลบจุดแวะจริงในเบราว์เซอร์แล้ว

**ผู้ใช้ต้องทำ:** ไม่มี — migration `0014` รันแล้ว

---

### เฟส 3 — แผนที่รวมเส้นทางทั้งวัน ✅ เสร็จ (7 ส.ค. 2026)
ผู้ใช้เปิด **Maps JavaScript API** บนคีย์เดิม `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` ใน Google Cloud Console แล้ว ยืนยันด้วยการทดสอบจริงในเบราว์เซอร์ — เห็นแผนที่ Busan จริงพร้อมหมุดเลข 1-2-3 เรียงตามลำดับจุดแวะถูกต้อง

- [x] แผนที่ต่อวัน: หมุดเลข 1-2-3 ตามลำดับจุดแวะ + เส้นเชื่อม + หมุดโรงแรมคนละสี — `components/DayMapPanel.tsx` ใช้ `@vis.gl/react-google-maps` (`Marker` ทรงกลมสี + label ตัวเลข/🏨 + `Polyline`) มี fallback ข้อความเมื่อไม่มีคีย์ **หมายเหตุ:** ตอนแรกใช้ `AdvancedMarker`+`Pin`+`mapId="DEMO_MAP_ID"` แต่เจอ `ApiProjectMapError` เมื่อมีหลายแผนที่พร้อมกันบนหน้าเดียว (DEMO_MAP_ID ไม่รองรับการใช้งานที่ scale หลาย instance) เปลี่ยนมาใช้ `Marker` แบบคลาสสิกกับ `google.maps.SymbolPath.CIRCLE` แทน ไม่ต้องพึ่ง Map ID เลย แก้ปัญหาได้
- [x] กดหมุด → เลื่อนไปแถวนั้นในลิสต์ / กดแถว → หมุดเด้ง (สองทาง) — `activeStopId` ใน `DayStopsSection` ผูกทั้งสองทาง ทดสอบฝั่งกดชื่อในลิสต์แล้วไฮไลต์ ring ติดแถวถูกต้อง (ฝั่งกดหมุดใช้ handler เดียวกัน `onSelectStop`)
- [x] มือถือ: แท็บสลับ "ลิสต์ / แผนที่" · คอม: วางคู่กันซ้าย-ขวา — ทดสอบแล้วทั้งจอมือถือ (375px) และจอใหญ่ (1400px)
- [x] จำกัด key ด้วย HTTP referrer ให้เฉพาะโดเมน Vercel + localhost — ผู้ใช้ตั้งค่าใน Google Cloud Console แล้ว

---

### เฟส 4 — รื้อ UI ให้สวยและ mobile-first ✅ เสร็จเกือบหมด (7 ส.ค. 2026)
เดิมทั้งเว็บอยู่ใน `app/page.tsx` ไฟล์เดียว 561 บรรทัด และเป็น desktop-first (มือถือใช้ได้แต่ไม่ได้ถูกออกแบบมา) — ตอนนี้แตกโครงแล้ว, มือถือมี bottom sheet, คอมใช้พื้นที่จอกว้างขึ้น, มี skeleton ตอนโหลด, ปุ่มมี feedback เหลือแค่ "การเปลี่ยนหน้าลื่น" ที่ตั้งใจข้ามไปก่อนเพราะยังไม่มี route ให้เปลี่ยนจริง (รอเฟส 6)

**รอบแก้ของพัง (7 ส.ค. 2026) — ตรวจของจริงในเบราว์เซอร์ที่ 360 / 375 / 768 / 1350px:**
- [x] **ทั้งเว็บล้นออกนอกจอมือถือ (กว้าง 559px บนจอ 375px เลื่อนซ้ายขวาได้)** — ต้นเหตุคือการ์ดที่พังใน `HotelLegsPanel` เป็น `flex flex-wrap` ซึ่ง flex item กว้างตามเนื้อหา `truncate` ข้างในเลยไม่มีผล ชื่อโรงแรมจาก Google ที่พ่วงที่อยู่เต็มมาด้วยจึงดันหน้าเว็บ · แก้เป็น `grid` (1 คอลัมน์มือถือ / 2 คอลัมน์ ≥`sm`) + `min-w-0` → ล้นแนวนอนเหลือ 0 ทุกความกว้าง
- [x] **ชื่อสถานที่ในแถวจุดแวะเหลือ 74px จาก 341px จนอ่านไม่ออก** — แถวเดียวยัด 5 อย่าง (จับลาก/เวลา/ชื่อ/ปรับเวลา/ลบ) · มือถือยกปุ่มปรับเวลา+ลบลงไปอยู่แถวโน้ตที่มีอยู่แล้ว (ไม่เพิ่มความสูง) → **ชื่อได้ 225px** · จอ `sm` ขึ้นไปยังเป็นแถวเดียวเหมือนเดิม
- [x] **ปุ่ม +/− เวลาที่อยู่เล็กเกินนิ้ว** — 20×18 → **36×36** บนมือถือ (คอม 28×28) พร้อมกวาดปุ่มเล็กอื่นทั้งเว็บ: เลือกโหมดเดินทาง 23→31px · ชื่อสถานที่ 24→36px · "+ แทรกร้านอาหาร" 17→33px · เลือกเมืองนอน 24→30px → **ปุ่มที่เล็กกว่า 28px เหลือ 0**
- [x] ช่องพิมพ์โน้ตแคบจนพิมพ์ไม่ได้จริง — มือถือให้ช่องพิมพ์กินเต็มบรรทัด ปุ่มบันทึก/ยกเลิก/ลบ ตกลงบรรทัดล่าง
- [x] ปุ่มลอย "📍 สถานที่" ทับปุ่ม "+ เพิ่มสถานที่" ของวันสุดท้าย — เพิ่ม `pb-28` ให้เนื้อหา + ปุ่มลอยเผื่อ `env(safe-area-inset-bottom)` ของ iPhone
- [x] ที่อยู่โรงแรมกิน 2-3 บรรทัดในหัวการ์ดวัน — ตัดเหลือบรรทัดเดียว

**เหลือทำ (งานรื้อโครง):**
- [x] **แตก `app/page.tsx` เป็นโครงที่อ่านออก** (7 ส.ค. 2026) — ลดจาก 561 → 356 บรรทัด แยกออกเป็น: `components/TripHeader.tsx` (ส่วนหัว + ตัวสลับแผน), `hooks/useLegacyBootstrap.ts` (bootstrap จากระบบ slot เดิม), `hooks/useHotelSchedule.ts` (hotelLegs/hotelForDay/hotelBeforeDay), `hooks/useTripDnd.ts` (sensors + drag handlers ทั้งหมด) — ย้ายโค้ดตรงๆ ไม่เปลี่ยนพฤติกรรม ผ่าน `npm run lint` + `tsc --noEmit` สะอาด ทดสอบจริงในเบราว์เซอร์แล้ว (header/plan switcher/sidebar/drag ทำงานปกติ)
- [x] **มือถือ: คลังสถานที่เป็น bottom sheet แทน overlay เต็มจอ** (7 ส.ค. 2026) — `components/PlaceSidebar.tsx` เปลี่ยน `MobileOverlay` เป็น `BottomSheet`: เลื่อนขึ้นจากขอบล่าง (max 85vh) มีฉากหลังจางๆ ให้เห็นบริบทหน้าเดิม + หัวชีตโชว์เมือง/วันที่ที่กำลังเพิ่มให้ตลอด (แก้ปัญหาเดิมที่เสียบริบท) แตะฉากหลังหรือปุ่ม ✕ เพื่อปิด แอนิเมชัน slide-up ใน `app/globals.css` ผ่าน lint + typecheck สะอาด ทดสอบจริงที่ 375px แล้ว (เปิดชีตเห็น context ถูกต้อง)
- [x] **คอม: ใช้พื้นที่จอกว้างให้คุ้ม** (7 ส.ค. 2026) — ต้นเหตุคือ container กว้าง `max-w-5xl` (1024px) แต่คอลัมน์จุดแวะข้างในยังโดน `max-w-2xl` (672px) ตรึงซ้อนอยู่อีกชั้น ทำให้แผนที่ต่อวัน+ลิสต์บีบเหลือ ~372px ทั้งที่จอเหลือพื้นที่เยอะ — ขยาย container เป็น `lg:max-w-7xl` (1280px) และปลด `max-w-2xl` ออกจากคอลัมน์เนื้อหาที่ `lg:` (`lg:max-w-none`) ให้ `flex-1` ทำงานจริง วัดจริงในเบราว์เซอร์ที่จอ 1600px: คอลัมน์จุดแวะกว้างขึ้นจาก ~344px → 904px หัวเว็บ (`TripHeader`) ขยายกว้างตามด้วยเพื่อให้ขอบซ้ายชิดกับเนื้อหาด้านล่าง ผ่าน lint + typecheck สะอาด
- [x] **ระบบสี/ระยะห่าง/ขนาดตัวอักษร — ตรวจแล้ว ไม่ต้องแก้** (7 ส.ค. 2026) — ไล่ดู `text-*` ทั้งเว็บ (67× `text-xs`, 45× `text-sm`, 19× `text-[11px]`, 10× `text-[10px]`) ตัวเลข px ที่ดูเหมือนสุ่มจริงๆ คือ tier ขนาดที่ตั้งใจ (11px = meta text รอง, 10px = ป้ายจิ๋วบนแผนที่/badge) ใช้สม่ำเสมอทั้งเว็บอยู่แล้ว ไม่ใช่ความไม่สอดคล้องจริง — สีก็ยึด `@theme` เดิมทั้งหมดไม่มีสีหลุด ไม่พบจุดที่ต้องแก้จริง (ตรงกับที่ PLAN.md บันทึกไว้แต่แรกว่าธีมดีอยู่แล้ว)
- [x] **สถานะกำลังโหลดให้ดูดี** (7 ส.ค. 2026) — เพิ่ม `components/DayCardSkeleton.tsx` (โครงการ์ดวัน ทรงเดียวกับ `DayStopsSection` จริง + `animate-pulse`) แทนข้อความเปล่า "กำลังโหลด..." ใน `app/page.tsx` ตอนโหลดครั้งแรก และแก้ `PhotoGallery.tsx` ให้โชว์โครง grid รูปแทนข้อความ "กำลังโหลดรูป..." ผ่าน lint + typecheck สะอาด (สถานะว่างเปล่าอื่นๆ อย่าง "ยังไม่มีจุดแวะ" / "เลือกครบทุกที่ในโซนนี้แล้ว 🎉" มีข้อความ+อีโมจิที่ช่วยนำทางอยู่แล้ว ไม่ต้องแก้)
- [x] **ปุ่มกดแล้วมีการตอบสนอง** (7 ส.ค. 2026) — เพิ่มกฎ CSS ทั่วเว็บใน `app/globals.css` (`button:active { opacity: 0.7 }`) ครอบคลุมทุกปุ่มโดยไม่ต้องไล่แก้ทีละไฟล์ ใช้ opacity ไม่ใช่ scale เพราะที่จับลาก (`⠿`) ก็เป็น `<button>` เหมือนกัน scale จะไปกวน bounding box ตอน dnd-kit เริ่มวัดระยะลาก ผ่าน lint + typecheck สะอาด
- [x] **การเปลี่ยนหน้าลื่น** (7 ส.ค. 2026) — ตอนนี้มี route ที่สองแล้ว (`/today` จากเฟส 6) ใช้ `next/link` มาตรฐานทั้งสองทิศทาง ("📍 วันนี้" ใน `TripHeader` ↔ "← หน้าแผน" ใน `/today`) Next.js prefetch + client-side transition ให้อัตโนมัติ ไม่ต้องเขียนอะไรเพิ่ม ทดสอบจริงแล้วเปลี่ยนหน้าลื่น ไม่มี full page reload
- [x] **ไม่มีจอกระพริบตอนโหลด — ตรวจแล้ว ไม่มีปัญหา** (7 ส.ค. 2026) — ใช้ `next/font/google` (`Noto_Sans_Thai` ใน `app/layout.tsx`) ซึ่ง self-host ฟอนต์ + จัดการ `font-display` ให้อัตโนมัติอยู่แล้ว ไม่พบ FOUC จริง

---

### เฟส 5 — ตั๋ว/booking + checklist + เดินทางระหว่างเมือง ✅

- [x] **ตาราง `bookings`** (7 ส.ค. 2026) — migration `0016_bookings.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `bookings` · ประเภท (เที่ยวบิน/ที่พัก/KTX/บัส/ตั๋วเข้าชม/อื่นๆ), ผูกกับวันได้ (ไม่บังคับ), วันที่/เวลา, เลขที่จอง, ลิงก์, โน้ต · UI ใหม่ `components/BookingsPanel.tsx` + `components/BookingEditModal.tsx` (เพิ่ม/แก้/ลบ) แสดงอยู่ใต้แผงที่พักในหน้าแรก · hook `hooks/useBookings.ts` sync สดผ่าน Supabase Realtime เหมือนที่พัก · ทดสอบเพิ่ม/แก้/ลบจริงในเบราว์เซอร์แล้ว ผ่าน lint + typecheck สะอาด
- [x] **ไฟล์แนบตั๋ว (รูป/PDF)** (7 ส.ค. 2026) — bucket `booking-files` สร้างใน Supabase Dashboard (Public) + migration `0019_bookings_file.sql` เพิ่มคอลัมน์ `file_url`/`file_name` ใน `bookings` และ policy อ่าน/เขียน/ลบสาธารณะบน `storage.objects` (ต้องตั้งแยกจากตารางปกติ เพราะ Public bucket แค่เปิดอ่าน ไม่ได้เปิดเขียนให้อัตโนมัติ) · `BookingEditModal.tsx` เพิ่มช่องอัปโหลดไฟล์ (รูป/PDF) อัปโหลดตรงไป Supabase Storage แล้วเก็บ public URL, แสดงเป็นลิงก์เปิดไฟล์ + ลบไฟล์ได้ · `BookingsPanel.tsx` โชว์ไอคอน 📎 เมื่อมีไฟล์แนบ · ยืนยัน bucket/policy ด้วย curl จริง (upload/read/delete ผ่านหมด HTTP 200) — **หมายเหตุ:** การอัปโหลดไฟล์จริงผ่าน UI ยังไม่ได้ทดสอบในเบราว์เซอร์ (เครื่องมือ automation ตอนนี้ set ค่า `<input type=file>` ไม่ได้) แต่ path อัปโหลด/ดึง public URL เดียวกับที่ curl ยืนยันแล้วว่าใช้ได้จริง
- [x] **ตาราง `checklist_items`** (7 ส.ค. 2026) — migration `0018_checklist_items.sql` รันแล้ว ยืนยันด้วย curl ตรงไปที่ `checklist_items` · ข้อความ, ติ๊กแล้วหรือยัง, ใครติ๊ก (`checked_by`) · UI ใหม่ `components/ChecklistPanel.tsx` แสดงอยู่ใต้แผง booking ในหน้าแรก เพิ่ม/ติ๊ก/ลบได้ ติ๊กแล้วเลื่อนไปท้ายลิสต์ + ขีดฆ่า · hook `hooks/useChecklist.ts` sync สดผ่าน Supabase Realtime แบบเดียวกับ bookings · ผ่าน lint + typecheck สะอาด ทดสอบเพิ่ม/ติ๊ก/ลบจริงในเบราว์เซอร์แล้ว
- [x] **เดินทางระหว่างเมืองกรอกเอง** (7 ส.ค. 2026) — migration `0017_trip_stops_intercity.sql` รันแล้ว ยืนยันด้วย curl · เพิ่มคอลัมน์ `kind`/`intercity_from`/`intercity_to`/`intercity_mode` ใน `trip_stops` เดิม แถวเดินทางข้ามเมือง (kind="intercity") แทรกกลางลิสต์จุดแวะได้เลยผ่านปุ่ม "+ แทรกเดินทางข้ามเมืองตรงนี้" (คู่กับปุ่มแทรกร้านอาหารเดิมทุกตำแหน่ง) เปิด `IntercityEditModal` เลือกพาหนะ (บัส/KTX/อื่นๆ) + จาก/ไป + ระยะเวลา (มี preset ชม.) · **กินเวลาใน timeline จริง** โดยไม่ต้องแก้ `lib/schedule.ts` เลย — ใช้ฟิลด์ `dwell_minutes` เดิมเก็บระยะเวลาเดินทาง, `place_id` ว่างเปล่าทำให้จุดแวะก่อน/หลังไม่ถูกคำนวณระยะทางข้ามเมืองมั่วๆ (จุดหลังเริ่มนับเวลาใหม่จากตอนถึงปลายทางอัตโนมัติ) · ลาก/ลบ/ปรับเวลาใช้ระบบเดิมของ `trip_stops` ได้ฟรีทั้งหมด · แก้ `RouteSuggestionModal.tsx` ให้ตำแหน่งแถวเดินทางข้ามเมือง **ปักหมุดอยู่ที่เดิมเสมอ** ตอนกด "ลองจัดเส้นทางใหม่" (ของเดิมจะดันไปท้ายวันผิดๆ) · ทดสอบจริงในเบราว์เซอร์แล้ว (แทรกกลางวัน, timeline คำนวณใหม่ถูกต้อง, route suggestion ไม่ย้ายตำแหน่ง, ลบแล้วกลับสภาพเดิม) ผ่าน lint + typecheck สะอาด

---

### เฟส 6 — หน้า "วันนี้" (โหมดใช้จริงตอนเที่ยว) ✅ เสร็จ (7 ส.ค. 2026)
เฟสสุดท้ายเพราะพึ่งทุกเฟสก่อนหน้า แต่นี่คือเฟสที่ทำให้เว็บนี้มีค่าตอนอยู่เกาหลีจริง

**เตรียมพื้นฐานก่อนแตะ UI ใหม่:**
- [x] แยก `hooks/useDaySchedule.ts` ออกจาก `DayStopsSection.tsx` (เดิมฝังในนั้นล้วนๆ) — ย้ายโค้ดตรงๆ ไม่เปลี่ยนพฤติกรรม เพื่อให้หน้าแผนกับหน้า "วันนี้" ใช้ตรรกะคำนวณ timeline เดียวกันเป๊ะ (เวลาถึง-ออก, เวลาจริงจาก Google, เตือนสถานที่ปิด, ที่พักหัว-ท้ายวัน) ไม่เสี่ยงเวลาสองหน้าไม่ตรงกัน ทดสอบหน้าแผนเดิมในเบราว์เซอร์แล้วทำงานเหมือนเดิมทุกอย่าง

- [x] route ใหม่ `app/today/page.tsx` — ออกแบบสำหรับ **มือถือถือเดินอย่างเดียว** ตัวหนังสือใหญ่ ปุ่มใหญ่ กดง่ายด้วยมือเดียว (ปุ่มทุกปุ่ม ≥44px) · ยึด **วันจริงตามนาฬิกาเครื่อง** เป็นค่าเริ่มต้น (ก่อนทริปคืนวันแรก/หลังทริปคืนวันสุดท้าย) + สลับดูวันอื่นได้อิสระด้วยปุ่ม ‹ › และปุ่ม "กลับไปวันนี้จริง" — ตัดสินใจไว้ตอนวางแผน: ใช้เทสต์ได้ตอนนี้ (ส.ค.) และตอนเที่ยวจริงก็ใช้ดูแผนวันถัดไปได้
- [x] **จุดถัดไปเด่นที่สุดบนหน้าจอ** (การ์ดขอบส้มเด่น) **+ ปุ่มนำทางครบ 3 แอปเสมอ** (`lib/mapLinks.ts`) — Google (ลิงก์เว็บมาตรฐาน `maps.google.com/maps/dir` ใช้ได้ทุกที่รวมฮานอย) + Kakao (`map.kakao.com/link/to` เอกสารทางการ ไม่ต้องมี API key) + Naver (`nmap://` app scheme พร้อม fallback ไปหน้าค้นหาเว็บถ้าไม่มีแอป) — ตัดสินใจให้ครบ 3 เพราะเฟส 1 พิสูจน์แล้วว่า Google เดิน/ขับในเกาหลีคืนค่าว่าง คนเกาหลีใช้ Naver/Kakao กัน แต่ฮานอยกลับ Google ดีสุด ให้ผู้ใช้เลือกเองหน้างาน
- [x] แสดงเวลาเปิด-ปิดของจุดถัดไป (เตือนถ้าอาจปิดแล้ว ใช้ตรรกะเดียวกับหน้าแผน) + **ตั๋ว/booking ของวันนี้** (กรองจาก `bookings` ตาม `day_id`/วันที่)
- [x] ติ๊ก "มาถึงแล้ว" ได้ — migration `0020_trip_stops_visited.sql` เพิ่มคอลัมน์ `visited_at` ใน `trip_stops` รันแล้ว ยืนยันด้วย curl · `markVisited`/`unmarkVisited` ใน `useStops` · แตะแถวใน "ผ่านมาแล้ว" เพื่อยกเลิกติ๊กได้ (เผื่อกดพลาด)
- [x] **ถ้าออกช้ากว่าแผน → เลื่อน timeline ที่เหลือของวันให้ดูอัตโนมัติ** (`lib/liveDelay.ts`) — เทียบเวลาจริงที่ติ๊ก "มาถึงแล้ว" ล่าสุดกับแผน (แม่นสุด) หรือถ้ายังไม่ติ๊กจุดไหนเลยเทียบเวลาปัจจุบันกับจุดแรก · **แสดงผลอย่างเดียว ไม่เขียนกลับ DB เด็ดขาด** (ตัดสินใจไว้ตอนวางแผน) · เช็กเวลาเปิด-ปิดซ้ำด้วยเวลาที่เลื่อนแล้ว ขึ้นป้าย ⚠️ ถ้าจุดไหนอาจไปไม่ทันเวลาปิดเพราะความล่าช้า
- [x] ทั้ง 2 คนเห็นสถานะเดียวกันสด — ใช้ระบบ realtime เดิมของ `trip_stops` (ไม่ต้องเพิ่ม channel ใหม่)
- [x] ลิงก์สลับหน้าแผน ↔ วันนี้ — "📍 วันนี้" ใน `TripHeader` และ "← หน้าแผน" ใน `/today`

ทดสอบจริงในเบราว์เซอร์ทั้งหมด: สลับวัน, ป้ายเตือนสถานที่ปิดจริง (Gamcheon Village), ลิงก์นำทาง 3 แอปครบถูกต้อง, ติ๊ก/ยกเลิกติ๊ก "มาถึงแล้ว" round-trip, แถบเลื่อนเวลาเมื่อช้ากว่าแผน (เทียบกับตรวจซ้ำผ่าน curl ว่า state จริงใน DB สะอาด ไม่เหลือข้อมูลทดสอบค้าง) ผ่าน `npm run lint` + `tsc --noEmit` สะอาด

---

### รอบแก้จากการใช้งานจริงบนมือถือ ✅ เสร็จ (7 ส.ค. 2026)
ฟีดแบ็ก 6 ข้อหลังลองใช้จริงบนมือถือ:

- [x] **คลังสถานที่บนมือถือเลื่อนดูไม่ได้** — เจอ 2 สาเหตุซ้อนกัน (1) `dnd-kit` ใส่ `touch-action:none` บนการ์ดทุกใบ นิ้วที่ปัดเลยถูกตีความเป็น "เริ่มลาก" → ตัด `useDraggable` ออกทั้งหมดในชีตมือถือ (`draggable` prop ใน `PlaceSidebar`, จอใหญ่ยังลากได้ตามเดิม) เพราะมือถือมีปุ่ม "+ เพิ่มลงวันนี้" บนการ์ดอยู่แล้ว (2) กล่องคลังใน bottom sheet สูงตามเนื้อหา 3044px ทะลุกรอบชีต 615px แล้วโดน `overflow-hidden` ตัดทิ้ง (`h-full` คำนวณไม่ได้เพราะความสูงพ่อมาจาก flex) → เปลี่ยนเป็น `min-h-0 flex-1` ตลอดสาย · วัดยืนยันในเบราว์เซอร์แล้ว: ช่องเลื่อน 487px เลื่อนถึงท้ายสุดได้จริง
- [x] **วันบิน (11 และ 21 ต.ค.) ใช้ได้เหมือนวันอื่นแล้ว** — เดิมวันที่มี `anchor:"before"` ถูกบังคับเวลาเริ่มตายตัว ตั้งเองไม่ได้ ตอนนี้ตั้งเวลา "ออกเดินทาง" ได้ทุกวัน โดยเวลาจากตารางบินเป็นแค่**ค่าเริ่มต้นที่แนะนำ** (มีปุ่ม "กลับไปใช้เวลาแนะนำ" ถ้าปรับแล้วอยากคืนค่า) — เผื่อกรณีผ่าน ตม./รับกระเป๋าเร็วหรือช้ากว่าที่เผื่อไว้ · ตัว `DayEvent` (ตารางบิน/เดดไลน์) ยังล็อกอ่านอย่างเดียวเหมือนเดิม ติดป้าย "🔒 ตั๋วจองแล้ว แก้ไม่ได้" ให้ชัด
- [x] **ล็อกวันที่ลงตัวแล้ว** — migration `0021_day_lock.sql` (คอลัมน์ `is_locked` ใน `trip_day_settings`) · ปุ่มล็อกรายวันบนหัวการ์ด + ปุ่ม "🔒 ล็อกทุกวัน" รวมทั้งทริปใน `TripHeader` · วันที่ล็อกจะปิดทั้ง drag handle, ปุ่มปรับเวลาที่อยู่, ปุ่มลบ, โน้ต, ปุ่มแทรก, ตัวเลือกโหมดเดินทาง และปิด droppable/sortable ของ dnd-kit (มีด่านกันซ้ำใน `useTripDnd` เผื่อลากด้วยคีย์บอร์ด) — แก้ปัญหา "เผลอลากตอนเลื่อนดู"
- [x] **หน้าสรุปแผน `/summary`** — ดูอย่างเดียวทั้งทริป ไม่มีปุ่มแก้สักปุ่ม: ที่พักทุก leg, ตั๋ว/booking, ของที่ต้องเตรียม, แล้วตามด้วยการ์ดรายวันครบ 11 วัน (เวลาตายตัว, เวลาถึง-ออกทุกจุด, โน้ต, สรุปเวลาเดินทาง/ระยะทาง) · ใช้ `useDaySchedule` ตัวเดียวกับอีก 2 หน้า เวลาจึงตรงกันเสมอ
- [x] **เข้าถึงหน้า "วันนี้" ง่ายขึ้น** — `components/BottomNav.tsx` แถบเมนูล่างของมือถือ 3 ปุ่ม (แผนทริป · วันนี้ · สรุปแผน) อยู่ทุกหน้า ไม่ต้องเลื่อนขึ้นไปหาลิงก์บนหัวเว็บ · จอใหญ่ซ่อนไว้ ใช้ลิงก์บนหัวเว็บแทน (เพิ่ม "📋 สรุปแผน" เข้าไปด้วย)
- [x] `/today` อ่าน `trip_day_settings` แล้ว (เดิม hard-code เวลาเริ่ม 07:00 + โหมดขากลับ null) เวลาที่โชว์จึงตรงกับหน้าแผนจริงๆ

---

## 3.5 รอบตรวจโค้ดทั้งโปรเจกต์ (7 ส.ค. 2026) — เฟส 7-12

ตรวจโค้ดทั้งหมด (~9,200 บรรทัด) + ทดสอบจริงในเบราว์เซอร์ทั้ง 3 หน้า พบบั๊ก 14 ตัว + ปัญหา performance
`npm run lint` และ `tsc --noEmit` สะอาดทั้งคู่ — บั๊กทั้งหมดเป็นชนิดที่ tooling จับไม่ได้ ต้องอ่าน/ทดสอบเอง

**สถานะล่าสุด (11 ส.ค. 2026): เฟส 7-12 ปิดครบทุกเฟสแล้ว** — แผนทั้งไฟล์นี้ไม่เหลือข้อค้าง
(ยังเหลือ ~8 สัปดาห์ก่อนบิน · งานหลังจากนี้เป็นของใหม่ที่ยังไม่ได้วางแผน ค่อยคุยกันเพิ่มเป็นเรื่องๆ)

**สถานะ (7 ส.ค. 2026 กลางคืน): เฟส 7-9 เสร็จหมดแล้วภายในวันเดียว** (เร็วกว่าเป้า "จบภายใน ส.ค." มาก)
เหลือ เฟส 6.9 ข้อเดียว (ลบ test data ใน DB — รอผู้ใช้อนุมัติ/ทำเอง) + เฟส 10-12 ที่ยังไม่เริ่ม
ไม่มี migration ใหม่เลยทั้ง 3 เฟสที่ทำไปตามที่ตั้งใจไว้ · โค้ดที่แก้ทั้งหมดผ่าน `npm test` + `lint` + `tsc --noEmit`
สะอาด และยืนยันซ้ำในเบราว์เซอร์จริงแล้ว (ไม่ใช่แค่เชื่อจากทฤษฎี)

**ธีมของสิ่งที่เจอ:** เกือบทุกบั๊กคือ **edge case ของเวลา** (ค่าว่าง / ข้ามเที่ยงคืน / "วันนี้จริง" vs "วันที่กำลังดู")
ซึ่งเป็นจุดที่ unit test จับได้หมดในไม่กี่เคส — เฟส 7 จึงรวมการวางเทสต์ชุดแรกไว้ด้วย ไม่ใช่แค่แก้บั๊ก

**ลำดับเฟสอิงจาก "เหลืออีก ~9 สัปดาห์ก่อนบิน":** ความถูกต้องของเวลา → ความถูกต้องของข้อมูล →
ความเร็ว → กันข้อมูลหาย → ฟีเจอร์เสริม → ความสะอาดโค้ด
เป้าหมายเวลา: **เฟส 7-9 ให้จบภายใน ส.ค.** · **เฟส 10 ภายใน ก.ย.** · เฟส 11-12 เท่าที่มีเวลา

---

### เฟส 6.9 — งานจิ๋วทำก่อนเลย (< 15 นาที) ✅ เสร็จ (8 ส.ค. 2026)

- [x] **ล้าง test data ค้างใน DB จริง** — `Mun Dining` (วัน `d0`, stop id `stop-v2twml5x4msimwzds`) มี `visited_at`
      ติดอยู่จากการทดสอบเฟส 6 ทำให้ `/today` คิดว่า "มาถึงแล้ว" ผู้ใช้รัน SQL เองใน Supabase Dashboard
      (`UPDATE trip_stops SET visited_at = NULL WHERE id = 'stop-v2twml5x4msimwzds'`) ยืนยันด้วย SELECT แล้วว่าเป็น NULL
- [x] **ลบคอมเมนต์ล้าสมัย** `lib/schedule.ts:21` (7 ส.ค. 2026) — "(ดู `app/api/travel-time/route.ts` ที่ยัง broken อยู่)"
      route นั้นเขียนใหม่ตั้งแต่เฟส 1 แล้ว แก้เป็นข้อความที่ตรงกับความจริงปัจจุบัน

---

### เฟส 7 — เวลาที่เชื่อถือได้ + เทสต์ชุดแรก ✅ เสร็จ (7 ส.ค. 2026)
ทุกอย่างในเว็บนี้ห้อยอยู่กับ timeline ถ้าเวลาผิด หน้า "วันนี้" ก็พาไปผิดที่ผิดเวลาตอนเที่ยวจริง

**7.1 วางระบบเทสต์**
- [x] ติดตั้ง `vitest@2.1.9` + `vite-tsconfig-paths@5.1.4` — **หมายเหตุ:** ตัวล่าสุด (`vitest@4` ที่พึ่ง `rolldown`)
      พังบนเครื่องนี้เพราะ Node `v20.12.2` เก่ากว่าที่ต้องการ (`^20.19.0`) หา native binding ไม่เจอ ลง `vitest@2.1.9`
      แทน (ยัง esbuild-based ไม่พึ่ง rolldown) ใช้งานได้ปกติ · `vitest.config.mts` ต้องเป็นนามสกุล `.mts` ไม่ใช่ `.ts`
      เพราะ `vite-tsconfig-paths` เป็น ESM-only แล้ว config ปกติถูกโหลดแบบ CJS ในโปรเจกต์นี้
- [x] เพิ่ม script `npm test` (`vitest run`) ใน `package.json`
- [x] เขียนเทสต์ครอบ pure function 6 ไฟล์ (เพิ่ม `lib/stopOrdering.ts` จากเฟส 8 เข้ามาด้วย) — **50 เทสต์ ผ่านหมด**
      ไฟล์: `lib/__tests__/schedule.test.ts`, `openingHours.test.ts`, `liveDelay.test.ts`, `hotelLegs.test.ts`,
      `optimizeRoute.test.ts`, `stopOrdering.test.ts`, `hotelAnchorId.test.ts`

**7.2 `/today` บอกว่า "ช้ากว่าแผน" ทั้งที่ยังไม่ถึงวันทริป** ✅ แก้แล้ว
- [x] `isRealToday` เทียบ `day.date === todayIso` ตรงๆ แทน `dayIndex === todayIndex` — ยืนยันในเบราว์เซอร์แล้ว
      เปิด `/today` วันนี้ (7 ส.ค.) ไม่มีแถบ "ช้ากว่าแผน" อีกต่อไป ขึ้น "(ไม่ใช่วันนี้จริง)" ถูกต้อง

**7.3 ล้างช่อง "🕐 ออกเดินทาง" แล้วตารางทั้งวันกลายเป็น `NaN:NaN`** ✅ แก้แล้ว
- [x] กัน `""` ที่ `onChange` ของ `<input type="time">` ใน `DayStopsSection` ไม่ให้เรียก `onStartTimeChange` เลย
- [x] `timeToMinutes` คืน `null` เมื่อ parse ไม่ได้ (`lib/schedule.ts`) — ทุกจุดที่เรียกใส่ fallback ครบ
      (`computeSchedule` → 0, `useDaySchedule.effectiveStartTime` → เช็ค `.trim() !== ""` ก่อนใช้ค่า)
- [x] `useDaySettings.setStartTime` อัปเดต state ในเครื่องก่อน (optimistic) + เพิ่มด่านกัน `""` ซ้ำอีกชั้น
      ก่อนยิงขึ้น DB (defense-in-depth ตามที่ตกลงไว้ เผื่อมีทางอื่นเรียกเข้ามาในอนาคต)

**7.4 ป้ายเตือน "จบช้ากว่าเดดไลน์" เงียบสนิทเมื่อเลยเที่ยงคืน** ✅ แก้แล้ว
- [x] `computeSchedule` คืน `arrivalMinutes`/`departureMinutes` ต่อจุด (นาทีสะสมไม่ wrap) และ `endOfDayMinutes`
      ระดับวัน — `deadlineOverrunMinutes` ใน `useDaySchedule` เทียบด้วยค่าดิบนี้แทน parse สตริง `HH:MM` ที่ห่อรอบมาแล้ว
- [x] `isOpenDuring` เปลี่ยนมารับนาทีดิบ (ไม่ใช่ `HH:MM` string) + generalize การสร้างช่วงเปิดให้ครอบ
      เมื่อวาน/วันนี้/พรุ่งนี้ทั้ง 3 วัน (เดิมมีแค่เมื่อวาน/วันนี้) แก้เคสจุดแวะคร่อมเที่ยงคืนไปวันถัดไปได้ถูกทั้งสองทาง
      อัปเดตทุกจุดที่เรียก (`useDaySchedule.isClosedAt`, `DayStopsSection` ใช้ `closedStopIds` จาก hook แทนคำนวณเอง
      ซ้ำ — เก็บงานเฟส 12 ที่ตั้งใจไว้ไปด้วยเลย, `RouteSuggestionModal`, `app/today/page.tsx` 2 จุด)

**7.5 `/today` ไม่ข้ามวันเอง** ✅ แก้แล้ว
- [x] `todayIndex` คำนวณใหม่ทุกครั้งที่ `now` เดิน (ทุก 30 วิ) ผ่าน `useMemo` แทน freeze ด้วย `useState` ครั้งเดียว
      + `useRef`/`useEffect` ตามวันจริงอัตโนมัติเฉพาะตอนผู้ใช้ยังไม่กด ‹ › เลือกดูวันอื่นเอง (ไม่กระโดดทับ)

**7.6 `useDayOpeningHours` cancelled bug** ✅ แก้แล้ว (รวมเข้ากับ 9.2 — ดูด้านล่าง เขียนใหม่ทั้งไฟล์ไปพร้อมกัน)

**ยืนยันปิดเฟส:** `npm test` (50/50 ผ่าน) · `npm run lint` + `tsc --noEmit` สะอาด · เปิด `/today` จริงในเบราว์เซอร์
ไม่มีแถบ "ช้ากว่าแผน" ผิดๆ อีกแล้ว · ไม่มี console error

---

### เฟส 8 — ลำดับจุดแวะไม่เพี้ยน ✅ เสร็จ (7 ส.ค. 2026)
ทั้ง 3 ข้อรากเดียวกัน: `order_index` มีช่องว่างได้ แต่โค้ดบางที่คิดว่ามันคือ "ตำแหน่งใน array"

**8.1 `order_index` ชนกันหลังลากจุดแวะข้ามวัน** ✅ แก้แล้ว
- [x] ย้าย `nextOrderIndex`/`orderIndexAtPosition` ไปเป็น pure function ใน `lib/stopOrdering.ts` ใหม่ (เทสต์ตรงๆ
      ได้ ไม่ต้องพึ่ง React/Supabase) — `addStop` เปลี่ยนมาใช้ `max(order_index) + 1` แทน `dayStops.length`
- [x] `moveStopToDay` จัดเลขวันต้นทางที่เหลือใหม่ให้ต่อเนื่อง 0..n-1 ทันทีในคำสั่งเดียวกัน (ทั้ง state ในเครื่อง
      และ DB — เขียนเฉพาะแถวที่เลขจริงๆ เปลี่ยน ไม่ยิง update เปล่าๆ ทุกแถว)

**8.2 "+ แทรกร้านตรงนี้" แทรกผิดช่องเมื่อมีช่องว่าง** ✅ แก้แล้ว
- [x] `insertStopAt`/`insertIntercityAt` แปลง `atIndex` (ตำแหน่งใน array) → `order_index` เป้าหมายจริงก่อน
      ด้วย `orderIndexAtPosition` แล้วค่อยส่งให้ `shiftForInsert`
- [x] เทสต์เคส "วันมีช่องว่าง 0,2,3 แล้วแทรกตรงกลาง (atIndex=1)" ต้องได้ order_index=2 ไม่ใช่ 1 — ผ่าน
      (`lib/__tests__/stopOrdering.test.ts`, 7 เทสต์)

**8.3 จุดที่ติ๊กแล้วโผล่ 2 ที่ใน `/today`** ✅ แก้แล้ว
- [x] `upcoming`/`upcomingSched` กรอง `!stop.visited_at` เพิ่มอีกชั้น ไม่พึ่ง `slice(nextIndex+1)` อย่างเดียว

⚠️ **เฟสนี้แตะลำดับข้อมูลจริงที่ใช้อยู่** — ไม่ได้ทดสอบด้วยการลาก/แทรกจริงในเบราว์เซอร์บนแผนหลัก (กติกาข้อ 5
ห้ามกดปุ่มแก้ข้อมูลจริงตอนทดสอบ) ยืนยันด้วย `tsc`/`lint` สะอาด + เทสต์ unit ครอบ edge case ที่ระบุไว้ครบ
**แนะนำ:** ทดสอบลาก/แทรกจริงกับ "แผน B" ที่ก๊อปมา ก่อนใช้แผนหลักจริงจัง

---

### เฟส 9 — แคชถูกต้อง + โหลดเร็ว ✅ เสร็จ (7 ส.ค. 2026)
วัดจริงตอนโหลดหน้าแรก (ก่อนแก้): **165 requests · 50 URL ซ้ำกันเป๊ะๆ** (`/api/place-photos` ×68, `/api/place-details` ×49)

**9.1 เวลาเดินทางจาก/ถึงโรงแรมค้างค่าเก่าถาวรเมื่อเปลี่ยนโรงแรม** ✅ แก้แล้ว
- [x] เพิ่ม `hotelAnchorId()` ใน `lib/hotelLegs.ts` — สร้าง id จากพิกัดที่พัก (`hotel@{lat.toFixed(5)},{lng...}`)
      แทน `leg_id` ตรงๆ ใช้แทนที่ทั้งใน `useDaySchedule` (anchor เริ่ม/จบวัน) และ `useHotelDistance`
      (label "🏨 X นาที จากที่พัก" บนการ์ดคลัง) → เปลี่ยนโรงแรมได้ key ใหม่เองทันที **ไม่ต้องเขียน migration ใหม่เลย**

**9.2 ยิง API ซ้ำเพราะไม่มี in-flight dedupe** ✅ แก้แล้ว
- [x] `usePlacePhotos`/`usePlaceDetails` เพิ่ม `inFlight` Map แบบเดียวกับ `useDayTravelTimes`
- [x] รวมแคช `place-details` เป็นก้อนเดียว — `usePlaceDetails.ts` export `placeDetailsCache`/`fetchPlaceDetails()`
      ให้ `useDayOpeningHours` เรียกใช้ร่วมแทนการมี `Map` แยกของตัวเอง (แก้บั๊ก 7.6 ไปพร้อมกันในตัว)

**9.3 คลังสถานที่ถูก mount 2 ชุดเสมอ** ✅ แก้แล้ว
- [x] เพิ่ม `hooks/useMediaQuery.ts` (ใช้ `useSyncExternalStore` — ปลอดภัยสำหรับ SSR ไม่ชน eslint
      `react-hooks/set-state-in-effect` ที่เจอตอนลองใช้ `useEffect` ตั้ง state ตรงๆ ก่อน) `PlaceSidebar`
      เรนเดอร์ `<aside>` (คอม) หรือปุ่มลอย+ชีต (มือถือ) แค่ชุดเดียวจริงๆ ตามขนาดจอ แทนเรนเดอร์ทั้งคู่แล้วซ่อนด้วย CSS

**ผลวัดหลังแก้ (เบราว์เซอร์จริง โหลดหน้าแรกซ้ำ):** **113 requests (ลด 32%) · URL ซ้ำเหลือ 0** (จาก 50)
`/api/place-photos` 68→34 · `/api/place-details` 49→34 · ยืนยันแล้วว่าคลังสถานที่เรนเดอร์แค่ 1 ชุดจริง
(`document.querySelectorAll('aside').length` = 0 บนมือถือ / 1 บนจอใหญ่ ไม่มีทั้งคู่พร้อมกัน)

**ยืนยันปิดเฟส:** `npm test` (50/50) · `lint` + `tsc --noEmit` สะอาด · วัด performance ซ้ำแล้วเห็นตัวเลขดีขึ้นจริง

---

### เฟส 10 — กันข้อมูลหาย + พกไปเที่ยวได้ ✅ เสร็จ (8 ส.ค. 2026)
RLS เปิดสาธารณะทุกตาราง + anon key อยู่ใน bundle = ใครเจอ URL Vercel ก็ลบข้อมูลทริปได้หมด
**เรื่องนี้ตั้งใจไว้แต่แรกและไม่แก้** (กติกาข้อ "ไม่ทำระบบล็อกอิน") — แต่ตอนนี้ **ไม่มี backup เลยสักชั้น** ซึ่งคนละเรื่องกัน

- [x] **ปุ่ม Export แผนเป็น JSON** — ปุ่ม "⬇️ Export JSON" บนหัว `/summary` ดาวน์โหลด plans/stops/hotels/bookings/
      checklist/day settings/overnight overrides/custom places รวมไฟล์เดียว (client ล้วน ไม่มี API ใหม่)
- [x] **`/summary` พิมพ์ได้จริง** (`@media print`) — ซ่อนลิงก์นำทาง/BottomNav ด้วย `print:hidden`, บังคับสีพื้นหลัง
      หัวการ์ดวันด้วย `print-color-adjust: exact`, `print:break-inside-avoid` ต่อวัน, ตั้ง `@page { margin: 12mm }`
      เพิ่มปุ่ม "🖨️ พิมพ์" เรียก `window.print()` ให้ด้วย
- [x] **`/api/place-autocomplete` เพิ่ม rate limit ต่อ IP** — `lib/rateLimit.ts` in-memory sliding window (60 req/นาที/IP)
      คืน 429 เมื่อเกิน พอสำหรับ 2 คนใช้งานจริง ไม่ต้องพึ่ง Redis/Upstash
- [x] **ไฟล์แนบ booking ไม่รั่วอีกแล้ว** — `BookingEditModal.tsx`: กด ✕ ลบไฟล์จริงใน bucket (`storagePathFromPublicUrl`
      แกะ path จาก public URL), ปิด modal โดยไม่กดบันทึกก็ลบไฟล์ที่เพิ่งอัปโหลดทิ้งอัตโนมัติ (`pendingUploadPathRef`),
      จำกัดขนาดไฟล์ 10MB
- [x] **ช่อง "ลิงก์" ของ booking กรอง protocol แล้ว** — `lib/url.ts` (`safeHttpUrl`) อนุญาตเฉพาะ `http:`/`https:`
      บังคับใช้ทั้งตอนบันทึก (โชว์ error ในฟอร์ม) และตอนเรนเดอร์เป็น `href` ใน `/today`

**ยืนยันปิดเฟส:** `npm test` (50/50) · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์ (`/summary`
โหลดได้ กด Export JSON ดาวน์โหลดไฟล์สำเร็จ ไม่มี console/server error)

---

### เฟส 11 — ฟีเจอร์ระหว่างเที่ยว ✅ เสร็จ (8 ส.ค. 2026)
ตอนวางแผนคิดว่าไม่ต้อง migration เลย — จริงๆ ต้องเพิ่ม 2 ตัว (checklist หมวด + รูปต่อจุดแวะ) เพราะข้อมูลใหม่ไม่มีที่เก็บ

- [x] **"จุดถัดไปจะปิดในอีก X นาที"** ใน `/today` — `lib/openingHours.ts` เพิ่ม `minutesUntilClose()` (แยก
      `buildOpenWindows()` ใช้ร่วมกับ `isOpenDuring` เดิม) เทียบเวลานาฬิกาจริงกับช่วงเปิดปัจจุบัน โชว์ป้าย
      "⏳ ปิดในอีก X นาที" เมื่อเหลือ ≤60 นาทีและยังไม่ปิด (ต่างจากป้าย "อาจปิดแล้ว" เดิมที่เตือนเฉพาะสายไปแล้ว)
      เฉพาะวันนี้จริงเท่านั้น (`isRealToday`) — เทสต์ 5 เคสใหม่ใน `openingHours.test.ts`
- [x] **ปุ่ม "นำทางกลับที่พัก"** ใน `/today` — โชว์ `NavButtons` (Google/Kakao/Naver เดิม) ชี้ไป `endAnchor`
      ในการ์ด "เที่ยวครบทุกจุดของวันนี้แล้ว" — ไม่ได้ทดสอบ state นี้จริงในเบราว์เซอร์เพราะต้องติ๊ก "มาถึงแล้ว"
      ครบทุกจุดในแผนหลักจริง (ผิดกติกาข้อ 5) พึ่ง code review + `NavButtons` ที่ผ่านการทดสอบอยู่แล้วจากการ์ดจุดถัดไป
- [x] **Checklist แยกหมวด** (🎒 ของใส่กระเป๋า / 🏨 ก่อนออกจากโรงแรม / ✈️ ก่อนขึ้นเครื่อง) — migration `0022`
      เพิ่มคอลัมน์ `checklist_items.category` (default `'packing'` ของเดิมทั้งหมดเข้ากลุ่มนี้อัตโนมัติ) UI มีแท็บเลือก
      หมวดตอนเพิ่ม + จัดกลุ่มแสดงผลเป็นหมวดๆ ใน `ChecklistPanel.tsx` **ไม่ได้ทำ "ผูกกับวัน"** ตามที่ระบุไว้ตอนแรก
      เพราะไอเดียเดิมไม่ชัดว่าจะผูกยังไงให้มีประโยชน์กว่าหมวดคงที่ 3 หมวดนี้ ถ้าต้องการเพิ่มทีหลังค่อยคุยกันใหม่
- [x] **โน้ต + รูปหน้างานต่อจุดแวะ** — migration `0023` เพิ่มคอลัมน์ `trip_stops.photo_url` ใช้ bucket
      `booking-files` เดิม (คนละ path prefix `stop-photo-*` กันชนกับไฟล์แนบ booking) อัปโหลดจาก `DayStopsSection`
      ทันทีที่เลือกไฟล์ (ไม่มีขั้นตอนร่าง/ยกเลิกเหมือน booking เพราะโน้ตเดิมก็เซฟทันทีอยู่แล้ว) ลบรูปเก่าออกจาก bucket
      ก่อนแทนที่ด้วยรูปใหม่เสมอ กันไฟล์ค้าง จำกัด 10MB เหมือนไฟล์แนบ booking · โชว์รูปที่จุดถัดไปใน `/today` ด้วย
- [x] **เวลาเปิด-ปิดวันหยุดพิเศษ** — **แก้ชื่อ field ที่ระบุผิดตอนวางแผน:** `currentSecondaryOpeningHours` เป็น
      เวลาช่องทางรอง (drive-through/delivery) ไม่ใช่วันหยุด ตัวที่ถูกต้องคือ **`currentOpeningHours`** (ยืนยันจาก
      Google docs) ซึ่งคืนเวลาจริง 7 วันข้างหน้ารวมวันหยุดพิเศษ ต่างจาก `regularOpeningHours` ที่เป็นตารางประจำ
      **จงใจไม่แคชถาวรใน `place_details_cache`** เพราะข้อมูลมีความหมายแค่ 7 วันข้างหน้านับจากตอนเรียก แคชไว้นาน
      จะกลายเป็นข้อมูลผิดเงียบๆ — เพิ่ม `?live=1` ใน `/api/place-details` ยิงสดทุกครั้ง (ข้าม cache ทั้ง DB และ
      Next.js fetch 30 วัน) เรียกเฉพาะจุดถัดไปใน `/today` ตอน `isRealToday` เท่านั้น โชว์ป้าย "🎌 วันนี้เวลาเปิด-ปิด
      อาจต่างจากปกติ" เมื่อต่างจาก `regularOpeningHours` ยืนยันด้วย curl จริงว่า field คืนค่าถูกต้อง

**ผู้ใช้ต้องทำ:** รัน migration `0022_checklist_categories.sql` และ `0023_trip_stop_photo.sql` ใน Supabase
Dashboard → SQL Editor (สองไฟล์นี้เท่านั้น ปลอดภัย แค่เพิ่มคอลัมน์ใหม่ ไม่กระทบข้อมูลเดิม)

**ยืนยันปิดเฟส:** `npm test` (55/55 ผ่าน) · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์
(หน้าแรก/`/today` โหลดได้ไม่มี console error, checklist หมวดใหม่แสดงถูกต้อง, ปุ่ม `+ รูป` เรนเดอร์ครบ 15 จุด,
`/api/place-details?live=1` ยืนยันด้วย curl คืน `currentOpeningHours` จริงจาก Google)

---

### เฟส 12 — ความสะอาดโค้ด (ไม่มีอะไรเปลี่ยนในสายตาผู้ใช้ ทำหลังสุดได้) ✅ เสร็จ (11 ส.ค. 2026)

- [x] **แตก `components/DayStopsSection.tsx`** (1,049 บรรทัด ตอนลงมือจริง — โตขึ้นจาก 973 หลังเฟส 11)
      แยกออกเป็น 3 ไฟล์ ย้ายโค้ดตรงๆ ไม่แตะพฤติกรรมเลย: `SortableStopRow.tsx` (422 บรรทัด),
      `TravelModeRow.tsx` (90) — ต้องแยกด้วยเพราะทั้ง `SortableStopRow` และ `DayStopsSection` ใช้ร่วมกัน,
      `DayEventsPanel.tsx` (45) · เหลือ `DayStopsSection.tsx` **511 บรรทัด** (ลด 51%)
- [x] **ยก hook ที่เป็น global state ขึ้นเป็น Context** — `useHotels`/`useBookings`/`useCustomPlaces` เปลี่ยนเป็น
      `.tsx` ไฟล์ละ 3 ส่วน: `useXStore()` (ตัวจริงที่ fetch + เปิด realtime channel, private), `XProvider`,
      และ `useX()` ที่อ่านจาก context (throw ถ้าถูกเรียกนอก provider) · รวมไว้ที่ `components/TripDataProvider.tsx`
      mount ครั้งเดียวใน `app/layout.tsx` — **ผู้เรียกทุกจุด import เหมือนเดิมทุกบรรทัด** ไม่ต้องแก้
      ผลที่ได้: `custom_places` เลิกโหลดซ้ำ/เปิด channel ที่สองตอนเปิด `NearbyPlacesModal` และทั้ง 3 ตาราง
      ไม่ต้องโหลดใหม่ตอนสลับหน้าแรก ↔ `/today` ↔ `/summary` (client-side nav ใช้ state เดิมต่อ)
- [x] `DayStopsSection` คำนวณ `isOpenDuring` ซ้ำในแถว — **เก็บไปแล้วตั้งแต่เฟส 7.4** ตอนเปลี่ยน `isOpenDuring`
      ให้รับนาทีดิบ (ยืนยันซ้ำรอบนี้: ไฟล์ไม่ import `isOpenDuring` เลย ใช้ `closedStopIds` จาก hook อย่างเดียว)

**ยืนยันปิดเฟส:** `npm test` (55/55 ผ่าน) · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์
ครบทั้ง 3 หน้า ไม่มี console error — หน้าแผนขึ้นครบ 19 จุด (12 จุดวันที่ปลดล็อกมีที่จับลาก/ปุ่มปรับเวลา/+โน้ต/+รูป
ครบ · 7 จุดวันที่ล็อกขึ้นไอคอน 🔒 ถูกต้อง), เปิด `NearbyPlacesModal` แล้วโหลดร้านใกล้ๆ ได้ปกติ (จุดที่เคยเรียก
`useCustomPlaces` ซ้ำ), กด "เปลี่ยน" ในแถวเดินทางแล้ว `TravelModeRow` ที่แยกไฟล์ออกมาสลับเป็นโหมดเลือกได้
ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

## 3.6 รอบตรวจสถาปัตยกรรม + UX (11 ส.ค. 2026) — เฟส 13-19

ตรวจโค้ดทั้งโปรเจกต์ (11,328 บรรทัด / 60 ไฟล์) + **วัดของจริงจากเบราว์เซอร์** ไม่ใช่เดาจากการอ่านโค้ด
จุดตั้งต้นคือฟีดแบ็ก 4 ข้อจากผู้ใช้หลังลองใช้จริง แล้วขยายเป็นการตรวจทั้งระบบ

**คำตัดสินเรื่อง "เขียนใหม่":** ผู้ใช้ให้สิทธิ์รื้อทั้งโปรเจกต์ได้ — **ตรวจแล้วไม่ทำ** เพราะเทสต์ผ่าน 55/55,
build สะอาด, 0 console error, การแบ่งชั้น (data → lib → hooks → components) ถูกต้อง และเหลือ **61 วันก่อนบิน**
บนฐานข้อมูลที่ใช้งานจริงอยู่ 2 คน · ปัญหาทั้ง 4 ข้อไม่ได้เกิดจากสถาปัตยกรรมผิด แต่เกิดจาก **3 ช่องว่างที่ยังไม่มีใครสร้าง**

**สถานะล่าสุด (12 ส.ค. 2026): เฟส 13-19 ปิดหมดแล้ว** เหลือค้างข้อเดียวคือ "ยกส่วน static ขึ้น
Server Component" (เฟส 19 ข้อสุดท้าย) ที่ตั้งใจเลื่อนไปหลังทริป · เหลืออีก **60 วันก่อนบิน**
→ **ทั้งไฟล์นี้ไม่เหลืองานที่ต้องทำก่อนบินแล้ว** งานหลังจากนี้เป็นของใหม่ที่ยังไม่ได้วางแผน

### ตัวเลขที่วัดได้จริง (เปิดหน้าแผน 1 ครั้ง, dev server)

| วัดอะไร | ค่า | หมายเหตุ |
|---|---|---|
| เรียก `/api/*` | **129 ครั้ง** | place-details 34 · place-photos 34 · place-photo 33 · travel-time 6 |
| แผนที่ Google ที่ mount | **10 ตัว** (จอคอม) | มือถือ mount 4 ตัว **มองเห็น 0 ตัว** |
| request ไป maps.googleapis.com | **43 ครั้ง** (มือถือ) | ทั้งที่ผู้ใช้ยังไม่ได้กดแท็บ 🗺️ เลย |
| ข้อมูล decode | **13.57 MB** | `<img>` ดิบ 249 ใบ ไม่ผ่าน next/image |
| Supabase realtime channel | **10 เส้น** | เฟส 12 ยกขึ้น Context ไป 3, เหลืออีก 7 |
| API route ที่ไม่มี rate limit | **7 จาก 8** | `lib/rateLimit.ts` มีอยู่แล้วแต่ใช้เส้นเดียว |
| สตริงภาษาเกาหลีในโปรเจกต์ | **0 ตัว** | ต้นเหตุตรงๆ ของฟีดแบ็กข้อ 1 |

### 3 ช่องว่างที่เป็นต้นเหตุของฟีดแบ็กทั้ง 4 ข้อ

1. **ไม่มีชื่อภาษาท้องถิ่นในระบบเลย** — `mapsQuery` ทั้ง 39 ที่เป็นอังกฤษถอดเสียง และ `app/today/page.tsx`
   ส่ง **ชื่อภาษาไทย** เข้า Naver/Kakao ตรงๆ (`NavButtons name={place.nameTh}`) ซึ่งค้นไม่เจอแน่นอน → เฟส 14
2. **ไม่มีชั้นภาษา (i18n)** — ไทยฝังตายใน ~25 ไฟล์ ผูกภาษาไว้ในชื่อฟิลด์เอง (`nameTh`/`cityTh`/`weekdayTh`)
   ส่วน `nameEn` มีครบทั้ง 39 ที่แล้วแต่ถูกใช้แค่ 2 จุดในทั้งเว็บ → เฟส 16
3. **`DayEvent` เป็นลิสต์ข้อความ ไม่ใช่โมเดลเที่ยวบิน** — ระบบไม่รู้ว่าอันไหนคือตั๋ว อันไหนแก้ได้
   ไม่รู้ว่ากระเป๋าเช็คทะลุหรือเปล่า ต้องผ่าน ตม. ไหม จึงคำนวณต่อไม่ได้และแก้ในเว็บไม่ได้ → เฟส 15

### คำตอบของผู้ใช้ต่อ 5 คำถามที่ค้างไว้ (11 ส.ค. 2026)

| เรื่อง | ตัดสินใจแล้ว |
|---|---|
| ขอบเขตภาษาอังกฤษ | **เฉพาะ `/summary`** ไม่แปลทั้งเว็บ |
| ชื่อท้องถิ่น | ผู้ใช้ให้ผมเลือก → **`nameLocal` + locale ผูกกับเมือง** (`hanoi → vi`, ที่เหลือ `→ ko`) ครอบทั้งทริปด้วยโค้ดชุดเดียว |
| offline / PWA | **ทำ** — กลับคำจากข้อ "ไม่ทำ" เดิม (ดูหัวข้อ 1) ขอบเขต = อ่านอย่างเดียว → เฟส 18 |
| กระเป๋า VN610→VN428 | **through-check ถึงกิมแฮจริง** — ยืนยันแล้ว แก้เวลาเผื่อ 45 → 30 นาทีได้ |
| ลำดับเฟส | เอาตามที่เสนอ (13 → 19) |

---

### เฟส 13 — ปิดรูรั่วก่อน (~30 นาที) ✅ เสร็จ (11 ส.ค. 2026)

งานเล็กสุด เสี่ยงศูนย์ ไม่มี migration แต่ปิดรูค่าใช้จ่ายที่เปิดอยู่จริงตอนนี้

- [x] เติม `isRateLimited()` ให้ครบ **7 route ที่ขาด** — ทุก endpoint ต่อตรงถึง Google และคิดเงินต่อ request
      แต่เปิดสาธารณะไม่มี auth · ใครได้ URL ไปก็ยิงจนบิลบานได้ (`geocode`, `place-details`, `place-nearby`,
      `place-photo`, `place-photos`, `place-search`, `travel-time`, `youtube-video`)
- [x] **แผนที่บนมือถือ: unmount จริงแทนซ่อนด้วย CSS** — `DayStopsSection` เดิมใช้ `className="hidden"`
      คอมโพเนนต์เลยยัง mount และโหลด tile ครบทุกตัวทั้งที่มองไม่เห็น (แก้ P-1 ครึ่งแรก)
      ใช้ `useMediaQuery("(min-width: 1024px)")` ที่มีอยู่แล้ว (SSR-safe ผ่าน `useSyncExternalStore`
      ตัวเดียวกับที่ `PlaceSidebar` ใช้) · **จงใจไม่ unmount ฝั่งลิสต์** เพราะลิสต์เป็น droppable target
      ของ dnd-kit ถ้า unmount ตอนสลับไปแท็บแผนที่จะลากข้ามวันไม่ได้

**ผลที่วัดได้จริง (มือถือ 375px, ก่อน → หลัง):**

| วัดอะไร | ก่อน | หลัง |
|---|---|---|
| แผนที่ที่ mount | 4 ตัว (เห็น 0) | **0 ตัว** |
| request ไป maps.googleapis.com | 43 | **6** (เหลือแค่ตัวโหลด API) |
| เรียก `/api/*` | 129 | **67** |
| ข้อมูล decode | 13.57 MB | **9.60 MB** |

(`/api/*` ลดตามไปด้วยเพราะ `DayMapPanel` มี `PlaceThumb` ในป๊อปอัพหมุด ซึ่งยิง `place-photos`/`place-photo`
ของมันเอง — รูปพวกนั้นอยู่ในป๊อปอัพที่ผู้ใช้เปิดไม่ได้อยู่แล้วตอนแท็บแผนที่ยังไม่ถูกกด)

**ยืนยันปิดเฟส (11 ส.ค. 2026):** `npm run lint` + `tsc --noEmit` สะอาด · `npm test` 55/55 ผ่าน ·
ทดสอบจริงในเบราว์เซอร์: กดแท็บ "🗺️ แผนที่" บนมือถือแล้ว mount แผนที่ 1 ตัวขึ้นมาถูกต้อง (หมุดเลข 1-2-3 ครบ)
· จอ 1400px ยังขึ้นแผนที่คู่ลิสต์เหมือนเดิมทุกวัน (4 ตัว mount / 4 ตัวมองเห็น) · console เหลือแค่ warning
`google.maps.Marker` deprecated ที่มีมาก่อนแล้ว (ตั้งใจใช้ Marker แบบคลาสสิก ดูเหตุผลที่เฟส 3)
· ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

### เฟส 13.5 — ล็อกเว็บด้วย PIN ✅ เสร็จ (11 ส.ค. 2026)

**ที่มา:** ตอนยืนยัน migration 0024 ด้วย curl (ตามกติกาข้อ 4) ก็เท่ากับสาธิตปัญหาไปในตัว — อ่านข้อมูลทริปจริง
ออกมาได้จาก terminal ด้วย anon key ที่ติดอยู่ในบันเดิลฝั่ง browser โดยไม่ต้องเข้าเว็บเลย
ผู้ใช้เลือก **"ใส่ PIN ก็พอ"** (ไม่เอา Supabase Auth เต็มรูปแบบตอนนี้) — เป็นการกลับคำข้อ "ไม่ทำระบบล็อกอิน" บางส่วน

- [x] `proxy.ts` ที่ root — **ไม่ใช่ `middleware.ts`** เพราะ Next 16 deprecate ไปแล้วและเปลี่ยนชื่อเป็น `proxy`
      (ยืนยันจาก `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` ตามกติกาข้อ 2)
      · รันบน Node.js runtime เป็นค่าเริ่มต้น จึงใช้ `node:crypto` ได้ตรงๆ
- [x] `lib/pinAuth.ts` — cookie เก็บ **HMAC(secret, pin)** ไม่ใช่ธง `authed=1` (ใครก็ตั้งเองได้)
      และไม่ใช่ `sha256(pin)` เฉยๆ (PIN 4 หลัก = 10,000 ค่า ทำตารางแฮชได้ในไม่กี่วินาที) · เทียบแบบ timing-safe
- [x] `app/unlock/page.tsx` + `app/api/unlock/route.ts` — **rate limit 10 ครั้ง/นาที** ที่ endpoint นี้
      คือสิ่งเดียวที่ทำให้ PIN สั้นๆ พอใช้ได้จริง (ไล่เดาครบ 10,000 ค่าต้องใช้ ~16 ชม.แทนที่จะไม่ถึงนาที) **ห้ามถอด**
- [x] cookie `httpOnly` + `sameSite=lax` + `secure` เฉพาะ production + อายุ 90 วัน (กรอกครั้งเดียวใช้ยาวจนจบทริป)
- [x] จำหน้าที่ตั้งใจเข้าไว้ใน `?next=` แล้วพากลับหลังปลดล็อก (เปิดลิงก์ `/today` ตรงๆ ได้)

**บั๊กที่เจอระหว่างทำ (บันทึกไว้กันพลาดซ้ำ):** matcher เขียนเป็น `(?!_next/static|_next/image|favicon.ico)`
ตามตัวอย่างในเอกสาร → **`/_next/hmr` โดนด่านดักไปด้วย** → HMR ตอน dev พัง → React ไม่ hydrate →
ฟอร์มกรอก PIN ตกไปเป็น native submit กดแล้วรีโหลดเฉยๆ · แก้เป็นตัด **`_next` ทั้งก้อน** ซึ่งปลอดภัยเพราะ
ข้อมูลของหน้าที่หวงเดินทางผ่าน URL ของหน้านั้นเอง (RSC ยิงไป path เดิมพ่วง `?_rsc=` ซึ่งยังเข้าด่านอยู่)

**⚠️ ขอบเขตที่กันได้จริง — อย่าเข้าใจผิด:** ด่านนี้กัน **หน้าเว็บ + API route ของเรา** (= กันบิล Google)
**ไม่ได้กัน Supabase** เพราะ browser คุยกับ Supabase ตรงด้วย anon key ที่ซ่อนไม่ได้ตามการออกแบบ
13 ตาราง 49 policy ยังเป็น `using (true)` ทั้งหมด และ bucket `booking-files` ยังลบได้จาก anon key
→ ถ้าจะปิดจริงต้องทำ **Supabase Auth + แก้ RLS** (ยังไม่ได้ทำ ผู้ใช้เลือกไว้ทีหลัง)

**ยืนยันปิดเฟส (11 ส.ค. 2026):** `lint` + `tsc --noEmit` + `test` 55/55 + `build` สะอาด · ทดสอบด้วย curl 7 เคส:
เข้าหน้าเว็บตอนล็อก → 307 ไป `/unlock?next=%2Ftoday` · ยิง API ตอนล็อก → 401 · PIN ผิด → 401 · PIN ถูก →
200 + `Set-Cookie` httpOnly 90 วัน · cookie ปลอมทั้งแบบสั้นและแบบยาวเท่าของจริง → ถูกปฏิเสธทั้งคู่ ·
ยิง PIN ผิดรัวๆ → 429 หลังครั้งที่ 7 · และทดสอบ UI จริงในเบราว์เซอร์: PIN ผิดขึ้นแถบ "PIN ไม่ถูกต้อง"
ไม่รีโหลดหน้า · PIN ถูกพาไป `/today` ตาม `next` เนื้อหาขึ้นครบรวมรูปจาก `/api/place-photo`
· ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

### เฟส 14 — ชื่อท้องถิ่น + นำทางที่ใช้ได้จริง (ฟีดแบ็กข้อ 1)

**กลไกหลัก:** Places API (New) ที่เปิดใช้อยู่แล้วคืนชื่อในภาษาที่ขอได้ ตอนนี้โค้ดทุกจุดขอ `languageCode: "th"`
เท่านั้น (`lib/googlePlaces.ts:78, 125, 196`) — ขอ `"ko"` เพิ่มก็ได้ `감천문화마을` มาเลย **ไม่ต้องแปลเอง
ไม่ต้องเปิด API เจ้าใหม่** (สอดคล้องกับกติกาข้อ 3 และการตัดสินใจ 6 ส.ค. ที่ไม่เพิ่ม ODsay/Kakao/Naver API)

- [x] migration `0024` — เพิ่ม `name_local`, `address_local`, `locale` ใน `place_details_cache` (รันแล้ว 11 ส.ค.)
- [x] **ฝัง `nameLocal` + `addressLocal` ลง `data/places.ts` ครบ 46 ที่** (39 ตามแผน + ที่เพิ่มมาทีหลัง)
      ดึงจาก Places API ด้วยสคริปต์ครั้งเดียว ไม่ได้พิมพ์เอง — ขึ้นทันทีแม้เน็ตหลุด ซึ่งสำคัญมากตอนอยู่บนแท็กซี่
      · เพิ่ม `CITY_LOCALE` (ฮานอย = `vi`, ที่เหลือ = `ko`) + `isKoreanCity()`
- [x] รื้อ `lib/mapLinks.ts` — เพิ่ม `navigationName()` ลำดับ fallback `local → en → th`
      **ยืนยันแล้วว่าลิงก์ Kakao ส่ง `감천문화마을` จริง** (เดิมส่งชื่อไทยซึ่งค้นไม่เจอ)
- [x] **การ์ด "ยื่นให้คนขับดู"** — `components/LocalNameCard.tsx` ชื่อตัว 2xl + ที่อยู่ กดคัดลอกได้
- [x] **สถานที่ที่ผู้ใช้เพิ่มเอง** — `/api/place-details?locale=ko|vi` resolve + แคชลง migration 0024
      ทดสอบแล้วเขียนจริงครบ 3 ร้านฮานอยใน `place_details_cache` · ขอมาพร้อมเวลาเปิด-ปิดในคำขอเดียว
      (ระวังบั๊ก 9.2 ที่เพิ่งแก้ไป — ห้ามยิงซ้ำสองรอบต่อสถานที่)
- [ ] **ยืนยัน `nmap://` บนมือถือจริงที่ลงแอป Naver ไว้** — ห้ามยืนยันจากความจำ เพราะ `nmap://`
      ไม่คืน response ให้เช็ก ต้องเห็นแอปเปิดขึ้นมาจริงเท่านั้นถึงนับว่าผ่าน
      (เบราว์เซอร์ของ agent เปิด `map.naver.com` ไม่ได้ ถูกบล็อกโดย policy — ผมทดสอบแทนไม่ได้จริงๆ)
- [x] **ที่พักยังไม่มีชื่อภาษาท้องถิ่น** — ✅ ปิดแล้วในเฟส 16 (12 ส.ค. 2026) ด้วย migration `0026`
      + `/api/geocode?locale=` + `hotelNavigationName()` ดูรายละเอียดที่หัวข้อเฟส 16

**เจอระหว่างทำ — คุณภาพข้อมูล `mapsQuery` (ไม่ใช่บั๊กของเฟสนี้ แต่ควรรู้):**
`mapsQuery` บางอันชี้ไปคนละที่กับที่ตั้งใจ ซึ่งกระทบ**เวลาเปิด-ปิดและรูปด้วย** ไม่ใช่แค่ชื่อ —
`seoul-seongsudong` ("Seongsu-dong Seoul cafe") ได้ร้าน 어니언 성수점 แทนย่าน ·
`Quán Café Maison De Hanoi` ได้ "Maison de Éte" ที่ Kim Liên คนละย่านกับที่วางไว้ ·
`hanoi-train-street` ได้ร้านอาหารแทนถนนรถไฟ · `gangneung-ojukheon` Google คืน `오죽헌_<상>` มี artifact ติดมา
→ 4 ตัวนี้ผมยิงใหม่ด้วยคำค้นภาษาท้องถิ่นแล้วใช้ผลนั้นแทนเฉพาะ `nameLocal`/`addressLocal`
**จงใจไม่แตะ `mapsQuery`** เพราะเป็น cache key ของทั้ง `place_details_cache`/`place_photo_cache` แก้แล้วแคชหลุดยกชุด

---

### เฟส 15 — วันบินแก้ได้ + ทรานซิตถูกต้อง (ฟีดแบ็กข้อ 3 + 4) ✅ เสร็จ (12 ส.ค. 2026)

หลักการใหม่: **ล็อกแค่สิ่งที่ซื้อตั๋วมาแล้ว** ที่เหลือของวันบินแก้ได้เหมือนวันเที่ยวปกติ

- [x] รื้อ `DayEvent` เป็นโมเดลจริง — `kind: "flight" | "layover" | "transfer" | "checkin" | "deadline"`
      + `editable?: boolean` (ไม่ใส่ = ล็อก เพื่อให้เหตุการณ์เก่าที่ยังไม่ระบุ kind พฤติกรรมไม่เปลี่ยน)
      · เพิ่ม type `FlightInfo` (`no`/`fromCode`/`toCode`/`fromEn`/`toEn`) ด้วย — **เฟส 16 ต้องใช้**
      เลขเที่ยวบิน + ต้นทาง/ปลายทางอังกฤษบนหน้า ตม. ดึงจากที่เดียวกับตารางบิน ไม่พิมพ์ซ้ำ
      · `DayEventsPanel` โชว์ 🔒/✏️ รายแถว + ป้าย `BKK → HAN` ของเที่ยวบิน แทนป้ายรวม "ล็อกทั้งแผง" เดิม
- [x] เพิ่ม type `Layover` — `baggage` · `immigration` · `leavesAirport` · `terminalChange`
      render เป็น `components/LayoverBadges.tsx` (ป้ายละ 1 คำถามที่คนถามจริงตอนต่อเครื่อง) สีส้ม = ต้องลงมือทำ
      / เขียว = ไม่ต้องทำอะไร · เพิ่ม event ช่วงพักเครื่องฮานอยเข้า `d0` ที่เดิมไม่มีเป็นแถวของตัวเองเลย
- [x] **แก้ข้อมูล `d0` ให้ตรงตั๋วจริง** — VN610→VN428 เช็คกระเป๋าทะลุถึงกิมแฮ (ผู้ใช้ยืนยันแล้ว)
      "ผ่าน ตม. + รับกระเป๋า ~45 น." → **"ผ่าน ตม. ~30 น. (ไม่ต้องรับกระเป๋า)"** และเวลาแนะนำ
      ถึงย่านเมืองเก่า **15:30 → 15:15** = ได้เวลาเที่ยวฮานอยคืนมา 15 นาที (6 ชม. 45 น.)
- [x] `d10` โฮจิมินห์ — `immigration: "none"`, `leavesAirport: false`, `terminalChange: false`
- [x] เพิ่ม `kind: "transfer"` ใน `trip_stops` (migration `0025`) — **ต่างจาก `intercity` ตรงที่แถวนี้มี
      `place_id` จริง** ชี้ไปสนามบินใน `data/transferPoints.ts` (แยกจาก `PLACES` เพราะสนามบินไม่ใช่ที่เที่ยว
      ไม่ควรโผล่ในคลัง — `resolvePlace` หาให้เจอผ่าน fallback ชั้นที่สอง) เวลาเดินทางจึงเป็นของจริงจาก
      Routes API เหมือนจุดแวะปกติ · `dwell_minutes` ใช้เก็บ "เวลาเผื่อที่สนามบิน" ใช้ปุ่ม +/− เดิมได้ฟรี
      · เพิ่มหมวด `transport` ใน `Category` (ไม่ได้ใส่ใน `CATEGORY_ORDER` ของ `PlaceSidebar` = ไม่โผล่ในคลัง)
      · **กัน `RouteSuggestionModal` ย้ายแถวนี้** ด้วย `isReorderable()` — เดิมกันแค่แถวที่ resolve place ไม่ได้
      ซึ่งไม่ครอบแถว transfer ที่มี place จริง (ถ้าไม่กัน "ลองจัดเส้นทางใหม่" จะดันสนามบินไปกลางวัน)
- [x] **ตัวคำนวณ "ควรออกกี่โมง"** — `lib/departureAdvice.ts` + `components/TransferAdvicePanel.tsx`
      `เวลาที่ควรออก = เวลาบิน − เผื่อเช็คอิน − เวลาเดินทางจริง − เผื่อความเสี่ยง 30 น.` (เช็คอินเริ่มต้น 3 ชม.
      ปรับได้ตอนแทรก) · เป็น**คำแนะนำ ไม่เขียนทับเวลาในแผน** · เตือน ⚠️ พร้อมบอกว่าช้าไปกี่นาทีเมื่อไปถึงช้ากว่าเส้นตาย
      · ใช้ **นาทีสะสมไม่ wrap** เทียบข้ามเที่ยงคืนได้ถูก (เคส d0 บินตี 1:15) และมีกฎ 12 ชม. กันเคสกลับด้าน
      "ถึง 11:00 แต่บิน 10:35" ไม่ให้ถูกตีเป็นเที่ยวบินพรุ่งนี้แล้วบอกว่า "มาเร็วไป 23 ชม." — **6 เทสต์ใหม่**
- [x] `d10` เทียบตัวเลือกจริง — `data/airportAccess.ts` (ICN: AREX ด่วน 43 น. / AREX ธรรมดา 59 น. /
      ลิมูซีนบัส ~80 น. · HAN: แท็กซี่ 40 น. / บัส 86 · PUS: BGL+สาย 2 / ลิมูซีน) กางดูได้ใต้แถว transfer
      **พร้อมเวลา "ออกไม่เกินกี่โมง" ของแต่ละตัวเลือก** · ติดป้ายชัดว่าตัวเลขพวกนี้เป็นตารางเดินรถของผู้ให้บริการ
      (คงที่) ส่วนเวลาในแถวเดินทางเป็นของ Google ตามพิกัดโรงแรมที่เลือกไว้จริง — กติกาเดียวกับป้าย "จริง/ประมาณการ"

**ผู้ใช้ต้องทำ:** รัน `0025_trip_stops_transfer.sql` (เพิ่ม 2 คอลัมน์ ปลอดภัย ไม่กระทบข้อมูลเดิม)
จนกว่าจะรัน ปุ่ม "✈️ + ไปสนามบิน" จะบันทึกไม่ผ่าน (Supabase ปฏิเสธคอลัมน์ที่ไม่มีจริง)

**ยืนยันปิดเฟส:** `npm test` **61/61 ผ่าน** · `npm run lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์
ที่ 375px: การ์ด `d0` ขึ้นแถบพักเครื่องฮานอยพร้อมป้าย 4 ใบถูกต้อง (กระเป๋าทะลุ/ต้องผ่าน ตม./ออกไปเที่ยว/อาคารเดิม),
เวลาแนะนำเป็น 15:15, ป้าย 🔒 กับ ✏️ แยกถูกตามชนิดเหตุการณ์, เปิด modal "✈️ + ไปสนามบิน" ที่ `d10` แล้ว
**เลือก ICN ให้อัตโนมัติตามเมือง + ดึง VN409 10:35 มาเป็นเส้นตายให้เอง** + โชว์ตัวเลือก AREX/ลิมูซีนครบ
· console สะอาดหลังรีโหลด · **ปิด modal โดยไม่กดบันทึก — ไม่มีการแก้ข้อมูลจริงใน DB (กติกาข้อ 5)**

⚠️ **ยังไม่ได้ทดสอบ:** แถว transfer ที่ถูกบันทึกลงวันจริงแล้ว (ต้องเขียนลง DB ที่ใช้งานจริง + ต้องรัน migration
0025 ก่อน) — ตรรกะเวลาครอบด้วย unit test 6 เคสแทน ตามแนวเดียวกับเฟส 8/11 · **แนะนำให้ผู้ใช้ลองแทรกจริง
หลังรัน migration แล้วดูว่าแถบ "ควรออกไม่เกิน HH:MM" ขึ้นถูก**

**อัปเดต (12 ส.ค. 2026 — ผู้ใช้ทักมาว่าการ์ด `d10` แสดงผลต่างจากวันอื่น):**
- [x] **แก้บั๊ก `d10`** — events ของ `d10` ไม่มีแท็ก `anchor` เลย (ต่างจาก `d0` ที่มี) ทำให้เหตุการณ์ทั้ง 6 อัน
      ถูกเทลง panel เดียวแทนที่จะแยก "ก่อนจุดแวะ"/"✈️ ต่อจากนั้น" แบบวันอื่น และ `defaultStartTime` ตกไปใช้
      fallback `07:00` ทั้งที่ต้องออก `05:45` จริง — เพิ่ม `anchor: "before"` ให้ event เช็คเอาต์ `05:45`
      ตามแพทเทิร์นเดียวกับ `d0` (ไม่ใส่ `anchor: "after"` เพราะวันนี้ไม่มีช่วงว่างให้แทรกจุดแวะจริงๆ ต่างจาก
      `d0` ที่มีช่วงว่าง 6 ชม. 45 น. ในฮานอย) ยืนยันในเบราว์เซอร์แล้ว: panel แยกถูก + เวลาแนะนำขึ้น `05:45`
- [x] **แทรกแถว "✈️ ไปสนามบิน" ให้ `d10` จริงแล้ว** (เขียนสคริปต์ยิง Supabase REST ตรง เหมือนแพทเทิร์น backfill
      ที่พักในเฟส 16 — dry-run ก่อนเขียนจริง) ค่าที่ใช้คือค่าเดียวกับที่ `TransferEditModal` จะตั้งให้เองถ้ากด
      "เพิ่ม" ทันที: `airport-icn`, เผื่อเวลา 180 นาที, เส้นตาย VN409 10:35 — ยืนยันด้วย curl + เห็นในเบราว์เซอร์
      แล้วว่าขึ้นแถวจุดแวะ + แผนที่ + `TransferAdvicePanel` ("ควรออกไม่เกิน 07:05") ครบเหมือนวันอื่น
      · ⚠️ **ที่พักโซล (คืน 17-20 ต.ค.) ยังไม่มีใน `trip_hotels`** ทำให้เวลาเดินทางที่พัก→สนามบินคำนวณเป็น
      **0 นาที** เลข "ควรออกไม่เกิน" ที่ขึ้นตอนนี้จึง**มองโลกในแง่ดีเกินจริง** — ต้องตั้งที่พักโซลก่อน เลขถึงจะแม่น
      · **`trip_day_settings.start_time` ของ `d10` ยังเป็น `07:00` เดิมที่ผู้ใช้เคยตั้งไว้เอง** — ไม่ได้แก้ให้
      (เป็นการแก้ข้อมูลจริง ไม่ใช่โค้ด) กดปุ่ม "กลับไปใช้เวลาแนะนำ (05:45)" เองได้ถ้าต้องการ

---

### เฟส 16 — TH/ENG + หน้าสำหรับ ตม. (ฟีดแบ็กข้อ 2) ✅ เสร็จ (12 ส.ค. 2026)

ผู้ใช้เลือก "เฉพาะ `/summary`" — แปลทั้งเว็บจะแตะ ~25 ไฟล์แต่ได้ประโยชน์จริงแค่ตอนยืนหน้าเคาน์เตอร์
โฟกัสหน้าเดียว = ได้ผลเต็มด้วยงาน ~20% และหน้าแผน/หน้าวันนี้ยังเป็นไทยเหมือนเดิมตามที่ใช้กันอยู่

- [x] `lib/i18n.ts` — dictionary 33 คีย์ + `useLang()` (ลำดับ: `?lang=` → localStorage → ไทย)
      กดสลับแล้ว**เขียนภาษาลง URL ด้วยเสมอ** ลิงก์ที่แชร์/บุ๊กมาร์กจึงพกภาษาติดไปเอง
      · **ต้องมี `<Suspense>` ครอบ** เพราะ `useSearchParams` บังคับให้ subtree ถึงขอบ Suspense
      เรนเดอร์ฝั่ง client ล้วน (ยืนยันจาก `node_modules/next/dist/docs/.../use-search-params.md`
      ตามกติกาข้อ 2) — ผลพลอยได้คืออ่าน localStorage ตอน initial state ได้เลยไม่เกิด hydration mismatch
      · ภาษาที่จำไว้เก็บเป็น `useSyncExternalStore` ไม่ใช่ `useState` เพื่อเขียนจาก effect ได้โดยไม่ชน
      eslint `react-hooks/set-state-in-effect` (ปัญหาและทางแก้เดียวกับ `useMediaQuery` ตอนเฟส 9.3)
- [x] เติมคู่ภาษาอังกฤษที่ยังขาด: `Day.cityEn` + `Day.weekdayEn` (ฝังค่าไว้ ไม่ derive จาก
      `toLocaleDateString` เพื่อไม่ให้ผลเปลี่ยนตาม locale ของเครื่องที่เปิด) · `DayEvent.titleEn` 13 ตัว
      · `CITY_NAME_EN` · `TRAVEL_MODE_LABEL_EN` · `BOOKING_CATEGORY_LABEL_EN` · `INTERCITY_MODE_LABEL_EN`
      · `LayoverBadges` รับ `lang` แล้ว · **จงใจไม่ทำ `detailEn`** — detail เป็นโน้ตวางแผนของเราเอง
      โหมด en จึงซ่อนทิ้ง (รวมถึง `day.note` และโน้ตต่อจุด) ดีกว่าโชว์ไทยปนกลางหน้าอังกฤษ
- [x] **`/summary?lang=en&for=immigration`** — `components/ImmigrationSheet.tsx` ตัดทุกอย่างที่เจ้าหน้าที่
      ไม่ได้ถามออกหมด เหลือ 4 ตาราง: ชื่อตามพาสปอร์ต · เที่ยวบิน 4 เที่ยว (วันที่/เลขบิน/เส้นทาง/ออก/ถึง) ·
      ที่พักทุก leg (ชื่ออังกฤษ + ที่อยู่ท้องถิ่น + เบอร์โทร) · ตารางรายวันย่อ (วันที่/วัน/เมือง/สถานที่ EN)
      · หัวเว็บสีเขียวถูก `print:hidden` ในโหมดนี้ กระดาษแผ่นแรกจึงเริ่มที่ "Travel Itinerary" ตรงๆ
      · **`DayEvent.dayOffset`** ใหม่ — VN428 ออกตี 1:15 ซึ่งเป็น *วันที่ 12* แต่แสดงอยู่ในการ์ดวันที่ 11
      ถ้าไม่มีฟิลด์นี้ วันบินเข้าประเทศบนเอกสารจะคลาดไป 1 วัน (ยืนยันในเบราว์เซอร์แล้วว่าขึ้น 12 Oct 2026 ถูก)
      · **ชื่อตามพาสปอร์ตเก็บใน localStorage ไม่ใช่ Supabase โดยตั้งใจ** — RLS โปรเจกต์นี้เปิดสาธารณะ
      ทุกตารางและ anon key อยู่ในบันเดิล (ดูขอบเขตเฟส 13.5) ชื่อ-นามสกุลตามพาสปอร์ตไม่ควรอยู่ในนั้น
- [x] ปุ่มสลับ 🇹🇭/🇬🇧 ที่หัว `/summary` ตำแหน่งเดียวกับปุ่มพิมพ์ + ปุ่มสลับไป/กลับหน้า ตม.

**แถม — ปิดงานค้างของเฟส 14 (ที่พักไม่มีชื่อภาษาท้องถิ่น) ไปพร้อมกัน:**
- [x] migration `0026_trip_hotels_local.sql` — `name_local` / `address_local` / `name_en` / `address_en`
      / `phone` ใน `trip_hotels` · `/api/geocode?locale=ko|vi` ยิง Place Details ซ้ำอีก 2 ภาษาแบบขนาน
      (เพิ่ม `languageCode` ให้ `getPlaceDetails`) เกิดเฉพาะตอนกดบันทึกที่พักซึ่งนานๆ ครั้ง
      · `HotelEditModal` โชว์ชื่อ/ที่อยู่ภาษาท้องถิ่น + เบอร์โทรให้เห็นก่อนกดบันทึก
      · `/today` ปุ่ม "นำทางกลับที่พัก" ใช้ `hotelNavigationName()` แล้ว — เดิมส่งชื่อไทยเข้า Naver/Kakao
      ซึ่งค้นไม่เจอ (บั๊กเดียวกับที่เฟส 14 แก้ให้จุดแวะไปแล้ว)
      · `setHotel()` เปลี่ยนจาก 6 อาร์กิวเมนต์เรียงกันเป็นอ็อบเจกต์เดียว (`HotelInput`) — พอเพิ่มอีก 5 ช่อง
      แล้วสลับตำแหน่งกันง่ายเกินไป

**ผู้ใช้ต้องทำ:** รัน `0026_trip_hotels_local.sql` แล้ว **เปิดที่พักแต่ละที่ในหน้าแผน เลือกจาก dropdown ซ้ำ
แล้วกดบันทึกอีกครั้ง** — แถวที่บันทึกไว้ก่อนหน้านี้ยังไม่มีชื่อ/ที่อยู่/เบอร์ (ตารางที่พักบนหน้า ตม. จะขึ้น "—")

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์:
`/summary?lang=en` ขึ้นอังกฤษทั้งหน้ารวมชื่อสถานที่/เมือง/ป้ายทรานซิต · กดปุ่ม 🇹🇭 แล้วกลับไทยทันที
URL เปลี่ยนเป็น `?lang=th` และ localStorage จำค่าไว้ · `?for=immigration` ขึ้นเอกสาร 4 ตารางครบ
**เที่ยวบิน VN428 ขึ้นวันที่ 12 Oct 2026 ถูกต้อง** (ไม่ใช่ 11) · console สะอาด · ไม่มีการแก้ข้อมูลจริงใน DB

---

### เฟส 17 — ของแถมสำหรับใช้จริงหน้างาน ✅ เสร็จ (12 ส.ค. 2026)

- [x] **พยากรณ์อากาศรายวัน** บนหัวการ์ดวัน + หัว `/today` — `/api/weather` → **Open-Meteo**
      (`lib/weather.ts` + `hooks/useTripWeather.ts` + `components/WeatherBadge.tsx`)
      **ทำไมไม่ใช้ Google Weather API:** กติกาข้อ 3 บอกว่าคีย์นี้เปิดไว้เฉพาะ `places`/`routes`
      การใช้ Weather ต้องไปเปิด API ตระกูลใหม่เพิ่ม · Open-Meteo ฟรี ไม่ต้องสมัคร ไม่ต้องมีคีย์เลย
      · ยิง **1 คำขอต่อเมือง** ครอบช่วงวันของเมืองนั้น (ไม่ใช่ 1 ต่อวัน) + แคชระดับโมดูล + in-flight
      dedupe แบบเดียวกับที่แก้บั๊ก 9.2 · แคชฝั่งเซิร์ฟเวอร์ 3 ชม.
      ⚠️ **พยากรณ์ล่วงหน้าได้ ~16 วันเท่านั้น** — ตอนนี้ (เหลือ 60 วัน) จึงยังไม่มีข้อมูลจริงสักวัน
      ซึ่งดูเหมือนฟีเจอร์พัง จึงขึ้นข้อความบอกตรงๆ เหนือลิสต์วันว่าจะขึ้นเมื่อไหร่
- [x] **การ์ดฉุกเฉิน** (`components/EmergencyCard.tsx` + `data/emergency.ts`) ท้ายหน้า `/today` ยุบไว้เป็นค่าเริ่มต้น
      · **เบอร์เปลี่ยนตามประเทศของวันนั้น** (ฮานอย = เวียดนาม 113/115 + สถานทูตฮานอย · ที่เหลือ =
      เกาหลี 119/112 + 1330 + สถานทูตโซล) กดโทรได้ตรงๆ ผ่าน `tel:` · เลขที่จองทั้งหมดรวมไว้ที่นี่ด้วย
      · โรงพยาบาลดึงสดรอบ**ที่พักคืนนั้น** (ไม่ต้องขอสิทธิ์ location) ยิงเฉพาะตอนกางการ์ด ไม่เสียโควตาฟรี
      · **เจอระหว่างทำ:** Google ติดป้าย `hospital` ให้คลินิกศัลยกรรม/ผิวหนังในเกาหลีเยอะมาก
      เรียงตาม `DISTANCE` แล้วได้คลินิกเสริมความงาม 3 อันดับแรกรอบซอมยอน → เปลี่ยนเป็น `POPULARITY`
      รัศมี 8 กม. ได้ Pusan National University Hospital / Busan Paik / 동아대학교병원 ขึ้นมาแทน (ยืนยันด้วยการยิงจริง)
      · เบอร์สถานทูตติดหมายเหตุให้ตรวจซ้ำจากเว็บทางการก่อนเดินทาง (ข้อมูลชนิดที่เปลี่ยนได้)
- [x] **ธีมมืดของ `/today`** — `hooks/useDarkTheme.ts` ตั้ง `data-theme` ที่ `<html>` (ไม่ใช่กล่องของหน้า
      เพื่อให้พื้น `body` เปลี่ยนตามด้วย) และ **ล้างทิ้งตอน unmount เสมอ** ธีมจึงไม่รั่วไปหน้าอื่น
      · ตามธีมของเครื่องเป็นค่าเริ่มต้น กดปุ่ม 🌙/☀️ ทับได้ จำใน localStorage
      · **บทเรียนสำคัญ:** `@theme inline` ของ Tailwind v4 **ฝังค่าสีลงคลาสยูทิลิตี้** การเขียนทับ
      `--color-pine-soft` ตอนรันไทม์จึงไม่มีผลเลย ต้องประกาศโทเคนใหม่ที่ชี้ไป `var(--surface)` ฯลฯ
      แล้วสลับค่าตัวแปรชั้นนอกแทน · และ**ห้ามพลิก `cream`/`ink` ตรงๆ** เพราะ `text-cream` ถูกใช้เป็น
      "ตัวอักษรสว่างบนพื้นสีเน้น" ด้วย (9 จุดใน `/today`) จะหายไปกับพื้นทันที
      → เพิ่มโทเคนเชิงความหมาย `surface`/`surface-raised`/`surface-soft`/`content`/`content-soft`/`line`
      + คู่แผงสี `panel-pine`/`panel-maple`/`panel-gold` (พื้นจาง+ตัวอักษรเข้ม สลับขั้วกันในธีมมืด)
      · ปุ่ม Kakao (`bg-[#FEE500]`) คงตัวอักษร `text-ink` ไว้ตายตัว — เป็นสีแบรนด์ ไม่ใช่พื้นของธีม
- [x] **แถบวัน sticky + ยุบวันที่ล็อกแล้วอัตโนมัติ** — `components/DayJumpBar.tsx` ชิป 11 วันเลื่อนแนวนอน
      กดแล้วกระโดดไปการ์ดวันนั้น ไฮไลต์วันที่กำลังดูอยู่เองด้วย `IntersectionObserver`
      · วันที่ล็อกแล้วยุบเหลือหัวการ์ด + สรุปบรรทัดเดียว ("📍 4 จุด · 07:00 – 14:15") แตะเพื่อกาง
      · ยุบแล้ว **unmount ทั้งลิสต์และแผนที่จริงๆ** ไม่ใช่ซ่อนด้วย CSS (บทเรียนจากเฟส 13) = ได้ performance
      มาฟรี · ปลอดภัยเพราะ droppable ของวันที่ล็อกถูก `disabled` อยู่แล้ว ลากมาวางไม่ได้ตั้งแต่แรก
      · ใช้สูตร `collapsed = locked && !manuallyExpanded` ไม่ใช่ effect sync — ปลดล็อกแล้วกางเองทันที

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · ไม่ต้องเปิด API อะไรเพิ่ม (Open-Meteo ไม่ต้องใช้คีย์)

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` สะอาด · ทดสอบจริงในเบราว์เซอร์ที่ 375px:
`/today` ขึ้นธีมมืดตามเครื่องถูกต้อง ปุ่มสลับทำงาน · การ์ดฉุกเฉินกางแล้วได้เบอร์เวียดนามในวัน `d0` และ
เบอร์เกาหลี + โรงพยาบาลใหญ่ 3 แห่งในวัน `d1` · หน้าแผนขึ้นชิปครบ 11 วัน กดแล้วกระโดดถูกวัน ไฮไลต์ตามสกรอลล์
· วันที่ล็อกไว้ 2 วันยุบอยู่ กด "กางดู" แล้วกางครบ · console สะอาด · ไม่มีการแก้ข้อมูลจริงใน DB (กติกาข้อ 5)

---

### เฟส 18 — PWA + offline อ่านอย่างเดียว ✅ เสร็จ (12 ส.ค. 2026)

**กลับคำจากการตัดสินใจเดิม** (ดูหัวข้อ 1) — ผู้ใช้ขอเอง 11 ส.ค. 2026

- [x] `app/manifest.ts` (รูปแบบของ Next เวอร์ชันนี้ ไม่ใช่ `public/manifest.json`) + ไอคอน 4 ขนาด
      สร้างด้วยสคริปต์ PIL (ใบเมเปิลสี maple บนพื้น pine ตามธีมเว็บ) รวมตัว `maskable` สำหรับ Android
      และ `apple-touch-icon` สำหรับ iOS ที่ไม่อ่านไอคอนจาก manifest · `start_url: "/today"` ไม่ใช่ `/`
      เพราะเหตุผลที่ติดลงโฮมสกรีนคือใช้ตอนเที่ยวจริง เปิดแอปต้องเห็น "ตอนนี้ไปไหนต่อ" ทันที
- [x] service worker (`public/sw.js`) — เขียนเอง ไม่ใช้ Workbox (กฎที่ต้องการมีแค่ 3 ข้อ ไม่คุ้ม dependency
      และคู่มือ PWA ของ Next พูดถึงแค่ push notification ส่วนคู่มือ offline-support ระบุเองว่า full offline
      load ต้องเขียน SW เอง) · navigation = network-first แล้วตกไปแคช แล้วตกไปหน้า "ยังไม่เคยเปิดหน้านี้"
      · รูป/รายละเอียดสถานที่/เวลาเดินทาง/อากาศ = stale-while-revalidate · `_next/static` = cache-first
      · **ข้ามทุกอย่างที่ไม่ใช่โดเมนตัวเอง** (Supabase ต้องสดเสมอเพราะเป็นทางเขียนข้อมูล)
      · **ไม่ precache หน้า HTML ตอน install** — ด่าน PIN จะตอบ 307 ไป `/unlock` แล้วเราจะเก็บหน้า unlock
      ไว้เป็นเนื้อหาของ `/today` ถาวร · กันซ้ำอีกชั้นด้วย `!response.redirected` ก่อนเก็บทุกครั้ง
      · **ลงทะเบียนเฉพาะ production** — ตอน dev SW จะเสิร์ฟบันเดิลเก่าทับของที่เพิ่งแก้ HMR พังแบบหาสาเหตุยาก
- [x] snapshot ข้อมูลใน localStorage (`lib/localCache.ts`) — hook ข้อมูล **7 ตัว** (plans/stops/hotels/
      bookings/daySettings/customPlaces/overnightOverrides) ขึ้นจากแคชก่อนแล้วค่อยทับด้วยของสด
      หน้าจึงขึ้นทันทีไม่ต้องรอ Supabase และยังอ่านได้ตอนเน็ตหลุด · เขียน/อ่านใน `init()` ที่เป็น async
      จึงไม่ชน eslint `set-state-in-effect` และไม่เกิด hydration mismatch (effect รันหลัง hydrate)
- [x] **ไม่ทำ offline editing** — ตอนออฟไลน์ขึ้นแถบ "📴 เน็ตหลุด — โหมดอ่านอย่างเดียว" บนสุดของทุกหน้า
      และหน้าแผน**บังคับล็อกทุกวันอัตโนมัติ** โดยใช้กลไก "ล็อกวัน" เดิมที่ปิดปุ่มแก้ครบทุกอันอยู่แล้ว
      (ที่จับลาก/ปรับเวลา/ลบ/โน้ต/รูป/ปุ่มแทรก/โหมดเดินทาง/droppable ของ dnd-kit) แทนการไล่ใส่
      `disabled` ทีละปุ่มใหม่ทั้งหน้า · ตัวนับ "ล็อกแล้ว n/11" ยังอ่านค่าจริงจาก DB ไม่ใช่ค่าที่ถูกบังคับ
- [x] อ่าน `node_modules/next/dist/docs/` ก่อนเขียนแล้ว (กติกาข้อ 2) — เจอ `experimental.useOffline` +
      `useOffline()` จาก `next/offline` ที่เวอร์ชันนี้มีให้ · **ตัดสินใจไม่ใช้** เพราะต้องเปิดธง experimental
      ที่เปลี่ยนพฤติกรรม navigation ทั้งเว็บ (คำขอที่ล้มจะค้างรอ retry แทน error) บนเว็บที่ต้องใช้จริง
      ตอนอยู่เกาหลีอีก 2 เดือน — และเมื่อมี SW เสิร์ฟ shell จากแคชแล้ว ประโยชน์ส่วนเพิ่มก็เหลือน้อย
- [x] เปิด `/sw.js` + `/manifest.webmanifest` ให้ผ่านด่าน PIN — เบราว์เซอร์ขอ 2 ไฟล์นี้แบบ**ไม่แนบคุกกี้**
      ถ้าโดนดักจะได้ HTML แทนไฟล์จริง → ลงทะเบียน SW ไม่ผ่านและปุ่ม "เพิ่มลงหน้าจอโฮม" ไม่ขึ้น

**บั๊กที่เจอระหว่างทำ (บันทึกไว้กันพลาดซ้ำ):** ตอนแรกอ่านสถานะเน็ตจาก `navigator.onLine` ตรงๆ
แล้วเจอของจริงว่า **เบราว์เซอร์ทดสอบคืน `false` ทั้งที่โหลดหน้าเว็บและคุย Supabase ได้ปกติ** — ผลคือเว็บ
ล็อกตัวเองเป็นอ่านอย่างเดียวถาวรโดยไม่มีทางกลับ ซึ่งแย่กว่าไม่มีฟีเจอร์นี้เลย · แก้เป็นใช้ `navigator.onLine`
เป็นแค่ **สัญญาณให้ไปตรวจ** แล้วยืนยันด้วยคำขอจริงไปที่ `/manifest.webmanifest` เสมอ (เริ่มต้นถือว่าออนไลน์
= ฝั่งที่แก้ข้อมูลได้) · ตรวจตอนเหตุการณ์ `online`/`offline`, ตอนกลับมาเปิดหน้าจอ (`visibilitychange` —
ท่าที่เกิดจริงที่สุดคือควักมือถือออกจากกระเป๋ากลางอุโมงค์) และยิงซ้ำทุก 15 วิ**เฉพาะตอนที่เชื่อว่าออฟไลน์**
· ผลพลอยได้: จับเคส "ต่อ WiFi อยู่แต่ไม่มีเน็ตออกนอก" ที่ `navigator.onLine` ไม่เคยจับได้ด้วย

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · หลัง deploy แล้วเปิดเว็บบนมือถือ 1 ครั้งเพื่อให้ SW ติดตั้ง
แล้วกด "เพิ่มลงหน้าจอโฮม" (iOS: ปุ่มแชร์ → Add to Home Screen) · **ต้องเปิด `/today` และ `/summary`
อย่างน้อยคนละครั้งตอนมีเน็ต** ถึงจะเปิดได้ตอนเน็ตหลุด (SW เก็บหน้าตอนเปิดจริง ไม่ได้ precache ไว้ล่วงหน้า)

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` + `npm run build` สะอาด ·
ทดสอบด้วย `next build && next start` ตามที่คู่มือ Next แนะนำ (ไม่ใช่ dev server):
SW ลงทะเบียนสำเร็จ scope `/` · `/manifest.webmanifest` เสิร์ฟด้วย MIME `application/manifest+json` ·
เปิด `/`, `/today`, `/summary` ครบแล้วมี shell 3 หน้า + ข้อมูล 33 รายการในแคช + localStorage 7 คีย์ ·
**แล้ว "ปิดเซิร์ฟเวอร์ทิ้ง" แล้วโหลด `/today` ใหม่ทั้งหน้า → ขึ้นครบทั้งหน้าจากแคช** (จุดถัดไป, รูป,
ปุ่มนำทาง, ธีมมืด) พร้อมแถบ "เน็ตหลุด" · เปิดหน้าที่ไม่เคยเข้า → ได้หน้า fallback ภาษาไทยถูกต้อง ·
จำลองเน็ตหลุด/กลับมาในเบราว์เซอร์: หลุด → แถบขึ้น + การ์ดวันล็อกทั้ง 11 วัน, กลับมา → แถบหาย +
เหลือ 2 วันที่ล็อกจริง · ไม่มีการแก้ข้อมูลจริงใน DB ระหว่างทดสอบ (กติกาข้อ 5)

---

### เฟส 19 — งานรื้อ performance ✅ เสร็จ 5 จาก 6 ข้อ (12 ส.ค. 2026)

ไม่มีข้อไหนกระทบการใช้งานจริงระหว่างทริป — ตั้งใจไว้ว่าตัดทั้งเฟสได้ แต่พอเฟส 15-18 จบเร็วกว่าที่คิด
จึงเก็บข้อที่เสี่ยงต่ำมาทำต่อเลย เหลือข้อสุดท้าย (Server Component) ที่ยังใหญ่เกินกว่าจะทำก่อนบิน

**ผลวัดจริง (เปิดหน้าแผน 1 ครั้ง, dev server, จอ 1400px — ก่อน → หลัง):**

| วัดอะไร | ก่อน | หลัง |
|---|---|---|
| request รวมทั้งหน้า | 147 | **90** |
| `/api/place-details` | 34 | **3** |
| `/api/place-photos` | 27 | **1** |
| ข้อมูลรูปที่โหลด | ~11.5 MB | **1.1 MB** |
| แผนที่ที่ mount ตอนเปิดหน้า (จอคอม) | 8-10 | **0** (mount เมื่อเลื่อนมาใกล้) |
| โหลด `trip_selections` | ทุกครั้ง | **ไม่โหลดแล้ว** |

- [x] **batch `/api/place-details`** — เพิ่ม `?queries=a|b|c` คืน `{ results: { [query]: {...} } }`
      ฝั่งเซิร์ฟเวอร์อ่าน `place_details_cache` ทีเดียวด้วย `.in()` แล้วยิง Google เฉพาะที่ยังไม่เคยแคช
      · ฝั่ง client เพิ่มชั้นรวมคำขอที่เกิดใน ~20ms เดียวกัน (แยกคิวตาม locale) — dedupe เดิมจากบั๊ก 9.2
      กัน "ยิงซ้ำที่เดิม" ได้แล้ว แต่กัน "หลายที่ในเวลาไล่เลี่ยกัน" ไม่ได้ · `?query=` เดี่ยวยังใช้ได้เหมือนเดิม
      (หน้า `/today` ที่ขอ `?live=1` จุดเดียวยังพึ่งอยู่) — **ไม่ได้ทำเวอร์ชัน Server Component** ตามที่เสนอไว้
      เป็นทางเลือก เพราะหน้าแผนเป็น client component ทั้งก้อนและ realtime อยู่แล้ว
- [x] **batch `/api/place-photos`** ด้วยกลไกเดียวกัน (ไม่ได้อยู่ในลิสต์เดิม แต่เป็นตัวที่ยิงถี่พอๆ กัน)
- [x] **lazy mount แผนที่ด้วย IntersectionObserver** — `hooks/useInViewOnce.ts` (`rootMargin: 300px`)
      กล่องเปล่าขนาดเท่าของจริงกันหน้ากระตุกตอนแผนที่โผล่ · **จงใจไม่ unmount ตอนเลื่อนพ้นสายตา**
      เพราะจะโหลด tile ซ้ำและเสียตำแหน่ง/ซูมที่ผู้ใช้ปรับไว้
- [x] **ลบระบบ slot เดิมทิ้ง** — ลบ `useSelections` + `useLegacyBootstrap` + `bulkInsert` + type
      `TripSelection` · ยืนยันก่อนลบแล้วว่า `trip_plans` มีข้อมูลจริง (`plan-default` / "แผนหลัก" สร้าง 6 ส.ค.)
      bootstrap จึงไม่มีทางทำงานอีก · `overallLoaded` ไม่ต้องรอตารางที่ไม่ได้ใช้อีกต่อไป
      · `Day.slots` ใน `data/itinerary.ts` **ยังอยู่แต่ไม่มีใครอ่านแล้ว** — เก็บไว้เป็นบันทึกการวางแผนรอบแรก
      ติดคอมเมนต์เตือนไว้ที่ type แล้ว ถ้าอยากลบจริงต้องเอา `Day.slots` ออกทั้งหมด
- [x] **รูปเล็กลงจริง แทนการใช้ `next/image`** — ต้นเหตุที่แท้จริงคือ `/api/place-photo` คืนรูปกว้าง
      800px เสมอ ทั้งที่รูปย่อในแถวจุดแวะกว้างจริง 40px · เพิ่มพารามิเตอร์ `?w=` (allowlist 160/400/800
      ไม่ใช่ตัวเลขอิสระ เพราะทุกค่าที่ต่างกันคือแคชอีกก้อนทั้งฝั่ง Next และ Vercel edge)
      `PlaceThumb` ขอ 160 · `PlaceCard`/`PhotoGallery` ขอ 400 · **ไม่ต้องใช้ `next/image`/`remotePatterns`
      เลย** — รูปมาจาก API route ของเราเองซึ่งเปลี่ยนขนาดที่ต้นทางได้ตรงๆ และไม่ต้องเพิ่ม proxy อีกชั้น
- [ ] **รวม realtime เหลือ channel เดียวต่อ plan** — **ตรวจแล้วไม่ทำ** `supabase-js` multiplex ทุก channel
      ผ่าน websocket เส้นเดียวอยู่แล้ว "10 เส้น" ที่วัดได้คือ 10 topic ไม่ใช่ 10 connection ของจริง
      ประโยชน์จึงน้อยกว่าที่ประเมินไว้ตอนวางแผนมาก ขณะที่ต้องรื้อ handler ของ 8 hook พร้อมกัน — ไม่คุ้มความเสี่ยง
      บนเว็บที่ต้องใช้จริงอีก 60 วัน
- [ ] **ยกส่วน static ขึ้น Server Component** — งานใหญ่สุดในลิสต์ **ยังไม่ทำ ทำหลังทริป**

**ยืนยันปิดเฟส:** `npm test` 61/61 · `lint` + `tsc --noEmit` + `build` สะอาด · ทดสอบจริงในเบราว์เซอร์
ครบทั้ง 3 หน้าที่จอ 1400px: หน้าแผนขึ้นครบ 11 วัน รูปย่อโหลดครบ 27 ใบ (`&w=160`), เลื่อนไปวัน `d1`
แล้วแผนที่ mount ขึ้นมา 1 ตัวถูกต้อง · `/today` ขึ้นการ์ดจุดถัดไป + เวลาเปิด-ปิดครบ ยิง place-details
ครั้งเดียว · `/summary` ขึ้นครบ 11 วัน · console สะอาด · ไม่มีการแก้ข้อมูลจริงใน DB (กติกาข้อ 5)

---

### เฟส 20 — ฟีดแบ็กจากการใช้จริงบนมือถือรอบ 2 ✅ เสร็จ (12 ส.ค. 2026)

ฟีดแบ็ก 4 ข้อหลังผู้ใช้ลองใช้จริงบน iPhone 16 Pro · **ไม่มี migration ใหม่** (คอลัมน์ `kind` ใน
`trip_stops` เป็น text อิสระ ไม่มี CHECK constraint จึงรับค่าใหม่ได้เลย — ดูคอมเมนต์ใน migration 0025)

- [x] **iOS ซูมหน้าจอเองตอนแตะช่องค้นหา** (ฟีดแบ็กข้อ 1) — iOS Safari บังคับซูมเมื่อ font ของช่องกรอก
      เล็กกว่า 16px ซึ่งช่องกรอกทั้งเว็บเป็น `text-sm` (14px) / `text-xs` (12px) หมดทั้ง 27 จุด
      · แก้ด้วยกฎเดียวใน `app/globals.css` บังคับ 16px เฉพาะ `max-width: 767px` แทนไล่แก้ทีละไฟล์
      (กฎอยู่นอก `@layer` จึงชนะคลาสยูทิลิตี้ของ Tailwind v4 เองโดยไม่ต้องใช้ `!important` —
      หลักการเดียวกับกฎ `button:active` ของเฟส 4)
      · **จงใจไม่ใช้ `maximum-scale=1` / `user-scalable=no`** เพราะปิดการซูมด้วยนิ้วทั้งเว็บ = ปัญหา
      accessibility (และ iOS สมัยใหม่ก็ไม่สนใจค่านั้นแล้ว)
      · **บั๊กที่เจอระหว่างทำ:** กฎรุ่นแรกเขียนเป็น "ขนาดตายตัว" แล้วไป **ย่อ** ช่อง PIN ในหน้า `/unlock`
      ที่เป็น `text-2xl` (24px) ลงเหลือ 16px — แก้เป็น "ขนาดขั้นต่ำ" โดยยกเว้น `.text-base` ขึ้นไปด้วย `:not()`
      · วัดจริงในเบราว์เซอร์: 375px → `text-sm` เป็น 16px, `text-2xl` ยังเป็น 24px, checkbox ไม่โดน ·
      1280px → `text-sm` ยังเป็น 14px ตามดีไซน์เดิม
- [x] **แวะที่พักกลางวันได้** (ฟีดแบ็กข้อ 2 — เช็คอิน / ฝากกระเป๋า / พักก่อนเที่ยวต่อ) — เดิมที่พักขึ้นเป็น
      anchor หัววัน/ท้ายวันเท่านั้น แทรกกลางวันไม่ได้ · เพิ่ม `kind: "hotel"` เป็นแถวจุดแวะจริง แทรกได้ทุก
      ตำแหน่งเหมือนร้านอาหาร/ข้ามเมือง (ปุ่ม `🏨 + แวะที่พัก` ท้ายการ์ด + `🏨 + แวะที่พักตรงนี้` ระหว่างจุด)
      · **`place_id` ฝังพิกัดไว้ในตัวเป็น `hotel@{lat},{lng}`** (รูปแบบเดียวกับ `hotelAnchorId` ของเฟส 9.1)
      `resolvePlace` จึง parse กลับเป็น `Place` ได้โดยไม่ต้องส่งตาราง `trip_hotels` เข้าไปทุกจุดที่เรียก
      (12 จุดใน 9 ไฟล์) — และเพราะใช้ id เดียวกับ anchor หัว-ท้ายวัน **แคชเวลาเดินทางจึงใช้ร่วมกันได้
      ไม่ยิง Routes API ซ้ำ** · ชื่อที่โชว์ดึงสดจาก `trip_hotels` ทับอีกชั้น เปลี่ยนโรงแรมแล้วแถวเปลี่ยนตาม
      (ต่างจากถ้าฝังชื่อไว้ใน id ซึ่งจะค้างเป็นของเก่า) · เพิ่ม 2 เทสต์ล็อกสัญญารูปแบบ id ระหว่าง 2 ไฟล์นี้
      เพราะถ้าหลุดจากกัน `resolvePlace` จะคืน null แล้วเวลาเดินทางหายเงียบๆ โดยไม่มี error
      · กัน `RouteSuggestionModal` ย้ายแถวนี้ (เหมือนแถว transfer) เพราะตำแหน่งมีความหมายเฉพาะที่ผู้ใช้วางเอง
      · ปุ่มขึ้นเฉพาะวันที่ตั้งที่พักไว้แล้ว — ตอนนี้คือปูซาน (`d1`-`d3`) กับซกโช (`d4`) เท่านั้น
- [x] **สถานี KTX/บัสจริงในตัวเลือก** (ฟีดแบ็กข้อ 3) — เพิ่ม 8 สถานีเข้า `data/transferPoints.ts`
      (ไฟล์เดิมที่คอมเมนต์เขียนไว้แต่แรกว่า "สนามบิน/สถานี" อยู่แล้ว) ครอบช่วงข้ามเมืองทั้ง 3 ช่วงของทริป:
      บัสปูซาน→ซกโช (15 ต.ค.) · ซกโช→คังนึง (16 ต.ค.) · KTX คังนึง→โซล (17 ต.ค.)
      · โผล่เป็นปุ่มเลือกด่วนใต้ช่อง "จาก"/"ไป" ใน `IntercityEditModal` (กดแล้วเติมชื่อให้ ไม่ต้องพิมพ์เอง)
      และเป็นปลายทางของแถว `kind="transfer"` ได้ด้วย — `TransferEditModal` จัดกลุ่ม ✈️ สนามบิน /
      🚉 สถานี แล้วเรียงเมืองของวันนั้นขึ้นก่อน (ลิสต์รวด 11 อันหาไม่เจอ)
      · ⚠️ **พิกัด/ชื่อทุกตัวดึงจาก Places API ด้วย query ภาษาเกาหลี ไม่ได้พิมพ์จากความจำ** — เพราะเมืองเดียว
      มีสถานีหลายแห่ง: **ซกโชมี 2 แห่งห่างกัน ~2.3 กม.** (고속버스터미널 บัสด่วนจากปูซาน อยู่ติดหาด ·
      시외버스터미널 บัสไปคังนึง อยู่เหนือเมืองใกล้ตลาด) ใส่ทั้งคู่พร้อมคำอธิบายกำกับว่าอันไหนใช้ตอนไหน
      · หมายเหตุ: Google มีแค่พิกัดทางเข้ารถไฟใต้ดินของ 서울역 (คลาดจากตัวสถานี KTX ~200 ม. รับได้)
- [x] **modal จัดการแผน แทน `window.prompt`/`confirm`** (ฟีดแบ็กข้อ 4) — `components/PlanEditModal.tsx`
      ใช้กล่องเดียวครอบทั้งสร้าง/เปลี่ยนชื่อ/ลบ · ตอนลบบอกชัดว่าอะไรหายอะไรไม่หาย (**ตรวจ cascade จริงใน
      โค้ดก่อนเขียนข้อความ**: ลบ `trip_plans` แล้ว `trip_stops` + `trip_day_settings` หายตาม ส่วน
      `trip_hotels`/`bookings`/`checklist_items` ไม่มีคอลัมน์ `plan_id` = ใช้ร่วมกันทุกแผน ไม่หาย)
      · **ฟีเจอร์นี้มีอยู่ก่อนแล้ว** ผู้ใช้ไม่เห็นเพราะปุ่ม "ลบแผนนี้" ขึ้นเฉพาะตอนมีแผนมากกว่า 1 แผน
      (ตอนนี้มีแผนเดียว) — งานรอบนี้คือเปลี่ยนหน้าตาให้เข้ากับเว็บ ไม่ใช่สร้างระบบใหม่

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · ไม่ต้องเปิด API เพิ่ม

**ยืนยันปิดเฟส:** `npm test` **63/63 ผ่าน** (เพิ่ม 2 เทสต์ id ที่พัก) · `lint` + `tsc --noEmit` + `build`
สะอาด · ทดสอบข้อ 1 จริงในเบราว์เซอร์ที่ 375px และ 1280px ครบ (ตัวเลขด้านบน)
⚠️ **ข้อ 2-4 ยังไม่ได้ตรวจด้วยตาในเบราว์เซอร์** — dev server ติดด่าน PIN และผมไม่กรอกรหัสผ่านเอง
ครอบด้วย typecheck/lint/build/unit test แทน · **แนะนำให้ลองจริงบนมือถือหลัง deploy**: แทรก "แวะที่พัก"
ที่วันปูซาน, เปิด modal ข้ามเมืองวันที่ 15 ต.ค. ดูปุ่มสถานี, กด "+ แผนใหม่" ดู modal ใหม่

---

## 3.7 รอบตรวจ UX/UI ทั้งเว็บ (12 ส.ค. 2026) — เฟส 21

เฟส 1-20 โฟกัส "ฟีเจอร์ครบไหม / เลขถูกไหม" มาตลอด รอบนี้เป็นรอบแรกที่ตรวจ **ชั้นปฏิสัมพันธ์**
(feedback ตอนกด · กันพลาด · คีย์บอร์ด/โฟกัส · ลำดับสิ่งที่เห็นก่อน) ผู้ใช้สั่งแก้ทั้ง 4 กลุ่มที่เจอ
และอนุญาตให้จัดโครงหน้าแผนใหม่ได้

### เฟส 21.1 — ของกลางที่ใช้ซ้ำทั้งเว็บ ✅

- **`components/Modal.tsx` + `hooks/useDismissable.ts` (ใหม่)** — โมดัล 8 ตัวเคยก๊อป shell เดียวกัน
  ไปแปะทีละไฟล์ และ**ทุกตัว**ขาดเหมือนกันหมด: `role="dialog"` / `aria-modal` / Esc / ล็อกโฟกัส /
  คืนโฟกัสตอนปิด / `aria-label` ที่ปุ่ม ✕ · รวมมาที่เดียวแล้ว refactor ครบทั้ง 8 ไฟล์
  (`TransferEdit` `NearbyPlaces` `PlanEdit` `RouteSuggestion` `PlaceDetail` `BookingEdit`
  `Intercity` `HotelEdit`) · `BottomSheet` ของคลังสถานที่ใช้ `useDismissable` ร่วมด้วย
  ⚠️ **ห้ามย้ายการโฟกัสเข้ากล่องไปไว้ใน `requestAnimationFrame`** — rAF ไม่ทำงานตอนแท็บถูกซ่อน
  โฟกัสจะค้างอยู่นอกกล่องแบบเงียบๆ (เจอของจริงตอนทดสอบ) ต้องทำ synchronous ใน effect
  ซึ่งปลอดภัยเพราะ React ใส่ `autoFocus` ให้ตั้งแต่ commit phase ที่จบก่อน `useEffect` เสมอ
- **`lib/toast.ts` + `components/ToastHost.tsx` (ใหม่)** — module singleton + `useSyncExternalStore`
  แบบเดียวกับ `hooks/useDarkTheme.ts` เรียกจาก hook ไหนก็ได้ไม่ต้อง prop drill ·
  `showToast(kind, msg)` / `showUndoToast(msg, onUndo)` (ค้าง 8 วิ) · mount ที่ `app/layout.tsx`
  ได้ครบ 3 หน้า · ข้อความซ้ำจะต่อเวลาอันเดิมแทนซ้อนกันเป็นตั้ง (reorder เขียนหลายแถวรวด)
- **กรอบโฟกัสทั้งเว็บ** — เดิมมี `focus:outline-none` 25 จุดแต่ไม่มี focus ring เลยสักอัน
  แก้ด้วยกฎ `:focus-visible` **นอก `@layer`** ใน `globals.css` (ชนะคลาสยูทิลิตี้ Tailwind v4
  โดยไม่ต้อง `!important` — หลักการเดียวกับกฎ `button:active` เดิม) ไม่ต้องไล่แก้ทั้ง 25 จุด
  เพราะ `:focus` กับ `:focus-visible` เป็นคนละสถานะ · พื้นเข้มใช้คลาส `.focus-ring-on-dark`
  สลับเป็นสีทอง (หัวเว็บ 3 หน้า + หัวการ์ดวัน)

### เฟส 21.2 — ความน่าเชื่อถือของการเซฟ + กันลบพลาด ✅

- **`lib/writeGuard.ts` (ใหม่)** — เดิม **0/21 hook** อ่านค่า `error` จาก Supabase เลย เซฟไม่ติดแล้ว
  เงียบสนิท · ตอนนี้ทุกจุดเขียนใน hook ทั้ง 9 ตัวห่อด้วย `writeGuard` แล้ว พังแล้ว toast บอก
  พร้อม `reload()` ดึงของจริงมาทับ state ที่เดาไว้ (ตรงกว่าการเขียน rollback รายฟิลด์ทุก action)
  · จงใจ **ไม่เงียบตอนออฟไลน์** เพราะ `navigator.onLine` เชื่อไม่ได้ เอามาใช้แค่เลือกคำพูด
- **`addStop` เดิมไม่ตั้ง local state เลย** รอ realtime echo อย่างเดียว → insert พัง = กดปุ่ม +
  แล้วไม่มีอะไรเกิดขึ้น ไม่มีข้อความบอก (แถม `flashNewStop` ไฮไลต์ id ที่ไม่มีจริง) แก้เป็น optimistic แล้ว
- **undo แทน confirm** — `removeStop`/`removeItem` คืน snapshot ทั้งแถว (โน้ต/รูป/โหมดเดินทาง/
  `order_index`) ให้ทำปุ่ม "เลิกทำ" ได้ · ครอบคลุม: ลากทิ้งคลัง · ✕ ที่แถวจุดแวะ · ✕ ของที่ต้องเตรียม ·
  ลากข้ามเมือง (เดิมเป็น `window.confirm` ขวางทาง ทั้งที่วันทางผ่านเที่ยว 2 เมืองได้จริง)
- **ลบตั๋ว/booking ยังยืนยันก่อน** (`ConfirmModal`) — เดิมกด "ลบ" ทีเดียวหายพร้อมไฟล์แนบ ไม่ถามสักคำ
  · ที่นี่ใช้กล่องยืนยันไม่ใช่ undo เพราะไฟล์ใน Storage เอากลับจากในเว็บไม่ได้

### เฟส 21.3 — จัดโครงหน้าแผนใหม่ ✅

- **`components/TripPrepPanel.tsx` (ใหม่)** — ยุบ ที่พัก + ตั๋ว + ของที่ต้องเตรียม เป็นบล็อกเดียว
  ปิดไว้ก่อนบนมือถือ (จอ `lg:` กางไว้ตามเดิม) จำสถานะที่ `localStorage: trip-prep-open`
  หัวบล็อกโชว์ "ที่พัก 4/4 · ตั๋ว N · ของที่ต้องเตรียม X/Y" และขึ้นสีเตือนถ้าที่พักยังไม่ครบ
- **`DayJumpBar` ย้ายขึ้นบนสุดใต้หัวเว็บ** — เดิมอยู่ใต้แผงทั้งสาม ต้องเลื่อนผ่านไปก่อนมันถึงจะติดบนจอ
  = ใช้ไม่ได้ตอนที่ต้องใช้ที่สุด · **ชิปที่ไฮไลต์เลื่อนเข้ามาในจอเอง** (เดิมดูวันที่ 9 แล้วชิปหลุดออกนอกจอ)
  มี flag กันชิงเลื่อนตอนผู้ใช้กำลังปัดแถบเอง
- **`TripHeader` เตี้ยลง + `TripSettingsModal` (ใหม่)** — ย้าย ชื่อคุณ / เลือกแผน / สร้าง-เปลี่ยนชื่อ-ลบแผน /
  ล็อกทุกวัน เข้าปุ่ม ⚙️ (ของที่ตั้งครั้งเดียวแล้วแทบไม่แตะอีก) · เหลือบนหัวแค่ชื่อทริป ชื่อแผน จำนวนจุด
  · **จุดแดงบน ⚙️ เมื่อยังไม่ได้ใส่ชื่อ** เพราะช่องกรอกหลบเข้าโมดัลแล้วไม่ได้เห็นเอง
- **`components/InsertBetweenRow.tsx` (ใหม่)** — แถวคั่นระหว่างจุดเดิมมีปุ่มตัวหนังสือ 11px สามอัน
  **ทุกช่องว่าง** (วันละ 6-7 แถว = กำแพงตัวหนังสือที่สูงกว่าตัวจุดแวะเอง) เปลี่ยนเป็นปุ่ม `+` เล็กๆ
  กดแล้วค่อยกาง · ปุ่มท้ายการ์ดเหลือ "+ เพิ่มสถานที่ให้วันนี้" เต็มความกว้าง ที่เหลือยุบใต้ "อื่นๆ"

### เฟส 21.4 — `/today` + เก็บรายละเอียด ✅

- skeleton แทนข้อความ "กำลังโหลด..." เปล่าๆ (ให้เหมือนหน้าแผนที่มี `DayCardSkeleton` อยู่แล้ว)
- **"✅ มาถึงแล้ว" มี undo** · แถบ "ช้ากว่าแผน" ได้ `role="status"` (มันโผล่เองตามเวลา ไม่ได้เกิดจากการกด)
- **ปัดซ้าย/ขวาบนหัวการ์ดเปลี่ยนวันได้** (ปุ่ม ‹ › ยังอยู่ครบ) — เช็คว่าแนวนอนชนะแนวตั้งชัดเจนก่อน
  ไม่งั้นจะไปแย่งกับการสกรอลล์
- **`/summary` รองรับธีมมืดแล้ว** ใช้โทเคนชุดเดียวกับ `/today` + มีปุ่มสลับธีมของตัวเอง
  (เดิมกลางดึกที่เกาหลีกดจาก `/today` มาหน้านี้แล้วโดนพื้นครีมสว่างจ้า ทั้งที่เป็นสองหน้าที่ใช้ต่อกันที่สุด)
  · `@media print` พลิกโทเคนกลับเป็นชุดสว่างเสมอ ไม่งั้นสั่งพิมพ์ตอนธีมมืดได้กระดาษหมึกดำเต็มแผ่น
  · **หน้าแผน `/` จงใจไม่ทำธีมมืด** — ใช้ `bg-white`/`text-ink` ตายตัวหลายร้อยจุด และเป็นหน้าที่ใช้
  ตอนวางแผนที่บ้าน ไม่ใช่ตอนอยู่เกาหลีกลางดึก (ถ้าอยากได้ค่อยแยกเป็นงานของมันเอง)
- `BottomNav` ได้ `aria-label` + `aria-current="page"` และเปลี่ยนมาใช้โทเคนธีม

**ผู้ใช้ต้องทำ:** ไม่มี migration ใหม่ · ไม่ต้องเปิด API เพิ่ม · ไม่แตะ schema

**ยืนยันปิดเฟส:** `lint` + `tsc --noEmit` + `build` + `npm test` (63/63) สะอาด ·
ตรวจในเบราว์เซอร์จริงที่ 375px และ 1280px แล้ว (dev server ปลดล็อกโดยผู้ใช้):
ลำดับ DOM = แถบวัน → เตรียมทริป → การ์ดวัน · แผงเตรียมทริปปิดอยู่บนมือถือ (`aria-expanded=false`) ·
ชิปวันที่ active เลื่อนตามเอง · **กรอบโฟกัสขึ้นจริงบน `<input>` ที่มีคลาส `focus:outline-none`**
(computed `outline: solid 2px` — พิสูจน์ว่ากฎนอก `@layer` ชนะ Tailwind) · โมดัลครบวงจร
(`role=dialog` + `aria-modal` + `aria-labelledby` + โฟกัสเข้ากล่อง + Esc ปิด + คืนโฟกัสให้ปุ่มเดิม +
คืน `body.overflow`) · toast + undo ของจริงบน `/today` (ติ๊กมาถึง → กดเลิกทำ → `visited_at` กลับเป็น null
และข้อมูลทริปกลับสภาพเดิมครบ) · ปัดเปลี่ยนวันได้ทั้งซ้าย/ขวา และปัดแนวตั้งไม่เปลี่ยนวัน · `/summary` ธีมมืด

**⚠️ ที่ยังไม่ได้ตรวจด้วยตา:** เส้นทาง toast ของ **error** (`writeGuard` ตอนเขียนพังจริง) — ต้องทำให้
Supabase ตอบ error จริงถึงจะเห็น เลี่ยงไว้เพราะเป็นฐานข้อมูลทริปจริง · ครอบด้วย typecheck/lint/build แทน

### บั๊ก 21.5 — สถานที่ที่เพิ่มเองไป resolve เป็นร้านชื่อเดียวกันคนละประเทศ ✅ แก้แล้ว (12 ส.ค. 2026)

**อาการที่ผู้ใช้เจอ:** ร้าน "Cup & Cup" (คาเฟ่ที่กวางอัลลี ปูซาน) แผนที่ในหน้ารายละเอียดชี้ไปร้านในไทย

**ต้นเหตุ:** `custom_places` เก็บ `maps_query` เป็น**ชื่อดิบล้วนๆ** ที่ Google คืนมา ([NearbyPlacesModal.tsx](components/NearbyPlacesModal.tsx))
ต่างจาก `data/places.ts` ที่ต่อท้ายชื่อเมืองเสมอ ("Jagalchi Market Busan") แล้วทุกเส้นทางที่คุยกับ Google
เอาข้อความนี้ไปยิง `searchText` **โดยไม่ส่งพิกัดกำกับเลย** ผลคือชื่อที่ซ้ำกันทั้งโลกไป resolve ผิดร้าน:
- `place_details_cache` แถว `"Cup & Cup"` เก็บ ⭐5 (611 รีวิว) + ที่อยู่ `8000 Coit Rd #800, Plano, TX` = **ร้านชานมที่ Texas**
  (ของจริงคือ 컵앤컵커피 ⭐4.5 · 125 รีวิว · 11:00–21:30) — เวลาเปิด-ปิด/รีวิวที่โชว์ทั้งทริปจึงเป็นของร้านอื่น
- `GoogleMapEmbed` + ลิงก์ Google Maps ในแท็บ 🗺️ ส่ง `q=Cup & Cup` เฉยๆ Google เลยเดาจากตำแหน่งคนเปิดเว็บ = ได้ร้านในไทย
- ปุ่มนำทางใน `/today` **ไม่โดน** เพราะใช้ `lat`/`lng` ตรงๆ — เลยเป็นอาการ "บางปุ่มไปถูก บางปุ่มไปไทย"

**ทางแก้:** เก็บ Google place id ไว้ตั้งแต่ตอนกดเพิ่ม แล้วใช้ id ระบุตัวร้านแทนการค้นด้วยชื่อ
- [x] migration `0027_custom_places_google_id.sql` — คอลัมน์ `google_place_id` + ลบแถวแคชที่ resolve ผิด
- [x] [lib/placeQuery.ts](lib/placeQuery.ts) — `placeQueryKey()` คืน `place_id:ChIJ...` เมื่อมี id · ตกไปใช้ข้อความเมื่อไม่มี
      (ที่คัดไว้เองใน `PLACES` ไม่ต้องมี id เพราะ `mapsQuery` มีชื่อเมืองต่อท้ายอยู่แล้ว) · คีย์นี้เป็นคีย์ของ
      `place_details_cache`/`place_photo_cache` ด้วย แถวเก่าที่ผิดจึงถูกทิ้งไปเองโดยไม่ต้องแก้ค่าในแถว
- [x] `lookupPlace()` ใน [lib/googlePlaces.ts](lib/googlePlaces.ts) — คีย์เป็น `place_id:` → ยิง **Place Details** ตรงๆ
      (ตัดคำนำหน้า `places.` ใน fieldMask ให้เอง) · เป็นข้อความ → ตกไป `searchText` เหมือนเดิม ·
      `/api/place-details` กับ `/api/place-photos` เรียกผ่านตัวนี้ตัวเดียว
- [x] `googleMapsPlaceUrl()` ใน [lib/mapLinks.ts](lib/mapLinks.ts) — รู้ id ใช้ `place/?q=place_id:...` ที่ชี้ร้านเป๊ะ
- [x] แถวเก่า 33 แถวเติมย้อนหลังด้วย [scripts/backfill-place-ids.mjs](scripts/backfill-place-ids.mjs) — ค้นด้วยชื่อเดิม
      **ในกรอบ 500 ม. รอบพิกัดที่บันทึกไว้** แล้วรับเฉพาะผลที่ห่างจากพิกัดเดิมไม่เกิน 300 ม. (วิธีเดียวกับที่เติม
      ข้อมูลที่พักในเฟส 16) · รันแบบดูก่อนได้ ไม่ใส่ `--write` = ไม่เขียนอะไรลง DB

**ยืนยันแล้วกับ Google จริง:** id `ChIJg9DiTibtaDURDaC-pP90JyU` → `컵앤컵커피` · 부산광역시 수영구 광안해변로 177 ·
⭐4.5 (125) · 카페 · 11:00–21:30 — ตรงกับร้านที่ผู้ใช้เลือกไว้ ไม่ใช่ร้านที่ Texas อีกแล้ว

**หมายเหตุ:** เจอตอนไล่บั๊กนี้ว่า `custom_places` มีแถวซ้ำอยู่หลายชุด (Cup & Cup 6 แถว · Kungkungtta,
Mun Dining, Gamcheon, ตลาดจากัลชิ อย่างละ 2) — น่าจะกด "ลงคลัง" รัวก่อนปุ่มจะ disable
มีแค่แถวเดียวต่อร้านที่ถูกใช้ในแผนจริง ที่เหลือเป็นการ์ดค้างในคลัง · SQL ลบให้เลือกรันเองอยู่ท้าย migration 0027

---

## 3.8 ฟีดแบ็กจากการใช้จริงรอบ 3 (12 ส.ค. 2026) — เฟส 22

ผู้ใช้แจ้ง 4 ข้อ: โน้ตหายตอนลากกลับคลัง · ไม่รู้ว่ารูปที่เพิ่มไปโผล่ที่ไหน · หน้าสรุปข้อมูลน้อยไป ·
หน้า ตม. ยังมีภาษาไทย

### เฟส 22.1 — โน้ตติดไปกับสถานที่ตอนเก็บกลับคลัง ✅

**อาการ:** ลากจุดแวะจากวันไปวางที่คลัง = `removeStop` ลบทั้งแถว โน้ต (0013) กับรูป (0023)
หายไปด้วย · ปุ่ม "เลิกทำ" บน toast กู้ได้แค่ 8 วิ พ้นจากนั้นหายถาวร ทั้งที่ในหัวผู้ใช้
"เก็บกลับคลัง" คือ **พักไว้ก่อน** ไม่ใช่ทิ้ง

- [x] migration `0028_place_notes.sql` — ตาราง `place_notes (plan_id, place_id, note, photo_url)`
      แยกตามแผนเหมือน `trip_stops` (โน้ตเป็นของแผนนี้ ไม่ใช่ของทริปทั้งก้อนแบบ `hidden_places`)
      · PK มี `plan_id` อยู่ในตัวแล้ว จึงไม่ต้อง `replica identity full` แบบ 0009
- [x] [hooks/usePlaceNotes.ts](hooks/usePlaceNotes.ts) — โหลด/subscribe/`stashNote`/`clearNote`
      **ยังไม่รัน migration = `available` เป็น false แล้วเขียนกลายเป็น no-op เงียบๆ** ไม่เด้ง toast
      "บันทึกไม่สำเร็จ" ทุกครั้งที่ลากการ์ด (พฤติกรรมกลับไปเหมือนก่อนเฟส 22 เป๊ะ)
- [x] ลากกลับคลัง → เขียนโน้ต+รูปลง `place_notes` ก่อนลบแถว · toast เปลี่ยนเป็น
      "เก็บ X กลับคลังแล้ว — โน้ตติดไปด้วย" · กด "เลิกทำ" = คืนแถว (ซึ่งมีโน้ตในตัว) แล้วล้างที่ฝากไว้
      ไม่งั้นโน้ตเดียวกันค้างอยู่ 2 ที่
- [x] ลากลงวันอีกครั้ง → `addStopWithStashedNote` ใน [app/page.tsx](app/page.tsx) ใส่โน้ต+รูปกลับ
      ให้จุดแวะใหม่แล้วล้างที่ฝากไว้ · เป็นทางเข้าเดียวของทั้งหน้า (ลากการ์ด · ปุ่ม "+ เพิ่มลงวันนี้" ·
      ปุ่มยืนยันในโมดัลรายละเอียด) ทุกทางจึงได้โน้ตคืนเหมือนกันหมด
- [x] การ์ดในคลังโชว์ "📝 โน้ต" + ใช้**รูปที่เราถ่ายเองแทนรูป Google** พร้อมป้าย "📷 รูปของเรา"
      — ไม่งั้นไม่มีทางรู้เลยว่าที่ฝากไว้มีอะไร
- [x] **ปุ่ม ✕ ก็ฝากโน้ตเหมือนกัน** — บนมือถือการ์ดในคลัง `draggable={false}` (กันนิ้วเลื่อนดูคลังไม่ได้
      ดูคอมเมนต์ใน PlaceSidebar) ปุ่ม ✕ จึงเป็น**ทางเดียว**ที่จะเอาจุดแวะออกบนมือถือ ถ้าไม่ทำด้วย
      ฟีเจอร์นี้จะใช้ได้แต่บนจอใหญ่ ซึ่งไม่ใช่เครื่องที่ใช้วางแผนจริงระหว่างทาง
- [x] `addStop` คืน `undefined` เมื่อเขียนไม่ผ่าน (เดิมคืน id เสมอ) — กันเคสเขียนพังแล้วโน้ตที่ฝากไว้
      ถูกล้างทิ้งทั้งที่จุดแวะไม่ได้ถูกสร้างจริง

**ทดสอบกับฐานข้อมูลจริงแล้วครบทุกเส้นทาง (12 ส.ค. 2026)** — ใช้จุดแวะ `Haeundae Blueline Park`
วัน 14 ต.ค. (โน้ต "จองล่วงหน้า 2 สัปดาห์") **เก็บ snapshot ทั้งแถวก่อน แล้วคืนค่าเดิมครบทุกฟิลด์
รวม `updated_at` เมื่อจบ** (ยืนยันด้วย curl ว่า d3 กลับมาเป็น order 0/1/2 ชุดเดิม · `place_notes` ว่าง ·
ทั้งแผนยัง 9 จุดเท่าเดิม):
1. กด ✕ → `place_notes` มีแถวโน้ต · แถว `trip_stops` หายไป
2. การ์ดในคลังขึ้น "📝 จองล่วงหน้า 2 สัปดาห์"
3. กด "+ เพิ่มลงวันนี้" → จุดแวะใหม่**ได้โน้ตกลับมา** + เดาโหมดเดินทางให้เป็น `transit` + `place_notes` ว่าง
4. ลากแถวไปวางที่คลัง → toast ขึ้น "เก็บ 📸 Haeundae Blueline Park - Mipo Station กลับคลังแล้ว — โน้ตติดไปด้วย"
5. ลากการ์ดจากคลังกลับลงวัน → ได้โน้ตคืนอีกครั้ง + `place_notes` ว่าง
6. ลากไปคลังแล้วกด "เลิกทำ" → แถวกลับมาที่ `order_index` เดิมพร้อมโน้ตในตัว **และ `place_notes` ถูกล้าง**
   (ถ้าไม่ล้าง โน้ตเดียวกันจะค้างอยู่ 2 ที่)

### เฟส 22.2 — รูปที่เพิ่มเองโผล่ที่ไหนบ้าง ✅

เดิมโชว์แค่ 2 ที่: แถวจุดแวะในหน้าแผน กับการ์ด "จุดต่อไป" ใน `/today` — หน้าสรุปกับหน้ารายละเอียด
ไม่เคยเห็นเลย ตอนนี้เพิ่ม:

- [x] **หน้ารายละเอียดสถานที่** — บล็อก "โน้ตของเรา" (โน้ต + รูป) อยู่**บนสุดก่อนข้อมูล Google**
      ไม่ต้องเลื่อนผ่านเวลาเปิด-ปิด/รีวิวคนอื่นไปหา · หัวข้อรูปจาก Google เปลี่ยนชื่อให้ไม่สับสน
- [x] **หน้าสรุป** — รูปเราขึ้นเป็นรูปย่อของแถวนั้น (ไม่มีค่อยตกไปใช้รูปแรกจาก Google)
- [x] **การ์ดในคลัง** — รูปที่ฝากไว้ตอนเก็บกลับคลัง (เฟส 22.1)

⚠️ URL สองชนิดนี้ต่างกัน: ของ Google ผ่าน `/api/place-photo` ที่รับ `?w=` ต่อท้ายได้ (ดู
`photoUrlAtWidth`) · ของเราเป็น public URL ของ Supabase Storage ตรงๆ ที่ **ไม่มี query string**
ต่อ `&w=400` เข้าไปแล้วพังเป็น 404 ทันที — ต้องแยกทางกันเสมอ

### เฟส 22.3 — หน้าสรุปกดดูสถานที่ได้ + มีรูปตัวอย่าง ✅

- [x] แถวจุดแวะกลายเป็นปุ่ม กดแล้วเปิด `PlaceDetailModal` ตัวเดียวกับหน้าแผน (ยังอ่านอย่างเดียว —
      ไม่ส่ง `onConfirm` เข้าไป จึงไม่มีปุ่มแก้อะไรเลย) · แถวข้ามเมือง/แวะที่พักไม่มีอะไรให้ดูต่อ กดไม่ได้
- [x] รูปย่อ 48px หน้าแถว + เรทติ้ง/ประเภทร้าน + เวลาเปิด-ปิดของวันนั้น — ข้อมูลชุดเดียวกับที่การ์ด
      ในคลังใช้ (`usePlaceDetails` แคชร่วมกันทั้งแท็บ) จึงไม่มีคำขอเพิ่ม
- [x] เรทติ้งกับเวลาเปิด-ปิดแยกคนละบรรทัด — บรรทัดเดียวบนจอ 375px โดนตัดเหลือ "🕐 Monday: Op..."

### เฟส 22.4 — ไล่ภาษาไทยออกจากหน้า ตม. ✅

**ต้นเหตุ:** สถานที่ที่เพิ่มเองระหว่างทางเก็บชื่อที่ Google คืนมาตอน `languageCode: "th"` ลง
`custom_places.name_th` แล้ว `name_en` เป็น **null ทั้ง 35 แถว** (ยืนยันด้วย curl) — `resolvePlace`
fallback `name_en ?? name_th` เอกสารที่ยื่นเจ้าหน้าที่จึงมี "ตลาดปลาจากัลชิ" ปนอยู่
(ตัวที่คัดไว้เองใน `data/places.ts` มี `nameEn` ครบ ไม่โดน)

- [x] `/api/place-name?queries=a|b|c&lang=en` (ใหม่) — คืน**แค่ชื่อ** ไม่พ่วงเรทติ้ง/เวลาเปิด-ปิด
      ⚠️ **จงใจไม่เก็บลง `place_details_cache`** เพราะคอลัมน์ `name_local`/`locale` ที่นั่นเก็บได้
      ภาษาเดียวต่อแถวและถูกจองไว้ให้ ko/vi (เฟส 14) — เขียนทับด้วย en เมื่อไหร่ ชื่อเกาหลีที่ `/today`
      ใช้ส่งเข้า Naver/Kakao หายทันที · อาศัย fetch cache 30 วันของ Next ใน `lookupPlace` แทน
- [x] [lib/latinScript.ts](lib/latinScript.ts) — `looksLatin()` เช็คด้วย `\p{Script=Latin}` รายตัวอักษร
      **เวียดนามผ่าน** ("Phở Gia Truyền Bát Đàn") ไทย/เกาหลีไม่ผ่าน · ยิงถาม Google เฉพาะตัวที่ไม่ผ่าน
      — ของจริงทั้งทริปเหลือ **1 คำขอ 1 สถานที่** (ยืนยันจาก network log)
- [x] [ImmigrationSheet.tsx](components/ImmigrationSheet.tsx) ใช้ชื่ออังกฤษที่ได้มาแทน · ขอไม่ได้
      (ออฟไลน์/Google ไม่มีให้) ก็ใช้ชื่อเดิม ดีกว่าเว้นว่างจนเจ้าหน้าที่อ่านไม่ออกว่าไปไหน
- [x] **หน้าสรุปโหมด EN ก็โดนบั๊กเดียวกัน** (ไม่ใช่แค่หน้า ตม.) — แก้ด้วย `placeNameEn()` ตรรกะเดียวกัน
      · โหมดไทยส่งลิสต์ว่างเข้า hook = ไม่ยิงอะไรเลย ไม่มีค่าใช้จ่ายเพิ่มสำหรับคนที่ใช้ไทยตลอด
- [x] แก้ที่ต้นทางด้วย — `NearbyPlacesModal` ขอชื่ออังกฤษมาใส่ `name_en` ตั้งแต่ตอนกดเพิ่ม
      แถวใหม่จึงไม่ต้องพึ่งเส้นนี้อีก (แถวเก่า 35 แถวยังเป็น null อยู่ ปล่อยให้หน้า ตม. ถามเอาตอนเปิด)

**ยืนยันในเบราว์เซอร์จริง (dev server ที่ผู้ใช้เปิดค้างไว้):** `/summary?lang=en&for=immigration`
วัน 12 ต.ค. เดิมเป็น "Arban Hotel, Jeonpo Cafe Street, Gamcheon Culture Village, **ตลาดปลาจากัลชิ**"
→ ตอนนี้ "... **Jagalchi Market**" · ยิง `/api/place-name` แค่ครั้งเดียวต่อการเปิดหน้า (dedupe ด้วย
`inFlight` แบบเก็บ Promise ไม่ใช่แค่ธง — ไม่งั้น StrictMode รอบสองจะข้ามไปแล้วไม่เรนเดอร์ใหม่เลย)

**ยืนยันปิดเฟส:** `lint` + `tsc --noEmit` + `build` + `npm test` (68/68 — เพิ่มชุด `latinScript` 5 เทสต์)
· ผู้ใช้รัน `0027` + `0028` + `backfill-place-ids` แล้ว (ยืนยันด้วย curl: `custom_places` มี
`google_place_id` ครบ 36/36 แถว · ตาราง `place_notes` สร้างแล้ว) · ทดสอบ flow โน้ตกับข้อมูลจริง
ครบ 6 ขั้นตามด้านบนแล้วคืนค่าเดิมเรียบร้อย
· ก่อนรัน migration ก็เปิดหน้าได้ปกติ (ตาราง 404 → ฟีเจอร์ปิดตัวเองเงียบๆ ตามที่ออกแบบ — ตรวจแล้ว)

---

## 3.9 กรอกแผนปูซาน 3 วันจริง (12 ส.ค. 2026)

ผู้ใช้ส่งแพลนละเอียด 12-14 ต.ค. มาให้กรอกลงระบบ · **ลงใน `Plan A` (แผนที่ active) เท่านั้น
`แผนหลัก` (plan-default) ไม่ถูกแตะเลย** — สลับแผนกลับได้ทันทีถ้าไม่ชอบ

**จุดแวะ 15 จุด (`trip_stops`) + เวลาเริ่มวัน/โหมดกลับที่พัก (`trip_day_settings`)**
- `d1` 10:00 · แวะที่พัก (kind=`hotel`) → โชรยางมิลมยอน → จอนโพ → คัมชอน → จากัลชิ · กลับด้วยขนส่งสาธารณะ
- `d2` 09:00 · ซอมยอน (มื้อเช้า) → ฮึนยอล → นัมโพดง → ฮวังรยองซาน → พิซซ่าอีแจโม · กลับด้วยเดิน
- `d3` 08:30 · แฮอึนแด → บลูไลน์มิโพ → ยงกุงซา → Cup & Cup → ควังอัลลี · กลับด้วยขนส่งสาธารณะ

แถวที่พักของ `d1` ใช้ `kind="hotel"` (`hotel@35.15458,129.05736`) แทนที่จะเพิ่ม Arban Hotel
เป็น custom place เหมือนเดิม — ชื่อ/เบอร์โทรจึงมาจาก `trip_hotels` สดๆ และแคชเวลาเดินทางใช้ key
เดียวกับ anchor หัว-ท้ายวัน ไม่ยิง Routes API ซ้ำ

**สถานที่ใหม่ 6 แห่งใน `data/places.ts`** (เข้าคลังถาวร ไม่ใช่ custom place — มี `nameLocal`/
`addressLocal` ฝังในไฟล์ จึงใช้ได้ตอนเน็ตหลุดด้วย): `busan-seomyeon`, `busan-choryang-milmyeon`,
`busan-nampodong`, `busan-hwangnyeongsan`, `busan-cup-and-cup`, `busan-lee-jae-mo-pizza`
· นี่เป็น 2 แถวแรกที่เป็น `category: "restaurant"` ในคลัง (เดิมร้านอาหารอยู่ใน `custom_places` ล้วน)

**แก้พิกัดผิด 8 จุดในคลังปูซาน** — เทียบกับ Places API ทีละจุด (สคริปต์ชั่วคราว ไม่ได้เก็บไว้)
พิกัดผิดไม่มีอาการให้เห็น แต่ทำให้เวลาเดินทาง/ระยะทางทั้งวันเพี้ยนเงียบๆ:

| จุด | เพี้ยนไป | หมายเหตุ |
|---|---|---|
| `busan-huinnyeoul` | 4.0 กม. | พิกัดเดิมไม่ได้อยู่บนเกาะยองโดด้วยซ้ำ |
| `busan-oryukdo` | 2.9 กม. | |
| `busan-igidae` | 1.7 กม. | |
| `busan-blueline-mipo` | 1.3 กม. | |
| `busan-bay101` | 556 ม. | |
| `busan-jeonpo` | 509 ม. | |
| `busan-cheongsapo` | 383 ม. | |
| `busan-bupyeong-biff` | 295 ม. | |

**`data/itinerary.ts`** — `d1` เพิ่มเหตุการณ์ `anchor: "before"` เวลา 10:00 "ถึงย่านซอมยอน"
(รูปแบบเดียวกับ `d0`) ทำให้ค่าเริ่มต้นของช่อง "ออกเดินทาง" ตรงกับแผนจริงแทนที่จะเป็น 07:00
· `note` ของ `d2`/`d3` เดิมสลับกันอยู่ (d2 เขียนว่าวันแฮอึนแด ทั้งที่แผนใหม่ย้ายไป d3) แก้ให้ตรงแล้ว

**ข้อจำกัดที่เจอซ้ำ (ไม่ใช่บั๊ก):** Routes API ไม่คืนเส้นทาง `DRIVE`/`WALK` ในเกาหลีเลย
(กฎหมายส่งออกข้อมูลแผนที่ — ดูหัวข้อ "ข้อจำกัดสำคัญ") ยืนยันซ้ำรอบนี้ทุกคู่จุดที่ลอง ·
ช่วงแท็กซี่/เดินจึงเป็นค่าประมาณเส้นตรงเสมอ และต่ำกว่าความจริงมากบนถนนขึ้นเขาฮวังรยองซาน
(แอปโชว์ 4 น. ของจริง ~20 น.) — เขียนเวลาจริงคร่าวๆ ลง `note` ของจุดปลายทางไว้แล้ว 5 จุด

**ยืนยัน:** `lint` + `tsc --noEmit` + `npm test` (68/68) ผ่าน · เปิด `/summary` กับหน้าแผนบน dev
server จริง — timeline คำนวณครบทั้ง 3 วัน, `/api/travel-time` + `/api/place-details` ตอบ 200 ทุกคู่,
เวลาเปิด-ปิดของทุกจุดใหม่ขึ้นถูก

---

## 3.10 กรอกแผนซกโช-คังนึง-โซล 15-17 ต.ค. (12 ส.ค. 2026)

ต่อจาก 3.9 — ผู้ใช้ส่งแพลนเต็ม 11-17 ต.ค. มา ส่วนปูซาน (12-14) กรอกไปแล้วรอบก่อน รอบนี้คือ
ที่เหลือทั้งหมด · **ลงใน `Plan A` เท่านั้น `แผนหลัก` ไม่ถูกแตะ** (ยืนยันหลังเขียน: plan-default
ยังมี 9 แถวเท่าเดิม) · d4/d5/d6 ของ Plan A ว่างเปล่าอยู่ก่อนเขียน จึงเป็นการเพิ่มล้วน ไม่ทับของเดิม

**จุดแวะ 23 จุด (`trip_stops`) + `trip_day_settings`**
- `d4` 06:45 · ✈️ สถานีโนโพ (เดดไลน์ 07:30) → 🚌 บัสด่วน 5 ชม. → 🏨 ฝากกระเป๋าซกโช → หาดซกโช →
  ซกโชอาย → ตลาดซกโช → หมู่บ้านอาไบ · กลับด้วยขนส่งสาธารณะ
- `d5` 06:30 · ซอรัคซาน 5 ชม. → 🏨 เก็บกระเป๋าซกโช → คาเฟ่ริมหาด → ✈️ สถานีระหว่างเมือง (เดดไลน์
  15:45) → 🚌 บัสเข้าคังนึง → 🏨 เช็คอินคังนึง → ตลาดจุงอัง · กลับด้วยเดิน
- `d6` 08:30 · ป้าย BTS → เขื่อนโกบลิน → กาแฟอันมก → ✈️ สถานีคังนึง (เดดไลน์ KTX 13:30) →
  🚄 KTX 2 ชม. → 🏨 เช็คอินเมียงดง → เมียงดง → เมียงดงคโยจา → นัมซานทาวเวอร์ · กลับด้วยขนส่งสาธารณะ

ช่วงข้ามเมืองใช้ `kind="transfer"` (มี `transfer_target_time`/`_label` = เวลารถ/รถไฟออกจริง
ระบบจึงเตือนได้ถ้าจุดแวะก่อนหน้ากินเวลาจนไปไม่ทัน) คู่กับ `kind="intercity"` (ตัวเวลาเดินทางเอง)
— รูปแบบเดียวกับแถวไปสนามบินของ `d10` ที่ทำไว้เฟส 15 · แถวที่พักใช้ `kind="hotel"` เหมือน 3.9

**bookings 5 แถว + checklist 5 แถว** — สองตารางนี้**ว่างเปล่ามาตลอด**ตั้งแต่สร้าง (เฟส 16/18)
รอบนี้เป็นข้อมูลจริงชุดแรก มาจากหัวข้อ SUMMARY CHECKLIST ที่ผู้ใช้ส่งมา:
- ต้องจอง: Express Bus ปูซาน→ซกโช (d4), KTX คังนึง→โซล (d6), Sky Capsule (d3)
- ซื้อหน้างาน (ใส่ไว้ในลิสต์เดียวกันโดยขึ้นต้นชื่อว่า "ซื้อหน้างาน —" กันมานั่งกังวลว่าลืมจอง):
  กระเช้าซอรัคซาน, บัสซกโช→คังนึง
- checklist: `packing` = Naver/Kakao Map, Kakao T, T-money · `before_flight` = K-ETA, Q-Code

**สถานที่ใหม่ 4 แห่งใน `data/places.ts`**: `sokcho-beach`, `gangneung-goblin-breakwater`,
`gangneung-jungang-market`, `seoul-myeongdong-kyoja`

**แก้พิกัดผิด 13 จุด (ซกโช/คังนึง/โซล/ซูวอน)** — วิธีเดียวกับ 3.9 (เทียบ Places API ทีละจุด
เกณฑ์ ≥300 ม. ถึงแก้) รอบนี้เพี้ยนหนักกว่าปูซานมาก:

| จุด | เพี้ยนไป | หมายเหตุ |
|---|---|---|
| `gangneung-bts-bus-stop` | 15.7 กม. | พิกัดเดิมอยู่ในตัวเมืองคังนึง ไม่ใช่จูมุนจิน |
| `suwon-starfield-library` | 6.9 กม. | |
| `sokcho-osaek` | 6.3 กม. | |
| `gangneung-anmok` | 4.3 กม. | |
| `sokcho-eye` | 1.7 กม. · `gangneung-ojukheon` 1.7 กม. · `gangneung-jumunjin` 1.3 กม. | |
| `gangneung-gyeongpo` 969 ม. · `suwon-hwaseong` 619 ม. · `suwon-haenglidan` 575 ม. | | |
| `seoul-myeongdong` 474 ม. · `sokcho-abai-village` 371 ม. · `sokcho-market` 321 ม. · `seoul-insadong` 301 ม. | | |

`sokcho-seoraksan` **จงใจไม่ใช้ค่าจาก API** — "설악산국립공원" คืนจุดกึ่งกลางอุทยานกลางเทือกเขา
(38.1574,128.4356) ซึ่งห่างทางเข้าที่คนไปจริง 5 กม. เปลี่ยนไปใช้พิกัดสถานีกระเช้าฝั่งโซกงวอน
(38.1731,128.4891) แทน พร้อมคอมเมนต์กำกับไว้ในไฟล์ว่าทำไม · `seoul-yeonnamdong` (297 ม.)
ปล่อยไว้ — เป็นพิกัดกึ่งกลาง "ย่าน" ขยับแล้วไม่เปลี่ยนอะไร

**แก้ `mapsQuery` 3 จุดที่ Google resolve ผิดตัว** (คีย์นี้เป็นตัวดึงเวลาเปิด-ปิด/เรทติ้ง/รูป
ผิดตัวแล้วเวลาเปิด-ปิดที่โชว์เป็นของที่อื่นเงียบๆ): `gangneung-anmok` → `안목커피거리 강릉`
(เดิมได้ป้ายรถเมล์ 3 รีวิว แทนถนนคาเฟ่ 1,865 รีวิว) · `gangneung-goblin-breakwater` →
`도깨비 드라마 촬영지 주문진` (5 → 3,479 รีวิว) · `station-gangneung` → `강릉역 용지로 176`
(ป้ายรถเมล์หน้าสถานี 82 รีวิว → ตัวสถานี 405 รีวิว)

**บั๊กที่เจอตอนกรอก: แถว “แวะที่พัก” ของวันย้ายเมืองขึ้นชื่อโรงแรมผิด** — `d5` มีที่พัก 2 แห่ง
ในวันเดียว (เช้ากลับไปเอากระเป๋าที่ซกโช เย็นเช็คอินคังนึง) แต่ทุกจุดที่โชว์ชื่อโรงแรมของแถว
`kind="hotel"` ใช้ "ที่พักคืนนี้" อย่างเดียว แถวตอนเช้าจึงขึ้นชื่อโรงแรมคังนึง ทั้งที่พิกัด/เวลา
เดินทางถูกอยู่แล้ว · แก้ด้วย `hotelForStop()` ใน `lib/hotelLegs.ts` (เทียบ `place_id` กับ
`hotelAnchorId` ของทั้งสองที่ แล้วตกกลับไปใช้ที่พักคืนนี้ถ้าไม่ตรงอันไหนเลย) ใช้ครบทั้ง 3 ที่:
`DayStopsSection` → `SortableStopRow`, `/today`, `/summary` · เพิ่มเทสต์ 4 เคส (68 → 72)

**เรื่องที่ต้องบอกผู้ใช้ (แผนที่ส่งมาชนของจริง):**
- **กระเช้าซอรัคซานเปิด 09:00 (จ-ศ)** แต่แผนตั้งไว้ว่าขึ้น 07:30 — 16 ต.ค. เป็นวันศุกร์
  เขียนทางออกไว้ในโน้ตแล้ว (ช่วง 07:00-09:00 เอาไปเดินชินฮึงซา + ทงอิลแดบุลก่อน)
- **ตลาดจุงอังคังนึงปิด 19:00** ตามข้อมูล Google แต่แผนตั้งไว้ 18:00-21:00
- **หา "Badasanjeong" ในซกโชไม่เจอ** — ที่ชื่อใกล้เคียงคือ 바다정원 อยู่โกซอง และ 영진해변
  อยู่คังนึง ทั้งคู่ไม่ใช่ซกโช · ใส่คาเฟ่ริมหาดที่เดินถึงจากโรงแรมจริงไว้ในโน้ตแทน
- นัมซานทาวเวอร์จบ ~22:00 (แผนเขียน 21:30) เพราะ Google ตอบว่าขึ้นเขาจากเมียงดง 33 น.
  — 17 ต.ค. เป็นเสาร์ หอคอยเปิดถึง 23:00 ยังทัน

**ยืนยัน:** `lint` + `tsc --noEmit` + `npm test` (72/72) ผ่าน · เปิด `/summary` บน dev server จริง
— timeline คำนวณครบทั้ง 3 วัน, แถวที่พักของ `d5` ขึ้นชื่อถูกทั้งสองแถว, เวลาเปิด-ปิดของจุดใหม่
ขึ้นถูก, บัตรตั๋ว/checklist โผล่ทั้งหน้าสรุปและใต้การ์ดรายวัน

---

## 3.11 กรอกแผนโซล-ซูวอน-วันกลับ 18-21 ต.ค. (12 ส.ค. 2026)

ปิดท้ายจาก 3.9/3.10 — ผู้ใช้ส่งแพลน 18-21 ต.ค. มา ครบทั้งทริปแล้วหลังรอบนี้
· **ลงใน `Plan A` เท่านั้น `แผนหลัก` ไม่ถูกแตะ** (ยืนยันหลังเขียน: plan-default ยังมี 9 แถวเท่าเดิม)
· d7/d8/d9 ของ Plan A ว่างอยู่ก่อนเขียน (สคริปต์เช็คก่อน insert แล้ว throw ถ้าเจอแถวเก่า) = เพิ่มล้วน

### 🔴 แผนที่ส่งมาชนวันหยุดของสถานที่ — สลับวัน 19 ↔ 20

**เคียงบกกุงปิดทุกวันอังคาร** (Places API: `화요일: 휴무일`) และ 20 ต.ค. 2026 = อังคารพอดี
แต่แผนตั้งใจใส่ฮันบกเข้าวังวันนั้น · เสนอผู้ใช้ 3 ทาง เลือก **สลับวันที่ 19 กับ 20** (18 คงเดิม
เพราะกลัวช้อปปิ้งไม่ทัน) — ตรวจแล้วสลับแล้วดีขึ้นทั้งสองวัน ไม่ใช่แค่แก้ปัญหา:

| วัน | ก่อนสลับ | หลังสลับ |
|---|---|---|
| 19 (จันทร์) | ซูวอน — 진미통닭 **ปิดวันจันทร์** · คาเฟ่แฮงลีดันกิลปิดวันจันทร์เยอะ | วัง/ฮันบก — เคียงบกกุงเปิด · บุคชนไม่ติดข้อห้ามวันอาทิตย์ |
| 20 (อังคาร) | วัง/ฮันบก — **วังปิด** | ซูวอน — 진미통닭 เปิด · คาเฟ่เปิดครบ |

ผลข้างเคียงที่ยอมรับแล้ว: 용성통닭 ปิดวันอังคาร (ไปได้เจ้าเดียวคือจินมี) · ช้อปคืนสุดท้ายกลาย
เป็น Starfield ซูวอน แต่กลับถึงเมียงดง 19:42 ยังทัน Olive Young ที่ปิด 22:30

### จุดแวะ 19 จุด (`trip_stops`) + `trip_day_settings`
- `d7` 09:30 · ฮงแด → ยอนนัมดง → The Hyundai → สวนแม่น้ำฮันยออีโด → ซองซูดง · กลับด้วยขนส่งสาธารณะ (กลับถึง 21:15)
- `d8` 08:45 · เช่าฮันบก → เคียงบกกุง → โทโซกชน → บุคชน → อินซาดง → อิกซอนดง → Olive Young เมียงดง · กลับด้วยเดิน (20:27)
- `d9` 08:15 · ชางรยงมุน → ฮวาฮงมุน → จางอันมุน → ตรอกไก่ทอด → แฮงลีดันกิล → วังแฮงกุง → Starfield · กลับด้วยขนส่งสาธารณะ (19:42)

**`d9` จงใจไม่ใช้ `kind="transfer"`/`"intercity"` แบบวันย้ายเมือง (d4/d5/d6)** — ซูวอนเป็นวัน
ไปเช้ากลับเย็น ที่พักไม่เปลี่ยน และสาย 1 คือรถไฟฟ้าโซลปกติที่แตะ T-money ขึ้นได้ ไม่มีเวลารถออก
ตายตัวให้ต้องเตือน · ข้อดีที่จับต้องได้คือ Routes API คำนวณ "โรงแรม → ชางรยงมุน" ให้เป็นเวลาจริง
(71 น.) และ "Starfield → โรงแรม" ตอนกลับ ซึ่งถ้าใส่แถว intercity คั่นจะได้ 0 นาที (แถวนั้นไม่มีพิกัด)
แล้วต้องไปยัดเวลาเดินทางใส่ dwell เอาเองแบบ d6 · เส้นทางรถไฟจริงเขียนไว้ในโน้ตจุดแรก/จุดสุดท้าย

### สถานที่ใหม่ 8 แห่งใน `data/places.ts` + สถานี 2 แห่งใน `data/transferPoints.ts`
`seoul-hanboknam`, `seoul-tosokchon`, `seoul-the-hyundai`, `seoul-yeouido-hangang`,
`seoul-olive-young-myeongdong`, `suwon-changnyongmun`, `suwon-haenggung`, `suwon-tongdak-street`
· `station-suwon` (수원역) + `station-hwaseo` (화서역 — ติด Starfield ขากลับขึ้นที่นี่ใกล้กว่า)
· พิกัด/ชื่อ/ที่อยู่ทุกตัวมาจาก Places API ด้วย query เกาหลี ไม่ได้พิมพ์จากความจำ (กติกาเดิมของ 2 ไฟล์นี้)

**ตรวจพิกัดของเดิมโซล/ซูวอน 9 จุดซ้ำ** (เกณฑ์เดียวกับ 3.9/3.10 คือ ≥300 ม. ถึงแก้) — รอบนี้
**ไม่มีจุดไหนต้องแก้** จุดที่เพี้ยนมากสุดคือ `seoul-bukchon` 207 ม. และ `seoul-yeonnamdong` 298 ม.
(ตัวหลังรู้อยู่แล้วตั้งแต่ 3.10 — เป็นพิกัดกึ่งกลาง "ย่าน" ขยับแล้วไม่เปลี่ยนอะไร)

**เติมเวลาเปิด-ปิด/ข้อห้ามลง `descriptionTh` ของเดิม 4 จุด** ที่ข้อมูลสำคัญพอจะทำแผนพัง:
`seoul-gyeongbokgung` (ปิดอังคาร + รอบเปลี่ยนเวรทหาร), `seoul-bukchon` (เขตควบคุมพิเศษ
เข้าได้ 10:00-17:00 · ซอย 북촌로11길 ห้ามนักท่องเที่ยววันอาทิตย์), `suwon-hwaseong` (เดินกำแพง
ช่วงไหนสวย), `suwon-starfield-library` + `suwon-haenglidan` (เวลาห้าง/ร้านปิดวันจันทร์)

### `data/itinerary.ts`
`d8` เปลี่ยนเป็นเมืองโซล · `d9` เปลี่ยนเป็นซูวอน (`overnightCity: "seoul"` เพื่อให้ leg ที่พักโซล
17-21 ต.ค. ยังต่อกันเป็นก้อนเดียว — `deriveHotelLegs` ใช้ `overnightCity ?? city`) · เขียน `note`
ใหม่ทั้ง d7/d8/d9 · `slots` (ข้อมูลตายที่ไม่มีใครอ่านแล้ว) อัปเดตตามด้วยเพื่อไม่ให้ขัดกับแผนจริง
· `d10` เพิ่มคำเตือน City Airport Check-in ลงในเหตุการณ์ AREX

### bookings 3 แถว + checklist 3 แถว
- 🚄 **AREX Express** (21 ต.ค. 06:15) — จองล่วงหน้า 1-2 สัปดาห์
- 🎫 **เช่าชุดฮันบก** (19 ต.ค. 09:00) — จอง Klook/Trazy ได้ทำผมฟรี
- 🚄 **ซื้อหน้างาน — รถไฟโซล → ซูวอน** (20 ต.ค.) — สาย 1 แตะบัตรขึ้นได้เลย ไม่ต้องจอง
- checklist: พกพาสปอร์ตตอนช้อป (คืนภาษี) · เก็บกระเป๋าคืน 20 · คืน T-money + คืนภาษีที่ ICN

### เรื่องที่ต้องบอกผู้ใช้ (แผนที่ส่งมาชนของจริง)
- **City Airport Check-in ที่สถานีโซลใช้กับ VN409 ไม่ได้** — SUMMARY NOTE ที่ส่งมาเขียนว่าโหลด
  กระเป๋า/ผ่าน ตม. ล่วงหน้าได้ตั้งแต่ 05:30 แต่บริการนี้รับเฉพาะ KE/OZ/7C/TW/RS/BX/LJ/ZE/LH
  (ไม่มีเวียดนามแอร์ไลน์) · เขียนไว้ทั้งในเหตุการณ์ AREX ของ `d10`, โน้ตแถวสนามบิน และโน้ต booking
- **บอลลูนฟลายอิงซูวอนขึ้นบิน 13:00 วันธรรมดา** (ส-อา 11:00) แต่แผนไปกำแพงช่วง 10:00-12:30
  — ตอนเช้าถ่ายรูปคู่บอลลูนที่จอดอยู่ได้ ถ้าอยากขึ้นจริงต้องวนกลับมาบ่าย
- **บุคชนเป็นเขตควบคุมพิเศษ** เข้าได้ 10:00-17:00 เท่านั้น (แผนตั้งไว้ 13:30-15:30 ทันพอดี)

**ยืนยัน:** `lint` + `tsc --noEmit` + `npm test` (72/72) ผ่าน · เปิด `/summary` บน dev server จริง
— timeline ครบทั้ง 3 วัน เวลาเดินทางเป็นของจริงจาก Routes API ทุกช่วง (โรงแรม→ซูวอน 71 น.),
เวลาเปิด-ปิดของ 8 จุดใหม่ขึ้นถูกทุกจุด, ตั๋ว/checklist ใหม่โผล่ทั้งหน้าสรุปและใต้การ์ดรายวัน,
คอนโซลแท็บใหม่สะอาด (error `hotelForStop is not defined` ที่เห็นตอนแรกเป็น HMR chunk ค้างของ
เซสชันก่อน ไม่ใช่บั๊ก) · `/summary` โหลดครั้งเดียวยิง `/api/travel-time` 58 ครั้ง ไม่ซ้ำเลย
(ลิมิต 150/นาที) — 429 ที่เห็นตอนทดสอบมาจากการรีโหลดรัวๆ ของผมเอง ไม่ใช่การใช้งานปกติ

---

### เฟส 23 — สถานะจองตั๋ว (จองแล้ว/รอจอง) + วันครบกำหนดจอง + ชื่อเกาหลีของสถานที่เพิ่มเอง ✅ เสร็จ (12 ส.ค. 2026)

ตอนนี้ตั๋ว/booking บอกได้แค่ "มีแถวอยู่ในระบบ" ไม่บอกว่าจองจริงหรือยัง — หัวข้อ 4 ต้องมานั่งไล่จำเอาเองว่า
3 ใบไหนยังต้องจอง (ดู 3.10/3.11) ผู้ใช้ขอฟีเจอร์สถานะ + วันครบกำหนดจองแทน กับอีกเรื่องที่ค้างจากเฟส 22
คือสถานที่เพิ่มเองยังไม่มีชื่อเกาหลีเก็บไว้ (มีแค่ `name_th`/`name_en`) ทำสองเรื่องพร้อมกันเพราะ
`NearbyPlacesModal.fetchNameEn` ทำ pattern เดียวกันอยู่แล้ว แค่เรียกซ้ำอีกภาษา

- [x] migration `0030_bookings_status.sql` — เพิ่ม `bookings.status` (`'booked' | 'pending'`, default `'booked'`
  เพราะแถวเก่าส่วนใหญ่มีเลขที่จองอยู่แล้ว) + `bookings.book_by_days_before` (int, ไม่บังคับ)
- [x] `lib/bookingDeadline.ts` — `bookByDate(date, daysBefore)` คำนวณวันครบกำหนดสด **ไม่เก็บวันครบกำหนด
  ตรงๆ ในคอลัมน์** เพราะถ้าย้ายวันเดินทางทีหลัง (ผูกวันใหม่ผ่าน `dayId`) ค่าที่คำนวณสดจะตามไปเองโดยไม่ต้องแก้ 2 ที่
  · `daysUntil(iso)` นับวันนับจากวันนี้ (ติดลบ = เลยกำหนดมาแล้ว)
- [x] `BookingEditModal.tsx` — ปุ่มเลือกสถานะ ✅ จองแล้ว/⏳ รอจอง + ช่องกรอก "ต้องจองล่วงหน้ากี่วัน" พร้อมโชว์
  วันครบกำหนดสดใต้ช่อง
- [x] `BookingsPanel.tsx` — ป้าย "รอจอง N" ที่หัวแผง (นับเฉพาะ `status="pending"`) + ป้ายวันครบกำหนดต่อใบ
  ("จองภายใน N วัน" / "⚠️ ต้องจองวันนี้" / "⚠️ เลยกำหนดจอง N วัน") — **โชว์เฉพาะใบที่ยัง "รอจอง"** จองแล้วไม่ต้องเตือนซ้ำ
- [x] migration `0029_custom_places_name_ko.sql` — เพิ่ม `custom_places.name_ko` (รูปแบบเดียวกับ
  `trip_hotels.name_local` จากเฟส 16)
- [x] `NearbyPlacesModal.tsx` — เปลี่ยน `fetchNameEn` เป็น `fetchNameIn(result, lang)` เรียกคู่ `en`+`ko`
  พร้อมกันตอนกด "เพิ่มลงคลัง" เก็บ `name_ko` ไว้ตั้งแต่ตอนบันทึกเลย ไม่ต้องมาตามขอทีหลัง

**ยืนยัน:** `lint` + `tsc --noEmit` ผ่าน · ผู้ใช้รัน migration `0029` + `0030` แล้ว (12 ส.ค. 2026) — เช็คด้วย
curl ตรงไปที่ Supabase แล้ว: `custom_places.name_ko` และ `bookings.status`/`book_by_days_before` มีจริง
(`status` ของแถวเก่าทุกแถวเป็น `'booked'` ตาม default ที่ตั้งไว้ ไม่กระทบข้อมูลเดิม) · ทดสอบในเบราว์เซอร์แล้ว:
เปิด `BookingEditModal` ของ Sky Capsule ใส่ "14" วันล่วงหน้า ระบบคำนวณ "ต้องจองภายใน 2026-09-30" ถูกต้อง
(ปิด modal โดยไม่กดบันทึก ไม่ได้แตะข้อมูลจริง)

**ต่อยอด (12 ส.ค. 2026, ผู้ใช้ขอเพิ่ม):**
- [x] `BookingsPanel.tsx` — การ์ดตั๋วโชว์ `note` (บรรทัดรายละเอียดการซื้อ/เว็บ/แอปที่ใช้จอง ตัดที่ 2 บรรทัด)
  + ถ้ามี `link` โชว์แถว "🔗 เปิดลิงก์จอง" กดเปิดแท็บใหม่ได้เลย (แยกเป็น `<a>` นอก `<button>` หลัก กัน
  interactive ซ้อน interactive ที่ผิด spec HTML) · เช็คด้วย `safeHttpUrl` ก่อน render href เสมอ
  (RLS เปิดสาธารณะ กันไม่ให้ค่าที่พิมพ์ผิดๆ อย่าง `javascript:` หลุดมาเป็นลิงก์ได้)
- [x] ตั้ง `status='pending'` + `book_by_days_before` ให้ตั๋ว 5 ใบที่ยังค้างจองจริง (เขียนผ่าน curl ตรงไปที่
  Supabase ตามเลขที่ระบุไว้ในโน้ตของแต่ละใบอยู่แล้ว): Sky Capsule (14 วัน), Express Bus ปูซาน→ซกโช (7 วัน),
  KTX คังนึง→โซล (30 วัน — ตรงกับ "17 ก.ย. 2026" ที่คำนวณได้), AREX (7 วัน), เช่าฮันบก (ไม่ใส่จำนวนวัน — โน้ต
  เดิมไม่ได้ระบุไว้ ไม่กุเลขเอง) · รายการ "ซื้อหน้างาน"/ไม่ต้องจอง 3 ใบปล่อย `status='booked'` (default) ไว้เหมือนเดิม
  เพราะไม่มีอะไรต้องทำต่อ ไม่ใช่ "รอ" จริงๆ
- ยืนยันในเบราว์เซอร์: หัวแผงขึ้น "รอจอง 5" ถูกต้อง แต่ละใบโชว์โน้ต + "จองภายใน N วัน" + ลิงก์กดได้ · `lint`
  + `tsc --noEmit` + `npm test` (72/72) ผ่านหลังแก้ทั้งหมด

---

### เฟส 24 — กดดูรูปเต็มจอได้ทุกที่ + แผนที่วันข้ามเมืองอ่านออก ⏳ รอยืนยันในเบราว์เซอร์ (12 ส.ค. 2026)

ผู้ใช้ถามสองเรื่องจากการใช้จริง: (1) รูปที่แนบไว้กับตั๋วกดดูได้ไหม (2) วันข้ามเมืองแผนที่ซูมออกจนเห็นทั้งประเทศ
ควรมี "แมพย่อย" ไหม — ตกลงกันว่าใช้แผนที่ instance เดียวเหมือนเดิมแล้วเพิ่มชิปสลับเมือง ไม่แตกเป็นหลายแผนที่
(หน้าแพลนมี 11 วัน · ดูหมายเหตุ `ApiProjectMapError` ที่เฟส 3)

**รูป — `components/PhotoLightbox.tsx` (ใหม่)**
- [x] ยกโค้ด overlay ที่เคยเขียนอยู่ใน `PlaceDetailModal` ไฟล์เดียวออกมาเป็นคอมโพเนนต์กลาง แล้วเติม
  `useBodyScrollLock` + `useDismissable` (ได้ Esc / ล็อกโฟกัส / คืนโฟกัส ชุดเดียวกับ `Modal`) + ปุ่ม ✕
  ของเดิมมีแค่ `onKeyDown` บน div ซึ่งทำงานเฉพาะตอนโฟกัสบังเอิญอยู่ที่ div นั้นพอดี
- [x] `isImageAttachment()` ใน `lib/url.ts` — ตาราง `bookings` มีช่องไฟล์แนบช่องเดียวที่รับทั้ง `image/*`
  และ PDF โดยไม่มีฟิลด์บอกชนิด ต้องเดาจากนามสกุล (ดู `file_name` ก่อน ถอยไปดู pathname ของ URL)
- [x] ต่อสายครบ: การ์ดตั๋ว (`BookingsPanel` — โชว์รูปย่อจริงแทนไอคอน 📎 · ต้องรื้อเป็นปุ่มพี่น้องกัน
  ไม่ใช่ปุ่มซ้อนปุ่ม), โมดัลแก้ตั๋ว, ตั๋วใน `/today`, ลิสต์ตั๋วใน `/summary`, รูปจุดแวะใน `SortableStopRow`
  และ `/today` · PDF ยังเป็นลิงก์ `📎` เปิดแท็บใหม่เหมือนเดิม
- [x] แถวจุดแวะใน `/summary` **จงใจไม่แตะ** — ทั้งแถวเป็น `<button>` เปิด `PlaceDetailModal` (ซึ่งมี lightbox
  ในตัว) อยู่แล้ว ใส่ปุ่มซ้อนเข้าไปจะได้ HTML ที่ผิด

**แผนที่ — `lib/citySegments.ts` (ใหม่) + `DayMapPanel`**
- [x] **ห้ามแบ่งช่วงตาม `place.city` ตรงๆ** เพราะ city tag เพี้ยนได้ 2 ทาง: `lib/resolvePlace.ts` ฮาร์ดโค้ด
  `city: "seoul"` ให้แถว `kind:"hotel"` ทุกแถว (คอมเมนต์เดิมบอกว่า "ไม่ได้ใช้จริง" — ไฟล์นี้คือโค้ดแรกที่
  กรองตามเมือง คอมเมนต์นั้นเลยเป็นเท็จแล้ว) · และสถานที่เพิ่มเองได้ `city = day.city` จาก `app/page.tsx`
  (ร้านแถวเมียงดงที่เพิ่มในวัน d6 ถูกเก็บเป็น "คังนึง") · ถ้าเชื่อ tag ล้วน วัน 15 ต.ค. จะมีชิป "โซล" ผีขึ้นกลางวัน
- [x] กติกาตัดช่วงจึงต้องเข้า 2 เงื่อนไข: `(เมืองเปลี่ยน && > 40 กม.) || > 100 กม.` — เลข 40 อยู่ระหว่าง
  โซล–ซูวอน (32.3 กม. ไม่ตัด) กับ ซกโช–คังนึง (56.7 กม. ต้องตัด) · จุดที่ tag เพี้ยนห่างเพื่อนบ้านแค่ ~1-5 กม.
  จึงรอดด่านนี้เสมอ · ด่านแข็ง 100 กม. ไว้จับ hop ยาวที่ tag ผิดทั้งสองฝั่ง
- [x] **ที่พักต้องเป็นจุดชั้นหนึ่งในการแบ่งช่วง** ไม่ใช่ผูกทีหลัง — ไม่งั้นวัน `d5` (16 ต.ค.) ไม่ถูกแก้เลย
  เพราะจุดแวะอยู่ซกโชทั้งหมด มีแต่โรงแรมคืนนี้ที่อยู่คังนึง (ห่าง 56.7 กม.)
- [x] แผนที่ **ยังวาดหมุดและเส้นครบทั้งวันเสมอ เปลี่ยนแค่ viewport** — เลขหมุด 1..N จึงยังตรงกับลิสต์
  และไฮไลต์สองทางไม่กระทบ · เส้นในเมืองเป็นเส้นทึบต่อช่วง รอยต่อข้ามเมืองเป็นเส้นประสีของเมืองปลายทาง
- [x] clamp ซูมที่ 15 ด้วย `addListenerOnce(map, "idle")` + คืน `removeListener` — ใช้ prop `maxZoom`
  ที่ `<Map>` ไม่ได้เพราะจะบล็อกการซูมด้วยมือของผู้ใช้ไปด้วย
- [x] ชิปวางในโฟลว์เหนือแผนที่ ไม่ใช่ overlay ลอยทับ (ช่องกว้าง ~288px overlay จะบังพื้นที่ที่พยายามทำให้
  อ่านออกพอดี และชนปุ่มซูม/เต็มจอของ Google) · โชว์เฉพาะตอนมี ≥ 2 ช่วง วันเมืองเดียวจึงไม่เปลี่ยนสักพิกเซล
  · **ห้ามใช้ `leading-none`** — "คังนึง"/"ซกโช" มีสระบนที่จะโดนตัดหัว
- [x] **🔴 บั๊กที่เจอตอนเอาข้อมูลทริปจริงมารัน (ไม่เจอจากเทสต์ที่เขียนไว้ก่อน)** — ตอนแรกให้ city ของแถว
  `hotel@` เป็น `null` แล้วปล่อยรับ label ของช่วงที่ตกอยู่ · **วันที่ 16 ต.ค. (d5) เลยยังยุบเหลือช่วงเดียว**
  เพราะลำดับแถวจริงคือ ท่ารถซกโช → [แถวข้ามเมือง ไม่มีพิกัด] → **แวะที่พักคังนึง** → ตลาดคังนึง
  รอยต่อ 56.7 กม. ตกลงบนแถว `hotel@` พอดี พอ city เป็น null ก็ไม่นับว่าเมืองเปลี่ยน (และยังไม่ถึงด่านแข็ง
  100 กม.) ส่วน hop ถัดไปก็แค่ ~1 กม. · แก้เป็นหาเมืองจากพิกัดจริง (`cityFromCoords` เทียบ `cityCenter()`
  ของทุกเมือง) แทนการทำเป็น null
- [x] ยืนยันด้วยข้อมูลทริปจริงจาก Supabase (อ่านอย่างเดียว) รันผ่าน `resolvePlace` + `deriveHotelLegs` +
  `buildDayCitySegments` ตัวจริงครบ 11 วัน: **d4 = ปูซาน 1 | ซกโช 5 · d5 = ซกโช 4 | คังนึง 2 · d6 = คังนึง 4 | โซล 3**
  ส่วนอีก 8 วันได้ช่วงเดียว = ไม่มีชิป พฤติกรรมเท่าเดิม (d9 ซูวอนรวมกับที่พักโซล 32 กม. ตามที่ตั้งใจ)
- [x] `lib/__tests__/citySegments.test.ts` 16 เคสด้วยพิกัดจริง — ครอบ d0/d4/d5/d6/d9 กับดัก `hotel@`
  (รวมเคสถดถอย d5 ข้างบน) กับดัก tag เพี้ยน และ `sameHotel` · `lint` + `tsc --noEmit` + `npm test` (88/88)
  + `npm run build` ผ่าน
- [ ] **ยังไม่ได้ยืนยันหน้าตาด้วยตาในเบราว์เซอร์** — ตอนทำ Browser pane ปิดอยู่ (`visibilityState: "hidden"`)
  แผนที่เลยไม่ mount เพราะ `useInViewOnce` ใช้ IntersectionObserver · เหลือเช็ก: ชิปตกบรรทัดไหมที่ 375px,
  สระบนของ "คังนึง"/"ซกโช" ไม่โดนตัด, กดชิปแล้วซูมถูกช่วง, กดจุดแวะข้ามช่วงแล้วชิปสลับเอง
  · รูปตั๋วต้องมีการ์ดที่แนบรูปไว้จริงถึงจะเห็น (ตอนนี้ยังไม่มีใบไหนแนบรูป)

### เฟส 25 — ป้ายสถานะบนตั๋วทุกใบ + กัน Supabase หลับ ✅ เสร็จ (12 ส.ค. 2026)

ผู้ใช้ถามว่า "จะแยกออกยังไง อันไหนรอจอง รีบจอง จองแล้ว ยังไม่จอง ซื้อหน้างาน" แล้วย้ำว่าอยากเห็นสถานะ
**บนทุกใบ** ไม่ใช่บางใบ · ต้นเหตุคือ `status` มีแค่ 2 ค่า ทำให้ "ซื้อหน้างาน" ไม่มีที่อยู่

**สถานะจอง — `lib/bookingStatus.ts` (ใหม่) + migration `0031`**
- [x] เพิ่มค่า `walk_up` เข้า `BookingStatus` — ของเดิม 3 ใบถูกยัดเป็น `booked` แล้วเขียน
  "ซื้อหน้างาน — " นำหน้า `title` เอาเอง ผลคือ **ทั้งทริปยังไม่มีใบไหนจองจริงสักใบ แต่หน้าจอบอกว่าจองแล้ว 3**
- [x] แยกให้ชัดว่าอะไรเก็บ อะไรคำนวณ: DB เก็บแค่ 3 ค่าที่คนตัดสิน (`pending`/`booked`/`walk_up`)
  ส่วน "รีบจอง/เลยกำหนด" **คำนวณสดตอน render ทุกครั้ง** เพราะเปลี่ยนเองทุกวันโดยไม่มีใครไปแก้
  — เก็บลง DB เมื่อไหร่กลายเป็นข้อมูลเน่าที่ต้องมีคนคอยไล่อัปเดตทันที
- [x] `bookingBadge()` คืนป้ายเสมอ ไม่มี null: 🔴 เลยกำหนด/ต้องจองวันนี้ · 🟠 รีบจอง (เหลือ ≤ 7 วัน)
  · 🟡 จองภายใน N วัน · ⚪ ยังไม่กำหนดวันจอง · ✅ จองแล้ว · 🎟️ ซื้อหน้างาน
- [x] ป้ายอยู่ **บรรทัดบนสุดของการ์ด ตำแหน่งเดียวกันทุกใบ** (ของเดิมอยู่ล่างสุดใต้โน้ตและมีเฉพาะใบที่มีเดดไลน์)
  · หัวแผงเปลี่ยนจาก "รอจอง 5" อันเดียวเป็น 3 ตัวเลขแยกกัน
- [x] **🔴 บั๊กที่เจอตอนเปิดดูของจริง (เทสต์รอบแรกจับไม่ได้เพราะเทสต์แต่ข้ามสี)** — ตอนแรก tie-break
  ในสีเดียวกันด้วย "วันเดินทาง" ซึ่งสร้างปัญหาเดิมขึ้นมาใหม่เป๊ะๆ: ตั๋ว 4 ใบเป็นสีเหลืองหมด แล้ว
  **KTX (เดดไลน์ 17 ก.ย. เร็วสุดในทริป) ไปโผล่เป็นใบที่ 3** เพราะใช้ตั๋ววันที่ 17 ต.ค. · แก้เป็น
  tie-break ด้วยวันครบกำหนดจอง — ยืนยันบนหน้าจอจริงแล้วว่าเรียง 36 → 49 → 57 → 63 วัน
- [x] ซ่อนช่อง "ต้องจองล่วงหน้ากี่วัน" เมื่อสถานะไม่ใช่ `pending` และไม่เซฟค่าที่ค้างอยู่
  (ไม่งั้นสลับสถานะกลับมาทีหลังจะได้เดดไลน์เก่าที่ไม่มีใครตั้งใจโผล่มาเงียบๆ)
- [x] `lib/__tests__/bookingStatus.test.ts` 7 เคส (รวมเคสถดถอยการเรียงข้างบน) · `npm test` 99/99 ผ่าน
- [x] **✅ รัน `0031_bookings_walkup_status.sql` แล้ว** (ยืนยันด้วย curl 13 ส.ค. 2026 — `walk_up` 3 ใบ · `pending` 5 ใบ ดูหัวข้อ 4)

**กัน Supabase หลับ — `app/api/keep-alive/route.ts` + `vercel.json` (ใหม่)**
- [x] ก่อนหน้านี้**ไม่มีอะไรกันเลย** ไม่มี cron ไม่มี GitHub Actions ไม่มี `vercel.json` · free tier
  พักโปรเจกต์เมื่อไม่มี request 7 วันติด แล้วเว็บเปิดไม่ขึ้นทั้งใบ — จุดที่อันตรายสุดคือช่วงอยู่เกาหลีจริง
- [x] Vercel Cron ยิงวันละครั้ง (03:00 UTC = 10:00 ไทย) → route ยิง `select` เบาสุดไปที่ `trip_meta`
- [x] **ต้องใส่ `/api/keep-alive` เข้า `PUBLIC_PATHS` ของ `proxy.ts` ด้วย** — cron ไม่มีคุกกี้ PIN
  ถ้าไม่ยกเว้นจะโดนตอบ 401 **ก่อนแตะ Supabase** = cron เขียวทุกวันแต่ DB หลับอยู่ดี (พังเงียบ)
  · route มีด่าน `CRON_SECRET` ของตัวเองแทน และไม่คืนข้อมูลทริปออกมาเลย (ตอบแค่ ok + เวลา)
- [x] ยืนยันด้วย curl บน dev: `/api/keep-alive` = 200 `{"ok":true}` ส่วน `/api/weather` = 401 (ด่าน PIN ยังทำงาน)

### เฟส 26 — ไล่ hardcode สีที่เหลือใน `components/` ให้ใช้ดีไซน์โทเคน ✅ เสร็จ (13 ส.ค. 2026)

ต่อจากคอมมิต `d1778e4` ที่ย้าย `Modal`/`PlaceDetailModal`/`LayoverBadges`/`TransferAdvicePanel` จาก
`bg-white`/`text-ink` ตายตัว ไปใช้โทเคน `surface-*`/`content-*`/`line` ที่รองรับธีมมืดใน `globals.css`
รอบนี้ไล่ให้ครบอีก 33 ไฟล์ที่เหลือใน `components/`

- [x] แทนที่ `bg-white`/`bg-gray-100`/`bg-gray-200`/`text-gray-400`/`text-gray-500`/`text-ink`/
  `text-ink-soft`/`border-cream-soft`/`bg-cream-soft`/`bg-cream` ด้วยโทเคนคู่กันตาม pattern เดิม
  รวมที่เจอเพิ่มระหว่างไล่ (ไม่อยู่ในสำรวจแรก): `border-ink`/`border-ink-soft` → `border-line`,
  `ring-cream-soft` → `ring-line`, `divide-cream-soft` → `divide-line`
- [x] **จงใจไม่แตะ 3 กลุ่มนี้** เพราะเป็นสไตล์ accent/overlay บนพื้นสีทึบอยู่แล้ว ไม่ใช่ card/surface ทั่วไป
  รอ P1 ตัดสินใจว่าจะทำโทเคนใหม่รองรับหรือปล่อยไว้: `bg-white/10` `bg-white/15` `bg-white/20`
  (ปุ่มบน header เขียว pine ใน `TripHeader.tsx`, `DayStopsSection.tsx`), `bg-black/40-90`
  (overlay รูปถ่าย/lightbox), `bg-ink/NN` (skeleton shimmer, sidebar backdrop, badge บนรูป),
  `shadow-ink/NN` (ยังไม่มีโทเคนเงาธีมมืด)
- [x] `text-cream` คู่กับพื้นทึบ `bg-pine`/`bg-maple`/`bg-gold` **ไม่แตะ** ตามที่ `globals.css` ระบุไว้แล้วว่า
  ตั้งใจให้คงเดิมข้ามธีม
- [x] `npm run lint` ผ่าน · เช็คในเบราว์เซอร์จริงทั้ง light/dark (สลับ `data-theme` ชั่วคราวทาง JS
  ไม่ได้แก้ localStorage ของผู้ใช้) — โมดัลที่แก้ (เช่น `HotelEditModal`) คอนทราสต์ถูกต้องทั้งสองธีม
- [x] ไฟล์นอกขอบเขต (`app/page.tsx`, `app/unlock/page.tsx`, `app/today/page.tsx`) ยังมี hardcode เหลือ
  — เป็นของทีมอื่น ไม่แตะ, แจ้ง P1 แยกไว้แล้ว
- **หมายเหตุ:** เฟส 21.4 เคยตั้งใจให้หน้าแผน `/` ไม่รองรับธีมมืด เพราะตอนนั้น component ที่ใช้ร่วมกัน
  (`SortableStopRow`, `DayStopsSection` ฯลฯ) ยัง hardcode สีอยู่ · ตอนนี้ component เหล่านั้นใช้โทเคนแล้ว
  แต่ `/` เองไม่มีปุ่มสลับธีมและไม่ตั้ง `data-theme` เอง จึงยังคงแสดงผลเป็นค่าธีมสว่าง (`:root`) เหมือนเดิม
  ไม่มีอะไรเปลี่ยนที่ตาเห็น — แค่ตอนนี้พร้อมกว่าเดิมถ้าจะต่อธีมมืดให้ `/` ทีหลัง
- **🔴 5 จุดที่การแทนโทเคนเปลี่ยนสิ่งที่ตาเห็น — P1 แก้ตอนรีวิวก่อน merge** (บทเรียน: เทียบ**ค่าสีจริง**
  ไม่ใช่เทียบ**ชื่อที่ดูเข้าคู่กัน** · `ink`/`ink-soft` คือ**สีตัวอักษร** ส่วน `line` คือ**เส้นคั่น** ต่างกันคนละขั้ว
  #2b241f vs #f7ead6 การ map `border-ink → border-line` เพราะ "ทั้งคู่เป็น border" จึงพลาด)
  - `ImmigrationSheet` เส้นคาดหัวเอกสาร + เส้นใต้หัวตาราง `border-ink` → `border-line` = **หายไปบนกระดาษ
    ที่พิมพ์ยื่นเจ้าหน้าที่ ตม.** · แก้เป็น `border-content` ซึ่งถูกทั้ง 3 กรณี ยืนยันค่าจริงในเบราว์เซอร์แล้ว:
    ธีมสว่าง `#2b241f` · ธีมมืด `#f3ece2` · `@media print` พลิกกลับเป็นชุดสว่าง
  - `PlaceSidebar` ปุ่มเส้นประ `border-ink-soft/30` → `border-line/30` = ครีมจางบนพื้นครีม แทบมองไม่เห็น
  - `GoogleMapEmbed` + `YouTubeEmbed` `bg-gray-100`/`hover:bg-gray-200` ยุบเป็น `surface-soft` ทั้งคู่
    = **hover เท่ากับพื้นตัวเอง ปุ่มกดโหลดแผนที่/วิดีโอกลายเป็นปุ่มตาย** · แก้เป็น `hover:brightness-95`
- **เรื่องขอบเขต:** สั่งไว้ว่าให้แตะเฉพาะ subtree ของ `/today` + `/summary` (หน้าแผนไม่มีธีมมืด แก้ไปก็ไม่มีใครเห็น)
  แต่ทำไป 33 ไฟล์ · รับผลไว้เพราะค่าธีมสว่างเท่าเดิมจริงและ build/test ผ่าน แต่ **2 ใน 5 บั๊กข้างบนอยู่ใน
  ไฟล์ที่สั่งห้ามแตะพอดี** — ราคาของงานที่ไม่มีใครวางแผนจะรีวิว

### เฟส 27 — ทุกหน้าเลิกโหลด Google Maps SDK ให้แผนที่ที่มีอยู่หน้าเดียว ✅ เสร็จ (13 ส.ค. 2026)

P3 วัด production build แล้วรายงานว่า `/today` โหลด Maps SDK ~840KB ทุกครั้ง · น่าสงสัยเพราะ
`app/today/page.tsx` ไม่ได้ import แผนที่เลยสักตัว — ไล่ import graph ต่อจึงเจอต้นเหตุจริง

- [x] **`MapsApiProvider` อยู่ใน root layout** และ `APIProvider` ของ `@vis.gl` โหลด SDK ทันทีที่ mount
  ไม่ว่าจะมีอะไรให้วาดหรือไม่ → **ทุกหน้าจ่ายค่านี้** รวม `/summary` และ `/unlock` · ขณะที่ `<Map>` มีที่เดียว
  คือ `DayMapPanel` ซึ่งไปถึงได้ผ่าน `DayStopsSection` ที่ `app/page.tsx` ใช้หน้าเดียว
- [x] **ทางแก้คือย้าย provider ไม่ใช่ lazy-load** (ที่ P3 เสนอ) — `/today` ไม่มีแผนที่ให้ lazy ตั้งแต่แรก
  มันโหลดมาแล้วทิ้งเปล่า 100% การ lazy-load จะเพิ่มความซับซ้อนและเปลี่ยน UX โดยไม่จำเป็น
- [x] วัดเองยืนยัน (dev server): `/today` **6 request / 840,343 bytes → 0** · หน้าแผน 6 request เท่าเดิม
  · `/today` คือหน้าที่เปิดบ่อยที่สุดตอนอยู่เกาหลีจริงบนเน็ตโรมมิ่ง
- [x] แผนที่ยัง lazy mount ต่อวันตาม `useInViewOnce` เหมือนเดิม — provider โหลด SDK อย่างเดียว ไม่ได้สั่งวาด
- [x] `lint` + `tsc --noEmit` + `build` + `npm test` (108/108) ผ่าน · commit `dc62e81`
- ⚠️ **ระวังตอนแก้ `app/page.tsx`: อย่ารัน `npx prettier --write`** ไฟล์นี้ไม่ได้ถูก format ด้วย prettier
  สั่งครั้งเดียว reformat 635 บรรทัดกลบ diff จริงจนหมด (เจอเองรอบนี้ ย้อนกลับแล้วแก้ด้วยมือแทน)
- [x] **P3 วัดซ้ำบน production build (จอ 375px) ยืนยันตรงกัน** — `/today` decoded รวม **1,911KB →
  1,071KB (ลดลง 44%)** · resource 41 → 35 · JS+CSS ของเราเอง ~278KB → ~263KB gzip · `/summary` 0 request
  · หน้าแผน 6 request / ~821KB ตามที่ตั้งใจ · `dnd-kit` กับ `@vis.gl` ไม่หลงเหลือใน chunk ที่ `/today` โหลดเลย
- [x] **P4 ตรวจซ้ำอีกชั้นด้วยการอ่านกราฟ import** (จุดที่การย้าย provider จะพังคือคอมโพเนนต์ที่เรียก hook
  ของ SDK นอกร่ม provider แล้ว throw) — มีแค่ 2 ไฟล์ที่ import จาก `@vis.gl`: ตัว provider เองกับ
  `DayMapPanel` ซึ่งอยู่ใต้ `app/page.tsx` ทั้งกราฟ **ไม่มีใครหลุดออกไปอยู่นอกร่ม** · วัดต่อหน้าได้
  `/today` `/summary` `/unlock` = **0 request ทุกหน้า** · `PlaceDetailModal` เปิดได้ทั้ง 2 หน้าและ iframe
  ของ `GoogleMapEmbed` โหลดสำเร็จจริง (923ms / 962ms) โดย SDK ยังเป็น 0
- [x] **`/unlock` เบาลงด้วย** — หน้าที่คนเจอก่อนใครเพื่อนตอนเปิดลิงก์ครั้งแรก ไม่เคยมีแผนที่อยู่แล้ว

**ตารางผลวัดของ P3 — production build จริง (`npm run build && npm start`) จอ 375px** (ไม่ใช่ dev server)

| หน้า | HEAD ที่วัด | resources รวม | API calls | maps.googleapis.com requests | decoded รวม |
|---|---|---|---|---|---|
| `/today` | ก่อนแก้ (`49a3322`) | 41 | 19 | 6 (~840KB) | 1,911KB |
| `/today` | หลัง `dc62e81` | 35 | 14 | 0 | 1,071KB (**-44%**) |
| `/today` | หลัง `a501607` (เฟส 28) | 34 | 13 | 0 | 1,051KB |
| `/summary` | หลัง `dc62e81` | 87 | 65 (62 travel-time) | 0 | 1,430KB |
| `/summary` | หลัง `a501607` | 96* | 65 (62 travel-time) | 0 | 1,431KB |
| `/` (หน้าแผน) | หลัง `dc62e81` และ `a501607` | 102 | 77 | 6 (~821KB, ตั้งใจ ไม่เปลี่ยน) | 2,421KB |

\* ตัวเลข resources ของ `/summary` ขยับ 87→96 ระหว่าง 2 รอบวัด แต่ decoded รวมแทบเท่าเดิม (1,430→1,431KB)
และ API call/byte เท่ากันเป๊ะ — น่าจะเป็นความแปรปรวนของการนับ resource entry ระหว่างรอบทดสอบ ไม่ใช่การถดถอย

⚠️ **ตัวเลขทั้งตารางนี้วัดจาก production _build_ บนเครื่อง (`next build && next start`) ไม่ใช่จากเว็บบน Vercel**
**ห้ามเขียนว่า 840KB วัดบน production (Vercel)** — ฝั่ง "before" วัดซ้ำบนโดเมนจริงไม่ได้แล้วเพราะ build เก่า
ไม่ได้ deploy อยู่

✅ **แต่ before/after ของ `dc62e81` บนโดเมนจริงได้แล้ว (P4 · 13 ส.ค. หลัง `07812ec`)** — ผู้ใช้เปิดแท็บ
ที่ปลดล็อก PIN ไว้ให้ (agent ไม่กรอก PIN เอง):
- **`/today` = 0 request ไป `maps.googleapis.com`** (วัด 2 ครั้งตรงกัน · 43 resources = vercel 35 + supabase 8)
  เทียบกับ **6 requests บน production ก่อน deploy** → `dc62e81` ทำงานจริงบนของจริง
- `/unlock` (หน้า public): 18 resources · 0 request ไป Maps · transfer รวม 280,315 bytes
- **หน้าแผน `/` ยังโหลด SDK และวาดแผนที่รายวันได้ปกติ** — ถูกต้องตามที่ตั้งใจ หน้าเดียวที่ควรจ่าย

**หลังเฟส 28 (`a501607`)**: console สะอาดทั้ง 3 หน้า (ไม่มี React error #418 อีกแล้ว) `/today` ลด API call
ลงอีก 1 ครั้ง (14→13) และ decoded ลดอีก ~20KB — สอดคล้องกับที่คาดว่า hydration mismatch เดิมทำให้ React
ทิ้ง subtree แล้ว render ใหม่ (อาจรวมถึงเอฟเฟกต์ที่ยิงซ้ำ) ส่วนเวลาที่เห็นการ์ด "จุดถัดไป" วัดจริงไม่ได้บน
localhost เพราะ loopback เร็วเกินจริง (domContentLoaded ~13-40ms) ไม่ใช่ตัวเลขที่มีความหมายเทียบกับเน็ตโรมมิ่ง

### เฟส 28 — เอาค่าที่เกิดตอน build ออกจาก HTML ที่ prerender ✅ เสร็จ (13 ส.ค. 2026)

P3 เจอ `Uncaught Minified React error #418` (hydration failed) บน **production build** ทั้ง `/` และ `/today`
ตั้งแต่โหลดหน้าแรกโดยไม่ต้องคลิกอะไร · เขาสงสัยว่าไม่ได้เกิดจาก `dc62e81` ซึ่งถูกต้อง — ต้นเหตุคนละเรื่องกัน

- [x] **`next build` prerender หน้าพวกนี้เป็น HTML ตั้งแต่ตอน build** (ดูของจริงได้ที่
  `.next/server/app/today.html`) อะไรที่เซิร์ฟเวอร์ render จึงถูกแช่แข็งไว้แล้วส่งให้ทุกคน
- [x] **ค่าที่หลุดเข้าไป 2 ตัว** — `/today` ฝัง**นาฬิกา**ลง HTML (`grep` ไฟล์ที่ build แล้วเจอ `09:35 น.`
  ซึ่งคือเวลาที่ build) · หน้าแผนฝัง**จุดแดงบนปุ่ม ⚙️** ที่ render เมื่อ `who` ว่าง — `who` มาจาก
  `localStorage` ตอนตั้ง state ตั้งต้น เซิร์ฟเวอร์จึงเห็นว่างเสมอ ส่วนเครื่องที่เคยใส่ชื่อไม่วาด
- [x] **นี่คือเหตุผลที่ dev ไม่มีใครเห็นปัญหา** — dev server render ตอนมี request พอดี เวลาสองฝั่งตรงกันเอง
  · P4 เห็น console สะอาดบน dev ส่วน P3 เห็น error บน production ทั้งคู่รายงานถูกทั้งคู่
  **บทเรียน: เรื่อง hydration ต้องวัดบน production build เท่านั้น**
- [x] `hooks/useMounted.ts` (ใหม่) — ใช้ `useSyncExternalStore` แทน `useState` + `useEffect` จึงไม่ต้อง
  setState ในเอฟเฟกต์เลย (เลี่ยง eslint `react-hooks/set-state-in-effect` หลักการเดียวกับ `useDarkTheme`)
- [x] `/today` คืน skeleton จนกว่า hydrate เสร็จ — **ไม่เสียอะไรเลยเพราะ HTML ที่ prerender ก็เป็น skeleton
  อยู่ก่อนแล้ว** (ข้อมูลมาจาก Supabase ฝั่ง client ทั้งหมด) ต่างกันแค่หัวเว็บที่เคยฝังเวลา build ลงไป
  · ยังแก้เคสอนาคตให้ด้วย: ตอนไปเที่ยวจริง `findTodayIndex` จะคืนคนละวันกับตอน build ซึ่งจะ mismatch ซ้ำ
- [x] ยืนยัน: `grep` ไฟล์ที่ build ใหม่ — เวลาหาย (1 → 0) จุดแดงหาย (1 → 0) · เปิด production build จริง
  `/today` แสดง **09:51 น. = เวลาปัจจุบัน** การ์ด "จุดถัดไป" ขึ้นปกติ · **console สะอาดทั้ง `/` `/today`
  `/summary`** · `lint` + `tsc` + `build` + `npm test` (108/108) ผ่าน · commit `a501607`

### สถานะ QA ของเฟส 24 (P4 รายงาน 13 ส.ค. 2026) — ✅ **ปิดครบ 5/5 แล้ว**

- [x] regression ของ `d1778e4` **ผ่าน 3 จาก 4**: พื้นที่แตะ 44px ของ checklist ไม่ทับปุ่มลบจริง (สแกน
  `elementFromPoint` ทีละพิกเซล — ห่างกัน 251px ที่จอ 375) · `aria-label` 3 ปุ่มบนหัว `/summary` อ่านชื่อออก
  ที่จอแคบ · ไม่มี element ล้นแนวนอนสักตัวทั้ง 3 หน้า ทั้งที่ 1280 และ 375 (เดิม `scrollWidth` 1489)
- [x] `useOnlineStatus` — **ตรรกะถูกทั้งเส้น** แถบขึ้นถูกข้อความ ห่างจาก abort แค่ 1ms และคืนสภาพเองเมื่อ
  probe ผ่าน · แต่ตัวเลข "3 วิ" ยืนยันไม่ได้เพราะแท็บโดน background throttling (`setTimeout(50)` ถูกดันเป็น
  ~1000ms, `AbortSignal.timeout(3000)` วัดได้ 5.2-7.0 วิ) — **ไม่ใช่บั๊กของโค้ด**
- [ ] **5 ข้อของเฟส 24 ยังค้าง** เพราะ Browser pane รายงาน `visibilityState: "hidden"` ตลอด · P4 พิสูจน์แล้วว่า
  ไม่ใช่แค่ "แผนที่อยู่นอกจอ": `requestAnimationFrame` ไม่ยิงเลยใน 1.2 วิ และ `IntersectionObserver` ตัวใหม่
  ที่จ่อ `<main>` กลางจอพอดีก็ไม่ยิงใน 2.5 วิ (หน้า hidden ข้ามสเต็ป "update the rendering" ทั้งสเต็ป)
  → `useInViewOnce` ไม่มีวันเป็น true → `DayMapPanel` ไม่ mount → **ชิปสลับเมืองอยู่ข้างในนั้นด้วย จึงตรวจ
  ด้วย DOM ไม่ได้เลย** · สกรีนช็อตหลังเลื่อนก็ได้ภาพว่างเพราะไม่มีการ raster tile ใหม่
- [x] **2 ใน 5 ข้อ P1 ปิดได้จากการอ่านโค้ดแทน** (13 ส.ค.): ชิปใช้ `flex flex-wrap` **จึงไม่มีทางล้นจอ**
  อย่างมากคือตกบรรทัด และที่ 375px ชิป 2 อัน + "ทั้งวัน" กว้างรวม ~250px ในช่องที่มี ~351px = อยู่บรรทัดเดียวได้
  · เรื่องสระบนโดนตัด: ปุ่มใช้ `leading-snug` (ไม่ใช่ `leading-none`) + `py-1.5` และ **ไม่มี `overflow-hidden`
  หรือความสูงคงที่บนตัวปุ่มเลย** ซึ่งเป็นสองอย่างเดียวที่ตัดตัวอักษรได้จริง — ความเสี่ยงต่ำมาก
- [x] **กดชิปแล้วเฟรมเข้าช่วงที่ถูก — ผ่าน** (วัน 15 ต.ค. · ผู้ใช้ทดสอบเอง P4 อ่านภาพ 13 ส.ค.)
  ชิปขึ้น 2 อันพอดี `🚌 ปูซาน 1` · `⭐ ซกโช 5` ตรงกับข้อมูลจริงทั้งชื่อเมืองและจำนวนจุด · **ไอคอนคั่นเป็น 🚌
  ตรงกับ Express Bus ของวันนั้น** (ไม่ใช่ `→` ตัวสำรอง) = `intercityModeBeforeStopId` ส่งค่าถูก · กด `ซกโช 5`
  แล้วเฟรมครอบซกโชจริง เห็น Abai Village 아바이마을 · Cheongchoho Lake Park · หมุด 4-7 + หมุดโรงแรม
- [x] **ผู้ใช้ยังซูมด้วยมือได้ — ผ่าน** (วัน 13 ต.ค.) กด `+` ซูมลึกจนเห็นชื่อร้านรายตัว (Kyocha, 롯데백화점)
  ไม่ตันที่ระดับไหนเลย + เลื่อนแผนที่ได้ → การตัดสินใจ **ไม่ใช้ prop `maxZoom`** ถูกต้อง ยืนยันบนของจริงแล้ว
- [x] **ไฮไลต์สองทาง ลิสต์→แผนที่ — ผ่าน** กดชื่อจุดแวะปกติ แถวขึ้น ring ส้ม + ป๊อปอัพเด้งที่หมุดนั้นพอดี
  เนื้อหาครบทุกส่วนของ `StopCard` (รูป · "จุดที่ 3 · ช้อปปิ้ง" · เวลา · `🚌 จากจุดก่อนหน้า ~24 น.` · โน้ต · 2 ปุ่ม)
- [x] **วันเมืองเดียวไม่มีชิปโผล่ — ผ่าน 3 วันแล้ว** (12, 13 ต.ค. และวัน 12 ต.ค. รอบก่อน)
- [x] **กดจุดแวะข้ามช่วงแล้วชิปสลับเอง — ผ่าน** (ข้อที่ยากที่สุด · ทิศ ปูซาน→ซกโช วัน 15 ต.ค.)
  เริ่มที่ชิป `ปูซาน 1` แล้วกดชื่อ "หาดชกโช" ได้ครบ 4 สัญญาณพร้อมกัน: ชิปกระโดดไปไฮไลต์ `⭐ ซกโช 5` **เอง** ·
  แถวขึ้น ring ส้ม · ป๊อปอัพเด้งที่หมุดถูกตัว (`จุดที่ 3 · ชายหาด` · `13:13–14:13` · `🚶 ~3 น.`) ·
  แผนที่ย้ายไปซกโชเห็น Sokcho Beach 속초해변
  → **ยืนยัน `selectSegment` + `stillVisible` ถูก** — ไม่ได้ล้าง `activeStopId` ทิ้งตอนสลับช่วง
  (ถ้าพลาดเคสนี้ ป๊อปอัพจะหายไปเลย) · **ของแถม: เลขหมุดตรงกับลิสต์ข้ามช่วง** "จุดที่ 3" = หมุดเลข 3
  ทั้งที่จุดที่ 1 อยู่คนละเมือง = ยืนยันหลักการ "วาดครบทั้งวันเสมอ เปลี่ยนแค่ viewport"
- [x] **วัดซูมจริงต่อชิปครบ 3 วัน** (P4 อ่านจาก URL ของ tile ที่ Google โหลด `!1i{z}!2i{x}!3i{y}`
  แม่นกว่าเดาจากภาพ · หลังผู้ใช้เอา Chrome ขึ้นหน้าสุด)

  | วัน | ชิปที่ขึ้นจริง | ซูมที่วัดได้ต่อชิป |
  |---|---|---|
  | d4 · 15 ต.ค. | `🌊 ปูซาน 1` · `🍁 ซกโช 5` | ปูซาน z11 → ซกโช z14 → ทั้งวัน z6 → กลับปูซาน z11 |
  | d5 · 16 ต.ค. | `🍁 ซกโช 4` · `☕ คังนึง 2` | ซกโช z11 → คังนึง **z15** |
  | d6 · 17 ต.ค. | `☕ คังนึง 4` · `🏯 โซล 3` | คังนึง z11 → โซล z13 |

  **จำนวนจุดต่อชิปตรงกับที่คำนวณจากข้อมูลจริงครบทั้ง 3 วัน ไม่คลาดสักตัว**
- [x] **"อีก 8 วันต้องไม่มีชิปเลย" — ยืนยันครบทั้ง 11 วันแล้ว** เลื่อนให้แผนที่ mount ทุกวันแล้วสแกน
  `button[aria-pressed]` ทั้งหน้า: **มีชิปเฉพาะ d4/d5/d6** · d0-d3, d7-d9 mount แผนที่แล้วแต่ไม่มีชิปสักอัน ·
  d10 (21 ต.ค.) ไม่ mount แผนที่เลยเพราะไม่มีจุดแวะที่มีพิกัด (`hasMapPoints` false) จึงไม่มีชิปโดยปริยาย
- [x] **สระบน "คังนึง" — วัดจริงแล้ว ไม่ใช่แค่อนุมานจากโครงสร้าง** ปุ่มชิป `☕ คังนึง 2`:
  `scrollHeight 23 = clientHeight 23` (ไม่มี overflow แนวตั้ง) · `overflow: visible` · กล่องหมึกสูง **17px**
  ห่างขอบบน 4.0px ขอบล่าง 4.1px ในปุ่มสูง 25.1px → หัวสระอยู่ในกรอบครบ
  ⚠️ **จุดที่ต้องระวังต่อไป:** กล่องหมึก 17px **สูงเกิน line box 15.125px จริง** ตามที่เคยห่วง —
  รอดเพราะ `overflow: visible` + `py-1.5` ล้วนๆ · **ใครเผลอใส่ `leading-none` หรือ `overflow-hidden`
  ที่ปุ่มชิปเมื่อไหร่ สระโดนตัดทันที** คอมเมนต์เตือนในโค้ดมีค่ามาก อย่าลบ
- [x] **clamp z15 — ไม่มีเคสให้ยิงกับข้อมูลทริปนี้** (P4 คำนวณล่วงหน้า แล้ววัดจริงตรงเป๊ะ:
  คังนึง d5 ได้ **z15 พอดี** ตามที่คำนวณไว้)
  ช่องแผนที่ 288×420 หัก padding ของ `fitBounds` (28/24/28/24) เหลือ **240×364 px** ·
  สูตร ม./พิกเซล = `156543.03 × cos(lat) / 2^z`

  | ช่วง | ขนาดกรอบ | ต้องการ | z ที่ `fitBounds` เลือก |
  |---|---|---|---|
  | **คังนึง (d5) — แคบสุดในทริป** | 872 × 363 ม. | 2.40 ม./px | **z15** (z15 ครอบได้ 1,375 ม. ✅ · z16 ได้ 688 ม. ❌) |
  | ซกโช (d4, 5 จุด) | 1,614 × 1,171 ม. | 4.88 ม./px | z14 |
  | ปูซาน (d4, โรงแรม→โนโพ) | ~14.5 กม. | — | ~z11 |

  → **แม้ช่วงแคบที่สุด `fitBounds` ก็เลือก z15 มาเอง เงื่อนไข `zoom > 15` ไม่เป็นจริงสักช่วง**
  clamp เป็นตาข่ายที่ข้อมูลทริปจริงไม่มีทางไปแตะได้ · **P4 ไม่เคลมว่า "ผ่าน" เคลมได้แค่ "ไม่มีทางเกิด"**
  📌 ถ้าจะยืนยันจริงต้องเป็น **เทสต์ยูนิต ไม่ใช่ QA ในเบราว์เซอร์** — ดึงการตัดสินใจ "zoom ที่ควรได้จาก
  bounds + ขนาดช่อง" ออกมาเป็นฟังก์ชันแล้วเทสต์เคส 2 จุดห่าง 300 ม. · **ไม่ทำก็ไม่กระทบทริป**
- [x] **ปิดข้อสังเกต: รูปในป๊อปอัพว่าง = แค่จังหวะ `PlaceThumb` โหลดไม่ทันตอนถ่าย ไม่ใช่บั๊ก**
  ป๊อปอัพ "หาดชกโช" รูปขึ้นเต็มปกติ · ไม่ต้องตามต่อ
#### 🔴 เรื่องใหญ่ที่ P4 เจอตอนวัดข้อสุดท้าย: **production รันโค้ดเก่า** (13 ส.ค. 2026)

ตอนไปวัด `AbortSignal.timeout(3000)` บน production P4 พบว่าโค้ดที่รันอยู่ **ตามหลัง working tree
หลาย commit** — สาเหตุคือ local `main` ค้างไม่ได้ push มา **8 commits** และ "deploy" ที่ทำตอนแยก Maps key
เป็นการกด **Redeploy ของ deployment เดิม** เพื่อให้ env ใหม่มีผล ซึ่ง **ใช้ build เก่า ไม่ได้ดึงโค้ดใหม่**

หลักฐาน 2 ตัว วัดบน production ก่อน push:
- **`/today` ยังโหลด Maps JS SDK 6 requests** (`maps/api/js`, `maps-api-v3/.../main.js`, `gen_204`)
  และ `window.google.maps` มีจริง — บน dev วัดได้ **0** → **ขาด `dc62e81`**
- **probe ของ `useOnlineStatus` ไม่ส่ง `signal` มาเลย** ดัก `window.fetch` อ่าน args จริงได้
  `fetch('/manifest.webmanifest', { cache: 'no-store' })` ทั้งที่ working tree มี
  `signal: AbortSignal.timeout(3_000)` → **ขาด `d1778e4`**
  · ยืนยันด้วยพฤติกรรม ไม่ใช่แค่ดู args: stub ให้ probe **ค้างถาวร** (จำลองอุโมงค์รถไฟ — สัญญาณติดแต่ข้อมูล
  ไม่วิ่ง) แล้วยิง event `offline` → **production: แถบ "📴 เน็ตหลุด" ไม่ขึ้นเลยใน 10 วินาที** ·
  dev ที่มี `d1778e4`: ขึ้นทันทีหลัง abort
  → **บั๊กที่ `d1778e4` แก้แล้วยังมีชีวิตบน production เต็มๆ** ตรงกับเคสที่คอมเมนต์ในฮุคเขียนไว้เป๊ะ
  ("ผู้ใช้เห็นหน้าปกติ แก้แผนไป แล้วการเขียนหายเงียบ") และเป็นหน้า `/today` ที่จะใช้จริงในอุโมงค์รถไฟ
  ใต้ดินโซล/ปูซาน กับซอรัคซาน

✅ **แก้แล้ว: push `26d7d22..07812ec` ขึ้น `origin/main` แล้ว deploy สำเร็จ (13 ส.ค. 2026)**
ส่งออก 9 commits รวด (8 ที่ค้าง + บั๊กฟิกซ์แถว transfer/hotel) · Vercel build จาก `07812ec` **Ready ใน 26s
ป้าย Production** · **ผลของ `d1778e4` / `49a3322` / `dc62e81` เพิ่งขึ้น production รอบนี้เป็นครั้งแรก**

**หลักฐานยืนยันทฤษฎี Redeploy จากหน้า Deployments ของ Vercel เอง** — แถวที่ deploy โค้ดจริงจะขึ้น
`⑃ main` + commit hash ส่วนแถวที่กดปุ่มซ้ำจะขึ้น **`↻ Redeploy of <id>`** · รอบที่ทำตอนแยก Maps key
คือ `↻ Redeploy of 8Tjr4rpMY` และชื่อ deployment ยังเป็น "Show a booking status on every ticket…"
ซึ่ง **เก่ากว่า `d1778e4` ด้วยซ้ำ** · Git integration ต่อกับ repo อยู่ปกติดีมาตลอด ปัญหาคือไม่ได้ push เฉยๆ

⚠️ **กับดักตอนเช็คว่า deploy ขึ้นหรือยัง (P1 พลาดมาแล้ว)** — P1 เฝ้า md5 ของหน้า `/unlock` 6 นาทีแล้ว
สรุปผิดว่า "ยังไม่ deploy" ทั้งที่ขึ้นไปแล้ว · **P4 พิสูจน์ว่าสาเหตุไม่ใช่ edge cache อย่างที่ P1 เดาตอนแรก**:
HTML ที่ได้ตอน `x-vercel-cache: HIT` อ้าง chunk hash ชุดเดียวกับตอนโหลดแบบ cache-bust เป๊ะ
(`0v602hkpe9x4h.js`, `437-mpr264sjw.js`, `turbopack-074gumx1v1e5-.js`) → **สิ่งที่ P1 แฮชอยู่คือ
build ใหม่ตั้งแต่แรก** · md5 ไม่เปลี่ยนเพราะ **output ของ `/unlock` เหมือนเดิมทุกไบต์ระหว่างสอง build นี้**
(หน้านั้นไม่ได้พึ่งอะไรที่ 9 commits ไปแตะ) ไม่ใช่เพราะแคชค้าง
**บทเรียน: อย่าใช้ "ไบต์ของหน้าใดหน้าหนึ่งไม่เปลี่ยน" เป็นหลักฐานว่า deploy ยังไม่ขึ้น** —
ให้ดูหน้า Deployments, `git log origin/main..HEAD`, หรือหา signal ที่ผูกกับโค้ดที่เปลี่ยนจริง
(P4 ใช้ `hasSignal: true` ใน probe ซึ่งโค้ดเก่าไม่มี — นั่นคือหลักฐานที่ถูกชนิด)

⚠️ **บทเรียน: "Redeploy" บน Vercel ≠ deploy โค้ดใหม่** — มันใช้ build เดิมกับ env ใหม่เท่านั้น
ถ้าต้องการโค้ดใหม่ต้อง push แล้วให้ Vercel build จาก commit นั้น · และก่อนเชื่อผล QA บน production
ทุกครั้ง **ให้เช็ค `git log origin/main..HEAD` ก่อนว่าว่างจริงไหม**

- [x] **✅✅ วัดซ้ำบนโดเมนจริงหลัง deploy `07812ec` แล้ว — ตรงสเปก ±1 ms ทั้ง 3 รอบ** (P4)

  | รอบ | probe เริ่ม | abort | แถบขึ้น |
  |---|---|---|---|
  | 1 | 0.1 ms | **3000.3 ms** | 3001.1 ms |
  | 2 | 0.0 ms | **3000.4 ms** | 3000.7 ms |
  | 3 | 0.1 ms | **3000.9 ms** | 3001.2 ms |

  ท่าวัด: stub `window.fetch` ให้ค้างตลอดแต่ **ยอมให้ abort ยกเลิกได้เหมือน fetch จริง** (จำลอง
  "สัญญาณติดแต่ข้อมูลไม่วิ่ง" ไม่ใช่ network error) → ยิง `offline` → MutationObserver จับตอนแถบโผล่ใน DOM
  · แถบขึ้นห่างจาก abort **ไม่ถึง 1 ms** — React re-render ไม่เพิ่มดีเลย์ที่รู้สึกได้ · ข้อความที่อ่านได้จริง:
  `📴 เน็ตหลุด — โหมดอ่านอย่างเดียว (ข้อมูลที่เห็นคือชุดล่าสุดที่โหลดไว้)` · แถบ sticky บนสุด ดันเนื้อหาลง ไม่ลอยทับ
  → **เทียบกับก่อน deploy ที่ production ไม่ขึ้นแถบเลยใน 10 วินาที = บั๊กอุโมงค์รถไฟตายจริงบนของจริงแล้ว**

  🧪 **กับดักที่เกือบทำให้รายงานเลขผิด — จดไว้ให้รอบหน้า** ยิงรอบแรกได้ **3888 ms** P4 เกือบรายงานว่า
  "เกินสเปก 30%" · ก่อนเชื่อ วัด control: `setTimeout(3000)` **เปล่าๆ** ในแท็บเดียวกันตอนนั้นก็ได้ **3380 ms**
  → เป็น timer throttling ของแท็บ `hidden` (ข้อจำกัดตัวเดียวกับ occlusion ในข้อ -4) ไม่ใช่โค้ดแอป ·
  พอ **arm control `AbortSignal.timeout(3000)` ใน tick เดียวกับ probe** เพื่อหักลบ environment ออก
  ได้ 3000.x ทั้ง 3 รอบ (control 3001.0–3001.5 ms)
  **กฎใหม่: เลขเวลาที่วัดจากเบราว์เซอร์ต้องมี control ในรอบเดียวกันเสมอ ยิงรอบเดียวแล้วเชื่อไม่ได้**

- [x] **(รอบก่อน deploy) `AbortSignal.timeout(3000)` วัดได้ 3,001 ms** (P4 · แท็บ `visibilityState: "visible"`)

  | | ค่าที่วัดได้ |
  |---|---|
  | `AbortSignal.timeout(3000)` | **3,001 ms** |
  | `setTimeout(3000)` | 3,001 ms |
  | `setTimeout(100)` | 101 ms |
  | `setTimeout(50)` | 51 ms |

  เทียบกับตอนแท็บ hidden (`setTimeout(50)` → 999 ms · `AbortSignal.timeout(3000)` → 5,207 และ 7,000 ms)
  → **ยืนยันว่าตัวเลขที่เพี้ยนก่อนหน้านี้เป็น background throttling ล้วนๆ ไม่ใช่โค้ด**

  **โซ่พิสูจน์ครบ 3 ท่อน:** (1) แอปส่ง `AbortSignal.timeout(3000)` เข้า `fetch` จริง — ดักอ่าน args
  ตอนรันเห็น `signal` เป็น `AbortSignal` และ abort listener ยิงจริง · (2) signal abort ที่ **3,001 ms**
  ตอนแท็บ visible · (3) แถบ "📴 เน็ตหลุด" โผล่ใน DOM **1 ms** หลัง abort (จับด้วย MutationObserver)
  → **"แถบเน็ตหลุดขึ้นภายใน ~3 วิ" = ผ่าน เคลมได้เต็มปาก** · regression ของ `d1778e4` ปิดครบ 4/4
- [x] **ปิดเคสรูปในป๊อปอัพว่างอย่างเป็นทางการ** — เปิดซ้ำในแท็บ visible แล้ววัด `<img>`:
  `complete: true` · `naturalWidth 160 × naturalHeight 90` · โหลดจาก `/api/place-photo` จริง ·
  รอเพิ่ม 3 วิ ค่าเท่าเดิม (ไม่ใช่ภาพกำลังโหลด) → **ไม่ใช่บั๊ก** สาเหตุคือแคชรอบแรก + แท็บ hidden
  ไม่ปลุก lazy load

- [x] **สอดส่องของที่เพิ่งขึ้น production ครั้งแรก — ผ่าน** (P4 · 13 ส.ค.)
  **โหมดมืด `/today`**: พื้นมืดทั้งหน้า ไม่มีแฟลชขาว · เปิด `PlaceDetailModal` ในโหมดมืดเป็นพาเนลมืด
  ตัวหนังสืออ่านออก รูป/รีวิว/แผนที่ครบ · **console ไม่มี error** → `49a3322` ยืนยันบนของจริง
  · **ธีม:** เครื่องผู้ใช้ `prefers-color-scheme: dark` แต่หน้าเป็น light เพราะ `localStorage` เก็บ `"light"`
  ไว้ — **ถูกต้อง ไม่ใช่ hydration พลาด** (P4 สลับไปมืดเพื่อทดสอบแล้วคืนค่าเป็น light ตามเดิม)
  **การ์ดตั๋ว**: "🟡 จองภายใน 35 / 48 / 56 / 62 วัน" สมเหตุสมผลกับ 13 ส.ค. 2026 ไม่มีอันไหนเพี้ยนเป็น "1 วัน"
  ⚠️ **แต่เคสขอบที่ `d1778e4` แก้จริงๆ (ช่วง 00:00–07:00 น.) วัดตอนนั้นไม่ได้** เพราะทดสอบ 15:22 น. —
  พึ่งเทสต์ยูนิตแทน: `vitest` บน HEAD **108/108 ผ่าน 13 ไฟล์**

#### 📌 ของแถมที่ P4 เจอระหว่างทาง — ไม่ใช่บั๊ก แต่สะดุดตาคนใช้ (ยังไม่ได้แก้)

**ก. ป๊อปอัพที่พักไม่มีชื่อโรงแรม** — แถวในลิสต์เขียน "แวะที่พัก · 강릉 호텔 플로어" แต่หัวป๊อปอัพขึ้นแค่
**"ที่พัก / Accommodation"** เพราะ `parseHotelPlaceId` ใน [resolvePlace.ts:16](lib/resolvePlace.ts:16)
คืน `nameTh: "ที่พัก"` กลางๆ ตามคอมเมนต์ที่ตั้งใจไว้ · ในโค้ดสมเหตุสมผล แต่กดชื่อโรงแรมแล้วได้ป๊อปอัพ
ไม่มีชื่อมันสะดุด · **ถ้าจะแก้: ส่ง `hotelName` เข้าโมดัลไปทับ เหมือนที่แถวในลิสต์ทำอยู่แล้ว**

**ข. ข้อความ "ต้องตั้งค่า Google API key ก่อน" โทษผิดเรื่อง** — [PhotoGallery.tsx:18](components/PhotoGallery.tsx:18)
ขึ้นข้อความนี้ทุกครั้งที่ `photos.length === 0` **ไม่ว่าสาเหตุอะไร** · P4 พิสูจน์แล้วว่าคีย์บน production
ใช้ได้ปกติ: ยิง `/api/place-photos?queries=ตลาดจุงอัง คังนึง` ได้ **200 พร้อม photo reference จริง**
และป๊อปอัพสถานีปูซานโหลดรูปมา 6 ใบ → บนป๊อปอัพที่พักมันแค่ **"ไม่เจอรูปสำหรับจุดนี้"**
⚠️ **อันตรายกว่าที่คิด: ข้อความนี้จะทำให้คนไล่บั๊กผิดทางเสียเวลา** โดยเฉพาะช่วงที่เพิ่งแยกคีย์ Maps กันไป
· **ควรเปลี่ยนเป็น "ไม่พบรูปสำหรับจุดนี้"**

> 🖥️ **หมายเหตุสภาพเครื่องตอนทดสอบ (P4 แจ้ง 13 ส.ค.):** dev server ของ plan-korea ดับหมดแล้ว
> (`localhost:59302` และ `62693` ตายทั้งคู่) · **`port 3000` ตอบ 200 แต่นั่นคือ mu-phone ไม่ใช่โปรเจกต์นี้**
> ใครจะเทสต์ต่อระวังเปิดผิดโปรเจกต์แล้วสรุปผิด

#### 🐛 บั๊กที่เจอระหว่าง QA รอบนี้ — ✅ แก้แล้ว (13 ส.ค. 2026)

**แถว `transfer` กับ `hotel` ในลิสต์กดไม่ได้ ทั้งที่มีหมุดบนแผนที่** — [SortableStopRow.tsx](components/SortableStopRow.tsx:260)

จุดแวะปกติเรนเดอร์เป็น `<button onClick={() => sched.place && onView()}>` แต่สองชนิดนี้เรนเดอร์เป็น
`<div>` เปล่าไม่มี handler เลย ทั้งที่เลย์เอาต์/ไอคอน/ชื่อ/เวลาหน้าตาเหมือนกันทุกอย่าง
· ยืนยันจากโค้ดแล้วว่าเป็นบั๊กจริง: [schedule.ts:153](lib/schedule.ts:153) resolve `place` จาก
`placesById.get(stop.placeId)` **ให้ทุก kind เท่ากันหมด ไม่ได้แยกชนิด** และ
[DayMapPanel](components/DayMapPanel.tsx:67) ก็ `filter(s => s.place != null)` ไม่ได้กรอง kind ออก
→ **กดหมุดบนแผนที่ได้ปกติ แต่ ลิสต์→แผนที่ ตันทางเดียว ทั้งที่เป็นจุดเดียวกัน**

ผลกระทบจริง: วัน 15 ต.ค. ช่วง "ปูซาน 1" **มีแถวเดียวและเป็น `transfer` พอดี** → ในลิสต์ไม่มีอะไรให้กดเลยสักแถว

**แก้แล้ว** — ครอบทั้งสองชนิดด้วย `<button onClick={onView} disabled={!sched.place}>` ตัวเดียวกับแถวปกติ
+ ใส่ `hover:underline` ที่ชื่อให้ affordance ตรงกัน · `tsc --noEmit` และ `eslint` ผ่าน
· แถว `intercity` กดไม่ได้ **ถูกต้องแล้ว ไม่ต้องแก้** — เป็นช่วงเดินทาง ไม่มีพิกัดจุดเดียว
· deploy ขึ้น production แล้วใน `07812ec`
- [x] ✅ **ยืนยันบนโดเมนจริงแล้ว** (P4 · ผู้ใช้เปิดแท็บที่ปลดล็อก PIN ไว้ให้)
  **"🚉 ไปสถานี · สถานีขนส่งปูซาน (โนโพ)" วัน 15 ต.ค.** → ring ส้มรอบแถว + ป๊อปอัพเต็ม
  (Busan Central Bus Terminal · ⭐ 3.8 จาก 366 รีวิว · รูป Google 6 ใบ · แผนที่) ·
  **"🏨 แวะที่พัก · 강릉 호텔 플로어" วัน 16 ต.ค.** → ring ส้ม + ป๊อปอัพ พร้อมโน้ต "เช็คอิน Hotel Flow-er"
  และหมุดตรงพิกัดโรงแรม · **`intercity` ยังกดไม่ได้ตามตั้งใจ** — เช็ค DOM แล้ว "รถบัส · ซกโช → คังนึง"
  และ "รถบัส · ปูซาน (โนโพ) → ซกโช" ทั้งคู่ `insideButton: false` · แถวที่แมตช์ทั้ง 7 แถว `disabled: false`

### เฟส 28 — วันบิน 11/21 ต.ค. หน้าตาเท่าวันอื่น: แถวตารางบินมีรูป กดดูรายละเอียด/นำทางได้ ✅ เสร็จ (14 ส.ค. 2026)

ผู้ใช้รายงานว่าวัน 11 กับ 21 "ไม่เหมือนชาวบ้าน" — ไล่แล้วเจอ **2 สาเหตุคนละเรื่องกัน**
1. **21 ต.ค. ไม่มีจุดแวะเลยสักจุด** (Plan A: d0=6 · d1–d9=4–8 · **d10=0**) → `hasMapPoints` เป็น false
   ทั้งการ์ดจึงไม่มีแท็บ 📋/🗺️ ไม่มีแผนที่ ไม่มีแถบสรุปท้ายวัน — **ยังค้างอยู่ ดูข้อ 10 ในกล่อง 🔴**
2. **แถวตารางบินเป็นตัวหนังสือล้วน** คนละทรงกับแถวจุดแวะ กดไม่ได้ ไม่มีรูป ← เฟสนี้แก้ข้อนี้

**`DayEvent.placeId` (ใหม่) — ผูกแถวตารางบินเข้ากับสถานที่จริง 18 แถวของ d0/d1/d10**
- เที่ยวบินผูกกับ**สนามบินปลายทาง** (คำถามตอนกดคือ "ลงที่ไหน ต่อยังไง") · ค่าพิเศษ `"@hotel"`
  = ที่พักคืนก่อนหน้า ดึงจาก `trip_hotels` ตอน render ผ่าน `hotelToPlace()` (ใหม่ ใน `lib/hotelLegs.ts`)
- resolve ด้วย `lib/eventPlace.ts` (ใหม่) ตัวเดียวกันทั้ง 3 หน้า — แถวเดียวกันชี้ที่เดียวกันเสมอ

**สนามบินใหม่ 2 แห่งใน `TRANSFER_POINTS`** — สุวรรณภูมิ · เตินเซินเญิ้ต · **พิกัด/ชื่อยิงจาก
Places API (New) จริงตามกติกาหัวไฟล์** และผูก `googlePlaceId` ไว้ด้วย (แม่นกว่าค้นด้วยชื่อ) ·
ตั้ง `pickerHidden` (ใหม่) ทั้งคู่จึงไม่โผล่ในลิสต์ "✈️ ไปสนามบิน/สถานี"

🔐 **ที่พักของเราเองที่กรุงเทพ (จุดออก 11 ต.ค. / จุดกลับ 21 ต.ค.) ไม่ได้อยู่ในโค้ด**
ผู้ใช้ตัดสินให้เก็บลง DB แทน (14 ส.ค. 2026) เพราะไฟล์ใน repo ถูก commit ขึ้น git —
อยู่ใน `custom_places` id **`home-base`** (city `bangkok`, `google_place_id` ผูกไว้) ·
ใน `data/itinerary.ts` พาดหัว/รายละเอียดเขียนกลางๆ ว่า "ที่พัก" เฉยๆ ชื่อจริงกับที่อยู่มาจาก DB
ตอน render · `resolveEventPlace()` จึงต้องรับ `customPlaces` เข้าไปด้วย ส่ง`[]` ไม่ได้
**เช็คก่อน commit ทุกครั้ง** ว่าชื่อคอนโด/ชื่อย่าน/เลขที่/พิกัด/`google_place_id` ของที่พัก
ไม่หลุดกลับเข้ามาในไฟล์ไหน — เอาค่าจริงมาจากแถว `home-base` ใน `custom_places` แล้ว grep หาเอง
(จงใจไม่พิมพ์ค่าจริงลงบรรทัดนี้ ไม่งั้น grep จะเจอบรรทัดนี้เองทุกครั้งจนเช็คไม่ได้)

⚠️ **`Place["city"]` กว้างขึ้นเป็น 8 ค่า** (+`bangkok` +`hcmc`) แต่ **`Day["city"]` ยังเป็น 6 เมืองเดิม**
— ตั้งใจแยกกัน เพราะ `lib/citySegments.ts` ใช้ `Object.keys(CITY_NAME_TH)` เป็นรายชื่อเมืองของทริป
ไปหาว่าพิกัดหนึ่งอยู่เมืองไหน ถ้าเติมกรุงเทพ/โฮจิมินห์เข้าไปช่วงเมืองบนแผนที่รายวันจะเพี้ยน ·
`tsc` จับให้ครบ 4 จุดที่ต้องตาม: `useDaySchedule` · `useTripDnd` ×2 · `citySegments`
· `CITY_LOCALE` เปลี่ยนเป็น `as const satisfies` **ไม่ใช่ `: Record<...>`** ไม่งั้นทุกการ index
คืน union เต็ม `"ko"|"vi"|"th"` แม้ index ด้วยเมืองที่เป็นไทยไม่ได้ แล้ว `useDayOpeningHours` พัง
· เพิ่ม `PLACE_CITY_NAME_TH` สำหรับตอนต้องเอ่ยชื่อเมืองของ `Place` ที่อาจอยู่นอกเกาหลี

**3 หน้าใช้โครงแถวของหน้าตัวเอง ไม่ใช่ทรงกลางทรงเดียว** (ผู้ใช้สั่งว่า "ต้องเหมือนของเก่าเป๊ะๆ")
| หน้า | ลอกโครงจาก | ค่าที่วัดจริงในเบราว์เซอร์ |
|---|---|---|
| `/` `DayEventsPanel` | `SortableStopRow` | เวลา/รูป/ชื่อ ห่างจากขอบ 122.94 vs 123 · 190.94 vs 191 (ต่าง 0.06px) |
| `/summary` | แถวจุดแวะของหน้าเดียวกัน | 29 / 85 / 141 px **ตรงกันเป๊ะทั้ง 3 คอลัมน์** · รูป 48px เท่ากัน |
| `/today` | ลิสต์ "ถัดจากนี้" | padding `10px 12px` · gap 12px · เวลา left 29 กว้าง 48 **เท่ากันหมด** |

🧪 **บั๊กที่เจอตอนวัดจริง ไม่ใช่ตอนอ่านโค้ด** — ช่องซ้ายสุดของ `SortableStopRow` ที่ `sm:` ขึ้นไปเป็น
`sm:w-auto` = **กว้างตามตัวอักษรข้างใน** ⠿ (16px) ได้ 18.94px แต่ 🔒/✏️ (12px) ได้ 23px →
ทั้งคอลัมน์เวลา+รูปของแผงตารางบินเยื้องขวา 4px จากแถวจุดแวะที่อยู่ใต้กัน · ตรึงเป็น `sm:w-[19px] sm:px-0`
**บทเรียน: ลอกคลาสมาเป๊ะๆ ไม่ได้แปลว่าได้ความกว้างเท่ากัน ถ้าคลาสนั้นเป็น `w-auto`**

**`/today` เคยโชว์แค่ 2 แถวจากทั้งตาราง** (`beforeAnchorEvent` + `afterAnchorEvent`) — วันกลับจึง
**ไม่เห็น AREX ไม่เห็นเช็คอิน ICN ไม่เห็นเที่ยวบินสักเที่ยว** ทั้งที่เป็นวันที่ต้องพึ่งหน้านี้ที่สุด ·
เปลี่ยนเป็นแผง "✈️ เวลาตายตัวของวันนี้" เต็มตาราง

**เพิ่ม `anchor: "after"` ให้ "ถึง ICN — เช็คอิน VN409" (07:35)** — d10 ไม่เคยมี anchor "after" เลย
แปลว่าถ้าเติมจุดแวะแล้วตารางลากยาวเลยเวลาเช็คอิน **แถบเตือน "จบช้ากว่ากำหนด" จะเงียบสนิท**
(d0 มีเดดไลน์ 21:00 คุมอยู่) · ตอนนี้ยังไม่เปลี่ยนอะไรที่ตาเห็นเพราะ `deadlineOverrunMinutes`
คืน null เมื่อไม่มีจุดแวะ

- [x] `tsc --noEmit` · `eslint` · `npm test` 108/108 ผ่าน
- [x] ยืนยันในเบราว์เซอร์จริงครบ 3 หน้า ทั้ง 375px/1280px และธีมสว่าง/มืด — สีชื่อแถวตารางบิน
  กับแถวจุดแวะเป็น `rgb(243,236,226)` ค่าเดียวกันเป๊ะในธีมมืด · แถวเตือนได้ `rgb(240,165,131)`
  บนพื้น maple เข้ม (ไม่ใช่ครีมบนครีม) · กดแถว "ถึงสุวรรณภูมิ" เปิดโมดัลได้จริง ⭐ 4.4 (73,901 รีวิว)
- [x] ✅ **commit + push แล้ว `ff1622d`** (14 ส.ค. 2026) — แตะ 13 ไฟล์ + `lib/eventPlace.ts` (ใหม่)
  · `git log origin/main..HEAD` ว่างแล้ว · Vercel build จาก push นี้เอง (ไม่ใช่ปุ่ม Redeploy — ดูบทเรียนเฟส 27)
  · **ยังไม่มีใครยืนยันบน production** เพราะทั้งเว็บอยู่หลัง PIN — ต้องให้ผู้ใช้เปิดดูเอง

📌 **error ที่เห็นใน console ระหว่างทดสอบ ไม่ได้มาจากงานรอบนี้** — `/api/place-details?queries=35.15458,129.05736|…`
คืน 500 เพราะแถว `kind="hotel"` (d1/d4/d5/d6 มีอยู่ก่อนแล้ว) มี `mapsQuery` เป็นพิกัดดิบ แล้ววันนั้น
dev server แก้ DNS `places.googleapis.com` ไม่ได้ (`ENOTFOUND`) · เป็นของเดิม ไม่ใช่ regression

---

## 4. สิ่งที่ต้องให้ผู้ใช้ทำเอง (ผมทำแทนไม่ได้)

> ### ✅ ปิดไปแล้ว (12 ส.ค. 2026)
>
> **1. รัน migration `0025` + `0026`** — ผู้ใช้รันเองแล้ว ยืนยันด้วย curl ว่าคอลัมน์ใหม่มีจริง
>
> **2. เติมชื่อ/ที่อยู่ท้องถิ่น + เบอร์โทรให้ที่พักแถวเก่า** — **ทำให้แล้วโดยไม่ต้องกดผ่าน UI**
> ตาราง `trip_hotels` ไม่ได้เก็บ `place_id` ไว้ จึงหา place ใหม่ด้วย `places:searchText` จากชื่อโรงแรม
> โดยจำกัดกรอบ 2 กม. รอบพิกัดที่บันทึกไว้เดิม แล้ว**ยืนยันว่าผลที่ได้ห่างจากพิกัดเดิมไม่เกิน 150 ม.
> ก่อนเชื่อว่าเป็นที่เดียวกัน** (ของจริงได้ 0 ม. ทั้ง 2 แถว = place เดียวกันแน่นอน) จากนั้นยิง Place Details
> ซ้ำภาษา `ko` + `en` เอา `displayName`/`formattedAddress`/`internationalPhoneNumber` มา PATCH
> เฉพาะ 5 คอลัมน์ใหม่ · `hotel_name`/`formatted_address`/`lat`/`lng` เดิม**ไม่ถูกแตะ** ยืนยันด้วย curl แล้ว
> - `d1` ปูซาน — 아르반호텔 · 대한민국 부산광역시 부산진구 중앙대로691번길 32 · +82 51-805-9901
> - `d4` ซกโช — Lecollective SokchoBeach · 대한민국 강원특별자치도 속초시 해오름로 201 · +82 70-8655-2966
>
> ⚠️ **มีที่พักแค่ 2 leg จาก 4 ที่ตั้งไว้ในระบบ** — leg โซล/คังนึงยังไม่มีแถวใน `trip_hotels` เลย
> พอตั้งที่พักใหม่ผ่าน UI ช่องพวกนี้จะถูกเติมให้เองตั้งแต่ครั้งแรก (เฟส 16 ทำ path นี้ไว้แล้ว)
> ไม่ต้องมาเติมย้อนหลังอีก
>
> **3. ตั้ง env `TRIP_PIN` + `TRIP_PIN_SECRET` บน Vercel** — ผู้ใช้ตั้งแล้ว
>
> **3.5 ✅ ตั้ง env `CRON_SECRET` บน Vercel แล้ว** (เฟส 25 — กัน Supabase หลับ · ยืนยัน 13 ส.ค. 2026)
> ตั้งไว้ที่ scope Production + Preview และ deploy ติดแล้ว · ยืนยันด้วย curl ไปที่
> `korea-trip-plan-one.vercel.app/api/keep-alive`: ไม่ใส่ header ได้ **401** และใส่ Bearer มั่วก็ได้ **401**
> (ก่อนหน้านี้ตอบ 200 ให้ทุกคน) · cron ของ Vercel แนบ `Authorization: Bearer $CRON_SECRET` มาเองอยู่แล้ว
> ⚠️ Vercel Hobby ให้ cron **วันละครั้ง** — ตาราง `0 3 * * *` = 10:00 น. ไทย ซึ่งพอสำหรับเพดาน 7 วันของ Supabase
>
> **4. ✅ รัน migration `0031_bookings_walkup_status.sql` แล้ว** (ยืนยันด้วย curl 13 ส.ค. 2026)
> ตั๋วทั้ง 8 ใบมี `status` ครบและแยก 3 กลุ่มถูกต้อง: `walk_up` 3 ใบ (กระเช้าซอรัคซาน · Intercity Bus
> ซกโช→คังนึง · รถไฟโซล→ซูวอน) ที่เหลือ 5 ใบเป็น `pending` — **ยังไม่มีใบไหนเป็น `booked` เลยสักใบ**
> ซึ่งตรงกับความจริง (ยังไม่ได้จองอะไร) · `trip_meta` ที่ keep-alive ยิงก็ตอบ 200 ปกติ
>
> **-4. ✅ แยก Google Maps key เป็น 2 ใบ — เสร็จครบ 4 ขั้น ยืนยันแล้วทุกทาง (13 ส.ค. 2026)**
>
> **โปรเจกต์ Google Cloud ที่ถือ key ทั้งสองใบ:** `galvanized-pipe-427006-t6` (ชื่อที่แสดง "My First Project")
>
> **สิ่งที่เจอ:** เดิมมี API key ใน Cloud Console **ใบเดียว** ชื่อ `Maps Platform API Key` ถือ 7 APIs
> และ env ทั้ง `GOOGLE_MAPS_API_KEY` กับ `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` **ถือค่าเดียวกัน**
> · Application restrictions = None · ถ้าเผลอใส่ HTTP referrer restriction กับใบนี้ตรงๆ จะพังทันที 4 จุด
> เพราะ server route ยิงออกไป **โดยไม่มี Referer header**: [googlePlaces.ts](lib/googlePlaces.ts) ·
> [place-photo](app/api/place-photo/route.ts) · [travelProvider.ts](lib/travelProvider.ts) ·
> [youtube-video](app/api/youtube-video/route.ts) — **นี่คือกับดักหลักของงานนี้**
>
> | API | ใครเรียก | อยู่ใบไหน |
> |---|---|---|
> | Maps Embed API · Maps JavaScript API | `GoogleMapEmbed` · `DayMapPanel` · `MapsApiProvider` | 🌐 browser |
> | Places API (New) · Routes API · YouTube Data API v3 | server เท่านั้น | 🔒 server |
> | Directions API · Geocoding API | **ไม่มีโค้ดเรียกเลย** (grep แล้ว) | ลบทิ้งได้ |
>
> ฝั่ง client ไม่มีที่ไหนโหลด Places/Geocoding ผ่าน JS library — browser key ต้องการแค่ 2 ตัวจริงๆ
>
> - [x] ขั้น 1 สร้าง key ใบใหม่สำหรับเบราว์เซอร์ + Websites restriction (`http://localhost:3000/*`,
>       `https://korea-trip-plan-one.vercel.app/*`) + จำกัดเหลือ Maps Embed / Maps JavaScript
> - [x] ขั้น 2 เปลี่ยนค่า `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` ทั้งบน Vercel และใน `.env.local`
> - [x] ขั้น 3 redeploy — **ยืนยันบน production แล้ว** (P1 ขับ Chrome ผ่านแท็บที่ผู้ใช้ล็อกอินไว้ 13 ส.ค.)
>       หน้า `/` เสิร์ฟ key ใบใหม่จริง และ **ไม่มีใบเดิมหลงเหลืออยู่ในหน้าเลย** (เช็คทั้ง HTML และ `script[src]`)
>       · `window.google.maps` โหลดสำเร็จ · รูปสถานที่ในไซด์บาร์ขึ้นครบ = ใบเดิมฝั่งเซิร์ฟเวอร์ยังทำงานปกติ
>       · ✅ **เห็น tile จริงแล้ว** — ผู้ใช้เลื่อนไปวัน 12 ต.ค. (ปูซาน) เอง แผนที่เรนเดอร์ครบ ถนน/ชื่อเกาหลี/
>       หมุดเลข 2-5/ปุ่มซูม/แถบข้อกำหนด **ไม่มีกล่องเทาและไม่มี `RefererNotAllowedMapError`**
>       = Maps JS API ยอมรับ referrer โดเมน Vercel ด้วยใบใหม่แล้ว
>       ⚠️ หมายเหตุวิธีตรวจ: ขับผ่านสคริปต์ไม่เห็น เพราะ `DayMapPanel` mount ต่อเมื่อ IntersectionObserver
>       เห็นกล่องเข้าจอจริง (`mapInView` ใน [DayStopsSection](components/DayStopsSection.tsx:578)) —
>       `scrollIntoView()` ไม่ trigger · **ต้องให้คนเลื่อนดูเอง** ครั้งหน้าไม่ต้องเสียเวลาลองใหม่
> - [x] **ขั้น 4 ✅ เก็บกวาดใบเดิมแล้ว** — ตัด API restrictions จาก 7 เหลือ **Places API (New) ·
>       Routes API · YouTube Data API v3** · **Application restrictions = None (คงไว้ ถูกต้องแล้ว)**
>       ⚠️ ห้ามใส่ Websites/IP กับใบเดิมเด็ดขาด (Vercel Hobby ไม่มี IP คงที่ + ไม่มี Referer)
>       ⚠️ **แถบเหลือง "this key can currently be used with any application" จะค้างอยู่กับใบนี้ตลอดไป
>       — ถูกต้องแล้ว ห้ามแก้ตาม** Google เตือนเหมารวมทุก key ที่ Application restrictions = None
>       ใครมาเห็นรอบหน้าแล้วเผลอ "แก้ให้เรียบร้อย" คือทำเว็บพัง 4 จุดข้างบนซ้ำอีกรอบ
>
> **P4 วัดซ้ำบน production หลังตัดแล้ว — ผ่านครบ (13 ส.ค. 2026)**
>
> | API | วิธีวัด | ผล |
> |---|---|---|
> | Places API (New) | `/api/place-photos?queries=Gamcheon…` | 200 คืน URL รูปจริง · หน้าแผนยิง `/api/place-photo` 14 ครั้ง โหลดสำเร็จ 13 ใบ |
> | Routes API | `/api/travel-time` (กัมชอน→จากัลชิ) | 200 `{durationMinutes:22, distanceMeters:2251, isReal:true}` · หน้าเว็บมีคำว่า "(จริง)" 32 ครั้ง |
> | YouTube Data API v3 | `/api/youtube-video?query=Gamcheon…` | 200 `{videoId:"jOHUlaNld50"}` · **รอบแรกที่มีคนเทสต์ API นี้** |
> | Maps JS (ใบใหม่) | อ่าน DOM ตรง | `.gm-style` ใน `day-card-d0` ขนาด 288×420 **มี tile image 28 ใบ** · console มีข้อความเดียวคือ `google.maps.Marker is deprecated` ซึ่งพ่นตอนสร้าง Marker **สำเร็จ** = วาดหมุดจริง |
> | **Maps Embed (ใบใหม่)** | เห็น tile กับตาในโมดัล | `GoogleMapEmbed` ใน `PlaceDetailModal` ของ "ถนนสายคาเฟ่จอนโพ" ขึ้นแผนที่จริง — `Jeonpo Cafe Street 전포 카페거리` + หมุดแดง + ย่านรอบข้าง (JEONPO-DONG, Seomyeon) + โลโก้ Google + แถบ `ข้อมูลแผนที่ ©2026 TMap Mobility` |
> | YouTube embed (ปลายทาง) | เห็น player บนจอ | คลิป `BUSAN, SOUTH KOREA [4K] Jeonpo Cafe…` ช่อง VioletVik มีปุ่ม play = ครบวงจรตั้งแต่ API ถึงตัวเล่น |
> | Routes API (บนจอ) | อ่านแถบเวลาในการ์ดวัน | วัน 12 ต.ค. ขึ้น "(จริง)" ครบทุกช่วง — 21 · 38 · 31 นาที |
>
> key ที่เสิร์ฟจริงเป็นใบใหม่ทั้ง 2 ทาง: SDK `maps/api/js?key=AIzaSyD30aG2…` และ iframe
> `google.com/maps/embed/v1/place?key=AIzaSyD30aG2…` · ไม่มี `RefererNotAllowedMapError` /
> `ApiTargetBlockedMapError` / `ApiNotActivatedMapError` สักตัว
>
> ⚠️ **อย่าตกใจกับ "รูป 62 ใบ โหลด 13"** — P4 ไล่แล้ว 49 ใบที่เหลือเป็น `loading="lazy"` ทุกใบและอยู่นอกจอทุกใบ
> เป็นผลของ environment ตอนวัด ไม่ใช่ผลของการตัด API
>
> ⚠️ **ตรวจ referrer restriction จากภายนอกแทนไม่ได้ ลองแล้ว** — ยิง Embed API ด้วย
> `Referer: https://evil.example.com/` เทียบกับ `localhost` ได้ HTML **ขนาดเท่ากันเป๊ะ 2220 bytes ทั้งคู่**
> เพราะ Embed API ตอบ shell เหมือนกันเสมอแล้วไปเช็ค referrer ตอน iframe รันจริงในเบราว์เซอร์ ·
> Maps JS API ก็เสิร์ฟ bootstrap script ให้เสมอแล้วค่อยโยน `RefererNotAllowedMapError` ตอนรัน
> · curl หน้าแรกก็อ่านไม่ได้เพราะด่าน PIN ตอบ 307 ก่อน — **ต้องเปิดดูในเบราว์เซอร์เท่านั้น**
>
> 🧪 **กับดักตอนขับ Chrome ผ่าน MCP (จดไว้ให้รอบหน้า)** — ถ้าหน้าต่าง Chrome ถูกหน้าต่างอื่นบัง **มิด 100%**
> macOS จะมาร์กว่า occluded แล้ว Chrome หยุด render ทั้งหน้าต่าง: `visibilityState` เป็น `hidden`
> และ `requestAnimationFrame` ไม่ยิง **ทั้งที่ `document.hasFocus()` เป็น `true`** (แท็บถูกเลือกอยู่จริง)
> ผลคือ IntersectionObserver ไม่ทำงาน · `loading="lazy"` ไม่ปลุก · แผนที่ไม่ mount — **ดูเหมือนเว็บพัง
> ทั้งที่ไม่พัง** · ต้องให้ผู้ใช้เอา Chrome ขึ้นหน้าสุดจริงๆ แค่เปิดค้างไว้ข้างหลังไม่พอ
> · ข้อดีที่ใช้ประโยชน์ได้: `useInViewOnce` ไม่ย้อนกลับเป็น false พอผู้ใช้เลื่อนดูครั้งเดียว
> `<Map>` จะค้างใน DOM ให้อ่านย้อนหลังได้
>
> 📌 **หนี้ทางเทคนิค ไม่ต้องแก้ก่อนทริป** — console เตือน `google.maps.Marker is deprecated`
> (Google ประกาศ 21 ก.พ. 2024 ให้ย้ายไป `AdvancedMarkerElement`) · `DayMapPanel` ใช้ `<Marker>` ของ
> `@vis.gl` ซึ่ง map ลงมาที่ตัวนี้ · ยังไม่มีกำหนดปิดและ Google สัญญาแจ้งล่วงหน้า 12 เดือน
> ทริปเหลืออีก ~2 เดือนจึงไม่กระทบ · ถ้าจะย้ายต้องใส่ `mapId` เพิ่ม ซึ่งเปลี่ยนสไตล์แผนที่ทั้งใบ **ไม่คุ้มตอนนี้**
>
> ### 🔴 ค้างอยู่ตอนนี้ (13 ส.ค. 2026)
>
> **✅ A. แผนที่พังบน production — แก้แล้ว ยืนยันด้วยตาแล้ว (18 ส.ค. 2026)**
> อาการ: เข้าเว็บได้ แต่เลื่อนลงจนกล่องแผนที่เข้าจอ **ทั้งหน้าดับเป็น "This page couldn't load"**
> · console: `InvalidKeyMapError` แล้วตามด้วย `getAt` undefined + IntersectionObserver — **สองตัวหลังเป็นผล ไม่ใช่สาเหตุ**
>
> **สาเหตุจริง:** ค่าใน Vercel `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` **ไม่ตรงกับคีย์จริง** ของ
> `plan-korea browser (embed+js)` ใน Google Cloud · **ฝั่ง Google ตั้งค่าถูกทุกอย่างอยู่แล้ว**
> (Maps Embed + Maps JavaScript · Websites = localhost + โดเมน Vercel) **ไม่ได้แตะสักบรรทัด**
>
> **แก้:** Google Cloud → Show key → copy → วางทับใน Vercel → **ปิด Sensitive** → redeploy
> ✅ **ยืนยันแล้ว:** ผู้ใช้เลื่อนดูเอง วัน 12 ต.ค. (ปูซาน) — tile จริง ชื่อเกาหลี หมุดเลข 2-5 ปุ่มซูม แถบ Google ครบ
>
> 🔴 **3 บทเรียนที่ต้องติดอยู่กับข้อนี้:**
> 1. **`NEXT_PUBLIC_*` ฝังตอน build ไม่ได้อ่านตอนใช้งาน** — แก้ค่าแล้วเว็บยังใช้ของเดิมจนกว่าจะ build ใหม่
>    · **redeploy รอบหมุน PIN คือตัวที่ทำให้ค่าที่ผิดอยู่แล้วเริ่มมีผล** ไม่ได้เป็นคนทำให้เสีย
>    → **redeploy เปลี่ยนทุกอย่างที่ฝังตอน build ไม่ใช่แค่ค่าที่เราตั้งใจเปลี่ยน**
> 2. 🔴 **ห้ามตั้ง `NEXT_PUBLIC_*` เป็น Sensitive บน Vercel** — ค่าพวกนี้อยู่ในบันเดิลให้ทุกคนเห็นอยู่แล้ว
>    การซ่อนไม่เพิ่มความปลอดภัยเลย **แต่ทำให้เราตรวจสอบไม่ได้ว่าค่าตรงไหม** ซึ่งคือสาเหตุที่หาไม่เจอตั้งนาน
>    · เกิดกับ `TRIP_PIN` ในวันเดียวกันด้วย — ผู้ใช้อ่านค่าตัวเองไม่ได้ เลยเข้าเว็บไม่ได้
> 3. **หลัง redeploy ทุกครั้งต้องเปิดเว็บเลื่อนดูจริง** — สคริปต์ตรวจแทนไม่ได้
>    (`DayMapPanel` mount ต่อเมื่อ IntersectionObserver เห็นกล่องเข้าจอ · `scrollIntoView()` ไม่ trigger)
>    · ตอนหมุน PIN ตรวจแค่ว่า **PIN เก่าใช้ไม่ได้** ไม่ได้ตรวจว่า **PIN ใหม่ใช้ได้** และไม่ได้เปิดเว็บดูเลย
>
> **4. ทดสอบบนมือถือจริง (ผมทดสอบแทนไม่ได้)**
> - [x] **✅ ปุ่ม Naver/Kakao — ผู้ใช้ยืนยันเองว่าใช้ได้จริงบนเครื่องที่ลงแอปไว้ (13 ส.ค. 2026)**
>   ปิดของค้างจากเฟส 14 · `nmap://` ไม่คืน response ตรวจอัตโนมัติไม่ได้ ต้องเห็นแอปเปิดเองเท่านั้น
> - [x] **✅ service worker + airplane mode — ผู้ใช้ยืนยันเองว่าใช้ได้จริง (13 ส.ค. 2026)**
>   เปิดเว็บให้ SW ติดตั้ง → เพิ่มลงหน้าจอโฮม → เปิด `/today` กับ `/summary` ตอนมีเน็ต → airplane mode ยังเปิดได้
> - [ ] **ยังไม่ได้ทดสอบ: แทรกแถว "✈️ ไปสนามบิน" ที่วัน 21 ต.ค.** ดูว่าแถบ "ควรออกไม่เกิน HH:MM"
>   ขึ้นถูก (หลังรัน `0025`) — ข้อนี้ไม่ได้อยู่ในสองข้อที่ผู้ใช้ยืนยันมา **อย่าเหมาว่าผ่านไปด้วย**
>   · เป็นการเขียนข้อมูลจริงลงวันเดินทางกลับ จึงควรให้ผู้ใช้กดเอง
>
> **5. ตรวจเนื้อหาที่ผมกรอกไว้แทน**
> - ~~สั่งพิมพ์ `/summary?lang=en&for=immigration` จริงว่าลง A4 พอดีไหม~~ **✅ ตอบแล้ว (13 ส.ค. 2026)**
>   P4 วัดด้วยการพลิกทุกบล็อก `@media print` เป็น `all` ชั่วคราวแล้ววัดค่าที่คำนวณจริง (แม่นกว่าอ่าน CSS
>   เฉยๆ · คืนค่ากลับแล้ว) — **ได้ 2 แผ่น ไม่ใช่แผ่นเดียว** แต่ไม่มีแถวไหนถูกผ่าครึ่งและไม่ล้นแนวนอน
>   สักตาราง · ทดสอบตอนธีมมืดเปิดอยู่ซึ่งเป็นเคสอันตรายสุด: โทเคนพลิกกลับชุดสว่างจริง เส้นคาดหัว
>   `2px solid rgb(43,36,31)` และเส้นใต้หัวตารางครบทั้ง 3 ตาราง · รายละเอียดที่หมายเหตุบน `ImmigrationSheet`
>   **✅ ผู้ใช้ตัดสินแล้ว (13 ส.ค. 2026): รับได้ ไม่ต้องบีบลงแผ่นเดียว** — ปิดเรื่องนี้ อย่าไปไล่ย่อฟอนต์
>   หรือตัดคอลัมน์ทีหลังโดยไม่ถาม เพราะแลกมาด้วยความอ่านง่ายตอนยื่นเจ้าหน้าที่ ตม. ซึ่งสำคัญกว่าจำนวนแผ่น
> - คำแปลอังกฤษของสถานที่ในเอกสาร ตม. — ยังต้องอ่านทวนเองก่อนเดินทาง
> - กรอกชื่อตามพาสปอร์ตในหน้า ตม. (เก็บในเครื่องนั้นเท่านั้น อีกเครื่องต้องกรอกเอง)
> - เบอร์สถานทูตในการ์ดฉุกเฉิน — ตรวจซ้ำจากเว็บทางการก่อนเดินทาง (เบอร์ชนิดที่เปลี่ยนได้)
> - ที่นั่ง/อาคารของ VN409 ที่อินชอน (T1 หรือ T2) — `data/transferPoints.ts` ใช้พิกัด T1 ไว้ก่อน
>
> **6. ✅ รัน migration `0027` + `0028` แล้ว (12 ส.ค. 2026)**
> ยืนยันด้วย curl: `custom_places.google_place_id` มีครบ **36/36 แถว** (รัน
> `scripts/backfill-place-ids.mjs` ด้วย) · ตาราง `place_notes` สร้างแล้วและทดสอบ flow เต็มวงจรผ่าน
>
> **7. จองตั๋ว 3 ใบที่ใส่ไว้ในแผง "ตั๋ว/booking" แล้ว (เฟส 3.10 — ผมจองแทนไม่ได้)**
> แถวรออยู่ในระบบแล้ว จองเสร็จแค่กดแก้ใส่เลขที่จอง/แนบไฟล์ตั๋ว
> - 🎫 **Sky Capsule** (14 ต.ค.) — จองได้เลย ควรจองก่อน 2-4 สัปดาห์ ที่ bluelinepark.com
> - 🚌 **Express Bus ปูซาน → ซกโช** (15 ต.ค.) — จองช่วง 1-2 สัปดาห์ก่อนบิน ⚠️ ต้องระบุลง
>   **Sokcho Express Bus Terminal (속초고속버스터미널)** เท่านั้น
> - 🚄 **KTX คังนึง → โซล** (17 ต.ค.) — เปิดจองล่วงหน้า 30 วัน = **ราว 17 ก.ย. 2026**
>
> **9. จองตั๋วเพิ่มอีก 2 ใบจากรอบ 3.11 (แถวรออยู่ในแผง "ตั๋ว/booking" แล้ว)**
> - 🚄 **AREX Express Train** (21 ต.ค. 06:15) — จองล่วงหน้า 1-2 สัปดาห์ที่ arex.or.kr หรือ Klook
>   ⚠️ **อย่าไปเช็คอิน/โหลดกระเป๋าที่สถานีโซล** — City Airport Check-in ไม่รับเวียดนามแอร์ไลน์
>   (รับเฉพาะ KE/OZ/7C/TW/RS/BX/LJ/ZE/LH) ต้องเช็คอินที่เคาน์เตอร์ ICN ตามปกติ
> - 👘 **ร้านเช่าชุดฮันบก** (19 ต.ค. 09:00) — จอง Klook/Trazy ได้สิทธิ์ทำผมฟรี ไม่ต้องต่อคิว
>   ช่วงไฮซีซั่น · ร้านที่ใส่ไว้คือ 한복남 경복궁점 (사직로 133-5) เดินถึงวัง 300 ม.
> - รถไฟโซล → ซูวอน (20 ต.ค.) ไม่ต้องจองก็ได้ — แตะ T-money ขึ้นสาย 1 ได้เลย (จอง ITX ถ้าอยากเร็วกว่า)
>
> **10. 🔴 วัน 21 ต.ค. ยังไม่มีจุดแวะสักจุด (เฟส 28)**
> Plan A มี d10 = **0 แถว** ตารางบินสวยเท่าวันอื่นแล้วก็จริง แต่ยังไม่มีแผนที่/แถบสรุปท้ายวัน
> เพราะของพวกนั้นผูกกับ `trip_stops` · หน้า `/summary` ขึ้นเตือนเองแล้วว่า "⚠️ 1 ยังไม่มีจุดแวะ: 21 ต.ค."
> · เติมแล้วลำดับไม่รวน (แถวเช็คเอาต์ 05:45 เป็น `anchor: "before"` อยู่แล้ว จุดแวะจะไปแทรกระหว่าง
> เช็คเอาต์กับ AREX เริ่มนับ 05:45) และมีเดดไลน์ 07:35 คุมแล้วตั้งแต่เฟส 28
> **เป็นการเขียนลง DB ทริปจริง — รอผู้ใช้สั่งก่อน**
>
> **11. 🟡 `travel_time_cache` RLS ขาด UPDATE policy แต่โค้ดเรียก `.upsert()`** (พบ 17 ส.ค. 2026 ตอนสำรวจ)
> `0010_travel_time_cache.sql` ให้แค่ `select` + `insert` · [travel-time](app/api/travel-time/route.ts) เรียก `.upsert()`
> ซึ่ง PostgREST แปลเป็น `INSERT … ON CONFLICT DO UPDATE` → **ต้องมี UPDATE policy ถึงจะผ่าน**
> ผลจริง: ชนเมื่อไหร่ upsert fail และ route **ทิ้ง error เงียบ** · เกิดเฉพาะตอน race เพราะเช็คแคชก่อนเสมอ
> **ไม่กระทบทริป ไม่ต้องแก้ก่อนบิน** — ทางแก้คือ migration เพิ่ม UPDATE policy 1 บรรทัด ทำตอนแตะโซนนี้รอบหน้า
> · ยังไม่ยืนยันกับ DB จริง (ตั้งใจไม่ยิง) — อ่านจากไฟล์ SQL กับโค้ดเท่านั้น
>
> **12. 🟡 `trip_selections` เป็นตารางตาย** — `grep` ทั้ง repo ไม่มีใครอ้างถึงเลย ถูกแทนด้วย `trip_stops`
> ตั้งแต่ `0006` · ลบได้ตอนงานแพลตฟอร์ม ไม่ต้องรีบ
>
> **8. เช็ค 2 เรื่องที่ข้อมูล Google ชนกับแผนที่วางไว้ (เฟส 3.10)**
> - **กระเช้าซอรัคซานเปิด 09:00 วันศุกร์** ตาม Google แต่แผนตั้งใจขึ้น 07:30 — ถามอุทยาน/เช็ค
>   ประกาศช่วงใบไม้เปลี่ยนสีว่าเปิดเร็วกว่าปกติไหม (ถ้าไม่ ก็เดินวัดชินฮึงซารอตามที่เขียนในโน้ตไว้)
> - **ตลาดจุงอังคังนึงปิด 19:00** ตาม Google แต่แผนตั้งไว้ถึง 21:00 — ตรอกอาหารกลางคืนน่าจะเปิดยาวกว่า
>   แต่ยืนยันไม่ได้จากข้อมูลที่มี


| เมื่อไหร่ | ต้องทำอะไร |
|---|---|
| เฟส 0 | บอกว่ามี GitHub remote ไหม (ถ้ายังไม่มี ต้องสร้างเพื่อ push กันของหาย) |
| เฟส 1 | เปิด **Routes API** ใน Google Cloud Console |
| เฟส 3 | เปิด **Maps JavaScript API** ใน Google Cloud Console |
| ทุกเฟส | รันไฟล์ SQL migration ใหม่ใน Supabase Dashboard → SQL Editor (ผมรัน SQL ตรงๆ ไม่ได้) |
| เฟส 5 | สร้าง Storage bucket ใน Supabase (หรือรัน SQL ที่ผมเตรียมให้) |
| เฟส 6.9 | ✅ ลบ `visited_at` ของ `Mun Dining` (วัน `d0`) แล้ว (8 ส.ค. 2026 — รันเอง) |
| เฟส 7 | อนุมัติเพิ่ม devDependency `vitest` + `vite-tsconfig-paths` (เฉพาะตอน dev ไม่ขึ้น production bundle) |
| เฟส 7-12 | **ไม่มี migration ใหม่เลยทั้ง 6 เฟส** — ตั้งใจออกแบบทางแก้ให้เลี่ยง SQL ทั้งหมด (ดูเหตุผลที่เฟส 9.1) |
| เฟส 13 | ไม่มี — ไม่มี migration ไม่ต้องตั้งค่าอะไรเพิ่ม |
| เฟส 13.5 | **ตั้ง env `TRIP_PIN` และ `TRIP_PIN_SECRET` บน Vercel** (Settings → Environment Variables) แล้ว redeploy · ⚠️ ถ้าไม่ตั้ง production จะ**ไม่มี PIN ป้องกันเลย** เพราะ proxy ตั้งใจให้ปล่อยผ่านเมื่อไม่มี env (กันเว็บตายตอนอยู่เกาหลี) · ค่าทั้งสองอยู่ใน `.env.local` เครื่อง dev |
| เฟส 14 | รัน `0024_place_local_names.sql` + **ทดสอบปุ่ม Naver/Kakao บนมือถือที่ลงแอปไว้จริง** (ผมเช็กแทนไม่ได้ `nmap://` ไม่คืน response) |
| เฟส 15 | ✅ รัน `0025_trip_stops_transfer.sql` แล้ว (12 ส.ค. 2026) · เหลือลองแทรกแถว "✈️ ไปสนามบิน" จริงที่วัน 21 ต.ค. ดูว่าแถบ "ควรออกไม่เกิน HH:MM" ขึ้นถูก |
| เฟส 16 | ✅ รัน `0026_trip_hotels_local.sql` แล้ว + **เติมข้อมูลที่พักแถวเก่าให้แล้วผ่านสคริปต์** (ไม่ต้องกดบันทึกซ้ำใน UI — ดูวิธีที่หัวข้อ 4) · เหลือตรวจคำแปลอังกฤษ + ลองสั่งพิมพ์ `/summary?lang=en&for=immigration` จริงว่าลง A4 พอดีไหม |
| เฟส 19 | ไม่มี — ไม่มี migration ไม่ต้องตั้งค่าอะไรเพิ่ม |
| เฟส 17 | ไม่มี — ไม่มี migration และ Open-Meteo ไม่ต้องใช้ API key · (ถ้าอยากได้พยากรณ์อากาศตอนนี้เลย รอไม่ได้ — มันจะขึ้นเองเมื่อเหลือ ~16 วันก่อนวันนั้น) |
| บั๊ก 21.5 | ✅ รัน `0027_custom_places_google_id.sql` + `backfill-place-ids` แล้ว (12 ส.ค. 2026 — `google_place_id` ครบ 36/36 แถว) · ในไฟล์ยังมี SQL ลบการ์ดซ้ำ comment ไว้ให้เลือกรันเอง (ยังไม่ได้รัน) |
| เฟส 18 | **เปิดเว็บบนมือถือ 1 ครั้งหลัง deploy** ให้ service worker ติดตั้ง แล้วกด "เพิ่มลงหน้าจอโฮม" · **เปิด `/today` และ `/summary` อย่างน้อยคนละครั้งตอนมีเน็ต** ถึงจะเปิดได้ตอนเน็ตหลุด · แล้วทดสอบ offline จริง (airplane mode) — ไม่มี migration ใหม่ |
| เฟส 3.11 | ไม่มี migration · เหลือ **จองตั๋ว AREX + คิวร้านฮันบก** (ดูข้อ 9) และตัดสินใจว่าจะขึ้นบอลลูนฟลายอิงซูวอนไหม (ขึ้นบิน 13:00 วันธรรมดา ต้องวนกลับมาช่วงบ่าย) |
| เฟส 22 | ✅ รัน `0028_place_notes.sql` แล้ว (12 ส.ค. 2026) · ทดสอบ flow โน้ตกับข้อมูลจริงครบทุกเส้นทางแล้วคืนค่าเดิม |
| เฟส 23 | ✅ รัน `0029_custom_places_name_ko.sql` + `0030_bookings_status.sql` แล้ว (ยืนยันด้วย curl 12 ส.ค. 2026 — `bookings.status` มีจริงและมีค่าครบทั้ง 8 แถว) |
| เฟส 25 | ✅ รัน `0031_bookings_walkup_status.sql` แล้ว + ตั้ง env `CRON_SECRET` บน Vercel แล้ว deploy ติด (ยืนยันด้วย curl 13 ส.ค. 2026 — `/api/keep-alive` ตอบ 401 เมื่อไม่มี/ผิด Bearer · ดูหัวข้อ 3.5 กับ 4) |

---

## 4.5 Dynamic Travel Platform Engine — 🔴 **ระยะสร้าง** (ออกแบบ 17 ส.ค. · เริ่มสร้าง 18 ส.ค. 2026)

> **งานคนละก้อนกับทุกอย่างข้างบน** — ข้างบนคือเว็บทริปเกาหลีที่ต้องใช้จริง 11–21 ต.ค.
> ก้อนนี้คือการขยายเป็นแพลตฟอร์มหลายประเทศ · **เอกสารทั้งหมดอยู่ที่ [`docs/engine/`](docs/engine/README.md)**

> ### 🔴 อ่านกล่องนี้ก่อนเชื่ออะไรในหัวข้อ 4.5 (แก้ 24 ส.ค. 2026)
>
> หัวข้อนี้เขียนเมื่อ **17 ส.ค.** ตอนที่ยังเป็นระยะออกแบบ · **มี 3 ข้อความข้างล่างที่ตายไปแล้ว ห้ามอ้างอิง:**
>
> | ข้อความเดิมในหัวข้อนี้ | ความจริงวันนี้ |
> |---|---|
> | *"ออกแบบก่อน ยังไม่เขียนโค้ด · รอลงมือจริงหลัง 21 ต.ค."* | 🔴 **`D33` (18 ส.ค.) ผู้ใช้สั่งเริ่มสร้างทันที** — เขียนโค้ดจริงได้แล้ว **เฉพาะบน branch `platform` ที่ `/Users/park/plan-korea-platform`** |
> | *"dev DB = Supabase local ผ่าน Docker"* | 🔴 **`D14` (17 ส.ค. เย็น) เปลี่ยนเป็น Supabase คลาวด์ org ใหม่** — org `Plan-trip-app` · project `engine-dev` · ref `pmvxwcimjebogjfimzqy` |
> | *"กติกาเหล็กของระยะออกแบบ"* 5 ข้อ | 🔴 **มีฉบับระยะสร้างแทนแล้วที่ [`docs/engine/README.md`](docs/engine/README.md)** — อ่านฉบับนั้น |
>
> ✅ **สิ่งที่ไม่เปลี่ยนเลยสักข้อคือความปลอดภัยของทริป:**
> `main` **freeze เหมือนเดิมทุกตัวอักษร** (แก้ได้เฉพาะบั๊กที่ทำให้ใช้งานไม่ได้ + เนื้อหาทริป)
> · **ห้ามแตะ `ejzibhgqhxdzkovsnpds` ทุกกรณี** · **ห้าม merge `platform` → `main` ก่อนกลับจากทริป 21 ต.ค.**
> · **ห้าม `git checkout` สลับ branch ในทรีหลัก** — ใช้ `git worktree` เท่านั้น
>
> **ผู้ใช้ตัดสินเพิ่ม 3 ข้อเมื่อ 24 ส.ค. 2026** → `D41` ขอบเขต = **M1 เท่านั้น** · `D42` ล็อกอิน = **Google OAuth + magic link ทั้งสองทาง**
> · `D43` ของค้างทริปเป็นรางที่ 2 ที่ P1 ถือเอง · รายละเอียดเต็มอยู่ใน `docs/engine/README.md`

**โจทย์:** ขยายจากเว็บทริปเกาหลีของคน 2 คน → engine ที่รับทริปประเทศไหนก็ได้
+ AI Copilot + Real-time Collaboration + Mobile App

**ทีมขยายจาก 4 เป็น 8 เซสชัน** (17 ส.ค. 2026) — P5-AI/Agent · P6-DevOps · P7-Mobile · P8-PM/BA
เข้ามาเพิ่ม · `TEAM.md` อัปเดตแล้วทั้งกลไกหาบทบาทตัวเอง (ต้องเห็น 7 ขาด 1) และตารางโซน

### ผู้ใช้ตัดสิน 4 ข้อ (17 ส.ค. 2026)

| เรื่อง | คำตอบ |
|---|---|
| จะเดินงานแบบไหน | ~~**ออกแบบก่อน ยังไม่เขียนโค้ด** — ส่งมอบเอกสาร รอลงมือจริงหลัง 21 ต.ค.~~ 🔴 **ตายแล้วโดย `D33` (18 ส.ค.)** — สร้างได้เลยบน `platform` |
| dev DB | ~~**Supabase local ผ่าน Docker** ไม่สร้างโปรเจกต์คลาวด์ใหม่~~ 🔴 **ตายแล้วโดย `D14` (วันเดียวกัน)** — ใช้คลาวด์ org ใหม่ `pmvxwcimjebogjfimzqy` |
| P5–P8 | เปิดเซสชันแล้ว ยืนยันครบใน `cwd=/Users/park/plan-korea` |
| งบประมาณ | **คงไว้ ไม่ทำ** — ยึดตามหัวข้อ 1 เดิม · P5 ห้ามใส่ budget optimization |

### ผู้ใช้ตัดสินเพิ่มอีก 4 ข้อ (17 ส.ค. 2026)

| เรื่อง | คำตอบ | ผลต่อแผน |
|---|---|---|
| ทริปเกาหลีหลัง migrate | **ทริปเต็มรูปที่ยังแก้ได้** ไม่ใช่คลังอ่านอย่างเดียว | **E7 หนักขึ้น** — ต้องแมป `added_by` ทุกแถวเข้าบัญชีจริง และต้องมีโมเดล conflict ตอน 2 คนแก้พร้อมกัน |
| Mobile app | **ตัดออก** — ผู้ใช้ระบุว่าเปิดผ่าน Safari แล้วเพิ่มลงหน้าจอโฮมก็ใกล้เคียงแอปมากแล้ว | **ตัด E8b ทิ้ง** · ไม่ต้องจ่าย Apple $99 · ไม่มีโค้ดเบสที่ 2 · **PWA เดิมคือคำตอบสุดท้าย** |
| uptime monitor ก่อนทริป | **เอา** | ผู้ใช้ต้องตั้งเอง (ผูกบริการภายนอก) — วิธีตั้งอยู่ในหัวข้อ 4 |
| dev DB | ถาม: ใช้ Supabase คลาวด์แทน Docker ได้ไหม → **ได้ และดีกว่าด้วย** | ดูด้านล่าง |

### 🔴 dev DB — เปลี่ยนจาก Docker เป็น Supabase คลาวด์ในองค์กรใหม่

**ยืนยันจากหน้าราคาจริง (17 ส.ค. 2026):** free plan = **"Limit of 2 active projects"** ต่อ**องค์กร**
· ไม่มี automatic backup และไม่มี PITR เลย · Pro เริ่ม $25/เดือน
ตอนนี้องค์กรปัจจุบันมี `plan-korea` + `mu-phone` = **ครบ 2 พอดี**

**ทางที่เลือก: สร้าง organization ใหม่ → ได้อีก 2 โปรเจกต์ฟรี** (staging + prod ของแพลตฟอร์ม)
**เหตุผลที่ดีกว่า Docker ไม่ใช่แค่ประหยัดแรงติดตั้ง:**
- P6 ระบุว่า local **ทดสอบ OAuth provider ภายนอกกับ Supavisor pooler ไม่ได้** ซึ่งเป็น 2 จุดที่ "บนเครื่องผ่าน ขึ้นจริงพัง" บ่อยที่สุด
- **องค์กรแยก = ขอบเขตแข็งกว่าโฟลเดอร์แยก** — คนละ org ไปเลย ไม่มีทางกดผิดโปรเจกต์ข้ามไปโดน DB ทริป
- ไม่กิน RAM ~4 GB บนเครื่องที่ต้องรัน `next dev` + เบราว์เซอร์อยู่แล้ว

⚠️ **ข้อแลก:** free tier หลับเมื่อไม่มีทราฟฟิก 7 วัน (เหตุผลเดียวกับที่ทริปต้องมี cron `keep-alive`) — staging ต้องมีกลไกเดียวกัน
⚠️ **ไม่มี backup/PITR บน free** — ตอนขึ้น prod จริงของแพลตฟอร์มต้องขยับเป็น Pro ตามที่ P6 ประเมินไว้

### 🔴 กติกาเหล็กของระยะออกแบบ — ⚠️ **ฉบับนี้หมดอายุแล้ว · ฉบับที่ใช้จริงอยู่ที่ [`docs/engine/README.md`](docs/engine/README.md)**

1. **ห้ามแก้โค้ดแอป** — `app/` `components/` `hooks/` `lib/` `data/` `supabase/migrations/` แตะไม่ได้
2. **ห้ามแตะ Supabase `ejzibhgqhxdzkovsnpds`** ทุกกรณี
3. **SQL ที่ออกแบบไว้ต้องอยู่ `docs/engine/schema/` ห้ามวางใน `supabase/migrations/`**
   เพราะหัวข้อ 5 ให้ผู้ใช้ copy-paste รันเอง — ไฟล์ที่นั่งในโฟลเดอร์จริงมีโอกาสถูกรันใส่ DB ทริปโดยเข้าใจผิด
4. 1 คน 1 ไฟล์ใน `docs/engine/` · commit เฉพาะไฟล์ตัวเอง **ห้าม `git add -A`**
5. **ระยะลงมือเริ่มได้ต่อเมื่อทริปจบและผู้ใช้สั่ง** ไม่เริ่มเองอัตโนมัติ

### 10 ข้อที่ขวางการเป็นแพลตฟอร์ม (ตรวจจากโค้ดจริง 17 ส.ค. 2026)

ไม่มี identity เลย · **RLS 53 policy เป็น `using (true)` ทั้งหมด ไม่มี `auth.uid()` สักที่**
· ด่าน PIN กันแค่ Next ไม่กัน Supabase · `day_id` เป็นสตริงตายตัว `d0`–`d10` **ไม่มี `trip_id` ที่ไหนเลยใน 14 ตาราง**
· tenancy key ไม่สม่ำเสมอ (4 ตารางลอย) · ประเทศอยู่ใน TS type ไม่ใช่ข้อมูล · คลังสถานที่อยู่ในไฟล์ TS 71 KB
· **ไม่มี server-side data fetching เลยสักจุด — แตะตาราง Supabase 67 จุดใน 13 ไฟล์ยิงจาก browser**
  (10 hooks ถือ 60 จาก 67 · component ศูนย์) · Storage `booking-files` เป็น public ไฟล์ตั๋วเปิดได้ด้วย URL ล้วน
· `next.config.ts` ว่างเปล่าทั้งไฟล์ · เทสต์ครอบเฉพาะ pure function
→ รายละเอียดเต็มพร้อมเหตุผลที่ [`docs/engine/README.md`](docs/engine/README.md)

### ลำดับเฟสระยะลงมือ — ห้ามสลับ  *(ตัดคำว่า "หลัง 21 ต.ค." ออก 24 ส.ค. — `D33` · เหลือแค่ `E9` Cutover ที่รอหลังทริป)*

`E0` ตั้งราง → **`E1` Identity** → `E2` Schema → `E3` DAL/Server Actions → `E4` Provider registry
→ `E5` UI → `E6` Perf → `E7` ซ้อม migrate ข้อมูลจริง 2 รอบ → `E8` AI + Mobile

**E1 ต้องมาก่อนทุกอย่าง** — 67 จุดที่อ่าน/เขียนตรงจาก browser จะพังพร้อมกันหมดวินาทีที่ RLS จริงถูกเปิด

### สถานะ (อัปเดต 24 ส.ค. 2026)

**ระยะออกแบบ — จบแล้ว**
- [x] สำรวจฐานรหัสทั้งโปรเจกต์ · [x] `TEAM.md` 4 → 8 บทบาท · [x] `docs/engine/README.md`
- [x] `docs/engine/architecture.md` (P1) · [x] ส่งบรีฟเข้าครบ 7 เซสชัน
- [x] เอกสารกลับมาครบทั้ง 8 บทบาท (~1 MB · ข้อตัดสิน `D1`–`D48`)

### 🟢 ส่งต่อ ณ 25 ส.ค. 2026 ~10:30 น. — **อ่านกล่องนี้ก่อนกล่องอื่นทั้งหมด**

**`E1` เหลือเกณฑ์เดียว: `E1-AC8`** · ที่เหลือปิดครบแล้ว

| AC | สถานะ |
|---|---|
| `AC1` `AC2` `AC3` `AC4` | ✅ ผ่าน พร้อมหลักฐาน |
| `AC5` | ➡️ ย้ายออกจาก `E1` (`D58`) → `E2` + `E7`/`E9` |
| `AC6` **ถอด PIN** | ✅ **เสร็จ 25 ส.ค.** (P4 · commit `4fecb30`) · platform `grep TRIP_PIN\|pinAuth` = **0** · `/unlock` หายแล้ว · 🔴 **`main` ยังมีครบ 48 บรรทัด ตามที่ต้องเป็น** |
| `AC7` | ✅ ผ่าน (`user id` เดิม · 1 บัญชี) |
| **`AC8`** | 🔴 **เหลือข้อเดียว — หลักฐานพร้อมแล้ว รอ P4 ตัดสิน** |

**`AC8` — ผู้ใช้ส่งภาพแดชบอร์ดมาแล้ว 25 ส.ค. · ตัวคุมข้างเคียงอยู่ในตำแหน่งปลอดภัยครบ:**
`Confirm email` ✅ **เปิด** · `Allow manual linking` ✅ ปิด · `Allow anonymous sign-ins` ✅ ปิด · Google `Skip nonce checks` ✅ ปิด · Google `Allow users without an email` ✅ ปิด
🔴 **แต่ไม่มีสวิตช์ไหนชื่อ "link เฉพาะเมื่อ verified"** — เห็น**ตัวคุมข้างเคียง** ไม่ใช่**กติกาการ link** ซึ่งเป็นพฤติกรรมภายในของ Supabase
· **P4 เป็นคนประกาศ ไม่ใช่ P1** · P1 เอียงไปทาง *"ตัวคุมข้างเคียงปลอดภัยครบ · กติกายังไม่ถูกยืนยันโดยตรง"*

### 🔴 ของที่ค้างกลางอากาศ — ห้ามลืม

1. ✅ **ปิดแล้ว 25 ส.ค. — P3 แก้คอมเมนต์ใน `sw.js` (`ab237d4` บน `platform`)**
   `public/sw.js` เคยเขียนว่าจงใจไม่ precache HTML **เพราะด่าน PIN ตอบ 307** · PIN หายแล้วเหตุผลนั้นหมดอายุ
   · ✅ **แก้แค่คอมเมนต์ · ไม่แตะ behavior · ไม่เปิด precache** (P1 ตรวจ diff แล้ว: บรรทัดที่ไม่ใช่คอมเมนต์ = **0**) · `main` ไม่ถูกแตะ
   · 🔴 **การตัดสินใจจริงยังไม่เกิด — ยกไป `E6-AC3`** · *"เหตุผลที่ห้ามหายไป" ไม่เท่ากับ "ตอนนี้ทำได้แล้ว"* (`D35`)
     ⚠️ และมีเงื่อนไขใหม่ที่ไม่เคยมีตอนเขียนข้อห้ามเดิม: **หน้าที่ต้องล็อกอินแล้ว precache ไว้ อาจเสิร์ฟ HTML ของคนอื่นจากแคช**
2. **push ค้าง** — `main` **5** · `platform` **11** commit *(นับจริง 25 ส.ค. 11:2x น.)*
3. ✅ **`E2` เริ่มแล้ว 25 ส.ค. — ตารางเนื้อหาตัวแรกลงฐานจริงแล้ว** *(P1 · platform `7f985e3`)*
   `trip_days` (`E2-AC4`·`E2-AC3`) + `app.can_write_trip()` + `updated_at` trigger (`E2-AC9`)
   · ยิงเข้า `engine-dev` ผ่าน `npm run db:push` แล้ว · **เห็นแดงด้วย `PGRST205` ก่อน push แล้วเขียวหลัง push**
   · 🎯 **`P-46` ปิดแล้วในทางปฏิบัติ** — เคสแรกในประวัติโปรเจกต์ที่ `viewer` เขียนไม่ได้แต่ `editor` เขียนได้
   · เพิ่มด่านโครงสร้าง 2 ตัวที่**ไม่ต้องรู้จักตารางในอนาคต**: policy ฝั่งเขียนห้ามกรองด้วย `can_read_trip`
     · ตารางเนื้อหาต้องขึ้นทะเบียนก่อน (ตารางใหม่ที่ไม่มีเคส 2 ทิศจะไม่ผ่านเงียบ ๆ)
   · 🔴 **ก้อนถัดไปของ P1:** `trip_plans` + `trip_day_plan_settings` (`D69`) → `trip_stops` (rank key `E2-AC8`) → `trip_invites` (`D68`)

### 🧰 เครื่องมือที่เพิ่งสร้าง ต้องรู้ว่ามีอยู่

- **`npm run db:push`** เท่านั้น · **ห้าม `supabase db push` ตรง ๆ** (ตรวจปลายทางก่อนยิง · `D50`)
- **`app.unsafe_state`** — ธงบอกว่า *"ฐานถูกทำให้พังโดยตั้งใจอยู่ตอนนี้"* · RPC 3 ตัว (`unsafe_state_reason/set/clear`) service_role เท่านั้น
  · เมทริกซ์เช็คตอนเริ่ม → **เจอธง = ล้มพร้อมบอกเหตุผล** · `mutation-test-trips-select.sql` ปัก/เก็บให้อัตโนมัติ
- **legacy JWT keys ของ `engine-dev` ถูกปิดถาวรแล้ว** (25 ส.ค.) — ใช้ `sb_publishable_`/`sb_secret_` เท่านั้น · ยิงทดสอบยืนยันแล้วว่า legacy ตอบ `401`

---

### 🔴 สถานะสด ณ สิ้นวัน 24 ส.ค. 2026 — อ่านข้อนี้ก่อนถ้าเพิ่งกลับมา

**อยู่ที่ `E1` Identity ~75%** · `E0` จบแล้ว · `E2`–`E9` ยังไม่เริ่ม (ยกเว้น `E2-AC6` ที่ทำล่วงหน้าแล้ว)

**ผ่านแล้ววันนี้:**
- ✅ `db push` สำเร็จ — สคีมา identity อยู่บน `engine-dev` จริง
- ✅ **`E1-AC1` ผ่าน** — ล็อกอิน Google ได้จริง · session รอดข้ามการโหลดหน้า (ยืนยันในเบราว์เซอร์ที่ `/account`)
- ✅ self-check 14/14 · ผู้ใช้ตั้ง `.env.local` ของทรี `platform` แล้ว (ชี้ `engine-dev`)
- ✅ `E2-AC6` แมป 111 คอลัมน์ครบ → [`docs/engine/column-map.md`](docs/engine/column-map.md)

### ✅ ~~ตัวขวางที่ใหญ่ที่สุด — CLI เข้า `engine-dev` ไม่ได้~~ **แก้แล้วคืน 24 ส.ค. 2026**

**ผู้ใช้ออก access token ใบใหม่จากหน้า Account → Access Tokens แล้วรัน `supabase login --token` เองในเทอร์มินัล**
(ค่า token ไม่เคยผ่านแชทเลยทั้งกระบวนการ) · `projects list` เห็น `engine-dev` แล้ว · **`.temp/project-ref` ของเดิมยังตรง ไม่ต้อง link ใหม่**

### 🟢🟢 แล้วคืนนั้นเดินต่อได้จนจบขั้นที่ 4 — **และได้หลักฐานจากฐานจริงชุดแรกของโปรเจกต์**

| ขั้น | ผล |
|---|---|
| 1 `projects list` | ✅ เห็น `pmvxwcimjebogjfimzqy` |
| 2 `npm run db:push:self-test` | ✅ **13/13** |
| 3 `.env.local` ทรี platform | ✅ ครบ 3 ตัว (ผู้ใช้ใส่แล้ว) |
| 4 `npm run db:push` | ✅ **ลงครบ 3 migration** (`…220618` `…221550` `…222206`) · `--dry-run` เห็นตรงก่อนยิงจริง · **ตัวที่จอดไม่ถูกแตะ** |

**ยิงจริงใส่ `engine-dev` แล้วผ่านหมด 8 ข้อ:**
```
✅ ทางเก่า insert().select() ยังถูกปฏิเสธ   ← P-27 เป็นข้อเท็จจริงแล้ว ไม่ใช่สมมติฐาน
✅ RPC create_trip คืนแถว · created_by = auth.uid()
✅ อ่านทริปกลับได้ด้วย client ปกติ          ← P-13 ไม่กลับมา
✅ แถว owner ถูกสร้างโดย AFTER trigger      ← P-29 ยังถูกปิด
✅ anon → "permission denied for function create_trip"  ← P-32 ถูกกันที่ชั้นสิทธิ์ ไม่ทะลุถึง body
✅ service_role ลบทริปได้                   ← P-28 grant ทำงาน
```
🎯 **และข้อสุดท้ายตอบคำถามที่ค้างมาทั้งวัน: คีย์ `sb_secret_` สวม role `service_role` จริง**
สมมติฐานที่ว่ามันอาจไม่ map — **ตัดออกได้** · `permission denied` ที่วัดได้ก่อนหน้าเป็นเรื่อง **grant ที่ขาดล้วน ๆ**

### 🎉 **เมทริกซ์ RLS รันเต็มชุดแล้วเป็นครั้งแรกในประวัติโปรเจกต์** (P4 แก้ `mk()` → RPC · commit `dc386ec`)

```
ทั้งวันก่อนหน้า : 20 ผ่าน · 28 skip · suite แดงที่ beforeAll
ตอนนี้          : 48 ผ่าน · 0 skip
ทั้งโปรเจกต์     : 275 ผ่าน · 0 skip · 24 ไฟล์   ← P1 รันยืนยันเองซ้ำแล้ว
```
**เคสสด 28 ตัวไม่เคยรันเลยสักครั้งจนถึงคืนนี้** · `afterAll` ของ P4 รัน 2 รอบติดไม่มีคำเตือน = **grant `…222206` เล็งถูกจุด ยืนยันแล้ว**

| AC | สถานะ | หลักฐาน |
|---|---|---|
| `E1-AC1` | ✅ | ล็อกอิน Google จริงในเบราว์เซอร์ (P1) |
| `E1-AC2` | ✅ | เทสต์อ่าน migration ที่รันจริง ไม่ใช่เอกสาร |
| `E1-AC3` | ✅ | `C` ที่ไม่ใช่สมาชิก อ่าน/แก้ทริป `A` ไม่ได้ — ยิงจริงใส่ engine-dev |
| `E1-AC4` | ✅ | anon ได้ `[]` หรือถูกปฏิเสธครบ 3 ตาราง · insert ตรงถูกปฏิเสธ**เพราะสิทธิ์** |
| `E1-AC5` | ➡️ **ย้ายออกจาก `E1`** (`D58`) | `added_by` เป็นคอลัมน์ของ `trip_stops`/`custom_places` — **ตารางที่ `E1` ไม่ได้สร้าง** · ครึ่งสคีมา → `E2` · ครึ่งข้อมูล → `E7`/`E9` |
| `E1-AC7` | ✅ **ผ่าน (25 ส.ค. · P4 ประกาศ)** | ผู้ใช้ล็อกอิน Google → ออก → กด magic link ในอีเมล → เข้าได้ · วัดจากฐาน: **1 บัญชี · `id` เดิม `b56d0964-…`** · เคสหายนะ (2 แถว) ไม่เกิด |
| `E1-AC8` | 🔴 **ยังไม่ปิด — และเหตุผลคือ *ไม่มีอะไรให้วัด*** | Supabase **ไม่ได้ link identity สองอัน** มันแมตช์ `auth.users.email` แล้วออก session · **เหตุการณ์ที่ `AC8` ถามถึง ยังไม่เคยเกิดในระบบนี้สักครั้ง** · **การที่มันไม่แดง ไม่ได้แปลว่าเขียว** → ต้องเปิดดูสวิตช์ auto-link ในแดชบอร์ด |

### 🔴 `D64` — เครื่องมือวัด `AC7` **พังทั้ง 3 ตัว และพังทางเดียวกัน**

| เครื่องมือ | เจ้าของ | พังยังไง |
|---|---|---|
| `selfcheck` ข้อ 9.2 | **P1** | เขียนว่าต้องได้ `identity_rows = 2` — **ของจริงได้ 1** (แก้แล้ว) |
| สคริปต์วัด `listUsers()` | P4 | ไม่คืน `identities` เลย → เห็น "0 ทาง" (แก้แล้ว) |
| หน้า `/account` | P2 | นับจำนวน identity → ขึ้น *"ยังวัดไม่ได้"* **ตลอดกาล** (ส่ง P2 แก้แล้ว) |

**ทั้งสามเข้ารหัสข้อสมมติเดียวกัน: "การ link จะสร้าง identity แถวที่สอง" — ไม่มีใครเคยวัดว่าจริงไหม ก่อนเขียนลงเครื่องมือ**
🔴 **และเรารอดเพราะเดาผิดในทิศที่ปลอดภัย ไม่ใช่เพราะเครื่องมือดี** — เดา 2 ได้ 1 → อ่านเป็น *"ยังไม่ผ่าน"* → ไปตรวจต่อ
**ถ้าเดากลับทาง (คาด 1 ของจริง 2) เราจะติ๊กผ่านทันทีบน AC ที่ล้มจริง** และอาการคือ *"ทริปหายทั้งใบ"* ที่ `AC7` เขียนไว้เองว่ากลัวที่สุด
· ⚠️ **ผู้ใช้ทำถูกทุกขั้นแล้วหน้าจอบอกว่ายังไม่ผ่าน — เขาเกือบกดซ้ำอีกรอบเพราะเชื่อหน้าจอ**
· 📌 `selfcheck` **ข้อ 9.3 ยังไม่เคยถูกวัดโดยใครเลย** (`service_role` ไม่มีสิทธิ์บน `profiles`) → รวมเข้ารอบ SQL Editor แล้ว

### 🔴 คีย์หลุดจากคำสั่งที่ P1 รันเอง (25 ส.ค. เช้า)

`supabase projects api-keys` คืน**ตัวคีย์เต็ม** ไม่ใช่แค่รายชื่อ → **legacy `service_role` JWT + legacy `anon` JWT ของ `engine-dev` หลุดเข้าแชท**
· ขอบเขต: **`engine-dev` ใบเดียว** — ในนั้นมีแค่บัญชีทดสอบของผู้ใช้ ไม่มีข้อมูลทริปจริง · ไม่กระทบ `Korea-Trip`/`mu-phone`
· 🟢 **คีย์ที่หลุดไม่ใช่คีย์ที่ระบบใช้** — `.env.local` ใช้ `sb_secret_…`/`sb_publishable_…` ทั้งคู่แล้ว **legacy เป็นของค้างที่ไม่มีใครใช้**
· **ทางแก้:** ปิด legacy keys ที่ Supabase → Settings → API Keys
  ⚠️ **ลำดับสำคัญ:** อัปเดต GitHub secret `DEV_SUPABASE_ANON_KEY` (ล่าสุด 18 ส.ค. = น่าจะยัง legacy) **ก่อน**ปิด ไม่งั้น CI แดง
· 🔴 **บทเรียน: คำสั่งที่ชื่อว่า `api-keys` คืนคีย์จริง — ต้องกรอง output ก่อนรัน ไม่ใช่หลังเห็น**
~~`E1-AC6` 🔴 ยังไม่เริ่ม~~ → ✅ **เสร็จแล้ว 25 ส.ค. (P4 · commit `4fecb30`)** — ดูกล่องส่งต่อด้านบน
  🔴 **แถวนี้ค้างเป็นเท็จอยู่ครึ่งวัน ในย่อหน้าที่ไม่เกี่ยวกับมันเลย** (P1 เจอ 25 ส.ค. บ่าย) —
  แถวตารางที่หลุดออกมาจากตารางแม่ **ไม่มีหัวตารางคุม จึงไม่มีใครอ่านมันพร้อมของจริง**

### 🔴 `P-35` / `D66` — ช่องที่หลักฐานความปลอดภัยหลักของเรามองไม่เห็น (25 ส.ค.)

เริ่มจากคำถามธรรมดา: *"ถ้า SQL ก้อนคืนค่าถูกพิมพ์กว้างกว่าเดิมนิดเดียวล่ะ"*

> `using (app.can_read_trip(id) **or created_by = auth.uid()**)` → **เคยทั้ง 67 เคสยังเขียวครบ**
> เจ้าของเห็นทริปตัวเอง ✓ · สมาชิกเห็น ✓ · คนนอกไม่เห็น ✓ (**`created_by` ไม่ช่วยคนนอก**)
> 🔴 **ไม่มีเคสไหนถามว่า "คนสร้างที่ถูกถอดออกจากทีมแล้ว ยังอ่านทริปได้ไหม"** (`grep` = 0)

**= `P-15` (สิทธิ์ต้องมาจากแหล่งเดียว) กลับมาได้เงียบสนิท** — `created_by` กลายเป็นสิทธิ์ถาวรที่ถอดไม่ได้

**ปิดแล้ว 3 ชั้น:**
1. **migration `reassert_trips_select`** — ประกาศ policy ใหม่จากไฟล์ **ฐานตรงกับไฟล์โดยไม่ต้องเชื่อว่าใครพิมพ์อะไร** · *ความเชื่อใจไม่ใช่กลไก*
2. **เคสใหม่** (P4) — คนสร้างที่ถูกถอดออก ต้องอ่านไม่ได้ · **พร้อมคู่ด้านบวก** (สมาชิกยังเห็น) ไม่งั้นเคสจะเขียวเท่ากันถ้าทริปหายไปเฉย ๆ
3. **fingerprint ของเงื่อนไข policy** (P4) — จับ *"ชื่อเดิมแต่เนื้อเปลี่ยน"* **แม้เรายังนึกไม่ออกว่ามันจะกว้างยังไง**

📌 **ข้อเสนอแก้ด่านของ P1 เอง ก็ยังมองไม่เห็น `P-35`** — ผมเสนอให้นับชื่อ policy ที่ไม่ซ้ำ **ซึ่งเป็นพร็อกซีคนละตัวของเรื่องเดียวกัน** · P4 จับได้โดยใช้ของที่ผมเพิ่งค้นพบเองมาเป็นตัวอย่าง
🎯 **และเหตุผลที่ห้ามให้ด่านแดงผิด:** ทางแก้จะเป็นการขึ้นเลขเสมอ **แล้วหลังจากนั้นมันจะไม่กัดอะไรอีกเลย — การแดงผิดครั้งเดียวฆ่าด่านได้ถาวร**

~~**เมทริกซ์ตอนนี้ 69 เคส · ทั้งชุด 296 ผ่าน 0 skip**~~
🔴 **แก้ 25 ส.ค. — ตัวเลข `296` เป็นของ*ก่อน* `AC6` และ commit ของ `AC6` เองก็เขียนไว้แล้วว่าเหลือ `267`**
`4fecb30` ลบ `lib/__tests__/pinAuth.test.ts` (220 บรรทัด · 24 เคสที่ทดสอบโมดูลที่ไม่มีอยู่แล้ว) — **กล่องนี้ไม่ได้ถูกอัปเดตตาม**
· **นับจริง 25 ส.ค. 11:17 น.: เมทริกซ์ 85 เคส · ทั้งชุด 283 ผ่าน · 0 skip · 24 ไฟล์**
· ⚠️ **ชุดสดรันได้ต่อเมื่อโหลด `.env.local` เข้า env ก่อน** — `readEnvKey` อ่าน `process.env` ล้วน
  และ vitest **ไม่โหลด `.env.local` ให้** · `npx vitest run` เปล่า ๆ = **59 skip โดยไม่มีอะไรบอกว่าพลาด**

🔴 **แก้ 24 ส.ค. — `E1` มี 8 ข้อ ไม่ใช่ 6 · `AC5` กับ `AC8` ไม่ถูกเอ่ยถึงเลยสักครั้งทั้งคืน** (P4 ไปเปิด `backlog.md` เจอ)
· **P1 รายงานผู้ใช้ไปแล้วว่า "เหลือ 2 ข้อ" ซึ่งผิด — แก้ที่ผู้ใช้แล้ว**
· 🎯 **`AC8` สำคัญกว่าที่เห็น: ผู้ใช้กด magic link ครั้งเดียว** ถ้าดูแค่ `AC7` เราจะติ๊กผ่านแล้วเดินต่อ **โดยไม่เคยถามคำถามของ `AC8` เลย**
  → P4 จะอ่าน `email_confirmed_at` / `identities[].identity_data.email_verified` ทั้ง 2 provider **ก่อน**ประกาศ `AC7` · และเปิดดูสวิตช์ auto-link ของ Supabase ซึ่ง**ทีมยังไม่เคยดูเลยสักครั้ง**

### 📋 รายการ "รบกวนผู้ใช้ครั้งเดียว" ตอนเช้า — รวมไว้ที่นี่ อย่าทยอยถาม

1. **magic link** ด้วยอีเมลเดิม → ปิด `AC7` **และ `AC8`** พร้อมกัน
2. **mutation test** — [`pending-review/mutation-test-trips-select.sql`](supabase-platform/pending-review/mutation-test-trips-select.sql) ใน SQL Editor (2 นาที)
3. ✅ **ตอบแล้ว 25 ส.ค. — `D68`** *(บันทึกที่ [`docs/engine/README.md`](docs/engine/README.md) บน branch `platform`)*
   ผู้ใช้ตอบว่าอยากได้ทั้ง **PIN ชั่วคราว 1 วัน · กรอกอีเมล · สมุดรายชื่อเพื่อน/ครอบครัว**
   → P1 อ่านเป็น **1 กลไก + 3 ทางเล็ง** · `trip_invites` ใบเดียว + RPC redeem ตัวเดียว · สมุดที่อยู่ไม่ถือสิทธิ์
   🔴 **และมันบังคับให้ `P-46` (editor ≟ viewer) ต้องตัดสินใน `E2` ไม่ใช่หลัง** — คำเชิญต้องเลือก role
   แต่ `D61` วัดแล้วว่าสองบทบาทนี้สิทธิ์เท่ากันเป๊ะ → จะได้ตัวเลือกที่ฐานไม่รู้จักความต่าง
4. ✅ **ปิดแล้ว 25 ส.ค. — `mu-phone` ไม่ได้พัง** (ผู้ใช้สั่งทีม `mu-phone` เช็คตัวเองแล้วส่งผลกลับมา)
   service role key ใช้ได้ทั้ง 2 ฐาน (`smqpq…` ฐานจริง · `mmbui…` ฐานทดสอบ) · อ่านตารางที่ RLS ปิดได้ · `anon` ยังถูก RLS กันไว้ทั้งคู่
   ⚠️ **ผลนี้ตอบว่า “ไม่พัง” ไม่ได้ตอบว่า “คีย์ที่หมุนเป็นของใคร”** — ไม่ต้องตามต่อ เพราะไม่มีใครเดือดร้อนแล้ว
5. **Google client secret ไปแก้ที่ Supabase แล้วหรือยัง**
6. **push** — `main` · `platform`

🔴 **`AC6` (ถอด PIN) ไม่อยู่ในรายการนี้โดยตั้งใจ** — P4 ขอทำตอนมีคนตื่นอยู่ด้วย **มันไม่ใช่ของที่ "ถามครั้งเดียวแล้วจบ"**

### ✅ **mutation test รันแล้ว 25 ส.ค. — ผ่านทั้ง 2 ทิศ (`D65`)**

| ก้อน | ผล |
|---|---|
| **A** หละหลวม `using (true)` | 🔴 แดง **5 เคส นำโดย `E1-AC3`** — ตรงกับที่ไฟล์ทำนายไว้ล่วงหน้าทุกตัวอักษร |
| **T** เข้มเกินไป `created_by = auth.uid()` | 🔴 แดง **1 เคส — ตัวที่ P4 เพิ่งเพิ่มเอง** |
| **R** คืนค่า | ✅ เขียวกลับ **67/67 · ทั้งชุด 294** (P1 ยืนยันเอง) |

> 🎯 **ครั้งแรกที่เมทริกซ์แดงเพราะ RLS ผิดจริง ไม่ใช่เพราะ harness พัง** — ข้อที่ P4 ไม่ยอมประกาศชัยชนะทั้งคืน **ปิดแล้ว**

🎯 **และมันได้พยานที่แข็งกว่าที่วางแผน:** P4 ขอให้ทำ *"ต่อหน้า P1"* · P8 รันกับผู้ใช้ตอน P1 ไม่ได้ดู **แล้วจดข้อนี้ไว้เองตรง ๆ**
· แต่ P1 บังเอิญรันชุดเทสต์ด้วยเหตุผลอื่น (เช็คคีย์หลังปิด legacy) **ระหว่างที่ก้อน A ยังค้าง** → **เห็นแดงที่ `E1-AC3` โดยไม่รู้ว่ามีใครทดสอบอะไรอยู่** แล้วรายงานเองว่า policy เปิดโล่ง
· **พยานที่ไม่รู้ว่ากำลังถูกทดสอบ แข็งกว่าพยานที่นั่งดูอยู่แล้วรู้ว่าจะเห็นอะไร**
· ⚠️ **อย่าอ่านเป็นระบบเฝ้าระวัง** — เป็นความบังเอิญที่โชคดี ถ้าไม่มีใครรันเทสต์ช่วงนั้น policy จะค้างเปิดโดยไม่มีอะไรบอก

### 🔴 `P-44` — ช่องที่ mutation test **หาไม่เจอ** และเป็นฟีเจอร์หลักของแพลตฟอร์มทั้งตัว (P4 · 24 ส.ค.)

> เขียน `trips_select` เป็น `created_by = auth.uid()` → **เมทริกซ์ 48 เคสเขียวหมด**
> A เห็นของตัวเอง · B เห็นแต่ของตัวเอง · C ไม่เห็นอะไร — **ข้ออ้างด้านความปลอดภัยยังจริงทุกข้อ**
> 🔴 **แต่ทุกคนที่ถูกเชิญเข้าทริป เปิดมาเจอหน้าเปล่า**

เคสด้านลบพิสูจน์ว่า**คนนอกไม่เห็น** · เคสด้านบวกพิสูจน์ว่า**เจ้าของเห็น** · **ไม่มีเคสไหนพิสูจน์ว่า*สมาชิกที่ไม่ใช่เจ้าของ*เห็น**
· เคสที่ใกล้ที่สุดเชิญ `C` เข้ามาแล้วให้ลาออกทันที **โดยไม่เคยถามว่าตอนเป็นสมาชิก `C` เห็นทริปไหม**
· ✅ P4 เพิ่มเคสแล้ว · **policy ถูกอยู่แล้ว แต่ตอนนี้มีอะไรจับถ้ามันเปลี่ยน**

**แล้ว P4 เจอชนิดเดียวกันอีกรอบในไฟล์เดียวกัน — `profiles_select` มี 2 กิ่ง เคยมีเคสอยู่แต่กิ่งแรก**
`or app.shares_trip_with(id)` **ไม่มีเคยเคสไหนแตะเลย** · เป็น self-join บน `trip_members` → **join ผิดข้างเมื่อไหร่ คืน `true` ให้ทุกคน**
· แคบไป = *"ใครเพิ่มจุดนี้"* ว่างเปล่า · 🔴 **กว้างไป = ทุกคนอ่านโปรไฟล์ทุกคนทั้งระบบ** — ทิศหลังไม่มีเคสไหนมองเห็นเลย

### 🟢 `D60` — **วิธีที่หาเจอทั้ง 2 รอบ · ราคาถูกที่สุดที่เรามี**

> **อ่าน policy · นับกิ่งของมัน · แล้วนับว่ามีกี่เคสวิ่งไปถึงแต่ละกิ่ง**

**ไม่ต้องมี DB ไม่ต้องรันอะไร — อ่าน SQL กับนับเคสเท่านั้น** · ทำได้ตั้งแต่เมื่อวานถ้ารู้ว่าต้องทำ
· 🔴 **mutation test หาทั้งสองรอบไม่เจอ** — มันพิสูจน์ว่า assert **ที่มีอยู่**กัด ไม่ได้บอกว่ากิ่งไหนไม่มีใครเดินไปถึง
· เมทริกซ์โต **48 → 60 เคส** ในคืนเดียวจากวิธีนี้ · ทั้งชุด **281 ผ่าน 0 skip**

**ไล่ `trip_members` ครบทั้ง 4 policy แล้ว — เจอกิ่งว่างอีก 4 · เมทริกซ์โตเป็น 66 เคส (ทั้งชุด 287 ผ่าน 0 skip)**
🔴 **กิ่งที่แพงที่สุด: `trip_members_update` ไม่มีเคสไหนถามว่า "viewer เลื่อนตัวเองเป็น owner ได้ไหม"**
> เขียน policy ผิดเป็น `can_read_trip` แทน `trip_role = 'owner'` → **ใครที่ถูกเชิญเข้าทริป ยึดทริปได้ทันที · และ 54 เคสยังเขียวครบ**

### 🔴 `D61` — รากของทั้งตระกูล (P4 ถอด)
> **"แขกไม่ใช่คนนอก · และเมทริกซ์รู้จักคิดแต่เรื่องคนนอก"**

เคสทุกตัวก่อนคืนนี้ใช้ `C` ในฐานะ**คนที่ไม่ได้อยู่ในทริปเลย** · **ไม่เคยใช้ในฐานะคนที่อยู่ในทริปแต่ไม่ควรมีอำนาจ** — ซึ่งเป็นสถานะที่**ผู้ใช้จริงส่วนใหญ่ของแพลตฟอร์มจะอยู่**

### 🔴 และมันพาไปเจอช่อง **ดีไซน์** ไม่ใช่ช่องเทสต์ — `editor` ยังไม่มีความหมายอะไรเลย

```
grep editor  →  พบที่เดียว: check (role in ('owner','editor','viewer'))
policy ทุกตัวที่แยกอำนาจ  →  ใช้ = 'owner' ทั้งหมด
```
**`editor` กับ `viewer` ทำอะไรได้เหมือนกันเป๊ะใน `E1`** — เป็นการ**เลื่อนที่ตั้งใจ** (migration บรรทัด 226 เขียนไว้เองว่ารอ `E2`) **แต่จดไว้ในคอมเมนต์ ไม่ใช่ในเกณฑ์**
🔴 `D47` ตัดสินไว้ว่า **B เป็น `editor` เพราะ "แก้ได้ ลบทริปไม่ได้"** → **ถ้า `E2` ลืม `D47` กลายเป็นเท็จเงียบ ๆ และ B จะดูทริปได้แต่แก้ไม่ได้**
**→ เกณฑ์ใหม่ของ `E2`:** ต้องมี policy อย่างน้อยหนึ่งตัวที่ `editor` ผ่านแต่ `viewer` ไม่ผ่าน **+ เคสที่พิสูจน์** (ส่ง P8 ใส่ `backlog.md` ตอนเช้า)

### ✅ ไล่ครบทุก policy ของ `E1` แล้ว — **26 กิ่ง · เดิม 13 กิ่งไม่มีเคสไหนวิ่งถึงเลย (50%) · ตอนนี้เหลือ 1**

เมทริกซ์ **65 เคส** · ทั้งชุด **294 ผ่าน 0 skip** (P1 รันยืนยันเอง)
📌 กิ่งที่ยังเป็น 0 คือ `profiles_insert` ด้านบวก — **จงใจไม่เติม** เพราะ client ไม่เคยแทรก `profiles` เอง (trigger สร้างให้)
· 🎯 **"จดไว้ดีกว่าเขียนเคสปลอมให้ตัวเลขสวย"**

> 🔴 **ครึ่งหนึ่งของกิ่งไม่เคยถูกทดสอบ ในไฟล์ที่ทั้งทีมเชื่อว่าเป็นหลักฐานความปลอดภัยของเฟส**
> `E1-AC3`/`AC4` ถูกประกาศผ่านไปแล้วบนไฟล์นั้น · **ข้อประกาศยังถูก** (policy เขียนถูกมาตลอด)
> **แต่หลักฐานที่รองรับมันบางกว่าที่ทุกคนเข้าใจ 2 เท่า**

### 🔴 `D62` / `P-48` — ช่องที่ P4 สร้างขึ้นเองโดยการ**ย้ายไปทางที่ปลอดภัยกว่า**

ย้าย fixture จาก `.insert()` ไป `rpc("create_trip")` — **ถูกต้องทุกประการ**
· `create_trip` เป็น `security definer` = **ข้าม RLS** → **ไม่มีอะไรเดินผ่าน `trips_insert` อีกเลย**
· เคสที่เหลือคือ anon ซึ่งถูกกันด้วย **grant ไม่ใช่ policy** → **กฎ "ห้ามสร้างทริปในนามคนอื่น" ไม่มีเทสต์เลยสักตัว**
· **ไม่มีอะไรพัง ไม่มีเคสไหนแดง ไม่มีเทสต์ถูกลบ — จำนวนเคสเพิ่มขึ้นด้วยซ้ำ** · ปิดแล้ว

> **รายการกิ่งหมดอายุทุกครั้งที่มีคนเปลี่ยนทางที่โค้ดเดิน** — `D60` จับได้เฉพาะเพราะ P4 ไล่ซ้ำ*หลัง*แก้

**→ `E2-AC11` ได้นิยามใหม่ และมันปิดข้อเถียงเรื่องตัวหารไปในตัว:**
> **วัด "ทุกกิ่งของทุก policy มีอย่างน้อย 1 เคสวิ่งถึง" ไม่ใช่ "มีเทสต์กี่ตัว"**
> 🔴 **ตัวเลขที่สองโตได้โดยที่ตัวเลขแรกแย่ลง — และ `P-48` คือหลักฐานว่ามันเกิดจริง**

### 🔴 `D63` — ข้อสุดท้ายของคืน และมันย้อนกลับมาที่ตัวเครื่องมือเอง

**บล็อก "ความครบของ matrix" มีจุดบอดเดียวกับที่มันมีหน้าที่จับ**
```ts
const PERSONAS = ["A_owner", "B_other_trip", "C_no_trip", "D_anon"];   // ไม่มี "คนใน" สักตัว
expect(TABLES.length * VERBS.length * PERSONAS.length).toBe(48);       // จริงเสมอ ไม่ว่าทดสอบอะไร
```
🎯 **มันประกาศว่า "ครบ 48 ช่อง" ในขณะที่ 13 จาก 26 กิ่งไม่มีใครเดินไปถึง** — `D61` ถูกเขียนลงในตัวตรวจความครบเอง

**บทเรียนที่ตามมา และเป็นรูปที่ `E2-AC11` จะใช้:**
| รูปของเกณฑ์ | บังคับได้ไหม |
|---|---|
| ตารางกิ่ง 26 แถวในเอกสาร | ❌ ถูกวันที่เขียน · **เงียบวันถัดไป** (หมดอายุกับ P4 เองใน 3 ชั่วโมง) |
| **เลขที่ตรึงไว้ในเทสต์** | ✅ เพิ่ม policy โดยไม่ไล่กิ่ง → **แดง** |
| **ด่านการหายไปโดยตั้งใจ** (`profiles`/`trips` ต้องไม่มี DELETE · `D18`) | ✅ ใครเติมเข้ามาก็แดง — เดิมไม่มีอะไรบังคับ |

### 📊 สิ่งที่เกิดกับเมทริกซ์ในคืนเดียว
```
เริ่มคืน : 20 ผ่าน · 28 skip · แดงที่ beforeAll · เคสสดไม่เคยรันเลยในประวัติโปรเจกต์
จบคืน   : 67 เคส · 0 skip · ทั้งชุด 294 ผ่าน
กิ่งว่าง : 13 จาก 26 (50%)  →  1 (ที่เหลือจงใจไม่เติม พร้อมเหตุผล)
```
**47 เคสที่เพิ่มมา ไม่มีอันไหนซ้ำของเดิม — ทุกอันปิดกิ่งที่ไม่เคยมีใครเดินไปถึง**

🔴 **`P-46` ที่ต้องตัดสินก่อน `E2`:** `editor` กับ `viewer` มีสิทธิ์เท่ากันทุกประการใน `E1` (ดู `D61`)
**ตัวเลขเคสจะยังโตอีก และนั่นคือสัญญาณที่ดี ไม่ใช่สัญญาณว่าของเดิมพัง**
· 🎯 **2 ใน 3 เคสใหม่เป็น mutation test ที่ไม่ต้องใช้ SQL Editor เลย** — *`C` เป็นสมาชิก → เห็น · ถอด `C` ออก → ไม่เห็น* · **mutate สมาชิกภาพ แทน mutate policy**

**→ `D59`: "เขียวเพราะ assert ไม่ไว" กับ "เขียวเพราะไม่มี assert" ใช้เครื่องมือคนละตัว และทีมถืออยู่ตัวเดียวมาทั้งวัน**

**เตรียมไว้แล้ว: [`supabase-platform/pending-review/mutation-test-trips-select.sql`](supabase-platform/pending-review/mutation-test-trips-select.sql)**
· 🔴 **ต้องทดสอบ 2 ทิศ ไม่ใช่ทิศเดียว (`P-44` · `D59`)** — ฉบับแรกของ P1 มีแต่ทิศ "หละหลวม" **ซึ่งเป็นตัวอย่างของปัญหาในตัวมันเอง**
  · ก้อน **A** `using (true)` → ต้องแดงที่ `E1-AC3` (คนนอกเห็นทริป)
  · ก้อน **T** `using (created_by = auth.uid())` → **ต้องแดงที่เคส "สมาชิกที่ไม่ใช่เจ้าของเห็นทริป"**
  · ขั้นตอน **R → A → R → T → R** (คืนค่าคั่นทุกครั้ง)
· ⚠️ **ต้องรันใน SQL Editor ของผู้ใช้** — เครื่องนี้ไม่มี `psql`/docker และ `db push` รันได้แค่ migration ตามลำดับ (เมทริกซ์ต้องรันคั่นกลาง 2 สถานะ)
· 🔴 **ห้ามแปลงไฟล์นั้นเป็น migration** — จะทิ้งสถานะหละหลวมโดยตั้งใจไว้ในประวัติ ซึ่งจะรันบนฐานใหม่ทุกใบตลอดไป
· **ก้อนคืนค่าเขียนไว้ก่อนก้อนที่ทำให้หละหลวม** และมีขั้นตอนบังคับให้รันก้อนคืนค่าก่อนเพื่อพิสูจน์ว่ามันใช้ได้

✅ **`engine-dev` สะอาด — ตรวจแล้ว 24 ส.ค. ปลายคืน:** เหลือ **บัญชีจริงของผู้ใช้ใบเดียว · fixture ค้าง 0**
· โน้ตเดิมที่เขียนว่า *"user ทดสอบของ `diag2` (`66f49941-…`) ยังอยู่"* **หมดอายุแล้ว** — ถูกเก็บไปแล้วระหว่างคืน
· P4 รันชุดเทสต์ราว **12 รอบ** ระหว่างไล่กิ่ง · **ไม่มีคำเตือนเก็บกวาดสักบรรทัด** → `afterAll` ทำงานจริง **ไม่ใช่ข้อสมมติอีกต่อไป**
· 📌 P4 ยังเจอและปิดรูใน `afterAll` ของตัวเองด้วย — `.catch(() => {})` กลืนความล้มเหลวของ `deleteUser` เงียบ ๆ
  **"ครึ่งที่ผมคิดถึงได้ warn · ครึ่งที่ผมไม่ได้คิดถึงถูกกลืน"** — รูปเดียวกับทั้งคืน แค่รอบนี้อยู่ในมือคนเขียนเอง
⚠️ **`authNoServiceRole.test.ts` เพิ่งกลายเป็นด่านจริง ไม่ใช่ด่านตามทฤษฎี** (P4 ชี้) — คีย์อยู่ใน `.env.local` แล้ว โค้ดฝั่งเซิร์ฟเวอร์หยิบได้ · **ห้ามผ่อนด่านนั้นเด็ดขาด**

---

<details><summary>บริบทเดิมของตัวขวางนี้ — เก็บไว้เพราะกับดักยังอยู่</summary>

### 🔴🔴 ~~CLI เข้า `engine-dev` ไม่ได้แล้ว~~ (P1 เจอ 24 ส.ค. หลังหมุนความลับ)

`supabase migration list` → **403** · `supabase projects list` คืนมาแค่ **2 โปรเจกต์** และ **ไม่มี `engine-dev` อยู่ในนั้น**

| ref | ชื่อ | หมายเหตุ |
|---|---|---|
| `ejzibhgqhxdzkovsnpds` | `Korea-Trip` | 🔴 DB ทริปจริง — ห้ามแตะ |
| `pceaovdyjuvwnytlatsw` | `a-gleam` | 🔴 โปรเจกต์ของร้าน — ห้ามแตะ |
| `pmvxwcimjebogjfimzqy` | `engine-dev` | ⛔️ **ไม่อยู่ในลิสต์** — token ใหม่ scope ไม่ครอบ org `Plan-trip-app` |

🔴 **ตรวจซ้ำอีกครั้งหลังผู้ใช้แจ้ง P8 ว่า "หมุนความลับครบ 4 อย่างแล้ว" (24 ส.ค. 22:1x น.) — ยังเหมือนเดิมเป๊ะ**
`projects list` ยังคืนแค่ 2 ใบเดิม · **token ใบใหม่ *ใช้ได้* แต่ scope ผูกกับ org `jndlzeerdeparzaffdlq` เท่านั้น**
· ⚠️ **นี่คือกับดักของข้อนี้: ผู้ใช้ทำครบตามที่ขอแล้วจริงๆ แต่ผลลัพธ์ยังไม่เปลี่ยน** — "หมุนแล้ว" ≠ "หมุนแล้วครอบ org ที่ถูก"
· ถ้า token ใบเก่ายังไม่ถูกเพิกถอน `projects list` จะ 401 ไม่ใช่คืนลิสต์ → **ที่เครื่องถืออยู่คือใบใหม่แน่นอน**

→ **`db push` · เมทริกซ์ RLS · `E1-AC2`–`AC7` ทุกข้อ เดินต่อไม่ได้จนกว่าผู้ใช้จะแก้ token**
🔴 **และห้าม "แก้ให้มันเดินได้" ด้วยการ re-link ไปโปรเจกต์ที่ token เห็นเด็ดขาด** — เหลือให้เลือกแค่ 2 ใบที่ห้ามแตะทั้งคู่
· 🔴 **และ `a-gleam` เดินผ่านด่านของเราได้ทุกชั้นจนถึงเมื่อคืน** — ปิดแล้ว 3 ชั้น (`D48` · `D50`) แต่**ห้ามอ่านว่าปิดสนิท**
  **ใช้ `npm run db:push` เท่านั้น อย่าพิมพ์ `supabase db push` ตรงๆ** — ตัวแรกตรวจปลายทางก่อน ตัวหลังไม่ตรวจอะไรเลย

</details>


### 🟢 ลำดับ 8 ขั้นทันทีที่ token ใช้ได้ — **ห้ามสลับ · เขียนไว้เพราะ `D46`**

> ของครบทุกชิ้นแล้ว **ที่ยังไม่มีคือลำดับการประกอบ** — และนั่นคือชนิดความพลาดที่ `D46` จดไว้เอง
> ทุกคำสั่งรันจาก `/Users/park/plan-korea-platform`

| # | ทำอะไร | ผ่านคือ | ใคร |
|---|---|---|---|
| 1 | `supabase projects list` | 🔴 **ต้องเห็น `pmvxwcimjebogjfimzqy`** · ถ้าเห็นแค่ `Korea-Trip`+`a-gleam` = **ยังไม่ผ่าน หยุด** | ผู้ใช้ |
| 2 | `npm run db:push:self-test` | ผ่าน 5 · ล้ม 0 (พิสูจน์ว่าด่านปลายทางยังทำงาน **ก่อน**พึ่งมัน) | ใครก็ได้ |
| 3 | ใส่ `SUPABASE_SERVICE_ROLE_KEY` ของ `engine-dev` ลง `.env.local` ของทรี platform | มีตัวแปรครบ 3 ตัว | ผู้ใช้ |
| 4 | `npm run db:push` | รัน **3 ไฟล์**: `…220618` `…221550` `…222206` · **ต้องไม่มี `…144235`** (จอดอยู่ `pending-review/`) | ใครก็ได้ |
| 5 | `diag2.mjs` (P4) | 🎯 **ตอบคำถามที่ยังไม่มีใครตอบได้: `sb_secret_` สวม role `service_role` จริงไหม** | P4 |
| 6 | `RLS_MATRIX_REQUIRED=1 npx vitest run rlsMatrix` | 22 เคสสดต้องขยับ **ไม่ใช่ skip** | P4 |
| 7 | `E1-AC7` — ออกจากระบบ เข้าใหม่ด้วย magic link อีเมลเดิม เทียบ `user id` | ตรงกับ `b56d0964-d160-4442-add7-a5a94348f137` | ผู้ใช้ + P4 |
| 8 | `E1-AC6` ถอด PIN (4 ไฟล์) | หลังเมทริกซ์เขียวเท่านั้น | P4 |

🔴 **ขั้น 3 ต้องมาก่อนขั้น 4–6** ทั้ง `diag2` และเมทริกซ์อ่านคีย์ตัวนั้น · **ใส่ในไฟล์เท่านั้น อย่าพิมพ์บนบรรทัดคำสั่งหรือแปะในแชท** (นี่คือคีย์ที่เพิ่งต้องหมุนเพราะมันหลุดออกมา)
🔴 **ขั้น 5 อาจล้มข้อ 6 ทั้งข้อ** — ถ้า `sb_secret_` ไม่ได้ map ไป `service_role` จริง **`…222206` จะ grant ไปที่ role ที่คีย์ไม่ได้สวมอยู่** = แก้ไม่ตรงจุด · **รู้ผลขั้น 5 ก่อนค่อยสรุปว่าขั้น 6 แดงเพราะอะไร**
⚠️ **ขั้น 4 ใช้ `npm run db:push` เท่านั้น** — `supabase db push` ตรง ๆ ไม่ตรวจปลายทางเลย (`D50`)

### 🔴 ค้างอยู่ตรงนี้เป๊ะ — เมทริกซ์ RLS รันครั้งแรกแล้วแดง *(แก้คำวินิจฉัย 24 ส.ค. — P1 รีวิวซ้ำก่อน push)*

1. ✅ **ตัดสินแล้ว 24 ส.ค. — ใช้ RPC `public.create_trip()` แทน · `BEFORE INSERT` ไม่ผ่าน (`D49`)**
   P4 รีวิวแล้ว **หักล้าง `P-27` ไม่ได้ และเอียงไปทางว่า P1 ถูก** แต่ชี้ทางที่ดีกว่าการเถียงต่อ:
   `BEFORE INSERT` **แพ้ทั้งสองกรณี** (ถ้า P1 ถูก = error เดิม + ทิ้ง FK deferrable ฟรี · ถ้า P4 ถูก = แก้ได้แต่เปิด `P-29`
   ช่องยึดทริปคนอื่นเป็น owner ผ่าน `upsert(ignoreDuplicates)`) · **RPC ชนะทั้งสองกรณี → ไม่ต้องรู้คำตอบ**
   · ลงแล้ว `20260824221550_create_trip_rpc.sql` ครบข้อบังคับ 6 ข้อของ P4 · **ไม่แตะ trigger เลย** (คง `AFTER INSERT` = ปิด `P-29`)
   · ⚠️ **ยังไม่ได้รัน** (token) · guards เขียว 9 ด่าน · 247 เทสต์ผ่าน · lint สะอาด

   <details><summary>บริบทเดิมของข้อนี้ (`P-27`) — เก็บไว้เพราะเหตุผลยังใช้อยู่</summary>

   🔒 **ย้ายออกจาก `supabase/migrations/` ไปไว้ที่ `supabase-platform/pending-review/` แล้ว** — `db push` มองไม่เห็น
   (โน้ตที่บอกว่าอย่า push อยู่คนละไฟล์กับคำสั่งที่คนกำลังพิมพ์ · ต้องกันด้วยโครงสร้าง ไม่ใช่ด้วยคำเตือน)
   ไฟล์ `pending-review/20260824144235_fix_trip_visible_on_create.sql` ย้าย trigger เป็น `BEFORE INSERT` ซึ่งแก้ **ลำดับการทำงาน**
   แต่ที่ทำให้ policy ไม่ผ่านคือ **snapshot มองไม่เห็นแถว** ไม่ใช่แถวยังไม่มี — `app.can_read_trip`/`app.trip_role` เป็น `stable`
   จึงใช้ snapshot ของคำสั่งที่เรียก ซึ่งมองไม่เห็นแถวที่ trigger เพิ่งเขียนในคำสั่งเดียวกัน
   · **คาดว่า push แล้วได้ error เดิมเป๊ะ แถมทิ้ง FK `deferrable` ไว้ถาวรโดยไม่ได้อะไร**
   · ทางที่เสนอแทน: `public.create_trip()` `security definer` → ไม่มี `RETURNING` ที่ต้องผ่าน RLS เลย · ไม่ต้องใช้ FK deferrable · `P-15` ไม่ถูกแตะ
   · **สถานะ: สมมติฐานมั่นใจสูง ยังพิสูจน์บนฐานจริงไม่ได้** (ไม่มี psql/docker + token 403)
   </details>

2. ~~`service_role` ไม่มีสิทธิ์ DML — P1 ลืม grant~~ 🔴 **วินิจฉัยผิด (`P-28`)**
   `afterAll` เรียกแค่ `auth.admin.deleteUser` **ไม่แตะ DML สักบรรทัด** · ที่บล็อกจริงคือ **FK ไม่ใช่สิทธิ์**:
   `deleteUser` → `profiles` cascade → ชน `trips.created_by … on delete restrict`
   · และ **ไม่มีทางลบทริปจากทางไหนเลย** (ไม่มี `trips_delete` policy · `authenticated` ไม่มี DELETE บน `trips`)
   · → **grant เฉยๆ ไม่แก้อะไร** · ต้องแก้ 2 ส่วน: `afterAll` ลบทริปก่อนลบ user + `service_role` มี DELETE บน `trips`
   · 🔴 **แก้อีกรอบ — ครึ่งหลังของคำวินิจฉัยนี้ P1 เดาผิด และ P4 มีหลักฐานตรง**
     P1 เขียนว่า *"อาจเป็น error ของ FK ที่อ่านผิด"* · **ไม่ใช่** — บรรทัดของ P4 มาจาก `admin.from("profiles").select("id")`
     **`select` เปล่าๆ ไม่มีการลบ ไม่มี FK เข้ามาเกี่ยว** แล้วได้ `permission denied for table profiles` ตรงๆ
     → **มี 2 เรื่องแยกกัน ทั้งคู่จริง**: FK RESTRICT (P1) **+** `service_role` ไม่มีสิทธิ์ (P4) · **แก้ข้อเดียวไม่พอ**
   · **P4 จะแก้ `afterAll` เอง** แต่รอ P1 grant DELETE บน `trips` ให้ `service_role` ก่อน แล้วแก้+commit ในเทิร์นเดียว
   · ⚠️ **ข้อยกเว้น `D38` ต้องจดใน `TEAM.md` ไม่ใช่แค่ใน commit** — วันนี้มีบทเรียนเรื่องข้อยกเว้นที่ไม่จดแล้วหายไปแล้ว

3. **`E1-AC7` ยังวัดไม่ได้** — ต้องออกจากระบบแล้วเข้าใหม่ด้วย magic link อีเมลเดิม
   แล้วเทียบ `user id` · **ค่าอ้างอิง `b56d0964-d160-4442-add7-a5a94348f137`** (ดูที่ `/account`)

4. **`E1-AC6` ถอด PIN — ยังไม่เริ่ม** เหลือ 4 ไฟล์ · P4 รอเมทริกซ์เขียวก่อนตามลำดับที่ล็อกไว้

**🟢 ตรวจแล้วว่า *ไม่* พัง — สามข้อที่กลัวกันวันนี้ (P1 ยิงจริง 24 ส.ค.):**
- ✅ **เว็บทริปที่ใช้งานจริงไม่ได้รับผลกระทบจากการหมุนวันนี้** — `anon key` ของ `Korea-Trip` ยังตอบ `HTTP 200`
  · และ `grep` ทั้งแอปทริปแล้ว **ไม่มีที่ไหนใช้ service role key เลยสักจุด** (`.env.local` ก็ไม่มี) → หมุน service role key ไม่กระทบเว็บทริปตามนิยาม
- ✅ **OAuth client ที่ `E1` ใช้อยู่ ยังมีชีวิต** — ดึง `client_id` จาก `/auth/v1/authorize?provider=google` ของ `engine-dev` แล้วยิงไป Google · Google รับปกติ
  → **ตัวที่ผู้ใช้ลบใน `A GLEAM` เป็นคนละใบ** ตามที่ตั้งใจ
- 🟡 **แต่ยังยืนยันไม่ได้ว่า client _secret_ ฝั่ง Supabase ตรงกับที่ Google ถืออยู่** (P8 เตือนถูก)
  ทดสอบจากตรงนี้ไม่ได้ — ต้องมี code จากการล็อกอินจริง · **ความเสี่ยงเหลือแค่ "secret ไม่ตรง" ไม่ใช่ "client หาย" แล้ว**

**🔴 ของที่ผู้ใช้ต้องทำ — เรียงตามที่ขวางงานมากที่สุด:**
1. 🔑 **ให้ CLI กลับเข้า `engine-dev` ได้** — ออก access token ใบใหม่ที่**ครอบ org `Plan-trip-app`** (ไม่ใช่ org ของ `a-gleam`/`Korea-Trip`) แล้ว `supabase login`
   · **ทุกอย่างของ `E1` ค้างอยู่ที่ข้อนี้ข้อเดียว**
2. **push ยังไม่ได้ทำ** — `platform` ค้าง 52 commit · `main` ค้าง 6 (มีการแก้ open redirect ที่ `/unlock` ซึ่ง**ยังไม่มีผลบน production จนกว่าจะ push**)
3. 🔑 **หมุนความลับที่เหลือ:** รหัส DB `engine-dev` · Google client secret · **service role key** (หลุดทางภาพหน้าจอ และแตะโปรเจกต์ mu-phone ได้ด้วย)
   · *(Supabase access token หมุนไปแล้ว — และนั่นคือสิ่งที่ทำให้ข้อ 1 เกิด)*
4. ลบ OAuth client ที่เผลอสร้างในโปรเจกต์ Google Cloud ของร้าน `A GLEAM` (⚠️ ห้ามแตะ Maps key · ห้ามทำช่วง 11–21 ต.ค.)

---


### 🟢 คืน 24 ส.ค. 2026 — `E1` ค้างที่ผู้ใช้ ทีมจึงเปิด `E2` ซึ่งเขียนได้โดยไม่ต้องมีฐาน

**`E2` ถูกล็อกไว้เองด้วยเงื่อนไขใน [`column-map.md`](docs/engine/column-map.md): มี 3 คำถามที่ *"ต้องปิดก่อนเขียน DDL ของ E2"***
🔴 **นี่คือเหตุผลที่คืนนี้ไม่มีใครเขียน DDL ออกมา — ไม่ใช่เพราะไม่มีเวลา แต่เพราะลำดับถูกเขียนไว้แล้วและมันห้ามไว้**

| # | คำถาม | สถานะ |
|---|---|---|
| 1 | `trip_hotels.leg_id` — "leg" ไม่มีนิยามบนโมเดลใหม่ | ✅ **ปิดแล้ว → `D51`** |
| 3 | `trips.active_plan_id` ↔ `trip_plans.trip_id` FK วน | ✅ **ปิดแล้ว → `D52`** |
| 2 | `catalog.cities` / `catalog.places` / `place_names` ยังไม่มี DDL | ✅ **ปิดแล้ว → `D53`–`D56`** |

**🟢 คำถามที่ล็อก `E2` ไว้ ปิดครบทั้ง 3 ข้อแล้ว — เขียน DDL ได้แล้ว** · และทีมส่งข้อกำหนดกลับมาเพิ่มอีกชุด:

| เลข | เรื่อง | มาจาก |
|---|---|---|
| `D53` | **แยก `catalog.places` / `custom_places`** · กลับคำ `architecture.md §1.4` ที่ P1 เขียนเอง · **`trip_stops` ใช้ 2 FK + `kind`** (ไม่ใช่ 3 — `D51` ตอบเรื่องที่พักไว้แล้ว) | P5 จับความขัดแย้ง · P3 ให้เหตุผลชี้ขาด (cache policy) · P4 ชี้ปัญหาย้ายข้อมูล |
| `D54` | `catalog.cities` ถือ `lat`/`lng` เอง — **`cityCenter()` วันนี้เฉลี่ยจากลูก เมืองที่มีที่เดียวจะเพี้ยนเงียบ** | P5 |
| `D55` | `place_names` มี `is_primary` + `source` · `address_local` อยู่บน `places` | P5 |
| `D56` | `pg_trgm` + `unaccent` · **ไม่เอา `pgvector`** — *"ถามว่าเติมทีหลังแล้วต้องรื้ออะไร ไม่ใช่ถามว่าจะได้ใช้ไหม"* | P5 |
| **`D57`** | 🔴 **ของที่แก้ทีหลังไม่ได้ 6 ข้อ** — `updated_by_user` · **`created_at` ต้องเป็นของเซิร์ฟเวอร์ด้วย** · ห้าม unique บน `rank` · `trip_hotels` ต้องดัก `23P01` | P7 |

🔴 **`D57` ข้อ 2 เป็นบั๊กที่มีอยู่จริงวันนี้ ยืนยันกับโค้ดแล้ว:** `useChecklist.ts:88` เขียน `created_at` จากนาฬิกาเครื่อง แล้ว `:14` เรียงด้วยมัน
→ **มือถือที่ตั้งเวลาผิดเพิ่มรายการ = รายการนั้นโผล่ผิดตำแหน่งถาวร เหมือนกันทุกเครื่อง แก้ย้อนหลังไม่ได้** · `E2-AC9` พูดถึงแต่ `updated_at` จึงไม่ครอบ

🔴 **`P-33` เปลี่ยนแผนแก้แล้ว (P5 จับ):** `/api/travel-time` เป็น route ฝั่งเซิร์ฟเวอร์ **แต่ยิง Supabase ด้วยสิทธิ์ `anon`**
→ `revoke … from anon` **ฆ่ามันทั้งขาอ่านและขาเขียน** · ผู้ใช้จะเห็น **เวลาเดินทางจริงหายทั้งเว็บ ตกเป็นค่าประมาณเงียบ ๆ ไม่มี error**
**revoke ต้องลงพร้อม definer ใน PR เดียวกัน ไม่ใช่งานตามทีหลัง**

**`D51`** — `leg` ยังเป็นค่าคำนวณจาก `trip_days` เหมือนเดิม · **`trip_hotels` เก็บ `check_in`/`check_out` ของตัวเอง**
เพราะวันนี้ `leg_id` เป็น **primary key ที่เป็นค่าคำนวณ** และตารางไม่มีวันที่เลย → **แก้แผนแล้วใบจองที่จ่ายเงินไปแล้วขยับตามอย่างเงียบ ๆ**
**`D52`** — ไม่มีคอลัมน์ `active_plan_id` · ใช้ `trip_plans.is_active` + partial unique index → ไม่มี FK วน ไม่ต้องใช้ `deferrable`

**กระจายงานคืนนี้ (ทุกข้อไม่ต้องรอ token):**

| เซสชัน | ได้อะไรไป |
|---|---|
| **P5-AI/Agent** | คลังต้องตอบ**คำถามอะไร**ของ copilot · ต้องมี `pgvector` ตั้งแต่ `E2` ไหม (เป็นการตัดสินระดับสคีมา) |
| **P3-FE/Perf** | วัดว่า `data/places.ts` 71 KB อยู่ในบันเดิลจริงไหม · รูปแบบแคช/ISR · **คลังลงฐานแล้วตอนเน็ตหลุดจะเหลืออะไร** |
| **P7-Mobile** | กฎร่วม 3 ข้อ (`updated_at` trigger · `deleted_at` · rank key) คือพื้นของ conflict model · **`updated_at` จะเป็นเวลา sync ไม่ใช่เวลาแก้** |
| **P2-UI/UX** | สถานะหน้าจอใหม่ที่ `D51` สร้างขึ้น — **"จองไว้แต่ไม่ตรงกับแผนแล้ว"** ห้ามซ่อน ห้ามลบอัตโนมัติ |
| **P4-QA/Sec** | `P-29`/`P-30`/`P-31`/`P-32` ตัวเต็ม · เคส 2 ทิศของ RPC · `E2-AC11` ตัวหารเมทริกซ์ |
| **P6-DevOps** | ด่าน CI · `check-env-keys.py` (ปิดช่องคนรัน `vitest` บนเครื่อง) |
| **P8-PM/BA** | กล่อง "5 ที่ที่ยังถือค่าเก่า" · กำลังเดินเรื่อง token กับผู้ใช้ |

---

### 📋 ของที่ทำเสร็จคืนนี้แต่ **ยังไม่เคยรันกับฐานใด ๆ เลยสักตัว**

> 🔴 **ทั้งหมดนี้ผ่านแค่ `tsc`/`eslint`/`guards.sh` ซึ่งพิสูจน์ได้แค่ว่ามันคอมไพล์ได้**
> **หลักฐานแดง-แล้ว-เขียวที่โปรเจกต์นี้เรียกร้อง ยังไม่มีสำหรับของพวกนี้สักชิ้น**

| ไฟล์ | คืออะไร | รอ |
|---|---|---|
| `…220618_project_identity_guard.sql` | marker `app.project_identity` (`D48`·`P-31`) | token |
| `…221550_create_trip_rpc.sql` | `create_trip()` — ทางแก้ `P-26` ตัวจริง (`D49`) + แก้ `P-32` | token |
| `…222206_service_role_test_cleanup_grant.sql` | grant ให้เทสต์เก็บกวาดได้ (`P-28`) | token **+ `diag2.mjs` ตอบเรื่อง `sb_secret_` ก่อน** |
| `supabase-platform/db-push.sh` | ด่านปลายทางที่ตัวยิงคำสั่ง (`D50`) | self-test 5/5 ผ่านแล้ว · ยังไม่เคยยิงจริง |
| `pending-review/…144235_fix_trip_visible_on_create.sql` | 🛑 **จอด ไม่ผ่าน** — `P-27`+`P-29` | ไม่กลับมา เว้นมีเหตุผลใหม่ |
| `lib/__tests__/rlsMatrix.test.ts` `afterAll` (P4) | ลบทริปด้วย `created_by` | token |

---

**ระยะสร้าง — บน branch `platform` ที่ `/Users/park/plan-korea-platform`**
- [x] **`E0` ตั้งราง** — CI 3 job · `.github/guards.sh` + self-test · `supabase-platform/` workdir · `migration-template.sql`
- [x] Supabase CLI 2.114.0 ลงเครื่องแล้ว · `supabase init` แล้ว
- [x] **`E1` migration แปลงเป็นของที่รันได้แล้ว** — `supabase-platform/supabase/migrations/20260824043822_identity.sql`
      · เจอและแก้ `P-24` (`create schema app` ต้องมาก่อน ADP ไม่งั้นตายที่คำสั่งที่ 2)
      · self-check แยกไฟล์ + เพิ่มหัวข้อ 9 สำหรับ `E1-AC7`
- [ ] 🔴 **ค้างที่ผู้ใช้ 4 ข้อ** — ยืนยัน Vercel · `supabase link` · GitHub secret 4 ตัว · เปิด auth provider 2 ทาง + บอกอีเมลของ A/B
- [x] **`D48`+`D50` ด่านกันรันผิดโปรเจกต์ — 3 ชั้น และไม่มีชั้นไหนพอเดี่ยวๆ**
      ① `npm run db:push` (`supabase-platform/db-push.sh`) เทียบ ref ที่ link กับ `.github/allowed-project-ref` **ล้มถ้าไม่ตรง** · self-test 5 เคสผ่าน
      ② ด่านใน `0001` — **`public` ต้องยังไม่มีตารางเลย** = ต้องเป็นโปรเจกต์ใหม่เอี่ยม · **`a-gleam` ตายตรงนี้**
      ③ allowlist ใน migration ตัวที่ 3+ — เช็ค `name`+`ref`+`environment` ครบ (`P-31`)
      · 🔴 **P4 ตีกลับฉบับแรกด้วย `P-30`** — allowlist เดี่ยวๆ กัน `a-gleam` **ไม่ได้เลย** เพราะ `0001` วิ่งก่อนแล้วสร้างเงื่อนไขที่ด่านตรวจให้เอง
      · ⚠️ **ยังไม่ได้รัน** (token) · guards.sh 9 ด่านเขียว · 247 เทสต์ผ่าน · lint สะอาด
- [ ] `E1` รันจริงบน `engine-dev` แล้วผ่าน AC1–AC7 (P4 วัด)
- [ ] ⚠️ **ยังไม่เคยรัน migration กับ DB ใดๆ** — เครื่องนี้ไม่มี psql ไม่มี docker · `db push` ครั้งแรกคือการรันครั้งแรก

---

## 5. กติกาการทำงานในโปรเจกต์นี้

- **คุยเป็นภาษาไทย** — โค้ด/ชื่อตัวแปร/ศัพท์เทคนิคเป็นอังกฤษได้ตามปกติ
- **Next.js เวอร์ชันนี้ต่างจากที่โมเดลเคยรู้จัก** — อ่าน `node_modules/next/dist/docs/` ก่อนใช้ API ที่ไม่แน่ใจ
- **ห้ามเรียก Google Maps API ตระกูล legacy** (`maps.googleapis.com/maps/api/*`) คีย์โปรเจกต์นี้ไม่ได้เปิดไว้ ใช้ตระกูลใหม่เท่านั้น (`places.googleapis.com`, `routes.googleapis.com`) และควรผ่าน helper ใน `lib/googlePlaces.ts`
- **migration ต้องให้ผู้ใช้ copy-paste รันเอง** แล้วยืนยันผลด้วย curl ก่อนเชื่อว่าฟีเจอร์ทำงาน อย่าเดาว่าเป็นบั๊กโค้ดก่อนเช็ก migration
- **โปรเจกต์นี้ (plan-korea) ใช้ Supabase project ref `ejzibhgqhxdzkovsnpds`** (`https://ejzibhgqhxdzkovsnpds.supabase.co`)
  — ลิงก์ตรงไป SQL Editor: `https://supabase.com/dashboard/project/ejzibhgqhxdzkovsnpds/sql/new`
  ⚠️ **ผู้ใช้มีอีกโปรเจกต์ (MU Phone ที่ `~/mu-phone`) ซึ่งใช้ Supabase คนละโปรเจกต์กัน** (ref `smqpqqwnpssiqewndvml`)
  ทำงาน 2 โปรเจกต์นี้สลับกันในเซสชันเดียวได้ (มี `.claude/launch.json` config `mu-phone-dev` ไว้เปิด preview ข้ามโฟลเดอร์)
  — **ทุกครั้งที่ขอให้รัน migration ให้ระบุ ref/ลิงก์ตรงนี้ชัดๆ ในข้อความเสมอ** กันสลับโปรเจกต์ผิด
  (เคยพลาดมาแล้ว 12 ส.ค. 2026 — รัน `0029` ไปลง mu-phone แทน เจอ `relation "public.custom_places" does not exist`
  เพราะ mu-phone ไม่มีตารางนี้ — ไม่มีผลเสียหายเพราะ statement fail ทั้งก้อน แต่เป็นสัญญาณเตือนว่าต้องกันไว้ก่อน)
- **ฐานข้อมูลนี้เป็นข้อมูลทริปจริงที่ใช้อยู่จริง 2 คน** — ตอนทดสอบใน browser ให้เลี่ยงการกดปุ่มที่แก้ข้อมูล ถ้าจำเป็นต้องแก้ ให้แก้กลับเป็นค่าเดิมเสมอ
- **ก่อนบอกว่าเสร็จ ต้อง `npm run lint` ผ่านเสมอ**
- **ปิดเฟสแล้วอัปเดตไฟล์นี้** ทุกครั้ง
