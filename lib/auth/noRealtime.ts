/**
 * transport ปลอมสำหรับ Realtime — เจ้าของ: P1-Lead (E1 · S1 ที่ P4 พบ 24 ส.ค. 2026)
 *
 * 🔴 **ปัญหาที่มันแก้ และมันเกือบทำให้เว็บ 500 ทั้งเว็บ:**
 * `createClient`/`createServerClient` ของ Supabase **สร้าง `RealtimeClient` ใน constructor เสมอ**
 * แม้เราไม่ได้ใช้ Realtime เลยสักบรรทัด · `RealtimeClient` ต้องการ `globalThis.WebSocket`
 * ซึ่ง **Node 20 ไม่มี** (เพิ่งมีตั้งแต่ Node 22) → `createServerClient` **โยนทันทีที่ถูกเรียก**
 *
 * ⚠️ **ไม่มีอะไรเตือนเลยสักชั้น:** `engines` ของ Next คือ `>=20.9.0` → Node 20 ถูกต้องตามสเปก ·
 * Next 16 ไม่ได้ polyfill WebSocket ให้ node runtime · `@supabase/ssr` ไม่ได้ส่ง transport เอง
 *
 * 🎯 **และเหตุผลที่มันร้ายเป็นพิเศษ:** `refreshSession()` อยู่บรรทัดแรกของ `proxy()` **ก่อน `PUBLIC_PATHS`**
 * → ถ้ามันโยน **ทุกเส้นทาง 500 รวม `/login` และ `/unlock`** = ไม่มีทางเข้าไปแก้ผ่านหน้าเว็บเลย
 * · สิ่งเดียวที่กันไว้ก่อนหน้านี้คือเงื่อนไข "ยังไม่ได้ตั้ง env" **ซึ่งกำลังจะเป็นเท็จ**
 *   วินาทีที่ผู้ใช้วางค่า `NEXT_PUBLIC_SUPABASE_URL` + `ANON_KEY` ลงไป
 *
 * ---
 *
 * `RealtimeClient` อ่าน `options?.transport` **ก่อน** เรียกตัวที่โยน → ส่งอะไรไปก็พอ
 *
 * 🔴 **และเราส่งตัวที่โยนถ้าถูกเรียก ไม่ใช่ WebSocket จริง โดยตั้งใจ:**
 * ฝั่งเซิร์ฟเวอร์ของแพลตฟอร์มนี้ **ไม่มีเหตุผลใดที่จะเปิด socket** — Realtime เป็นเรื่องของเบราว์เซอร์
 * ถ้าวันหนึ่งมีใครเผลอเรียก **มันจะแดงพร้อมบอกเหตุผล ไม่ใช่เงียบแล้วเปิด socket ค้างไว้ทุก request**
 * → **เป็นด่าน ไม่ใช่แค่ทางเลี่ยง** · ตรงกับหลักของโปรเจกต์นี้: ถ้าความล้มเหลวไม่ส่งเสียง ต้องทำให้มันส่งเสียง
 *
 * 📌 **หนี้ที่ยังไม่จ่าย และไฟล์นี้ไม่ได้จ่ายให้:** ที่ถูกจริงคือขยับไป **Node 22 + `engines` + `.nvmrc`**
 * ให้ local · CI · Vercel ตรงกัน · วันนี้ **ไม่มีอะไรในรีโปกำหนด Node version ของ Vercel เลย**
 * (ไม่มี `engines` ไม่มีใน `vercel.json` — P4 ตรวจแล้วทั้ง 2 ทรี)
 * → **dev กับ prod ทำงานไม่เหมือนกันได้โดยไม่มีสัญญาณ** ซึ่งแย่ในแบบของมันเอง · เป็นงานของ P6
 */
export const NO_REALTIME_TRANSPORT = function NoRealtimeTransport(): never {
  throw new Error(
    "ฝั่งเซิร์ฟเวอร์ของแพลตฟอร์มนี้ต้องไม่เปิด WebSocket — มีคนเรียกใช้ Realtime เข้าให้แล้ว\n" +
      "  ทางแก้ที่ถูกคือถามว่าโค้ดตรงนั้นควรใช้ Realtime จริงไหม\n" +
      "  **ไม่ใช่เปลี่ยนตัวนี้ให้เป็น WebSocket จริง** — Realtime เป็นเรื่องของเบราว์เซอร์ (lib/auth/browser.ts)",
  );
} as unknown as never;
