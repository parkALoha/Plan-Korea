// 🔴 **ด่านโครงสร้าง ไม่ใช่คำเตือน** — `build` ล้มทันทีถ้า client component ลากไฟล์นี้เข้าไป
//    P4 ไล่กราฟ import จาก 78 ไฟล์ที่มี `"use client"` เมื่อ 26 ส.ค. 2026 แล้วพบเส้นทางจริง:
//      `hooks/useStops.ts` → `lib/engine/trip.ts` → **ไฟล์นี้**
//    (`trip.ts` import `tripsVisibleToMe` ที่หัวไฟล์ · hook import แค่ฟังก์ชันบริสุทธิ์ข้าง ๆ)
//    ⚠️ คีย์ไม่ได้รั่ว — ไฟล์นี้รับ `Db` เข้ามา ไม่ import client ตัวไหนเอง
//       **สิ่งที่จะรั่วคือตรรกะกับชื่อตารางลงบันเดิลเบราว์เซอร์ ถ้า tree-shaking ไม่ตัด**
//    🎯 และประเด็นจริงคือ **เส้นแบ่ง server/client ถูกค้ำด้วย tree-shaking ซึ่งไม่มีใครตรวจ**
//       — *"มองไม่เห็น = ปลอดภัย"* อีกรูปหนึ่ง (คำของ P4 · รับเต็ม ๆ)
import "server-only";

/**
 * ชั้นเดียวที่ได้รับอนุญาตให้เรียกชื่อตารางของแพลตฟอร์ม — `D81` ⑦.๕
 * เจ้าของ: P1-Lead · 26 ส.ค. 2026
 *
 * ## ทำไมไฟล์นี้ถึงมีอยู่ (P6 เสนอรูป · P4 แก้รูปให้แคบไม่ได้)
 *
 * `D81` ③ · ③.๕ · ③.๖ ผูกกติกาไว้กับ **คิวรี** ไม่ใช่กับ **สคีมา** — และ DDL บังคับมันไม่ได้เลย:
 * ```
 * ถามว่า "แถวไหนกำหนดขอบของวัน"  →  ต้องมี  deleted_at is null  และ  order by rank, id
 * browse คลังสถานที่               →  ต้องมี  not picker_hidden
 * ```
 * 🔴 **ทั้งสามข้อ ลืมแล้วไม่แดง** — ผลลัพธ์หน้าตาถูกต้องทุกประการ:
 * · ลืม `order by`      → 2 เครื่องเลือกคนละแถว → timeline ตัดคนละที่
 * · ลืม `deleted_at`    → ทั้ง 2 เครื่องเลือก **แถวที่ผู้ใช้ลบไปแล้วและ UI ไม่แสดง** เหมือนกัน
 * · ลืม `picker_hidden` → Copilot ตอบ *"ปูซานมีที่ไหนน่าไปอีก"* ด้วยลิสต์ที่มี **สถานีบัส** ปนอยู่
 *
 * 🎯 **P6 ปฏิเสธด่านที่ถามว่า *"ทุกคิวรีจำ predicate ได้ไหม"*** — มันต้องเดาว่าคิวรีไหนเป็น
 * browse query ซึ่งเป็นหมวดหมู่**เชิงความหมาย** · ด่านที่ต้องเดา คือด่านที่จะเดาผิดสักวัน
 * > **ย้ายการบังคับจาก *"จำได้ไหม"* → *"เลี่ยงไม่ได้"***
 *
 * ## 🔴 สถานะวันนี้: 2 ใน 3 ขา — และผมเขียนไว้ตรงนี้แทนที่จะปล่อยให้เข้าใจว่าครบ
 * ```
 * ① DDL       ✅ 20260826010130_e2_d81_trip_stop_events.sql
 * ② helper    ✅ ไฟล์นี้
 * ③ ด่าน CI   ⏳ โซนของ P6 — ห้าม `.from(` นอกไฟล์นี้ (ยกเว้น Array.from / .storage.from)
 * ```
 * `D81` ⑦.๕ เขียนว่าทั้งสามต้องมาพร้อมกัน · **ขาที่ ③ อยู่คนละโซน ผมเขียนแทนไม่ได้**
 * · เหตุผลที่ยังลง ② ก่อน: **`catalog_places` มี `.from(` ในโค้ดที่เสิร์ฟผู้ใช้ = 0 จุด** (`P-61`)
 *   ช่องว่างระหว่าง ② กับ ③ จึงกว้างเท่ากับ *จำนวนจุดเรียกที่ถูกเขียนในระหว่างนั้น* — ซึ่งวันนี้คือศูนย์
 * · 🔴 **และผมจะไม่เขียนจุดเรียกใหม่จนกว่า ③ จะลง** — ไม่งั้นเหตุผลข้างบนหมดอายุด้วยมือผมเอง
 */

import type { SupabaseClient } from "@supabase/supabase-js";
import type { Database } from "./database.types";
import { PLACE_ID_PREFIX } from "@/lib/placeQuery";

/** ตารางของแพลตฟอร์มที่ชั้นนี้ดูแล — เพิ่มตารางใหม่ = เพิ่มฟังก์ชันในไฟล์นี้ ไม่ใช่เรียก `.from` ที่อื่น */
export type EngineTable =
  | "trips"
  | "trip_members"
  | "catalog_countries"
  | "catalog_cities"
  | "catalog_places"
  | "catalog_place_names"
  | "catalog_country_contacts"
  | "catalog_place_access"
  | "trip_days"
  | "trip_destinations"
  | "trip_stops"
  | "custom_places"
  | "hidden_places"
  | "place_notes"
  | "trip_hotels"
  | "checklist_items"
  | "trip_day_plan_settings"
  | "bookings"
  | "trip_plans"
  | "profiles";

/**
 * 🔴 **ไคลเอนต์ถูก *ส่งเข้ามา* ไม่ใช่ import — และนี่คือทั้งหมดของ `E3`**
 *
 * ฉบับแรกผม `import { supabase }` จาก `lib/supabase` ตรง ๆ ซึ่งเป็น**ไคลเอนต์ฝั่งเบราว์เซอร์**
 * → ผูกชั้นข้อมูลไว้กับเบราว์เซอร์ถาวร **ทั้งที่ `E3-AC1` ต้องการให้คิวรีย้ายไปฝั่งเซิร์ฟเวอร์**
 *
 * 🎯 **รับเข้ามาเป็นพารามิเตอร์ = ฟังก์ชันเดียวใช้ได้ทั้งสองฝั่ง และ *ผู้เรียกเป็นคนตัดสินว่าเป็นใคร***
 * · ฝั่งเซิร์ฟเวอร์ส่ง `createServerSupabase()` ซึ่งผูก session ผู้ใช้จริง → **RLS ทำงานเหมือนเดิมทุกประการ**
 * · 🔴 **นี่คือ `D38` ในรูปโครงสร้าง ไม่ใช่ในรูปคำเตือน** — ย้ายไปเซิร์ฟเวอร์**ไม่ได้**แปลว่าได้สิทธิ์เพิ่ม
 *   เพราะไม่มีที่ไหนในไฟล์นี้ที่ *เลือก* ตัวตนได้เลย · ใครส่ง client อะไรมา ก็ได้สิทธิ์เท่านั้น
 * · ⚠️ **ห้ามใส่ค่าเริ่มต้นให้ `db`** — ค่าเริ่มต้นคือการเลือกตัวตนแทนผู้เรียก ซึ่งคือสิ่งที่ย่อหน้านี้ห้าม
 */
export type Db = SupabaseClient<Database>;

/**
 * ✅ **ช่องนี้ปิดแล้ว 28 ส.ค. 2026 — ผู้ใช้อนุมัติ**
 *
 * เดิมฟังก์ชันเขียนรับ `Record<string, unknown>` ที่ route ปั้นจาก `await req.json()`
 * → **พิมพ์ชื่อคอลัมน์ผิดกี่ตัวก็ผ่านคอมไพล์** แล้วไปตายที่ฐานเป็น `400`/`PGRST204`
 * · ตระกูลเดียวกับที่ทำเว็บ 502 เมื่อ 27 ส.ค. — **ฝั่ง `select` ปิดไปก่อนแล้ว ฝั่ง `insert` ปิดรอบนี้**
 *
 * ## ยืนยันว่าด่านยิงจริง ไม่ใช่แค่คอมไพล์ผ่าน
 * ฉีดชื่อคอลัมน์ที่ไม่มีจริง 3 ที่ (`hotel_name_typo` · `"dwell_minutes_typo"` · `"title_typo"`)
 * → **แดงทั้ง 3 ไฟล์** · ถอนออก → `0 error` · **เคสควบคุมฝั่งบวก ไม่ใช่การเชื่อความเงียบ**
 *
 * ## ราคาที่จ่าย และทำไมถึงจ่ายได้
 * ปิดช่องนี้ = route ต้องตรวจชนิด body ให้ครบ ซึ่ง **เปลี่ยนสิ่งที่ API ยอมรับ**
 * · เดิมตรวจ *ครึ่ง ๆ*: `typeof b.lat !== "number"` ตรวจจริง แต่ `!b.hotelName` แค่เช็คว่าไม่ว่าง
 *   → `hotelName: { a: 1 }` **ลงฐานได้** · 🔴 **ครึ่ง ๆ แย่กว่าไม่ตรวจ เพราะมันดูเหมือนตรวจแล้ว**
 * · ที่จ่ายได้เพราะ **ไม่มีไคลเอนต์ภายนอก** — ไม่มี CORS · ผู้เรียกทั้งหมดคือ 29 ไฟล์ในแอปนี้เอง
 *   (ข้อนี้เคยถูกยกเป็นเหตุผล *ไม่* ปิด — **แล้วไม่มีใครตรวจว่าไคลเอนต์นั้นมีอยู่จริงไหม**)
 *
 * ⚠️ **สิ่งที่ยัง *ไม่* ถูกตรวจ:** ค่าที่คัดลอกแบบไดนามิกผ่าน `WRITABLE` ถูกตรวจว่าเป็น
 *    `string | number | boolean | null` เท่านั้น — **ไม่ได้ตรวจว่าตรงชนิดของคอลัมน์นั้น**
 *    (ส่ง `dwellMinutes: "abc"` ยังไปตายที่ฐาน) · **ชื่อคอลัมน์ปิดแล้ว ชนิดของค่ายังเปิดครึ่งหนึ่ง**
 */
/** แถวสำหรับ `insert` ของตารางหนึ่ง — มาจากสคีมาจริง ไม่ใช่ `Record<string, unknown>` */
export type InsertRow<T extends EngineTable> = Database["public"]["Tables"][T]["Insert"];
/** แพตช์สำหรับ `update` ของตารางหนึ่ง — ทุกฟิลด์ optional ตามสคีมา */
export type UpdateRow<T extends EngineTable> = Database["public"]["Tables"][T]["Update"];


/**
 * 🔴 **จุดเดียวในแอปที่พิมพ์ชื่อตารางของแพลตฟอร์มได้**
 *
 * ตั้งใจไม่ `export` — ถ้ามันออกไปข้างนอกได้ ด่านของ P6 จะเห็นแค่ `engineTable(db, "x")`
 * ซึ่งเป็นสตริงเหมือนเดิม **แต่ predicate ไม่ถูกใส่ให้** = ได้ท่ากลับมาโดยไม่ได้อะไรเลย
 */
function engineTable<T extends EngineTable>(db: Db, name: T) {
  return db.from(name);
}

// ───────────────────────────────────────────────────────────────────────────
// คลังสถานที่ — `D81` ③.๖
// ───────────────────────────────────────────────────────────────────────────

/**
 * **browse** คลัง = "มีที่ไหนน่าไปอีก" → ต้องไม่มีสนามบิน/สถานีปนมา
 *
 * `transferPoints.ts:28` เขียนเหตุผลไว้เองว่า *"สนามบิน/สถานี ไม่ใช่ที่เที่ยว
 * ไม่ควรโผล่ในคลังสถานที่ให้เลือกเพิ่มลงวัน"* — วันนี้กฎนั้นบังคับด้วย**การอยู่คนละไฟล์**
 * พอทั้งคู่เป็นแถวใน `catalog_places` ตารางเดียวกัน เหลือแค่คอลัมน์ธง
 */
export function browseCatalogPlaces(db: Db, opts: { cityId?: string; countryId?: string; limit?: number }) {
  // 🔴 **สองเงื่อนไข ไม่ใช่หนึ่ง — และเหตุผลคือสองคอลัมน์นี้ตอบคนละคำถาม** (P4 ชี้ · P1 แก้ · 27 ส.ค. 2026)
  //
  //   `source = 'transfer'`  → *"แถวนี้เป็นสนามบิน/สถานีใช่ไหม"*  → **ไม่ใช่ที่เที่ยว ไม่ควรอยู่ในคลัง**
  //   `picker_hidden`        → *"ซ่อนจากลิสต์ 'ไปสนามบิน/สถานี' ไหม"* → ตอบเฉพาะ modal นั้น
  //
  // 🎯 **ก่อนหน้านี้ที่นี่กรองแค่ `picker_hidden` และมันตอบผิดคำถาม** — วัดจริงแล้ว
  //    25 จาก 28 จุดเปลี่ยนเส้นทางมี `picker_hidden = false` **ซึ่งถูกต้องตามความหมายของมัน**:
  //    คุณ *ต้องการ* เลือก "ไปสนามบินกิมแฮ" ในโมดัลนั้น · ที่ซ่อนมีแค่ 3 คือสนามบินต้นทาง/ต่อเครื่อง
  //    (`airport-bkk` · `airport-dmk` · `airport-sgn`) ที่ไม่มีวันไหนต้องแทรกแถว "ไปสนามบิน" ไปหา
  //
  // 🔴 **ทางที่ปฏิเสธ: กลับ `picker_hidden` เป็น `true` ให้ทุกแถว transfer**
  //    มันจะทำให้ browse ถูก **โดยแลกกับการทำให้โมดัล "✈️ ไปสนามบิน/สถานี" ว่างเปล่า**
  //    = ยัดความหมายที่สองลงคอลัมน์เดิม แล้วความหมายแรกตายเงียบ ๆ
  //    · ตระกูลเดียวกับที่ปฏิเสธไปแล้ววันนี้ตอน `read_only_selftest()` (คอลัมน์ `blocked`
  //      ที่จะแปลว่าคนละอย่างตามแถว) · **คอลัมน์หนึ่งตอบคำถามเดียว**
  //
  // ⚠️ วันนี้ยังไม่มีใครเรียกฟังก์ชันนี้จากโค้ดแอป (ไซด์บาร์ยังอ่าน `data/places.ts` สถิตย์ — งาน `B6`)
  //    → **ผู้ใช้ยังไม่เห็นบั๊กนี้** · แก้ตอนนี้เพราะ `B6` จะเปิดมันทันทีที่ย้าย ไม่ใช่เพราะกำลังไหม้
  //
  // 🔴 **ฝังชื่อ+คำบรรยายมาด้วย ไม่ใช่ `select("*")`** (28 ส.ค. 2026 · `B6`)
  //    `catalog_places` **ไม่มีคอลัมน์ชื่อเลยสักคอลัมน์** — ชื่ออยู่ใน `catalog_place_names`
  //    (แยกตาม locale · `B6`) และคำบรรยายอยู่ใน `catalog_place_descriptions` (แยกตาม locale · `Q6`)
  //    → `select("*")` คืนแถวที่ **แสดงบนการ์ดไม่ได้เลย** เพราะไม่มีชื่อ
  //    ⚠️ ผู้เรียกจะต้องยิงเพิ่มอีก 2 คำขอต่อสถานที่ **หนึ่งใบ** ถ้าไม่ฝังมาตรงนี้
  //
  // 🔴 **`catalog_cities!inner` ไม่ใช่แค่เอา slug เมือง** — `catalog_places` ไม่มี `country_id`
  //    (ประเทศผูกกับ *เมือง* ไม่ใช่กับ *สถานที่*) · ฉบับก่อนหน้าเขียน `.eq("country_id", …)`
  //    ตรงกับตารางนี้ ซึ่ง **เป็นคอลัมน์ที่ไม่มีอยู่จริง** → ตัวกรองนั้นไม่เคยทำงาน
  //    · ไม่มีใครเห็นเพราะยังไม่มีโค้ดไหนเรียกฟังก์ชันนี้เลย
  let q = engineTable(db, "catalog_places")
    .select(
      // 🔴 **`source` ต้องอยู่ใน select เสมอ ถึงแม้ UI ไม่ได้ใช้** — `catalogCoverage.test.ts:190`
      //    assert ว่าไม่มีแถวไหน `source === 'transfer'` · ถอดออกเมื่อไหร่ ทุกแถวจะเป็น `undefined`
      //    แล้ว **เคสนั้นผ่านฟรีตลอดกาลโดยไม่ตรวจอะไรเลย** — ปลดด่านโดยไม่มีอะไรฟ้อง
      //    (ผมถอดออกจริงตอนเขียนรอบแรก · `tsc` จับได้เพราะเทสต์ cast ชนิดไว้ ไม่ใช่เพราะมีเคสจับ)
      "id, legacy_slug, category, source, lat, lng, address_local, maps_query, google_place_id, youtube_query, weather_sensitivity, catalog_cities!inner(legacy_slug, country_id), catalog_place_names(locale, name, priority), catalog_place_descriptions(locale, description)",
    )
    .eq("picker_hidden", false)
    .neq("source", "transfer");
  if (opts.cityId) q = q.eq("city_id", opts.cityId);
  if (opts.countryId) q = q.eq("catalog_cities.country_id", opts.countryId);
  return q.order("legacy_slug").limit(opts.limit ?? 50);
}

/**
 * **resolve** คลัง = "แถวนี้คือที่ไหน" → **ต้องไม่กรอง `picker_hidden`**
 *
 * 🔴 **ความต่างนี้คือทั้งหมดของ `picker_hidden` และมันกลับด้านกับ browse:**
 * จุดแวะที่ `kind='transfer'` ชี้ไปที่สนามบินจริง ๆ · ถ้า resolve กรองมันออกด้วย
 * **แถวสนามบินในแผนของผู้ใช้จะกลายเป็นแถวที่ "ไม่รู้จักสถานที่" ทั้งที่ข้อมูลอยู่ครบ**
 * · `picker_hidden` แปลว่า *"ไม่โผล่ในลิสต์ให้เลือก"* **ไม่ได้แปลว่า "ไม่มีอยู่"**
 */
/**
 * ตัวไหนใน `slugs` เป็นสถานที่ของ **คลังสาธารณะ** จริง — คืนเฉพาะตัวที่พิสูจน์ได้
 *
 * 🔴 **มีไว้ตอบคำถามเดียว: "id นี้แคชลงตารางกลางได้ไหม"** (`E3-AC6` · P1 · 2 ก.ย. 2026)
 * `travel_time_cache` ใช้ `from_place_id` เป็น **คีย์** และตารางนั้นใช้ร่วมกันทั้งระบบ
 * → ค่าที่ไม่ใช่ของสาธารณะจะกลายเป็นข้อมูลของทริปหนึ่งที่ทุกคนอ่านได้ทันทีที่เปิดสิทธิ์ (`D87`)
 * · ของจริงที่เคยไหลเข้าไป: `hotel@<lat>,<lng>` (พิกัดที่พัก) และ `custom_places.id`
 *
 * 🎯 **บัญชี*ขาว* ไม่ใช่บัญชี*ดำ* — และความต่างนี้คือทั้งหมดของฟังก์ชันนี้**
 * ถ้าถามว่า *"หน้าตาเป็นของส่วนตัวไหม"* เราต้องมีรายการรูปแบบที่ส่วนตัว **ซึ่งจะผิดเงียบ
 * วันที่มีรูปแบบ id ใหม่โผล่มา** · ถามว่า *"พิสูจน์ได้ไหมว่าสาธารณะ"* แล้วรูปใหม่ตกไปเอง
 * · ⚠️ **ตรวจด้วยรูปร่างไม่ได้เลย** — `custom_places.id` เป็น UUID ซึ่งเข้าเงื่อนไข
 *   `^[a-z0-9-]+$` เหมือน slug ทุกประการ (P4 ชี้ว่าด่านที่ตรวจ *รูปร่าง* พลาดเสมอ)
 *
 * ⚠️ **ล้มแล้วต้องได้เซตว่าง ไม่ใช่โยน** — ผู้เรียกอ่านว่า "แคชไม่ได้" แล้วยิงต้นทางตรง
 *   คือ fail-closed: แพงขึ้น แต่ไม่มีทางรั่ว · ตรงข้ามกับการปล่อยผ่านเวลาตรวจไม่ได้
 */
export async function catalogPublicSlugs(db: Db, slugs: string[]): Promise<Set<string>> {
  return catalogPublicValues(db, "legacy_slug", slugs);
}

/**
 * คู่ของ `catalogPublicSlugs` สำหรับ `place_details_cache` ซึ่งคีย์ด้วย **ข้อความค้นหา** ไม่ใช่ id
 *
 * 🔴 **ทำไมต้องมีตัวที่สอง — `place_details_cache` รั่วคนละทางกับ `travel_time_cache`**
 * `placeQueryKey()` (`lib/placeQuery.ts:19`) คืน `place.mapsQuery` ตรง ๆ เมื่อสถานที่ไม่มี
 * `googlePlaceId` → **สำหรับสถานที่ที่ผู้ใช้เพิ่มเอง นั่นคือข้อความที่เขาพิมพ์** (ชื่อ/ที่อยู่)
 * · และ **ตัวแถวยังถือ `name_local`/`address_local` ที่ Google ตอบกลับ** ซึ่งบอกได้เองว่าเป็นที่ไหน
 *   → ต่างจาก `travel_time_cache` ที่แถวมีแค่ โหมด/นาที/เมตร
 *
 * 🎯 **จึงปิดด้วยการ *ไม่ให้เข้าตาราง* เท่านั้น — ไม่มีวิธีปิดที่ตัวแถว** · หลักเดียวกับที่ผู้ใช้เลือกไว้
 *   สำหรับ `E3-AC6`: **แคชเฉพาะสิ่งที่พิสูจน์ได้ว่าเป็นของคลังสาธารณะ**
 */
export async function catalogPublicMapsQueries(db: Db, queries: string[]): Promise<Set<string>> {
  /**
   * 🔴 **แก้ 3 ก.ย. 2026 — ฉบับแรกเทียบคอลัมน์เดียว แล้วคีย์ครึ่งหนึ่งไม่มีทางผ่าน (P1 เจอเอง)**
   * `placeQueryKey()` คืน **สองรูป** · ฉบับแรกของประตูนี้รู้จักรูปเดียว
   * ```
   * "<mapsQuery>"      → เทียบ catalog_places.maps_query        ← ฉบับแรกทำเฉพาะอันนี้
   * "place_id:<gid>"   → ต้องเทียบ catalog_places.google_place_id  ← ไม่เคยถูกเทียบเลย
   * ```
   * วัดกับข้อมูลจริง (สำเนาแช่แข็ง · สนามซ้อมในเครื่อง):
   * ```
   * place_details_cache  คีย์รูป place_id:  22 / 140   ผ่านประตูฉบับแรก **0 แถว**
   * ในนั้นเป็นของคลังจริง (มี google_place_id ตรงกัน)   3 แถว   ← ถูกกันทิ้งทั้งที่พิสูจน์ได้ว่าสาธารณะ
   * ```
   * 🔴 **ทิศของความเสียหายคือ fail-closed — ไม่ใช่ช่องรั่ว** แต่แคชตายถาวรสำหรับ *ของที่เราคัดเอง*
   *    ซึ่งเป็นกลุ่มที่เปิดบ่อยที่สุด **และเป็นกลุ่มที่ปลอดภัยที่สุดที่จะแคช**
   * ⚠️ **และมันเงียบ** — ไม่มี error · ผู้ใช้ยังได้คำตอบ (ยิง Google ใหม่ทุกครั้ง) · เห็นที่บิลค่า API เท่านั้น
   * 📌 วันนี้คลังมี `google_place_id` แค่ 3/203 แถว — **นั่นคือช่องว่างของ *ข้อมูล* คนละเรื่องกับช่องว่างของ *ประตู* นี้**
   */
  const wanted = queries.filter((q) => q.length > 0);
  if (wanted.length === 0) return new Set();

  const plain = wanted.filter((q) => !q.startsWith(PLACE_ID_PREFIX));
  const byPlaceId = new Map<string, string>();          // google place id → คีย์รูปเดิม
  for (const q of wanted) {
    if (q.startsWith(PLACE_ID_PREFIX)) byPlaceId.set(q.slice(PLACE_ID_PREFIX.length), q);
  }

  const [okPlain, okIds] = await Promise.all([
    catalogPublicValues(db, "maps_query", plain),
    catalogPublicValues(db, "google_place_id", [...byPlaceId.keys()]),
  ]);

  // 🔴 คืน **คีย์ในรูปที่ผู้เรียกส่งมา** ไม่ใช่ค่าที่คลังเก็บ — ผู้เรียกเอาไปทำ `.in("maps_query", …)` ต่อ
  const out = new Set(okPlain);
  for (const gid of okIds) {
    const key = byPlaceId.get(gid);
    if (key) out.add(key);
  }
  return out;
}

/**
 * **`Q3` ก้าวที่ 2 — ฝั่งดึงข้อมูลให้ตัวอุ่นแคช** · P1 · 3 ก.ย. 2026
 *
 * 🔴 **PostgREST ตัดผลลัพธ์ที่ `db-max-rows` (ค่าเริ่มต้น 1000) *เงียบ ๆ*** — ไม่มี error ไม่มีสัญญาณ
 * · วัดแล้ว `catalog_places` วันนี้ **1118 แถว** ⇒ อ่านรวดเดียวจะได้ไม่ครบ **แล้วตัวอุ่นจะข้ามของจริงไปเงียบ ๆ**
 * · ⚠️ **ไม่มีใครใน `db.ts` ใช้ `.range()` มาก่อนเลย** — ตัวอื่นอ่านทีละทริป/ทีละวัน จึงไม่เคยชนเพดานนี้
 * 🎯 **อาการเวลาโดนตัด อ่านเหมือน "อุ่นครบแล้ว"** เพราะแถวที่หายไปไม่เคยเข้ามาในรายการตั้งแต่แรก
 *
 * 📌 **ทั้งสามตัวคืน `null` เมื่ออ่านไม่ได้ ไม่ใช่รายการว่าง** — ผู้เรียกต้องแยก *"ไม่มีอะไรต้องอุ่น"*
 *    ออกจาก *"ถามฐานไม่ได้"* · ถ้าคืนว่างทั้งสองกรณี cron จะรายงานว่าไม่มีงานทำในวันที่ฐานล่ม
 * ⚠️ **ไม่มีตัวไหนกรอง fixture ด้วยรหัสประเทศสงวน** — และไม่ต้อง: fixture ในคลังมี `maps_query` เป็น `null`
 *    ทั้ง 916 แถว (วัดแล้ว) → `warmTargets()` กรองทิ้งเองด้วยกฎ *"ไม่มีคีย์ = ข้าม"* · **เกณฑ์อยู่ที่เดียว**
 */
const WARM_PAGE = 500;

export async function catalogKeyRows(db: Db) {
  const out: { id: string; maps_query: string | null; google_place_id: string | null }[] = [];
  for (let from = 0; ; from += WARM_PAGE) {
    const { data, error } = await engineTable(db, "catalog_places")
      .select("id, maps_query, google_place_id")
      .range(from, from + WARM_PAGE - 1);
    if (error || !data) return null;
    out.push(...data);
    if (data.length < WARM_PAGE) return out;
  }
}

/**
 * **อายุแคชสูงสุด — ผู้ใช้ตัดสิน 3 ก.ย. 2026: 30 วัน**
 *
 * 🔴 **ก่อนหน้านี้แคชไม่มีวันหมดอายุเลย** — เก็บแล้วเก็บเลย · P6 จดไว้ใน `4f26483`
 *    ว่า heartbeat ตรวจ *ความครบ* ไม่ได้ตรวจ *ความสด* · ใบนี้ปิดครึ่งหลัง
 *
 * 🎯 **สองฟิลด์ที่เอกสารของเราเองจัดว่าเน่าเร็วที่สุดคือเวลาเปิด-ปิดกับเรตติ้ง**
 *    · 30 วันยังห่างจากทริป 11–21 ต.ค. พอที่ข้อมูลจะสดตอนใช้จริง
 *    · ราคาคือเรียก Google เพิ่มประมาณเดือนละรอบต่อคีย์
 *
 * ⚠️ **ตัวเลขนี้เป็นการแลก ไม่ใช่ค่าที่ถูกต้องทางเทคนิค** — เปลี่ยนได้ แต่ต้องเป็นการเลือก
 */
export const CACHE_MAX_AGE_DAYS = 30;

/**
 * **เส้นที่ตัวอุ่นเริ่มไล่ต่ออายุ — สั้นกว่า TTL โดยตั้งใจ**
 *
 * 🔴 **ถ้าตัวอุ่นใช้เส้นเดียวกับ TTL ระบบจะแดงเป็นช่วง ๆ:**
 * ```
 * แถวที่ถูกอุ่นวันเดียวกัน  →  หมดอายุพร้อมกันในวันเดียวกัน
 * heartbeat เพดาน MISSING_DETAILS=0 →  แดงทันทีที่นาฬิกาข้าม 30 วัน
 * ตัวอุ่นไล่ตามด้วย limit ต่อรอบ     →  **แดงค้างจนกว่าจะไล่ครบ**
 * ```
 * 🎯 ***ไม่มีใครทำอะไรผิดสักขั้น*** — เป็นคุณสมบัติของการที่ของถูกสร้างเป็นกอง ไม่ใช่ของบั๊ก
 *
 * 🔴 **แก้ 3 ก.ย. 2026 — ฉบับแรกของบล็อกนี้เขียนว่า *"แถวทั้ง 174 ใบถูกอุ่นวันเดียวกัน"***
 *    **ผมไม่เคยวัด · ผมอนุมานจากตัวเลข `174/174` ที่ P6 รายงานเมื่อวาน**
 *    · แล้วส่งตัวเลขที่คำนวณจากสมมติฐานนั้นให้ P6 ไปใช้ตั้งความถี่ cron ด้วย
 *    **ของจริง (วัดจากฐาน) — สาม cohort ไม่ใช่ก้อนเดียว:**
 *    ```
 *    2026-08-06   26 แถว   → เข้าเกณฑ์ต่ออายุ (20 วัน) มาแล้ว **8 วัน** · หมดอายุ 5 ก.ย.
 *    2026-08-12    7 แถว   → หมดอายุ 11 ก.ย.
 *    2026-09-03  141 แถว   → หมดอายุ 3 ต.ค.
 *    ```
 *    ✅ **ดีไซน์ยังถูกทุกตัวอักษร** (แยกเส้นต่ออายุออกจาก TTL) — **เหตุผลประกอบผิด**
 *    · 🔴 **และมันเปลี่ยนความเร่งด่วน: 33 แถวเลยเส้นต่ออายุไปแล้ว ไม่ใช่ "อีก 20 วันค่อยคิด"**
 *    🎯 ***ตัวเลขที่อนุมานจากตัวเลขที่คนอื่นวัด อ่านเหมือนตัวเลขที่วัด*** —
 *       `174/174` เป็นจริง · *"อุ่นวันเดียวกัน"* เป็นสิ่งที่ผมเติมเข้าไปเอง
 *
 * ⚠️ **สิ่งที่ TTL นี้ *ไม่* ทำ — วัดแล้ว 3 ก.ย. 2026:**
 *    **ไม่มีเส้นทางที่ผู้ใช้ใช้กรอง `fetched_at` เลยสักเส้น**
 *    (`app/api/place-details` · `place-photos` · `place-name` · `lib/placeQuery` · `lib/mapLinks` = 0)
 *    ⇒ **แถวที่หมดอายุยังถูกเสิร์ฟให้ผู้ใช้ตามปกติ · TTL ขับแค่ *การต่ออายุ* ไม่ใช่ *การมองเห็น***
 *    · ✅ ตั้งใจให้เป็นแบบนี้: ข้อมูลเก่าดีกว่าไม่มีข้อมูล โดยเฉพาะตอนออฟไลน์
 *    · 🔴 **ห้ามเติมตัวกรองนี้เข้าเส้นทางผู้ใช้โดยไม่คิด** — วันที่เติม ผู้ใช้จะเห็นสถานที่หายไปเฉย ๆ
 *
 * ✅ **แยกสองคำถามที่ถูกยุบเป็นข้อเดียว:**
 * ```
 * "เก่าจนใช้ไม่ได้แล้วหรือยัง"   → CACHE_MAX_AGE_DAYS (30)  ← heartbeat วัดด้วยอันนี้
 * "ถึงเวลาต่ออายุหรือยัง"        → CACHE_REFRESH_AFTER_DAYS (20)  ← ตัวอุ่นใช้อันนี้
 * ```
 * ⇒ ตัวอุ่นมีเวลา **10 วัน** ไล่ต่ออายุก่อนแถวจะเข้าเกณฑ์ "ขาด" ของ heartbeat
 *
 * ⚠️ **เงื่อนไขที่ต้องจริงถึงจะได้ผล — ปริมาณงานต่อวันต้องพอ:**
 *    174 คีย์ × 2 ตาราง ต้องถูกไล่ครบภายใน 10 วัน ⇒ **≥ 18 คีย์/วัน ต่อตาราง**
 *    · `limit: 3` ต่อรอบ ⇒ ต้องรัน **≥ 6 รอบ/วัน** · cron รายชั่วโมงเกินพอ (24 รอบ)
 *    · 🔴 **ถ้าความถี่ไม่พอ เส้นนี้ไม่ช่วยอะไรเลย** — มันซื้อเวลา ไม่ได้ลดงาน
 */
export const CACHE_REFRESH_AFTER_DAYS = 20;

function freshSince(maxAgeDays: number): string {
  return new Date(Date.now() - maxAgeDays * 86_400_000).toISOString();
}

/**
 * คีย์ที่มีแถว **ที่ยังสด** อยู่ใน `place_details_cache`
 *
 * 🔴 **แถวที่เก่ากว่า `maxAgeDays` ถูกนับว่า "ยังไม่มี" โดยตั้งใจ** — ไม่ใช่บั๊ก
 *    ⇒ มันจะกลับไปอยู่ในรายการที่ต้องอุ่นเอง **โดยไม่ต้องแก้ `warmTargets` เลยสักบรรทัด**
 * 🎯 นี่คือเหตุผลที่ TTL ลงที่ *ตัวอ่าน* ไม่ใช่ที่ *ตัวเลือกเป้าหมาย* —
 *    ตัวเลือกเป้าหมายถามว่า "อะไรยังไม่มีแคชที่ใช้ได้" ซึ่งเป็นคำถามที่ถูกอยู่แล้ว
 */
export async function cachedDetailKeys(db: Db, maxAgeDays = CACHE_MAX_AGE_DAYS) {
  const out: string[] = [];
  for (let from = 0; ; from += WARM_PAGE) {
    // 🔴 ตารางแคชไม่อยู่ใน `EngineTable` โดยตั้งใจ → ใช้ `db.from()` ตรงเหมือนที่ `route` ทำ
    const { data, error } = await db
      .from("place_details_cache")
      .select("maps_query")
      .gte("fetched_at", freshSince(maxAgeDays))
      .range(from, from + WARM_PAGE - 1);
    if (error || !data) return null;
    for (const r of data) out.push(r.maps_query);
    if (data.length < WARM_PAGE) return new Set(out);
  }
}

/**
 * คีย์ที่มีแถว **ที่ยังสด** อยู่ใน `place_photo_cache`
 *
 * 🔴 **แถวที่เก่ากว่า `maxAgeDays` ถูกนับว่า "ยังไม่มี" โดยตั้งใจ** — ไม่ใช่บั๊ก
 *    ⇒ มันจะกลับไปอยู่ในรายการที่ต้องอุ่นเอง **โดยไม่ต้องแก้ `warmTargets` เลยสักบรรทัด**
 * 🎯 นี่คือเหตุผลที่ TTL ลงที่ *ตัวอ่าน* ไม่ใช่ที่ *ตัวเลือกเป้าหมาย* —
 *    ตัวเลือกเป้าหมายถามว่า "อะไรยังไม่มีแคชที่ใช้ได้" ซึ่งเป็นคำถามที่ถูกอยู่แล้ว
 */
export async function cachedPhotoKeys(db: Db, maxAgeDays = CACHE_MAX_AGE_DAYS) {
  const out: string[] = [];
  for (let from = 0; ; from += WARM_PAGE) {
    // 🔴 ตารางแคชไม่อยู่ใน `EngineTable` โดยตั้งใจ → ใช้ `db.from()` ตรงเหมือนที่ `route` ทำ
    const { data, error } = await db
      .from("place_photo_cache")
      .select("maps_query")
      .gte("fetched_at", freshSince(maxAgeDays))
      .range(from, from + WARM_PAGE - 1);
    if (error || !data) return null;
    for (const r of data) out.push(r.maps_query);
    if (data.length < WARM_PAGE) return new Set(out);
  }
}

/**
 * `catalog_places.id` ที่ถูกอ้างจาก `trip_stops` — สัญญาณลำดับความสำคัญ
 * 🔴 **ผู้ใช้ไม่ต้องเขียนอะไรเพื่อให้สัญญาณนี้เกิด** — เป็นผลข้างเคียงของการวางแผนทริปตามปกติ
 *    (นั่นคือเหตุผลที่ปฏิเสธคิว cache-miss ที่ `route` เป็นคนเขียน)
 */
export async function tripReferencedCatalogPlaceIds(db: Db) {
  const out: string[] = [];
  for (let from = 0; ; from += WARM_PAGE) {
    const { data, error } = await engineTable(db, "trip_stops")
      .select("catalog_place_id")
      .not("catalog_place_id", "is", null)
      .range(from, from + WARM_PAGE - 1);
    if (error || !data) return null;
    for (const r of data) if (r.catalog_place_id) out.push(r.catalog_place_id);
    if (data.length < WARM_PAGE) return new Set(out);
  }
}

/** แกนร่วมของสองตัวบน — ถามคลังว่าค่าไหน "มีอยู่จริง" · **ล้ม = เซตว่าง ไม่ใช่โยน ไม่ใช่เดา** */
async function catalogPublicValues(
  db: Db,
  column: "legacy_slug" | "maps_query" | "google_place_id",
  values: string[]
): Promise<Set<string>> {
  const wanted = values.filter((v) => v.length > 0);
  if (wanted.length === 0) return new Set();
  const { data, error } = await engineTable(db, "catalog_places").select(column).in(column, wanted);
  if (error || !data) return new Set();
  return new Set(
    data.map((r) => (r as Record<string, string | null>)[column]).filter((v): v is string => !!v)
  );
}

export function catalogPlaceById(db: Db, id: string) {
  return engineTable(db, "catalog_places").select("*").eq("id", id).maybeSingle();
}

/**
 * resolve คลังสถานที่ **เป็นชุดในคำขอเดียว** — `E8` (P5 ขอ · P1 เขียน · 28 ส.ค. 2026)
 *
 * 🔴 **ทำไมต้องมี ทั้งที่ `catalogPlaceById` ทำได้อยู่แล้ว**
 * `trip_stops` ถือแค่ `catalog_place_id` → วันหนึ่งมี 5–8 จุด = **5–8 คำขอ** ถ้า resolve ทีละใบ
 * · และของ Copilot **ยิงทุกเทิร์นของบทสนทนา ไม่ใช่ทุกการโหลดหน้า** — N+1 ที่นั่นแพงกว่าที่หน้าจอมาก
 * · ฝั่ง custom มี `customPlaceRowsOfTrip()` ครอบให้แล้วในคำขอเดียว **ขาดแค่ฝั่งคลัง**
 *
 * 🔴 **ไม่กรอง `picker_hidden`** — เหตุผลเดียวกับ `catalogPlaceById` ข้างบนทุกตัวอักษร:
 * นี่คือ resolve (*"แถวนี้คือที่ไหน"*) ไม่ใช่ browse (*"มีที่ไหนให้เลือกบ้าง"*)
 * · กรองที่นี่ = **แถวสนามบิน/สถานีในแผนของผู้ใช้กลายเป็น "ไม่รู้จักสถานที่" ทั้งที่ข้อมูลอยู่ครบ**
 *
 * 🔴 **`ids` เป็น tuple ที่ต้องมีอย่างน้อย 1 ตัว — โดยตั้งใจ ไม่ใช่ความเข้มงวดเกินเหตุ**
 * `.in("id", [])` สร้าง query string `id=in.()` (ยืนยันจาก `postgrest-js/src/PostgrestFilterBuilder.ts`
 * ที่ `join(",")` อาเรย์ว่างได้สตริงว่าง) — **ผมไม่ได้ยืนยันว่า PostgREST ตอบอะไรกับรูปนั้น**
 * และคำตอบที่เป็นไปได้ทั้งสองทางแย่คนละแบบ: 400 คือ error ที่โผล่เฉพาะตอนรายการว่าง ·
 * 200 พร้อมอาเรย์ว่างคือ **"ไม่พบ" ที่แยกไม่ออกจาก "ไม่ได้ถาม"** ซึ่งเป็นตระกูลที่ทีมนี้ไล่ปิดกันทั้งวัน
 * → **บังคับที่ชนิด ผู้เรียกต้องตัดสินใจเรื่องรายการว่างเองก่อนถึงที่นี่** และคอมไพเลอร์เป็นคนทวง
 * ```ts
 * const ids = stops.map((s) => s.catalog_place_id).filter((v) => v !== null);
 * const places = ids.length > 0 ? (await catalogPlacesByIds(db, ids as [string, ...string[]])).data : [];
 * ```
 */
export function catalogPlacesByIds(db: Db, ids: readonly [string, ...string[]]) {
  /**
   * 🔴 **แก้ 2 ก.ย. 2026 — เดิมเป็น `select("*")` ซึ่งคืนแถวที่ *ไม่มีชื่อ***
   * `db.ts:152` (ห่างขึ้นไปไม่ถึง 40 บรรทัด) เตือนเรื่องนี้ไว้เองแล้วว่า
   * **`catalog_places` ไม่มีคอลัมน์ชื่อเลยสักคอลัมน์** — ชื่ออยู่ `catalog_place_names` แยกตาม locale
   * 🎯 **คำเตือนถูกเขียน *ก่อน* ฟังก์ชันนี้ และฟังก์ชันนี้ก็ยังพลาดข้อเดียวกัน** —
   *    เพราะตอนเขียน ผมกำลังตอบคำถาม *"ยิงทีเดียวได้หลายใบไหม"* ไม่ใช่ *"แถวที่ได้ใช้งานได้ไหม"*
   * ⚠️ **ไม่มีใครเห็นเพราะยังไม่มีใครเรียก** — เขียนล่วงหน้าให้ `E8` · ผู้เรียกรายแรกคือ `E6-AC13`
   *
   * 🔴 **ไม่ใส่ `!inner` โดยตั้งใจ** — นี่คือ *resolve* ไม่ใช่ *browse* (เหตุผลเดียวกับ `picker_hidden` ข้างบน)
   * · P3 ไล่ migration ยืนยันว่า `city_id` เป็น `not null` → `!inner` ทำแถวหายไม่ได้ **วันนี้**
   *   **แต่เส้นทาง resolve ไม่ควรมีตัวกรองที่ทำแถวหายได้อยู่ตั้งแต่แรก** ไม่ว่ามันจะยิงโดนหรือไม่
   *   · ถ้าวันหนึ่ง `city_id` เปิดให้ null ได้ ตัวกรองนั้นจะเริ่มกินแถวเงียบ ๆ โดยไม่มีใครแก้บรรทัดนี้
   *
   * 📌 รูปที่ select ต้องตรงกับ `RawCatalogPlace` ใน `trip.ts` เพราะผลถูกส่งต่อให้ `catalogPlaceCards()`
   *    — **ตัวเลือกชื่อตาม `locale`/`priority` อยู่ที่นั่นใบเดียว ห้ามเขียนซ้ำที่ผู้เรียก**
   */
  return engineTable(db, "catalog_places")
    .select(
      "id, legacy_slug, category, source, lat, lng, address_local, maps_query, google_place_id, youtube_query, weather_sensitivity, catalog_cities(legacy_slug, country_id), catalog_place_names(locale, name, priority), catalog_place_descriptions(locale, description)",
    )
    .in("id", [...ids]);
}

/**
 * รายชื่อประเทศที่ผลิตภัณฑ์รองรับ — ขั้นแรกของ picker "เลือกประเทศ → เลือกเมือง" (`E5` · ผู้ใช้สั่ง 27 ส.ค. 2026)
 *
 * 🔴 **ทำไมต้องเป็น endpoint แยก ไม่ใช่ derive จาก `searchCatalogCities`** (P2 ชี้ และถูก)
 * `/api/engine/cities` มีเพดาน `limit` 50 → ประเทศที่เมืองเยอะกินโควตาจนประเทศอื่นหายไปเลย
 * · **เป็นกับดักเดียวกับที่ผมเพิ่งชนเองตอนเรียง `country_id` ก่อน แค่ย้ายจากชั้นเมืองมาชั้นประเทศ**
 * · และประเทศ **ไม่ใช่ข้อมูลปลายเปิด** — มีจำนวนจำกัดโดยธรรมชาติ จึงไม่มี `limit` โดยตั้งใจ
 *
 * 🔴 **`supported` กรองที่นี่ คือจุดที่ปิดช่องได้ทั้งช่อง** — ผู้ใช้เลือกประเทศทดสอบไม่ได้ตั้งแต่แรก
 * จึงไม่มีทางเห็นหัวข้อกลุ่มชื่อ "ทดสอบสาม" · ดูเหตุผลของคอลัมน์ใน `20260828001500`
 *
 * ⚠️ **ต้องรัน `20260828001500` ก่อน ไม่งั้นฟังก์ชันนี้ 502 ทุกคำขอ** (คอลัมน์ยังไม่มี)
 * · ยอมรับได้เพราะนี่เป็น **route ใหม่ที่ยังไม่มีใครเรียกได้อยู่แล้ว** (วันนี้ตอบ 404)
 *   ต่างจากการแก้ `select` ของเส้นทางที่ใช้งานอยู่ ซึ่งทำให้ของที่เคยทำงานพัง (`fad69d0`)
 */
export function listSupportedCountries(db: Db) {
  return engineTable(db, "catalog_countries")
    .select("id, name_th, name_en")
    .eq("supported", true)
    .order("name_th");
}

/**
 * เมืองทั้งคลัง (เฉพาะ `country_id` + ชื่อไทย) — ใช้นับ "N เมือง" ต่อประเทศบนหน้าแรก (`E5`)
 * เจ้าของ: P1-Lead · 4 ก.ย. 2026 · ขอโดย P2 สำหรับการ์ดประเทศ
 *
 * 🔴 **ดึงทั้งคลังแล้วนับฝั่งเซิร์ฟเวอร์ — ไม่ใช่ยิงรายประเทศ**
 * PostgREST ไม่มี `group by` ⇒ ทางเลือกคือ *ยิง 9 ครั้ง* หรือ *ดึงทีเดียวแล้วนับ*
 * วันนี้ทั้งคลังมี **78 เมือง** ⇒ แถวเดียวจบ · P2 คิดถูกที่ปฏิเสธการยิง 9 คำขอเพื่อของประดับ
 * · ⚠️ **ถ้าวันหนึ่งคลังโตเป็นหลักพัน ต้องเปลี่ยนเป็น RPC ที่ `group by` ในฐาน** —
 *   เกณฑ์ไม่ใช่ "รู้สึกเยอะ" แต่คือ **เวลาตอบของ `/countries` เริ่มขยับ**
 *
 * ⚠️ `.order("created_at")` **ไม่ใช่การจัดอันดับความนิยม** — คลังถูก seed เมืองหลักก่อน
 *    ⇒ สามชื่อแรกจึงมัก *เป็น* เมืองหลัก **โดยผลข้างเคียง ไม่ใช่โดยการวัด**
 *    🔴 ห้ามเรียกมันว่า "เมืองยอดนิยม" ที่ไหนทั้งใน UI และในโค้ด — เราไม่มีข้อมูลนั้น
 */
export function listCatalogCityNames(db: Db) {
  return engineTable(db, "catalog_cities")
    .select("country_id, name_th")
    .order("created_at");
}

/**
 * ค้นเมืองในคลังด้วยคำที่ผู้ใช้พิมพ์ — ตัวป้อนของ autocomplete "เมืองปลายทาง" ในฟอร์มสร้างทริป (`E5`)
 *
 * ค้นทั้ง 3 ชื่อ (`name_th` · `name_en` · `name_local`) เพราะผู้ใช้พิมพ์ได้ทั้ง "ฮานอย" · "Hanoi" · "Hà Nội"
 * และ **ไม่มีชื่อไหนเป็นชื่อหลัก** — คลังเก็บทั้งสามเป็นข้อมูลเท่ากัน
 *
 * 🔴 **`q` ถูกล้างก่อนต่อเป็น filter ของ PostgREST เสมอ**
 * ไวยากรณ์ `.or()` แยกเงื่อนไขด้วย `,` และห่อด้วย `()` → คำค้นที่มีอักขระพวกนั้น
 * **ไม่ได้ทำให้คิวรีพัง มันทำให้คิวรีกลายเป็นคิวรีอื่น** ซึ่งอ่านผลเป็น "ไม่เจอ" ได้สนิท
 * · `%` และ `_` เป็น wildcard ของ `ilike` → ผู้ใช้พิมพ์ `%` แล้วได้ทั้งคลัง เป็นเรื่องคนละเรื่องกับที่เขาถาม
 * 🎯 กันที่นี่ ไม่ใช่ที่ route — ใครก็ตามที่เรียกฟังก์ชันนี้ในอนาคตได้การกันไปด้วยโดยไม่ต้องรู้
 */
export function searchCatalogCities(
  db: Db,
  opts: { q: string; countryId?: string; limit?: number },
) {
  const safe = opts.q.replace(/[,()%_*\\]/g, " ").trim();
  // 🔴 ฝัง `catalog_countries` เพิ่ม 27 ส.ค. 2026 — ผู้ใช้ขอให้ "แบ่งเมืองปลายทางตามประเทศ"
  //    ฝั่ง UI ต้องมี **ชื่อประเทศ** ไปทำหัวข้อกลุ่ม · `country_id` ที่มีอยู่เดิมเป็นรหัส 2 ตัวอักษร
  //    (`jp`) ซึ่งเอาไปโชว์ไม่ได้ · การให้ UI ยิงตารางประเทศเองอีกรอบ = คำขอที่สองต่อการพิมพ์หนึ่งครั้ง
  //
  // ⚠️ **เพิ่มอย่างเดียว ไม่แตะฟิลด์เดิมสักตัว** — `TripDestinationPicker` อ่าน `id`/`name_th`/
  //    `name_en`/`country_id` อยู่แล้ว · เปลี่ยนรูปข้อมูลใต้มือคนอื่นคือสิ่งที่ทำหน้า Home พัง 502
  //    มาแล้ววันเดียวกันนี้ (`fad69d0`) · ของใหม่จึงเป็นคีย์ใหม่ล้วน ผู้เรียกเดิมไม่รู้สึกอะไร
  // 🔴 `!inner` + `supported` — **กัน fixture ของชุดทดสอบไม่ให้โผล่ในช่องเลือกจุดหมาย**
  //    วัดก่อนแก้: เมืองในฐาน 1,736 · ของ 4 ประเทศที่รองรับ 42 · **fixture ที่ผู้ใช้ค้นเจอได้ 1,694 (98%)**
  //    เจอเพราะ `q="อ"` คืนเมืองชื่อ "เมืองC" จากประเทศชื่อ "ทดสอบสาม" (ดู `20260828001500`)
  //
  //    ⚠️ **`!inner` จำเป็น ไม่ใช่ของแถม** — ถ้าฝังแบบธรรมดา PostgREST จะ *ยังคืนแถวเมือง*
  //    แล้วให้ `catalog_countries` เป็น `null` แทนที่จะตัดแถวทิ้ง → กรองไม่ติด และ**ดูเหมือนติด**
  //    เพราะผลลัพธ์หน้าตาเปลี่ยนไป
  let query = engineTable(db, "catalog_cities")
    // 🔴 `legacy_slug` เพิ่ม 27 ส.ค. 2026 (P2 ขอ) — เป็น**คีย์ของรูปประจำเมือง** ไม่ใช่ของแถม
    //    `TripCoverImage` บนการ์ด Home ประกอบ URL เป็น `/covers/city-<slug>.svg` แล้วตกไป
    //    `/covers/country-<id>.svg` ถ้าไม่เจอ · picker ต้องใช้ **cascade เดียวกัน** ไม่ใช่ระบบรูปที่สอง
    //    · ฐานมีข้อมูลอยู่แล้ว (`tripsForUser()` ดึง `legacy_slug` อยู่) แค่ไม่ได้ส่งออกทางนี้
    //    ⚠️ **เพิ่มอย่างเดียว ไม่แตะฟิลด์เดิม** — ผู้เรียกที่มีอยู่ไม่รู้สึกอะไร
    .select(
      // 🔴 **`lat`/`lng` เพิ่ม 2 ก.ย. 2026 — ให้ "เมือง" มีรูปเดียวกันทั้งสอง endpoint** (P2 ชี้)
      //    `E2-AC16` เพิ่มพิกัดให้ `/api/engine/trips` (`trip.destinations[]`) แต่ **ไม่ได้เพิ่มที่นี่**
      //    → แถวจาก `/api/engine/cities` ส่งเข้า `cityCenterOf()` จะได้ `null` **ทุกใบ** โดยชนิดไม่ห้าม
      //    🎯 **และมันจะดูเหมือนบั๊กของ `cityCenterOf` ไม่ใช่ของ endpoint** เพราะฟังก์ชันคืน `null`
      //       อย่างสุภาพตามที่ออกแบบ · **ผู้เรียกจะไปไล่ผิดที่**
      //    ⚠️ วันนี้ยังไม่มีใครทำแบบนั้น (`TripDestinationPicker` ไม่ใช้พิกัด) — **เติมก่อนมีคนเจอ**
      //    · เพิ่มอย่างเดียว ไม่แตะฟิลด์เดิม ตามกติกาที่คอมเมนต์ข้างบนตั้งไว้เอง
      "id, country_id, legacy_slug, name_th, name_en, name_local, lat, lng, catalog_countries!inner(id, name_th, name_en)",
    )
    .eq("catalog_countries.supported", true);
  if (safe !== "") {
    query = query.or(
      `name_th.ilike.%${safe}%,name_en.ilike.%${safe}%,name_local.ilike.%${safe}%`,
    );
  }
  if (opts.countryId) query = query.eq("country_id", opts.countryId);
  // 🔴 **เรียงตามชื่อเมืองเท่านั้น — ห้ามเรียงตามประเทศก่อน** แม้ปลายทางคือลิสต์ที่แบ่งตามประเทศ
  //
  // ฉบับแรกของบรรทัดนี้ผมเขียน `.order("country_id").order("name_th")` เพราะคิดว่า
  // "ผลที่เรียงตามประเทศมาแล้ว = UI จัดกลุ่มง่ายที่สุด" · **ยิงจริงแล้วมันพังทันที**:
  //   q="า" limit=12 → ได้ญี่ปุ่น 12 เมืองรวด · ไทย/เกาหลี/เวียดนาม **ไม่โผล่เลยสักเมือง**
  //
  // 🎯 เพราะ `limit` ตัด *หลัง* เรียง — เรียงตามประเทศแล้วตัด แปลว่า **ประเทศท้ายตัวอักษร
  //    ไม่มีวันปรากฏ** จนกว่าประเทศแรกจะหมดเมือง · ยิ่งแบ่งกลุ่ม ยิ่งพัง เพราะกลุ่มที่หายไป
  //    ดูเหมือน "ไม่มีเมืองชื่อนี้ในประเทศนั้น" ทั้งที่มี
  //
  // เรียงตามชื่อ → 12 แถวแรกกระจายข้ามประเทศตามความใกล้เคียงจริง · **การจัดกลุ่มเป็นเรื่องของ
  // การแสดงผล ไม่ใช่ของการเลือกแถว** — ข้อมูลประเทศฝังมาครบแล้ว UI จัดกลุ่มเองได้โดยไม่ยิงเพิ่ม
  return query.order("name_th").limit(opts.limit ?? 20);
}

// ───────────────────────────────────────────────────────────────────────────
// จุดแวะและเหตุการณ์ของวัน — `D81` ③ · ③.๕
// ───────────────────────────────────────────────────────────────────────────

/**
 * จุดแวะทั้งหมดของวันในแผนนี้ **ที่ยังไม่ถูกลบ** เรียงตาม `(rank, id)`
 *
 * `rank` ไม่ unique โดยตั้งใจ (`D6`) → **`id` คือ tie-break ที่ทำให้ 2 เครื่องได้ลำดับเดียวกัน**
 * ไม่ใช่รายละเอียดของ `order by` แต่เป็นเหตุผลที่ `order by` มีอยู่
 */
export function dayStops(db: Db, opts: { tripDayId: string; planId: string }) {
  return engineTable(db, "trip_stops")
    .select("*")
    .eq("trip_day_id", opts.tripDayId)
    .eq("plan_id", opts.planId)
    .is("deleted_at", null)
    .order("rank", { ascending: true })
    .order("id", { ascending: true });
}

/**
 * แถวที่ **กำหนดขอบของวัน** — `schedule_bound = 'before' | 'after'`
 *
 * 🔴 **ห้ามมีทางเรียกที่ได้ผลลัพธ์นี้โดยไม่มี `deleted_at is null` + `order by rank, id`**
 * เพราะสองครึ่งนี้พังคนละแบบ และไม่มีครึ่งไหนส่งเสียง:
 * ```
 * ขาด order by   → 2 เครื่องเลือกคนละแถว
 * ขาด deleted_at → 2 เครื่องเลือกแถวที่ตายแล้ว เหมือนกันทั้งคู่ (แย่กว่า เพราะดู "สอดคล้อง")
 * ```
 * · `D81` ④ ห้าม unique *"หนึ่ง `before` ต่อวัน"* → **ที่นี่ต้องพร้อมรับหลายแถว**
 *
 * 🔴 **ฉบับแรกของคอมเมนต์นี้เขียนว่า *"ตัวแรกของลิสต์คือตัวที่ชนะ"* — ผิด และ P5 แก้ให้ (26 ส.ค. 2026)**
 * > **ขอบคือ *ข้อจำกัด* ขอบที่ซ้อนกันจึง *ตัดกัน*** — `after` เอา**น้อยที่สุด** · `before` เอา**มากที่สุด**
 * `rank` เรียงหน้าจอ และผม**จงใจไม่ผูกมันกับเวลา** → **เป็นได้แค่ tie-break**
 * · และเทียบต้องทำบน**นาทีจริง** (`day_offset * 1440 + HH:MM`) ไม่ใช่บนสตริง — ทริปนี้มีแถว 01:15 ที่ `day_offset=1`
 *
 * 📌 **กฎอยู่ที่ [`scheduleBounds.ts`](./scheduleBounds.ts) → `pickScheduleBounds()` ใช้ตัวนั้น อย่าเลือกเอง**
 */
export function dayScheduleBounds(db: Db, opts: { tripDayId: string; planId: string }) {
  return engineTable(db, "trip_stops")
    .select("*")
    .eq("trip_day_id", opts.tripDayId)
    .eq("plan_id", opts.planId)
    .eq("kind", "event")
    .not("schedule_bound", "is", null)
    .is("deleted_at", null)
    .order("rank", { ascending: true })
    .order("id", { ascending: true });
}

/**
 * ⚠️ **จงใจคืน tombstone ด้วย** — ใช้กับกฎ merge ของ `E2-AC12` เท่านั้น
 *
 * เป็นฟังก์ชันแยกเพราะ *"อ่าน tombstone"* ต้องเป็นการ **เลือก** ที่มีชื่อ
 * ไม่ใช่สิ่งที่เกิดขึ้นเพราะใครลืมใส่ `.is("deleted_at", null)`
 */
export function dayStopsIncludingDeleted(db: Db, opts: { tripDayId: string; planId: string }) {
  return engineTable(db, "trip_stops")
    .select("*")
    .eq("trip_day_id", opts.tripDayId)
    .eq("plan_id", opts.planId)
    .order("rank", { ascending: true })
    .order("id", { ascending: true });
}

// ───────────────────────────────────────────────────────────────────────────
// ค้นชื่อสถานที่ — `D56` · รูปพารามิเตอร์จาก `copilot-spec.md §25` (P5)
// ───────────────────────────────────────────────────────────────────────────

/**
 * 🔴 **`intent` เป็น required และ TS ก็บังคับเหมือนที่ SQL บังคับ — จงใจซ้ำกันสองชั้น**
 *
 * P5: *ค่าเริ่มต้นใดก็ตาม **ถูกครึ่งหนึ่งของเวลา และเงียบอีกครึ่งหนึ่ง***
 * · default `discover` → ผู้ใช้ถามถึงจุดในแผนตัวเอง แล้วได้ *"ไม่เจอ"*
 * · default `identify` → ขอบเขตแคบเกิน แล้วได้ *"ไม่เจอ"* เหมือนกัน
 * **ทั้งสองพังด้วยข้อความเดียวกันเป๊ะ → แยกไม่ออกจาก log ว่าตั้ง default ผิดข้าง**
 *
 * | | `identify` — *"'ตลาดกลางคืน' คือจุดไหนในแผน"* | `discover` — *"หาที่แบบนี้ให้เพิ่ม"* |
 * |---|---|---|
 * | ขอบเขต | เฉพาะที่ทริปนี้อ้างถึงแล้ว (`trip_stops`) · **รวม `custom_places`** | `catalog_places` ของเมืองนั้น |
 * | `picker_hidden` | **ไม่กรอง** — สนามบินคือจุดแวะจริง | **กรอง** |
 * | ตัดของที่อยู่ในแผนแล้ว | **ไม่** — นั่นคือสิ่งที่กำลังหา | **ใช่** |
 */
export type PlaceSearchIntent = "identify" | "discover";

export type PlaceSearchHit = {
  source: "catalog" | "custom";
  place_id: string;
  city_id: string | null;
  matched_name: string;
  locale: string;
  score: number;
};

/**
 * 🔴 **RPC นี้เป็น `security invoker`** — RLS เป็นตัวจำกัดขอบเขตให้เอง
 * ไม่มีบรรทัดไหนในนั้นเช็คว่าใครเป็นเจ้าของทริป และนั่นคือเหตุผลที่มันจะยังถูกวันที่ policy เปลี่ยน (`D38`)
 */
export function searchPlaceNames(db: Db, opts: {
  tripId: string;
  query: string;
  intent: PlaceSearchIntent;
  cityId?: string;
  limit?: number;
}) {
  return db.rpc("search_place_names", {
    p_trip_id: opts.tripId,
    p_query: opts.query,
    p_intent: opts.intent,
    p_city_id: opts.cityId ?? undefined,
    p_limit: opts.limit ?? 20,
  });
}

// ───────────────────────────────────────────────────────────────────────────
// ทริปของผู้ใช้ — `E3` · ตรรกะการเลือกอยู่ที่ [`trip.ts`](./trip.ts) ไม่ใช่ที่นี่
// ───────────────────────────────────────────────────────────────────────────

/**
 * โหมดของทั้งระบบ — `E3-AC7` · **`anon` เรียกได้โดยตั้งใจ**
 *
 * 🔴 คนที่กลับมาเปิดแอประหว่าง cutover **ต้องรู้ก่อนเริ่มพิมพ์ ไม่ใช่หลังจากพิมพ์โน้ตยาว ๆ
 * แล้วกดบันทึกไม่ได้** (P7 · ข้อที่เขาบอกว่าสำคัญที่สุดใน 4 ข้อ)
 * → ธงต้องอ่านได้ตั้งแต่ก่อนล็อกอิน · `P-50`: **ธงที่อ่านไม่ได้ ไม่ใช่ธง**
 *
 * ⚠️ คืนแค่ `read_only` + `reason` · **ไม่คืน `allow_maintenance_write`**
 *    เพราะไม่ใช่เรื่องของผู้ใช้ และเป็นครึ่งหนึ่งของด่าน
 */
export function systemMode(db: Db) {
  return db.rpc("system_mode");
}

/**
 * สร้างทริปใหม่ **พร้อมแผนเริ่มต้น** — `create_trip` (`20260825143958`)
 *
 * ## 🔴 ทำไมเพิ่งมี ทั้งที่ RPC อยู่ในฐานมาตั้งแต่ 25 ส.ค.
 * `grep -rn "create_trip" app/ components/ hooks/ lib/` → **ไม่มีอะไรเรียกมันเลยสักที่**
 * → **ไม่มีทางสร้างทริปจาก UI** → บัญชีใหม่ทุกบัญชีค้างอยู่ที่ *"ยังไม่มีทริป"* ตลอดกาล
 *
 * 🎯 **และนั่นคือเหตุผลที่ไม่มีใคร live-verify อะไรได้เลยทั้งวัน** — P2 รายงานว่าเปิดหน้าจริงไม่ได้
 * 4 รอบติดกัน (`TripHeader` · `NearbyPlacesModal` · โมดัล read-only · `ImmigrationSheet`)
 * **ทุกครั้งด้วยเหตุผลเดียวกัน และไม่มีใคร (รวมผม) ถามว่าทำไมบัญชีนั้นถึงไม่มีทริป**
 * · ครึ่งที่เหลือของ `E4-AC1` (เปิดหน้าทริปญี่ปุ่นจริง) ก็ติดข้อนี้เหมือนกัน
 *
 * ## ⚠️ `security definer` ตัวนี้ **ไม่ใช่สิทธิ์พิเศษ** (`D38`)
 * มันอ่าน `auth.uid()` ของ**ผู้เรียก** · เรียกผ่าน `createServerSupabase()` = session ของผู้ใช้จริง
 * **ไม่มี service role key เข้ามาเกี่ยวข้องเลย** · `authNoServiceRole.test.ts` ยังบังคับ `app/` เหมือนเดิม
 * · ที่มันต้องเป็น `definer` เพราะมันเขียน `trips` + `trip_plans` ในทรานแซกชันเดียว
 */
export function createTrip(
  db: Db,
  args: { title: string; startDate: string; endDate: string; baseTimezone?: string | null },
) {
  return db.rpc("create_trip", {
    p_title: args.title,
    p_start_date: args.startDate,
    p_end_date: args.endDate,
    p_base_timezone: args.baseTimezone ?? undefined,
  });
}


/**
 * ทริปทั้งหมดที่ผู้ใช้เห็นได้ **เรียงตามเวลาสร้าง**
 *
 * 🔴 **ไม่มี `where` เรื่องสิทธิ์ในนี้เลย — `trips_select` เป็นคนกรอง**
 * เขียน `.eq("owner", …)` เอง = แหล่งความจริงที่สองเรื่องสิทธิ์ ที่ต้องคอยให้ตรงกับ policy ตลอดไป (`P-15`)
 *
 * ⚠️ **`order` มีไว้ให้ผลคงที่ ไม่ได้มีไว้ให้ใครหยิบตัวแรก** — ดู `trip.ts` ว่าทำไมการหยิบตัวแรกถึงผิด
 */
/**
 * สมาชิกของทริป พร้อมชื่อที่แสดง — ตัวป้อนแถว avatar ใน `TripHeader` (`E5`)
 *
 * 🔴 **ไม่มีบรรทัดไหนกรองสิทธิ์ที่นี่ และนั่นคือทั้งหมดของข้อนี้** (`D38`)
 * · แถวไหนเห็นได้ → `trip_members_select` (`app.can_read_trip`) ตัดสิน
 * · ชื่อไหนเห็นได้ → `profiles_select` (`app.shares_trip_with`) ตัดสิน **อีกชั้นแยกกัน**
 * → คนนอกทริปได้ **0 แถว** ไม่ใช่ `403` · ไม่ต้องเพิ่ม guard และ **ไม่ควรเพิ่ม**:
 *   guard ที่นี่จะเป็นแหล่งความจริงที่สองเรื่อง "ใครอ่านได้" ที่ต้องคอยให้ตรงกับ policy ตลอดไป (`P-15`)
 *
 * 🎯 **`[]` ไม่ได้แปลว่า "ทริปไม่มีสมาชิก" — ทุกทริปมีเจ้าของอย่างน้อยหนึ่งคนเสมอ**
 * (bootstrap trigger) → **`[]` = คุณไม่ใช่สมาชิก** · และมันตอบเท่ากับกรณี `tripId` ที่ไม่มีอยู่จริง
 * ซึ่งเป็นสิ่งที่ต้องการ: **ไม่มีทางถามว่าทริปนี้มีอยู่ไหมด้วยการดูรหัสตอบกลับ**
 *
 * 🔴 **`displayName` เป็น `null` แปลว่า *อ่านชื่อเขาไม่ได้* ไม่ใช่ *เขายังไม่ตั้งชื่อ*** (P4 ชี้ · P1 ตรวจแล้วรับ)
 *    `app.handle_new_user()` สร้างแถว `profiles` ให้ **ทุก** บัญชีตั้งแต่วินาทีที่สมัคร
 *    (`display_name` = metadata หรือ local-part ของอีเมล · `identity.sql:318-333`) → **แถวมีเสมอ**
 *    ⚠️ ฉบับแรกของคอมเมนต์นี้เขียนว่า *"บัญชีจาก `createUser` ไม่มีแถวใน `profiles`"* ซึ่ง **ไม่จริง**
 *    🎯 และมันผิดในทางที่อันตราย: `null` คือสัญญาณว่า **ชั้นที่สอง (`profiles_select` →
 *       `app.shares_trip_with`) ปฏิเสธการอ่าน** ทั้งที่ชั้นแรก (`can_read_trip`) ปล่อยแถวมาแล้ว
 *       — คนที่อ่านคอมเมนต์เก่าจะยักไหล่ให้ `null` ทั้งที่มันแปลว่าสิทธิ์สองชั้นไม่ตรงกัน
 *    · ผู้เรียกยังต้องเผื่อ `null` เหมือนเดิม **แต่ต้องเผื่อด้วยความเข้าใจที่ถูก**
 */
export function tripMembers(db: Db, tripId: string) {
  return engineTable(db, "trip_members")
    // 🔴 **`!user_id` ไม่ใช่ของประดับ — ไม่มีมันแล้ว route คืน 502 ให้สมาชิกทุกคน**
    //    `trip_members` มี FK ไป `profiles` **สองเส้น**: `user_id` (คนที่เป็นสมาชิก) และ
    //    `invited_by` (คนที่เชิญเขามา · `identity.sql:142,144`) → PostgREST เลือกไม่ถูกว่าจะ join
    //    เส้นไหน แล้วตอบ `PGRST201` **ไม่ใช่เดาเส้นใดเส้นหนึ่งเงียบ ๆ** (ซึ่งดีกว่ามาก)
    //    · ใช้ชื่อ **คอลัมน์** ไม่ใช่ชื่อ constraint (`!trip_members_user_id_fkey`) — ทนต่อการเปลี่ยนชื่อ
    //      constraint ที่ไม่มีใครตั้งใจให้กระทบโค้ด
    //    📌 P4 จับได้ด้วย probe ข้ามผู้ใช้ **ตอนที่ผมยังไม่ได้รันมันเลย** — `viewer` ควรได้ 200 แต่ได้ 502
    .select("user_id, role, profiles!user_id(display_name)")
    .eq("trip_id", tripId)
    .order("created_at");
}

/**
 * เขียนจุดหมายของทริป — ใช้ตอนสร้างทริปใหม่ (`E5`)
 *
 * 🔴 **ไม่ใช่ RPC และไม่ต้องเป็น** — `trip_destinations_insert` (`can_write_trip`) เป็นคนตัดสินสิทธิ์อยู่แล้ว
 *    เขียนผ่าน client ของผู้ใช้จริงตรง ๆ = **RLS ทำงานเหมือนทุกตาราง ไม่มีทางลัดใหม่ให้ดูแล** (`D38`)
 *    · ทางที่ปฏิเสธ: ขยาย `create_trip` ให้รับ `p_city_ids` — Postgres ถือว่าลายเซ็นใหม่เป็น
 *      **ฟังก์ชันคนละตัว** (overload) → PostgREST กำกวม (`PGRST203`) ต้อง `drop` ตัวเดิมก่อน
 *      = แตะ RPC ที่ flow สร้างทริปจริงใช้อยู่ **เพื่อฟีเจอร์ที่ไม่ต้องแตะมันเลยก็ได้**
 *
 * ⚠️ **`rank` มาจากลำดับใน array ที่ผู้เรียกส่งมา** — ผู้ใช้เรียงเมืองเอง ไม่ใช่เราจัดให้
 *    ไม่ unique โดยตั้งใจ (เหตุผลเดียวกับ `trip_stops.rank`) · tie-break อยู่ที่ `tripsForUser()`
 */
export function insertTripDestinations(db: Db, tripId: string, cityIds: readonly string[]) {
  return engineTable(db, "trip_destinations").insert(
    cityIds.map((cityId, i) => ({ trip_id: tripId, city_id: cityId, rank: i })),
  );
}

/**
 * เขียนทับจุดหมายทั้งรายการ — **สามก้าว ไม่ใช่หนึ่ง** (คู่กับ `setTripDestinationRank` · `deleteTripDestinationsExcept`)
 * เจ้าของ: P1-Lead · 4 ก.ย. 2026
 *
 * ## 🔴 ฉบับแรกใช้ `upsert` แล้วมันแดง — และรากคือสิ่งที่ต้อง *ไม่* แก้
 * ```
 * PUT /destinations → 403 { code: "42501", error: "permission denied for table trip_destinations" }
 * แต่ POST /trips ที่ใช้ .insert() ล้วน → 201    ⇒ สิทธิ์ insert มีจริง ที่แดงคือ upsert เท่านั้น
 * ```
 * `upsert` ของ PostgREST = `insert … on conflict do update set <ทุกคอลัมน์ในเพย์โหลด>`
 * ⇒ มันพยายาม `set trip_id` ด้วย · แต่ `20260827180000:150` ให้แค่ **`update (city_id, rank)`**
 *
 * 🎯 ***และการไม่ให้ `update trip_id` คือการป้องกันที่ตั้งใจ ไม่ใช่ของที่ลืม*** —
 * เขียน `trip_id` ทับได้ = **ย้ายจุดหมายไปแปะทริปของคนอื่นได้** (เหตุผลเดียวกับที่
 * `trip_days_insert` ต้องมี `with check`, `20260825110903:152`)
 * ⇒ **ทางแก้คือเลิกใช้ upsert ไม่ใช่ขยาย grant** · ท่าด้านล่างใช้เฉพาะสิทธิ์ที่มีอยู่แล้ว
 *
 * ## ลำดับสามก้าว — เลือกจาก "ล้มกลางทางแล้วเหลืออะไร"
 * ```
 * ① insert เมืองใหม่      ล้ม ⇒ รายการเดิมอยู่ครบ ไม่มีอะไรหาย
 * ② update rank ที่คงอยู่  ล้ม ⇒ เมืองครบ ลำดับเพี้ยน · กดใหม่ก็ตรง
 * ③ delete ส่วนเกิน        ล้ม ⇒ มีเมืองเกิน · กดใหม่ก็หาย
 * ```
 * 🔴 **ไม่มีทิศไหนเหลือ 0 เมือง — ซึ่งเป็นสภาพเดียวที่กู้ไม่ได้ และเป็นบั๊กที่ route นี้เกิดมาเพื่อแก้**
 * · 📌 ฉบับแรกผมเขียนคอมเมนต์ว่า *"ต้องมี policy insert และ update จึงจะ upsert ได้ · มีครบแล้ว"*
 *   **โดยอ่านจากไฟล์ migration ไม่ได้ยิงจริง** — และมันผิดเพราะสิทธิ์ระดับ *คอลัมน์* ไม่ใช่ระดับ *ตาราง*
 *   🎯 ***`grant` ในไฟล์ ตอบว่า "ให้อะไรไป" ไม่ได้ตอบว่า "คำสั่งนี้ผ่านไหม" — คนละคำถาม***
 *   (`TEAM.md §3.5` เขียนข้อนี้ไว้แล้วสำหรับฟังก์ชัน · **มันจริงกับคอลัมน์ด้วย**)
 */
export function insertTripDestinations2(db: Db, tripId: string, rows: readonly { cityId: string; rank: number }[]) {
  return engineTable(db, "trip_destinations")
    .insert(rows.map((r) => ({ trip_id: tripId, city_id: r.cityId, rank: r.rank })))
    .select("city_id");
}

/**
 * ตั้ง `rank` ของเมืองที่อยู่ในรายการอยู่แล้ว
 *
 * 🔴 **`set` เฉพาะ `rank` เท่านั้น** — `trip_id`/`city_id` อยู่ใน `where` ไม่ใช่ใน `set`
 * นั่นคือสิ่งที่ทำให้คำสั่งนี้ผ่าน `grant update (city_id, rank)` ในขณะที่ `upsert` ไม่ผ่าน
 */
export function setTripDestinationRank(db: Db, tripId: string, cityId: string, rank: number) {
  return engineTable(db, "trip_destinations")
    .update({ rank })
    .eq("trip_id", tripId)
    .eq("city_id", cityId)
    .select("city_id");
}

/** จุดหมายปัจจุบันของทริป — ต้องอ่านก่อนเขียน เพราะสามก้าวข้างบนต้องรู้ว่าใบไหนใหม่ ใบไหนเดิม */
export function tripDestinationsOf(db: Db, tripId: string) {
  return engineTable(db, "trip_destinations")
    .select("city_id, rank")
    .eq("trip_id", tripId)
    .order("rank")
    // 🔴 **tie-break — ขาดไปในฉบับแรก P4 จับได้ (4 ก.ย. 2026)**
    //    `rank` **ไม่ unique โดยตั้งใจ** (`20260827180000:96` — เหตุผลเดียวกับ `trip_stops.rank`)
    //    และ **เส้นทางของไฟล์นี้เองสร้างค่าซ้ำได้จริง**: `PUT /destinations` ก้าว ② (จัดลำดับ) ล้มกลางทาง
    //    ⇒ มีเมืองสองใบถือ `rank` เดียวกัน → **ลำดับที่ผู้ใช้เห็นต่างกันไปในแต่ละเครื่อง และไม่มีอะไรส่งเสียง**
    //    ⚠️ `city_id` ไม่ใช่ `id` — ตารางนี้ `primary key (trip_id, city_id)` **ไม่มีคอลัมน์ `id`**
    //       ⇒ `city_id` คือคอลัมน์ unique ต่อทริป จึงเป็น tie-break ที่ถูกต้องของตารางนี้
    .order("city_id", { ascending: true });
}

/**
 * ก้าวที่ **2 จาก 2** — ถอนเมืองที่หลุดออกจากรายการใหม่
 *
 * 🔴 **`keepCityIds` ต้องไม่ว่าง** — ผู้เรียกเป็นคนกัน (route ตอบ 400 ให้รายการว่าง)
 * ถ้าเผลอเรียกด้วยรายการว่าง `.not("city_id","in","()")` จะกลายเป็น *"ลบทุกแถว"* ซึ่งคือหายนะที่ข้อบนกันไว้
 * ⇒ กันซ้ำที่นี่ด้วย **ไม่ใช่เพราะไม่เชื่อผู้เรียก แต่เพราะราคาของการพลาดคือทริปที่กู้ไม่ได้**
 */
export function deleteTripDestinationsExcept(db: Db, tripId: string, keepCityIds: readonly string[]) {
  if (keepCityIds.length === 0) {
    throw new Error("deleteTripDestinationsExcept: keepCityIds ว่างไม่ได้ — จะกลายเป็นลบทั้งหมด");
  }
  return engineTable(db, "trip_destinations")
    .delete()
    .eq("trip_id", tripId)
    .not("city_id", "in", `(${keepCityIds.join(",")})`)
    .select("city_id");
}

export function tripsVisibleToMe(db: Db) {
  // 🔴 **`title` ไม่ใช่ `name`** — แก้ 27 ส.ค. 2026 (P4 เจอตอนสร้าง harness ยิง route จริง)
  //    คอลัมน์ชื่อ `title` มาตั้งแต่ `…043822_identity.sql:122` และ `create_trip` ก็ insert `title`
  //    ผมเขียน `name` ไว้ตั้งแต่แรก → **`GET /api/engine/trips` คืน `502` ให้ทุกผู้ใช้ ทุกครั้ง**
  //
  // 🎯 **ไม่มีเทสต์ไหนจับได้เลย และเหตุผลคือรูปของชุดทดสอบ ไม่ใช่ความประมาท**
  //    `rlsMatrix` ทดสอบสิทธิ์ที่ **ชั้นตาราง** — มันยิง `.from("trips")` เองด้วยชื่อคอลัมน์ของมันเอง
  //    **ไม่เคยเรียก `tripsForUser()`** · helper กับเทสต์จึงเห็นสคีมาคนละใบโดยไม่มีอะไรเทียบให้
  //    → นี่คือเหตุผลที่ `E3-AC9` ② ต้องยิง *route จริง* ไม่ใช่ทดสอบ db helper แยก
  // 🔴 **`start_date`/`end_date` เพิ่ม 27 ส.ค. 2026 — P2 flag ตอนทำ `TripHeader`**
  //    หัวหน้าจอเคยฝังช่วงวันที่ตายตัว (`"11–21 ต.ค. · เที่ยวเกาหลี 12–20"`) · P2 **ลบทิ้งแทนที่จะเดาใหม่**
  //    เพราะ helper นี้ไม่ได้ส่งวันที่มาให้ ทั้งที่ฐานมีคอลัมน์อยู่แล้ว — **ลบดีกว่าเดา**
  // ⚠️ `revoke` ที่ `20260825122247` ถอนแค่ `insert`/`update` · **`select` ระดับตารางยังอยู่** (ตรวจแล้ว)
  // 🔴 **เพิ่ม 27 ส.ค. บ่าย — จุดหมาย + รูปปก + จำนวนสมาชิก สำหรับการ์ดบนหน้า Home**
  //    ฝัง 3 ชั้น: `trip_destinations` → `catalog_cities` → `catalog_countries`
  //    ทุกชั้นมี policy `select` ของตัวเองอยู่แล้ว · **ไม่มี `where` เรื่องสิทธิ์ที่นี่สักตัว** (`P-15`)
  //    · `trip_members(count)` = จำนวนสมาชิก โดยไม่ต้องดึงรายชื่อมาทั้งชุด
  //    ⚠️ **ยังไม่เรียงที่นี่** — `rank` เรียงใน `tripsForUser()` เพราะรายการมี 2-5 ใบ
  //       การเรียงในฝั่ง PostgREST ต้องพึ่ง `referencedTable` ซึ่งพังเงียบถ้าชื่อความสัมพันธ์เปลี่ยน
  return engineTable(db, "trips")
    .select(
      // 🔴 **`lat`/`lng` เพิ่ม 2 ก.ย. 2026 — `E2-AC16` / `D54`** · เมืองถือพิกัดของตัวเอง **ห้ามเฉลี่ยจากลูก**
      //    `catalog_cities.lat`/`lng` เป็น `not null` (`…catalog_geo…sql:95-96`) → ทุกแถวมีพิกัดแน่นอน
      //    ⚠️ **ไม่ใช่ฟิลด์เสริม** — `cityCenter()` เดิมเฉลี่ยจาก `PLACES` → เมืองที่มีสถานที่ 0 แห่งได้ `NaN`
      //    และมันเจ็บเงียบ (แผนที่ซูมผิด · ระยะข้ามเมืองผิด · Copilot รายงานตัวเลขนั้นอย่างมั่นใจ)
      "id, title, start_date, end_date,trip_destinations(rank, catalog_cities(id, legacy_slug, name_th, name_en, lat, lng, catalog_countries(id, name_th, name_en))),trip_members(count)",
    )
    .order("created_at");
}

/**
 * แถวคลังสถานที่ของทริป **พร้อมชื่อทุกภาษาและ slug ของเมือง ในคำขอเดียว**
 *
 * 🔴 **join ที่นี่ ไม่ใช่ยิงทีละใบจากชั้นบน** — คลังของทริปหนึ่งมีได้หลายสิบแถว
 * ยิงชื่อทีละแถวคือ N+1 ที่จะไม่มีใครสังเกตจนกว่าทริปจะใหญ่
 * · การแปลงรูปอยู่ที่ [`customPlaces.ts`](./customPlaces.ts) — ที่นี่มีแต่ *รูปคิวรี*
 */
export function customPlaceRowsOfTrip(db: Db, tripId: string) {
  return engineTable(db, "custom_places")
    // 🔴 `country_id` (ใน `catalog_cities` ที่ฝังมา) เพิ่ม 27 ส.ค. 2026 (`E4-AC3`/`AC4`)
    //    **ประเทศผูกกับ *เมือง* ไม่ใช่ *ทริป*** — `trips` ไม่มีคอลัมน์ประเทศเลย (มี `base_timezone`)
    //    และนั่นถูก: ทริปปัจจุบันข้ามเวียดนาม→เกาหลีอยู่แล้ว · **ทริปเดียวมีได้หลายประเทศ**
    //    → UI ต้องเลือกปุ่มแผนที่จากเมืองของสถานที่นั้น ไม่ใช่จากทริป
    //
    // ⚠️ **คอมเมนต์ต้องอยู่เหนือสตริง ห้ามคั่นกลางด้วย `+`** (28 ส.ค. 2026)
    //    ต่อสตริงด้วย `+` ทำให้ TypeScript ไม่ยุบเป็น literal → supabase-js คืน `GenericStringError`
    //    → **อ่านฟิลด์อะไรก็ได้จากผลลัพธ์โดยไม่มีใครจับ** · สตริงเดียวได้ `{ id: any; … }` ที่จำกัดฟิลด์จริง
    .select(
      "id, city_id, category, lat, lng, maps_query, google_place_id, legacy_added_by, created_at, catalog_cities(legacy_slug, country_id), custom_place_names(locale, name, priority), custom_place_descriptions(locale, description)",
    )
    .eq("trip_id", tripId)
    .order("created_at");
}

/**
 * สร้างสถานที่ในคลังของทริป — **หนึ่งคำขอ หนึ่งทรานแซกชัน**
 *
 * 🔴 หนึ่งสถานที่ = 1 แถวใน `custom_places` + N แถวใน `custom_place_names`
 * เขียนทีละคำสั่งแล้วล้มกลางคัน = **สถานที่ที่ไม่มีชื่อ ซึ่งไม่พังอะไรเลย มันแค่เป็นการ์ดเปล่า**
 * · RPC เป็น `security invoker` → **ไม่ได้ให้สิทธิ์ใครเพิ่ม ให้แค่ทรานแซกชัน** (`D38`)
 */
export function createCustomPlace(
  db: Db,
  input: {
    tripId: string;
    citySlug: string;
    category: string;
    lat: number;
    lng: number;
    mapsQuery: string;
    nameTh: string;
    nameEn?: string | null;
    nameKo?: string | null;
    description?: string | null;
    googlePlaceId?: string | null;
    legacyAddedBy?: string | null;
  }
) {
  return db.rpc("create_custom_place", {
    p_trip_id: input.tripId,
    p_city_slug: input.citySlug,
    p_category: input.category,
    p_lat: input.lat,
    p_lng: input.lng,
    p_maps_query: input.mapsQuery,
    p_name_th: input.nameTh,
    p_name_en: input.nameEn ?? undefined,
    p_name_ko: input.nameKo ?? undefined,
    p_description: input.description ?? undefined,
    p_google_place_id: input.googlePlaceId ?? undefined,
    p_legacy_added_by: input.legacyAddedBy ?? undefined,
  });
}

// ───────────────────────────────────────────────────────────────────────────
// วันของทริป — `E3` · `D80` (ความตั้งใจเรื่องที่นอนอยู่บน `trip_days`)
// ───────────────────────────────────────────────────────────────────────────

/**
 * วันทั้งหมดของทริป **พร้อม slug ของเมืองที่ตั้งใจนอน ในคำขอเดียว**
 *
 * 🔴 `order by date` ไม่ใช่เพื่อความสวยงาม — [`dayBridge.ts`](./dayBridge.ts) จับคู่ด้วย `date`
 * และผลที่ลำดับไม่แน่นอนทำให้ *ตัวไหนชนะตอนวันที่ซ้ำ* เปลี่ยนไปมาระหว่างเครื่อง
 */
export function tripDaysOfTrip(db: Db, tripId: string) {
  return engineTable(db, "trip_days")
    // 🔴 ฝัง `catalog_cities` **สองเส้น** — `city_id` (วันนั้นอยู่เมืองไหน) กับ `overnight_city_id` (นอนที่ไหน)
    //    เป็นคนละคำถาม และ `E5` ต้องใช้ทั้งคู่ · ไม่ระบุเส้น = `PGRST201` (ยิงยืนยันแล้ว 28 ส.ค.)
    // ⚠️ ระบุด้วยชื่อ **คอลัมน์** ตามกติกาที่ตั้งไว้ที่ `membersOfTrip` — ทนต่อการเปลี่ยนชื่อ constraint
    //    · คีย์ `catalog_cities` (ไม่ใส่ alias) คือของ *overnight* เพราะ `DayOvernightRow` เดิมอ่านชื่อนั้น
    //      **เปลี่ยนชื่อคีย์ = ทำ UI ที่ใช้อยู่พัง** ของใหม่จึงเป็น alias `city` ล้วน ๆ
    .select("id, date, city_id, overnight_kind, overnight_city_id, city:catalog_cities!city_id(id, legacy_slug, name_th, name_en), catalog_cities!overnight_city_id(legacy_slug)")
    .eq("trip_id", tripId)
    .order("date");
}

// ───────────────────────────────────────────────────────────────────────────
// แก้ช่วงวันของทริป — `E5` · P1-Lead · 4 ก.ย. 2026
// สิทธิ์มาจาก `20260904120000_e5_trip_dates_editable.sql`
// ───────────────────────────────────────────────────────────────────────────

/** `id` + `date` ล้วน ๆ — เบากว่า `tripDaysOfTrip` มาก และการซิงก์ช่วงวันไม่ต้องรู้เรื่องเมืองเลย */
export function tripDayDatesOf(db: Db, tripId: string) {
  return engineTable(db, "trip_days").select("id, date").eq("trip_id", tripId).order("date");
}

/**
 * นับจุดแวะของวันที่กำลังจะถูกถอน — **ตัวที่ทำให้ `409` มีข้อมูลจริง ไม่ใช่คำเตือนลอย ๆ**
 *
 * 🔴 นับ *ทุกแผน* ของทริปโดยตั้งใจ ไม่ใช่แค่แผนที่ active
 * ผู้ใช้ที่มีแผน B ค้างอยู่จะเสียของในแผนนั้นเหมือนกัน **และเขามองไม่เห็นมันบนจอตอนกดย่อวัน**
 * ⇒ ถามคำถามที่ตรงกับสิ่งที่จะหายจริง ไม่ใช่คำถามที่ตอบง่าย
 */
export function stopCountInDays(db: Db, dayIds: readonly string[]) {
  return engineTable(db, "trip_stops")
    .select("id, trip_day_id")
    .in("trip_day_id", dayIds as string[])
    // 🔴 **`.is("deleted_at", null)` — ขาดไปในฉบับแรก และ P4 จับได้ (4 ก.ย. 2026)**
    //    `trip_stops` เป็น **soft delete** (`D76`) ⇒ ไม่กรอง = **นับ tombstone ของจุดแวะที่ผู้ใช้ลบไปแล้ว**
    //    ```
    //    ผู้ใช้ลบจุดแวะของวัน 20–21 ออกหมด → ย่อทริป 21 → 19
    //    → 409 "จะเสียจุดแวะ 4 จุด"  ทั้งที่ไม่มีจุดแวะเหลืออยู่เลยสักจุด
    //    ```
    //    🎯 ***`409` ที่ควรเป็นด่านกันข้อมูลหาย กลายเป็นด่านที่บล็อกการกระทำที่ไม่ทำให้อะไรหายเลย***
    //    · และผลที่แพงกว่าตัวเลขผิด: **ผู้ใช้จะเรียนรู้ว่าต้องกด `force` ทุกครั้ง ⇒ ด่านตายทั้งใบ**
    //      (`§3.4` — ด่านที่แดงใส่คนที่ทำถูก จะถูกลบ และของที่มันเคยกันไว้ก็หายไปด้วย)
    .is("deleted_at", null);
}

/** ช่วงวันของทริป — `trips_update` (owner เท่านั้น) เป็นคนตัดสินสิทธิ์ ไม่มีการกรองที่นี่ (`P-15`) */
export function updateTripDates(db: Db, tripId: string, startDate: string, endDate: string) {
  return engineTable(db, "trips")
    .update({ start_date: startDate, end_date: endDate })
    .eq("id", tripId)
    .select("id");
}

/** เพิ่มวันที่ยังไม่มี · `date` เป็น `YYYY-MM-DD` — `timezone` ปล่อย null ตาม `D37` (ใช้ของทริป) */
export function insertTripDays(db: Db, tripId: string, dates: readonly string[]) {
  return engineTable(db, "trip_days")
    .insert(dates.map((date) => ({ trip_id: tripId, date })))
    .select("id, date");
}

/** ถอนวันตาม id ที่ผู้เรียกคำนวณมาแล้ว — **ไม่รับช่วงวันที่** เพราะการตัดสินว่าใบไหนเกินอยู่ที่ route */
export function deleteTripDaysByIds(db: Db, tripId: string, dayIds: readonly string[]) {
  return engineTable(db, "trip_days")
    .delete()
    .eq("trip_id", tripId)
    .in("id", dayIds as string[])
    .select("id");
}

/**
 * ตั้ง *ความตั้งใจ* เรื่องที่นอนของวันหนึ่ง — `D80`
 *
 * 🔴 **`overnight_kind` กับ `overnight_city_id` ต้องเขียนพร้อมกันเสมอ**
 * `trip_days_overnight_consistent` บังคับให้ทั้งคู่สอดคล้องกัน → เขียนทีละตัวจะชน `check`
 * · `null` = **ยังไม่ตัดสิน** ซึ่งต่างจาก `'none'` (ตั้งใจไม่นอนโรงแรม) — `D80` ห้ามยุบสองอันนี้
 */
/** สามสถานะของ "ที่นอน" — `D80` · แยกชนิดออกมาเพื่อให้ผู้เรียกอ้างได้โดย**ไม่ผูกกับตำแหน่งพารามิเตอร์**
 *  (เดิมใช้ `Parameters<typeof setOvernightIntent>[2]` แล้วพังทันทีที่แทรกพารามิเตอร์ — `tsc` จับได้ 28 ส.ค.) */
export type OvernightIntent =
  | { kind: "city"; cityId: string }
  | { kind: "none" }
  | { kind: "undecided" };

export function setOvernightIntent(db: Db, tripId: string, dayId: string, intent: OvernightIntent) {
  const patch =
    intent.kind === "city"
      ? { overnight_kind: "city", overnight_city_id: intent.cityId }
      : intent.kind === "none"
        ? { overnight_kind: "none", overnight_city_id: null }
        : { overnight_kind: null, overnight_city_id: null };
  // `.select()` เพื่อให้ `writeGuard` เห็นว่าแตะกี่แถว — 0 แถว = RLS กรองออก ไม่ใช่สำเร็จ
  return engineTable(db, "trip_days").update(patch).eq("trip_id", tripId).eq("id", dayId).select("id");
}

/** หา `city_id` จาก slug เดิม — `null` = ไม่รู้จักเมืองนั้น
 *  🔴 รวม `country_id` มาด้วยเสมอ (`E5` — P1 ขอให้ P3 ส่ง `country` กลับหลังเขียนที่พัก) — เพิ่ม
 *  แบบ additive ล้วน ผู้เรียกเดิม 2 จุด (`hotels`/`days` route) ยังอ่านแค่ `.id` เหมือนเดิมได้ */
/**
 * ตั้ง *เมืองที่วันนั้นอยู่* — คนละเรื่องกับ "นอนที่ไหน" (`setOvernightIntent`)
 *
 * 🔴 **`null` = ล้างค่า ไม่ใช่ "ไม่เปลี่ยน"** — ผู้ใช้ตัดสิน 28 ส.ค. 2026 ว่า
 *    **ไม่ให้ระบบเดาเมืองให้วันตอนสร้างทริป** (`create_trip` จึงยังใส่ `null` เหมือนเดิม ไม่ถูกแตะ)
 *    → เส้นนี้เป็น **ทางเดียว** ที่ `trip_days.city_id` จะมีค่า · ล้างกลับเป็นว่างต้องทำได้ด้วย
 * · `.select("id")` เพื่อให้แยก "RLS กรองออก" (0 แถว) ออกจาก "สำเร็จ" — แบบเดียวกับ `setOvernightIntent`
 */
export function setDayCity(db: Db, tripId: string, dayId: string, cityId: string | null) {
  return engineTable(db, "trip_days")
    .update({ city_id: cityId })
    .eq("trip_id", tripId)
    .eq("id", dayId)
    .select("id");
}

export function cityIdBySlug(db: Db, slug: string) {
  return engineTable(db, "catalog_cities").select("id, country_id").eq("legacy_slug", slug).maybeSingle();
}

/**
 * เมืองนี้มีอยู่จริงไหม — **ใช้แยก "เมืองไม่มี" ออกจาก "เมืองนี้ไม่มีสถานที่" เท่านั้น** (P2 ชี้ · 28 ส.ค. 2026)
 *
 * 🔴 `GET /api/engine/places` คืน `200 []` ทั้งสองกรณี — **แยกไม่ออกจาก response**
 * · วันนี้ยังไม่กัดเพราะ `cityId` มาจาก `/api/engine/cities` ของเราเองเสมอ
 * · 🎯 **แต่ `B6` จะส่ง `cityId` มาจากวันในทริป** — วันที่ชี้เมืองที่ถูกลบออกจากคลัง
 *   จะได้ไซด์บาร์ว่างเปล่าเงียบ ๆ แทนที่จะได้ error · ผู้ใช้อ่านว่า "เมืองนี้ไม่มีที่เที่ยว"
 *
 * ⚠️ **เรียกเฉพาะตอนผลว่างเท่านั้น** — จ่ายค่าคิวรีที่สองเมื่อความกำกวมเกิดขึ้นจริง
 * ไม่ใช่ทุกคำขอ · เส้นทางปกติ (มีสถานที่) ยังยิงครั้งเดียวเหมือนเดิม
 */
export function catalogCityExists(db: Db, cityId: string) {
  return engineTable(db, "catalog_cities").select("id").eq("id", cityId).maybeSingle();
}

/**
 * เมืองหลายใบในคำขอเดียว — คืน **เฉพาะ id ที่มีจริง** ให้ผู้เรียกเทียบเอง
 *
 * 🔴 **ไม่คืน `boolean`** เพราะคำถามที่ผู้เรียกต้องตอบคือ *"ใบไหนไม่มี"* ไม่ใช่ *"ครบไหม"*
 * ผู้ใช้ที่ส่ง 5 เมืองแล้วผิด 1 ใบ ควรได้รู้ว่าใบไหน ไม่ใช่ "มีเมืองที่ไม่รู้จัก"
 */
export function catalogCitiesThatExist(db: Db, cityIds: readonly string[]) {
  return engineTable(db, "catalog_cities").select("id").in("id", cityIds as string[]);
}

// ───────────────────────────────────────────────────────────────────────────
// สถานที่ที่ซ่อนไว้ — `E3`
// ───────────────────────────────────────────────────────────────────────────

/**
 * สถานที่ที่ซ่อนของทริป **พร้อม slug เดิม** — UI อ้างสถานที่ด้วย slug ไม่ใช่ `uuid`
 *
 * 🔴 **การแปลง slug ⇄ uuid อยู่ *ฝั่งเซิร์ฟเวอร์* ไม่ใช่ฝั่งไคลเอนต์**
 * ต่างจากสะพานวัน (`dayBridge`) ที่ต้องอยู่ฝั่ง client เพราะ `"d0"` มีอยู่แต่ในไฟล์ TS
 * · **`legacy_slug` อยู่ในฐาน** → เซิร์ฟเวอร์แปลงได้เอง และไคลเอนต์ไม่ต้องรู้จัก `uuid` เลย
 * 🎯 **เลือกฝั่งตาม *ข้อมูลอยู่ที่ไหน* ไม่ใช่ตามความเคยชิน** — ผิดฝั่งแล้วต้องส่ง uuid ไปกลับโดยไม่มีเหตุผล
 */
export function hiddenPlacesOfTrip(db: Db, tripId: string) {
  return engineTable(db, "hidden_places")
    .select("hidden_at, legacy_hidden_by, catalog_places(legacy_slug)")
    .eq("trip_id", tripId);
}

/** ซ่อนสถานที่ — รับ slug แล้วให้ฐานหา `uuid` เอง ผ่านคิวรีซ้อน */
export function hidePlaceBySlug(db: Db, tripId: string, placeId: string, legacyHiddenBy: string | null) {
  return engineTable(db, "hidden_places")
    .insert({ trip_id: tripId, catalog_place_id: placeId, legacy_hidden_by: legacyHiddenBy })
    // `hidden_at` มาจาก `default now()` ฝั่งฐาน — ดึงกลับมาด้วย ไม่ให้ไคลเอนต์ปั้นเอง (`D7`)
    .select("catalog_place_id, hidden_at");
}

export function unhidePlace(db: Db, tripId: string, placeId: string) {
  return engineTable(db, "hidden_places")
    .delete()
    .eq("trip_id", tripId)
    .eq("catalog_place_id", placeId)
    .select("catalog_place_id");
}

/** `legacy_slug` → `catalog_places.id` · `null` = คลังไม่รู้จัก slug นั้น */
export function catalogPlaceIdBySlug(db: Db, slug: string) {
  return engineTable(db, "catalog_places").select("id").eq("legacy_slug", slug).maybeSingle();
}

// ───────────────────────────────────────────────────────────────────────────
// โน้ต/รูปที่ฝากไว้กับสถานที่ — `E3`
// ───────────────────────────────────────────────────────────────────────────

/**
 * โน้ตของแผนหนึ่ง **พร้อม slug ของสถานที่ทั้งสองชนิด**
 *
 * 🔴 `place_notes` ชี้สถานที่ได้**สองทาง** (`catalog_place_id` หรือ `custom_place_id`)
 * โดยมี `place_notes_one_place` บังคับว่าต้องมีทางเดียว
 * → ต้อง join **ทั้งสองฝั่ง** แล้วเลือกอันที่ไม่ใช่ `null` · **join ฝั่งเดียวจะทำให้โน้ตของ
 *   สถานที่ที่ผู้ใช้เพิ่มเองหายไปเงียบ ๆ** ซึ่งเป็นครึ่งหนึ่งของโน้ตทั้งหมดในทริปจริง
 */
export function placeNotesOfPlan(db: Db, tripId: string, planId: string) {
  return engineTable(db, "place_notes")
    .select("note, photo_path, updated_at, catalog_places(legacy_slug), custom_places(id)")
    .eq("trip_id", tripId)
    .eq("plan_id", planId)
    .is("deleted_at", null);
}

/** เขียนโน้ต — ผู้เรียกต้องระบุแล้วว่าเป็นสถานที่ชนิดไหน (คลังกลาง vs ของทริป) */
/**
 * 🔴 **ไม่ใช้ `.upsert()` — ตัวที่สองของวัน · คนละกลไกกับ `upsertDaySettings` แต่ตระกูลเดียวกัน** (P4 พบ)
 *
 * ## อาการ: **เจ้าของทริป PUT โน้ตของตัวเองก็ได้ `502`**
 * ```
 * 42P10 there is no unique or exclusion constraint matching the ON CONFLICT specification
 * ```
 *
 * ## เหตุ
 * ดัชนีกันซ้ำของตารางนี้เป็น **partial** (`20260826132428`):
 * ```sql
 * create unique index place_notes_one_per_catalog_place
 *   on public.place_notes (plan_id, catalog_place_id)
 *   where catalog_place_id is not null;      -- ← predicate
 * ```
 * `ON CONFLICT (cols)` จะ match ดัชนี partial ได้ **ก็ต่อเมื่อมี `WHERE` ที่ตรงกันด้วย**
 * แต่ PostgREST `.upsert()` ส่งแค่**รายชื่อคอลัมน์** ไม่ส่ง predicate → planner หาไม่เจอ → **ล้มทุกคน**
 * · เป็นความล้มเหลวตอน *วางแผน* → ไม่เกี่ยวกับ RLS/fixture · **เจ้าของก็โดน**
 *
 * ## 🎯 ทางที่ **ไม่** เลือก: ทำให้ดัชนีเลิก partial
 * migration เขียนเหตุผลไว้เอง: *"**ไม่ partial ตาม `deleted_at`** — tombstone ต้องกันที่ของมันไว้
 * ไม่งั้นลบโน้ตแล้วเขียนใหม่จะได้สองแถว แล้ว `D76` จะทำให้มันค้างตลอดไป"*
 * · และ predicate `is not null` ก็จำเป็น เพราะแถวหนึ่งผูกกับ *คลัง* หรือ *สถานที่ของทริป* อย่างใดอย่างหนึ่ง
 * 🔴 **แก้ดัชนีให้เข้ากับ `.upsert()` = เปลี่ยนโครงข้อมูลเพื่อให้เข้ากับไลบรารี — กลับหัวกลับหาง**
 *
 * ## ทางที่เลือก: `update` → `insert` เฉพาะที่ยังไม่มี — **รูปเดียวกับ `upsertDaySettings`**
 * · `update` ส่งเฉพาะ `note`/`photo_path` ซึ่งเป็น **ทั้งหมดที่ `authenticated` มีสิทธิ์เขียน**
 * · ชน `23505` → แก้ทับ (อีกเครื่องสร้างแทรกระหว่างสองจังหวะ)
 *
 * ## 🔴 `.is("deleted_at", null)` — **แก้ 27 ส.ค. 2026 หลัง P4 วัดได้ `403` แล้วผมไล่ต่อ**
 *
 * ฉบับแรกของการแก้นี้ (`04b7171`) ผม `update` โดย**ไม่กรอง `deleted_at`** เพราะคิดว่า
 * *"พฤติกรรมเหมือน `.upsert()` เดิมทุกประการ"* · **ผิด และผลของมันคือเคสที่แย่ที่สุดชนิดหนึ่ง:**
 *
 * ```
 * ① update (ไม่กรอง) → policy `place_notes_update` ไม่มีเงื่อนไข deleted_at → **เขียนทับ tombstone สำเร็จ**
 * ② .select(...)      → policy `place_notes_select` มี `and deleted_at is null` → **คืน 0 แถว**
 * ③ เห็น 0 แถว → insert → ชนดัชนี (tombstone กินที่อยู่) → 23505
 * ④ retry update      → **เขียนอีกครั้ง** → คืน 0 แถวอีก
 * ⑤ route เห็น 0 แถว → 403 "ไม่มีสิทธิ์เขียนโน้ตในแผนนี้"
 * ```
 * 🔴 **ผู้ใช้ได้ `403` · แต่ข้อมูลถูกเขียนไปแล้วสองรอบ** — คำตอบว่า "ถูกปฏิเสธ" ที่มีการเขียนสำเร็จอยู่ข้างหลัง
 * 🎯 **และมันเป็นของใหม่ที่ *การแก้ของผมเอง* สร้างขึ้น** — ฉบับ `.upsert()` ล้มด้วย `42P10` ตั้งแต่ตอนวางแผน
 *    **ยังไม่ทันเขียนอะไรเลย** · ผมเขียนในคอมมิตก่อนว่า *"เหมือนเดิมทุกประการ"* โดยไม่ได้ไล่ policy ทั้งสองใบ
 *
 * **กรอง `deleted_at is null` แล้ว: ไม่มีการเขียนเกิดขึ้นเลยในเส้นทาง tombstone** — ยังได้ `403` เหมือนเดิม
 * แต่เป็น `403` ที่**ซื่อสัตย์** · อาการที่ผู้ใช้เห็นไม่เปลี่ยน **สิ่งที่เปลี่ยนคือฐานไม่ถูกแตะแล้ว**
 *
 * ⚠️ **บั๊กที่ยังเปิดอยู่ (ไม่ใช่ของใหม่): ลบโน้ตแล้วจดใหม่ที่เดิมไม่ได้เลย** — tombstone กินที่ในดัชนี
 * ต้องตัดสินก่อนว่า *"เขียนใหม่ควรปลุกแถวเดิมไหม"* และไคลเอนต์เขียน `deleted_at` ไม่ได้ตามการออกแบบ
 * · P4 มี xfail ตรึงไว้แล้ว (`449dc9e`) → **วันที่แก้ มันจะแดงเองและประกาศว่าแก้แล้ว**
 */
export async function upsertPlaceNote(
  db: Db,
  // 🔴 **ต้องมี `catalogPlaceId` หรือ `customPlaceId` อย่างใดอย่างหนึ่ง — บังคับที่ชนิด ไม่ใช่ที่ runtime**
  //    (28 ส.ค. 2026 · เจอตอนใส่ generic `Database` ให้ `Db` — ไม่ใช่เจอด้วยเทสต์)
  //
  //    เดิมทั้งคู่เป็น optional → `keyVal` เป็น `string | null` → `.eq(keyCol, null)`
  //    ซึ่ง PostgREST แปลเป็น `col=eq.null` = SQL `col = NULL` **ไม่มีวันเป็นจริงสักแถว**
  //    → update ไม่โดนอะไร → insert → ฐานปฏิเสธด้วย `place_notes_one_place`
  //    ผู้ใช้ได้ `502` พร้อมข้อความ constraint ดิบ หลังเสีย round trip สองรอบ
  //
  // 🎯 **เลือกบังคับที่ชนิดแทนด่าน runtime เพราะมันทำให้สถานะที่ผิด *แทนไม่ได้***
  //    ด่าน runtime ต้องมีคนเขียนเทสต์มาพิสูจน์ว่ามันทำงาน · ชนิดพิสูจน์ตัวเองตอนคอมไพล์
  //    · ฐานก็กันอยู่แล้ว (`check (num_nonnulls(catalog_place_id, custom_place_id) = 1)`)
  //      → สามชั้นตรงกัน: **ชนิด · โค้ด · ฐาน** ไม่มีชั้นไหนพูดคนละเรื่อง
  row: {
    tripId: string;
    planId: string;
    note: string | null;
    photoPath: string | null;
    legacyAddedBy?: string | null;
  } & (
    | { catalogPlaceId: string; customPlaceId?: null }
    | { catalogPlaceId?: null; customPlaceId: string }
  )
) {
  const keyCol = row.catalogPlaceId ? "catalog_place_id" : "custom_place_id";
  const keyVal = row.catalogPlaceId ?? row.customPlaceId;   // `string` โดยโครงสร้าง — ดูลายเซ็น
  const patch = { note: row.note, photo_path: row.photoPath };

  const upd = await engineTable(db, "place_notes")
    .update(patch)
    .eq("plan_id", row.planId)
    .eq(keyCol, keyVal)
    // 🔴 กันไม่ให้เขียนทับ tombstone — `place_notes_update` ไม่มีเงื่อนไขนี้ แต่ `place_notes_select` มี
    //    → ถ้าไม่กรอง จะ **เขียนสำเร็จแล้วคืน 0 แถว** ซึ่งกลายเป็น `403` ที่มีการเขียนอยู่ข้างหลัง
    .is("deleted_at", null)
    .select("id, updated_at");
  if (upd.error) return upd;
  if (upd.data && upd.data.length > 0) return upd;

  const ins = await engineTable(db, "place_notes")
    .insert({
      trip_id: row.tripId,
      plan_id: row.planId,
      catalog_place_id: row.catalogPlaceId ?? null,
      custom_place_id: row.customPlaceId ?? null,
      note: row.note,
      photo_path: row.photoPath,
      legacy_added_by: row.legacyAddedBy ?? null,
    })
    // 🔴 `updated_at` ต้องเดินทางกลับไปถึงไคลเอนต์ (`D7`) — ไม่งั้นฝั่งนั้นจะปั้นเวลาจากนาฬิกาตัวเอง
    .select("id, updated_at");
  if (!ins.error) return ins;
  if (ins.error.code !== "23505") return ins;

  return engineTable(db, "place_notes")
    .update(patch)
    .eq("plan_id", row.planId)
    .eq(keyCol, keyVal)
    .is("deleted_at", null)     // เหตุผลเดียวกับข้างบน
    .select("id, updated_at");
}

/**
 * ลบโน้ต — **ผ่าน RPC `soft_delete_place_note` เท่านั้น** (`E2-AC12`)
 *
 * 🔴 ไคลเอนต์ถูกถอด `update` สิทธิ์เขียน `deleted_at` ออกไปแล้ว → **ลบเองไม่ได้ตามการออกแบบ**
 * ต้องหา `id` ของโน้ตก่อน เพราะ RPC รับ `id` ไม่ใช่ `(plan_id, place_id)`
 */
export function placeNoteId(db: Db, tripId: string, planId: string, place: { catalogId?: string | null; customId?: string | null }) {
  let q = engineTable(db, "place_notes")
    .select("id")
    .eq("trip_id", tripId)
    .eq("plan_id", planId)
    .is("deleted_at", null);
  q = place.catalogId
    ? q.eq("catalog_place_id", place.catalogId)
    : q.eq("custom_place_id", place.customId ?? "");
  return q.maybeSingle();
}

export function softDeletePlaceNote(db: Db, id: string) {
  return db.rpc("soft_delete_place_note", { p_id: id });
}

// ───────────────────────────────────────────────────────────────────────────
// ที่พัก — `E3` · `D51` (ไม่มี `leg_id` · ใช้ช่วงวันที่ของตัวเอง)
// ───────────────────────────────────────────────────────────────────────────

/**
 * ที่พักของทริป **พร้อม slug ของเมือง**
 *
 * 🔴 **ไม่มี `leg_id` ในสคีมาใหม่ตามที่ `D51` ตัดสิน** — `leg` เป็น *ค่าคำนวณจาก `trip_days`*
 * ที่พักเก็บ `check_in`/`check_out` ของตัวเอง · *"คืนนี้นอนที่ไหน"* = แถวที่ `check_in <= วันนั้น < check_out`
 * · `trip_hotels_no_overlap` (exclusion ด้วย `gist`) บังคับว่าช่วงวันซ้อนกันไม่ได้ **ฐานจึงกันคำตอบกำกวมให้เอง**
 */
export function tripHotelsOfTrip(db: Db, tripId: string) {
  return engineTable(db, "trip_hotels")
    // `country_id` (ใน `catalog_cities` ที่ฝังมา) — ดูเหตุผลที่ `customPlaceRowsOfTrip`
    // ประเทศมาจากเมือง ไม่ใช่ทริป · ⚠️ สตริงเดียว ห้ามคั่นด้วย `+` (ดูเหตุผลที่ฟังก์ชันเดียวกันนั้น)
    .select(
      "id, city_id, hotel_name, formatted_address, name_local, address_local, name_en, address_en, phone, lat, lng, check_in, check_out, updated_at, catalog_cities(legacy_slug, country_id)",
    )
    .eq("trip_id", tripId)
    .is("deleted_at", null)
    .order("check_in");
}

/**
 * บันทึกที่พักของช่วงวันหนึ่ง
 *
 * 🔴 **`onConflict` ใช้ `(trip_id, check_in)` ไม่ได้ — ไม่มี unique ตัวนั้น**
 * ที่กันการซ้อนคือ **exclusion constraint** ซึ่ง `upsert` ใช้ไม่ได้
 * → ต้อง **ลบช่วงเดิมก่อนแล้วค่อยเขียนใหม่** ในคำสั่งของผู้เรียก · ที่นี่ทำแค่ `insert`
 */
export function insertTripHotel(db: Db, row: InsertRow<"trip_hotels">) {
  // `updated_at` มาจาก trigger ฝั่งฐาน — ดึงกลับมาด้วย ไม่ให้ไคลเอนต์ปั้นเอง (`D7`)
  return engineTable(db, "trip_hotels").insert(row).select("id, updated_at");
}

/** หาแถวที่ครอบช่วงวันนั้นพอดี — `null` = ยังไม่มีที่พักของช่วงนี้ */
export function tripHotelByRange(db: Db, tripId: string, checkIn: string, checkOut: string) {
  return engineTable(db, "trip_hotels")
    .select("id")
    .eq("trip_id", tripId)
    .eq("check_in", checkIn)
    .eq("check_out", checkOut)
    .is("deleted_at", null)
    .maybeSingle();
}

export function softDeleteTripHotel(db: Db, id: string) {
  return db.rpc("soft_delete_trip_hotel", { p_id: id });
}

// ───────────────────────────────────────────────────────────────────────────
// ของที่ต้องเตรียม — `E3`
// ───────────────────────────────────────────────────────────────────────────

export function checklistOfTrip(db: Db, tripId: string) {
  return engineTable(db, "checklist_items")
    .select("id, text, category, is_checked, legacy_checked_by, legacy_added_by, created_at, updated_at")
    .eq("trip_id", tripId)
    .is("deleted_at", null)
    .order("created_at");
}

/**
 * เพิ่มรายการ — 🔴 **`id` ไม่อยู่ใน grant** ฐานเป็นคนออกให้
 * · `legacy_checked_by` ก็ไม่ต้องส่ง — **trigger `stamp_checked_by` เป็นคนเขียน** (`P-56`)
 */
export function insertChecklistItem(db: Db, row: { tripId: string; text: string; category: string; legacyAddedBy: string | null }) {
  return engineTable(db, "checklist_items")
    .insert({ trip_id: row.tripId, text: row.text, category: row.category, legacy_added_by: row.legacyAddedBy })
    .select("id, text, category, is_checked, legacy_checked_by, legacy_added_by, created_at, updated_at")
    .single();
}

/** แก้รายการ — grant เปิดแค่ `text` · `category` · `is_checked` เท่านั้น */
export function updateChecklistItem(db: Db, id: string, patch: UpdateRow<"checklist_items">) {
  // `updated_at` มาจาก trigger ฝั่งฐาน — ดึงกลับมาด้วย ไม่ให้ไคลเอนต์ปั้นเอง (`D7`)
  return engineTable(db, "checklist_items").update(patch).eq("id", id).select("id, updated_at");
}

export function softDeleteChecklistItem(db: Db, id: string) {
  return db.rpc("soft_delete_checklist_item", { p_id: id });
}

// ───────────────────────────────────────────────────────────────────────────
// ตั้งค่ารายวันของแผน — `E3` · `D69` (เป็นของ *วัน × แผน* ไม่ใช่ของวัน)
// ───────────────────────────────────────────────────────────────────────────

export function daySettingsOfPlan(db: Db, tripId: string, planId: string) {
  return engineTable(db, "trip_day_plan_settings")
    .select("trip_day_id, start_time, return_travel_mode, is_locked, note")
    .eq("trip_id", tripId)
    .eq("plan_id", planId);
}

/**
 * เขียนตั้งค่าของวัน × แผน
 *
 * 🔴 PK คือ `(plan_id, trip_day_id)` → `upsert` ใช้ได้จริง **ต่างจาก `trip_hotels`**
 * ที่กันด้วย exclusion constraint ซึ่ง `on conflict` ใช้ไม่ได้
 */
/**
 * 🔴 **ไม่ใช้ `.upsert()` — และเหตุผลไม่ใช่รสนิยม** (P1 · 27 ส.ค. 2026 · P4 probe จับได้)
 *
 * ## อาการ: **เจ้าของทริปแก้ตั้งค่าวันของตัวเองไม่ได้เลย** — `42501 permission denied`
 * เคส *ด้านบวก* ของ `engineCrossUser` แดง (A ควรได้ `200` แต่ได้ `403`)
 *
 * ## เหตุ
 * `.upsert(rows, { onConflict })` ให้ PostgREST สร้าง
 * `insert … on conflict (…) do update set <ทุกคอลัมน์ใน payload> = excluded.<col>`
 * → **`SET` รวม `trip_id` · `plan_id` · `trip_day_id`**
 *
 * แต่ `20260825125024_e2_narrow_key_grants` **ถอน `update` บนคอลัมน์คีย์ออกโดยตั้งใจ**:
 * > *"`grant update` ระดับตารางบน `trip_day_plan_settings` — **คือการให้ไคลเอนต์เขียนคีย์ของแถวตัวเอง**"*
 *
 * `authenticated` จึงมี `update` แค่ `start_time` · `return_travel_mode` · `is_locked` (+ `note` จาก `Q8`)
 * ⚠️ **Postgres ตรวจสิทธิ์ของ `DO UPDATE SET` ตอน *วางแผน* ไม่ใช่ตอนชน**
 * → **ล้มทุกครั้ง แม้แถวยังไม่มีอยู่เลย** · นั่นคือเหตุที่มันพังตั้งแต่การบันทึกครั้งแรก
 *
 * ## 🎯 ทางที่ **ไม่** เลือก: คืน `update` ให้คอลัมน์คีย์
 * นั่นคือการเปิดรูที่ migration นั้นตั้งใจปิด — **ไคลเอนต์จะย้ายแถวข้ามแผน/ข้ามวันได้ด้วยการเขียนคีย์ทับ**
 * · **อาการที่ง่ายที่สุดที่จะซ่อม คือการถอนด่านความปลอดภัย** — รูปเดียวกับที่ `S6` เตือนไว้
 *
 * ## ทางที่เลือก: `update` ก่อน แล้ว `insert` เฉพาะที่ยังไม่มี
 * `update` ส่ง **เฉพาะคอลัมน์ที่เขียนได้** · `insert` ส่งครบ (สิทธิ์ `insert` ครอบทั้ง 6 อยู่แล้ว)
 * · ไม่ต้องเพิ่ม RPC ใหม่ = **ไม่เพิ่มพื้นผิวที่ผู้ใช้เรียกได้** ซึ่งต้องรีวิวความปลอดภัยของตัวเอง
 * · 🔴 **แข่งกันเขียนจาก 2 เครื่อง:** `insert` อาจชน `23505` → **ตกไป `update` อีกรอบ ไม่ใช่โยนทิ้ง**
 *   (ผลที่ผู้ใช้ต้องการคือ "ค่าล่าสุดถูกบันทึก" ไม่ใช่ "ใครถึงก่อนได้ก่อน")
 */
const DAY_SETTING_KEYS = ["trip_id", "plan_id", "trip_day_id"] as const;

export async function upsertDaySettings(db: Db, rows: InsertRow<"trip_day_plan_settings">[]) {
  const touched: string[] = [];

  for (const row of rows) {
    const dayId = row.trip_day_id as string;
    const planId = row.plan_id as string;
    // 🔴 `Object.fromEntries` คืน index signature เสมอ — ผูกกลับเป็นชนิดของตาราง
    //    ไม่งั้นคอลัมน์ที่พิมพ์ผิดจะหลุดผ่านอีกทาง ทั้งที่ลายเซ็นข้างบนตรวจแล้ว
    const patch: UpdateRow<"trip_day_plan_settings"> = Object.fromEntries(
      Object.entries(row).filter(([k]) => !(DAY_SETTING_KEYS as readonly string[]).includes(k)),
    );

    // ① ลองแก้ของที่มีอยู่ — ส่งเฉพาะคอลัมน์ที่ `authenticated` มีสิทธิ์ `update`
    if (Object.keys(patch).length > 0) {
      const upd = await engineTable(db, "trip_day_plan_settings")
        .update(patch).eq("plan_id", planId).eq("trip_day_id", dayId).select("trip_day_id");
      if (upd.error) return { data: null, error: upd.error };
      if (upd.data && upd.data.length > 0) { touched.push(dayId); continue; }
    }

    // ② ยังไม่มีแถว → สร้าง (สิทธิ์ `insert` ครอบคอลัมน์คีย์อยู่แล้ว)
    const ins = await engineTable(db, "trip_day_plan_settings").insert(row).select("trip_day_id");
    if (!ins.error) { touched.push(dayId); continue; }

    // ③ มีคนสร้างแทรกระหว่าง ① กับ ② → แก้ทับ ไม่ใช่ล้ม
    if (ins.error.code !== "23505") return { data: null, error: ins.error };
    const retry = await engineTable(db, "trip_day_plan_settings")
      .update(patch).eq("plan_id", planId).eq("trip_day_id", dayId).select("trip_day_id");
    if (retry.error) return { data: null, error: retry.error };
    if (retry.data && retry.data.length > 0) touched.push(dayId);
  }

  return { data: touched.map((trip_day_id) => ({ trip_day_id })), error: null };
}

// ───────────────────────────────────────────────────────────────────────────
// ตั๋ว/การจอง — `E3`
// ───────────────────────────────────────────────────────────────────────────

const BOOKING_COLS =
  "id, trip_day_id, category, title, date, time, confirmation_number, link, note, file_path, file_name, status, book_by_days_before, legacy_added_by, created_at, updated_at";

export function bookingsOfTrip(db: Db, tripId: string) {
  return engineTable(db, "bookings")
    .select(BOOKING_COLS)
    .eq("trip_id", tripId)
    .is("deleted_at", null)
    .order("created_at");
}

export function insertBooking(db: Db, row: InsertRow<"bookings">) {
  return engineTable(db, "bookings").insert(row).select(BOOKING_COLS).single();
}

export function updateBooking(db: Db, id: string, patch: UpdateRow<"bookings">) {
  return engineTable(db, "bookings").update(patch).eq("id", id).select("id");
}

export function softDeleteBooking(db: Db, id: string) {
  return db.rpc("soft_delete_booking", { p_id: id });
}

// ───────────────────────────────────────────────────────────────────────────
// แผน — `E3` · `D52` (ไม่มี `trips.active_plan_id` · ใช้ `trip_plans.is_active`)
// ───────────────────────────────────────────────────────────────────────────

export function plansOfTrip(db: Db, tripId: string) {
  return engineTable(db, "trip_plans")
    .select("id, name, is_active, created_at")
    .eq("trip_id", tripId)
    .order("created_at");
}

export function insertPlan(db: Db, tripId: string, name: string) {
  return engineTable(db, "trip_plans")
    .insert({ trip_id: tripId, name, is_active: false })
    .select("id, name, is_active, created_at")
    .single();
}

export function renamePlan(db: Db, id: string, name: string) {
  return engineTable(db, "trip_plans").update({ name }).eq("id", id).select("id");
}

export function deletePlan(db: Db, id: string) {
  return engineTable(db, "trip_plans").delete().eq("id", id).select("id");
}

/**
 * 🔴 สลับแผนผ่าน RPC เพราะ `trip_plans_one_active` เป็น **partial unique index** (`D52`)
 * ปลดของเก่าแล้วตั้งของใหม่ต้องอยู่ทรานแซกชันเดียว **ไม่งั้นชน index ระหว่างทาง**
 */
export function setActivePlan(db: Db, tripId: string, planId: string) {
  return db.rpc("set_active_plan", { p_trip_id: tripId, p_plan_id: planId });
}

/**
 * ก๊อปแผนทั้งใบ **ในทรานแซกชันเดียว** — `P-71`
 * ของเดิมเขียนทีละคำสั่งแล้วทิ้งผลลัพธ์ → **แผนที่ก๊อปมาไม่ครบโดยไม่มีใครรู้**
 */
export function duplicatePlan(db: Db, tripId: string, sourcePlanId: string, name: string) {
  return db.rpc("duplicate_trip_plan", {
    p_trip_id: tripId, p_source_plan_id: sourcePlanId, p_name: name,
  });
}

// ───────────────────────────────────────────────────────────────────────────
// จุดแวะ — `E3` · `D6` (`rank` แทน `order_index`)
// ───────────────────────────────────────────────────────────────────────────

const STOP_COLS =
  "id, trip_day_id, catalog_place_id, kind, rank, dwell_minutes, travel_mode, note, photo_path, intercity_from, intercity_to, intercity_mode, transfer_target_time, transfer_target_label, visited_at, legacy_added_by, updated_at, custom_place_id, event_kind, schedule_bound, fixed_start_time, fixed_end_time, day_offset, title, title_en, icon, is_alert, time_is_flexible, flight_no, flight_from_code, flight_to_code, flight_from_en, flight_to_en, layover_baggage, layover_immigration, layover_leaves_airport, layover_terminal_change, place_ref, catalog_places(legacy_slug)";

/** จุดแวะของแผน **ที่ยังไม่ถูกลบ** เรียง `(rank, id)` — `D81` ③ */
export function stopsOfPlan(db: Db, tripId: string, planId: string) {
  return engineTable(db, "trip_stops")
    .select(STOP_COLS)
    .eq("trip_id", tripId)
    .eq("plan_id", planId)
    .is("deleted_at", null)
    .order("rank", { ascending: true })
    .order("id", { ascending: true });
}

/** `rank` ของ **จุดแวะเท่านั้น** ในวันหนึ่ง — ไม่รวมแถว `kind='event'`
 *
 *  🔴 **ใช้ตัวนี้ทุกที่ที่แปล `atIndex` → `rank`** (`POST` แทรก · `PATCH` ย้าย) · 30 ส.ค. 2026 · P2 ไล่เจอ
 *  `atIndex` มาจากลิสต์ที่ผู้ใช้เห็น ซึ่ง **ไม่มีแถว event** (`splitDayEvents` ดึงออกไปแสดงแยก)
 *  → ถ้าเทียบกับลิสต์เต็ม ตำแหน่งจะเลื่อนตามจำนวน event ของวันนั้น **และเลื่อนแบบเงียบ**
 *
 *  🎯 **ไม่ใช่แค่กันปัญหาของไคลเอนต์ — มันผิดอยู่แล้วก่อนใครจะกรอง**
 *  ตำแหน่งของ event เทียบกับจุดแวะ **ไม่มีความหมาย**: `splitDayEvents` แบ่ง before/after จาก
 *  *ลำดับของแถว event กันเอง* ไม่ได้ดูว่ามันอยู่ตรงไหนเทียบจุดแวะ → เอามานับตำแหน่งแทรกไม่ได้ตั้งแต่ต้น
 *
 *  ⚠️ **`ranksInDay` (ลิสต์เต็ม) ยังต้องใช้ที่ `PUT` เรียงใหม่** — ตรงนั้นใช้ตรวจว่า `orderedIds`
 *  เป็นของวันนี้จริงไหม ซึ่งต้องเทียบกับ *ทุกแถว* ไม่ใช่เฉพาะจุดแวะ */
export function stopRanksInDay(db: Db, tripId: string, planId: string, tripDayId: string) {
  return engineTable(db, "trip_stops")
    .select("id, rank")
    .eq("trip_id", tripId)
    .eq("plan_id", planId)
    .eq("trip_day_id", tripDayId)
    .neq("kind", "event")
    .is("deleted_at", null)
    .order("rank", { ascending: true })
    .order("id", { ascending: true });
}

/** `rank` ของจุดแวะในวันหนึ่ง เรียงแล้ว — ใช้คำนวณตำแหน่งแทรก */
export function ranksInDay(db: Db, tripId: string, planId: string, tripDayId: string) {
  return engineTable(db, "trip_stops")
    .select("id, rank")
    .eq("trip_id", tripId)
    .eq("plan_id", planId)
    .eq("trip_day_id", tripDayId)
    .is("deleted_at", null)
    .order("rank", { ascending: true })
    .order("id", { ascending: true });
}

export function insertStop(db: Db, row: InsertRow<"trip_stops">) {
  return engineTable(db, "trip_stops").insert(row).select(STOP_COLS).single();
}

export function updateStop(db: Db, id: string, patch: UpdateRow<"trip_stops">) {
  return engineTable(db, "trip_stops").update(patch).eq("id", id).select("id");
}

/**
 * แก้จุดแวะ **โดยผูกกับวันที่ผู้เรียกอ้างว่ากำลังจัดลำดับอยู่**
 *
 * 🔴 P4 เจอ 26 ส.ค. 2026: `PUT` อ่าน rank ด้วย `ranksInDay()` ซึ่งมีขอบครบ (trip · plan · day)
 * แล้วเขียนกลับด้วย `updateStop()` ซึ่งมีแค่ `.eq("id")` — **อ่านมีขอบ เขียนไม่มีขอบ**
 * → `orderedIds` ที่ปน id ของ *วันอื่น* เขียนทับ `rank` ของวันนั้นได้จริง (ยิงผ่าน HTTP แล้ว)
 * · RLS ยังกันข้ามผู้ใช้ได้ครบ **แต่ผู้ใช้ทำลำดับวันอื่น *ของตัวเอง* พังได้จากคำขอเดียว**
 *
 * 🎯 route กันด้วยการตรวจก่อน (คืน `409`) อยู่แล้ว — **ตัวนี้คือชั้นที่ทำให้มันเกิดไม่ได้
 * ถึงแม้การตรวจนั้นจะถูกข้าม** · ขอบของการเขียนต้องเท่ากับขอบของการอ่านเสมอ ไม่ใช่เพราะจำได้
 */
export function updateStopInDay(
  db: Db,
  id: string,
  scope: { tripId: string; planId: string; tripDayId: string },
  patch: UpdateRow<"trip_stops">
) {
  return engineTable(db, "trip_stops")
    .update(patch)
    .eq("id", id)
    .eq("trip_id", scope.tripId)
    .eq("plan_id", scope.planId)
    .eq("trip_day_id", scope.tripDayId)
    .select("id");
}

export function softDeleteStop(db: Db, id: string) {
  return db.rpc("soft_delete_trip_stop", { p_id: id });
}

/**
 * ลบสถานที่ที่ผู้ใช้เพิ่มเอง — **soft delete ผ่าน RPC เท่านั้น** (P1-Lead · 4 ก.ย. 2026)
 *
 * 🔴 **ไม่มีทาง `DELETE` ตรง และนั่นตั้งใจ** — `20260825142639_e2_soft_delete.sql:70-75`
 * ถอด **ทั้ง policy และ grant** ออกพร้อมกัน พร้อมเหตุผลว่า
 * *"เหลือทางใดทางหนึ่งไว้ = soft delete จะถูกข้ามได้ทุกครั้งโดยไม่มีอะไรค้าน"*
 * · และ `deleted_at` ก็ **ไม่อยู่ใน grant update** (มี assert บังคับข้อนี้อยู่) ⇒ RPC เป็นทางเดียวจริง ๆ
 *
 * 🎯 ***ความสามารถนี้อยู่ในฐานมาตั้งแต่ 25 ส.ค. · `grant execute` ให้ `authenticated` แล้ว ·
 *    ขาดแค่ route ที่เรียกมัน*** — รูปเดียวกับ `create_trip` ที่ไม่มี UI เรียกอยู่สองวัน
 *    และ `trip_destinations` ที่มี policy ครบ 4 verb แต่ไม่มีทางเข้า
 *
 * ⚠️ trigger `custom_places_not_in_use` ยิงตอน `update` ⇒ **สถานที่ที่ยังถูกใช้เป็นจุดแวะอยู่ ลบไม่ได้**
 * ผู้เรียกต้องแปลงข้อความนั้นให้ผู้ใช้อ่านรู้เรื่อง ไม่ใช่ส่ง error ของ Postgres ต่อดิบ ๆ
 */
export function softDeleteCustomPlace(db: Db, id: string) {
  return db.rpc("soft_delete_custom_place", { p_id: id });
}

/**
 * ปัก/ถอนหมุดทริป — **มุมมองส่วนตัวของผู้เรียก คนอื่นในทริปไม่เห็น** (P1-Lead · 4 ก.ย. 2026)
 *
 * 🔴 **RPC ไม่ใช่ `update` ตรง และเหตุผลคือช่องยกระดับสิทธิ์ที่จะเปิดถ้าทำอีกทาง**
 * `trip_members_update` จำกัด `owner` และมี `grant update (role)` ให้ `authenticated` อยู่แล้ว
 * ⇒ เพิ่ม policy *"สมาชิกแก้แถวตัวเองได้"* = **editor ตั้ง `role` ตัวเองเป็น `owner` ได้ทันที**
 * (policy ของ UPDATE ถูก OR กัน · `grant` ที่เคยปลอดภัยเพราะ policy แคบ กลายเป็นเปิดพร้อมกัน)
 * 🎯 ***policy คุมว่า "แถวไหน" · `grant` คุมว่า "คอลัมน์ไหน" — ผ่อน policy จึงผ่อนทุกคอลัมน์ที่ `grant`
 *    เคยให้ไว้ตอนที่ policy ยังแคบ*** · เหตุผลเต็มอยู่ที่ `20260904140000_e5_pin_trip.sql`
 */
export function setTripPinned(db: Db, tripId: string, pinned: boolean) {
  return db.rpc("set_trip_pinned", { p_trip_id: tripId, p_pinned: pinned });
}

/**
 * ทริปที่ *ผู้เรียก* ปักหมุดไว้ — คืน `trip_id` + `pinned_at` เรียงใหม่สุดก่อน
 *
 * 🔴 **`.eq("user_id", userId)` จำเป็น ไม่ใช่การกันไว้เฉย ๆ** — `trip_members_select` ใช้ `can_read_trip`
 * ⇒ อ่านแถวของ **เพื่อนร่วมทริปทุกคน** ได้ตามปกติ · ไม่กรอง = เอาหมุดของคนอื่นมาแสดงเป็นของตัวเอง
 * · 🎯 RLS ที่นี่ตอบว่า *"เห็นได้ไหม"* ไม่ได้ตอบว่า *"ของใคร"* — สองคำถามนี้ต่างกัน และตัวที่สอง
 *   ไม่มีอะไรในฐานตอบให้ ต้องถามเอง
 */
export function pinnedTripIdsOf(db: Db, userId: string) {
  return engineTable(db, "trip_members")
    .select("trip_id, pinned_at")
    .eq("user_id", userId)
    .not("pinned_at", "is", null)
    .order("pinned_at", { ascending: false });
}

// ───────────────────────────────────────────────────────────────────────────
// โปรไฟล์ผู้ใช้ — `E5` · P1-Lead · 4 ก.ย. 2026 (ผู้ใช้สั่งรื้อหน้า `/account`)
// ───────────────────────────────────────────────────────────────────────────

/**
 * โปรไฟล์ของผู้เรียก
 *
 * 🔴 **`.eq("id", userId)` ไม่ใช่การกันไว้เฉย ๆ** — รูปเดียวกับ `pinnedTripIdsOf`
 * `profiles_select` เปิดให้อ่านโปรไฟล์ของ**เพื่อนร่วมทริป**ได้ (ไม่งั้นแสดงชื่อคนในทริปไม่ได้)
 * ⇒ ไม่ระบุ `id` = อาจได้โปรไฟล์คนอื่นมาแสดงเป็นของตัวเอง
 * 🎯 ***RLS ตอบว่า "เห็นได้ไหม" ไม่ได้ตอบว่า "ของใคร"***
 */
export function profileOf(db: Db, userId: string) {
  return engineTable(db, "profiles")
    .select("id, display_name, locale, home_country")
    .eq("id", userId)
    .maybeSingle();
}

/**
 * แก้ชื่อที่แสดง
 *
 * ## 🔴 ทำไมเขียนคอลัมน์เดียว ไม่ใช่รับอ็อบเจกต์แล้ว spread
 * `20260825122247:71` ให้ `grant update (display_name, locale, home_country)` — **สามคอลัมน์**
 * ⇒ ฟังก์ชันที่รับอ็อบเจกต์แล้วส่งต่อทั้งก้อน จะพาคีย์ที่ผู้เรียกเผลอใส่มาไปถึงฐานด้วย
 * 🎯 ***ชุดคอลัมน์ที่เขียนได้ ต้องถูกตัดสินที่นี่ ไม่ใช่ที่ payload ของ HTTP***
 * · ⚠️ วันที่มีหน้าให้แก้ `locale`/`home_country` **ให้เพิ่มฟังก์ชันแยก อย่าเปลี่ยนตัวนี้ให้รับอ็อบเจกต์**
 *   — `§3.4`: การขยายชุดฟิลด์ทำให้ช่องที่เคยปลอดภัยเพราะชุดมันเล็ก กว้างพอจะเดินผ่าน
 *
 * ## 🔴 `.eq("id", userId)` ต้องมี แม้ policy จะกันอยู่แล้ว
 * ถ้า policy ผ่อนวันหนึ่ง (เช่นให้แอดมินแก้ของคนอื่น) บรรทัดนี้คือสิ่งเดียวที่ยังกันไว้
 * · และถ้าไม่มีมัน `update` จะกลายเป็น "แก้ทุกแถวที่ RLS ยอม" ซึ่งกว้างกว่าที่ผู้เรียกขอเสมอ
 */
export function updateDisplayName(db: Db, userId: string, displayName: string) {
  return engineTable(db, "profiles")
    .update({ display_name: displayName })
    .eq("id", userId)
    .select("id, display_name")
    .maybeSingle();
}

// ───────────────────────────────────────────────────────────────────────────
// ทริปแนะนำ — `E5` · P1-Lead · 4 ก.ย. 2026 (ผู้ใช้สั่งเอง)
// ───────────────────────────────────────────────────────────────────────────

/**
 * รายการทริปแนะนำ (สรุปสำหรับการ์ดบนหน้าแรก)
 *
 * 🔴 **เป็น RPC ไม่ใช่ `select` และนั่นคือแกนของการออกแบบ ไม่ใช่รสนิยม**
 * `trips_select` = `can_read_trip(id)` ⇒ **ไม่มีใครอ่านทริปที่ตัวเองไม่ได้เป็นสมาชิกได้เลย**
 * รวมถึงใบที่เป็น template · เราจงใจ **ไม่** เพิ่ม policy ให้อ่าน
 * ⇒ ทางเดียวที่เนื้อออกได้คือ definer 2 ตัวนี้ · เส้นทาง *อ่านทริปคนอื่น* ไม่มีอยู่จริง
 * 🎯 ***"ติดธงผิดใบ" จึงมีราคาแค่ มีคนก๊อปแผนที่ไม่ได้ตั้งใจเผยแพร่ — ไม่ใช่ ข้อมูลรั่ว***
 *
 * ⚠️ `cities` เป็น `Json` (jsonb จากฐาน) — **ผู้เรียกต้องแปลงเอง** · ชนิดที่ `supabase gen types`
 *    ให้ได้แค่ `Json` เพราะ Postgres ไม่ได้บอกรูปข้างในของ `jsonb`
 *    🔴 ⇒ **`as` ตรงนี้เป็นคำกล่าวอ้าง ไม่ใช่การตรวจ** — route ต้องตรวจรูปด้วยตัวเองก่อนใช้
 */
export function listTripTemplates(db: Db) {
  return db.rpc("list_trip_templates");
}

/**
 * ก๊อปทริปแนะนำมาเป็นทริปของผู้เรียก
 *
 * 🔴 **ด่านทั้งหมดของฟังก์ชันนี้คือ `where published_template_at is not null` ในตัว RPC**
 * (`20260904180000`) — `security definer` ทำให้ `where` เป็นด่านเดียวที่เหลือ
 * ⇒ **ห้ามเพิ่ม wrapper ที่รับ `tripId` แล้วส่งต่อโดยไม่ผ่าน RPC นี้**
 *
 * ⚠️ `P0002` = ไม่พบ/ไม่ใช่ template · `22023` = ไม่ได้ส่งวันเริ่ม · `P0003` = ก๊อปไม่ครบ (ของภายใน)
 */
export function copyTripTemplate(
  db: Db,
  args: { templateId: string; startDate: string; title?: string | null },
) {
  return db.rpc("copy_trip_template", {
    p_template_id: args.templateId,
    p_start_date: args.startDate,
    // 🔴 `?? undefined` ไม่ใช่ `?? null` — พารามิเตอร์มี `default null` ในฐาน
    //    ส่ง `null` ไปจะเป็นการ *ตั้งค่าเป็น null* ซึ่งเหมือนกันในเคสนี้ **แต่ชนิดที่ gen มาประกาศ
    //    `p_title?: string`** ⇒ ส่ง null ไม่ผ่าน tsc · รูปเดียวกับ `p_base_timezone` ที่ `schemaPins` พินไว้
    p_title: args.title ?? undefined,
  });
}

/**
 * เมืองในคลัง + ประเทศของมัน — ใช้เติมข้อมูลที่ RPC ไม่ได้คืน
 *
 * 🔴 **มีอยู่เพราะ `list_trip_templates()` คืน `slug` แต่ไม่คืน `country_id`**
 * `TripCoverImage` ไล่รูปปก `slug → countryId → พื้นไล่สี` ⇒ ขาด `countryId` = **ชั้นที่สองหายไปทั้งชั้น**
 * 🎯 ***การ์ดที่ตกไปพื้นไล่สีทุกใบ อ่านเหมือน "รูปพัง" ไม่ใช่ "ยังไม่มีรูป"*** (P2 ชี้)
 * · ⚠️ **ทางที่ถูกกว่าคือให้ RPC คืนมาเอง** — แต่ต้อง `drop`+`create` (เปลี่ยนชนิดที่คืน) = migration ใบใหม่
 *   ⇒ เติมที่ route ไปก่อน · **ย้ายเข้า RPC เมื่อมีเหตุอื่นต้องแตะไฟล์นั้นอยู่แล้ว** ไม่ใช่แตะเพื่อเรื่องนี้อย่างเดียว
 * · 📌 78 แถววันนี้ ⇒ ถูกกว่าการยิงรายเมือง · **เกณฑ์ที่ต้องเปลี่ยน: เวลาตอบของ route เริ่มขยับ**
 */
export function catalogCityCountryMap(db: Db) {
  return engineTable(db, "catalog_cities").select("id, country_id, legacy_slug");
}
