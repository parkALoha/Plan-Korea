/**
 * เพดานที่ใช้ร่วมกันของทุกเส้นทางที่เขียน "จุดหมายของทริป"
 * เจ้าของ: P1-Lead · 4 ก.ย. 2026
 *
 * ## 🔴 ทำไมต้องมีไฟล์นี้ — มันเกิดจากช่องที่เปิดขึ้นวันเดียวกับที่ปิด
 * ```
 * POST /api/engine/trips        เพดาน 20   ← สร้างทริปใหม่ (มีมาก่อน · `7230241`)
 * PUT  /…/[tripId]/destinations เพดาน 30   ← แก้ทริปเดิม (ผมเพิ่ม 4 ก.ย. 2026)
 * ```
 * 🎯 ***ทริปที่ "สร้างไม่ได้" ด้วย 25 เมือง กลับ "แก้ให้เป็น" ได้*** — เพดานไม่ได้หายไป
 * **มันมีสองค่าที่ไม่ตรงกัน** และค่าที่หลวมกว่าคือค่าที่ชนะเสมอ (P4 พบ · 4 ก.ย. 2026)
 *
 * · 🔴 **และรูปของมันคือรูปที่ `TEAM.md §3.4` เตือนไว้**: เดิมมีทางเข้าทางเดียว เพดานจึงบังคับได้จริง
 *   **ใบใหม่เปิดทางที่สองด้วยเลขของตัวเอง** ⇒ ของที่เคยปลอดภัยเพราะ *ชุดมันเล็ก* กว้างพอจะเดินผ่าน
 * · ⚠️ **ผมรายงานผิดด้วยว่า `POST` "ไม่มีเพดานเลย"** ทั้งที่ผมเขียนมันเอง — ถ้าไม่มีใครไปเปิดอ่าน
 *   เราจะได้เพดานใบที่สามเพิ่มมาอีก
 *
 * ## ทำไมเลือก 20 ไม่ใช่ 30
 * **20 คือค่าที่มีผลบังคับใช้อยู่แล้ว** ⇒ ไม่เปลี่ยนพฤติกรรมของใครที่ใช้อยู่เลย
 * เลือก 30 = **ผ่อนเพดานให้ทริปที่มีอยู่ ซึ่งเป็นการตัดสินใจที่ต้องมีเหตุผลของตัวเอง** — และเราไม่มี
 * · 📌 `30` ที่ผมตั้งไว้เดิมเป็นเลขที่ตั้งลอย ๆ · `20` มีคอมเมนต์ที่มาติดตัว (*"ทริปข้ามเมืองจริง
 *   ยังไม่เคยเกิน 6 เมือง"*) ⇒ **เลขที่มีที่มา ชนะเลขที่ไม่มี** โดยไม่ต้องเถียงกันว่าเท่าไหร่ถึงพอดี
 *
 * 🔴 **ห้ามคัดลอกค่านี้ไปเขียนซ้ำที่ไหน** — `import` เท่านั้น
 * *เลขที่ต้องมีคนซิงก์ จะล้าเสมอ* (`§3.3`) และไฟล์นี้เกิดมาจากการล้าครั้งแรกพอดี
 */
export const MAX_TRIP_DESTINATIONS = 20;

/**
 * **จำนวนวันสูงสุดต่อทริป — ผู้ใช้ตัดสินเอง 4 ก.ย. 2026: *"สูงสุด 30 วันพอ"***
 *
 * ## 🔴 อ่านให้ชัดก่อนคิดจะ "แก้ให้ตรงกัน" — ที่นี่มีสองเพดาน และมัน **ตอบคนละคำถาม**
 * ```
 * 30   ที่นี่ (แอป)        "ทริปยาวสุดที่เราออกแบบให้รองรับคือเท่าไหร่"   ← เพดานของ *สินค้า*
 * 366  ในฐาน (create_trip) "กันคนพิมพ์ปีผิดแล้วสร้าง 3,653 แถวรวดเดียว"  ← เพดาน *กันอุบัติเหตุ*
 * ```
 * 🎯 ***ต่างจากเคส `MAX_TRIP_DESTINATIONS` ข้างบนอย่างสิ้นเชิง*** — ที่นั่นสองเลขตอบ **คำถามเดียวกัน**
 *    (จำนวนเมืองสูงสุด) แล้วไม่ตรงกัน ⇒ เป็นบั๊ก · ที่นี่ตอบคนละคำถาม ⇒ **ตั้งใจให้ต่างกัน**
 * 🔴 **ห้ามลด 366 ในฐานลงมาเป็น 30** — ถ้าวันหนึ่งผู้ใช้อยากได้ 60 วัน เราจะแก้ที่นี่ที่เดียว
 *    และฐานต้องยังกันปีพิมพ์ผิดอยู่เหมือนเดิม
 *
 * ## ที่มาของเลข — และสิ่งที่มัน **ไม่ได้** มาจาก
 * ผู้ใช้เลือก 30 หลังผมเสนอ · เหตุผลที่ผมให้ตอนเสนอคือ **ทริปที่เราเห็นจริงยาวสุด 11 วัน**
 * (`เกาหลี ต.ค. 2026` — ทริปจริงของเขาเอง) ⇒ 30 เผื่อไว้เกือบสามเท่า
 * · ⚠️ **ไม่ได้มาจากการวัดว่าเว็บช้าที่กี่วัน** — ผมบอกเขาตรง ๆ ตอนเสนอว่ายังไม่มีใครวัด
 *   (การ์ด N ใบ + คำนวณเวลาเดินทางทุกวัน · ไม่รู้ว่าคอขวดอยู่ตรงไหน)
 * · ✅ ตรวจก่อนลด: ทริปในฐาน ณ 4 ก.ย. 2026 มี 5 ใบ · **ยาวสุด 11 วัน · เกิน 30 วัน = 0 ใบ**
 *   ⇒ ลดจาก 366 มา 30 **ไม่กระทบทริปที่มีอยู่สักใบ** — ถ้าวันหนึ่งมี ต้องเช็คซ้ำก่อนลดอีก
 *
 * 🔴 **ห้ามคัดลอกค่านี้ไปเขียนซ้ำที่ไหน — `import` เท่านั้น**
 * ทั้ง UI (dropdown) และ route ต้องได้คำตอบเดียวกัน · *"ตัวนับที่นับคนละแบบกับด่าน คือตัวนับที่โกหก"*
 */
export const MAX_TRIP_DAYS = 30;
