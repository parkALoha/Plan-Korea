/**
 * กฎ *"ทริปไหน"* ฉบับบริสุทธิ์ — **ไม่ import อะไรเลย ทั้งไฟล์**
 * เจ้าของ: P1-Lead · 26 ส.ค. 2026 · `E3` · แยกออกจาก `trip.ts` หลัง P4 ไล่กราฟ import
 *
 * ## 🔴 ทำไมต้องแยกไฟล์ ทั้งที่ฟังก์ชันนี้ *เป็น* pure อยู่แล้ว
 * `chooseSoleTrip` เคยอยู่ใน `trip.ts` และมีคอมเมนต์กำกับว่า *"แยกออกมาเป็น pure
 * เพราะฝั่งเบราว์เซอร์ต้องใช้กฎเดียวกัน"* — **ถูกทั้งประโยค แต่ไม่พอ**
 * บรรทัดแรกของ `trip.ts` คือ `import { tripsVisibleToMe } from "./db"`
 * → hook ฝั่ง client 9 ตัวลาก **`lib/engine/db.ts` ทั้งไฟล์** เข้ากราฟของเบราว์เซอร์
 *
 * 🎯 **สิ่งที่ค้ำเส้นแบ่ง server/client ไว้จึงเป็น *tree-shaking* ซึ่งไม่มีใครตรวจ**
 * P4 ชี้ตรง ๆ ว่านั่นคือ *"มองไม่เห็น = ปลอดภัย"* อีกรูปหนึ่ง — และเขาถูก
 * · คีย์ไม่ได้รั่ว (`db.ts` รับ client เข้ามา ไม่ import ตัวไหนเอง)
 *   **สิ่งที่จะรั่วคือตรรกะกับชื่อตารางลงบันเดิลเบราว์เซอร์**
 * · ไฟล์นี้คือครึ่งแรกของทางแก้ · ครึ่งหลังคือ `import "server-only"` ที่หัว `db.ts`
 *   ซึ่งทำให้ **build ล้ม** ถ้าใครลากมันกลับเข้าไปอีก — โครงสร้าง ไม่ใช่คำเตือน
 */

export type SoleTrip =
  | { ok: true; tripId: string }
  /** ผู้ใช้ยังไม่มีทริปเลย — หน้าจอควรชวนสร้าง ไม่ใช่ขึ้น error */
  | { ok: false; reason: "none" }
  /** 🔴 มีหลายทริป — **ต้องให้ผู้ใช้เลือก ห้ามเดา** · `E5-AC1` คือทางออกถาวร */
  | { ok: false; reason: "ambiguous"; tripIds: string[] }
  | { ok: false; reason: "error"; message: string };

/**
 * กฎการเลือก — **ฝั่งเบราว์เซอร์กับฝั่งเซิร์ฟเวอร์ต้องใช้ตัวนี้ตัวเดียวกัน**
 *
 * 🔴 hook ฝั่ง client อ่านรายชื่อทริปผ่าน `GET /api/engine/trips` แล้วต้องตัดสินแบบเดียวกันเป๊ะ
 * ถ้าปล่อยให้แต่ละฝั่งเขียนกฎเอง วันหนึ่งฝั่งหนึ่งจะหยิบตัวแรกและอีกฝั่งจะไม่หยิบ
 * **แล้วผู้ใช้จะเห็นทริปคนละใบระหว่างเฟรมแรกกับเฟรมที่สอง** — `D46` ที่รอยต่อ client/server
 *
 * 🔴 **ห้ามเลือกทริปให้เงียบ ๆ** — *"เอาตัวแรกที่เจอ"* ถูกเสมอจนวันที่ผู้ใช้สร้างทริปที่สอง
 * วันนั้นเขาจะแก้ทริปผิดใบ **โดยหน้าจอไม่มีอะไรผิดปกติเลยสักอย่าง**
 */
export function chooseSoleTrip(trips: readonly { id: string }[]): SoleTrip {
  if (trips.length === 0) return { ok: false, reason: "none" };
  if (trips.length > 1) return { ok: false, reason: "ambiguous", tripIds: trips.map((t) => t.id) };
  return { ok: true, tripId: trips[0].id };
}

/** ข้อความที่ผู้ใช้อ่านออก — เก็บไว้ที่เดียวเพื่อไม่ให้แต่ละหน้าคิดคำเอง */
export function soleTripMessage(r: Exclude<SoleTrip, { ok: true }>): string {
  switch (r.reason) {
    case "none":
      return "ยังไม่มีทริป — สร้างทริปแรกก่อนเริ่มวางแผน";
    case "ambiguous":
      return `มี ${r.tripIds.length} ทริป — เลือกทริปที่ต้องการก่อน`;
    case "error":
      return `เปิดทริปไม่ได้: ${r.message}`;
  }
}
