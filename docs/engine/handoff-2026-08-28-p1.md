# Handoff — P1-Lead · 28 ส.ค. 2026

> เก็บเฉพาะของที่**ยังไม่มีที่อยู่ในเอกสารอื่น** · สถานะ AC อยู่ที่ [`backlog.md`](./backlog.md)
> · กติกาอยู่ที่ `/Users/park/plan-korea/TEAM.md` · งานออฟไลน์อยู่ที่ [`mobile-arch.md`](./mobile-arch.md) §11–§13

---

## 🔴 `E7-AC8` — ผ่านครึ่งแรกแล้ว และของอยู่นอกรีโป

**ผู้ใช้ dump ฐานทริปจริงสำเร็จเมื่อ 28 ส.ค. 2026 (~18:03)**

```
~/trip-schema.sql    29K     ← นอกรีโป โดยตั้งใจ
~/trip-data.sql     1.3M
~/trip-roles.sql       0B    ← ล้มตอนยังไม่มี Docker · ไม่ได้ใช้ · ลบทิ้งได้
```

🔴 **ไฟล์อยู่บนเครื่องผู้ใช้เท่านั้น ไม่มีสำเนาที่อื่น** — ถ้าเครื่องหาย สำเนาแช่แข็งหายไปด้วย

**ตรวจแล้วว่าครบจริง ไม่ใช่แค่มีขนาด** — นับแถวทั้งสองฝั่งแล้วเทียบด้วยสคริปต์:

```
14 ตาราง · 690 แถว · ต่างกัน 0
bookings 8 · checklist_items 8 · custom_places 37 · hidden_places 39
place_details_cache 140 · place_notes 2 · place_photo_cache 142 · travel_time_cache 185
trip_day_settings 18 · trip_hotels 4 · trip_meta 1 · trip_plans 2 · trip_selections 13 · trip_stops 71
```

**ยังไม่ผ่าน AC** — *"ไฟล์ dump ที่ยังไม่เคยถูก restore ไม่นับเป็น backup"* · ขั้น restore ยังไม่ได้ทำ

---

## 🔴 ข้อเท็จจริงที่เกณฑ์ `E7-AC8` เขียนผิด — **ฐานทริปไม่มีตาราง `days`**

เกณฑ์เดิมสั่งให้ตรวจว่า *"เปิดสำเนาแล้วเห็นทริปเกาหลี **ครบ 11 วัน**"* — **ตรวจแบบนั้นไม่ได้**

```
ตารางในฐานทริป 14 ตาราง — ไม่มี days · ไม่มี trip_days
วันอยู่ใน  data/itinerary.ts  (ไฟล์ในรีโป)
ฐานเก็บแค่ตัวอ้างอิง  day_id = 'd0'…'d10'
   ปรากฏใน trip_stops · trip_day_settings · trip_selections · bookings
```

🎯 **ทริปเกาหลีมีตัวตนสองที่: โครงวันอยู่ในโค้ด · เนื้อหาอยู่ในฐาน** — และนั่นคือสิ่งที่ `E7` มีไว้ย้ายให้มาอยู่ที่เดียว
· 🔴 **เกณฑ์เขียนราวกับ `E7` เสร็จแล้ว** — บรรยายสภาพ*หลัง*ย้าย แล้วเอาไปใช้เป็นด่าน*ก่อน*ย้าย
· **ไม่มีใครจับได้จนมีคนลงมือทำจริง** — ส่ง P8 แก้ถ้อยคำแล้ว

---

## 🔴 สองอย่างที่บล็อกขั้นถัดไป และทีมแก้เองไม่ได้

**① โควตาโปรเจกต์เต็ม**
```
org osjepwxnnlvhubylqoyg (free = 2 โปรเจกต์)
  ✅ engine-dev                        ACTIVE
  ⚪ "muphoneseller-netizen's Project"  INACTIVE · ref bjlinogorqpjpksavdjl · สร้าง 17 ส.ค.
```
สำเนาแช่แข็งต้องมีที่อยู่ · **ห้ามลบตัว INACTIVE จนกว่าจะมีคนเปิดดูว่าว่างจริง** (ชื่อบอกว่าเป็นของ MU Phone ซึ่งคนละโปรเจกต์)

**② ขั้น restore เป็นสิทธิ์ของผู้ใช้** — และเป็นขั้นที่**พิมพ์ผิดแล้วเขียนทับฐานทริปจริงได้**
🔴 `ejzibhgqhxdzkovsnpds` **ห้ามโผล่ในคำสั่ง restore เด็ดขาด**

---

## 📌 เครื่องมือ — `supabase db dump` ใช้ไม่ได้บนเครื่องนี้

```
supabase db dump  →  "failed to run docker. Docker Desktop is a prerequisite"
```
**ทางที่ใช้ได้:** `brew install libpq && brew link --force libpq` แล้วเรียก `pg_dump` ตรง ๆ (หลักสิบ MB แทน Docker หลาย GB)
· ⚠️ `supabase db dump` **ค่าเริ่มต้นดัมป์แค่ schema** — รันตัวเดียวแล้ว restore จะได้ฐานที่มีตารางครบแต่ว่างเปล่า **โดยไม่ error**
· ใช้ `--schema=public` เท่านั้น — schema อื่น (`auth`/`storage`/`realtime`) โปรเจกต์ปลายทางสร้างมาให้แล้ว ดัมป์ไปด้วยจะชนกัน
· 📌 แลกมาด้วยการ**ไม่ได้ `auth.users`** — พอสำหรับ `E7-AC8` แต่ถ้าต้องการบัญชีด้วยต้องเพิ่มทีหลัง

---

## 📌 `LAST BACKUP: No backups` — ยืนยันจาก dashboard จริง

free tier ไม่มี backup อัตโนมัติเลย · **ก่อน 28 ส.ค. 2026 ทริปจริงไม่มีสำเนาสำรองอยู่เลยแม้แต่ชิ้นเดียว**
· 🔴 **สำเนาแช่แข็งไม่ได้แทน PITR** — ข้อ (ข) ใน [`devops.md`](./devops.md) (อาจต้องจ่าย Pro ชั่วคราวตอนแปลง schema) ยังใช้ได้ทุกตัวอักษร
