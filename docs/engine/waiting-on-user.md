# ⏳ ของที่รอผู้ใช้ตัดสิน — อัปเดตล่าสุด 29 ส.ค. 2026 (P1)

> เขียนคืนที่ผู้ใช้ไปนอนและสั่งให้ทีมเดินงานต่อ · **ทุกข้อในนี้ทีมทำแทนไม่ได้**
> ไม่ใช่เพราะไม่รู้วิธี แต่เพราะเป็นการกระทำที่สิทธิ์อนุมัติเป็นของผู้ใช้เท่านั้น

---

## 🔴 กลุ่ม 1 — ต้องกดเอง (ทีมไม่มีสิทธิ์)

### 1.1 `push` ขึ้น `origin/platform`
`platform` ล้ำ `origin/platform` อยู่หลายสิบ commit จาก **6 เซสชัน** และไม่มีใคร push เลย
· **`D72`:** คน push รับผิดชอบว่า *หัวทั้งหัว* เขียว — ต้องรัน 11 คำสั่งของ CI ครบก่อน ไม่ใช่แค่ของตัวเอง
· ⚠️ ชุดเต็ม **เขียนฐาน dev** → ต้องประกาศก่อนรัน และถามว่ามีใครใช้ฐานอยู่ไหม
· **ทุกเซสชันต้องขออนุมัติจากผู้ใช้ของตัวเอง แยกกัน** — P1 ให้ไฟเขียวแทนไม่ได้ (P6 ทัก 2 รอบ · รับแล้ว)

### 1.2 สร้างโปรเจกต์ Supabase ใบที่ 2 — `E7-AC8` ②
**ลำดับที่ตกลงกับ P6 แล้ว ทำตามนี้:**
1. dashboard → org `Plan-trip-app` → **กด New project**
   · **กดได้** = โควตาไม่เคยเต็ม (FAQ ว่า *"Paused projects do not count towards your quota"* และ `muphoneseller` เป็น `INACTIVE`) → จบ ไม่ต้องแตะของใคร
   · **กดไม่ได้** = ส่งข้อความ error มา แล้วค่อยคุยเรื่อง `muphoneseller`
2. ถ้าต้องแตะ `muphoneseller`: **ปลุกขึ้นมาดูข้างในก่อน · ผู้ใช้เป็นคนลบเอง** — มันเป็นของคนละธุรกิจ (MU Phone)
3. 🔴 **หลังสร้างเสร็จ ต้อง seed ก่อน `db push`** — ไม่งั้นแดงที่ `20260826172600` ซึ่งปฏิเสธฐานเปล่าโดยตั้งใจ
   ขั้นตอนเต็มอยู่ที่ [`devops.md §17.4`](./devops.md) (P6 เขียน) — **ถ้าไม่รู้ล่วงหน้าจะอ่านว่า migration พัง**
· 🔴 **สร้าง org ใหม่เป็นทางสุดท้าย** — P6 อ่าน FAQ แล้วพบว่าโควตาน่าจะผูกกับ *บัญชี* ไม่ใช่ *org* → เปิด org ใหม่อาจไม่ได้ช่องเพิ่มเลย
· 📌 **ไม่ต้องรีบ** — `AC8` ① ปิดแล้ว (restore พิสูจน์ในเครื่องแล้ว) · ② ใช้ตอนซ้อม `AC1`/`AC6` เท่านั้น

### 1.3 ✅ **รันแล้ว** — `20260829020000_e7_hidden_places_accept_custom.sql`
ผู้ใช้รันบน `engine-dev` เมื่อ 29 ส.ค. (`Success. No rows returned`) แล้วเติมแถวใน
`supabase_migrations.schema_migrations` ตามที่ `rlsMatrix` ฟ้อง · **ปิดรูข้อมูลหาย 21 แถว**

### 1.4 🔴 รัน migration `20260829040000_e7_bookings_status_legacy_domain.sql` บน `engine-dev`
**รูที่สองของ `E7` — และคราวนี้ไม่ใช่คอลัมน์ที่ขาด แต่เป็น *โดเมนของค่า***

```
column-map.md:70    `status`  →  "คงเดิม"
e2_bookings.sql:67  ของจริง   →  check (status in ('todo','booked','cancelled'))

โดเมนเดิม   booked · pending · walk_up      (lib/supabase.ts:140)
ที่ลงจริง    todo   · booked  · cancelled
ทับกัน      **แค่ `booked`**
```
· ข้อมูลจริง 8 แถว = `pending` 5 · `walk_up` 3 → **ไม่มีแถวไหนใส่ได้เลยสักแถว**
· `walk_up` มีโค้ดอ่านอยู่ 3 ที่ (`supabase.ts:137` · `bookingStatus.ts:37,58` · `BookingEditModal.tsx:22`)
  และ **ไม่มีค่าปลายทางที่แทนได้** — `todo` = "ยังไม่ทำ" ตรงข้ามความหมาย · `booked` = "ทำแล้ว" ก็ผิด
· ตัดสิน: `column-map.md` เป็นฉบับผูกพัน → คืนเป็น `pending · booked · walk_up · cancelled`
  **ถอน `todo` ทิ้ง** เพราะมันคือ `pending` คนละชื่อ · วัดแล้ว**ไม่มีผู้บริโภคสักราย**
· ทดสอบในสนามซ้อมแล้ว ยิงครบ 4 ทิศในตัว migration เอง + ทิศแดงอีก 3 ใบจากฝั่งสคริปต์ย้าย
· ยังไม่ได้แตะ `engine-dev` — การแตะฐานด้วย credential จริงเป็นสิทธิ์ของผู้ใช้

### 1.5 🟡 ไฟล์รูปใน storage ของโปรเจกต์เก่า — **ยังไม่มีใครเขียนขั้นตอนนี้**
`trip_stops` 1 แถวถือรูปที่ `ejzibhgqhxdzkovsnpds` (**โปรเจกต์ที่ห้ามแตะทุกกรณี**)
· สคริปต์ย้ายแปลง URL → path ถูกต้องแล้ว (`<trip_id>/<ชื่อไฟล์>`) แต่ **ตัวไฟล์ยังอยู่ที่เดิม**
· ปลายทางต้องคัดลอกผ่าน **Storage API** — SQL ทำไม่ได้ (`e5_drop_trip_cover_storage.sql:55`
  บันทึกไว้ว่า Supabase บล็อก `delete from storage.objects` ที่ระดับฐาน · ฝั่ง insert ก็ทางเดียวกัน)
· **ไม่ด่วน** — 1 แถว · รูปเดียว · ไม่บล็อกอะไร แต่ถ้าไม่ทำ รูปจะหายไปเงียบ ๆ ตอนขึ้นระบบใหม่

### 1.6 🟡 รัน migration `20260829060000_e7_mode_domains.sql` บน `engine-dev`
**ไม่ด่วนเท่า 1.4 · ไม่บล็อก `E7` · แต่มันปิดช่องที่ *ทำงานอยู่แล้ววันนี้***

P3 ถามว่าฐานกันคอลัมน์โหมดเดินทางไหม (เขาเขียนเองว่า *"ต้องมีคนยืนยัน ไม่ใช่สันนิษฐาน"*) · วัดแล้ว:
```
travel_time_cache.travel_mode              ✅ in ('walk','transit','drive')
trip_stops.travel_mode                     ❌ ไม่มีเลย  ← ที่ที่ผู้ใช้เขียนจริง
trip_stops.intercity_mode                  ❌ ไม่มีเลย
trip_day_plan_settings.return_travel_mode  ❌ ไม่มีเลย
```
· **ใบที่มีด่านคือแคช ซึ่งไคลเอนต์เขียนไม่ได้อยู่แล้ว · ใบที่ไม่มีด่านคือใบที่ผู้ใช้เขียนได้**
· โค้ดประกาศ `TravelMode` เป็น union แล้ว index `Record<TravelMode,…>` ด้วยค่าจากคอลัมน์นี้ **12 จุด**
  → **โค้ดเชื่อว่าฐานกัน · ฐานไม่ได้กัน · ไม่มีใครเคยเทียบสองอย่างนี้จนวันนี้**
· 🔴 **จงใจไม่แตะ `*.category` ทั้งสามตาราง** — มันเป็น `length 1..40` และควรเปิดต่อไป
  (แพลตฟอร์มหลายประเทศต้องเพิ่มหมวดได้โดยไม่ migrate) · **ฝั่งที่ผิดคือโค้ด ไม่ใช่ฐาน**
· ทดสอบในสนามซ้อมแล้ว · ยิงทิศแดง 2 ใบ

### 1.7 🔴 **นำสำเนาแช่แข็ง `legacy.*` ขึ้น `engine-dev` — ยังไม่มีใครเขียนขั้นตอนนี้**

**ก้อน E7 ทั้ง 9 ใบอ่าน `legacy.*`** · ถ้าไม่มี schema นี้ ก้อน 01 จะล้มทันทีด้วย
`relation "legacy.trip_plans" does not exist`

🎯 **ผมเขียน `RUN.md` จากลำดับที่ผม *รัน* มา ซึ่งเริ่มหลัง `legacy` มีอยู่แล้วในสนามซ้อม**
— **ขั้นที่ผมไม่เคยต้องทำเอง คือขั้นที่ผมมองไม่เห็นว่าขาด** · เจอตอนเขียนก้อน 00 ไม่ใช่ตอนทบทวน `RUN.md`

**ของที่มีอยู่แล้ว:**
· dump แช่แข็ง 2 ไฟล์ในเครื่อง — `~/trip-schema.sql` · `~/trip-data.sql`
· สนามซ้อมสร้าง `legacy` ด้วย `alter schema public rename to legacy` **หลัง** restore เข้า `public`
  (`scripts/e7-local-rehearsal.sh:44-50`) — **ท่านี้ใช้กับ `engine-dev` ไม่ได้** เพราะ `public` ที่นั่นมีสคีมาแพลตฟอร์มอยู่

**ของที่ยังไม่มี:** วิธี restore dump เข้า **schema ชื่อ `legacy` โดยตรง** บน `engine-dev`
· 🔴 **ผมยังไม่ได้ทดสอบวิธีไหนเลย** จึงไม่เขียนขั้นตอนที่ยังไม่ได้ยิง
· ทางที่น่าจะได้ (ยังไม่ยืนยัน): แก้ `search_path`/`SET search_path` ใน dump · หรือ restore เข้า DB ชั่วคราวแล้ว `pg_dump --schema` ออกมาใหม่โดยเปลี่ยนชื่อ schema
· ⚠️ **และมันแตะฐานจริงด้วย credential จริง** → สิทธิ์ของผู้ใช้ ไม่ใช่ของทีม

**✅ ตรวจได้ว่าพร้อมหรือยังโดยไม่ต้องเดา:** รัน `supabase-platform/e7/00_preflight.sql`
**อ่านอย่างเดียว ไม่เขียนอะไรเลย** · บอกครบว่าอะไรขาด — schema · migration 2 ใบ · สแลกในคลัง 65+6 · ทริปซ้ำ · role

---

## 🟡 กลุ่ม 2 — ตัดสินใจ (ไม่ต้องกดอะไร แค่ตอบ)

### 2.1 ข้อความไทย hardcode — **3 จุดที่ยืนยันแล้ว** (P2 ไล่จนจบ)
```
components/CreateTripForm.tsx:124   >เริ่ม<
components/CreateTripForm.tsx:138   >สิ้นสุด<
components/HomeScreen.tsx:109       title="อ่านจำนวนสมาชิกไม่ได้…"
```
🔴 **ตัวที่สามไม่เคยอยู่ในรายการใคร** — เป็น `title=` ไม่ใช่ข้อความในเนื้อ จึงรอดสายตามาตลอด
**คำถาม:** แก้แค่ 3 จุดนี้ หรือเอา `CreateTripForm.tsx` ทั้งไฟล์ (~14 สตริงถ้านับ error/toast)
· ลำดับที่ตกลงกันแล้ว: **แก้ก่อน แล้วค่อยเปิดด่าน** ไม่ใช่เปิดด่านแดงแล้วให้คนไปปิด

### 2.2 P2 ขออนุมัติ commit ของค้าง 8 ไฟล์
เขาปฏิเสธคำสั่งของ P1 ที่บอกว่า *"commit ได้"* — **ถูกต้อง P1 ไม่มีสิทธิ์ให้**
· ของค้าง: `AnchoredPanel` · `CreateTripForm` · `Dropdown` · `HomeScreen` · `Modal` · `TripDestinationPicker` · `TripStatusFallback` · `lib/i18n.ts`
· **นี่คือคำถามถึงคุณ ไม่ใช่ของค้างของเขา** — เขารอคำตอบ ไม่ได้ค้างงาน

### 2.2 `postgresql@17` ที่ผมติดตั้งไว้ในเครื่อง
ใช้ทำ `AC8` ① · ตอนนี้ **ไม่มี service ที่จะเริ่มเองตอน boot** · ถอนได้ด้วย `brew uninstall postgresql@17`
· มี symlink 2 ใบที่ `/opt/homebrew/{share,lib}/postgresql@17` — **ไม่ได้ link ทับ `psql`/`pg_dump` ของ `libpq` ที่ใช้อยู่**
· เก็บไว้จะสะดวกตอนซ้อม migrate ในเครื่อง

### 2.3 ✅ **ปิดแล้ว 29 ส.ค. 2026** — P2 commit (`d61cf7e`) · P3 ถอด `countryOfCity()` ถาวร (`c62466d`)
· วัดตามวิธีที่จดไว้: `git grep` ที่ `HEAD` → **โค้ดจริงเหลือ 0** · `tsc exit=0` ที่หมุด · เทสต์ 36/36
· เก็บคอมเมนต์อธิบายไว้แทนตัวฟังก์ชัน — *ของที่ถูกถอดโดยไม่มีเหตุผลกำกับ จะถูกเขียนกลับ*

<details><summary>เนื้อหาเดิมของข้อนี้</summary>

เมื่อผู้ใช้อนุมัติให้ P2 commit `components/EmergencyCard.tsx` แล้ว:
**→ บอก P3 ทันที เขาจะถอด `countryOfCity()` ออกในคอมมิตเดียว**
· เขาใส่กลับเป็นการชั่วคราวเอง (`1885e58`) เพื่อให้หัว branch เขียว **และประกาศว่าจะเป็นคนเก็บเอง**
· 🔴 **เหตุผลเดิมที่ต้องถอดยังจริงทุกตัวอักษร** — ฟังก์ชันนี้ index `Record` ที่คีย์เป็น union โดยไม่มี fallback
  · คอมเมนต์ในไฟล์เตือนไว้แล้วว่า **อย่าเขียนผู้เรียกใหม่มาใช้มัน**
· 📌 วัดได้: `git grep -n "countryOfCity(" HEAD -- components/ app/ hooks/` → **โค้ดจริงต้องเหลือ 0** (ที่เหลือเป็นคอมเมนต์)

</details>

---

## 📋 กลุ่ม 3 — กติกาที่รออนุมัติวางลง `TEAM.md §3.4`

> 📌 **หัวข้อในกลุ่มนี้ใช้ *วันที่จริง* ไม่ใช่ "วันนี้"/"คืนนี้"** (P3 ชี้ 29 ส.ค. 2026)
> **เอกสารนี้อยู่ยาวกว่าวันที่เขียนมัน** — คำสัมพัทธ์จะกลายเป็นเท็จเงียบ ๆ พรุ่งนี้
> 🎯 รูปเดียวกับ *"เขียวที่ `<sha>`"* — ข้อความที่ถูกตอนพิมพ์ แล้วหมดอายุโดยหน้าตาไม่เปลี่ยน

> `TEAM.md` อยู่บน `main` เป็นฉบับที่ผูกพันของทั้ง 8 เซสชัน · **P1 ไม่วางเองตอนผู้ใช้ไม่อยู่**
> เหตุผล: *กติกาที่เพิ่มตอนคนอนุมัติไม่อยู่ จะกลายเป็นกติกาที่ไม่มีใครจำได้ว่าใครรับ*

### 3.1 ✅ **นิ่งแล้ว · ยิงพิสูจน์แล้ว** — ทิศแดงต้องยิงใน `git worktree` แยก

> 🔴 **ทิศแดงต้องยิงผ่าน *เส้นทางจริง* — และยิงใน `git worktree` แยก ไม่ใช่ทรีร่วม**
>
> ป้อนสตริงละเมิดเข้าตัวจับ พิสูจน์ว่า **ตัวจับ** ทำงาน · ไม่ได้พิสูจน์ว่า *เดินไฟล์ → อ่าน → ตัดคอมเมนต์ → จับ* ต่อกันติด
> — **ด่านที่ตัวจับดีแต่เดินไฟล์ไม่เจอ เขียวเหมือนด่านที่ผ่านทุกประการ**
>
> ```bash
> SHA=$(git rev-parse HEAD); W=<scratchpad>/probe
> git worktree add --detach "$W" "$SHA" && ln -s <ทรีจริง>/node_modules "$W/node_modules"
> cd "$W" && npx vitest run <ไฟล์ด่าน>              # ① ต้องเขียวก่อน
> printf '<ของละเมิด>' > <พาธในโซนสแกน> && ls <พาธ>   # ls = หลักฐานว่าของถูกวางจริง
> npx vitest run <ไฟล์ด่าน>                          # ② ต้องแดง และข้อความต้องระบุพาธที่วาง
> cd <ทรีจริง> && git worktree remove --force "$W" && git worktree prune
> ```
> · ⚠️ **"ต้องแดง" ไม่พอ ต้อง *แดงแล้วเรียกชื่อไฟล์ที่เราวางถูก*** — ไม่งั้นแยกไม่ออกจากด่านที่แดงด้วยเหตุอื่น
> · 🔴 **ต้องเขียวรอบแรกก่อนวางของละเมิด** — ด่านที่แดงอยู่แล้วด้วยเหตุอื่น ทำให้รอบสองไม่มีความหมาย
> · ✅ **`ln -s node_modules` พอสำหรับ `vitest`** — `cp -al` 508M เป็นราคาของ `build`/`tsc` เท่านั้น
> · 🎯 **ไม่มีขั้น "ลบไฟล์โพรบ" ให้ข้าม** — `worktree remove --force` พาไปเอง
>   **ท่าทรีร่วมมีขั้นนั้น และขั้นที่ข้ามได้จะถูกข้ามสักวัน** (รูปเดียวกับที่ `push` จาก worktree ชนะท่า `$SHA`)
> · **ข้อยกเว้นเดียว:** ด่านที่ต้องการของที่ `build` แล้ว → ยิงบนทรีร่วมได้ **แต่เป็นทางรอง**
>   *ราคาบนทรีร่วมเป็นภาระของคนอื่น · ราคาของ worktree เป็นภาระของตัวเอง*

**ที่มา:** P7 ร่าง · ฉบับแรกเขียนเฉพาะท่าทรีร่วม ซึ่ง **P4 ไม่ได้ใช้** (เขายิง 7 ใบใน worktree)
· P7 **ไม่รับข้อเท็จจริงนั้นมาเขียนต่อ — ยิงเองที่หมุด `0b7a26d`** แล้วพบว่าราคาที่ตัวเองกลัวเป็นของ `build` ไม่ใช่ของ `vitest`

### 3.2 ⏳ **ยังโตอยู่** — เกณฑ์ปิด AC ชนิด "ข้อห้าม"

ตอนนี้ 4 ข้อ (แดงเมื่อละเมิด · เคสควบคุมฝั่งบวก · ไม่มีของให้ตรวจ = แดง · **จักรวาลต้องมาจากคนละแหล่งกับอินพุตของ runner**)
· ขยับ 3 ครั้งใน 1 ชั่วโมง · P4 เสนอชั้นที่ 5 แล้ว (*ยัดของต้องห้ามแล้วเขียว → สงสัยโพรบก่อนสงสัยด่าน*)
🔴 **เส้นที่ตั้งไว้: ส่งฉบับที่มี ณ ตอนคุณตื่น ไม่ว่านิ่งหรือไม่** — เพราะ *"รอให้นิ่งก่อน"* ไม่มีเงื่อนไขจบด้วยตัวมันเอง (P7 ทัก · ถูก)

### 3.3 ✅ **นิ่ง** — `HEAD` กับ sha ของคนอื่น ใช้คนละงาน (P8 เสนอ · เกิดจริง 4 รอบ คืน 28→29 ส.ค. 2026)

> **จดสถานะปัจจุบันของไฟล์ → อ่านจาก `HEAD` เสมอ**
> **ยืนยันผลที่คนอื่นวัดไว้ → ปักหมุดจาก sha ที่เขารายงาน**
>
> เส้นแบ่ง: *"งานนี้กำลังตอบคำถามอะไร"* — ถ้าตอบ *"ตอนนี้ไฟล์เป็นยังไง"* ใช้ `HEAD` ·
> ถ้าตอบ *"ผลที่เขาวัดตอนนั้นถูกไหม"* ใช้ sha ของเขา
> 🔴 **การรับ sha มาในข้อความแล้วใช้เป็นฐานความจริงของสถานะปัจจุบัน คือจุดที่พลาดง่ายที่สุด**
> เพราะ **sha *ดูเหมือนความแม่นยำ*** — เจาะจงกว่า `HEAD` เลยรู้สึกปลอดภัยกว่า **ทั้งที่เป็นค่าที่ตายแล้ว**
>
> 🔴 **และขยาย `§3.3` ที่มีอยู่ (*"ห้ามเขียน sha ลงในประกาศ"*) ให้ครอบ *ประกาศหลังทำ* ด้วย**
> ข้อเดิมเขียนไว้กันการประกาศ**ก่อน**วัด · รูปเดียวกันกัดตอนประกาศ**หลัง**ทำ:
> P1 ใส่ sha ในทุกข้อความรายงาน → P8 ยึดตาม sha นั้น → P1 commit ต่อ → **บันทึกล้าทันที**

**หลักฐาน — เกิดจริง 4 รอบติดกันคืนนี้ กับด่าน `E2-AC8`:**
```
รอบ 1  P8 จด "3 ทาง"   → P1 ปิด B5      → ล้า
รอบ 2  P8 จด "5 ทาง"   → P1 ปิด B7      → ล้า
รอบ 3  P8 จด "5 ทาง"   → P1 ปิด B7      → ล้า
รอบ 4  P8 เช็ค HEAD ถูก → P1 ปิด B10/B11 → ล้า
```
🎯 **รอบ 4 คือหลักฐานที่สำคัญที่สุด: P8 ทำถูกทุกขั้นตามกติกาใหม่แล้วยังล้า**
· **วิธีหยุดคือคนที่ *ขยับ* หยุด ไม่ใช่คนที่ *จด* ตามให้ทัน** — ไม่มีวินัยฝั่งผู้จดข้อไหนแก้ได้ ถ้าของที่ถูกจดยังเดิน
· ✅ **และทางแก้ที่ใช้จริงคือถอดตัวเลขออกจากบันทึก** — จด *ชนิด* ของช่อง (ไม่เปลี่ยน) + ชี้ `git log --follow -- <ไฟล์>` (อัปเดตตัวเอง) · **P8 ทำแล้วที่ `80b0830` และลูปหยุดทันที**

### 3.4 ✅ **นิ่งแล้ว · วัดแล้ว** — `git worktree lock` แทนการห้าม `prune` (P7 เสนอ · P1 รับ · ถอนข้อเสนอเดิมของ P1)

**ที่มา:** P7 รัน `git worktree prune` แล้วเมทาดาทา worktree ของอีกเซสชันหายไป (ตรวจแล้วไดเรกทอรีนั้น
หายจากดิสก์ไปก่อนแล้ว · **ไม่มีไฟล์ใครหาย**) — แต่เปิดช่องที่ไม่มีในกติกา: **`prune` กระทำต่อของกลาง**

🔴 **ข้อเสนอแรกของ P1 ผิด และ P7 ค้านถูก:** *"ตรวจ `list` ก่อน"* / *"ตั้งชื่อพาธให้มีเจ้าของ"*
— ทั้งคู่ **ฝากการป้องกันไว้กับคนที่รัน `prune` ซึ่งเป็นคนเดียวที่ไม่รู้ว่าใบอื่นสำคัญแค่ไหน**

**✅ ถ้อยคำที่เสนอลง `TEAM.md §3.3`:**
> เมื่อกาง worktree เพื่อยืนยัน/push ให้ `git worktree lock <พาธ>` **ทันทีหลังกาง** · ปลดล็อกตอนจะลบเท่านั้น
> **`git worktree prune` ไม่ต้องห้าม** — ใบที่ถูกล็อกรอดตามนิยาม · ใบที่ไม่ถูกล็อกคือใบที่เจ้าของบอกว่าทิ้งได้
> 📌 **ใบล็อกที่ค้าง ถูกเจอด้วยกฎ *"ลบใบเก่าก่อนกางใบใหม่"* ที่มีอยู่แล้ว — สองข้อนี้ต้องอยู่ด้วยกัน**

**P7 วัดจริง ไม่ได้อ่านคู่มือ:** กาง 2 ใบ (ล็อก/ไม่ล็อก) → ย้ายไดเรกทอรีออก → `prune`
· ใบไม่ล็อก **ตายถาวรแม้คืนไดเรกทอรีกลับ** (`fatal: not a git repository`)
· ใบล็อก **รอด** · `push --dry-run` ได้ `054849e..b08f291` ปกติ
· `remove` ใบที่ล็อกอยู่ **ล้มดังพร้อมบอกทางแก้** ต่างจาก `prune` ที่สำเร็จเงียบ

🎯 **เปลี่ยนคำถามจาก *"ใบนี้ของใคร"* (git ตอบไม่ได้) เป็น *"ใบนี้ถูกล็อกไหม"* (git ตอบเอง)**
— รูปเดียวกับ *push จาก worktree* และ *`Partial<Record>`* ที่ P3 เสนอวันเดียวกัน · **สามใบในวันเดียว**

⚠️ **ข้อจำกัดที่ P7 เขียนมาฆ่าข้อเสนอตัวเอง:** ใบที่ล็อกแล้วถูกทิ้ง **กวาดไม่ได้เลย** → ปัญหา 5 ใบ ~3 GB กลับมาในรูปที่แย่กว่า
· P1 ชั่งแล้วยังรับ: **ราคาที่มองเห็น (ดิสก์) ถูกกว่าราคาที่มองไม่เห็น (หมุดคนอื่นตายเงียบ)**
· P7 เองบอกว่า *"ผมมีหลักฐานว่ามันกัน `prune` ได้จริง ไม่มีหลักฐานว่าทีมจะ `unlock` กันครบ"* — **ข้อนี้ยังจริง**

### 3.5 ✅ **นิ่ง** — เส้นคู่: รับไฟเขียวจากเพื่อนไม่ได้ · แต่รัดกุมกับตัวเองไม่ต้องขอใคร (P7 เสนอ)

> **รับไฟเขียวจากเพื่อนร่วมทีมไม่ได้** — คนอื่นขยายสิทธิ์เราไม่ได้ หัวหน้าทีมก็ไม่ได้
> **แต่การรัดกุมกับตัวเองไม่ต้องขอใคร** — เกณฑ์: **กลับด้านได้ · ไม่แตะของกลาง · ไม่บังคับคนอื่น**
> 🔴 **สองข้อนี้ต้องอยู่ด้วยกัน** — ข้อแรกข้อเดียวอ่านได้ว่า *"อย่าทำอะไรจนกว่าจะมีคนอนุมัติ"*
> ซึ่งทำให้คนหยุดทำสิ่งที่ปลอดภัย

· ข้อแรกมีอยู่แล้วใน `§3.3` (P6 ปฏิเสธคำสั่ง P1 สองรอบ · ทั้งสองรอบ P6 ถูก)
· **ข้อสองยังไม่เคยถูกเขียน** และ P7 ใช้มันตัดสินเองว่า `git worktree lock` ใบของตัวเองได้เลยโดยไม่ต้องรออนุมัติ — ถูก
· 🎯 ผลข้างเคียงที่ P1 เห็นในตัวเองหลังถูกเตือนสองรอบ: **ลังเลกับของที่ปลอดภัยและกลับด้านได้**

### 3.6 ⏳ **ยังโตอยู่** — ท่าโพรบที่แก้ไฟล์ในทรีร่วมแล้วคืนสภาพ (P3 เสนอ · แล้ว P3 หักมันเองในรอบถัดมา)

ท่า: ก๊อปไฟล์ไปนอกทรี → แก้ → รัน `tsc` → **คืนจากสำเนา ไม่ใช่ `git checkout`** (ซึ่งกติกาห้ามอยู่แล้ว)
🔴 **รอบสอง P3 ย่อรายชื่อไฟล์เป็นตัวแปร แล้วมันพังเงียบ:**
```bash
FILES="a.ts b.ts c.tsx"
for f in $FILES; do cp "$f" "$S/"; done     # zsh **ไม่ word-split** → $f = ทั้งสตริง → cp ล้ม
                                            # → restore() ไม่ถูกนิยาม → trap ไม่มีอะไรให้เรียก
```
· ทรีค้างสภาพโพรบ 4 ไฟล์ · **รอดเพราะบังเอิญมีสำเนาจากโพรบรอบก่อน ไม่ใช่เพราะ `trap`**
· ⚠️ **ใน `bash` โค้ดนี้ทำงานถูก** — ท่าล้มเพราะ *ความต่างของเชลล์* ไม่ใช่เพราะตรรกะผิด
· 📌 **P1 เจอกับดักเดียวกันในเซสชันเดียวกันวันนี้** (`PSQL="psql -h …"` แล้วเรียก `$PSQL` → `command not found`) — **สองครั้ง คนละคน วันเดียว เชลล์เดียวกัน**

**ถ้าจะยกเป็นท่ามาตรฐาน ต้องยกพร้อมสามข้อนี้ (P3 กำหนด):**
① **เขียนรายชื่อไฟล์เป็น literal ในลูป ห้ามใส่ตัวแปร** — ตรวจด้วยตาได้ ไม่ต้องรู้กฎ word-split ของเชลล์
② **หลัง `cp` สำรอง ให้ `ls` แล้วยืนยันจำนวนไฟล์ก่อนเริ่มแก้** — สำรองไม่สำเร็จต้องรู้ *ก่อน* แก้
③ `trap` ยังมี **แต่ห้ามพึ่งเป็นชั้นเดียว** — มันพึ่งฟังก์ชันที่อาจไม่ถูกนิยาม

### 3.7 ⏳ **ยังโตอยู่** — ประกาศว่า "ทำแล้ว" ก่อนทำจริง (P1 ทำ **6 ครั้ง** ระหว่าง 27–29 ส.ค. 2026 · ครั้งที่ 6 หลังเขียนกฎนี้แล้ว)

```
27–28 ส.ค.  บอก P7 ว่าจด E7-AC9 แล้ว          → ยังไม่จด
            บอก P4 ว่า "ส่ง P8 แล้ว"           → ยังไม่ส่ง
            บอกผู้ใช้ว่ากฎ HEAD/sha เป็นข้อ 8   → ยังไม่มีในรายการ
29 ส.ค.     บอก P7 ว่า "จดลง §3.5 แล้ว"        → grep = 0 ตอนพิมพ์
            บอก P3 ว่า "ส่ง P4 แล้ว"           → ยังไม่ส่ง (ในข้อความเดียวกับที่กำลังรับผิดข้อบน)
```
🔴 **ครั้งที่ 5 เกิดในข้อความที่กำลังรับผิดครั้งที่ 4** — แปลว่า *"ตั้งใจให้มากขึ้น"* ไม่ใช่ทางแก้
🔴 **และครั้งที่ 6 เกิด *หลัง* หัวข้อนี้ถูกเขียนลงไฟล์แล้ว** (บอก P3 ว่า *"ส่งต่อ P4 แล้ว"* · ส่งจริงในเทิร์นถัดมา)
→ **กฎฝั่งผู้เขียนไม่ได้ผลแม้แต่กับคนที่เพิ่งเขียนกฎนั้นเอง** · นี่คือหลักฐานที่แข็งที่สุดว่าฝั่งผู้อ่านต้องอยู่ถาวร

🔴 **และครั้งที่ 4 มีชั้นที่สองที่แย่กว่าตัวมันเอง — ผมเขียน sha ที่ไม่มีอยู่จริง (`e2f5ad4`)**
ของจริงคือ `d8e0e98` · **`git log e2f5ad4` คืนค่าว่าง** → คนที่ไปตรวจตามที่ผมบอกจะได้ความว่างเปล่า
**แล้วยิ่งมั่นใจว่าผมไม่ได้จด** · P7 เกือบส่งข้อกล่าวหาใบที่สามด้วยเหตุนี้ซ้อนกับจังหวะที่เขา `grep` ก่อน commit ลง
· 🎯 **นี่คือ *"ห้ามเขียน sha ลงในประกาศ"* ของ P4 (28 ส.ค.) เป๊ะ ๆ** — และผมยกกฎนี้ไปเตือนคนอื่นมาแล้วสองรอบในวันเดียวกัน
· 🔴 **ต่างจากการพิมพ์ผิดธรรมดา: sha ผิดไม่ได้แค่ *ไม่ช่วย* — มัน *ส่งคนไปตรวจผิดที่แล้วได้คำตอบที่ยืนยันข้อกล่าวหา***
· ✅ **ท่าที่ P7 เสนอและใช้ได้ทันที: ค้นด้วย *โครงสร้าง* ก่อน (`grep "^### "`) แล้วค่อยค้นด้วย *ถ้อยคำ*** — ถ้อยคำเป็นของคนเขียน โครงสร้างเป็นของไฟล์


---

### 3.8 ✅ **นิ่ง · เกิดจริง 29 ส.ค. 2026** — ข้อความที่ประกาศผล ต้องมาจากผล ไม่ใช่จากคนพิมพ์ (P3 · P4 · P1 คนละสื่อ)

P3 ทำ `tsc` แดงบนหัว branch 1 commit ด้วยบรรทัดนี้:
```bash
npx tsc --noEmit 2>&1 | head -3; echo tsc-clean && npx eslint … && git commit …
#                                ↑ `;` ไม่ใช่ `&&`     ↑ พิมพ์คำว่า clean เสมอ
```
· error โผล่ต่อหน้าเขาจริง ๆ **แล้ว commit ทับไปเลย เพราะบรรทัดถัดมาพิมพ์คำว่า `clean`**
· 🔴 **`| head` กลืน exit code ด้วย** — สองอย่างรวมกันทำให้ *ผลจริง* กับ *ข้อความ* ขาดจากกันสนิท

🎯 **รูปเดียวกับ *"ห้ามเขียน sha ลงในประกาศ"* ของ P4 เป๊ะ — คนละสื่อ:**
```
P4  sha ในประกาศ           → ยกเลขที่ไม่ได้วัดมาใช้
P3  `echo tsc-clean`        → พิมพ์คำว่าผ่าน โดยไม่ผูกกับผล
P1  `e2f5ad4` ที่ไม่มีจริง   → ส่งคนไปตรวจผิดที่แล้วได้คำตอบที่ยืนยันข้อกล่าวหา
```
**สามใบในวันเดียว · สามคน · สามสื่อ · รากเดียว: ข้อความถูกผลิตโดยคน ไม่ใช่โดยเครื่องมือ**

**✅ กฎ:** `npx tsc --noEmit && …` เท่านั้น · **ห้ามมี `| head` คั่น** (กลืน exit code) · **ห้ามมี `echo` ที่พูดแทนผล**

🔴 **แก้ 29 ส.ค. 2026 (เย็น) — กฎเดียวกันนี้มีอีกด้าน และ P1 ละเมิดด้านนั้นซ้ำ (P3 ชี้ให้รวมเป็นข้อเดียว)**

ผมส่ง `d94a4f9` ให้ P3 · ของจริง `30ba821` (`git cat-file -t d94a4f9` → `Not a valid object name`)
**สาเหตุ: ส่งข้อความ *ในบล็อกเครื่องมือเดียวกับ `git commit`*** → ตอนพิมพ์ sha **ผลลัพธ์ยังไม่กลับมา ผมจึงเดา**
· 🔴 เป็นครั้งที่สองในวันเดียว (ครั้งแรก `e2f5ad4` ส่ง P7) · และเลข 7 หลักที่เดา **หน้าตาเหมือนของจริงทุกประการ**

**✅ กฎรวม (P3 เสนอถ้อยคำ): *ห้ามอ้างค่าที่เครื่องมือยังไม่คืน***
```
ด้านหนึ่ง  ข้อความที่ประกาศผล ต้องมาจากผล        ← `echo tsc-clean` · notice ที่พิมพ์คำคุณศัพท์
อีกด้าน    ตอนไหนที่ *ผลยังมาไม่ถึง* แน่ ๆ         ← sha ในบล็อกเดียวกับ commit ที่สร้างมัน
```
· 🎯 **สองข้อนี้คือข้อเดียวกันมองจากคนละด้าน** — แยกเป็นสองหัวข้อเมื่อไหร่ จะต้องอ่านคู่กันถึงจะครบ
  ซึ่งเป็นรูปที่ `TEAM.md §3.5` เขียนไว้เองว่าแยกได้เฉพาะเมื่อ *เนื้อที่ต้องผ่าคือเหตุผล* · **เคสนี้ไม่ใช่**
· ✅ ท่าที่ P3 ใช้อยู่แล้วโดยไม่ได้ตั้งใจ: `git commit … && git log --oneline -1` **แล้วเขียนข้อความในเทิร์นถัดไป**
  — *"ไม่ได้เพราะระวัง แต่เพราะอ่านผลก่อนเขียนเสมอ"* · **นั่นคือเหตุผลที่เขาไม่เคยเจอปัญหานี้**

### 3.9 ✅ **นิ่ง** — "ผมน่าจะผิดเอง" คือสมมติฐานที่ถูกตรวจน้อยที่สุด (P7 · เจอกับตัวเอง)

P7 `grep` ได้ `0` → ถูกต้องตามจังหวะ (commit ยังไม่ลง) · แล้ว **ถอนข้อสรุปที่ถูกของตัวเอง** ด้วยคำอธิบายว่า *"ผมค้นผิดคำเอง"*
· เขามีผลวัดสองครั้งที่ขัดกัน แล้ว**เลือกคำอธิบายที่โทษตัวเอง แทนคำอธิบายว่าไฟล์เปลี่ยนใต้มือ** — ทั้งที่อย่างหลังคือสภาพปกติของทรีที่ 8 คนใช้ร่วมกัน
· 🔴 ***"ผมน่าจะผิดเอง" ฟังดูเหมือนความรอบคอบ จึงถูกตรวจน้อยที่สุด*** — รูปเดียวกับ *"ประโยคที่ฟังเหมือนความระมัดระวัง"* **แค่หันเข้าหาตัวเอง**
· 🎯 **ต้องอยู่คู่กัน: ระวังคำที่เข้าข้างตัวเอง *และ* คำที่โทษตัวเอง** — ใบหลังหายากกว่าและไม่มีใครค้าน

🔴 **แก้ 29 ส.ค. 2026 (ดึก) — และมันมีทิศที่สาม: *คำชม* (P1 ทำ · P3 ค้าน)**
P1 เขียนให้ P3 ว่า *"คุณทำตามกติกา ไม่ใช่คุณลืมกฎ"* — **เข้าข้างเกินจริง** · P3 ปฏิเสธ:
> **กติกาที่ผิดอธิบายว่าทำไมผมถึงไม่ได้ตรวจ · แต่ไม่ได้ทำให้การไม่ตรวจกลายเป็นเรื่องถูก**
> *"ไม่งั้นรอบหน้าที่กติกาถูกแล้ว ผมจะยังพลาดได้ด้วยเหตุผลเดิม"*

**ชั้นที่ P3 เพิ่ม และเป็นส่วนที่ใช้ตัดสินได้:**
> 🔴 **คำติที่ผิด จะถูกค้านทันทีโดยคนที่ถูกติ · คำชมที่ผิด *ไม่มีใครค้าน* เพราะทั้งสองฝ่ายพอใจ**
> — ต้องมีใครสักคนออกแรงสวน **และคนนั้นมีแค่คนถูกชม**

· 🎯 **คำชมจึงเป็นข้อความชนิดเดียวในทีมที่ไม่มีกลไกตรวจในตัวมันเอง** — ต่างจากคำติ ตัวเลข และผลรัน
· 📌 ครบสามทิศแล้ว: **เข้าข้างตัวเอง** (มีคนค้าน) · **โทษตัวเอง** (ไม่มีใครค้าน · P7) · **ชมคนอื่น** (ไม่มีใครค้าน · P1→P3)
**✅ ท่า: ผลวัดสองครั้งที่ขัดกัน → เทียบ `git log` ก่อน อย่าเพิ่งสรุปว่าใครผิด**
```bash
git log -S "<คำ>" -- <ไฟล์>      # ตอบทันทีว่าคำนั้นเข้ามาเมื่อไหร่
```

### 3.10 ✅ **นิ่ง · วัดแล้ว 2 ฝั่ง** — ของค้างในทรีร่วมทำให้ *เขียวหลอก* ได้ ไม่ใช่แค่แดงหลอก (P4 · 29 ส.ค. 2026)

`§3.3` เขียนไว้ทิศเดียว: **ของค้างของคนอื่น → ชุดเทสต์แดงทั้งที่ `HEAD` เขียว** (P4 เจอ 28 ส.ค.)
🔴 **29 ส.ค. เกิดทิศตรงข้ามเป๊ะ:**
```
P3 ถอด countryOfCity() ออกจาก data/emergency.ts   → commit 597237e
P2 ย้าย EmergencyCard ไปใช้ countryOfCitySlug()    → **อยู่บนดิสก์ ยังไม่ commit**

รันในทรีร่วม  → ไฟล์ที่แก้แล้วอยู่ตรงนั้น → เขียว → "ถอดสำเร็จ"
รันที่หมุด    → ตัวแก้ไม่อยู่ในนั้น       → tsc แดง 3 error
```
· ⚠️ **ไม่มีใครทำผิดสักขั้น** — P3 วัดผู้เรียก = 0 **จากทรีร่วม ซึ่งตอนนั้นถูก** · P2 กำลังพิมพ์อยู่
· 🔴 **ทิศนี้อันตรายกว่า:** แดงหลอกยังมีคนไปดู · **เขียวหลอกถูก commit แล้วเดินต่อ** และไปโผล่ที่ CI ของคนที่ push ทั้งหัว **ซึ่งไม่ใช่คนที่ทำ**
· 📌 **เกณฑ์นับเคสจับข้อนี้ไม่ได้เลย** (P4 ชี้เอง — เกณฑ์ ③ ของเขา) · สิ่งที่จับได้คือ **การรันที่หมุด ไม่ใช่ที่ทรี**

**✅ กฎ: ก่อนบอกว่า *"ผู้เรียกเหลือ 0"* / *"ไม่มีใครใช้แล้ว"* ต้องวัดที่ `HEAD` ไม่ใช่ที่ดิสก์**
```bash
git grep -n "<ชื่อ>" HEAD -- <พาธ>     # ค้นที่ commit ตรง ๆ **ไม่ผ่านดิสก์เลย** (P3 เสนอ)
```
· 🎯 **ถูกกว่าการกาง worktree มาก** และตอบคำถามที่ถูก: *"ที่ commit ที่จะกลายเป็นหัว มีใครเรียกไหม"*
  ไม่ใช่ *"บนดิสก์มีใครเรียกไหม"* ซึ่งเป็นคำถามที่ `grep` ธรรมดาตอบ
· ⚠️ **อ่านผลให้ครบ: แยกโค้ดจริงออกจากคอมเมนต์** — เคสนี้ `git grep` เจอ 6 บรรทัดใน 5 ไฟล์
  **แต่เป็นโค้ดจริงใบเดียว** (`EmergencyCard.tsx:29`) ที่เหลือเป็นคอมเมนต์ที่พูดถึงชื่อนั้น
  → *"เจอ 6 ที่"* กับ *"มีผู้เรียก 6 ราย"* เป็นคนละอย่าง · **เลขจาก `grep` เป็นเบาะแส ไม่ใช่จำนวน** อีกใบ

🔴 **เกิดครั้งที่สองในวันเดียวกัน (P3 · หลัง `§3.10` ถูกจดแล้ว)** — เขา commit `597237e` โดยรัน `tsc`
**ในทรีร่วมที่มี WIP ของ P2 อยู่** → เขียว · ที่ `HEAD` แดง 3 error
· ⚠️ **และเขารู้กฎข้อนี้อยู่แล้ว — ยกไปอ้างเองสองรอบวันนั้น** · ที่พลาดคือ **คิดว่ากฎใช้กับ *ชุดเทสต์* จึงไม่ได้เอามาใช้กับ `tsc`**
· 🎯 **กฎที่ผูกกับ *เครื่องมือ* แทนที่จะผูกกับ *คำถาม* จะไม่ถูกนำไปใช้กับเครื่องมือถัดไป**
  · **ทำนายได้ว่าใครหลุดต่อไป: `lint` · `npm run build` · `guards.sh`** — รันในทรีร่วมได้เหมือนกันทุกประการ
  · 🔴 **และสองตัวหลังให้ผลของ *ดิสก์* โดยธรรมชาติ ไม่ใช่โดยบังเอิญ** (P3 · วัดแล้ว):
    `guards.sh:396` ใช้ `find "$ROOT"` **สแกนไฟล์บนดิสก์ตรง ๆ** · `next build` อ่าน `.next/` ที่ค้างจากรอบก่อนได้
    (ยืนยัน: `.next/` มีอยู่จริงบนทรีร่วมตอนนี้)

🔴 **และข้อนี้ขัดกับคำแนะนำที่มีอยู่แล้วใน `TEAM.md §3.3` — ต้องแก้คู่กัน**
```
TEAM.md เขียนว่า:  worktree ใหม่จะฟ้อง `Cannot find name 'LayoutProps'` เพราะไม่มี `.next/types`
                   → **"ให้รัน `tsc` ในทรีจริงแทน"**
แต่การรัน tsc ในทรีจริง  คือสิ่งที่ทำให้ P3 รายงานเขียวขณะที่ HEAD แดง
```
**✅ ทางออกที่ P3 ค้นพบและใช้จริงแล้ว: `npx next typegen` ในตัว worktree ก่อน `tsc`**
· ยืนยันแล้วทั้งสองฝั่ง — P3 ใช้ตอนแก้ `1885e58` · P1 ใช้ตอนตรวจหัว (`tsc exit=0 · 0 บรรทัด`)
· 🎯 **มันถอด *เหตุผลเดียว* ที่ทำให้ต้องรันในทรีร่วมออก** → คำแนะนำเดิมใน `TEAM.md` หมดความจำเป็นและควรถูกแทน

🔴 **และคำตอบอยู่ใน `TEAM.md` ไฟล์เดียวกันอยู่แล้ว ห่างกัน 31 บรรทัด — สองบรรทัดขัดกันเอง**
```
TEAM.md:297   "ให้รัน `tsc` ในทรีจริงแทน"                        ← ทางแก้ที่ผิด
TEAM.md:327   npx next typegen   # ต้องรันก่อน tsc ไม่งั้น LayoutProps หาย   ← ทางแก้ที่ถูก
```
· บรรทัด 327 ถูกเพิ่มทีหลัง (ตอน P6 ไล่รายการ CI จากไฟล์จริง 28 ส.ค.) **แต่ไม่มีใครกลับไปถอนบรรทัด 297**
· 🎯 ***ทางแก้ที่ถูกถูกเพิ่มเข้ามา โดยที่ทางแก้ที่ผิดยังอยู่ที่เดิม*** — และคนอ่านเจอ 297 ก่อนเสมอ เพราะมันอยู่ในหัวข้อ *"ตัวหลอกใน worktree"* ซึ่งเป็นที่ที่คนไปหาเมื่อเจอปัญหานี้
· 🔴 **นี่คือใบที่สามของวันเดียวกันที่ *คำตอบอยู่ใกล้แล้วไม่ถูกอ่าน*:**
```
P1  เดาชื่อคอลัมน์  ขณะที่ `insert` เขียนชื่อจริงไว้    เหนือขึ้นไป 20 บรรทัด
P3  เติมคอมเมนต์    ขัดกับคอมเมนต์ที่มีอยู่             เหนือขึ้นไป  2 บรรทัด
P1  บันทึกว่าต้อง typegen  ขณะที่คำแนะนำตรงข้ามยังอยู่   ห่างกัน   31 บรรทัด
```
🎯 **P3 ตั้งข้อสังเกตที่อธิบายทั้งสามใบ: *คนที่กำลังเขียนของใหม่ อ่านของเก่าที่อยู่ติดกันน้อยที่สุด* — เพราะกำลังคิดเรื่องที่จะเขียน ไม่ได้กำลังคิดเรื่องที่มีอยู่**

### 3.13 ✅ **นิ่ง** — คนที่ทำให้หัวแดง เป็นคนเสนอทางแก้ชั่วคราวเอง (P3 เสนอถ้อยคำ · 29 ส.ค. 2026)

P1 ส่งข้อความหา P2 ว่า *"ด่วน — หัว branch แดง ไฟล์ที่แก้ได้มีไฟล์เดียวคือของคุณ"*
· **ถูกทุกตัวอักษร · หัวแดงจริง · ไฟล์นั้นของ P2 จริง** · แต่ P2 ต้องรออนุมัติจากผู้ใช้ของเขา → **แรงกดดันที่ไม่ควรมี**
· P3 คืนฟังก์ชันในไฟล์ *ของตัวเอง* แล้วประกาศว่า *"การ commit ของ P2 ไม่ใช่ตัวปลดบล็อกอีกต่อไป"*

> **✅ กฎ: คนที่ทำให้หัวแดง เป็นคนเสนอทางแก้ชั่วคราวเอง *ก่อน* ที่ใครจะไปขอให้คนอื่นแก้**

· 🎯 **เป็น *ลำดับการกระทำ* ไม่ใช่ *มารยาท*** (P3 ยืนยันถ้อยคำนี้เอง) — จึงตรวจได้ และไม่กลายเป็นข้อห้ามเรื่องน้ำเสียง
· 📌 ไม่ได้ห้ามบอกว่าใครแก้ได้ — **`D72` ข้อ 2(ก) ยังบังคับให้บอกเจ้าของเหมือนเดิม** · เพิ่มแค่ว่าใครควรขยับก่อน
· ✅ เขาแก้ด้วยการ **คืนของในไฟล์ของตัวเอง ไม่แตะไฟล์ที่ P2 ค้างอยู่** แล้วยืนยันที่หมุด — และประกาศว่า
  *"การ commit ของ P2 ไม่ใช่ตัวปลดบล็อกอีกต่อไป"* **ถอนแรงกดดันเวลาที่ตัวเองสร้าง**
· 🎯 คำถามที่แยกสองอย่างนี้: ***"อยู่บนดิสก์" กับ "อยู่ในประวัติ" ต่างกัน และคำว่า "แก้แล้ว" ไม่ได้บอกว่าอันไหน***
· 📌 P1 ยกรายงาน *"P2 ปิด `EmergencyCard` แล้ว"* ไปบอกทั้ง P3 และผู้ใช้ **โดยไม่แยกสองอย่างนี้** — จริงบนดิสก์ ไม่จริงในประวัติ

---

### 3.11 ✅ **นิ่ง** — คนรีวิวเอกสารที่มีคนกำลังแก้อยู่ ต้องบอก sha ที่ตัวเองอ่าน (P3 · 29 ส.ค. 2026)

P3 รีวิว `RUN.md` แล้วรายงานว่า *"`00_preflight` ไม่ถูกอ้างถึงเลย"* — **จริงตอนเขาวัด · เท็จตอนผมอ่าน · ห่างกัน ~8 นาที**
```
fcc1af0 17:52  ← ฉบับที่เขาอ่าน
b32525a 18:00  ← เพิ่ม "ขั้นที่ 0 — รัน 00_preflight.sql ก่อนเสมอ"
```
🎯 **นี่คือ *"เขียวที่ `<sha>` ไม่ใช่หัวเขียว"* ย้ายมาอยู่ฝั่ง *คนรีวิว*** — ทีมบังคับข้อนี้กับผลเทสต์มาตลอด **แต่ไม่เคยบังคับกับการรีวิวเอกสาร**
· 🔴 และ P3 **มีหลักฐานอยู่ในมือโดยไม่ได้ใช้** — เขารัน `git log -2 -- RUN.md` ในคำสั่งเดียวกับ `grep` แล้วเห็น `fcc1af0` อยู่หัว
**✅ กฎ: รีวิวไฟล์ที่มีคนกำลังแก้ ให้เขียนว่า *"ผมอ่าน `<ไฟล์>` ที่ `<sha>`"*** — คนรับเห็นทันทีว่าเก่าไปกี่ commit

### 3.12 ✅ **นิ่ง** — "แพตเทิร์นแคบกว่าคลาส" มีสามแบบ และแบบที่สามแก้ด้วยการขยายแพตเทิร์นไม่ได้ (P3)

```
แคบที่ *ชื่อ*   `CITY_META[` ไม่ครอบ `CITY_NAME_TH[`        → ขยายแพตเทิร์นแก้ได้
แคบที่ *รูป*    `countryOfCity()` ห่อ index ไว้ในฟังก์ชัน      → ต้องอ่าน *นิยาม* แทนที่ใช้
แคบที่ *สื่อ*    ค้น *เนื้อไฟล์* ขณะที่ข้อเท็จจริงอยู่ใน *ชื่อไฟล์*   → **ต้องเปลี่ยนเครื่องมือ**
```
· เกิดจริง: P3 ค้น `grep -rn "makes_days"` → `0` · ของจริงคือ `20260827080000_e5_create_trip_makes_days.sql`
  **เนื้อไฟล์ไม่มีคำนั้นเลยสักครั้ง** → คำสั่งนั้นหาไม่เจอ **ตามนิยาม ไม่ว่าจะรันกี่รอบ**
· 🔴 **สื่อในโปรเจกต์นี้มีอย่างน้อย 4:** ชื่อไฟล์ · เนื้อไฟล์ · ข้อความ commit · **แคตาล็อกของฐาน**
  — เจอครบ 3 ใน 2 วัน (P1 ต้องยิง `pg_constraint` ถึงรู้ว่าไม่มี `CHECK` · P3 ต้อง `ls` ถึงเจอ migration)
· ✅ **ถามก่อนค้น: *ข้อเท็จจริงนี้ถูกบันทึกไว้ในสื่อไหน*** — ไม่ใช่ *"ค้นด้วยคำว่าอะไร"*

🔴 **และมันมีคู่แฝด: *แพตเทิร์นที่กว้างเกิน* ให้ false positive** (P3 · เจอในคำสั่งตรวจผลงานตัวเอง)
```
grep -c "export function countryOfCity" data/emergency.ts   →  1   ← "ยังถอดไม่สำเร็จ?"
  ของจริง: แมตช์ `countryOfCitySlug` เพราะ **`countryOfCity` เป็นคำนำหน้าของมัน**
grep -cE "export function countryOfCity\("                  →  0   ← ถูก
```
· 🎯 ***ชื่อที่เป็นคำนำหน้าของชื่ออื่น ทำให้ `grep` ตอบเกินโดยไม่มีอะไรบอก*** — รีโปนี้มีคู่แบบนี้เยอะ
  (`countryOfCity`/`countryOfCitySlug` · `trip_day`/`trip_days` · `place_details_cache`/`place_details_local_cache`)
· ✅ **ใส่ตัวคั่นที่รู้ว่าต้องมี** (`\(` · `\b` · `:`) **ไม่ใช่พิมพ์ชื่อเปล่า ๆ**
· 📌 ครบทั้งสองด้านแล้ว: **แคบเกิน → นับขาด** · **กว้างเกิน → นับเกิน** · ทั้งคู่เงียบเท่ากัน

### 3.15 ✅ **นิ่ง** — ตัวชี้วัดที่ดีขึ้นเมื่อไม่มีใครทำงาน คือตัวชี้วัดที่กลับหัว (P2 ตั้ง · P3 คม)

P2 ทำตัวนับสตริงไทย hardcode ได้ **0 จุด** แล้ว **ปฏิเสธที่จะเปิดเป็น CI ด้วยตัวเอง**
· เหตุผลเขา: มันนับ *บรรทัดที่ diff แตะ* → ตอบ *"ของใหม่สะอาดไหม"* **ไม่ได้ตอบ *"ไฟล์สะอาดไหม"***
  · `CreateTripForm` ยังเหลือ ~11 สตริงที่ไม่โผล่ในตัวนับ **เพราะบรรทัดพวกนั้นไม่ถูกแก้**
· 🎯 **ชั้นที่ P3 เสริม: มันจะ *ดีขึ้นเรื่อย ๆ* โดยที่ไฟล์ไม่ได้สะอาดขึ้นเลย — ยิ่งไม่มีใครแตะบรรทัดเก่า ตัวเลขยิ่งสวย**
· 🔴 **เปิดเป็น CI = ประกาศว่ามันตอบคำถามที่มันตอบไม่ได้** · การที่คนสร้างตัวนับเป็นคนปฏิเสธเอง คือสิ่งที่ทำให้เลข 0 นั้นยังมีความหมาย
· 📌 และข้อสรุปของ P3 รอบนั้น (*"ไม่ใช่ความเสี่ยง"*) **บังเอิญถูกด้วยเหตุผลผิด** — เขาปฏิเสธที่จะนับว่าเป็นการวิเคราะห์ที่ใช้ได้ **ซึ่งถูก**

---


### 3.14 ✅ **นิ่ง** — แดงที่ชี้ผิดทาง แพงกว่าไม่แดง เมื่อมาพร้อม *คำอธิบายที่น่าเชื่อ* (P3 · 29 ส.ค. 2026)

เกิดจริง: `legacy` ว่าง → สิ่งเดียวที่ดังคือเคส `n_unprefixed = 0` ในก้อน 02
· ข้อความที่ออกมา: *"ทุกแถวมี prefix `custom-` แล้ว — เคสที่กันการเดาจากชื่อหมดอำนาจ ให้คนตัดสินใหม่"*
· **สมเหตุสมผล · มีทางเดินต่อ · และผิดสนิท** — ปัญหาจริงคือ restore ไม่ครบ

> 🔴 **แดงที่ *ไม่มี* คำอธิบาย จะถูกไล่หาสาเหตุ · แดงที่มีคำอธิบาย *ผิด* จะถูกเชื่อ**

· 🎯 **ต่างจากเขียวหลอกตรงที่เขียวหลอก *ไม่มีใครไปดู* · แดงชี้ผิดทาง *มีคนไปดู แล้วไปผิดที่***
  — เสียเวลาของคนที่ตั้งใจทำถูก **ซึ่งแพงกว่าในทีมที่ทุกคนไล่ตามหลักฐาน**
· 📌 กลับด้านกับสัญชาตญาณ *"แดงไว้ก่อนดีกว่า"* — จริงเฉพาะเมื่อข้อความ **ไม่อ้างสาเหตุ**
· ✅ **ท่า: เคสที่ดัง *โดยบังเอิญ* (ไม่ได้เขียนขึ้นเพื่อจับข้อนั้น) ต้องมีเคสที่เขียนขึ้นเพื่อจับข้อนั้นจริง ๆ มาคู่**
  — ไม่ใช่ปล่อยให้เคสบังเอิญทำหน้าที่แทน · เกิดจริงรอบนี้: เติมด่าน *"จำนวนแถวต่อตาราง"* ในก้อน 00

---


### 3.16 ✅ **นิ่ง** — เส้นแบ่งที่ถูกคือ *คีย์มาจากไหน* ไม่ใช่ *ถูกใช้หรือถูกแสดง* (P2 · 29 ส.ค. 2026)

P3 กับ P1 ตกลงกันว่าเส้นแบ่งคือ **ค่าถูก *ใช้* (ชนิดปิดได้) vs ถูก *แสดง* (ชนิดช่วยไม่ได้)**
🔴 **P2 ชี้ว่ามันยังไม่ใช่เส้นที่ใช้ตัดสินว่า *ต้องแก้จุดไหน*:**
```
คีย์มาจากฐาน   place.category · stop.intercity_mode · booking.category   → **ต้องแก้**
คีย์มาจาก union วนลิสต์เพื่อทำตัวกรอง/ตัวเลือก                            → **ปลอดภัยโดยโครงสร้าง**
```
· `ChecklistPanel:75,91` · `IntercityEditModal:139` · `TravelModeRow:85` วนจากลิสต์ของ union เอง
  → **แก้ไปก็สิ้นเปลือง และทำให้อ่านไม่ออกว่าจุดไหนคือจุดที่รับค่าภายนอกจริง**
· 🎯 เส้นเดิมบอกว่า *ชนิดช่วยได้ไหม* · เส้นนี้บอกว่า *มีอันตรายไหม* — **ต้องใช้คู่กัน ไม่ใช่แทนกัน**

🔴 **และจำนวนจุดที่ P1 รายงาน (4) เป็นสับเซต — ของจริงในโซน P2 คือ 23**
· 4 คือรูป `? TABLE[x] : DEFAULT` เท่านั้น · **P1 นับรูปประโยคเดียวแล้วรายงานเป็นจำนวนทั้งหมด** — `§3.12` อีกใบ

### 3.17 ✅ **นิ่ง** — ลอกรูปที่ใช้ได้ที่หนึ่ง ไปใช้อีกที่ ต้องถามว่า *ปลายทางกินอะไร* (P2)

`cityMetaOf` ใช้โทเคน CSS (`var(--content-soft)`) เป็นค่ากลางได้ · **`categoryMetaOf` ใช้ไม่ได้**
```
PlaceThumb.tsx:33   `${CATEGORY_COLOR[c]}22`   ต่อสตริงทำสีโปร่ง → `var(--x)22` เป็นขยะ
DayMapPanel         fillColor/strokeColor ของ Google Maps → ไม่ผ่านเครื่องมือ CSS ของเบราว์เซอร์
```
· 🎯 **สีเมืองเป็น CSS ล้วน · สีหมวดไม่ใช่** — ลอกทั้งดุ้นจะได้ของที่พังเงียบอีกใบ
· 📌 P2 หาเหตุผลจาก *โค้ดที่บริโภคค่า* ไม่ใช่จากความชอบ — และนั่นคือสิ่งที่ทำให้ข้อนี้ตรวจได้

---

## ✅ ที่ตอบไปแล้ว — ไม่ต้องตอบซ้ำ

| เรื่อง | คำตอบ | เมื่อ |
|---|---|---|
| `E7-AC5` — `added_by` แมปเข้าบัญชีไหน | **`claude` (57) · `P1` (1) · `Park` (4) → บัญชีผู้ใช้ทั้งหมด** · `legacy_added_by` เก็บสตริงเดิมครบ | 29 ส.ค. |
| `E7-AC3` — ตารางวัน `d0`–`d10` | **ยืนยันแล้ว** `d0`=11 ต.ค. … `d10`=21 ต.ค. | 29 ส.ค. |
| commit `e7-ac8-restore-runbook.md` | ✅ commit แล้ว (`d1a5e3e`) | 29 ส.ค. |
| commit `e7-ac2-fingerprint.sql` | ✅ commit แล้ว (`2eea0cb`) — ปิดการอ้างอิงที่ห้อยจาก `backlog.md` | 29 ส.ค. |

---

## 📌 ที่ทีมทำต่อได้เองคืนนี้ — ไม่ต้องรอ

`E6-AC5`/`AC9` เขียนตัวสแกน+เคสควบคุม (P3/P7) · ไล่ทิศ ② ในชุดเทสต์ (P4) · `E8-AC5` rate limit (P5)
· ไล่ทิศ ② ในโซน CI (P6) · commit ของค้าง (P2) · จดบันทึก (P8) · สคริปต์ migrate `E7` (P1)
