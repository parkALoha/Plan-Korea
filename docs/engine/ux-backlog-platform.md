# งาน UX/UI ที่ค้างอยู่ฝั่งแพลตฟอร์ม — **เจ้าของ: P7-Mobile (ผู้ใช้มอบ 4 ก.ย. 2026)**

> 🔴 **เดิมชื่อ `ux-backlog-p2.md` และเดิมส่งให้ P2 — เปลี่ยนเพราะผู้ใช้ย้าย P2 ไปทำ UX/UI บนทรี `main` จนจบ**
> P2 เป็นคนตรวจเองว่าไฟล์ทั้งหมดที่อ้างในเอกสารนี้ **มีเฉพาะทรี `platform`** ⇒ เอกสารนี้ไม่ใช่ของเขา
> · 🎯 **เปลี่ยนชื่อไฟล์ ไม่ใช่แค่แก้บรรทัดเจ้าของ** — ชื่อ `-p2` จะทำให้คนที่ `grep` หางานของตัวเองไม่เจอมัน
>   และ *งานที่ไม่มีใครหาเจอ กับงานที่ไม่มีใครถือ จบเหมือนกัน*
> · ⚠️ **บรรทัด "เจ้าของ" นี้เป็นคำบรรยายสภาพ มันหมดอายุได้** — ของที่ตรวจได้จริงคือ `git log -- <ไฟล์นี้>`

> **เขียนโดย P1 · 4 ก.ย. 2026** ตามที่ผู้ใช้สั่งให้จดไว้ ตอนมอบงาน UX/UI ใหม่ให้ P2 ทำเอง
> 🔴 **นี่คือ *ของที่ฝั่งข้อมูลเจอแล้วส่งต่อ* ไม่ใช่ลำดับความสำคัญ** — P2 จัดลำดับเอง
> · ทุกข้อมี `ไฟล์:บรรทัด` ให้ตรวจเอง **อย่าเชื่อสรุปในนี้โดยไม่เปิดต้นทาง**
>   (ทีมนี้พลาดเรื่องนี้มาแล้ว 3 ครั้งในวันเดียว — รายการที่ส่งต่อกันมาล้าเร็วกว่าที่ใครคาด)

---

## ① หมวด `sight` — คลังมี UI ไม่รู้จัก · **24.3% และเป็นหมวดที่ใหญ่ที่สุดในคลัง**

**สภาพ:** `data/places.ts:108` นิยาม 10 หมวด —
`culture · nature · beach · market · cafe · nightlife · viewpoint · shopping · restaurant · transport`
**`sight` ไม่อยู่ในนั้น** · ตัวนำเข้าคลังของผม (`scripts/catalog-suggest.py`) ใช้ `sight` เป็น *ค่าตกกลับ*
เมื่อ Google ไม่ให้ type ที่แมปได้ → แถวจำนวนมากได้ `sight` ไป

**สิ่งที่ผู้ใช้เห็นวันนี้:** `categoryMetaOf()` (`components/categoryMeta.ts:38`) ตกกลับไปที่
`UNSET_CATEGORY_META` = 📍 สีเทา ป้าย *"อื่น ๆ"*
· ✅ **การ์ดไม่หาย** — `PlaceSidebar.tsx:210` มีด่านกันไว้ตรง ๆ แล้วว่าหมวดนอก `CATEGORY_ORDER` ต้องไม่หายเงียบ
· 🔴 **แต่มันไปกองรวมในถังเทาถังเดียว** ⇒ ตัวกรองตามหมวดใช้กับของกองนั้นไม่ได้เลย

**ทำไมเป็นงานของ P2 ไม่ใช่ของฝั่งข้อมูล:** คำถามจริงคือ ***"นักเดินทางอยากกรองด้วยหมวดอะไร"***
ไม่ใช่ *"Google เรียกที่นี่ว่าอะไร"* · P2 ตัดสินชุดหมวด แล้วฝั่งข้อมูล (P5/P1) backfill ตาม
· ทางเลือกที่เห็น: **(ก)** เพิ่ม `sight` เป็นหมวดที่ 11 (พร้อม emoji/สี/ป้าย) ·
  **(ข)** ยุบเข้าหมวดเดิมทีละแถว · **(ค)** แยกเป็นหมวดย่อยใหม่หลายตัว
· 🔴 **ถ้าเลือก (ก) `CATEGORY_COLOR` ต้องเป็นเลขฐานสิบหกตรง ๆ ห้ามใช้ `var(--…)`** —
  เหตุผลอยู่ที่ `components/categoryMeta.ts:7-16` (Google Maps `fillColor` ไม่แปล custom property
  และ `PlaceThumb.tsx` ต่อสตริง `` `${color}22` ``) · **ข้อจำกัดของผู้บริโภค ไม่ใช่รสนิยม**

### 📊 การกระจายจริง — วัดจากฐาน dev 4 ก.ย. 2026 (`supabase db query` อ่านอย่างเดียว)
```
🔴 sight        224  24.3%   ← UI ไม่รู้จัก · ใหญ่กว่าทุกหมวดที่รู้จัก
   culture      221  24.0%
   shopping     174  18.9%
   market       103  11.2%
   nature        91   9.9%
   viewpoint     41   4.5%
   transport     28   3.0%
   restaurant    13   1.4%
   beach         11   1.2%
   cafe           8   0.9%
   nightlife      7   0.8%
   ── รวม 921 แห่ง · 51 เมือง · 7 ประเทศ (supported ครบทั้ง 7) ──
```
🔴 **แก้ตัวเลขของตัวเอง: ฉบับแรกของเอกสารนี้เขียน "~20% · 152 แถว" ซึ่งผิด** —
เลขนั้นวัดตอนคลังยังเล็กกว่านี้ แล้วผมยกมาใช้กับคลังปัจจุบันโดยไม่ได้วัดใหม่
· 🎯 **รูปที่ทีมนี้จดไว้แล้ว: *เลขที่วัดไว้เมื่อวาน วางข้างเลขที่วัดวันนี้ แยกไม่ออก*** — ผมเพิ่งทำเอง

### 🔴 และตัวเลขชุดนี้เผยของที่ผมไม่ได้ตั้งใจมอง — **คลังนี้ไม่มีของกิน**
`restaurant` 13 · `cafe` 8 · `nightlife` 7 — **รวมกัน 3.1%** ของทั้งคลัง
เพราะตัวนำเข้าของผมค้นแค่ 3 ชนิด: `tourist_attraction` · `market` · `shopping_mall`
⇒ **คลัง "สถานที่แนะนำ" ที่ผู้ใช้ขอ ตอบได้แค่ *ไปดูอะไร* ตอบ *กินอะไร* แทบไม่ได้เลย**
· ไม่ใช่ข้อบกพร่องของ UI — **เป็นช่องว่างของข้อมูล** ผมส่งให้ P5 แล้ว (เขารับงานหาสถานที่ต่อจากผู้ใช้)
· 📌 แต่มันเปลี่ยนงานออกแบบของคุณ: ถ้าจะทำตัวกรองตามหมวด **วันนี้มี 3 หมวดที่แทบไม่มีของอยู่ในนั้น**

---

## ② `TripPlanScreen` ยุบ *"ทริปนี้ไม่มีเมือง"* กับ *"ถามไม่ได้"* เป็นค่าเดียว

**หลักฐานเขียนไว้แล้วในโค้ด — ไม่ใช่ข้อสังเกตใหม่ของผม:**
`hooks/useTripCatalogCities.ts:85-89` เขียนตรง ๆ ว่า

> เปิดทริปนี้ครั้งแรกตอนออฟไลน์ → ไม่มีอะไรในเครื่องให้ตอบ → `status: "error"`
> และ `TripPlanScreen` **ยังตกไปที่ `ITINERARY` เหมือนเดิม** เพราะมันยุบสองสภาพนี้เข้าด้วยกัน
> → **นั่นเป็นของที่ต้องแก้ที่ `components/` (โซน P2) ไม่ใช่ที่นี่**

**อาการที่ผู้ใช้เจอ:** เปิดทริปแพลตฟอร์มครั้งแรกตอนไม่มีเน็ต → เห็นข้อมูลทริปเกาหลีของเว็บเดิม
แทนที่จะเห็นข้อความว่าโหลดไม่ได้ · **ผิดแบบเงียบ ๆ ซึ่งแย่กว่าแสดง error**

**"เสร็จ" หน้าตาแบบไหน:** สามสภาพต้องแยกกันได้จากจอ — *กำลังโหลด* · *ไม่มีเมืองจริง ๆ* · *ถามไม่ได้*

---

## ③ 🛂 มุมมองวีซ่า/ตม. ผูกกับ **ประเทศ** — ห้ามยุบ `hk`/`mo` เข้า `cn`

`catalog_countries` มี `cn` · `hk` · `mo` เป็น **สามประเทศแยกกันโดยตั้งใจ** (migration `20260904040000`)
เหตุผล: **กฎเข้าเมืองต่างกันจริง** และแอปมีมุมมอง 🛂 ที่ผูกกับ *ประเทศ*
· 🔴 ถ้า UI จัดกลุ่มให้ดูสวย ("จีน" อันเดียวแล้วมีฮ่องกง/มาเก๊าอยู่ข้างใน) **ต้องไม่ทำให้ค่าที่ส่งเข้ามุมมอง 🛂 เปลี่ยน**
· รูปเดียวกับที่ `E4-AC3` ห้ามถาม *"ประเทศนี้คือเกาหลีไหม"* — จัดกลุ่มเพื่อ *แสดง* ได้ · เพื่อ *ตัดสิน* ไม่ได้

---

## ④ ผิวหน้า "แนะนำสถานที่" ออกแบบตอนคลังมี ~200 แห่ง · ตอนนี้ 900+

**สิ่งที่เปลี่ยนใต้มือ (4 ประเทศ 42 เมือง → 7 ประเทศ 51 เมือง และ P5 กำลังเพิ่มต่อ):**
· `components/PlaceSidebar.tsx` (571 บรรทัด) **ไม่มีช่องค้นหา ไม่มีการแบ่งหน้า** —
  จัดกลุ่มตามหมวดอย่างเดียว (`groupPlaceCards`, บรรทัด 214)
· เมืองยอดฮิตบางเมืองมีสถานที่ 30–50 แห่ง ⇒ เลื่อนยาวมาก และ ~20% อยู่ในถังเทา (ข้อ ①)

**คำถามออกแบบที่เป็นของ P2 จริง ๆ:** ผู้ใช้เปิด sidebar มาเพื่อ *เลือกที่จะไป* —
จะให้เขาเห็น **อะไรก่อน** เมื่อของมี 50 ชิ้น (ยอดนิยม? ใกล้ที่พัก? ยังไม่ได้ใส่ในแผน?)
· 📌 ฝั่งข้อมูลมี `google_place_id` ครบทุกแถวแล้ว → ดึงคะแนน/จำนวนรีวิวมาจัดอันดับได้ **ถ้า UI ต้องการ**
  ⚠️ **แต่จำนวนรีวิวเทียบข้ามประเทศไม่ได้** (จีนแผ่นดินใหญ่ต่ำผิดปกติเพราะ Google ถูกบล็อก —
  พระราชวังต้องห้าม 16,784 vs ปราสาทโอซาก้า 99,313) · **เรียงข้ามประเทศด้วยเลขนี้ = ผิด**

---

## ⑤ ของที่ P2 เคยจับได้เอง และผมอยากให้ถือต่อ

28 ส.ค. P2 เป็นคนพบว่า **`/api/engine/countries` คืน 4 ประเทศ ทั้งที่ฐานมี 7** —
เพราะ migration ที่ผมเขียนลืมตั้ง `supported = true` ⇒ **119 สถานที่มองไม่เห็นจากฝั่งผู้ใช้**
· 🎯 assert ของผมสามข้อผ่านหมด **เพราะทุกข้อถามว่า "แถวอยู่ในตารางไหม" ไม่มีข้อไหนถามว่า "ผู้ใช้เห็นไหม"**
· ⇒ **เวลาฝั่งข้อมูลบอกว่า "ลงคลังแล้ว" ให้ถามกลับเสมอว่ายิง endpoint จริงแล้วนับได้เท่าไร**
  นั่นคือคำถามที่ฝั่งข้อมูลถามตัวเองไม่ได้ตามโครงสร้าง

---

## ⑥ พื้นสีเมเปิลตกเกณฑ์ contrast — **และวิธีที่ผมจัดรายการไว้ตอนแรก *ผิด* สองใบ**

**ตัวเลขที่วัดแล้วสามคน (P1 · P2 · P7) ตรงกันทุกตัว:**
```
ขาว   #ffffff  บน maple      #d9683a   3.50:1   🔴
cream #fdf6ec  บน maple      #d9683a   3.27:1   🔴  ← **แย่กว่าขาว** · ผมมองข้ามไปตอนแรก (P7 จับ)
ขาว   #ffffff  บน maple-dark #b8502a   4.98:1   ✅
cream #fdf6ec  บน maple-dark #b8502a   4.64:1   ✅
ขาว   #ffffff  บน #a8342a (สีเตือนที่ P2 เลือกไว้แล้ว)  6.58:1   ✅
```
`app/globals.css:8-9` ของทรีนี้มีค่าเดียวกับ `main` เป๊ะ ⇒ **ตกโดยอัตโนมัติ**

### 🔴 ผมเคยเขียนไว้ตรงนี้ว่าใบไหน "ไม่ต้องแก้" — **สองในสามใบผิด และมันผิดด้วยกลไกที่ผมเตือนไว้เอง**
```
❌ ผมเขียนว่า  TripHeader.tsx:161 = "พื้นอวาตาร์ ไม่ต้องแก้"
✅ ของจริง     name={m.displayName ?? "?"} บน text-[11px] · เป็นตัว "?" ตอนอ่านชื่อไม่ได้
              = **ข้อความขนาดปกติ ต้องการ 4.5** · cream บนเมเปิล 3.27 → **ต้องแก้**

❌ ผมไม่เห็นเลย PlanEditModal.tsx:61  `bg-maple` บรรทัด 61 · `text-white` **บรรทัด 60**
✅ ของจริง     ปุ่มส่งฟอร์มที่มีข้อความเต็ม → **ต้องแก้** · การสแกนทีละบรรทัดมองไม่เห็น

✅ ที่ผมถูก    TripHeader.tsx:133  จุดแจ้งเตือน 2.5px ไม่มีข้อความจริง → ไม่ต้องแก้
```
🎯 **และผมเขียนคำเตือนที่กันเรื่องนี้ไว้ในเอกสารฉบับเดียวกัน:**
> *"`bg-maple` บอกว่าใช้สีอะไร ไม่ได้บอกว่ามีข้อความอยู่บนนั้นไหม — จัดสมาชิกออกจากหมวดด้วยชื่อคลาสไม่ได้"*

· ใบ `:161` ผมจัดออกด้วย **คำอธิบายว่ามันคืออะไร** (*"พื้นอวาตาร์"*) — ซึ่งเป็น **ชื่อ ไม่ใช่คุณสมบัติ**
· ใบ `:61` หลุดเพราะ **คุณสมบัติอยู่คนละบรรทัดกับคลาส**
· 🔴 ***คำเตือนที่ถูกต้อง ไม่ได้กันคนเขียนมันเอง — เพราะตอนจัดของ เราคิดด้วยชื่อ ไม่ได้คิดด้วยคุณสมบัติ*** (P7 ชี้)

### 🔴 ⇒ ด่านอัตโนมัติแบบสแกนบรรทัด **ทำไม่ได้** — และเรามีหลักฐานทั้งสองขั้วแล้ว
```
พลาดของจริง    PlanEditModal.tsx:61   สีกับข้อความอยู่คนละบรรทัด
แดงใส่คนทำถูก  TripHeader.tsx:133     จุด 2.5px ที่ไม่มีข้อความ
```
⇒ เข้าเงื่อนไข *"อย่าสร้าง"* ของ `TEAM.md §3.4` ตรงตัว
· ✅ **ทางที่เหลือ: แก้ที่โทเคน** — ทำให้ค่าที่ใช้เป็น *พื้นของข้อความ* ผ่านเอง ไม่ใช่ไล่จับผู้ใช้คลาส (P7 เสนอ)
· 📌 **จำนวนบรรทัดที่ `grep` ได้ไม่ตรงกันระหว่างผมกับ P7 (28 vs 26)** — ยังไม่ได้ไล่ว่าต่างกันตรงไหน
  🔴 **และนั่นคือเหตุผลที่ห้ามใช้ตัวเลขนี้เป็นเช็กลิสต์** — `grep` เป็นเบาะแสว่าต้องไปดูตรงไหน ไม่ใช่จำนวนงาน

---

## ⑦ เลขลำดับบนหมุดแผนที่อ่านไม่ออก 2 หมวด (P2 เจอบน `main` · ยืนยันตัวเลขแล้ว)

ถ้าทรีนี้ใช้ `CATEGORY_COLOR` ชุดเดียวกัน (หรือ `categoryMeta.ts` ที่ derive จากมัน) ปัญหาเดียวกัน:
```
เลขลำดับสีขาวบนหมุด
  viewpoint  #c39338   2.78:1   ❌ ตกแม้แต่เกณฑ์ 3:1 ของสัญลักษณ์
  restaurant #d9683a   3.50:1   ❌
  อีก 8 หมวดผ่าน — ต่ำสุด market #c94f2e ที่ 4.52
```
✅ **ทางแก้ที่ไม่ต้องแตะพาเลตต์: เปลี่ยน *สีตัวเลข* ไม่ใช่สีหมุด** — เลือกขาวหรือ `#1a1512` อันที่คอนทราสต์สูงกว่า
⇒ ผ่านครบ 10 หมวด แย่สุด 4.52 · มีแค่ 2 หมวดที่สลับเป็นหมึก · โค้ดพร้อมใช้ที่ `components/markerLabel.ts` ของ P2
· ⚠️ **`--color-ink` ปกติ (`#2b241f`) ไม่พอ** — บน `#d9683a` ได้ **4.36** (ผมวัดเอง ตรงกับ P2) ต้องเข้มกว่านั้น
· 🔴 **และ `CATEGORY_COLOR` ต้องเป็น hex ตรง ๆ ห้ามย้ายเข้า `var(--…)`** (`categoryMeta.ts:7-16`) —
  แผนเดิมของ P2 เขียนว่า *"ย้ายเข้าระบบโทเคน"* ซึ่ง**ผิด** ถ้าทำตามหมุดจะหายทั้งแผนที่
