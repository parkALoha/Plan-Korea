"use client";

import { useEffect } from "react";

/**
 * ล็อกไม่ให้หน้าเว็บข้างหลังเลื่อนตอนเปิดชั้นซ้อน (modal · lightbox · sidebar)
 *
 * ## 🔴 นับจำนวนผู้ถือ ไม่ใช่ save/restore ต่อคน (5 ก.ย. 2026 · P4 สังเกต · P7 ไล่ · P2 ตัดสินและแก้)
 * ฉบับเดิมเก็บ `original` **ของใครของมัน** แล้วคืนค่าตอน unmount
 * ⇒ **ถูกต่อเมื่อ *ลำดับ cleanup เป็นการกลับด้านของลำดับ mount* เท่านั้น**
 *
 * 🔴 **ฉบับแรกของบรรทัดนี้เขียนกลไกไว้ *กลับด้าน* — "ตัวในสุดถอนก่อนเสมอ" ซึ่งเป็นเท็จ**
 * (P7 ส่งมาโดยไม่ได้วัด · P2 เอามา commit · **P4 ยิงจริงแล้วหักให้** — React 19.2.8)
 * ```
 * mount    วิ่ง **ลูกก่อน**  ⇒ ลูกเป็นคนจับค่าบริสุทธิ์ ""
 * cleanup  วิ่ง **พ่อก่อน**  ⇒ ลูกเป็นคนคืน **ทีหลังสุด**
 * ```
 * 🎯 ***มันปลอดภัยเพราะ mount กับ cleanup เป็นภาพสะท้อนกัน ⇒ คนที่เขียนทับเป็นคนสุดท้าย
 *    คือคนที่จับค่าบริสุทธิ์ไว้*** — **ไม่ใช่เพราะ "ตัวในสุดถอนก่อน"**
 * · 🔴 **React ให้การกลับด้านนั้นกับ *บรรพบุรุษ–ลูกหลาน* — ไม่ให้กับ *พี่น้อง*** (พี่น้อง cleanup ตามลำดับเดิม ไม่ใช่ย้อน)
 * · ⚠️ ***ข้อสรุปถูก แต่คำอธิบายผิด*** คือรูปที่อันตรายที่สุด — ไม่มีอะไรให้จับผิด และ **คนมารีแฟกเตอร์จะใช้คำอธิบายนั้นตัดสินใจ**
 *   เขาจะอ่านว่า *"ตราบใดที่ลูก cleanup ก่อนก็ปลอดภัย"* แล้วสร้างเคสที่พังโดยเชื่อว่าอยู่ในเงื่อนไขปลอดภัย
 * ```
 * PlaceDetailModal:218  <PhotoLightbox> อยู่ **ใน** <Modal>   → ลูก
 * PlaceSidebar          เรนเดอร์ <PlaceDetailModal> → <Modal>  → ลูก
 * BookingEditModal:481  <PhotoLightbox> ในตัวมันเอง           → ลูก
 * ```
 * 🎯 ***มันปลอดภัยด้วย *รูปทรงของต้นไม้คอมโพเนนต์* ไม่ใช่ด้วยกลไกของตัวเอง***
 * 🔴 **และวันที่มีคนย้าย `<PhotoLightbox>` ออกมาเป็น *พี่น้อง* ของ `<Modal>` — ซึ่งเป็นรีแฟกเตอร์ที่อ่านแล้วดูดีขึ้น —
 *    ลำดับ unmount ไม่ถูกรับประกันอีกต่อไป ⇒ ตัวนอกถอนก่อนจะ `restore("")` **ทั้งที่ยังมีชั้นเปิดค้างอยู่**
 *    ⇒ หน้าเลื่อนได้ใต้ชั้นที่เปิดอยู่ · **`tsc`/`lint`/เทสต์ ไม่มีตัวไหนส่งเสียง**
 *
 * ## ✅ ตัวนับปิดช่องนั้นโดยไม่ต้องรู้ว่าใครเป็นลูกใคร
 * ```
 * 0 → 1   เก็บค่าเดิมครั้งเดียว แล้วล็อก
 * 1 → 2   ไม่ทำอะไร (ล็อกอยู่แล้ว)
 * 2 → 1   ไม่ทำอะไร — **ยังมีคนถืออยู่**   ← จุดที่ฉบับเดิมปลดล็อกผิดจังหวะได้
 * 1 → 0   คืนค่าเดิมที่เก็บไว้ตอน 0 → 1
 * ```
 * 🎯 ***เปลี่ยนคำถามจาก "ใครถอนก่อน" เป็น "ยังมีใครถืออยู่ไหม" — คำถามที่สองไม่ขึ้นกับลำดับ***
 *
 * ## ⚠️ ขอบเขต — เขียนเพราะข้อจำกัดที่ไม่ได้เขียน จะถูกอ่านว่าไม่มี
 * · ตัวนับเป็น **module-level** ⇒ **หนึ่งตัวนับต่อหนึ่ง JS realm**
 *   ✅ **`iframe`/`window.open` ปลอดภัย เพราะได้ realm แยก ⇒ ได้โมดูลของตัวเอง ⇒ `holders` เริ่มที่ 0 แยกกัน**
 *      (วันนี้มี `<iframe>` 2 ใบ · `window.open` 2 จุด — **ไม่มีใบไหนเข้าเงื่อนไข**)
 *   🔴 ตัวที่พังแคบกว่านั้นมาก: ***realm เดียว เขียน `document.body` มากกว่าหนึ่งใบ*** —
 *      `createPortal(node, otherDoc.body)` จากคอมโพเนนต์ที่เรียกฮุคนี้
 *      · วัดแล้ว: `createPortal` มี 2 จุด (`Modal.tsx` · `AnchoredPanel.tsx`) **ชี้ document หลักทั้งคู่** ⇒ 0 จุดที่เข้าเงื่อนไข
 *   🎯 ***ฉบับแรกของบรรทัดนี้เขียนว่า "iframe/popup จะพัง" ซึ่งกว้างเกินจริง*** (P7 ชี้ · เขาไล่ของจริงมาหักให้)
 *      ⇒ คนถัดไปที่เปิด `GoogleMapEmbed.tsx` แล้วเจอ `<iframe>` **จะสรุปว่าละเมิดไปแล้ว** หรือแย่กว่านั้นคือ *ชินแล้วเลิกอ่าน*
 *      · `§3.4`: **กลไก (และคำเตือน) ที่แดงใส่คนที่ทำถูก จะถูกเลิกเชื่อทั้งใบ**
 * · ✅ **เคส "พี่น้อง" ไม่ใช่ของสมมติอีกแล้ว — P4 ยิงแล้วมันพังจริง**
 *   ```
 *   พี่–น้อง:  cleanup พี่ → ""  ·  cleanup น้อง → "hidden"   ⇒ ค้างที่ "hidden" 🔴
 *   ```
 *   ⇒ ***ตัวนับนี้ไม่ได้กันของสมมติ มันกันของที่วัดแล้วว่าพัง*** · และเคสนั้นเป็น **ทิศบวก** ของการวัดด้วย
 *     (ถ้าเครื่องวัดพิมพ์ `""` เสมอ ผลของเคสพ่อ–ลูกจะไม่มีความหมาย — อันนี้พิมพ์ `"hidden"` ออกมา)
 *   · `createPortal` **ไม่เปลี่ยนลำดับ** — วัดแล้ว log เหมือนกันทุกบรรทัด
 * · ⚠️ **ขอบเขตของผลนั้น (P4 เขียนเอง · ห้ามยกไปเขียนว่า "วัดครบแล้ว"):** วัดใน **ต้นไม้สังเคราะห์**
 *   ไม่ใช่คอมโพเนนต์จริงของแอป · **ไม่ได้วัด** App Router transition / `<Suspense>` /
 *   เคส *ปิดลูกก่อนแล้วค่อยปิดพ่อ* ซึ่งเป็นเคสที่ผู้ใช้ทำบ่อยที่สุด
 *
 * ## ✅ StrictMode — **เปิดอยู่จริง และการวัดของผมเกิดใต้มันไปแล้วโดยไม่รู้ตัว** (P7 ตั้งคำถาม · P2 วัด)
 * ```
 * วัดยังไง: /explore ยิง GET /api/engine/countries จาก useEffect([]) → **2 ครั้ง ห่างกัน 1ms**
 * ⇒ dev double-invoke ทำงานอยู่ (next.config ไม่ได้ตั้ง · app/layout ไม่ได้ครอบ — **แต่ Next เปิดให้ใน dev**)
 * ```
 * 🔴 **ลำดับที่ P7 กังวลเกิดจริง**: `mountA mountB cleanupA cleanupB mountA mountB` ⇒ `holders` **แตะ 0 กลางทาง**
 * ✅ **แต่มันไม่ทำให้ค่าเพี้ยน** — ตอนแตะ 0 เราคืน `originalOverflow` แล้ว *เก็บใหม่ทันที* ซึ่งเป็นค่าที่เพิ่งคืนไป
 *    ⇒ ค่าปลายทางเท่าเดิม · และ React รัน cleanup+remount **ในคอมมิตเดียว ไม่มีเฟรมให้วาดคั่น**
 * 🎯 ***และการทดสอบเปิด/ปิดโมดัลที่ผมรันตอน `af61da8` **เกิดใต้ StrictMode อยู่แล้ว** — ผมแค่ไม่รู้***
 *    ⇒ บรรทัด *"ไม่ได้วัด StrictMode"* ในฉบับก่อน **เป็นเท็จโดยที่ผมเขียนมันเอง**
 * · ⚠️ **สิ่งที่ยังไม่ได้วัด: มีเฟรมที่ *วาด* สถานะปลดล็อกกลางทางไหม** — ผมยืนยันได้แค่ *ค่าปลายทางถูก*
 *   🔴 ถ้ามีคนเจอ "หน้าเลื่อนได้หนึ่งกะพริบใน dev" **นั่นคือของนี้ ไม่ใช่บั๊กของ React** และ **ห้ามแก้ด้วยการถอดตัวนับ**
 *      — ช่องพี่น้องที่ P4 วัดแล้วว่าพังจริง จะกลับมาทันที
 */
let holders = 0;
let originalOverflow = "";

export function useBodyScrollLock() {
  useEffect(() => {
    if (holders === 0) {
      originalOverflow = document.body.style.overflow;
      document.body.style.overflow = "hidden";
    }
    holders += 1;
    return () => {
      holders -= 1;
      if (holders === 0) document.body.style.overflow = originalOverflow;
    };
  }, []);
}
