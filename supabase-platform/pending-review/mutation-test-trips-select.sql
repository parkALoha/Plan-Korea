-- ═══════════════════════════════════════════════════════════════════════════
-- 🧪 mutation test ของเมทริกซ์ RLS — **ไม่ใช่ migration · ห้ามวางใน migrations/**
-- เจ้าของ: P1-Lead · P4-QA/Sec เป็นคนเรียกร้อง · 24 ส.ค. 2026
-- ═══════════════════════════════════════════════════════════════════════════
--
-- 🎯 **ทำไมต้องมี:** เมทริกซ์ 48 เคสเขียวหมดแล้ว **แต่ยังไม่มีใครเคยเห็นมันแดงเพราะ RLS ผิดจริง**
--   ทุกครั้งที่มันแดงวันนี้ มันแดงเพราะ *harness* (คีย์ผิดใบ · `owner_id` · `P-26`)
--   → กฎ `E0` ของเราเอง: **ด่านที่ไม่เคยเห็นแดง พิสูจน์อะไรไม่ได้**
--
-- ⚠️ **ต้องรันใน SQL Editor ของผู้ใช้** — เครื่องนี้ไม่มี `psql`/docker และ `db push`
--   รันได้แค่ migration ตามลำดับ ซึ่งใช้กับงานนี้ไม่ได้ (เมทริกซ์ต้องรันคั่นกลาง 2 สถานะ)
--   🔴 **และห้ามแปลงไฟล์นี้เป็น migration** — มันจะทิ้งสถานะ "หละหลวมโดยตั้งใจ"
--      ไว้ในประวัติ migration ซึ่งจะรันบนฐานใหม่ทุกใบตลอดไป
--
--   SQL Editor: https://supabase.com/dashboard/project/pmvxwcimjebogjfimzqy/sql/new
--
-- 🔴 **ต้องทดสอบ 2 ทิศ ไม่ใช่ทิศเดียว — `P-44` (P4 · 24 ส.ค. 2026)**
--   ฉบับแรกของไฟล์นี้มีแต่ก้อน "หละหลวม" · **ซึ่งไม่พอ และ P4 พิสูจน์ว่าทำไม:**
--   เขียน `trips_select` เป็น `created_by = auth.uid()` (**เข้มเกินไป** ไม่ใช่หลวมเกินไป)
--   → **เมทริกซ์ 48 เคสเดิมเขียวหมด** · A เห็นของตัวเอง · B เห็นแต่ของตัวเอง · C ไม่เห็นอะไร
--   → **แต่ทุกคนที่ถูกเชิญเข้าทริปเปิดมาเจอหน้าเปล่า** = สิ่งเดียวที่แพลตฟอร์มนี้มีไว้ทำ พังเงียบสนิท
--   🎯 **ด่านที่จับได้แต่ "หลวมเกินไป" ปล่อย "เข้มเกินไป" ผ่านตลอด — และอันหลังฆ่าฟีเจอร์หลัก**
--
-- ── ขั้นตอน — ห้ามสลับ ─────────────────────────────────────────────────────
--   ① รัน **ก้อน R (คืนค่า)** ก่อน เพื่อพิสูจน์ว่ามันรันได้จริง — ถ้าก้อนคืนค่าพัง
--      อย่าเพิ่งแตะก้อนอื่นเด็ดขาด
--   ② รันก้อน **A (หละหลวม)** → รันเมทริกซ์ → 🔴 **ต้องแดงที่เคสของ `E1-AC3`**
--      (`C` ที่ไม่ใช่สมาชิก มองเห็นทริปของ `A`)
--   ③ รันก้อน **R** → รันก้อน **T (เข้มเกินไป)** → รันเมทริกซ์
--      → 🔴 **ต้องแดงที่เคส "สมาชิกที่ไม่ใช่เจ้าของเห็นทริป"** (เคสที่ P4 เพิ่งเพิ่ม `cb6feaf`)
--      · ถ้าทิศนี้ **เขียว** = เคสชุดนั้นไม่ได้วัดสิ่งที่มันอ้าง **หยุดแล้วมาคุยกัน**
--   ④ รันก้อน **R** ปิดท้าย → รันเมทริกซ์ซ้ำ → ต้องเขียวครบเหมือนเดิม
--
--   คำสั่งเมทริกซ์:  RLS_MATRIX_REQUIRED=1 npx vitest run rlsMatrix
-- ═══════════════════════════════════════════════════════════════════════════


-- ╔═══════════════════════════════════════════════════════════════════════╗
-- ║  ก้อน R — คืนค่า · **คัดลอกเก็บไว้ก่อนรันก้อนอื่น** · รันได้ซ้ำเสมอ      ║
-- ╚═══════════════════════════════════════════════════════════════════════╝
drop policy if exists trips_select on public.trips;

create policy trips_select on public.trips
  for select to authenticated
  using (app.can_read_trip(id));


-- ╔═══════════════════════════════════════════════════════════════════════╗
-- ║  ก้อน A — หละหลวมเกินไป · 🔴 อย่ารันถ้ายังไม่ได้เก็บก้อน R ไว้          ║
-- ╚═══════════════════════════════════════════════════════════════════════╝
-- drop policy if exists trips_select on public.trips;
--
-- create policy trips_select on public.trips
--   for select to authenticated
--   using (true);            -- 🔴 นี่คือสิ่งที่ `E2-AC2` มีไว้จับ · ห้ามค้างไว้เกิน 1 รอบเทสต์


-- ╔═══════════════════════════════════════════════════════════════════════╗
-- ║  ก้อน T — **เข้มเกินไป** · ทิศที่เมทริกซ์เดิมมองไม่เห็นเลย (`P-44`)     ║
-- ╚═══════════════════════════════════════════════════════════════════════╝
-- 🎯 ทิศนี้คือทิศที่ฆ่าฟีเจอร์หลักของแพลตฟอร์ม: **คนที่ถูกเชิญเปิดมาเจอหน้าเปล่า**
--    และมัน **ไม่ทำให้เคสความปลอดภัยข้อไหนแดงเลย** เพราะไม่มีใครเห็นสิ่งที่ไม่ควรเห็น
-- ⚠️ `created_by` ยังเป็นคอลัมน์จริงในตาราง (`D38`/`P-15` ห้ามใช้เป็นแหล่งสิทธิ์ — ก้อนนี้
--    จงใจละเมิดข้อนั้นชั่วคราวเพื่อพิสูจน์ว่าเมทริกซ์จับได้ **ห้ามค้างไว้เกิน 1 รอบเทสต์**)
--
-- drop policy if exists trips_select on public.trips;
--
-- create policy trips_select on public.trips
--   for select to authenticated
--   using (created_by = (select auth.uid()));   -- 🔴 เจ้าของเท่านั้น สมาชิกคนอื่นตกหมด
