# `pending-review/` — migration ที่เขียนแล้ว แต่ **ยังไม่ให้ CLI เห็น**

ไฟล์ในโฟลเดอร์นี้เป็น SQL ที่เขียนเสร็จแล้วและ **ตั้งใจไม่วางใน `supabase/migrations/`**
เพราะ `supabase db push` จะกวาดทุกไฟล์ `.sql` ในโฟลเดอร์นั้นไปรันโดยไม่ถามใคร

🔴 **เหตุผลที่ต้องมีโฟลเดอร์นี้ ไม่ใช่แค่คอมเมนต์ว่า "อย่าเพิ่ง push":**
ตอนที่ใครสักคนแก้ปัญหาที่ค้างอยู่ได้ (เช่นได้ token ใบใหม่) สิ่งที่จะทำต่อทันทีคือ `db push`
· โน้ตที่บอกว่าอย่าทำ **อยู่คนละไฟล์กับคำสั่งที่เขากำลังพิมพ์**

## ย้ายกลับเมื่อไหร่ — **ตรวจสถานะจริงแล้ว 27 ส.ค. 2026 (P1)**

เมื่อเจ้าของโซนตัดสินแล้วว่าทางแก้ในไฟล์นั้นถูก — ย้ายกลับด้วย `git mv` ไปที่
`supabase-platform/supabase/migrations/`

🔴 **แก้ประโยคเดิม: *"ไฟล์เก่าในนี้ยังถือด่านฉบับ denylist อยู่ ต้องเปลี่ยนบล็อกด่านก่อน"* — ไม่จริงแล้ว**
วัดจริง (`grep app.project_identity` ทุกไฟล์):

| ไฟล์ | ด่าน | ย้ายได้เลยไหม |
|---|---|---|
| `…061500_read_only_covers_storage` · `…080000_create_trip_makes_days` · `…090000_fixture_lock` · `…100000_promote_plan_on_delete` · `…113000_assert_engine_dev_rpc` | ✅ allowlist ฉบับปัจจุบัน | **ย้ายได้ทันที ไม่ต้องแก้ด่าน** |
| `20260824144235_fix_trip_visible_on_create.sql` | ด่านเก่า | 🛑 **ห้ามย้าย** — จอดถาวร (`P-27`+`P-29`) |
| `mutation-test-trips-select.sql` | ไม่มี | 🛑 **ไม่ใช่ migration** — รันใน SQL Editor เท่านั้น |

⚠️ **ทำไมต้องแก้ประโยคนี้:** มันสั่งให้ทำขั้นตอนแปลงด่านที่ **5 ไฟล์ที่รันได้ไม่มีไฟล์ไหนต้องทำ**
🎯 **และมันจะถูกอ่านตอนตี 3 ในหน้าต่างที่ผู้ใช้กำลังรีบ** — คำสั่งให้แก้ไฟล์ก่อนรัน คือคำเชิญให้แก้พลาด
· ประโยคเดิมจริงตอนเขียน (ตอนนั้นในโฟลเดอร์มีแต่ไฟล์เก่า) · **เท็จเมื่อมีไฟล์ใหม่มาจอดเพิ่ม 5 ไฟล์**
· **ข้อเท็จจริงที่มีวันหมดอายุ และไม่ประกาศวันหมดอายุของตัวเอง** — ตระกูลเดียวกับที่แก้ใน `PLAN.md` และ `migration-template.sql` วันเดียวกัน

### ลำดับที่ควรรัน (เหตุผลเต็มของแต่ละตัวอยู่ในตารางข้างล่าง)
```
1. 20260827080000_e5_create_trip_makes_days.sql     ← ทริปใหม่ไม่มีวันเลย (แก้ได้ 1 ใน 3 ชั้น)
2. 20260827061500_e3_read_only_covers_storage.sql   ← ช่องลบไฟล์ตั๋วตอน cutover · กู้ไม่ได้
3. 20260827100000_e3_promote_plan_on_delete.sql     ← ลบแผนที่ใช้อยู่แล้วหน้าจอว่างถาวร
4. 20260827113000_e0_assert_engine_dev_rpc.sql      ← ด่านของ reaper (ต้องมาก่อน 5 ถ้าจะเปิด reaper)
5. 20260827090000_e0_fixture_lock.sql               ← กันสองเซสชันรันชุดสดชนกัน
```
· ✅ **ทั้ง 5 เป็นอิสระต่อกัน** — ไล่แล้วไม่มีตัวไหนต้องรอตัวอื่น (ตัวเดียวที่มีเงื่อนไขคือข้อ 2 ซึ่งต้องมี
  `app.write_is_blocked()` และมันลงฐานตั้งแต่ `20260826194500` แล้ว) · **ลำดับนี้เรียงตามความเจ็บ ไม่ใช่ตาม dependency**
· 🔴 **ทุกไฟล์มีด่าน `app.project_identity` ในตัว → รันผิดฐานจะล้มเองก่อนแตะอะไร**

## ของที่อยู่ในนี้ตอนนี้

| ไฟล์ | รออะไร |
|---|---|
| `20260824144235_fix_trip_visible_on_create.sql` | 🔴 **P4 ตัดสิน `P-27`** — P1 เชื่อว่าทางแก้นี้ยังไม่พอ (helper เป็น `stable` → snapshot มองไม่เห็นแถวที่ BEFORE trigger เพิ่งเขียน) · ดู `docs/engine/README.md` หัวข้อ `P-27` |
| `mutation-test-trips-select.sql` | 🧪 **ไม่ใช่ migration และจะไม่มีวันเป็น** — SQL สำหรับพิสูจน์ว่าเมทริกซ์ RLS แดงได้จริงเมื่อ policy หละหลวม · **ต้องรันใน SQL Editor ของผู้ใช้** เพราะต้องมี 2 สถานะคั่นด้วยการรันเทสต์ · P4 เรียกร้อง (*"ด่านที่ไม่เคยเห็นแดง พิสูจน์อะไรไม่ได้"*) |
| `20260827090000_e0_fixture_lock.sql` | 🟡 **รอ `db push` เหมือนกัน · เร่งน้อยที่สุดในสามตัว** — ล็อกกันสองเซสชันรันชุดสดทับกัน (P1+P4 ชนกัน 4+ ครั้ง/ไม่กี่ชม.) · **ไม่ใช่บั๊กของผู้ใช้ เป็นค่าเสียเวลาของทีม + ความเสี่ยงเขียวหลอก** · ไม่มีผลกับโค้ดแอปเลย (สิทธิ์ให้ `service_role` เท่านั้น) |
| `20260827080000_e5_create_trip_makes_days.sql` | 🔴 **รอ `db push` เหมือนกัน** — `create_trip` ไม่สร้าง `trip_days` เลย · P3 วัดจากฐานจริงว่า `trip_days = 0 แถว` · 🔴 **ถอด backfill ออกแล้ว หลังวัดว่าจะเพิ่ม 6,166 แถวให้ fixture 562 ใบ** — ไฟล์นี้**ไม่แตะข้อมูลเดิมเลย** · ⚠️ **รันแล้วทริปใหม่ยัง*ไม่*ใช้งานได้ — ดูข้างล่าง** |
| `20260827061500_e3_read_only_covers_storage.sql` | 🔴 **รอมีคนรัน `db push` ใส่ engine-dev** — ไม่ได้รอการตัดสินใจ · P1 จอดเพราะ `supabase` CLI ค้างแบบ non-interactive และ**ไม่ฝืนรันคำสั่งที่แก้ฐาน** (27 ส.ค. 2026) · ดูรายละเอียดข้างล่าง |
| `20260827100000_e3_promote_plan_on_delete.sql` | 🔴 **รอ `db push` เหมือนกัน · ไม่ได้รอใครตัดสิน** — ลบแผนที่ใช้อยู่แล้ว **ไม่มีแผนไหนขึ้นมาแทน** → `activePlanId = null` → จุดแวะ/ตั้งค่ารายวัน/โน้ตหายทั้งหน้า ทั้งที่แผนอีกใบยังมีของครบ · **ค้างถาวร** · ⚠️ **UI ลบได้เฉพาะแผนที่ active** → เป็นทางเดียวที่เมนูเปิดให้เดิน · 🔴 ด่านในไฟล์ตรวจได้แค่ว่า trigger ถูกติดตั้ง **ตรวจพฤติกรรมไม่ได้ — ต้องมีเคสในชุดสด (P4)** |
| `20260827113000_e0_assert_engine_dev_rpc.sql` | 🟡 **รอ `db push` · ไม่ได้รอใครตัดสิน** — `public.assert_engine_dev()` = **ชั้น B ของ fixture reaper** (P4 ขอ · P1 เขียน) · `service_role` อ่าน `app.project_identity` ตรง ๆ ไม่ได้ (ไม่มี `usage` บน schema `app`) จึงต้องเป็น RPC `definer` · 🔴 **reaper ลบข้อมูลได้โดยไม่มีมนุษย์ในลูป** — ไม่มี RPC นี้ reaper `fail-close` (ไม่ลบ) ซึ่งถูก · ⚠️ **คำประกาศของฐานเกี่ยวกับตัวเอง พิสูจน์ไม่ได้ว่าต่อไปฐานไหน** — ต้องมีชั้น A (URL) คู่เสมอ |

---

## 🔴 `20260827061500_e3_read_only_covers_storage.sql` — **ช่องยังเปิดอยู่จนกว่าจะรัน**

**ทำอะไร:** ปิดช่องที่ P2 พบ — `read-only mode` ผูก trigger ด้วย `… on public.%I`
→ **ครอบเฉพาะ schema `public`** แต่ไฟล์ตั๋วอยู่ใน `storage.objects` (schema `storage`)
→ ระหว่างโหมดอ่านอย่างเดียว ผู้ใช้ยัง **อัปโหลดและ *ลบ* ไฟล์ตั๋วได้**

🎯 **ตัวที่เจ็บคือ `delete`** — ลบตอน cutover (`E7`) = ไฟล์แนบหายถาวรตอนฐานกำลังถูกคัดลอก **กู้ไม่ได้**
· อัปโหลดตอน cutover = ไฟล์กำพร้า ซึ่งกู้ได้

⚠️ **ต่างจากอีก 2 ไฟล์ในโฟลเดอร์นี้: ตัวนี้ *ไม่ได้* รอใครตัดสิน** — มันรอแค่คนรัน
· ปุ่มที่ P2 ปิดใน `BookingEditModal` **เป็นด่านเดียวจริง ๆ สำหรับอัปโหลด/ลบไฟล์ตอนนี้**
  ซึ่งเป็นสภาพชั่วคราวที่ไม่ควรอยู่นาน
· หลังรันแล้ว เคส *"migration ในทรี กับที่ฐานรันไปแล้ว ต้องเป็นชุดเดียวกัน"* ใน `rlsMatrix`
  **จะบอกเองว่าตรงกันแล้วหรือยัง — ไม่ต้องเชื่อใคร**
· 🔴 ด่านยืนยันในไฟล์ตรวจได้แค่ว่า **policy อ้าง `write_is_blocked` จริง**
  **ตรวจไม่ได้ว่าผู้ใช้จริงอัปโหลดไม่ได้จริง** — ต้องมีเคสยิงผ่าน storage-api ใน `rlsMatrix.test.ts` (ยังไม่มี)

---

## ⚠️ `20260827080000_e5_create_trip_makes_days.sql` — **รันแล้วยังไม่จบ · แก้ข้อความที่ผมเขียนไว้เอง**

ผมเขียนตอนแรกว่า *"ทริปใหม่ใช้งานไม่ได้จนกว่าจะรัน"* ซึ่ง**อ่านได้ว่ารันแล้วจะใช้ได้ — ไม่จริง**

**P2 วัดจากหน้าจอจริง (ทริปวันเดียว 1 พ.ย.) แล้วพบว่ามี 3 ชั้น ไม่ใช่ชั้นเดียว:**
| ชั้น | อาการ | migration นี้แก้ไหม |
|---|---|---|
| ① `trip_days` ว่าง | `GET /days` คืน `[]` | ✅ **แก้** |
| ② หน้าเว็บ **fallback ไปเรนเดอร์ `ITINERARY` สถิตย์ทั้งก้อน** | ทริปวันเดียวโชว์ **11 วันของทริปเกาหลี** พร้อมเมือง/หมุด/พยากรณ์อากาศ | ❌ **ไม่แก้** |
| ③ เพิ่มจุดแวะล้มแบบเงียบต่อผู้ใช้ | `useStops.ts:154` โยนพร้อม dayId `"d1"` ออก `console.error` · **ไม่มี toast** | ❌ **ไม่แก้** |

🔴 **ชั้น ② คือชั้นที่ไม่มีใครระบุมาก่อน และมันหนักที่สุด** — ผู้ใช้เห็น**เนื้อหาของทริปอื่น**ใต้ชื่อทริปตัวเอง
· ไม่ใช่ข้อมูลรั่ว (`ITINERARY` เป็นข้อมูลนิ่งในรีโป ไม่ใช่ของผู้ใช้คนอื่น) **แต่เป็นหน้าจอที่ผิดทั้งหน้า**
· 🎯 **แปลว่าแอปยังไม่มีสถานะ "ทริปที่ไม่มีวัน" อยู่เลย — มันแทนที่ด้วยทริปเกาหลีเงียบ ๆ**

📌 **ชั้น ②/③ เป็นงาน `E5`** (ตัดสินแล้ว: `trip_days.id` เป็นตัวตนของวัน · ข้อมูลของวันมาจากฐาน) — **ไม่ใช่ migration**

