-- ═══════════════════════════════════════════════════════════════════════════
-- ให้ชุดทดสอบ "ปักธงแล้วเก็บ" ได้เอง — ไม่งั้นทางที่ธงถูกตั้ง จะไม่มีใครเคยรัน
-- เจ้าของ: P1-Lead · 25 ส.ค. 2026
-- ═══════════════════════════════════════════════════════════════════════════
--
-- 🔴 **ปัญหาที่ migration ก่อนหน้ายังไม่ปิด:**
--   `service_role` มีแค่ `select` บน `app.unsafe_state` → harness ทดสอบได้เฉพาะ **ตอนธงว่าง**
--   **ทางที่ธงถูกตั้งอยู่ จะไม่เคยถูกรันเลยจนกว่าจะมีคนทำ mutation test จริง**
--   🎯 นั่นคือ *"เดี๋ยวค่อยพิสูจน์ตอนที่มันสำคัญ"* — **ซึ่งคือสิ่งที่ทีมนี้ไล่ปิดกันมาสองวัน**
--      และเป็น `P-50` ในรูปที่เล็กลงหนึ่งขั้น: ด่านที่พิสูจน์ได้แค่ครึ่งเดียว
--
-- ── ทำไม grant นี้แคบและปลอดภัย ────────────────────────────────────────────
--   `app.unsafe_state` **ไม่มีข้อมูลผู้ใช้เลยแม้แต่ฟิลด์เดียว** — มันคือธงสถานะของฐานทดสอบ
--   · `anon`/`authenticated` ยัง `revoke all` เหมือนเดิม · ตารางยังซ่อนจาก PostgREST (อยู่ใน `app`)
--   · ทางเดียวที่แตะได้คือ `service_role` ซึ่งอยู่ใน harness กับ SQL Editor เท่านั้น
--   ⚠️ **ผลข้างเคียงที่ยอมรับ:** เทสต์ที่บั๊กอาจทิ้งธงค้างไว้ → **ชุดจะแดงดังทุกรอบจนมีคนเก็บ**
--      ซึ่งเป็นทิศที่ปลอดภัย (ดังเกินดีกว่าเงียบ) และเป็นพฤติกรรมที่ธงนี้ออกแบบมาให้เป็นอยู่แล้ว
--
-- 📌 ข้อยกเว้น `D38` ข้อที่ 3 — ขอบเขต: **ตารางที่ไม่มีข้อมูลผู้ใช้ และมีไว้ให้ชุดทดสอบใช้เท่านั้น**
--    (ข้อ 1 = เทสต์ของ P4 · ข้อ 2 = `grant select, delete on trips` เพื่อเก็บ fixture)
--    🔴 **โค้ดที่เสิร์ฟผู้ใช้ยังห้ามแตะ service role key เหมือนเดิมทุกตัวอักษร**
--
-- ── rollback ──────────────────────────────────────────────────────────────
--   revoke insert, delete on app.unsafe_state from service_role;
-- ═══════════════════════════════════════════════════════════════════════════

begin;

do $guard$
begin
  if not exists (
    select 1 from app.project_identity
     where name = 'plan-korea-platform' and ref = 'pmvxwcimjebogjfimzqy' and environment = 'dev'
  ) then
    raise exception 'ผิดโปรเจกต์: app.project_identity ไม่ตรงกับ engine-dev';
  end if;
exception
  when undefined_table then
    raise exception 'ผิดโปรเจกต์: ไม่มี app.project_identity → ฐานนี้ไม่ใช่ engine-dev';
end $guard$;

grant insert, delete on app.unsafe_state to service_role;

commit;
