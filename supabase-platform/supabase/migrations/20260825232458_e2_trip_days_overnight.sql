-- ═══════════════════════════════════════════════════════════════════════════
-- `Q5` ปิด — `trip_days` เก็บ *ความตั้งใจ* เรื่องที่นอน · ดีไซน์ของ P7
-- เจ้าของ: P1-Lead · 25 ส.ค. 2026 · **P7 แก้ความเข้าใจผิดของผมเรื่องมติของผมเอง**
-- ═══════════════════════════════════════════════════════════════════════════
--
-- ── 🔴 ผมถาม `Q5` โดยเชื่อว่ามันชนกับ `D51` · **มันไม่ชน และ `D51` เป็นมติของผมเอง** ──
-- P7 ตอบจาก**ตัวบท** ไม่ใช่จากความจำ · `D51` เหตุผลข้อ 2 เขียนไว้เองว่า:
--   > **"`trip_days` ยังเป็นแหล่งความจริงเดียวของ 'นอนเมืองไหน'"**
-- และข้อ 1: *"`leg` เป็นเรื่องของ**แผน** ไม่ใช่เรื่องของ**ใบจอง**"*
-- 🎯 **`D51` มอบคำถามนี้ให้ `trip_days` ตั้งแต่แรกแล้ว — สิ่งที่มันย้ายออกคือ *ขอบเขตของใบจอง***
--    **ผมอ่านมติของตัวเองผิด แล้วเปิดคำถามที่มติเดิมตอบไปแล้ว**
--
-- ── ② ข้อที่แรงกว่านั้น (P7): ประโยชน์หลักที่ `D51` โฆษณาไว้ ทำไม่ได้เลยถ้าไม่มีคอลัมน์นี้ ──
-- `D51` เขียนว่า UI ต้องแสดง *"จองไว้แต่ไม่ตรงกับแผนแล้ว"* และว่า
--   > *"วันนี้ความไม่ตรงแบบนี้มองไม่เห็นเลย … **ของใหม่ทำให้มันมองเห็นได้เป็นครั้งแรก**"*
-- 🔴 **"ไม่ตรงกับแผน" ต้องมี "แผน" ให้เทียบ** — ถ้า `trip_days` ไม่เก็บว่าตั้งใจนอนเมืองไหน
--    **ไม่มีอะไรให้ `trip_hotels` ไม่ตรงกับมันได้เลย** → คำสัญญาข้อนั้นเป็นโมฆะเงียบ ๆ
-- · **สองตารางตอบคนละคำถาม:** `trip_days` = *ตั้งใจ* · `trip_hotels` = *จองแล้ว*
--   **ความต่างระหว่างสองอันคือฟีเจอร์ ไม่ใช่ความไม่สอดคล้องที่ต้องกัน**
--
-- ── ③ `noHotel` — 4 สถานะที่ต้องแยกออกจากกัน (P7 · ยืนยันจาก `hotelLegs.ts:69`) ──
-- ```
-- ยังไม่ตัดสินว่านอนไหน          → ไม่มีแถว trip_hotels
-- ตั้งใจนอนเมือง X · ยังไม่จอง    → ไม่มีแถว
-- ตั้งใจนอนเมือง X · จองแล้ว      → มีแถว ✅
-- ตั้งใจไม่นอนโรงแรม (บนเครื่อง)  → ไม่มีแถว
-- ```
-- **3 ใน 4 ยุบเป็นอาการเดียว** → ตัวเตือน *"ยังไม่จองที่พัก"* จะดังใส่คืนที่ตั้งใจนอนบนเครื่อง
-- (ทริปจริงมีคืนแบบนั้นแน่ — บินกลับ 21 ต.ค.) · **และนั่นคือวิธีที่คำเตือนจริงกลายเป็นคำเตือนที่คนปิดทิ้ง**
--
-- ── 🔴 ทำไมไม่ใช่ `no_hotel boolean not null default false` (P7 ค้าน และถูก) ──
-- 1. อนุญาตให้ `no_hotel = true` **พร้อมมีเมือง** = ชุดค่าที่ขัดกันในตัวเอง เขียนลงไปได้
-- 2. **`default false` ทำให้ทุกวันอ้างตั้งแต่เกิดว่า "ตั้งใจนอนโรงแรม" ทั้งที่ยังไม่มีใครตัดสิน**
--    → ซ่อน *"ยังไม่ตัดสิน"* ไว้ใต้ค่าเริ่มต้น · **สปีชีส์เดียวกับด่านที่เขียวเพราะไม่เคยรัน**
-- ✅ ท่าที่ใช้: `overnight_kind` + `check` ที่ทำให้ **ชุดค่าที่ขัดกันเขียนลงไปไม่ได้ตั้งแต่แรก**
--
-- ── ของที่ `Q5` ถามแล้ว **จงใจไม่เก็บ** — เขียนไว้ไม่ให้ใครคิดว่าลืม ──────────
-- · **`overnightOptions`** (คืนที่ยังเลือกได้ มีตัวเลือกอะไรบ้าง) — **ไม่เก็บ**
--   เป็น *คำแนะนำที่เราคัดมา* ไม่ใช่ข้อมูลที่ผู้ใช้กรอก · บนแพลตฟอร์ม **ผู้ใช้เลือกเมืองไหนก็ได้**
--   → `overnight_kind is null` = ยังไม่ตัดสิน **ซึ่งครอบเจตนาเดิมครบแล้ว** · ไม่มีข้อมูลของใครหาย
-- · **`cityTh` / `cityEn`** (พาดหัววันย้ายเมือง เช่น *"Gangneung → Seoul"*) — **ไม่เก็บ · คำนวณ**
--   `lib/citySegments.ts` คำนวณช่วงเมืองจากจุดแวะอยู่แล้ว → พาดหัวมาจากช่วงแรก→ช่วงสุดท้าย
--   🔴 **เก็บเป็นสตริงไทย/อังกฤษบนแถว = ฝังภาษาลงสคีมาของแพลตฟอร์มหลายประเทศ** ซึ่งเป็นสิ่งที่ `B6` ไล่ออกอยู่
-- · **`day.note`** — ✅ **เก็บ** เป็น `trip_days.note` (ผู้ใช้แก้ได้ ไม่ใช่ของที่เราคัด)
--
-- ── 🔴 และแก้คอมเมนต์ของ `city_id` ตามที่ P5 ชี้ ─────────────────────────────
-- P5: **`city_id` เป็นค่าเดียวต่อวัน แต่ทริปนี้มีวันที่ข้ามประเทศ ตั้งแต่วันแรก**
-- → มันคือ *เมืองหลักของวัน* **ไม่ใช่ "วันนี้อยู่เมืองนี้เท่านั้น"** · วันข้ามเมืองต้องอ่านจากช่วง (`citySegments`)
-- **คอมเมนต์เดิมของผมเชิญให้อ่านผิดข้อนี้พอดี** — แก้แล้ว
--
-- ── rollback ──────────────────────────────────────────────────────────────
--   alter table public.trip_days drop column overnight_kind, drop column overnight_city_id, drop column note;
-- ═══════════════════════════════════════════════════════════════════════════

begin;

do $guard$
begin
  if not exists (
    select 1 from app.project_identity
     where name = 'plan-korea-platform' and ref = 'pmvxwcimjebogjfimzqy' and environment = 'dev'
  ) then
    raise exception 'ผิดโปรเจกต์: ไม่ใช่ engine-dev';
  end if;
end $guard$;

alter table public.trip_days
  add column overnight_kind    text check (overnight_kind in ('city', 'none')),
  add column overnight_city_id uuid references public.catalog_cities(id) on delete restrict,
  add column note              text check (note is null or length(note) <= 2000),
  -- 🔴 ชุดค่าที่ขัดกันต้องเขียนลงไปไม่ได้ ไม่ใช่ตรวจทีหลัง (P7)
  add constraint trip_days_overnight_consistent check (
       (overnight_kind = 'city' and overnight_city_id is not null)
    or (overnight_kind = 'none' and overnight_city_id is null)
    or (overnight_kind is null  and overnight_city_id is null)
  );

comment on column public.trip_days.overnight_kind is
  'Q5/D51 — *ความตั้งใจ* เรื่องที่นอน · null = ยังไม่ตัดสิน · city = ตั้งใจนอนเมืองหนึ่ง · none = ตั้งใจไม่นอนโรงแรม '
  '🔴 ห้ามเปลี่ยนเป็น boolean not null default false: default จะอ้างแทนผู้ใช้ว่าตัดสินแล้ว (P7)';
comment on column public.trip_days.overnight_city_id is
  'Q5/D51 — เมืองที่ *ตั้งใจ* จะนอน · คนละคำถามกับ trip_hotels ที่ตอบว่า *จองแล้ว* '
  'ความต่างระหว่างสองอันคือฟีเจอร์ที่ D51 สัญญาไว้ ("จองไว้แต่ไม่ตรงกับแผนแล้ว") ไม่ใช่ความไม่สอดคล้อง';
comment on column public.trip_days.note is
  'day.note เดิม — ข้อความของผู้ใช้ต่อวัน';

comment on column public.trip_days.city_id is
  'P-57 — **เมืองหลักของวัน** (day.city เดิม · ใช้ 30+ จุด) · nullable = ยังไม่รู้ '
  '🔴 P5: ค่าเดียวต่อวัน **แต่มีวันที่ข้ามประเทศ ตั้งแต่วันแรกของทริปนี้** '
  '→ ไม่ใช่ "วันนี้อยู่เมืองนี้เท่านั้น" · วันข้ามเมืองต้องอ่านจากช่วง (lib/citySegments.ts) ไม่ใช่จากคอลัมน์นี้ '
  'restrict: ลบเมืองจากคลังต้องไม่ลากวันของผู้ใช้ไปด้วย';

grant insert (overnight_kind, overnight_city_id, note) on public.trip_days to authenticated;
grant update (overnight_kind, overnight_city_id, note) on public.trip_days to authenticated;

commit;
