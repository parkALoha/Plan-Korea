-- ═══════════════════════════════════════════════════════════════════════════
-- `app.unsafe_state` — ธงบอกว่า "ฐานนี้กำลังถูกทำให้พังโดยตั้งใจอยู่ตอนนี้"
-- เจ้าของ: P1-Lead · P4 เสนอหลังพบว่าไม่มีกลไกใดแจ้งเซสชันอื่นเลย · 25 ส.ค. 2026
-- ═══════════════════════════════════════════════════════════════════════════
--
-- 🔴 **ช่องที่ปิด — และมันเกิดขึ้นจริงแล้ววันนี้ ไม่ใช่สมมติ**
--   25 ส.ค. ~09:50–09:56 `engine-dev` อยู่ในสภาพ **RLS เปิดโล่ง** (`trips_select using (true)`)
--   ระหว่างรัน mutation test · **ไม่มีใครในทีมรู้ นอกจากคนที่ทำ**
--     · P1 ไม่รู้ · P4 ไม่รู้ · ผู้ใช้เปิดเว็บอยู่ตอนนั้นได้ด้วย
--     · สิ่งเดียวที่จับได้คือ P1 **บังเอิญ**รันเทสต์ด้วยเหตุผลคนละเรื่อง
--
--   🎯 **P1 จดข้อนี้ไว้ตอนแรกว่า "ความบังเอิญที่โชคดี" ซึ่งถูก แต่หยุดก่อนถึงข้อสรุปที่ควรได้ (P4 ชี้):**
--   **เรามีวิธีบอกกันว่า "ฐานกำลังพังโดยตั้งใจอยู่ตอนนี้" — ศูนย์วิธี**
--
--   **2 ทางที่พังได้ และทั้งคู่เป็นพฤติกรรมปกติของคน:**
--     ① คนถัดไปเห็นแดงระหว่างหน้าต่างนั้น → อ่านว่า **"เทสต์แกว่ง"** → รันใหม่ → เดินต่อ
--     ② ไม่มีใครรันเทสต์เลยช่วงนั้น → **มีคนเขียนข้อมูลผ่าน RLS ที่เปิดอยู่** โดยไม่มีอะไรบันทึก
--
-- ── ทำไมแยกตาราง ไม่ใช้ `app.project_identity` ที่ P4 เสนอ ──────────────────
--   `project_identity` ตอบว่า **"ฐานนี้คือฐานอะไร"** — ข้อเท็จจริงถาวร ไม่เคยเปลี่ยน
--   ธงนี้ตอบว่า **"ตอนนี้ฐานอยู่ในสภาพไหน"** — สถานะชั่วคราว เปลี่ยนวันละหลายรอบ
--   🔴 **ตารางเดียวสองความหมาย คือรูปแบบที่ทีมนี้เจ็บมาแล้วทั้งสัปดาห์** (`D53` cache policy · `P-15` แหล่งสิทธิ์)
--   · และถ้าปนกัน **การลบธงพลาดจะลบตัวตนของฐานไปด้วย** ซึ่งทำให้ด่านทุกตัวล้มพร้อมกัน
--
-- ── ข้อจำกัดที่ต้องพูด ไม่ใช่ซ่อน ──────────────────────────────────────────
--   ⚠️ ธงนี้พึ่งการที่คนวาง SQL **ทั้งก้อน** — ถ้าวางแค่ครึ่งเดียว (แก้ policy แต่ไม่เขียนธง)
--      มันไม่ช่วยอะไรเลย · **มันลดโอกาสพลาด ไม่ได้ปิดช่อง**
--   🔴 **ห้ามอ่านว่าเป็นระบบเฝ้าระวัง** — ไม่มีอะไรคอยดูฐาน มีแต่เทสต์ที่บังเอิญถูกรัน
--
-- ── rollback ──────────────────────────────────────────────────────────────
--   drop table if exists app.unsafe_state;
--   ⚠️ ถอยแล้วเมทริกซ์ที่เช็คธงจะล้ม — ถอยพร้อมกับฝั่งเทสต์เท่านั้น
-- ═══════════════════════════════════════════════════════════════════════════

begin;

do $guard$
begin
  if not exists (
    select 1 from app.project_identity
     where name = 'plan-korea-platform' and ref = 'pmvxwcimjebogjfimzqy' and environment = 'dev'
  ) then
    raise exception 'ผิดโปรเจกต์: app.project_identity ไม่ตรงกับ engine-dev';
  end if;
exception
  when undefined_table then
    raise exception 'ผิดโปรเจกต์: ไม่มี app.project_identity → ฐานนี้ไม่ใช่ engine-dev ของแพลตฟอร์ม';
end $guard$;

create table app.unsafe_state (
  -- แถวเดียวเสมอ — ฐานอยู่ในสภาพไม่ปลอดภัย "หรือไม่" ไม่ใช่ "กี่แบบ"
  singleton  boolean primary key default true check (singleton),
  reason     text not null,
  started_at timestamptz not null default now(),
  note       text
);

alter table app.unsafe_state enable row level security;   -- ไม่มี policy โดยตั้งใจ (D18)
revoke all on app.unsafe_state from anon, authenticated;

-- ชุดทดสอบอ่านธงนี้ผ่าน service_role — ต้อง select ได้ ไม่งั้นเช็คไม่ได้
grant select on app.unsafe_state to service_role;

commit;
