-- ═══════════════════════════════════════════════════════════════════════════
-- ลบตารางทดลองของ P5 — **วัดเสร็จแล้ว และคำตอบคือ P5 ถูก**
-- เจ้าของ: P1-Lead · 25 ส.ค. 2026
-- ═══════════════════════════════════════════════════════════════════════════
--
-- ── ผลการวัด (ยิงผ่าน PostgREST ด้วยผู้ใช้จริง) ────────────────────────────
-- ```
-- service_role (มี BYPASSRLS)  : 1 แถว   ← ยืนยันว่าแถวมีอยู่จริง ไม่ใช่ตารางว่าง
-- authenticated (grant select) : 0 แถว   ← **ไม่ใช่ 42501** — grant ผ่าน แต่ไม่เห็นอะไร
-- เทียบ: authenticated → place_photo_cache : error 42501  ← ถูกกันคนละด่าน
-- ```
--
-- 🎯 **P5 ถูกทั้งข้อ: `grant` กับ RLS เป็นคนละด่าน และต้องผ่านทั้งคู่**
--    role ธรรมดาที่มี grant ครบ ยังได้ **0 แถว** เพราะ **0 policy = ไม่มีอะไรอนุญาต**
--    → **`Q3` ตัวเลือก (ค) `cache_writer` ต้องมี `create policy … to cache_writer` ด้วย ไม่ใช่แค่ grant**
--    → **แคชจะเลิกเป็น "ตารางที่มี 0 policy"** ซึ่งเป็นสิ่งที่ `e2_caches.sql` สั่งให้เฝ้าไว้
--
-- 📌 **สิ่งที่ P4 ต้องเฝ้าเปลี่ยนไป** (P5 ชี้ · ผมรับ): จาก *"ยืนยันว่า 0 policy"*
--    → *"ยืนยันว่ามี policy เดียว และมันชี้ role เดียว"* · **ไม่ใช่เหตุผลให้ทิ้ง (ค)** —
--    policy ที่ระบุ role เดียวและตรวจได้ ยังดีกว่า BYPASSRLS ทั้งใบมาก
--
-- ⚠️ **ข้อที่การวัดนี้ทำให้ชัดขึ้นและ P5 ไม่ได้พูดถึง:** ส่วนที่ว่า *"`cache_writer` ต้องมี policy"*
--    **จริงมาตั้งแต่ก่อน `force row level security` แล้ว** — `force` เปลี่ยนอะไรเฉพาะกับ**เจ้าของตาราง**
--    (ซึ่งคือทางของ `security definer`) · role ธรรมดาถูก RLS บังคับอยู่แล้วตั้งแต่ต้น
--    → **`force` ปิดประตู definer · มันไม่ได้สร้างข้อจำกัดของ `cache_writer`** สองข้อนี้ต้องแยกกัน
--       ไม่งั้นวันหนึ่งจะมีคนถอน `force` ออกโดยคิดว่ากำลังปลดล็อกให้ `cache_writer`
--
-- 🔬 **และเหตุผลที่ต้องยิงแทนที่จะอนุมาน:** วันนี้อนุมานที่ฟังดูถูกพาไปผิดที่มาแล้ว 2 ครั้ง
--    (`§16.1` ของ P5 เอง · `§11.18` ของ P7) · **รอบนี้อนุมานถูก และตอนนี้มันไม่ใช่การอนุมานอีกแล้ว**
--
-- ── rollback ──────────────────────────────────────────────────────────────
--   ไม่ต้องคืน — ตารางนี้เป็นของทดลอง ไม่มีใครอ้างถึง
-- ═══════════════════════════════════════════════════════════════════════════

begin;

do $guard$
begin
  if not exists (
    select 1 from app.project_identity
     where name = 'plan-korea-platform' and ref = 'pmvxwcimjebogjfimzqy' and environment = 'dev'
  ) then
    raise exception 'ผิดโปรเจกต์: ไม่ใช่ engine-dev';
  end if;
end $guard$;

drop table if exists public.rls_force_probe;

commit;
