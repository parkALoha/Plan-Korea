-- ═══════════════════════════════════════════════════════════════════════════
-- `P-57` — วันหนึ่งอยู่เมืองไหน **ไม่มีที่เก็บในสคีมาใหม่** · เจอตอนไล่ `E2-AC6`
-- เจ้าของ: P1-Lead · 25 ส.ค. 2026
-- ═══════════════════════════════════════════════════════════════════════════
--
-- ── เจอได้ยังไง — `E2-AC6` ที่ P8 ยืนยันว่าต้อง un-close และเขาถูก ──────────
-- ไล่ปลายทางทั้ง **111 คอลัมน์** ใน `column-map.md` เทียบกับ**คอลัมน์จริงในฐาน** (221 คอลัมน์ · 22 ตาราง)
-- → **94 ยืนยันได้ · 17 ยืนยันไม่ได้** · ในนั้นมีข้อนี้:
--
--   `trip_meta.overnight_overrides` → เอกสารเขียนว่า **`trip_days.city_id` รายวัน**
--   🔴 **`trip_days` ไม่มีคอลัมน์ `city_id`** (`created_at · date · id · timezone · trip_id · updated_at · updated_by_user`)
--
-- 🎯 **นี่คือชนิดที่ `E2-AC6` มีอยู่เพื่อจับพอดี** — ข้อของมันเขียนไว้เองว่า
--    *"ถ้าไม่ทำข้อนี้ที่ `E2` จะไปรู้ตอน `E7` ว่าสคีมาใหม่เก็บของเดิมไม่ได้ ซึ่งแปลว่าต้องรื้อ `E2`–`E5` ใหม่ทั้งหมด"*
--
-- ── และของที่ขาดไม่ใช่แค่คอลัมน์เดียว — `data/itinerary.ts` ทั้งก้อนไม่มีที่ไป ──
-- ```
-- day.city (daytime)  : ใช้ 30+ จุด   ← ไฟล์นี้แก้ข้อนี้
-- day.overnightCity   : ใช้ 13 จุด    ← `Q5`
-- day.overnightOptions: ใช้ 11 จุด    ← `Q5`
-- day.noHotel         : ใช้  8 จุด    ← `Q5`
-- day.cityTh/cityEn   : ใช้  8 จุด    ← `Q5` (พาดหัววันย้ายเมือง เช่น "Gangneung → Seoul")
-- day.note            : ใช้  3 จุด    ← `Q5`
-- ```
-- 🔴 **`column-map.md` แมป *ตาราง DB 14 ใบ* — โครงของทริปอยู่ใน *ไฟล์* ไม่ใช่ตาราง**
--    `B6` สั่งให้ `data/itinerary.ts` หายไป **แต่ไม่มี AC ข้อไหนบังคับให้มันมีปลายทาง**
--    → **ทั้งคลาสนี้ไม่มีพันธะให้ใครไล่ และไม่มีด่านไหนจะสะดุด**
--
-- ⚠️ **และมันอยู่ร่วมกับ `E2-AC4` ที่เขียวได้อย่างสบายใจ** — ทริปเบอร์ลิน 3 วันที่ผมพิสูจน์
--    เมื่อเช้า **ผ่านเพราะมันไม่ต้องการ day→city เลยสักวัน** · เกณฑ์ที่ผ่านไม่ได้แปลว่าไม่มีรู
--
-- ── ทำไมไฟล์นี้แก้แค่ `city_id` ข้อเดียว ────────────────────────────────────
-- `day.city` = *"วันนี้กิจกรรมอยู่เมืองไหน"* — **ไม่ชนกับมติไหนเลย** และ `citySegments.ts`
-- ต้องใช้เพื่อไม่ให้แผนที่ซูมออกทั้งประเทศตอนวันย้ายเมือง → **ตัดสินได้เลย ลงเลย**
--
-- 🔴 **ส่วน `overnightCity` ตัดสินคนเดียวไม่ได้ จึงเปิดเป็น `Q5`** — มันชนกับ `D51`:
--    `D51` บอกว่า *"คืนนี้นอนที่ไหน"* = แถว `trip_hotels` ที่ `check_in <= วันนั้น < check_out`
--    **แต่ `overnight_overrides` มีอยู่เพื่อตอบ *ก่อน* จะมีโรงแรม** (`day.overnightOptions` = คืนที่ยังเลือกได้)
--    → ไม่ใช่แหล่งข้อมูลซ้ำ **มันคือ *ความตั้งใจ* ที่มาก่อน *ข้อเท็จจริง*** — และการเลือกว่าจะเก็บ
--      ความตั้งใจไว้ที่ไหน กระทบทั้ง `D51` · โมเดล sync ของ P7 · และ `get_capabilities` ของ P5
--
-- ── `city_id` เป็น nullable โดยตั้งใจ ─────────────────────────────────────
-- วันที่เพิ่งสร้างยังไม่รู้ว่าอยู่เมืองไหน · **บังคับ `not null` = สร้างวันไม่ได้จนกว่าจะเลือกเมือง**
-- ซึ่งกลับลำดับการใช้งานจริง (ผู้ใช้กางวันก่อน แล้วค่อยเติมของ)
-- · `on delete restrict` เหมือน `trip_hotels.city_id` — **ลบเมืองออกจากคลังกลาง ต้องไม่ลากวันของผู้ใช้ไปด้วย**
--
-- ── rollback ──────────────────────────────────────────────────────────────
--   alter table public.trip_days drop column city_id;
-- ═══════════════════════════════════════════════════════════════════════════

begin;

do $guard$
begin
  if not exists (
    select 1 from app.project_identity
     where name = 'plan-korea-platform' and ref = 'pmvxwcimjebogjfimzqy' and environment = 'dev'
  ) then
    raise exception 'ผิดโปรเจกต์: ไม่ใช่ engine-dev';
  end if;
end $guard$;

alter table public.trip_days
  add column city_id uuid references public.catalog_cities(id) on delete restrict;

comment on column public.trip_days.city_id is
  'P-57 — "วันนี้กิจกรรมอยู่เมืองไหน" (day.city ใน data/itinerary.ts · ใช้ 30+ จุด) '
  'nullable โดยตั้งใจ: วันที่เพิ่งสร้างยังไม่รู้เมือง · restrict: ลบเมืองจากคลังต้องไม่ลากวันผู้ใช้ไปด้วย '
  '🔴 ไม่ใช่ "คืนนี้นอนเมืองไหน" — อันนั้นคือ D51 (trip_hotels) และ Q5 (ความตั้งใจก่อนจอง)';

create index trip_days_city_idx on public.trip_days (city_id) where city_id is not null;

-- 🔴 `E2-AC9`/`D7` — grant ระดับคอลัมน์ ไม่ใช่ระดับตาราง · คอลัมน์ใหม่ต้องถูกเติมเข้าไปเอง
--    ไม่งั้นไคลเอนต์เขียนไม่ได้เลย และจะดูเหมือน RLS ปฏิเสธ ทั้งที่เป็นเรื่อง grant
grant insert (city_id) on public.trip_days to authenticated;
grant update (city_id) on public.trip_days to authenticated;

commit;
