-- ═══════════════════════════════════════════════════════════════════════════
-- 🔴 `P-63` **ครั้งที่สาม** — และครั้งนี้ *เทคนิคที่ผมคิดขึ้นเพื่อกันมันเอง* ก็ไม่กัน
-- เจ้าของ: P1-Lead · 26 ส.ค. 2026 · แก้ `20260826121226`
-- ═══════════════════════════════════════════════════════════════════════════
--
-- ── สิ่งที่ผมทำ ─────────────────────────────────────────────────────────────
-- ตอนถอด `description` ออกจาก grant ของ `custom_places` ผม re-grant แล้ว **ใส่ `deleted_at` เข้าไป**
-- ซึ่ง `20260825142949_e2_soft_delete_rpc.sql` **ถอดออกไปแล้วโดยตั้งใจ** พร้อมคอมเมนต์ว่า
--   > *"`deleted_at` ถูกถอดออกจาก `grant update` ของไคลเอนต์ทั้งหมด — เหลือทางเดียวจริง ๆ"*
-- → **ทางลบที่ไม่ผ่าน `soft_delete_custom_place` เปิดกลับมา**
--
-- ── 🔴 และนี่คือส่วนที่ผมอยากให้อ่านมากกว่าตัวบั๊ก ────────────────────────
-- รอบที่แล้ว (`20260826083613`) ผมสรุปบทเรียนไว้เองว่า:
--   > *"`do $verify$` เทียบ **ชื่อคอลัมน์** ไม่ใช่ **จำนวน** — จำนวนผิดได้เงียบเพราะมาจากลิสต์
--   >  เดียวกับที่พิมพ์ · **ชื่อผิดต้องพิมพ์ชื่อผิดจริง ๆ**"*
--
-- **ผมทำตามนั้นเป๊ะในไฟล์ `…121226` — เทียบชื่อครบทั้ง INSERT และ UPDATE — และมันผ่าน**
-- 🎯 **เพราะ `want := array[…]` ที่ผมเขียน มาจากลิสต์ผิดใบเดียวกับที่ผม `grant`**
--
-- > **การเทียบชื่อแก้ปัญหา *หน่วยวัด* · ไม่ได้แก้ปัญหา *แหล่งที่มา***
-- > ตราบใดที่ค่าคาดหวังมาจากมือผมเอง มันจะเห็นด้วยกับผมทุกครั้ง ไม่ว่าผมผิดแค่ไหน
--
-- ── สิ่งที่จับได้จริง: **ด่านที่คนอื่นดูแล สองตัว ยิงพร้อมกัน** ─────────────
-- · `E2-AC12` ① (P4) — *"ไคลเอนต์เขียน `deleted_at` ได้ตรง ๆ"* · ถามฐานจริงผ่าน `table_exposure`
-- · ทะเบียน column grant (P4 · ลงเมื่อเช้านี้เอง) — *"column grant ไม่ตรงกับทะเบียน"*
-- 🔴 **ทั้งสองตัวไม่มีค่าคาดหวังมาจากผมเลยแม้แต่ตัวเดียว** — และนั่นคือคุณสมบัติเดียวที่ต่างจากของผม
-- 📌 **บทเรียนที่ถูก: ตัวตรวจต้องมี *เจ้าของคนละคน* กับของที่ถูกตรวจ · ไม่ใช่แค่ *หน่วยวัดที่ดีขึ้น***
-- ═══════════════════════════════════════════════════════════════════════════

begin;

do $guard$
begin
  if not exists (
    select 1 from app.project_identity
     where name = 'plan-korea-platform' and ref = 'pmvxwcimjebogjfimzqy' and environment = 'dev'
  ) then
    raise exception 'ผิดโปรเจกต์: ไม่ใช่ engine-dev';
  end if;
end $guard$;

-- ลิสต์จาก `20260825142949` **ลบ `description` ออกตัวเดียว** — ไม่มี `deleted_at` เหมือนที่นั่น
revoke update on public.custom_places from authenticated;
grant update (city_id, category, lat, lng, maps_query, google_place_id)
  on public.custom_places to authenticated;

do $verify$
begin
  -- ⚠️ **ตั้งใจตรวจแค่ข้อเดียว และเป็นข้อเชิงคุณสมบัติ** — ไม่ใช่รายชื่อทั้งลิสต์
  --    รายชื่อทั้งลิสต์จากมือผม พิสูจน์แล้วสองรอบว่ามันเห็นด้วยกับผมเสมอ
  --    **ตัวตัดสินจริงคือทะเบียนของ P4 และเคส `E2-AC12` ① ซึ่งผมไม่ได้เป็นคนเขียนค่าคาดหวัง**
  if exists (
    select 1 from information_schema.column_privileges
     where table_schema = 'public' and table_name = 'custom_places'
       and grantee = 'authenticated' and privilege_type = 'UPDATE'
       and column_name = 'deleted_at'
  ) then
    raise exception 'deleted_at ยังอยู่ใน grant update ของ custom_places';
  end if;
end $verify$;

commit;
