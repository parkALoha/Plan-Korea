-- ┌───────────────────────────────────────────────────────────────────────────┐
-- │ แม่แบบ migration ของแพลตฟอร์ม — คัดลอกไปตั้งชื่อใหม่ใน migrations/         │
-- │ ⚠️ ไฟล์นี้อยู่ "นอก" migrations/ โดยตั้งใจ จะได้ไม่ถูก CLI หยิบไปรัน        │
-- └───────────────────────────────────────────────────────────────────────────┘
--
-- rollback:
--   (เขียนคำสั่งย้อนกลับของ migration นี้ไว้ตรงนี้ตั้งแต่วันที่เขียน)
--   drop table if exists public.<ตารางที่สร้าง>;
--
-- 🔴 ห้ามปล่อยช่อง rollback ว่าง — migration ที่ไม่มีทางถอย แก้ได้ทางเดียวคือเขียน SQL สดหน้างาน

-- ── ด่านกันรันผิดโปรเจกต์ · ต้องเป็นบล็อกแรกเสมอ ก่อน DDL ทุกบรรทัด ──────────
-- ทำงานตอนคนพลาด ไม่ใช่ตอนคนอ่านเอกสารล่วงหน้า · raise exception = rollback ทั้ง transaction
--
-- 🔴 คัดลอกบล็อกนี้ไป **ทั้งก้อน ไม่ต้องแก้อะไร** — มันถามว่า "ฐานนี้ใช่ engine-dev ไหม"
--    ไม่ใช่ "ฐานนี้ใช่ฐานที่ห้ามแตะไหม" · ความต่างนี้คือ `D48` ทั้งข้อ
do $guard$
begin
  if not exists (
    select 1 from information_schema.tables
    where table_schema = 'app' and table_name = 'project_identity'
  ) then
    raise exception 'ผิดโปรเจกต์: ไม่มี app.project_identity → ฐานนี้ไม่ใช่ engine-dev ของแพลตฟอร์ม';
  end if;

  -- 🔴 `P-31`: ต้องเช็ค `ref` + `environment` ด้วย · `name` อย่างเดียวแยก dev ออกจาก prod ไม่ได้
  --    วันที่มี prod มันจะชื่อ `plan-korea-platform` เหมือนกันเป๊ะ
  --    ⚠️ **เปลี่ยน ref ตรงนี้ = เจตนาเล็งไปฐานอื่น** ต้องเป็นการตัดสินใจ ไม่ใช่การคัดลอก
  if not exists (
    select 1 from app.project_identity
     where name = 'plan-korea-platform'
       and ref  = 'pmvxwcimjebogjfimzqy'
       and environment = 'dev'
  ) then
    raise exception 'ผิดโปรเจกต์: app.project_identity มีอยู่ แต่ไม่ใช่ engine-dev (ตรวจ name+ref+environment)';
  end if;
end $guard$;

-- ── DDL ของจริงเริ่มตรงนี้ ────────────────────────────────────────────────────


-- ═══════════════════════════════════════════════════════════════════════════
-- ประวัติของบล็อกด่าน — เก็บไว้เพราะ "ทำไมถึงเปลี่ยน" มีค่ากว่า "เปลี่ยนเป็นอะไร"
-- ═══════════════════════════════════════════════════════════════════════════
-- ฉบับเดิม (ใช้ถึง `20260824220618_project_identity_guard.sql` เท่านั้น) เป็น **denylist**:
--
--   if exists (select 1 from information_schema.tables
--              where table_schema = 'public' and table_name = 'trip_meta') then
--     raise exception 'ผิดโปรเจกต์: ฐานนี้มีตาราง trip_meta = นี่คือ DB ทริปจริง';
--   end if;
--
-- มันล้มเฉพาะกับ **ฐานที่คนเขียนด่านนึกออก ณ วันที่เขียน** ซึ่งตอนนั้นมีใบเดียวคือ DB ทริปจริง
-- 🔴 24 ส.ค. 2026 พบว่า token ที่เครื่องถืออยู่มองเห็น **`a-gleam` ฐาน production ของร้าน** ด้วย
--    และ `a-gleam` **ไม่มี `trip_meta` → เดินผ่านด่านเดิมได้ทั้งดุ้น** (`D48`)
--
-- 📌 และข้อที่ต้องจำให้ได้จริง ๆ: **ทางแก้นี้ถูกเขียนไว้ในไฟล์นี้อยู่แล้วตั้งแต่ `E0` — เป็นคอมเมนต์**
--    แล้ว migration ตัวที่ 2 ก็คัดลอก*บล็อกที่ทำงานอยู่* ไปแทน ซึ่งเป็นสิ่งที่ถูกต้องที่จะทำ
--    🔴 **แผนที่ถูกทิ้งไว้เป็นคอมเมนต์ ไม่ใช่แผน** · ถ้ารู้ว่าบล็อกต้องเปลี่ยน ให้เปลี่ยนที่บล็อก

-- ═══════════════════════════════════════════════════════════════════════════
-- 🔴 ก่อนเขียน RPC ตัวใหม่: **`public.` กับ `app.` ตอบคนละคำถาม** (P5 พบ · P1 จด · 27 ส.ค. 2026)
-- ═══════════════════════════════════════════════════════════════════════════
--
-- | คำถาม | ตอบด้วยอะไร |
-- |---|---|
-- | ฟังก์ชันนี้ **รันด้วยสิทธิ์ของใคร** | `security definer` / `invoker` |
-- | ฟังก์ชันนี้ **ใครเรียกถึงได้** | **schema ที่มันอยู่** |
--
-- 🔴 **PostgREST เปิดเฉพาะ schema `public`** (`guards.sh` ข้อ `api-config` บังคับว่า `app` ห้ามถูก expose)
--    → **ฟังก์ชันใน `app.*` ไม่มีเส้นทางจากเบราว์เซอร์เลย ไม่ว่าจะ `grant execute` ให้ใครก็ตาม**
--
-- ⚠️ **กับดัก: `grant execute on function app.foo(...) to authenticated;` อ่านผ่านรีวิวได้ทุกครั้ง**
--    มี grant · ระบุ role ถูก · ไม่ `grant all` — **แต่ถ้าตั้งใจให้ผู้ใช้เรียก มันจะไม่ทำงาน**
--    🎯 **และมันจะไม่แดงตอน migration ลง · มันจะแดงตอนผู้ใช้กดปุ่มครั้งแรก**
--
-- ## กติกา
-- · **ผู้ใช้เรียกเอง** (ผ่าน route หรือ `supabase.rpc()`) → ต้องอยู่ **`public.`**
--   ตัวอย่างที่มีอยู่: `public.create_trip` · `public.set_active_plan` · `public.duplicate_trip_plan`
-- · **ของภายใน** (policy เรียก · trigger เรียก · ฟังก์ชันอื่นเรียกต่อ) → อยู่ **`app.`** และ `revoke all`
--
-- ## `app.*` ที่ grant ให้ `authenticated` ได้ — มีเงื่อนไขเดียว
-- 🔴 **มันถูกเรียกจาก *policy* ไม่ใช่จากเบราว์เซอร์** — policy รันในเซสชันของผู้ใช้ จึงต้องมีสิทธิ์เรียก
--    ที่ลงฐานแล้ววันนี้มี **2 ตัว** และทั้งคู่เข้าเงื่อนไขนี้:
--      `app.can_write_trip(uuid)`        ← RLS policy เรียก
--      `app.booking_file_trip(text)`     ← policy ของ storage.objects เรียก
--
-- 🔴 **และตัวที่ 3 จ่ออยู่แล้วใน `pending-review/`** (`20260827061500_e3_read_only_covers_storage.sql`):
--      `app.write_is_blocked()`          ← `booking_files_{insert,update,delete}` บน `storage.objects` เรียก
--    **วันที่ไฟล์นั้นถูกรัน รายการข้างบนจะกลายเป็น 3 ตัว** — เขียนไว้ตรงนี้เพราะ:
--    ⚠️ **ประโยค "วันนี้มี 2 ตัวเท่านั้น" เป็นข้อเท็จจริงที่มีวันหมดอายุ และมันไม่ประกาศวันหมดอายุของตัวเอง**
--    🎯 รูปเดียวกับที่ `PLAN.md` เคยเขียนว่า *"ยังไม่เคยรัน migration กับฐานใด ๆ"* — **จริงตอนเขียน · เท็จ 3 วันถัดมา
--       · และไม่มีอะไรในประโยคนั้นบอกว่ามันจะเก่า** (P1 · 27 ส.ค. 2026 · แก้ไปแล้วหนึ่งที่ในคืนเดียวกัน)
--
--    📌 **เพิ่มตัวถัดไปต้องเขียนกำกับว่า *policy ชื่ออะไร* เรียกมัน** ไม่ใช่แค่คำว่า "policy" — ไม่งั้นมันคือกรณีกับดักข้างบน
